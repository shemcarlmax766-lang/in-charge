(function() {
	//#region \0rolldown/runtime.js
	var __create = Object.create;
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __getProtoOf = Object.getPrototypeOf;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __esmMin = (fn, res, err) => () => {
		if (err) throw err[0];
		try {
			return fn && (res = fn(fn = 0)), res;
		} catch (e) {
			throw err = [e], e;
		}
	};
	var __commonJSMin = (cb, mod) => () => (mod || (cb((mod = { exports: {} }).exports, mod), cb = null), mod.exports);
	var __exportAll = (all, no_symbols) => {
		let target = {};
		for (var name in all) __defProp(target, name, {
			get: all[name],
			enumerable: true
		});
		if (!no_symbols) __defProp(target, Symbol.toStringTag, { value: "Module" });
		return target;
	};
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
				get: ((k) => from[k]).bind(null, key),
				enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule || !__hasOwnProp.call(mod, "default") ? __defProp(target, "default", {
		value: mod,
		enumerable: true
	}) : target, mod));
	//#endregion
	//#region ../node_modules/react/cjs/react.development.js
	/**
	* @license React
	* react.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_react_development = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		(function() {
			"use strict";
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(/* @__PURE__ */ new Error());
			var ReactVersion = "18.3.1";
			var REACT_ELEMENT_TYPE = Symbol.for("react.element");
			var REACT_PORTAL_TYPE = Symbol.for("react.portal");
			var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
			var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
			var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
			var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
			var REACT_CONTEXT_TYPE = Symbol.for("react.context");
			var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
			var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
			var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
			var REACT_MEMO_TYPE = Symbol.for("react.memo");
			var REACT_LAZY_TYPE = Symbol.for("react.lazy");
			var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
			var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
			var FAUX_ITERATOR_SYMBOL = "@@iterator";
			function getIteratorFn(maybeIterable) {
				if (maybeIterable === null || typeof maybeIterable !== "object") return null;
				var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
				if (typeof maybeIterator === "function") return maybeIterator;
				return null;
			}
			/**
			* Keeps track of the current dispatcher.
			*/
			var ReactCurrentDispatcher = { 
			/**
			* @internal
			* @type {ReactComponent}
			*/
current: null };
			/**
			* Keeps track of the current batch's configuration such as how long an update
			* should suspend for if it needs to.
			*/
			var ReactCurrentBatchConfig = { transition: null };
			var ReactCurrentActQueue = {
				current: null,
				isBatchingLegacy: false,
				didScheduleLegacyUpdate: false
			};
			/**
			* Keeps track of the current owner.
			*
			* The current owner is the component who should own any components that are
			* currently being constructed.
			*/
			var ReactCurrentOwner = { 
			/**
			* @internal
			* @type {ReactComponent}
			*/
current: null };
			var ReactDebugCurrentFrame = {};
			var currentExtraStackFrame = null;
			function setExtraStackFrame(stack) {
				currentExtraStackFrame = stack;
			}
			ReactDebugCurrentFrame.setExtraStackFrame = function(stack) {
				currentExtraStackFrame = stack;
			};
			ReactDebugCurrentFrame.getCurrentStack = null;
			ReactDebugCurrentFrame.getStackAddendum = function() {
				var stack = "";
				if (currentExtraStackFrame) stack += currentExtraStackFrame;
				var impl = ReactDebugCurrentFrame.getCurrentStack;
				if (impl) stack += impl() || "";
				return stack;
			};
			var enableScopeAPI = false;
			var enableCacheElement = false;
			var enableTransitionTracing = false;
			var enableLegacyHidden = false;
			var enableDebugTracing = false;
			var ReactSharedInternals = {
				ReactCurrentDispatcher,
				ReactCurrentBatchConfig,
				ReactCurrentOwner
			};
			ReactSharedInternals.ReactDebugCurrentFrame = ReactDebugCurrentFrame;
			ReactSharedInternals.ReactCurrentActQueue = ReactCurrentActQueue;
			function warn(format) {
				for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
				printWarning("warn", format, args);
			}
			function error(format) {
				for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) args[_key2 - 1] = arguments[_key2];
				printWarning("error", format, args);
			}
			function printWarning(level, format, args) {
				var stack = ReactSharedInternals.ReactDebugCurrentFrame.getStackAddendum();
				if (stack !== "") {
					format += "%s";
					args = args.concat([stack]);
				}
				var argsWithFormat = args.map(function(item) {
					return String(item);
				});
				argsWithFormat.unshift("Warning: " + format);
				Function.prototype.apply.call(console[level], console, argsWithFormat);
			}
			var didWarnStateUpdateForUnmountedComponent = {};
			function warnNoop(publicInstance, callerName) {
				var _constructor = publicInstance.constructor;
				var componentName = _constructor && (_constructor.displayName || _constructor.name) || "ReactClass";
				var warningKey = componentName + "." + callerName;
				if (didWarnStateUpdateForUnmountedComponent[warningKey]) return;
				error("Can't call %s on a component that is not yet mounted. This is a no-op, but it might indicate a bug in your application. Instead, assign to `this.state` directly or define a `state = {};` class property with the desired state in the %s component.", callerName, componentName);
				didWarnStateUpdateForUnmountedComponent[warningKey] = true;
			}
			/**
			* This is the abstract API for an update queue.
			*/
			var ReactNoopUpdateQueue = {
				/**
				* Checks whether or not this composite component is mounted.
				* @param {ReactClass} publicInstance The instance we want to test.
				* @return {boolean} True if mounted, false otherwise.
				* @protected
				* @final
				*/
				isMounted: function(publicInstance) {
					return false;
				},
				/**
				* Forces an update. This should only be invoked when it is known with
				* certainty that we are **not** in a DOM transaction.
				*
				* You may want to call this when you know that some deeper aspect of the
				* component's state has changed but `setState` was not called.
				*
				* This will not invoke `shouldComponentUpdate`, but it will invoke
				* `componentWillUpdate` and `componentDidUpdate`.
				*
				* @param {ReactClass} publicInstance The instance that should rerender.
				* @param {?function} callback Called after component is updated.
				* @param {?string} callerName name of the calling function in the public API.
				* @internal
				*/
				enqueueForceUpdate: function(publicInstance, callback, callerName) {
					warnNoop(publicInstance, "forceUpdate");
				},
				/**
				* Replaces all of the state. Always use this or `setState` to mutate state.
				* You should treat `this.state` as immutable.
				*
				* There is no guarantee that `this.state` will be immediately updated, so
				* accessing `this.state` after calling this method may return the old value.
				*
				* @param {ReactClass} publicInstance The instance that should rerender.
				* @param {object} completeState Next state.
				* @param {?function} callback Called after component is updated.
				* @param {?string} callerName name of the calling function in the public API.
				* @internal
				*/
				enqueueReplaceState: function(publicInstance, completeState, callback, callerName) {
					warnNoop(publicInstance, "replaceState");
				},
				/**
				* Sets a subset of the state. This only exists because _pendingState is
				* internal. This provides a merging strategy that is not available to deep
				* properties which is confusing. TODO: Expose pendingState or don't use it
				* during the merge.
				*
				* @param {ReactClass} publicInstance The instance that should rerender.
				* @param {object} partialState Next partial state to be merged with state.
				* @param {?function} callback Called after component is updated.
				* @param {?string} Name of the calling function in the public API.
				* @internal
				*/
				enqueueSetState: function(publicInstance, partialState, callback, callerName) {
					warnNoop(publicInstance, "setState");
				}
			};
			var assign = Object.assign;
			var emptyObject = {};
			Object.freeze(emptyObject);
			/**
			* Base class helpers for the updating state of a component.
			*/
			function Component(props, context, updater) {
				this.props = props;
				this.context = context;
				this.refs = emptyObject;
				this.updater = updater || ReactNoopUpdateQueue;
			}
			Component.prototype.isReactComponent = {};
			/**
			* Sets a subset of the state. Always use this to mutate
			* state. You should treat `this.state` as immutable.
			*
			* There is no guarantee that `this.state` will be immediately updated, so
			* accessing `this.state` after calling this method may return the old value.
			*
			* There is no guarantee that calls to `setState` will run synchronously,
			* as they may eventually be batched together.  You can provide an optional
			* callback that will be executed when the call to setState is actually
			* completed.
			*
			* When a function is provided to setState, it will be called at some point in
			* the future (not synchronously). It will be called with the up to date
			* component arguments (state, props, context). These values can be different
			* from this.* because your function may be called after receiveProps but before
			* shouldComponentUpdate, and this new state, props, and context will not yet be
			* assigned to this.
			*
			* @param {object|function} partialState Next partial state or function to
			*        produce next partial state to be merged with current state.
			* @param {?function} callback Called after state is updated.
			* @final
			* @protected
			*/
			Component.prototype.setState = function(partialState, callback) {
				if (typeof partialState !== "object" && typeof partialState !== "function" && partialState != null) throw new Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");
				this.updater.enqueueSetState(this, partialState, callback, "setState");
			};
			/**
			* Forces an update. This should only be invoked when it is known with
			* certainty that we are **not** in a DOM transaction.
			*
			* You may want to call this when you know that some deeper aspect of the
			* component's state has changed but `setState` was not called.
			*
			* This will not invoke `shouldComponentUpdate`, but it will invoke
			* `componentWillUpdate` and `componentDidUpdate`.
			*
			* @param {?function} callback Called after update is complete.
			* @final
			* @protected
			*/
			Component.prototype.forceUpdate = function(callback) {
				this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
			};
			var deprecatedAPIs = {
				isMounted: ["isMounted", "Instead, make sure to clean up subscriptions and pending requests in componentWillUnmount to prevent memory leaks."],
				replaceState: ["replaceState", "Refactor your code to use setState instead (see https://github.com/facebook/react/issues/3236)."]
			};
			var defineDeprecationWarning = function(methodName, info) {
				Object.defineProperty(Component.prototype, methodName, { get: function() {
					warn("%s(...) is deprecated in plain JavaScript React classes. %s", info[0], info[1]);
				} });
			};
			for (var fnName in deprecatedAPIs) if (deprecatedAPIs.hasOwnProperty(fnName)) defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
			function ComponentDummy() {}
			ComponentDummy.prototype = Component.prototype;
			/**
			* Convenience component with default shallow equality check for sCU.
			*/
			function PureComponent(props, context, updater) {
				this.props = props;
				this.context = context;
				this.refs = emptyObject;
				this.updater = updater || ReactNoopUpdateQueue;
			}
			var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
			pureComponentPrototype.constructor = PureComponent;
			assign(pureComponentPrototype, Component.prototype);
			pureComponentPrototype.isPureReactComponent = true;
			function createRef() {
				var refObject = { current: null };
				Object.seal(refObject);
				return refObject;
			}
			var isArrayImpl = Array.isArray;
			function isArray(a) {
				return isArrayImpl(a);
			}
			function typeName(value) {
				return typeof Symbol === "function" && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
			}
			function willCoercionThrow(value) {
				try {
					testStringCoercion(value);
					return false;
				} catch (e) {
					return true;
				}
			}
			function testStringCoercion(value) {
				return "" + value;
			}
			function checkKeyStringCoercion(value) {
				if (willCoercionThrow(value)) {
					error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
					return testStringCoercion(value);
				}
			}
			function getWrappedName(outerType, innerType, wrapperName) {
				var displayName = outerType.displayName;
				if (displayName) return displayName;
				var functionName = innerType.displayName || innerType.name || "";
				return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
			}
			function getContextName(type) {
				return type.displayName || "Context";
			}
			function getComponentNameFromType(type) {
				if (type == null) return null;
				if (typeof type.tag === "number") error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
				if (typeof type === "function") return type.displayName || type.name || null;
				if (typeof type === "string") return type;
				switch (type) {
					case REACT_FRAGMENT_TYPE: return "Fragment";
					case REACT_PORTAL_TYPE: return "Portal";
					case REACT_PROFILER_TYPE: return "Profiler";
					case REACT_STRICT_MODE_TYPE: return "StrictMode";
					case REACT_SUSPENSE_TYPE: return "Suspense";
					case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_CONTEXT_TYPE: return getContextName(type) + ".Consumer";
					case REACT_PROVIDER_TYPE: return getContextName(type._context) + ".Provider";
					case REACT_FORWARD_REF_TYPE: return getWrappedName(type, type.render, "ForwardRef");
					case REACT_MEMO_TYPE:
						var outerName = type.displayName || null;
						if (outerName !== null) return outerName;
						return getComponentNameFromType(type.type) || "Memo";
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return getComponentNameFromType(init(payload));
						} catch (x) {
							return null;
						}
				}
				return null;
			}
			var hasOwnProperty = Object.prototype.hasOwnProperty;
			var RESERVED_PROPS = {
				key: true,
				ref: true,
				__self: true,
				__source: true
			};
			var specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs = {};
			function hasValidRef(config) {
				if (hasOwnProperty.call(config, "ref")) {
					var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
					if (getter && getter.isReactWarning) return false;
				}
				return config.ref !== void 0;
			}
			function hasValidKey(config) {
				if (hasOwnProperty.call(config, "key")) {
					var getter = Object.getOwnPropertyDescriptor(config, "key").get;
					if (getter && getter.isReactWarning) return false;
				}
				return config.key !== void 0;
			}
			function defineKeyPropWarningGetter(props, displayName) {
				var warnAboutAccessingKey = function() {
					if (!specialPropKeyWarningShown) {
						specialPropKeyWarningShown = true;
						error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
					}
				};
				warnAboutAccessingKey.isReactWarning = true;
				Object.defineProperty(props, "key", {
					get: warnAboutAccessingKey,
					configurable: true
				});
			}
			function defineRefPropWarningGetter(props, displayName) {
				var warnAboutAccessingRef = function() {
					if (!specialPropRefWarningShown) {
						specialPropRefWarningShown = true;
						error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
					}
				};
				warnAboutAccessingRef.isReactWarning = true;
				Object.defineProperty(props, "ref", {
					get: warnAboutAccessingRef,
					configurable: true
				});
			}
			function warnIfStringRefCannotBeAutoConverted(config) {
				if (typeof config.ref === "string" && ReactCurrentOwner.current && config.__self && ReactCurrentOwner.current.stateNode !== config.__self) {
					var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
					if (!didWarnAboutStringRefs[componentName]) {
						error("Component \"%s\" contains the string ref \"%s\". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref", componentName, config.ref);
						didWarnAboutStringRefs[componentName] = true;
					}
				}
			}
			/**
			* Factory method to create a new React element. This no longer adheres to
			* the class pattern, so do not use new to call it. Also, instanceof check
			* will not work. Instead test $$typeof field against Symbol.for('react.element') to check
			* if something is a React Element.
			*
			* @param {*} type
			* @param {*} props
			* @param {*} key
			* @param {string|object} ref
			* @param {*} owner
			* @param {*} self A *temporary* helper to detect places where `this` is
			* different from the `owner` when React.createElement is called, so that we
			* can warn. We want to get rid of owner and replace string `ref`s with arrow
			* functions, and as long as `this` and owner are the same, there will be no
			* change in behavior.
			* @param {*} source An annotation object (added by a transpiler or otherwise)
			* indicating filename, line number, and/or other information.
			* @internal
			*/
			var ReactElement = function(type, key, ref, self, source, owner, props) {
				var element = {
					$$typeof: REACT_ELEMENT_TYPE,
					type,
					key,
					ref,
					props,
					_owner: owner
				};
				element._store = {};
				Object.defineProperty(element._store, "validated", {
					configurable: false,
					enumerable: false,
					writable: true,
					value: false
				});
				Object.defineProperty(element, "_self", {
					configurable: false,
					enumerable: false,
					writable: false,
					value: self
				});
				Object.defineProperty(element, "_source", {
					configurable: false,
					enumerable: false,
					writable: false,
					value: source
				});
				if (Object.freeze) {
					Object.freeze(element.props);
					Object.freeze(element);
				}
				return element;
			};
			/**
			* Create and return a new ReactElement of the given type.
			* See https://reactjs.org/docs/react-api.html#createelement
			*/
			function createElement(type, config, children) {
				var propName;
				var props = {};
				var key = null;
				var ref = null;
				var self = null;
				var source = null;
				if (config != null) {
					if (hasValidRef(config)) {
						ref = config.ref;
						warnIfStringRefCannotBeAutoConverted(config);
					}
					if (hasValidKey(config)) {
						checkKeyStringCoercion(config.key);
						key = "" + config.key;
					}
					self = config.__self === void 0 ? null : config.__self;
					source = config.__source === void 0 ? null : config.__source;
					for (propName in config) if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) props[propName] = config[propName];
				}
				var childrenLength = arguments.length - 2;
				if (childrenLength === 1) props.children = children;
				else if (childrenLength > 1) {
					var childArray = Array(childrenLength);
					for (var i = 0; i < childrenLength; i++) childArray[i] = arguments[i + 2];
					if (Object.freeze) Object.freeze(childArray);
					props.children = childArray;
				}
				if (type && type.defaultProps) {
					var defaultProps = type.defaultProps;
					for (propName in defaultProps) if (props[propName] === void 0) props[propName] = defaultProps[propName];
				}
				if (key || ref) {
					var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
					if (key) defineKeyPropWarningGetter(props, displayName);
					if (ref) defineRefPropWarningGetter(props, displayName);
				}
				return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
			}
			function cloneAndReplaceKey(oldElement, newKey) {
				return ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);
			}
			/**
			* Clone and return a new ReactElement using element as the starting point.
			* See https://reactjs.org/docs/react-api.html#cloneelement
			*/
			function cloneElement(element, config, children) {
				if (element === null || element === void 0) throw new Error("React.cloneElement(...): The argument must be a React element, but you passed " + element + ".");
				var propName;
				var props = assign({}, element.props);
				var key = element.key;
				var ref = element.ref;
				var self = element._self;
				var source = element._source;
				var owner = element._owner;
				if (config != null) {
					if (hasValidRef(config)) {
						ref = config.ref;
						owner = ReactCurrentOwner.current;
					}
					if (hasValidKey(config)) {
						checkKeyStringCoercion(config.key);
						key = "" + config.key;
					}
					var defaultProps;
					if (element.type && element.type.defaultProps) defaultProps = element.type.defaultProps;
					for (propName in config) if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
						if (config[propName] === void 0 && defaultProps !== void 0) props[propName] = defaultProps[propName];
						else props[propName] = config[propName];
					}
				}
				var childrenLength = arguments.length - 2;
				if (childrenLength === 1) props.children = children;
				else if (childrenLength > 1) {
					var childArray = Array(childrenLength);
					for (var i = 0; i < childrenLength; i++) childArray[i] = arguments[i + 2];
					props.children = childArray;
				}
				return ReactElement(element.type, key, ref, self, source, owner, props);
			}
			/**
			* Verifies the object is a ReactElement.
			* See https://reactjs.org/docs/react-api.html#isvalidelement
			* @param {?object} object
			* @return {boolean} True if `object` is a ReactElement.
			* @final
			*/
			function isValidElement(object) {
				return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
			}
			var SEPARATOR = ".";
			var SUBSEPARATOR = ":";
			/**
			* Escape and wrap key so it is safe to use as a reactid
			*
			* @param {string} key to be escaped.
			* @return {string} the escaped key.
			*/
			function escape(key) {
				var escapeRegex = /[=:]/g;
				var escaperLookup = {
					"=": "=0",
					":": "=2"
				};
				return "$" + key.replace(escapeRegex, function(match) {
					return escaperLookup[match];
				});
			}
			/**
			* TODO: Test that a single child and an array with one item have the same key
			* pattern.
			*/
			var didWarnAboutMaps = false;
			var userProvidedKeyEscapeRegex = /\/+/g;
			function escapeUserProvidedKey(text) {
				return text.replace(userProvidedKeyEscapeRegex, "$&/");
			}
			/**
			* Generate a key string that identifies a element within a set.
			*
			* @param {*} element A element that could contain a manual key.
			* @param {number} index Index that is used if a manual key is not provided.
			* @return {string}
			*/
			function getElementKey(element, index) {
				if (typeof element === "object" && element !== null && element.key != null) {
					checkKeyStringCoercion(element.key);
					return escape("" + element.key);
				}
				return index.toString(36);
			}
			function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
				var type = typeof children;
				if (type === "undefined" || type === "boolean") children = null;
				var invokeCallback = false;
				if (children === null) invokeCallback = true;
				else switch (type) {
					case "string":
					case "number":
						invokeCallback = true;
						break;
					case "object": switch (children.$$typeof) {
						case REACT_ELEMENT_TYPE:
						case REACT_PORTAL_TYPE: invokeCallback = true;
					}
				}
				if (invokeCallback) {
					var _child = children;
					var mappedChild = callback(_child);
					var childKey = nameSoFar === "" ? SEPARATOR + getElementKey(_child, 0) : nameSoFar;
					if (isArray(mappedChild)) {
						var escapedChildKey = "";
						if (childKey != null) escapedChildKey = escapeUserProvidedKey(childKey) + "/";
						mapIntoArray(mappedChild, array, escapedChildKey, "", function(c) {
							return c;
						});
					} else if (mappedChild != null) {
						if (isValidElement(mappedChild)) {
							if (mappedChild.key && (!_child || _child.key !== mappedChild.key)) checkKeyStringCoercion(mappedChild.key);
							mappedChild = cloneAndReplaceKey(mappedChild, escapedPrefix + (mappedChild.key && (!_child || _child.key !== mappedChild.key) ? escapeUserProvidedKey("" + mappedChild.key) + "/" : "") + childKey);
						}
						array.push(mappedChild);
					}
					return 1;
				}
				var child;
				var nextName;
				var subtreeCount = 0;
				var nextNamePrefix = nameSoFar === "" ? SEPARATOR : nameSoFar + SUBSEPARATOR;
				if (isArray(children)) for (var i = 0; i < children.length; i++) {
					child = children[i];
					nextName = nextNamePrefix + getElementKey(child, i);
					subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
				}
				else {
					var iteratorFn = getIteratorFn(children);
					if (typeof iteratorFn === "function") {
						var iterableChildren = children;
						if (iteratorFn === iterableChildren.entries) {
							if (!didWarnAboutMaps) warn("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
							didWarnAboutMaps = true;
						}
						var iterator = iteratorFn.call(iterableChildren);
						var step;
						var ii = 0;
						while (!(step = iterator.next()).done) {
							child = step.value;
							nextName = nextNamePrefix + getElementKey(child, ii++);
							subtreeCount += mapIntoArray(child, array, escapedPrefix, nextName, callback);
						}
					} else if (type === "object") {
						var childrenString = String(children);
						throw new Error("Objects are not valid as a React child (found: " + (childrenString === "[object Object]" ? "object with keys {" + Object.keys(children).join(", ") + "}" : childrenString) + "). If you meant to render a collection of children, use an array instead.");
					}
				}
				return subtreeCount;
			}
			/**
			* Maps children that are typically specified as `props.children`.
			*
			* See https://reactjs.org/docs/react-api.html#reactchildrenmap
			*
			* The provided mapFunction(child, index) will be called for each
			* leaf child.
			*
			* @param {?*} children Children tree container.
			* @param {function(*, int)} func The map function.
			* @param {*} context Context for mapFunction.
			* @return {object} Object containing the ordered map of results.
			*/
			function mapChildren(children, func, context) {
				if (children == null) return children;
				var result = [];
				var count = 0;
				mapIntoArray(children, result, "", "", function(child) {
					return func.call(context, child, count++);
				});
				return result;
			}
			/**
			* Count the number of children that are typically specified as
			* `props.children`.
			*
			* See https://reactjs.org/docs/react-api.html#reactchildrencount
			*
			* @param {?*} children Children tree container.
			* @return {number} The number of children.
			*/
			function countChildren(children) {
				var n = 0;
				mapChildren(children, function() {
					n++;
				});
				return n;
			}
			/**
			* Iterates through children that are typically specified as `props.children`.
			*
			* See https://reactjs.org/docs/react-api.html#reactchildrenforeach
			*
			* The provided forEachFunc(child, index) will be called for each
			* leaf child.
			*
			* @param {?*} children Children tree container.
			* @param {function(*, int)} forEachFunc
			* @param {*} forEachContext Context for forEachContext.
			*/
			function forEachChildren(children, forEachFunc, forEachContext) {
				mapChildren(children, function() {
					forEachFunc.apply(this, arguments);
				}, forEachContext);
			}
			/**
			* Flatten a children object (typically specified as `props.children`) and
			* return an array with appropriately re-keyed children.
			*
			* See https://reactjs.org/docs/react-api.html#reactchildrentoarray
			*/
			function toArray(children) {
				return mapChildren(children, function(child) {
					return child;
				}) || [];
			}
			/**
			* Returns the first child in a collection of children and verifies that there
			* is only one child in the collection.
			*
			* See https://reactjs.org/docs/react-api.html#reactchildrenonly
			*
			* The current implementation of this function assumes that a single child gets
			* passed without a wrapper, but the purpose of this helper function is to
			* abstract away the particular structure of children.
			*
			* @param {?object} children Child collection structure.
			* @return {ReactElement} The first and only `ReactElement` contained in the
			* structure.
			*/
			function onlyChild(children) {
				if (!isValidElement(children)) throw new Error("React.Children.only expected to receive a single React element child.");
				return children;
			}
			function createContext(defaultValue) {
				var context = {
					$$typeof: REACT_CONTEXT_TYPE,
					_currentValue: defaultValue,
					_currentValue2: defaultValue,
					_threadCount: 0,
					Provider: null,
					Consumer: null,
					_defaultValue: null,
					_globalName: null
				};
				context.Provider = {
					$$typeof: REACT_PROVIDER_TYPE,
					_context: context
				};
				var hasWarnedAboutUsingNestedContextConsumers = false;
				var hasWarnedAboutUsingConsumerProvider = false;
				var hasWarnedAboutDisplayNameOnConsumer = false;
				var Consumer = {
					$$typeof: REACT_CONTEXT_TYPE,
					_context: context
				};
				Object.defineProperties(Consumer, {
					Provider: {
						get: function() {
							if (!hasWarnedAboutUsingConsumerProvider) {
								hasWarnedAboutUsingConsumerProvider = true;
								error("Rendering <Context.Consumer.Provider> is not supported and will be removed in a future major release. Did you mean to render <Context.Provider> instead?");
							}
							return context.Provider;
						},
						set: function(_Provider) {
							context.Provider = _Provider;
						}
					},
					_currentValue: {
						get: function() {
							return context._currentValue;
						},
						set: function(_currentValue) {
							context._currentValue = _currentValue;
						}
					},
					_currentValue2: {
						get: function() {
							return context._currentValue2;
						},
						set: function(_currentValue2) {
							context._currentValue2 = _currentValue2;
						}
					},
					_threadCount: {
						get: function() {
							return context._threadCount;
						},
						set: function(_threadCount) {
							context._threadCount = _threadCount;
						}
					},
					Consumer: { get: function() {
						if (!hasWarnedAboutUsingNestedContextConsumers) {
							hasWarnedAboutUsingNestedContextConsumers = true;
							error("Rendering <Context.Consumer.Consumer> is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
						}
						return context.Consumer;
					} },
					displayName: {
						get: function() {
							return context.displayName;
						},
						set: function(displayName) {
							if (!hasWarnedAboutDisplayNameOnConsumer) {
								warn("Setting `displayName` on Context.Consumer has no effect. You should set it directly on the context with Context.displayName = '%s'.", displayName);
								hasWarnedAboutDisplayNameOnConsumer = true;
							}
						}
					}
				});
				context.Consumer = Consumer;
				context._currentRenderer = null;
				context._currentRenderer2 = null;
				return context;
			}
			var Uninitialized = -1;
			var Pending = 0;
			var Resolved = 1;
			var Rejected = 2;
			function lazyInitializer(payload) {
				if (payload._status === Uninitialized) {
					var ctor = payload._result;
					var thenable = ctor();
					thenable.then(function(moduleObject) {
						if (payload._status === Pending || payload._status === Uninitialized) {
							var resolved = payload;
							resolved._status = Resolved;
							resolved._result = moduleObject;
						}
					}, function(error) {
						if (payload._status === Pending || payload._status === Uninitialized) {
							var rejected = payload;
							rejected._status = Rejected;
							rejected._result = error;
						}
					});
					if (payload._status === Uninitialized) {
						var pending = payload;
						pending._status = Pending;
						pending._result = thenable;
					}
				}
				if (payload._status === Resolved) {
					var moduleObject = payload._result;
					if (moduleObject === void 0) error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))\n\nDid you accidentally put curly braces around the import?", moduleObject);
					if (!("default" in moduleObject)) error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))", moduleObject);
					return moduleObject.default;
				} else throw payload._result;
			}
			function lazy(ctor) {
				var lazyType = {
					$$typeof: REACT_LAZY_TYPE,
					_payload: {
						_status: Uninitialized,
						_result: ctor
					},
					_init: lazyInitializer
				};
				var defaultProps;
				var propTypes;
				Object.defineProperties(lazyType, {
					defaultProps: {
						configurable: true,
						get: function() {
							return defaultProps;
						},
						set: function(newDefaultProps) {
							error("React.lazy(...): It is not supported to assign `defaultProps` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
							defaultProps = newDefaultProps;
							Object.defineProperty(lazyType, "defaultProps", { enumerable: true });
						}
					},
					propTypes: {
						configurable: true,
						get: function() {
							return propTypes;
						},
						set: function(newPropTypes) {
							error("React.lazy(...): It is not supported to assign `propTypes` to a lazy component import. Either specify them where the component is defined, or create a wrapping component around it.");
							propTypes = newPropTypes;
							Object.defineProperty(lazyType, "propTypes", { enumerable: true });
						}
					}
				});
				return lazyType;
			}
			function forwardRef(render) {
				if (render != null && render.$$typeof === REACT_MEMO_TYPE) error("forwardRef requires a render function but received a `memo` component. Instead of forwardRef(memo(...)), use memo(forwardRef(...)).");
				else if (typeof render !== "function") error("forwardRef requires a render function but was given %s.", render === null ? "null" : typeof render);
				else if (render.length !== 0 && render.length !== 2) error("forwardRef render functions accept exactly two parameters: props and ref. %s", render.length === 1 ? "Did you forget to use the ref parameter?" : "Any additional parameter will be undefined.");
				if (render != null) {
					if (render.defaultProps != null || render.propTypes != null) error("forwardRef render functions do not support propTypes or defaultProps. Did you accidentally pass a React component?");
				}
				var elementType = {
					$$typeof: REACT_FORWARD_REF_TYPE,
					render
				};
				var ownName;
				Object.defineProperty(elementType, "displayName", {
					enumerable: false,
					configurable: true,
					get: function() {
						return ownName;
					},
					set: function(name) {
						ownName = name;
						if (!render.name && !render.displayName) render.displayName = name;
					}
				});
				return elementType;
			}
			var REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
			function isValidElementType(type) {
				if (typeof type === "string" || typeof type === "function") return true;
				if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) return true;
				if (typeof type === "object" && type !== null) {
					if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) return true;
				}
				return false;
			}
			function memo(type, compare) {
				if (!isValidElementType(type)) error("memo: The first argument must be a component. Instead received: %s", type === null ? "null" : typeof type);
				var elementType = {
					$$typeof: REACT_MEMO_TYPE,
					type,
					compare: compare === void 0 ? null : compare
				};
				var ownName;
				Object.defineProperty(elementType, "displayName", {
					enumerable: false,
					configurable: true,
					get: function() {
						return ownName;
					},
					set: function(name) {
						ownName = name;
						if (!type.name && !type.displayName) type.displayName = name;
					}
				});
				return elementType;
			}
			function resolveDispatcher() {
				var dispatcher = ReactCurrentDispatcher.current;
				if (dispatcher === null) error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
				return dispatcher;
			}
			function useContext(Context) {
				var dispatcher = resolveDispatcher();
				if (Context._context !== void 0) {
					var realContext = Context._context;
					if (realContext.Consumer === Context) error("Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be removed in a future major release. Did you mean to call useContext(Context) instead?");
					else if (realContext.Provider === Context) error("Calling useContext(Context.Provider) is not supported. Did you mean to call useContext(Context) instead?");
				}
				return dispatcher.useContext(Context);
			}
			function useState(initialState) {
				return resolveDispatcher().useState(initialState);
			}
			function useReducer(reducer, initialArg, init) {
				return resolveDispatcher().useReducer(reducer, initialArg, init);
			}
			function useRef(initialValue) {
				return resolveDispatcher().useRef(initialValue);
			}
			function useEffect(create, deps) {
				return resolveDispatcher().useEffect(create, deps);
			}
			function useInsertionEffect(create, deps) {
				return resolveDispatcher().useInsertionEffect(create, deps);
			}
			function useLayoutEffect(create, deps) {
				return resolveDispatcher().useLayoutEffect(create, deps);
			}
			function useCallback(callback, deps) {
				return resolveDispatcher().useCallback(callback, deps);
			}
			function useMemo(create, deps) {
				return resolveDispatcher().useMemo(create, deps);
			}
			function useImperativeHandle(ref, create, deps) {
				return resolveDispatcher().useImperativeHandle(ref, create, deps);
			}
			function useDebugValue(value, formatterFn) {
				return resolveDispatcher().useDebugValue(value, formatterFn);
			}
			function useTransition() {
				return resolveDispatcher().useTransition();
			}
			function useDeferredValue(value) {
				return resolveDispatcher().useDeferredValue(value);
			}
			function useId() {
				return resolveDispatcher().useId();
			}
			function useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
				return resolveDispatcher().useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			}
			var disabledDepth = 0;
			var prevLog;
			var prevInfo;
			var prevWarn;
			var prevError;
			var prevGroup;
			var prevGroupCollapsed;
			var prevGroupEnd;
			function disabledLog() {}
			disabledLog.__reactDisabledLog = true;
			function disableLogs() {
				if (disabledDepth === 0) {
					prevLog = console.log;
					prevInfo = console.info;
					prevWarn = console.warn;
					prevError = console.error;
					prevGroup = console.group;
					prevGroupCollapsed = console.groupCollapsed;
					prevGroupEnd = console.groupEnd;
					var props = {
						configurable: true,
						enumerable: true,
						value: disabledLog,
						writable: true
					};
					Object.defineProperties(console, {
						info: props,
						log: props,
						warn: props,
						error: props,
						group: props,
						groupCollapsed: props,
						groupEnd: props
					});
				}
				disabledDepth++;
			}
			function reenableLogs() {
				disabledDepth--;
				if (disabledDepth === 0) {
					var props = {
						configurable: true,
						enumerable: true,
						writable: true
					};
					Object.defineProperties(console, {
						log: assign({}, props, { value: prevLog }),
						info: assign({}, props, { value: prevInfo }),
						warn: assign({}, props, { value: prevWarn }),
						error: assign({}, props, { value: prevError }),
						group: assign({}, props, { value: prevGroup }),
						groupCollapsed: assign({}, props, { value: prevGroupCollapsed }),
						groupEnd: assign({}, props, { value: prevGroupEnd })
					});
				}
				if (disabledDepth < 0) error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
			}
			var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher;
			var prefix;
			function describeBuiltInComponentFrame(name, source, ownerFn) {
				if (prefix === void 0) try {
					throw Error();
				} catch (x) {
					var match = x.stack.trim().match(/\n( *(at )?)/);
					prefix = match && match[1] || "";
				}
				return "\n" + prefix + name;
			}
			var reentry = false;
			var componentFrameCache = new (typeof WeakMap === "function" ? WeakMap : Map)();
			function describeNativeComponentFrame(fn, construct) {
				if (!fn || reentry) return "";
				var frame = componentFrameCache.get(fn);
				if (frame !== void 0) return frame;
				var control;
				reentry = true;
				var previousPrepareStackTrace = Error.prepareStackTrace;
				Error.prepareStackTrace = void 0;
				var previousDispatcher = ReactCurrentDispatcher$1.current;
				ReactCurrentDispatcher$1.current = null;
				disableLogs();
				try {
					if (construct) {
						var Fake = function() {
							throw Error();
						};
						Object.defineProperty(Fake.prototype, "props", { set: function() {
							throw Error();
						} });
						if (typeof Reflect === "object" && Reflect.construct) {
							try {
								Reflect.construct(Fake, []);
							} catch (x) {
								control = x;
							}
							Reflect.construct(fn, [], Fake);
						} else {
							try {
								Fake.call();
							} catch (x) {
								control = x;
							}
							fn.call(Fake.prototype);
						}
					} else {
						try {
							throw Error();
						} catch (x) {
							control = x;
						}
						fn();
					}
				} catch (sample) {
					if (sample && control && typeof sample.stack === "string") {
						var sampleLines = sample.stack.split("\n");
						var controlLines = control.stack.split("\n");
						var s = sampleLines.length - 1;
						var c = controlLines.length - 1;
						while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) c--;
						for (; s >= 1 && c >= 0; s--, c--) if (sampleLines[s] !== controlLines[c]) {
							if (s !== 1 || c !== 1) do {
								s--;
								c--;
								if (c < 0 || sampleLines[s] !== controlLines[c]) {
									var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
									if (fn.displayName && _frame.includes("<anonymous>")) _frame = _frame.replace("<anonymous>", fn.displayName);
									if (typeof fn === "function") componentFrameCache.set(fn, _frame);
									return _frame;
								}
							} while (s >= 1 && c >= 0);
							break;
						}
					}
				} finally {
					reentry = false;
					ReactCurrentDispatcher$1.current = previousDispatcher;
					reenableLogs();
					Error.prepareStackTrace = previousPrepareStackTrace;
				}
				var name = fn ? fn.displayName || fn.name : "";
				var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
				if (typeof fn === "function") componentFrameCache.set(fn, syntheticFrame);
				return syntheticFrame;
			}
			function describeFunctionComponentFrame(fn, source, ownerFn) {
				return describeNativeComponentFrame(fn, false);
			}
			function shouldConstruct(Component) {
				var prototype = Component.prototype;
				return !!(prototype && prototype.isReactComponent);
			}
			function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
				if (type == null) return "";
				if (typeof type === "function") return describeNativeComponentFrame(type, shouldConstruct(type));
				if (typeof type === "string") return describeBuiltInComponentFrame(type);
				switch (type) {
					case REACT_SUSPENSE_TYPE: return describeBuiltInComponentFrame("Suspense");
					case REACT_SUSPENSE_LIST_TYPE: return describeBuiltInComponentFrame("SuspenseList");
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_FORWARD_REF_TYPE: return describeFunctionComponentFrame(type.render);
					case REACT_MEMO_TYPE: return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
						} catch (x) {}
				}
				return "";
			}
			var loggedTypeFailures = {};
			var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
			function setCurrentlyValidatingElement(element) {
				if (element) {
					var owner = element._owner;
					var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
					ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
				} else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
			}
			function checkPropTypes(typeSpecs, values, location, componentName, element) {
				var has = Function.call.bind(hasOwnProperty);
				for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
					var error$1 = void 0;
					try {
						if (typeof typeSpecs[typeSpecName] !== "function") {
							var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
							err.name = "Invariant Violation";
							throw err;
						}
						error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
					} catch (ex) {
						error$1 = ex;
					}
					if (error$1 && !(error$1 instanceof Error)) {
						setCurrentlyValidatingElement(element);
						error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
						setCurrentlyValidatingElement(null);
					}
					if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
						loggedTypeFailures[error$1.message] = true;
						setCurrentlyValidatingElement(element);
						error("Failed %s type: %s", location, error$1.message);
						setCurrentlyValidatingElement(null);
					}
				}
			}
			function setCurrentlyValidatingElement$1(element) {
				if (element) {
					var owner = element._owner;
					setExtraStackFrame(describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null));
				} else setExtraStackFrame(null);
			}
			var propTypesMisspellWarningShown = false;
			function getDeclarationErrorAddendum() {
				if (ReactCurrentOwner.current) {
					var name = getComponentNameFromType(ReactCurrentOwner.current.type);
					if (name) return "\n\nCheck the render method of `" + name + "`.";
				}
				return "";
			}
			function getSourceInfoErrorAddendum(source) {
				if (source !== void 0) {
					var fileName = source.fileName.replace(/^.*[\\\/]/, "");
					var lineNumber = source.lineNumber;
					return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
				}
				return "";
			}
			function getSourceInfoErrorAddendumForProps(elementProps) {
				if (elementProps !== null && elementProps !== void 0) return getSourceInfoErrorAddendum(elementProps.__source);
				return "";
			}
			/**
			* Warn if there's no key explicitly set on dynamic arrays of children or
			* object keys are not valid. This allows us to keep track of children between
			* updates.
			*/
			var ownerHasKeyUseWarning = {};
			function getCurrentComponentErrorInfo(parentType) {
				var info = getDeclarationErrorAddendum();
				if (!info) {
					var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
					if (parentName) info = "\n\nCheck the top-level render call using <" + parentName + ">.";
				}
				return info;
			}
			/**
			* Warn if the element doesn't have an explicit key assigned to it.
			* This element is in an array. The array could grow and shrink or be
			* reordered. All children that haven't already been validated are required to
			* have a "key" property assigned to it. Error statuses are cached so a warning
			* will only be shown once.
			*
			* @internal
			* @param {ReactElement} element Element that requires a key.
			* @param {*} parentType element's parent's type.
			*/
			function validateExplicitKey(element, parentType) {
				if (!element._store || element._store.validated || element.key != null) return;
				element._store.validated = true;
				var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
				if (ownerHasKeyUseWarning[currentComponentErrorInfo]) return;
				ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
				var childOwner = "";
				if (element && element._owner && element._owner !== ReactCurrentOwner.current) childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
				setCurrentlyValidatingElement$1(element);
				error("Each child in a list should have a unique \"key\" prop.%s%s See https://reactjs.org/link/warning-keys for more information.", currentComponentErrorInfo, childOwner);
				setCurrentlyValidatingElement$1(null);
			}
			/**
			* Ensure that every element either is passed in a static location, in an
			* array with an explicit keys property defined, or in an object literal
			* with valid key property.
			*
			* @internal
			* @param {ReactNode} node Statically passed child of any type.
			* @param {*} parentType node's parent's type.
			*/
			function validateChildKeys(node, parentType) {
				if (typeof node !== "object") return;
				if (isArray(node)) for (var i = 0; i < node.length; i++) {
					var child = node[i];
					if (isValidElement(child)) validateExplicitKey(child, parentType);
				}
				else if (isValidElement(node)) {
					if (node._store) node._store.validated = true;
				} else if (node) {
					var iteratorFn = getIteratorFn(node);
					if (typeof iteratorFn === "function") {
						if (iteratorFn !== node.entries) {
							var iterator = iteratorFn.call(node);
							var step;
							while (!(step = iterator.next()).done) if (isValidElement(step.value)) validateExplicitKey(step.value, parentType);
						}
					}
				}
			}
			/**
			* Given an element, validate that its props follow the propTypes definition,
			* provided by the type.
			*
			* @param {ReactElement} element
			*/
			function validatePropTypes(element) {
				var type = element.type;
				if (type === null || type === void 0 || typeof type === "string") return;
				var propTypes;
				if (typeof type === "function") propTypes = type.propTypes;
				else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_MEMO_TYPE)) propTypes = type.propTypes;
				else return;
				if (propTypes) {
					var name = getComponentNameFromType(type);
					checkPropTypes(propTypes, element.props, "prop", name, element);
				} else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
					propTypesMisspellWarningShown = true;
					error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", getComponentNameFromType(type) || "Unknown");
				}
				if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
			}
			/**
			* Given a fragment, validate that it can only be provided with fragment props
			* @param {ReactElement} fragment
			*/
			function validateFragmentProps(fragment) {
				var keys = Object.keys(fragment.props);
				for (var i = 0; i < keys.length; i++) {
					var key = keys[i];
					if (key !== "children" && key !== "key") {
						setCurrentlyValidatingElement$1(fragment);
						error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
						setCurrentlyValidatingElement$1(null);
						break;
					}
				}
				if (fragment.ref !== null) {
					setCurrentlyValidatingElement$1(fragment);
					error("Invalid attribute `ref` supplied to `React.Fragment`.");
					setCurrentlyValidatingElement$1(null);
				}
			}
			function createElementWithValidation(type, props, children) {
				var validType = isValidElementType(type);
				if (!validType) {
					var info = "";
					if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
					var sourceInfo = getSourceInfoErrorAddendumForProps(props);
					if (sourceInfo) info += sourceInfo;
					else info += getDeclarationErrorAddendum();
					var typeString;
					if (type === null) typeString = "null";
					else if (isArray(type)) typeString = "array";
					else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
						typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
						info = " Did you accidentally export a JSX literal instead of a component?";
					} else typeString = typeof type;
					error("React.createElement: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
				}
				var element = createElement.apply(this, arguments);
				if (element == null) return element;
				if (validType) for (var i = 2; i < arguments.length; i++) validateChildKeys(arguments[i], type);
				if (type === REACT_FRAGMENT_TYPE) validateFragmentProps(element);
				else validatePropTypes(element);
				return element;
			}
			var didWarnAboutDeprecatedCreateFactory = false;
			function createFactoryWithValidation(type) {
				var validatedFactory = createElementWithValidation.bind(null, type);
				validatedFactory.type = type;
				if (!didWarnAboutDeprecatedCreateFactory) {
					didWarnAboutDeprecatedCreateFactory = true;
					warn("React.createFactory() is deprecated and will be removed in a future major release. Consider using JSX or use React.createElement() directly instead.");
				}
				Object.defineProperty(validatedFactory, "type", {
					enumerable: false,
					get: function() {
						warn("Factory.type is deprecated. Access the class directly before passing it to createFactory.");
						Object.defineProperty(this, "type", { value: type });
						return type;
					}
				});
				return validatedFactory;
			}
			function cloneElementWithValidation(element, props, children) {
				var newElement = cloneElement.apply(this, arguments);
				for (var i = 2; i < arguments.length; i++) validateChildKeys(arguments[i], newElement.type);
				validatePropTypes(newElement);
				return newElement;
			}
			function startTransition(scope, options) {
				var prevTransition = ReactCurrentBatchConfig.transition;
				ReactCurrentBatchConfig.transition = {};
				var currentTransition = ReactCurrentBatchConfig.transition;
				ReactCurrentBatchConfig.transition._updatedFibers = /* @__PURE__ */ new Set();
				try {
					scope();
				} finally {
					ReactCurrentBatchConfig.transition = prevTransition;
					if (prevTransition === null && currentTransition._updatedFibers) {
						if (currentTransition._updatedFibers.size > 10) warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
						currentTransition._updatedFibers.clear();
					}
				}
			}
			var didWarnAboutMessageChannel = false;
			var enqueueTaskImpl = null;
			function enqueueTask(task) {
				if (enqueueTaskImpl === null) try {
					var requireString = ("require" + Math.random()).slice(0, 7);
					enqueueTaskImpl = (module && module[requireString]).call(module, "timers").setImmediate;
				} catch (_err) {
					enqueueTaskImpl = function(callback) {
						if (didWarnAboutMessageChannel === false) {
							didWarnAboutMessageChannel = true;
							if (typeof MessageChannel === "undefined") error("This browser does not have a MessageChannel implementation, so enqueuing tasks via await act(async () => ...) will fail. Please file an issue at https://github.com/facebook/react/issues if you encounter this warning.");
						}
						var channel = new MessageChannel();
						channel.port1.onmessage = callback;
						channel.port2.postMessage(void 0);
					};
				}
				return enqueueTaskImpl(task);
			}
			var actScopeDepth = 0;
			var didWarnNoAwaitAct = false;
			function act(callback) {
				var prevActScopeDepth = actScopeDepth;
				actScopeDepth++;
				if (ReactCurrentActQueue.current === null) ReactCurrentActQueue.current = [];
				var prevIsBatchingLegacy = ReactCurrentActQueue.isBatchingLegacy;
				var result;
				try {
					ReactCurrentActQueue.isBatchingLegacy = true;
					result = callback();
					if (!prevIsBatchingLegacy && ReactCurrentActQueue.didScheduleLegacyUpdate) {
						var queue = ReactCurrentActQueue.current;
						if (queue !== null) {
							ReactCurrentActQueue.didScheduleLegacyUpdate = false;
							flushActQueue(queue);
						}
					}
				} catch (error) {
					popActScope(prevActScopeDepth);
					throw error;
				} finally {
					ReactCurrentActQueue.isBatchingLegacy = prevIsBatchingLegacy;
				}
				if (result !== null && typeof result === "object" && typeof result.then === "function") {
					var thenableResult = result;
					var wasAwaited = false;
					var thenable = { then: function(resolve, reject) {
						wasAwaited = true;
						thenableResult.then(function(returnValue) {
							popActScope(prevActScopeDepth);
							if (actScopeDepth === 0) recursivelyFlushAsyncActWork(returnValue, resolve, reject);
							else resolve(returnValue);
						}, function(error) {
							popActScope(prevActScopeDepth);
							reject(error);
						});
					} };
					if (!didWarnNoAwaitAct && typeof Promise !== "undefined") Promise.resolve().then(function() {}).then(function() {
						if (!wasAwaited) {
							didWarnNoAwaitAct = true;
							error("You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);");
						}
					});
					return thenable;
				} else {
					var returnValue = result;
					popActScope(prevActScopeDepth);
					if (actScopeDepth === 0) {
						var _queue = ReactCurrentActQueue.current;
						if (_queue !== null) {
							flushActQueue(_queue);
							ReactCurrentActQueue.current = null;
						}
						return { then: function(resolve, reject) {
							if (ReactCurrentActQueue.current === null) {
								ReactCurrentActQueue.current = [];
								recursivelyFlushAsyncActWork(returnValue, resolve, reject);
							} else resolve(returnValue);
						} };
					} else return { then: function(resolve, reject) {
						resolve(returnValue);
					} };
				}
			}
			function popActScope(prevActScopeDepth) {
				if (prevActScopeDepth !== actScopeDepth - 1) error("You seem to have overlapping act() calls, this is not supported. Be sure to await previous act() calls before making a new one. ");
				actScopeDepth = prevActScopeDepth;
			}
			function recursivelyFlushAsyncActWork(returnValue, resolve, reject) {
				var queue = ReactCurrentActQueue.current;
				if (queue !== null) try {
					flushActQueue(queue);
					enqueueTask(function() {
						if (queue.length === 0) {
							ReactCurrentActQueue.current = null;
							resolve(returnValue);
						} else recursivelyFlushAsyncActWork(returnValue, resolve, reject);
					});
				} catch (error) {
					reject(error);
				}
				else resolve(returnValue);
			}
			var isFlushing = false;
			function flushActQueue(queue) {
				if (!isFlushing) {
					isFlushing = true;
					var i = 0;
					try {
						for (; i < queue.length; i++) {
							var callback = queue[i];
							do
								callback = callback(true);
							while (callback !== null);
						}
						queue.length = 0;
					} catch (error) {
						queue = queue.slice(i + 1);
						throw error;
					} finally {
						isFlushing = false;
					}
				}
			}
			var createElement$1 = createElementWithValidation;
			var cloneElement$1 = cloneElementWithValidation;
			var createFactory = createFactoryWithValidation;
			exports.Children = {
				map: mapChildren,
				forEach: forEachChildren,
				count: countChildren,
				toArray,
				only: onlyChild
			};
			exports.Component = Component;
			exports.Fragment = REACT_FRAGMENT_TYPE;
			exports.Profiler = REACT_PROFILER_TYPE;
			exports.PureComponent = PureComponent;
			exports.StrictMode = REACT_STRICT_MODE_TYPE;
			exports.Suspense = REACT_SUSPENSE_TYPE;
			exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactSharedInternals;
			exports.act = act;
			exports.cloneElement = cloneElement$1;
			exports.createContext = createContext;
			exports.createElement = createElement$1;
			exports.createFactory = createFactory;
			exports.createRef = createRef;
			exports.forwardRef = forwardRef;
			exports.isValidElement = isValidElement;
			exports.lazy = lazy;
			exports.memo = memo;
			exports.startTransition = startTransition;
			exports.unstable_act = act;
			exports.useCallback = useCallback;
			exports.useContext = useContext;
			exports.useDebugValue = useDebugValue;
			exports.useDeferredValue = useDeferredValue;
			exports.useEffect = useEffect;
			exports.useId = useId;
			exports.useImperativeHandle = useImperativeHandle;
			exports.useInsertionEffect = useInsertionEffect;
			exports.useLayoutEffect = useLayoutEffect;
			exports.useMemo = useMemo;
			exports.useReducer = useReducer;
			exports.useRef = useRef;
			exports.useState = useState;
			exports.useSyncExternalStore = useSyncExternalStore;
			exports.useTransition = useTransition;
			exports.version = ReactVersion;
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(/* @__PURE__ */ new Error());
		})();
	}));
	//#endregion
	//#region ../node_modules/react/index.js
	var require_react = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_react_development();
	}));
	//#endregion
	//#region ../node_modules/scheduler/cjs/scheduler.development.js
	/**
	* @license React
	* scheduler.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_scheduler_development = /* @__PURE__ */ __commonJSMin(((exports) => {
		(function() {
			"use strict";
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(/* @__PURE__ */ new Error());
			var enableSchedulerDebugging = false;
			var enableProfiling = false;
			var frameYieldMs = 5;
			function push(heap, node) {
				var index = heap.length;
				heap.push(node);
				siftUp(heap, node, index);
			}
			function peek(heap) {
				return heap.length === 0 ? null : heap[0];
			}
			function pop(heap) {
				if (heap.length === 0) return null;
				var first = heap[0];
				var last = heap.pop();
				if (last !== first) {
					heap[0] = last;
					siftDown(heap, last, 0);
				}
				return first;
			}
			function siftUp(heap, node, i) {
				var index = i;
				while (index > 0) {
					var parentIndex = index - 1 >>> 1;
					var parent = heap[parentIndex];
					if (compare(parent, node) > 0) {
						heap[parentIndex] = node;
						heap[index] = parent;
						index = parentIndex;
					} else return;
				}
			}
			function siftDown(heap, node, i) {
				var index = i;
				var length = heap.length;
				var halfLength = length >>> 1;
				while (index < halfLength) {
					var leftIndex = (index + 1) * 2 - 1;
					var left = heap[leftIndex];
					var rightIndex = leftIndex + 1;
					var right = heap[rightIndex];
					if (compare(left, node) < 0) {
						if (rightIndex < length && compare(right, left) < 0) {
							heap[index] = right;
							heap[rightIndex] = node;
							index = rightIndex;
						} else {
							heap[index] = left;
							heap[leftIndex] = node;
							index = leftIndex;
						}
					} else if (rightIndex < length && compare(right, node) < 0) {
						heap[index] = right;
						heap[rightIndex] = node;
						index = rightIndex;
					} else return;
				}
			}
			function compare(a, b) {
				var diff = a.sortIndex - b.sortIndex;
				return diff !== 0 ? diff : a.id - b.id;
			}
			var ImmediatePriority = 1;
			var UserBlockingPriority = 2;
			var NormalPriority = 3;
			var LowPriority = 4;
			var IdlePriority = 5;
			if (typeof performance === "object" && typeof performance.now === "function") {
				var localPerformance = performance;
				exports.unstable_now = function() {
					return localPerformance.now();
				};
			} else {
				var localDate = Date;
				var initialTime = localDate.now();
				exports.unstable_now = function() {
					return localDate.now() - initialTime;
				};
			}
			var maxSigned31BitInt = 1073741823;
			var IMMEDIATE_PRIORITY_TIMEOUT = -1;
			var USER_BLOCKING_PRIORITY_TIMEOUT = 250;
			var NORMAL_PRIORITY_TIMEOUT = 5e3;
			var LOW_PRIORITY_TIMEOUT = 1e4;
			var IDLE_PRIORITY_TIMEOUT = maxSigned31BitInt;
			var taskQueue = [];
			var timerQueue = [];
			var taskIdCounter = 1;
			var currentTask = null;
			var currentPriorityLevel = NormalPriority;
			var isPerformingWork = false;
			var isHostCallbackScheduled = false;
			var isHostTimeoutScheduled = false;
			var localSetTimeout = typeof setTimeout === "function" ? setTimeout : null;
			var localClearTimeout = typeof clearTimeout === "function" ? clearTimeout : null;
			var localSetImmediate = typeof setImmediate !== "undefined" ? setImmediate : null;
			typeof navigator !== "undefined" && navigator.scheduling !== void 0 && navigator.scheduling.isInputPending !== void 0 && navigator.scheduling.isInputPending.bind(navigator.scheduling);
			function advanceTimers(currentTime) {
				var timer = peek(timerQueue);
				while (timer !== null) {
					if (timer.callback === null) pop(timerQueue);
					else if (timer.startTime <= currentTime) {
						pop(timerQueue);
						timer.sortIndex = timer.expirationTime;
						push(taskQueue, timer);
					} else return;
					timer = peek(timerQueue);
				}
			}
			function handleTimeout(currentTime) {
				isHostTimeoutScheduled = false;
				advanceTimers(currentTime);
				if (!isHostCallbackScheduled) {
					if (peek(taskQueue) !== null) {
						isHostCallbackScheduled = true;
						requestHostCallback(flushWork);
					} else {
						var firstTimer = peek(timerQueue);
						if (firstTimer !== null) requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
					}
				}
			}
			function flushWork(hasTimeRemaining, initialTime) {
				isHostCallbackScheduled = false;
				if (isHostTimeoutScheduled) {
					isHostTimeoutScheduled = false;
					cancelHostTimeout();
				}
				isPerformingWork = true;
				var previousPriorityLevel = currentPriorityLevel;
				try {
					if (enableProfiling) try {
						return workLoop(hasTimeRemaining, initialTime);
					} catch (error) {
						if (currentTask !== null) {
							exports.unstable_now();
							currentTask.isQueued = false;
						}
						throw error;
					}
					else return workLoop(hasTimeRemaining, initialTime);
				} finally {
					currentTask = null;
					currentPriorityLevel = previousPriorityLevel;
					isPerformingWork = false;
				}
			}
			function workLoop(hasTimeRemaining, initialTime) {
				var currentTime = initialTime;
				advanceTimers(currentTime);
				currentTask = peek(taskQueue);
				while (currentTask !== null && !enableSchedulerDebugging) {
					if (currentTask.expirationTime > currentTime && (!hasTimeRemaining || shouldYieldToHost())) break;
					var callback = currentTask.callback;
					if (typeof callback === "function") {
						currentTask.callback = null;
						currentPriorityLevel = currentTask.priorityLevel;
						var continuationCallback = callback(currentTask.expirationTime <= currentTime);
						currentTime = exports.unstable_now();
						if (typeof continuationCallback === "function") currentTask.callback = continuationCallback;
						else if (currentTask === peek(taskQueue)) pop(taskQueue);
						advanceTimers(currentTime);
					} else pop(taskQueue);
					currentTask = peek(taskQueue);
				}
				if (currentTask !== null) return true;
				else {
					var firstTimer = peek(timerQueue);
					if (firstTimer !== null) requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
					return false;
				}
			}
			function unstable_runWithPriority(priorityLevel, eventHandler) {
				switch (priorityLevel) {
					case ImmediatePriority:
					case UserBlockingPriority:
					case NormalPriority:
					case LowPriority:
					case IdlePriority: break;
					default: priorityLevel = NormalPriority;
				}
				var previousPriorityLevel = currentPriorityLevel;
				currentPriorityLevel = priorityLevel;
				try {
					return eventHandler();
				} finally {
					currentPriorityLevel = previousPriorityLevel;
				}
			}
			function unstable_next(eventHandler) {
				var priorityLevel;
				switch (currentPriorityLevel) {
					case ImmediatePriority:
					case UserBlockingPriority:
					case NormalPriority:
						priorityLevel = NormalPriority;
						break;
					default: priorityLevel = currentPriorityLevel;
				}
				var previousPriorityLevel = currentPriorityLevel;
				currentPriorityLevel = priorityLevel;
				try {
					return eventHandler();
				} finally {
					currentPriorityLevel = previousPriorityLevel;
				}
			}
			function unstable_wrapCallback(callback) {
				var parentPriorityLevel = currentPriorityLevel;
				return function() {
					var previousPriorityLevel = currentPriorityLevel;
					currentPriorityLevel = parentPriorityLevel;
					try {
						return callback.apply(this, arguments);
					} finally {
						currentPriorityLevel = previousPriorityLevel;
					}
				};
			}
			function unstable_scheduleCallback(priorityLevel, callback, options) {
				var currentTime = exports.unstable_now();
				var startTime;
				if (typeof options === "object" && options !== null) {
					var delay = options.delay;
					if (typeof delay === "number" && delay > 0) startTime = currentTime + delay;
					else startTime = currentTime;
				} else startTime = currentTime;
				var timeout;
				switch (priorityLevel) {
					case ImmediatePriority:
						timeout = IMMEDIATE_PRIORITY_TIMEOUT;
						break;
					case UserBlockingPriority:
						timeout = USER_BLOCKING_PRIORITY_TIMEOUT;
						break;
					case IdlePriority:
						timeout = IDLE_PRIORITY_TIMEOUT;
						break;
					case LowPriority:
						timeout = LOW_PRIORITY_TIMEOUT;
						break;
					case NormalPriority:
					default: timeout = NORMAL_PRIORITY_TIMEOUT;
				}
				var expirationTime = startTime + timeout;
				var newTask = {
					id: taskIdCounter++,
					callback,
					priorityLevel,
					startTime,
					expirationTime,
					sortIndex: -1
				};
				if (startTime > currentTime) {
					newTask.sortIndex = startTime;
					push(timerQueue, newTask);
					if (peek(taskQueue) === null && newTask === peek(timerQueue)) {
						if (isHostTimeoutScheduled) cancelHostTimeout();
						else isHostTimeoutScheduled = true;
						requestHostTimeout(handleTimeout, startTime - currentTime);
					}
				} else {
					newTask.sortIndex = expirationTime;
					push(taskQueue, newTask);
					if (!isHostCallbackScheduled && !isPerformingWork) {
						isHostCallbackScheduled = true;
						requestHostCallback(flushWork);
					}
				}
				return newTask;
			}
			function unstable_pauseExecution() {}
			function unstable_continueExecution() {
				if (!isHostCallbackScheduled && !isPerformingWork) {
					isHostCallbackScheduled = true;
					requestHostCallback(flushWork);
				}
			}
			function unstable_getFirstCallbackNode() {
				return peek(taskQueue);
			}
			function unstable_cancelCallback(task) {
				task.callback = null;
			}
			function unstable_getCurrentPriorityLevel() {
				return currentPriorityLevel;
			}
			var isMessageLoopRunning = false;
			var scheduledHostCallback = null;
			var taskTimeoutID = -1;
			var frameInterval = frameYieldMs;
			var startTime = -1;
			function shouldYieldToHost() {
				if (exports.unstable_now() - startTime < frameInterval) return false;
				return true;
			}
			function requestPaint() {}
			function forceFrameRate(fps) {
				if (fps < 0 || fps > 125) {
					console["error"]("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported");
					return;
				}
				if (fps > 0) frameInterval = Math.floor(1e3 / fps);
				else frameInterval = frameYieldMs;
			}
			var performWorkUntilDeadline = function() {
				if (scheduledHostCallback !== null) {
					var currentTime = exports.unstable_now();
					startTime = currentTime;
					var hasTimeRemaining = true;
					var hasMoreWork = true;
					try {
						hasMoreWork = scheduledHostCallback(hasTimeRemaining, currentTime);
					} finally {
						if (hasMoreWork) schedulePerformWorkUntilDeadline();
						else {
							isMessageLoopRunning = false;
							scheduledHostCallback = null;
						}
					}
				} else isMessageLoopRunning = false;
			};
			var schedulePerformWorkUntilDeadline;
			if (typeof localSetImmediate === "function") schedulePerformWorkUntilDeadline = function() {
				localSetImmediate(performWorkUntilDeadline);
			};
			else if (typeof MessageChannel !== "undefined") {
				var channel = new MessageChannel();
				var port = channel.port2;
				channel.port1.onmessage = performWorkUntilDeadline;
				schedulePerformWorkUntilDeadline = function() {
					port.postMessage(null);
				};
			} else schedulePerformWorkUntilDeadline = function() {
				localSetTimeout(performWorkUntilDeadline, 0);
			};
			function requestHostCallback(callback) {
				scheduledHostCallback = callback;
				if (!isMessageLoopRunning) {
					isMessageLoopRunning = true;
					schedulePerformWorkUntilDeadline();
				}
			}
			function requestHostTimeout(callback, ms) {
				taskTimeoutID = localSetTimeout(function() {
					callback(exports.unstable_now());
				}, ms);
			}
			function cancelHostTimeout() {
				localClearTimeout(taskTimeoutID);
				taskTimeoutID = -1;
			}
			var unstable_requestPaint = requestPaint;
			var unstable_Profiling = null;
			exports.unstable_IdlePriority = IdlePriority;
			exports.unstable_ImmediatePriority = ImmediatePriority;
			exports.unstable_LowPriority = LowPriority;
			exports.unstable_NormalPriority = NormalPriority;
			exports.unstable_Profiling = unstable_Profiling;
			exports.unstable_UserBlockingPriority = UserBlockingPriority;
			exports.unstable_cancelCallback = unstable_cancelCallback;
			exports.unstable_continueExecution = unstable_continueExecution;
			exports.unstable_forceFrameRate = forceFrameRate;
			exports.unstable_getCurrentPriorityLevel = unstable_getCurrentPriorityLevel;
			exports.unstable_getFirstCallbackNode = unstable_getFirstCallbackNode;
			exports.unstable_next = unstable_next;
			exports.unstable_pauseExecution = unstable_pauseExecution;
			exports.unstable_requestPaint = unstable_requestPaint;
			exports.unstable_runWithPriority = unstable_runWithPriority;
			exports.unstable_scheduleCallback = unstable_scheduleCallback;
			exports.unstable_shouldYield = shouldYieldToHost;
			exports.unstable_wrapCallback = unstable_wrapCallback;
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(/* @__PURE__ */ new Error());
		})();
	}));
	//#endregion
	//#region ../node_modules/scheduler/index.js
	var require_scheduler = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_scheduler_development();
	}));
	//#endregion
	//#region ../node_modules/react-dom/cjs/react-dom.development.js
	/**
	* @license React
	* react-dom.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_react_dom_development = /* @__PURE__ */ __commonJSMin(((exports) => {
		(function() {
			"use strict";
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(/* @__PURE__ */ new Error());
			var React = require_react();
			var Scheduler = require_scheduler();
			var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
			var suppressWarning = false;
			function setSuppressWarning(newSuppressWarning) {
				suppressWarning = newSuppressWarning;
			}
			function warn(format) {
				if (!suppressWarning) {
					for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
					printWarning("warn", format, args);
				}
			}
			function error(format) {
				if (!suppressWarning) {
					for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) args[_key2 - 1] = arguments[_key2];
					printWarning("error", format, args);
				}
			}
			function printWarning(level, format, args) {
				var stack = ReactSharedInternals.ReactDebugCurrentFrame.getStackAddendum();
				if (stack !== "") {
					format += "%s";
					args = args.concat([stack]);
				}
				var argsWithFormat = args.map(function(item) {
					return String(item);
				});
				argsWithFormat.unshift("Warning: " + format);
				Function.prototype.apply.call(console[level], console, argsWithFormat);
			}
			var FunctionComponent = 0;
			var ClassComponent = 1;
			var IndeterminateComponent = 2;
			var HostRoot = 3;
			var HostPortal = 4;
			var HostComponent = 5;
			var HostText = 6;
			var Fragment = 7;
			var Mode = 8;
			var ContextConsumer = 9;
			var ContextProvider = 10;
			var ForwardRef = 11;
			var Profiler = 12;
			var SuspenseComponent = 13;
			var MemoComponent = 14;
			var SimpleMemoComponent = 15;
			var LazyComponent = 16;
			var IncompleteClassComponent = 17;
			var DehydratedFragment = 18;
			var SuspenseListComponent = 19;
			var ScopeComponent = 21;
			var OffscreenComponent = 22;
			var LegacyHiddenComponent = 23;
			var CacheComponent = 24;
			var TracingMarkerComponent = 25;
			var enableClientRenderFallbackOnTextMismatch = true;
			var enableNewReconciler = false;
			var enableLazyContextPropagation = false;
			var enableLegacyHidden = false;
			var enableSuspenseAvoidThisFallback = false;
			var disableCommentsAsDOMContainers = true;
			var enableCustomElementPropertySupport = false;
			var warnAboutStringRefs = true;
			var enableSchedulingProfiler = true;
			var enableProfilerTimer = true;
			var enableProfilerCommitHooks = true;
			var allNativeEvents = /* @__PURE__ */ new Set();
			/**
			* Mapping from registration name to event name
			*/
			var registrationNameDependencies = {};
			/**
			* Mapping from lowercase registration names to the properly cased version,
			* used to warn in the case of missing event handlers. Available
			* only in true.
			* @type {Object}
			*/
			var possibleRegistrationNames = {};
			function registerTwoPhaseEvent(registrationName, dependencies) {
				registerDirectEvent(registrationName, dependencies);
				registerDirectEvent(registrationName + "Capture", dependencies);
			}
			function registerDirectEvent(registrationName, dependencies) {
				if (registrationNameDependencies[registrationName]) error("EventRegistry: More than one plugin attempted to publish the same registration name, `%s`.", registrationName);
				registrationNameDependencies[registrationName] = dependencies;
				var lowerCasedName = registrationName.toLowerCase();
				possibleRegistrationNames[lowerCasedName] = registrationName;
				if (registrationName === "onDoubleClick") possibleRegistrationNames.ondblclick = registrationName;
				for (var i = 0; i < dependencies.length; i++) allNativeEvents.add(dependencies[i]);
			}
			var canUseDOM = !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
			var hasOwnProperty = Object.prototype.hasOwnProperty;
			function typeName(value) {
				return typeof Symbol === "function" && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
			}
			function willCoercionThrow(value) {
				try {
					testStringCoercion(value);
					return false;
				} catch (e) {
					return true;
				}
			}
			function testStringCoercion(value) {
				return "" + value;
			}
			function checkAttributeStringCoercion(value, attributeName) {
				if (willCoercionThrow(value)) {
					error("The provided `%s` attribute is an unsupported type %s. This value must be coerced to a string before before using it here.", attributeName, typeName(value));
					return testStringCoercion(value);
				}
			}
			function checkKeyStringCoercion(value) {
				if (willCoercionThrow(value)) {
					error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
					return testStringCoercion(value);
				}
			}
			function checkPropStringCoercion(value, propName) {
				if (willCoercionThrow(value)) {
					error("The provided `%s` prop is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
					return testStringCoercion(value);
				}
			}
			function checkCSSPropertyStringCoercion(value, propName) {
				if (willCoercionThrow(value)) {
					error("The provided `%s` CSS property is an unsupported type %s. This value must be coerced to a string before before using it here.", propName, typeName(value));
					return testStringCoercion(value);
				}
			}
			function checkHtmlStringCoercion(value) {
				if (willCoercionThrow(value)) {
					error("The provided HTML markup uses a value of unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
					return testStringCoercion(value);
				}
			}
			function checkFormFieldValueStringCoercion(value) {
				if (willCoercionThrow(value)) {
					error("Form field values (value, checked, defaultValue, or defaultChecked props) must be strings, not %s. This value must be coerced to a string before before using it here.", typeName(value));
					return testStringCoercion(value);
				}
			}
			var RESERVED = 0;
			var STRING = 1;
			var BOOLEANISH_STRING = 2;
			var BOOLEAN = 3;
			var OVERLOADED_BOOLEAN = 4;
			var NUMERIC = 5;
			var POSITIVE_NUMERIC = 6;
			var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
			var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
			var VALID_ATTRIBUTE_NAME_REGEX = new RegExp("^[" + ATTRIBUTE_NAME_START_CHAR + "][" + ATTRIBUTE_NAME_CHAR + "]*$");
			var illegalAttributeNameCache = {};
			var validatedAttributeNameCache = {};
			function isAttributeNameSafe(attributeName) {
				if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) return true;
				if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) return false;
				if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
					validatedAttributeNameCache[attributeName] = true;
					return true;
				}
				illegalAttributeNameCache[attributeName] = true;
				error("Invalid attribute name: `%s`", attributeName);
				return false;
			}
			function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
				if (propertyInfo !== null) return propertyInfo.type === RESERVED;
				if (isCustomComponentTag) return false;
				if (name.length > 2 && (name[0] === "o" || name[0] === "O") && (name[1] === "n" || name[1] === "N")) return true;
				return false;
			}
			function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
				if (propertyInfo !== null && propertyInfo.type === RESERVED) return false;
				switch (typeof value) {
					case "function":
					case "symbol": return true;
					case "boolean":
						if (isCustomComponentTag) return false;
						if (propertyInfo !== null) return !propertyInfo.acceptsBooleans;
						else {
							var prefix = name.toLowerCase().slice(0, 5);
							return prefix !== "data-" && prefix !== "aria-";
						}
					default: return false;
				}
			}
			function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
				if (value === null || typeof value === "undefined") return true;
				if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) return true;
				if (isCustomComponentTag) return false;
				if (propertyInfo !== null) switch (propertyInfo.type) {
					case BOOLEAN: return !value;
					case OVERLOADED_BOOLEAN: return value === false;
					case NUMERIC: return isNaN(value);
					case POSITIVE_NUMERIC: return isNaN(value) || value < 1;
				}
				return false;
			}
			function getPropertyInfo(name) {
				return properties.hasOwnProperty(name) ? properties[name] : null;
			}
			function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace, sanitizeURL, removeEmptyString) {
				this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
				this.attributeName = attributeName;
				this.attributeNamespace = attributeNamespace;
				this.mustUseProperty = mustUseProperty;
				this.propertyName = name;
				this.type = type;
				this.sanitizeURL = sanitizeURL;
				this.removeEmptyString = removeEmptyString;
			}
			var properties = {};
			[
				"children",
				"dangerouslySetInnerHTML",
				"defaultValue",
				"defaultChecked",
				"innerHTML",
				"suppressContentEditableWarning",
				"suppressHydrationWarning",
				"style"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, RESERVED, false, name, null, false, false);
			});
			[
				["acceptCharset", "accept-charset"],
				["className", "class"],
				["htmlFor", "for"],
				["httpEquiv", "http-equiv"]
			].forEach(function(_ref) {
				var name = _ref[0], attributeName = _ref[1];
				properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, null, false, false);
			});
			[
				"contentEditable",
				"draggable",
				"spellCheck",
				"value"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, name.toLowerCase(), null, false, false);
			});
			[
				"autoReverse",
				"externalResourcesRequired",
				"focusable",
				"preserveAlpha"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, name, null, false, false);
			});
			[
				"allowFullScreen",
				"async",
				"autoFocus",
				"autoPlay",
				"controls",
				"default",
				"defer",
				"disabled",
				"disablePictureInPicture",
				"disableRemotePlayback",
				"formNoValidate",
				"hidden",
				"loop",
				"noModule",
				"noValidate",
				"open",
				"playsInline",
				"readOnly",
				"required",
				"reversed",
				"scoped",
				"seamless",
				"itemScope"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, BOOLEAN, false, name.toLowerCase(), null, false, false);
			});
			[
				"checked",
				"multiple",
				"muted",
				"selected"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, BOOLEAN, true, name, null, false, false);
			});
			["capture", "download"].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, OVERLOADED_BOOLEAN, false, name, null, false, false);
			});
			[
				"cols",
				"rows",
				"size",
				"span"
			].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, POSITIVE_NUMERIC, false, name, null, false, false);
			});
			["rowSpan", "start"].forEach(function(name) {
				properties[name] = new PropertyInfoRecord(name, NUMERIC, false, name.toLowerCase(), null, false, false);
			});
			var CAMELIZE = /[\-\:]([a-z])/g;
			var capitalize = function(token) {
				return token[1].toUpperCase();
			};
			[
				"accent-height",
				"alignment-baseline",
				"arabic-form",
				"baseline-shift",
				"cap-height",
				"clip-path",
				"clip-rule",
				"color-interpolation",
				"color-interpolation-filters",
				"color-profile",
				"color-rendering",
				"dominant-baseline",
				"enable-background",
				"fill-opacity",
				"fill-rule",
				"flood-color",
				"flood-opacity",
				"font-family",
				"font-size",
				"font-size-adjust",
				"font-stretch",
				"font-style",
				"font-variant",
				"font-weight",
				"glyph-name",
				"glyph-orientation-horizontal",
				"glyph-orientation-vertical",
				"horiz-adv-x",
				"horiz-origin-x",
				"image-rendering",
				"letter-spacing",
				"lighting-color",
				"marker-end",
				"marker-mid",
				"marker-start",
				"overline-position",
				"overline-thickness",
				"paint-order",
				"panose-1",
				"pointer-events",
				"rendering-intent",
				"shape-rendering",
				"stop-color",
				"stop-opacity",
				"strikethrough-position",
				"strikethrough-thickness",
				"stroke-dasharray",
				"stroke-dashoffset",
				"stroke-linecap",
				"stroke-linejoin",
				"stroke-miterlimit",
				"stroke-opacity",
				"stroke-width",
				"text-anchor",
				"text-decoration",
				"text-rendering",
				"underline-position",
				"underline-thickness",
				"unicode-bidi",
				"unicode-range",
				"units-per-em",
				"v-alphabetic",
				"v-hanging",
				"v-ideographic",
				"v-mathematical",
				"vector-effect",
				"vert-adv-y",
				"vert-origin-x",
				"vert-origin-y",
				"word-spacing",
				"writing-mode",
				"xmlns:xlink",
				"x-height"
			].forEach(function(attributeName) {
				var name = attributeName.replace(CAMELIZE, capitalize);
				properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, null, false, false);
			});
			[
				"xlink:actuate",
				"xlink:arcrole",
				"xlink:role",
				"xlink:show",
				"xlink:title",
				"xlink:type"
			].forEach(function(attributeName) {
				var name = attributeName.replace(CAMELIZE, capitalize);
				properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, "http://www.w3.org/1999/xlink", false, false);
			});
			[
				"xml:base",
				"xml:lang",
				"xml:space"
			].forEach(function(attributeName) {
				var name = attributeName.replace(CAMELIZE, capitalize);
				properties[name] = new PropertyInfoRecord(name, STRING, false, attributeName, "http://www.w3.org/XML/1998/namespace", false, false);
			});
			["tabIndex", "crossOrigin"].forEach(function(attributeName) {
				properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, attributeName.toLowerCase(), null, false, false);
			});
			var xlinkHref = "xlinkHref";
			properties[xlinkHref] = new PropertyInfoRecord("xlinkHref", STRING, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
			[
				"src",
				"href",
				"action",
				"formAction"
			].forEach(function(attributeName) {
				properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, attributeName.toLowerCase(), null, true, true);
			});
			var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*\:/i;
			var didWarn = false;
			function sanitizeURL(url) {
				if (!didWarn && isJavaScriptProtocol.test(url)) {
					didWarn = true;
					error("A future version of React will block javascript: URLs as a security precaution. Use event handlers instead if you can. If you need to generate unsafe HTML try using dangerouslySetInnerHTML instead. React was passed %s.", JSON.stringify(url));
				}
			}
			/**
			* Get the value for a property on a node. Only used in DEV for SSR validation.
			* The "expected" argument is used as a hint of what the expected value is.
			* Some properties have multiple equivalent values.
			*/
			function getValueForProperty(node, name, expected, propertyInfo) {
				if (propertyInfo.mustUseProperty) return node[propertyInfo.propertyName];
				else {
					checkAttributeStringCoercion(expected, name);
					if (propertyInfo.sanitizeURL) sanitizeURL("" + expected);
					var attributeName = propertyInfo.attributeName;
					var stringValue = null;
					if (propertyInfo.type === OVERLOADED_BOOLEAN) {
						if (node.hasAttribute(attributeName)) {
							var value = node.getAttribute(attributeName);
							if (value === "") return true;
							if (shouldRemoveAttribute(name, expected, propertyInfo, false)) return value;
							if (value === "" + expected) return expected;
							return value;
						}
					} else if (node.hasAttribute(attributeName)) {
						if (shouldRemoveAttribute(name, expected, propertyInfo, false)) return node.getAttribute(attributeName);
						if (propertyInfo.type === BOOLEAN) return expected;
						stringValue = node.getAttribute(attributeName);
					}
					if (shouldRemoveAttribute(name, expected, propertyInfo, false)) return stringValue === null ? expected : stringValue;
					else if (stringValue === "" + expected) return expected;
					else return stringValue;
				}
			}
			/**
			* Get the value for a attribute on a node. Only used in DEV for SSR validation.
			* The third argument is used as a hint of what the expected value is. Some
			* attributes have multiple equivalent values.
			*/
			function getValueForAttribute(node, name, expected, isCustomComponentTag) {
				if (!isAttributeNameSafe(name)) return;
				if (!node.hasAttribute(name)) return expected === void 0 ? void 0 : null;
				var value = node.getAttribute(name);
				checkAttributeStringCoercion(expected, name);
				if (value === "" + expected) return expected;
				return value;
			}
			/**
			* Sets the value for a property on a node.
			*
			* @param {DOMElement} node
			* @param {string} name
			* @param {*} value
			*/
			function setValueForProperty(node, name, value, isCustomComponentTag) {
				var propertyInfo = getPropertyInfo(name);
				if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) return;
				if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) value = null;
				if (isCustomComponentTag || propertyInfo === null) {
					if (isAttributeNameSafe(name)) {
						var _attributeName = name;
						if (value === null) node.removeAttribute(_attributeName);
						else {
							checkAttributeStringCoercion(value, name);
							node.setAttribute(_attributeName, "" + value);
						}
					}
					return;
				}
				if (propertyInfo.mustUseProperty) {
					var propertyName = propertyInfo.propertyName;
					if (value === null) node[propertyName] = propertyInfo.type === BOOLEAN ? false : "";
					else node[propertyName] = value;
					return;
				}
				var attributeName = propertyInfo.attributeName, attributeNamespace = propertyInfo.attributeNamespace;
				if (value === null) node.removeAttribute(attributeName);
				else {
					var _type = propertyInfo.type;
					var attributeValue;
					if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) attributeValue = "";
					else {
						checkAttributeStringCoercion(value, attributeName);
						attributeValue = "" + value;
						if (propertyInfo.sanitizeURL) sanitizeURL(attributeValue.toString());
					}
					if (attributeNamespace) node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
					else node.setAttribute(attributeName, attributeValue);
				}
			}
			var REACT_ELEMENT_TYPE = Symbol.for("react.element");
			var REACT_PORTAL_TYPE = Symbol.for("react.portal");
			var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
			var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
			var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
			var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
			var REACT_CONTEXT_TYPE = Symbol.for("react.context");
			var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
			var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
			var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
			var REACT_MEMO_TYPE = Symbol.for("react.memo");
			var REACT_LAZY_TYPE = Symbol.for("react.lazy");
			var REACT_SCOPE_TYPE = Symbol.for("react.scope");
			var REACT_DEBUG_TRACING_MODE_TYPE = Symbol.for("react.debug_trace_mode");
			var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
			var REACT_LEGACY_HIDDEN_TYPE = Symbol.for("react.legacy_hidden");
			var REACT_CACHE_TYPE = Symbol.for("react.cache");
			var REACT_TRACING_MARKER_TYPE = Symbol.for("react.tracing_marker");
			var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
			var FAUX_ITERATOR_SYMBOL = "@@iterator";
			function getIteratorFn(maybeIterable) {
				if (maybeIterable === null || typeof maybeIterable !== "object") return null;
				var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
				if (typeof maybeIterator === "function") return maybeIterator;
				return null;
			}
			var assign = Object.assign;
			var disabledDepth = 0;
			var prevLog;
			var prevInfo;
			var prevWarn;
			var prevError;
			var prevGroup;
			var prevGroupCollapsed;
			var prevGroupEnd;
			function disabledLog() {}
			disabledLog.__reactDisabledLog = true;
			function disableLogs() {
				if (disabledDepth === 0) {
					prevLog = console.log;
					prevInfo = console.info;
					prevWarn = console.warn;
					prevError = console.error;
					prevGroup = console.group;
					prevGroupCollapsed = console.groupCollapsed;
					prevGroupEnd = console.groupEnd;
					var props = {
						configurable: true,
						enumerable: true,
						value: disabledLog,
						writable: true
					};
					Object.defineProperties(console, {
						info: props,
						log: props,
						warn: props,
						error: props,
						group: props,
						groupCollapsed: props,
						groupEnd: props
					});
				}
				disabledDepth++;
			}
			function reenableLogs() {
				disabledDepth--;
				if (disabledDepth === 0) {
					var props = {
						configurable: true,
						enumerable: true,
						writable: true
					};
					Object.defineProperties(console, {
						log: assign({}, props, { value: prevLog }),
						info: assign({}, props, { value: prevInfo }),
						warn: assign({}, props, { value: prevWarn }),
						error: assign({}, props, { value: prevError }),
						group: assign({}, props, { value: prevGroup }),
						groupCollapsed: assign({}, props, { value: prevGroupCollapsed }),
						groupEnd: assign({}, props, { value: prevGroupEnd })
					});
				}
				if (disabledDepth < 0) error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
			}
			var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
			var prefix;
			function describeBuiltInComponentFrame(name, source, ownerFn) {
				if (prefix === void 0) try {
					throw Error();
				} catch (x) {
					var match = x.stack.trim().match(/\n( *(at )?)/);
					prefix = match && match[1] || "";
				}
				return "\n" + prefix + name;
			}
			var reentry = false;
			var componentFrameCache = new (typeof WeakMap === "function" ? WeakMap : Map)();
			function describeNativeComponentFrame(fn, construct) {
				if (!fn || reentry) return "";
				var frame = componentFrameCache.get(fn);
				if (frame !== void 0) return frame;
				var control;
				reentry = true;
				var previousPrepareStackTrace = Error.prepareStackTrace;
				Error.prepareStackTrace = void 0;
				var previousDispatcher = ReactCurrentDispatcher.current;
				ReactCurrentDispatcher.current = null;
				disableLogs();
				try {
					if (construct) {
						var Fake = function() {
							throw Error();
						};
						Object.defineProperty(Fake.prototype, "props", { set: function() {
							throw Error();
						} });
						if (typeof Reflect === "object" && Reflect.construct) {
							try {
								Reflect.construct(Fake, []);
							} catch (x) {
								control = x;
							}
							Reflect.construct(fn, [], Fake);
						} else {
							try {
								Fake.call();
							} catch (x) {
								control = x;
							}
							fn.call(Fake.prototype);
						}
					} else {
						try {
							throw Error();
						} catch (x) {
							control = x;
						}
						fn();
					}
				} catch (sample) {
					if (sample && control && typeof sample.stack === "string") {
						var sampleLines = sample.stack.split("\n");
						var controlLines = control.stack.split("\n");
						var s = sampleLines.length - 1;
						var c = controlLines.length - 1;
						while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) c--;
						for (; s >= 1 && c >= 0; s--, c--) if (sampleLines[s] !== controlLines[c]) {
							if (s !== 1 || c !== 1) do {
								s--;
								c--;
								if (c < 0 || sampleLines[s] !== controlLines[c]) {
									var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
									if (fn.displayName && _frame.includes("<anonymous>")) _frame = _frame.replace("<anonymous>", fn.displayName);
									if (typeof fn === "function") componentFrameCache.set(fn, _frame);
									return _frame;
								}
							} while (s >= 1 && c >= 0);
							break;
						}
					}
				} finally {
					reentry = false;
					ReactCurrentDispatcher.current = previousDispatcher;
					reenableLogs();
					Error.prepareStackTrace = previousPrepareStackTrace;
				}
				var name = fn ? fn.displayName || fn.name : "";
				var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
				if (typeof fn === "function") componentFrameCache.set(fn, syntheticFrame);
				return syntheticFrame;
			}
			function describeClassComponentFrame(ctor, source, ownerFn) {
				return describeNativeComponentFrame(ctor, true);
			}
			function describeFunctionComponentFrame(fn, source, ownerFn) {
				return describeNativeComponentFrame(fn, false);
			}
			function shouldConstruct(Component) {
				var prototype = Component.prototype;
				return !!(prototype && prototype.isReactComponent);
			}
			function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
				if (type == null) return "";
				if (typeof type === "function") return describeNativeComponentFrame(type, shouldConstruct(type));
				if (typeof type === "string") return describeBuiltInComponentFrame(type);
				switch (type) {
					case REACT_SUSPENSE_TYPE: return describeBuiltInComponentFrame("Suspense");
					case REACT_SUSPENSE_LIST_TYPE: return describeBuiltInComponentFrame("SuspenseList");
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_FORWARD_REF_TYPE: return describeFunctionComponentFrame(type.render);
					case REACT_MEMO_TYPE: return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
						} catch (x) {}
				}
				return "";
			}
			function describeFiber(fiber) {
				fiber._debugOwner && fiber._debugOwner.type;
				fiber._debugSource;
				switch (fiber.tag) {
					case HostComponent: return describeBuiltInComponentFrame(fiber.type);
					case LazyComponent: return describeBuiltInComponentFrame("Lazy");
					case SuspenseComponent: return describeBuiltInComponentFrame("Suspense");
					case SuspenseListComponent: return describeBuiltInComponentFrame("SuspenseList");
					case FunctionComponent:
					case IndeterminateComponent:
					case SimpleMemoComponent: return describeFunctionComponentFrame(fiber.type);
					case ForwardRef: return describeFunctionComponentFrame(fiber.type.render);
					case ClassComponent: return describeClassComponentFrame(fiber.type);
					default: return "";
				}
			}
			function getStackByFiberInDevAndProd(workInProgress) {
				try {
					var info = "";
					var node = workInProgress;
					do {
						info += describeFiber(node);
						node = node.return;
					} while (node);
					return info;
				} catch (x) {
					return "\nError generating stack: " + x.message + "\n" + x.stack;
				}
			}
			function getWrappedName(outerType, innerType, wrapperName) {
				var displayName = outerType.displayName;
				if (displayName) return displayName;
				var functionName = innerType.displayName || innerType.name || "";
				return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
			}
			function getContextName(type) {
				return type.displayName || "Context";
			}
			function getComponentNameFromType(type) {
				if (type == null) return null;
				if (typeof type.tag === "number") error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
				if (typeof type === "function") return type.displayName || type.name || null;
				if (typeof type === "string") return type;
				switch (type) {
					case REACT_FRAGMENT_TYPE: return "Fragment";
					case REACT_PORTAL_TYPE: return "Portal";
					case REACT_PROFILER_TYPE: return "Profiler";
					case REACT_STRICT_MODE_TYPE: return "StrictMode";
					case REACT_SUSPENSE_TYPE: return "Suspense";
					case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_CONTEXT_TYPE: return getContextName(type) + ".Consumer";
					case REACT_PROVIDER_TYPE: return getContextName(type._context) + ".Provider";
					case REACT_FORWARD_REF_TYPE: return getWrappedName(type, type.render, "ForwardRef");
					case REACT_MEMO_TYPE:
						var outerName = type.displayName || null;
						if (outerName !== null) return outerName;
						return getComponentNameFromType(type.type) || "Memo";
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return getComponentNameFromType(init(payload));
						} catch (x) {
							return null;
						}
				}
				return null;
			}
			function getWrappedName$1(outerType, innerType, wrapperName) {
				var functionName = innerType.displayName || innerType.name || "";
				return outerType.displayName || (functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName);
			}
			function getContextName$1(type) {
				return type.displayName || "Context";
			}
			function getComponentNameFromFiber(fiber) {
				var tag = fiber.tag, type = fiber.type;
				switch (tag) {
					case CacheComponent: return "Cache";
					case ContextConsumer: return getContextName$1(type) + ".Consumer";
					case ContextProvider: return getContextName$1(type._context) + ".Provider";
					case DehydratedFragment: return "DehydratedFragment";
					case ForwardRef: return getWrappedName$1(type, type.render, "ForwardRef");
					case Fragment: return "Fragment";
					case HostComponent: return type;
					case HostPortal: return "Portal";
					case HostRoot: return "Root";
					case HostText: return "Text";
					case LazyComponent: return getComponentNameFromType(type);
					case Mode:
						if (type === REACT_STRICT_MODE_TYPE) return "StrictMode";
						return "Mode";
					case OffscreenComponent: return "Offscreen";
					case Profiler: return "Profiler";
					case ScopeComponent: return "Scope";
					case SuspenseComponent: return "Suspense";
					case SuspenseListComponent: return "SuspenseList";
					case TracingMarkerComponent: return "TracingMarker";
					case ClassComponent:
					case FunctionComponent:
					case IncompleteClassComponent:
					case IndeterminateComponent:
					case MemoComponent:
					case SimpleMemoComponent:
						if (typeof type === "function") return type.displayName || type.name || null;
						if (typeof type === "string") return type;
				}
				return null;
			}
			var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
			var current = null;
			var isRendering = false;
			function getCurrentFiberOwnerNameInDevOrNull() {
				if (current === null) return null;
				var owner = current._debugOwner;
				if (owner !== null && typeof owner !== "undefined") return getComponentNameFromFiber(owner);
				return null;
			}
			function getCurrentFiberStackInDev() {
				if (current === null) return "";
				return getStackByFiberInDevAndProd(current);
			}
			function resetCurrentFiber() {
				ReactDebugCurrentFrame.getCurrentStack = null;
				current = null;
				isRendering = false;
			}
			function setCurrentFiber(fiber) {
				ReactDebugCurrentFrame.getCurrentStack = fiber === null ? null : getCurrentFiberStackInDev;
				current = fiber;
				isRendering = false;
			}
			function getCurrentFiber() {
				return current;
			}
			function setIsRendering(rendering) {
				isRendering = rendering;
			}
			function toString(value) {
				return "" + value;
			}
			function getToStringValue(value) {
				switch (typeof value) {
					case "boolean":
					case "number":
					case "string":
					case "undefined": return value;
					case "object":
						checkFormFieldValueStringCoercion(value);
						return value;
					default: return "";
				}
			}
			var hasReadOnlyValue = {
				button: true,
				checkbox: true,
				image: true,
				hidden: true,
				radio: true,
				reset: true,
				submit: true
			};
			function checkControlledValueProps(tagName, props) {
				if (!(hasReadOnlyValue[props.type] || props.onChange || props.onInput || props.readOnly || props.disabled || props.value == null)) error("You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set either `onChange` or `readOnly`.");
				if (!(props.onChange || props.readOnly || props.disabled || props.checked == null)) error("You provided a `checked` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultChecked`. Otherwise, set either `onChange` or `readOnly`.");
			}
			function isCheckable(elem) {
				var type = elem.type;
				var nodeName = elem.nodeName;
				return nodeName && nodeName.toLowerCase() === "input" && (type === "checkbox" || type === "radio");
			}
			function getTracker(node) {
				return node._valueTracker;
			}
			function detachTracker(node) {
				node._valueTracker = null;
			}
			function getValueFromNode(node) {
				var value = "";
				if (!node) return value;
				if (isCheckable(node)) value = node.checked ? "true" : "false";
				else value = node.value;
				return value;
			}
			function trackValueOnNode(node) {
				var valueField = isCheckable(node) ? "checked" : "value";
				var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
				checkFormFieldValueStringCoercion(node[valueField]);
				var currentValue = "" + node[valueField];
				if (node.hasOwnProperty(valueField) || typeof descriptor === "undefined" || typeof descriptor.get !== "function" || typeof descriptor.set !== "function") return;
				var get = descriptor.get, set = descriptor.set;
				Object.defineProperty(node, valueField, {
					configurable: true,
					get: function() {
						return get.call(this);
					},
					set: function(value) {
						checkFormFieldValueStringCoercion(value);
						currentValue = "" + value;
						set.call(this, value);
					}
				});
				Object.defineProperty(node, valueField, { enumerable: descriptor.enumerable });
				return {
					getValue: function() {
						return currentValue;
					},
					setValue: function(value) {
						checkFormFieldValueStringCoercion(value);
						currentValue = "" + value;
					},
					stopTracking: function() {
						detachTracker(node);
						delete node[valueField];
					}
				};
			}
			function track(node) {
				if (getTracker(node)) return;
				node._valueTracker = trackValueOnNode(node);
			}
			function updateValueIfChanged(node) {
				if (!node) return false;
				var tracker = getTracker(node);
				if (!tracker) return true;
				var lastValue = tracker.getValue();
				var nextValue = getValueFromNode(node);
				if (nextValue !== lastValue) {
					tracker.setValue(nextValue);
					return true;
				}
				return false;
			}
			function getActiveElement(doc) {
				doc = doc || (typeof document !== "undefined" ? document : void 0);
				if (typeof doc === "undefined") return null;
				try {
					return doc.activeElement || doc.body;
				} catch (e) {
					return doc.body;
				}
			}
			var didWarnValueDefaultValue = false;
			var didWarnCheckedDefaultChecked = false;
			var didWarnControlledToUncontrolled = false;
			var didWarnUncontrolledToControlled = false;
			function isControlled(props) {
				return props.type === "checkbox" || props.type === "radio" ? props.checked != null : props.value != null;
			}
			/**
			* Implements an <input> host component that allows setting these optional
			* props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
			*
			* If `checked` or `value` are not supplied (or null/undefined), user actions
			* that affect the checked state or value will trigger updates to the element.
			*
			* If they are supplied (and not null/undefined), the rendered element will not
			* trigger updates to the element. Instead, the props must change in order for
			* the rendered element to be updated.
			*
			* The rendered element will be initialized as unchecked (or `defaultChecked`)
			* with an empty value (or `defaultValue`).
			*
			* See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
			*/
			function getHostProps(element, props) {
				var node = element;
				var checked = props.checked;
				return assign({}, props, {
					defaultChecked: void 0,
					defaultValue: void 0,
					value: void 0,
					checked: checked != null ? checked : node._wrapperState.initialChecked
				});
			}
			function initWrapperState(element, props) {
				checkControlledValueProps("input", props);
				if (props.checked !== void 0 && props.defaultChecked !== void 0 && !didWarnCheckedDefaultChecked) {
					error("%s contains an input of type %s with both checked and defaultChecked props. Input elements must be either controlled or uncontrolled (specify either the checked prop, or the defaultChecked prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type);
					didWarnCheckedDefaultChecked = true;
				}
				if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValueDefaultValue) {
					error("%s contains an input of type %s with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type);
					didWarnValueDefaultValue = true;
				}
				var node = element;
				var defaultValue = props.defaultValue == null ? "" : props.defaultValue;
				node._wrapperState = {
					initialChecked: props.checked != null ? props.checked : props.defaultChecked,
					initialValue: getToStringValue(props.value != null ? props.value : defaultValue),
					controlled: isControlled(props)
				};
			}
			function updateChecked(element, props) {
				var node = element;
				var checked = props.checked;
				if (checked != null) setValueForProperty(node, "checked", checked, false);
			}
			function updateWrapper(element, props) {
				var node = element;
				var controlled = isControlled(props);
				if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
					error("A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
					didWarnUncontrolledToControlled = true;
				}
				if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
					error("A component is changing a controlled input to be uncontrolled. This is likely caused by the value changing from a defined to undefined, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://reactjs.org/link/controlled-components");
					didWarnControlledToUncontrolled = true;
				}
				updateChecked(element, props);
				var value = getToStringValue(props.value);
				var type = props.type;
				if (value != null) {
					if (type === "number") {
						if (value === 0 && node.value === "" || node.value != value) node.value = toString(value);
					} else if (node.value !== toString(value)) node.value = toString(value);
				} else if (type === "submit" || type === "reset") {
					node.removeAttribute("value");
					return;
				}
				if (props.hasOwnProperty("value")) setDefaultValue(node, props.type, value);
				else if (props.hasOwnProperty("defaultValue")) setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
				if (props.checked == null && props.defaultChecked != null) node.defaultChecked = !!props.defaultChecked;
			}
			function postMountWrapper(element, props, isHydrating) {
				var node = element;
				if (props.hasOwnProperty("value") || props.hasOwnProperty("defaultValue")) {
					var type = props.type;
					if ((type === "submit" || type === "reset") && (props.value === void 0 || props.value === null)) return;
					var initialValue = toString(node._wrapperState.initialValue);
					if (!isHydrating) {
						if (initialValue !== node.value) node.value = initialValue;
					}
					node.defaultValue = initialValue;
				}
				var name = node.name;
				if (name !== "") node.name = "";
				node.defaultChecked = !node.defaultChecked;
				node.defaultChecked = !!node._wrapperState.initialChecked;
				if (name !== "") node.name = name;
			}
			function restoreControlledState(element, props) {
				var node = element;
				updateWrapper(node, props);
				updateNamedCousins(node, props);
			}
			function updateNamedCousins(rootNode, props) {
				var name = props.name;
				if (props.type === "radio" && name != null) {
					var queryRoot = rootNode;
					while (queryRoot.parentNode) queryRoot = queryRoot.parentNode;
					checkAttributeStringCoercion(name, "name");
					var group = queryRoot.querySelectorAll("input[name=" + JSON.stringify("" + name) + "][type=\"radio\"]");
					for (var i = 0; i < group.length; i++) {
						var otherNode = group[i];
						if (otherNode === rootNode || otherNode.form !== rootNode.form) continue;
						var otherProps = getFiberCurrentPropsFromNode(otherNode);
						if (!otherProps) throw new Error("ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.");
						updateValueIfChanged(otherNode);
						updateWrapper(otherNode, otherProps);
					}
				}
			}
			function setDefaultValue(node, type, value) {
				if (type !== "number" || getActiveElement(node.ownerDocument) !== node) {
					if (value == null) node.defaultValue = toString(node._wrapperState.initialValue);
					else if (node.defaultValue !== toString(value)) node.defaultValue = toString(value);
				}
			}
			var didWarnSelectedSetOnOption = false;
			var didWarnInvalidChild = false;
			var didWarnInvalidInnerHTML = false;
			/**
			* Implements an <option> host component that warns when `selected` is set.
			*/
			function validateProps(element, props) {
				if (props.value == null) {
					if (typeof props.children === "object" && props.children !== null) React.Children.forEach(props.children, function(child) {
						if (child == null) return;
						if (typeof child === "string" || typeof child === "number") return;
						if (!didWarnInvalidChild) {
							didWarnInvalidChild = true;
							error("Cannot infer the option value of complex children. Pass a `value` prop or use a plain string as children to <option>.");
						}
					});
					else if (props.dangerouslySetInnerHTML != null) {
						if (!didWarnInvalidInnerHTML) {
							didWarnInvalidInnerHTML = true;
							error("Pass a `value` prop if you set dangerouslyInnerHTML so React knows which value should be selected.");
						}
					}
				}
				if (props.selected != null && !didWarnSelectedSetOnOption) {
					error("Use the `defaultValue` or `value` props on <select> instead of setting `selected` on <option>.");
					didWarnSelectedSetOnOption = true;
				}
			}
			function postMountWrapper$1(element, props) {
				if (props.value != null) element.setAttribute("value", toString(getToStringValue(props.value)));
			}
			var isArrayImpl = Array.isArray;
			function isArray(a) {
				return isArrayImpl(a);
			}
			var didWarnValueDefaultValue$1 = false;
			function getDeclarationErrorAddendum() {
				var ownerName = getCurrentFiberOwnerNameInDevOrNull();
				if (ownerName) return "\n\nCheck the render method of `" + ownerName + "`.";
				return "";
			}
			var valuePropNames = ["value", "defaultValue"];
			/**
			* Validation function for `value` and `defaultValue`.
			*/
			function checkSelectPropTypes(props) {
				checkControlledValueProps("select", props);
				for (var i = 0; i < valuePropNames.length; i++) {
					var propName = valuePropNames[i];
					if (props[propName] == null) continue;
					var propNameIsArray = isArray(props[propName]);
					if (props.multiple && !propNameIsArray) error("The `%s` prop supplied to <select> must be an array if `multiple` is true.%s", propName, getDeclarationErrorAddendum());
					else if (!props.multiple && propNameIsArray) error("The `%s` prop supplied to <select> must be a scalar value if `multiple` is false.%s", propName, getDeclarationErrorAddendum());
				}
			}
			function updateOptions(node, multiple, propValue, setDefaultSelected) {
				var options = node.options;
				if (multiple) {
					var selectedValues = propValue;
					var selectedValue = {};
					for (var i = 0; i < selectedValues.length; i++) selectedValue["$" + selectedValues[i]] = true;
					for (var _i = 0; _i < options.length; _i++) {
						var selected = selectedValue.hasOwnProperty("$" + options[_i].value);
						if (options[_i].selected !== selected) options[_i].selected = selected;
						if (selected && setDefaultSelected) options[_i].defaultSelected = true;
					}
				} else {
					var _selectedValue = toString(getToStringValue(propValue));
					var defaultSelected = null;
					for (var _i2 = 0; _i2 < options.length; _i2++) {
						if (options[_i2].value === _selectedValue) {
							options[_i2].selected = true;
							if (setDefaultSelected) options[_i2].defaultSelected = true;
							return;
						}
						if (defaultSelected === null && !options[_i2].disabled) defaultSelected = options[_i2];
					}
					if (defaultSelected !== null) defaultSelected.selected = true;
				}
			}
			/**
			* Implements a <select> host component that allows optionally setting the
			* props `value` and `defaultValue`. If `multiple` is false, the prop must be a
			* stringable. If `multiple` is true, the prop must be an array of stringables.
			*
			* If `value` is not supplied (or null/undefined), user actions that change the
			* selected option will trigger updates to the rendered options.
			*
			* If it is supplied (and not null/undefined), the rendered options will not
			* update in response to user actions. Instead, the `value` prop must change in
			* order for the rendered options to update.
			*
			* If `defaultValue` is provided, any options with the supplied values will be
			* selected.
			*/
			function getHostProps$1(element, props) {
				return assign({}, props, { value: void 0 });
			}
			function initWrapperState$1(element, props) {
				var node = element;
				checkSelectPropTypes(props);
				node._wrapperState = { wasMultiple: !!props.multiple };
				if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValueDefaultValue$1) {
					error("Select elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled select element and remove one of these props. More info: https://reactjs.org/link/controlled-components");
					didWarnValueDefaultValue$1 = true;
				}
			}
			function postMountWrapper$2(element, props) {
				var node = element;
				node.multiple = !!props.multiple;
				var value = props.value;
				if (value != null) updateOptions(node, !!props.multiple, value, false);
				else if (props.defaultValue != null) updateOptions(node, !!props.multiple, props.defaultValue, true);
			}
			function postUpdateWrapper(element, props) {
				var node = element;
				var wasMultiple = node._wrapperState.wasMultiple;
				node._wrapperState.wasMultiple = !!props.multiple;
				var value = props.value;
				if (value != null) updateOptions(node, !!props.multiple, value, false);
				else if (wasMultiple !== !!props.multiple) {
					if (props.defaultValue != null) updateOptions(node, !!props.multiple, props.defaultValue, true);
					else updateOptions(node, !!props.multiple, props.multiple ? [] : "", false);
				}
			}
			function restoreControlledState$1(element, props) {
				var node = element;
				var value = props.value;
				if (value != null) updateOptions(node, !!props.multiple, value, false);
			}
			var didWarnValDefaultVal = false;
			/**
			* Implements a <textarea> host component that allows setting `value`, and
			* `defaultValue`. This differs from the traditional DOM API because value is
			* usually set as PCDATA children.
			*
			* If `value` is not supplied (or null/undefined), user actions that affect the
			* value will trigger updates to the element.
			*
			* If `value` is supplied (and not null/undefined), the rendered element will
			* not trigger updates to the element. Instead, the `value` prop must change in
			* order for the rendered element to be updated.
			*
			* The rendered element will be initialized with an empty value, the prop
			* `defaultValue` if specified, or the children content (deprecated).
			*/
			function getHostProps$2(element, props) {
				var node = element;
				if (props.dangerouslySetInnerHTML != null) throw new Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
				return assign({}, props, {
					value: void 0,
					defaultValue: void 0,
					children: toString(node._wrapperState.initialValue)
				});
			}
			function initWrapperState$2(element, props) {
				var node = element;
				checkControlledValueProps("textarea", props);
				if (props.value !== void 0 && props.defaultValue !== void 0 && !didWarnValDefaultVal) {
					error("%s contains a textarea with both value and defaultValue props. Textarea elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled textarea and remove one of these props. More info: https://reactjs.org/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component");
					didWarnValDefaultVal = true;
				}
				var initialValue = props.value;
				if (initialValue == null) {
					var children = props.children, defaultValue = props.defaultValue;
					if (children != null) {
						error("Use the `defaultValue` or `value` props instead of setting children on <textarea>.");
						if (defaultValue != null) throw new Error("If you supply `defaultValue` on a <textarea>, do not pass children.");
						if (isArray(children)) {
							if (children.length > 1) throw new Error("<textarea> can only have at most one child.");
							children = children[0];
						}
						defaultValue = children;
					}
					if (defaultValue == null) defaultValue = "";
					initialValue = defaultValue;
				}
				node._wrapperState = { initialValue: getToStringValue(initialValue) };
			}
			function updateWrapper$1(element, props) {
				var node = element;
				var value = getToStringValue(props.value);
				var defaultValue = getToStringValue(props.defaultValue);
				if (value != null) {
					var newValue = toString(value);
					if (newValue !== node.value) node.value = newValue;
					if (props.defaultValue == null && node.defaultValue !== newValue) node.defaultValue = newValue;
				}
				if (defaultValue != null) node.defaultValue = toString(defaultValue);
			}
			function postMountWrapper$3(element, props) {
				var node = element;
				var textContent = node.textContent;
				if (textContent === node._wrapperState.initialValue) {
					if (textContent !== "" && textContent !== null) node.value = textContent;
				}
			}
			function restoreControlledState$2(element, props) {
				updateWrapper$1(element, props);
			}
			var HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
			var MATH_NAMESPACE = "http://www.w3.org/1998/Math/MathML";
			var SVG_NAMESPACE = "http://www.w3.org/2000/svg";
			function getIntrinsicNamespace(type) {
				switch (type) {
					case "svg": return SVG_NAMESPACE;
					case "math": return MATH_NAMESPACE;
					default: return HTML_NAMESPACE;
				}
			}
			function getChildNamespace(parentNamespace, type) {
				if (parentNamespace == null || parentNamespace === HTML_NAMESPACE) return getIntrinsicNamespace(type);
				if (parentNamespace === SVG_NAMESPACE && type === "foreignObject") return HTML_NAMESPACE;
				return parentNamespace;
			}
			/**
			* Create a function which has 'unsafe' privileges (required by windows8 apps)
			*/
			var createMicrosoftUnsafeLocalFunction = function(func) {
				if (typeof MSApp !== "undefined" && MSApp.execUnsafeLocalFunction) return function(arg0, arg1, arg2, arg3) {
					MSApp.execUnsafeLocalFunction(function() {
						return func(arg0, arg1, arg2, arg3);
					});
				};
				else return func;
			};
			var reusableSVGContainer;
			/**
			* Set the innerHTML property of a node
			*
			* @param {DOMElement} node
			* @param {string} html
			* @internal
			*/
			var setInnerHTML = createMicrosoftUnsafeLocalFunction(function(node, html) {
				if (node.namespaceURI === SVG_NAMESPACE) {
					if (!("innerHTML" in node)) {
						reusableSVGContainer = reusableSVGContainer || document.createElement("div");
						reusableSVGContainer.innerHTML = "<svg>" + html.valueOf().toString() + "</svg>";
						var svgNode = reusableSVGContainer.firstChild;
						while (node.firstChild) node.removeChild(node.firstChild);
						while (svgNode.firstChild) node.appendChild(svgNode.firstChild);
						return;
					}
				}
				node.innerHTML = html;
			});
			/**
			* HTML nodeType values that represent the type of the node
			*/
			var ELEMENT_NODE = 1;
			var TEXT_NODE = 3;
			var COMMENT_NODE = 8;
			var DOCUMENT_NODE = 9;
			var DOCUMENT_FRAGMENT_NODE = 11;
			/**
			* Set the textContent property of a node. For text updates, it's faster
			* to set the `nodeValue` of the Text node directly instead of using
			* `.textContent` which will remove the existing node and create a new one.
			*
			* @param {DOMElement} node
			* @param {string} text
			* @internal
			*/
			var setTextContent = function(node, text) {
				if (text) {
					var firstChild = node.firstChild;
					if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
						firstChild.nodeValue = text;
						return;
					}
				}
				node.textContent = text;
			};
			var shorthandToLonghand = {
				animation: [
					"animationDelay",
					"animationDirection",
					"animationDuration",
					"animationFillMode",
					"animationIterationCount",
					"animationName",
					"animationPlayState",
					"animationTimingFunction"
				],
				background: [
					"backgroundAttachment",
					"backgroundClip",
					"backgroundColor",
					"backgroundImage",
					"backgroundOrigin",
					"backgroundPositionX",
					"backgroundPositionY",
					"backgroundRepeat",
					"backgroundSize"
				],
				backgroundPosition: ["backgroundPositionX", "backgroundPositionY"],
				border: [
					"borderBottomColor",
					"borderBottomStyle",
					"borderBottomWidth",
					"borderImageOutset",
					"borderImageRepeat",
					"borderImageSlice",
					"borderImageSource",
					"borderImageWidth",
					"borderLeftColor",
					"borderLeftStyle",
					"borderLeftWidth",
					"borderRightColor",
					"borderRightStyle",
					"borderRightWidth",
					"borderTopColor",
					"borderTopStyle",
					"borderTopWidth"
				],
				borderBlockEnd: [
					"borderBlockEndColor",
					"borderBlockEndStyle",
					"borderBlockEndWidth"
				],
				borderBlockStart: [
					"borderBlockStartColor",
					"borderBlockStartStyle",
					"borderBlockStartWidth"
				],
				borderBottom: [
					"borderBottomColor",
					"borderBottomStyle",
					"borderBottomWidth"
				],
				borderColor: [
					"borderBottomColor",
					"borderLeftColor",
					"borderRightColor",
					"borderTopColor"
				],
				borderImage: [
					"borderImageOutset",
					"borderImageRepeat",
					"borderImageSlice",
					"borderImageSource",
					"borderImageWidth"
				],
				borderInlineEnd: [
					"borderInlineEndColor",
					"borderInlineEndStyle",
					"borderInlineEndWidth"
				],
				borderInlineStart: [
					"borderInlineStartColor",
					"borderInlineStartStyle",
					"borderInlineStartWidth"
				],
				borderLeft: [
					"borderLeftColor",
					"borderLeftStyle",
					"borderLeftWidth"
				],
				borderRadius: [
					"borderBottomLeftRadius",
					"borderBottomRightRadius",
					"borderTopLeftRadius",
					"borderTopRightRadius"
				],
				borderRight: [
					"borderRightColor",
					"borderRightStyle",
					"borderRightWidth"
				],
				borderStyle: [
					"borderBottomStyle",
					"borderLeftStyle",
					"borderRightStyle",
					"borderTopStyle"
				],
				borderTop: [
					"borderTopColor",
					"borderTopStyle",
					"borderTopWidth"
				],
				borderWidth: [
					"borderBottomWidth",
					"borderLeftWidth",
					"borderRightWidth",
					"borderTopWidth"
				],
				columnRule: [
					"columnRuleColor",
					"columnRuleStyle",
					"columnRuleWidth"
				],
				columns: ["columnCount", "columnWidth"],
				flex: [
					"flexBasis",
					"flexGrow",
					"flexShrink"
				],
				flexFlow: ["flexDirection", "flexWrap"],
				font: [
					"fontFamily",
					"fontFeatureSettings",
					"fontKerning",
					"fontLanguageOverride",
					"fontSize",
					"fontSizeAdjust",
					"fontStretch",
					"fontStyle",
					"fontVariant",
					"fontVariantAlternates",
					"fontVariantCaps",
					"fontVariantEastAsian",
					"fontVariantLigatures",
					"fontVariantNumeric",
					"fontVariantPosition",
					"fontWeight",
					"lineHeight"
				],
				fontVariant: [
					"fontVariantAlternates",
					"fontVariantCaps",
					"fontVariantEastAsian",
					"fontVariantLigatures",
					"fontVariantNumeric",
					"fontVariantPosition"
				],
				gap: ["columnGap", "rowGap"],
				grid: [
					"gridAutoColumns",
					"gridAutoFlow",
					"gridAutoRows",
					"gridTemplateAreas",
					"gridTemplateColumns",
					"gridTemplateRows"
				],
				gridArea: [
					"gridColumnEnd",
					"gridColumnStart",
					"gridRowEnd",
					"gridRowStart"
				],
				gridColumn: ["gridColumnEnd", "gridColumnStart"],
				gridColumnGap: ["columnGap"],
				gridGap: ["columnGap", "rowGap"],
				gridRow: ["gridRowEnd", "gridRowStart"],
				gridRowGap: ["rowGap"],
				gridTemplate: [
					"gridTemplateAreas",
					"gridTemplateColumns",
					"gridTemplateRows"
				],
				listStyle: [
					"listStyleImage",
					"listStylePosition",
					"listStyleType"
				],
				margin: [
					"marginBottom",
					"marginLeft",
					"marginRight",
					"marginTop"
				],
				marker: [
					"markerEnd",
					"markerMid",
					"markerStart"
				],
				mask: [
					"maskClip",
					"maskComposite",
					"maskImage",
					"maskMode",
					"maskOrigin",
					"maskPositionX",
					"maskPositionY",
					"maskRepeat",
					"maskSize"
				],
				maskPosition: ["maskPositionX", "maskPositionY"],
				outline: [
					"outlineColor",
					"outlineStyle",
					"outlineWidth"
				],
				overflow: ["overflowX", "overflowY"],
				padding: [
					"paddingBottom",
					"paddingLeft",
					"paddingRight",
					"paddingTop"
				],
				placeContent: ["alignContent", "justifyContent"],
				placeItems: ["alignItems", "justifyItems"],
				placeSelf: ["alignSelf", "justifySelf"],
				textDecoration: [
					"textDecorationColor",
					"textDecorationLine",
					"textDecorationStyle"
				],
				textEmphasis: ["textEmphasisColor", "textEmphasisStyle"],
				transition: [
					"transitionDelay",
					"transitionDuration",
					"transitionProperty",
					"transitionTimingFunction"
				],
				wordWrap: ["overflowWrap"]
			};
			/**
			* CSS properties which accept numbers but are not in units of "px".
			*/
			var isUnitlessNumber = {
				animationIterationCount: true,
				aspectRatio: true,
				borderImageOutset: true,
				borderImageSlice: true,
				borderImageWidth: true,
				boxFlex: true,
				boxFlexGroup: true,
				boxOrdinalGroup: true,
				columnCount: true,
				columns: true,
				flex: true,
				flexGrow: true,
				flexPositive: true,
				flexShrink: true,
				flexNegative: true,
				flexOrder: true,
				gridArea: true,
				gridRow: true,
				gridRowEnd: true,
				gridRowSpan: true,
				gridRowStart: true,
				gridColumn: true,
				gridColumnEnd: true,
				gridColumnSpan: true,
				gridColumnStart: true,
				fontWeight: true,
				lineClamp: true,
				lineHeight: true,
				opacity: true,
				order: true,
				orphans: true,
				tabSize: true,
				widows: true,
				zIndex: true,
				zoom: true,
				fillOpacity: true,
				floodOpacity: true,
				stopOpacity: true,
				strokeDasharray: true,
				strokeDashoffset: true,
				strokeMiterlimit: true,
				strokeOpacity: true,
				strokeWidth: true
			};
			/**
			* @param {string} prefix vendor-specific prefix, eg: Webkit
			* @param {string} key style name, eg: transitionDuration
			* @return {string} style name prefixed with `prefix`, properly camelCased, eg:
			* WebkitTransitionDuration
			*/
			function prefixKey(prefix, key) {
				return prefix + key.charAt(0).toUpperCase() + key.substring(1);
			}
			/**
			* Support style names that may come passed in prefixed by adding permutations
			* of vendor prefixes.
			*/
			var prefixes = [
				"Webkit",
				"ms",
				"Moz",
				"O"
			];
			Object.keys(isUnitlessNumber).forEach(function(prop) {
				prefixes.forEach(function(prefix) {
					isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
				});
			});
			/**
			* Convert a value into the proper css writable value. The style name `name`
			* should be logical (no hyphens), as specified
			* in `CSSProperty.isUnitlessNumber`.
			*
			* @param {string} name CSS property name such as `topMargin`.
			* @param {*} value CSS property value such as `10px`.
			* @return {string} Normalized style value with dimensions applied.
			*/
			function dangerousStyleValue(name, value, isCustomProperty) {
				if (value == null || typeof value === "boolean" || value === "") return "";
				if (!isCustomProperty && typeof value === "number" && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) return value + "px";
				checkCSSPropertyStringCoercion(value, name);
				return ("" + value).trim();
			}
			var uppercasePattern = /([A-Z])/g;
			var msPattern = /^ms-/;
			/**
			* Hyphenates a camelcased CSS property name, for example:
			*
			*   > hyphenateStyleName('backgroundColor')
			*   < "background-color"
			*   > hyphenateStyleName('MozTransition')
			*   < "-moz-transition"
			*   > hyphenateStyleName('msTransition')
			*   < "-ms-transition"
			*
			* As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
			* is converted to `-ms-`.
			*/
			function hyphenateStyleName(name) {
				return name.replace(uppercasePattern, "-$1").toLowerCase().replace(msPattern, "-ms-");
			}
			var warnValidStyle = function() {};
			var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;
			var msPattern$1 = /^-ms-/;
			var hyphenPattern = /-(.)/g;
			var badStyleValueWithSemicolonPattern = /;\s*$/;
			var warnedStyleNames = {};
			var warnedStyleValues = {};
			var warnedForNaNValue = false;
			var warnedForInfinityValue = false;
			var camelize = function(string) {
				return string.replace(hyphenPattern, function(_, character) {
					return character.toUpperCase();
				});
			};
			var warnHyphenatedStyleName = function(name) {
				if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) return;
				warnedStyleNames[name] = true;
				error("Unsupported style property %s. Did you mean %s?", name, camelize(name.replace(msPattern$1, "ms-")));
			};
			var warnBadVendoredStyleName = function(name) {
				if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) return;
				warnedStyleNames[name] = true;
				error("Unsupported vendor-prefixed style property %s. Did you mean %s?", name, name.charAt(0).toUpperCase() + name.slice(1));
			};
			var warnStyleValueWithSemicolon = function(name, value) {
				if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) return;
				warnedStyleValues[value] = true;
				error("Style property values shouldn't contain a semicolon. Try \"%s: %s\" instead.", name, value.replace(badStyleValueWithSemicolonPattern, ""));
			};
			var warnStyleValueIsNaN = function(name, value) {
				if (warnedForNaNValue) return;
				warnedForNaNValue = true;
				error("`NaN` is an invalid value for the `%s` css style property.", name);
			};
			var warnStyleValueIsInfinity = function(name, value) {
				if (warnedForInfinityValue) return;
				warnedForInfinityValue = true;
				error("`Infinity` is an invalid value for the `%s` css style property.", name);
			};
			warnValidStyle = function(name, value) {
				if (name.indexOf("-") > -1) warnHyphenatedStyleName(name);
				else if (badVendoredStyleNamePattern.test(name)) warnBadVendoredStyleName(name);
				else if (badStyleValueWithSemicolonPattern.test(value)) warnStyleValueWithSemicolon(name, value);
				if (typeof value === "number") {
					if (isNaN(value)) warnStyleValueIsNaN(name, value);
					else if (!isFinite(value)) warnStyleValueIsInfinity(name, value);
				}
			};
			var warnValidStyle$1 = warnValidStyle;
			/**
			* Operations for dealing with CSS properties.
			*/
			/**
			* This creates a string that is expected to be equivalent to the style
			* attribute generated by server-side rendering. It by-passes warnings and
			* security checks so it's not safe to use this value for anything other than
			* comparison. It is only used in DEV for SSR validation.
			*/
			function createDangerousStringForStyles(styles) {
				var serialized = "";
				var delimiter = "";
				for (var styleName in styles) {
					if (!styles.hasOwnProperty(styleName)) continue;
					var styleValue = styles[styleName];
					if (styleValue != null) {
						var isCustomProperty = styleName.indexOf("--") === 0;
						serialized += delimiter + (isCustomProperty ? styleName : hyphenateStyleName(styleName)) + ":";
						serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);
						delimiter = ";";
					}
				}
				return serialized || null;
			}
			/**
			* Sets the value for multiple styles on a node.  If a value is specified as
			* '' (empty string), the corresponding style property will be unset.
			*
			* @param {DOMElement} node
			* @param {object} styles
			*/
			function setValueForStyles(node, styles) {
				var style = node.style;
				for (var styleName in styles) {
					if (!styles.hasOwnProperty(styleName)) continue;
					var isCustomProperty = styleName.indexOf("--") === 0;
					if (!isCustomProperty) warnValidStyle$1(styleName, styles[styleName]);
					var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
					if (styleName === "float") styleName = "cssFloat";
					if (isCustomProperty) style.setProperty(styleName, styleValue);
					else style[styleName] = styleValue;
				}
			}
			function isValueEmpty(value) {
				return value == null || typeof value === "boolean" || value === "";
			}
			/**
			* Given {color: 'red', overflow: 'hidden'} returns {
			*   color: 'color',
			*   overflowX: 'overflow',
			*   overflowY: 'overflow',
			* }. This can be read as "the overflowY property was set by the overflow
			* shorthand". That is, the values are the property that each was derived from.
			*/
			function expandShorthandMap(styles) {
				var expanded = {};
				for (var key in styles) {
					var longhands = shorthandToLonghand[key] || [key];
					for (var i = 0; i < longhands.length; i++) expanded[longhands[i]] = key;
				}
				return expanded;
			}
			/**
			* When mixing shorthand and longhand property names, we warn during updates if
			* we expect an incorrect result to occur. In particular, we warn for:
			*
			* Updating a shorthand property (longhand gets overwritten):
			*   {font: 'foo', fontVariant: 'bar'} -> {font: 'baz', fontVariant: 'bar'}
			*   becomes .style.font = 'baz'
			* Removing a shorthand property (longhand gets lost too):
			*   {font: 'foo', fontVariant: 'bar'} -> {fontVariant: 'bar'}
			*   becomes .style.font = ''
			* Removing a longhand property (should revert to shorthand; doesn't):
			*   {font: 'foo', fontVariant: 'bar'} -> {font: 'foo'}
			*   becomes .style.fontVariant = ''
			*/
			function validateShorthandPropertyCollisionInDev(styleUpdates, nextStyles) {
				if (!nextStyles) return;
				var expandedUpdates = expandShorthandMap(styleUpdates);
				var expandedStyles = expandShorthandMap(nextStyles);
				var warnedAbout = {};
				for (var key in expandedUpdates) {
					var originalKey = expandedUpdates[key];
					var correctOriginalKey = expandedStyles[key];
					if (correctOriginalKey && originalKey !== correctOriginalKey) {
						var warningKey = originalKey + "," + correctOriginalKey;
						if (warnedAbout[warningKey]) continue;
						warnedAbout[warningKey] = true;
						error("%s a style property during rerender (%s) when a conflicting property is set (%s) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.", isValueEmpty(styleUpdates[originalKey]) ? "Removing" : "Updating", originalKey, correctOriginalKey);
					}
				}
			}
			var voidElementTags = assign({ menuitem: true }, {
				area: true,
				base: true,
				br: true,
				col: true,
				embed: true,
				hr: true,
				img: true,
				input: true,
				keygen: true,
				link: true,
				meta: true,
				param: true,
				source: true,
				track: true,
				wbr: true
			});
			var HTML = "__html";
			function assertValidProps(tag, props) {
				if (!props) return;
				if (voidElementTags[tag]) {
					if (props.children != null || props.dangerouslySetInnerHTML != null) throw new Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
				}
				if (props.dangerouslySetInnerHTML != null) {
					if (props.children != null) throw new Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
					if (typeof props.dangerouslySetInnerHTML !== "object" || !(HTML in props.dangerouslySetInnerHTML)) throw new Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
				}
				if (!props.suppressContentEditableWarning && props.contentEditable && props.children != null) error("A component is `contentEditable` and contains `children` managed by React. It is now your responsibility to guarantee that none of those nodes are unexpectedly modified or duplicated. This is probably not intentional.");
				if (props.style != null && typeof props.style !== "object") throw new Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
			}
			function isCustomComponent(tagName, props) {
				if (tagName.indexOf("-") === -1) return typeof props.is === "string";
				switch (tagName) {
					case "annotation-xml":
					case "color-profile":
					case "font-face":
					case "font-face-src":
					case "font-face-uri":
					case "font-face-format":
					case "font-face-name":
					case "missing-glyph": return false;
					default: return true;
				}
			}
			var possibleStandardNames = {
				accept: "accept",
				acceptcharset: "acceptCharset",
				"accept-charset": "acceptCharset",
				accesskey: "accessKey",
				action: "action",
				allowfullscreen: "allowFullScreen",
				alt: "alt",
				as: "as",
				async: "async",
				autocapitalize: "autoCapitalize",
				autocomplete: "autoComplete",
				autocorrect: "autoCorrect",
				autofocus: "autoFocus",
				autoplay: "autoPlay",
				autosave: "autoSave",
				capture: "capture",
				cellpadding: "cellPadding",
				cellspacing: "cellSpacing",
				challenge: "challenge",
				charset: "charSet",
				checked: "checked",
				children: "children",
				cite: "cite",
				class: "className",
				classid: "classID",
				classname: "className",
				cols: "cols",
				colspan: "colSpan",
				content: "content",
				contenteditable: "contentEditable",
				contextmenu: "contextMenu",
				controls: "controls",
				controlslist: "controlsList",
				coords: "coords",
				crossorigin: "crossOrigin",
				dangerouslysetinnerhtml: "dangerouslySetInnerHTML",
				data: "data",
				datetime: "dateTime",
				default: "default",
				defaultchecked: "defaultChecked",
				defaultvalue: "defaultValue",
				defer: "defer",
				dir: "dir",
				disabled: "disabled",
				disablepictureinpicture: "disablePictureInPicture",
				disableremoteplayback: "disableRemotePlayback",
				download: "download",
				draggable: "draggable",
				enctype: "encType",
				enterkeyhint: "enterKeyHint",
				for: "htmlFor",
				form: "form",
				formmethod: "formMethod",
				formaction: "formAction",
				formenctype: "formEncType",
				formnovalidate: "formNoValidate",
				formtarget: "formTarget",
				frameborder: "frameBorder",
				headers: "headers",
				height: "height",
				hidden: "hidden",
				high: "high",
				href: "href",
				hreflang: "hrefLang",
				htmlfor: "htmlFor",
				httpequiv: "httpEquiv",
				"http-equiv": "httpEquiv",
				icon: "icon",
				id: "id",
				imagesizes: "imageSizes",
				imagesrcset: "imageSrcSet",
				innerhtml: "innerHTML",
				inputmode: "inputMode",
				integrity: "integrity",
				is: "is",
				itemid: "itemID",
				itemprop: "itemProp",
				itemref: "itemRef",
				itemscope: "itemScope",
				itemtype: "itemType",
				keyparams: "keyParams",
				keytype: "keyType",
				kind: "kind",
				label: "label",
				lang: "lang",
				list: "list",
				loop: "loop",
				low: "low",
				manifest: "manifest",
				marginwidth: "marginWidth",
				marginheight: "marginHeight",
				max: "max",
				maxlength: "maxLength",
				media: "media",
				mediagroup: "mediaGroup",
				method: "method",
				min: "min",
				minlength: "minLength",
				multiple: "multiple",
				muted: "muted",
				name: "name",
				nomodule: "noModule",
				nonce: "nonce",
				novalidate: "noValidate",
				open: "open",
				optimum: "optimum",
				pattern: "pattern",
				placeholder: "placeholder",
				playsinline: "playsInline",
				poster: "poster",
				preload: "preload",
				profile: "profile",
				radiogroup: "radioGroup",
				readonly: "readOnly",
				referrerpolicy: "referrerPolicy",
				rel: "rel",
				required: "required",
				reversed: "reversed",
				role: "role",
				rows: "rows",
				rowspan: "rowSpan",
				sandbox: "sandbox",
				scope: "scope",
				scoped: "scoped",
				scrolling: "scrolling",
				seamless: "seamless",
				selected: "selected",
				shape: "shape",
				size: "size",
				sizes: "sizes",
				span: "span",
				spellcheck: "spellCheck",
				src: "src",
				srcdoc: "srcDoc",
				srclang: "srcLang",
				srcset: "srcSet",
				start: "start",
				step: "step",
				style: "style",
				summary: "summary",
				tabindex: "tabIndex",
				target: "target",
				title: "title",
				type: "type",
				usemap: "useMap",
				value: "value",
				width: "width",
				wmode: "wmode",
				wrap: "wrap",
				about: "about",
				accentheight: "accentHeight",
				"accent-height": "accentHeight",
				accumulate: "accumulate",
				additive: "additive",
				alignmentbaseline: "alignmentBaseline",
				"alignment-baseline": "alignmentBaseline",
				allowreorder: "allowReorder",
				alphabetic: "alphabetic",
				amplitude: "amplitude",
				arabicform: "arabicForm",
				"arabic-form": "arabicForm",
				ascent: "ascent",
				attributename: "attributeName",
				attributetype: "attributeType",
				autoreverse: "autoReverse",
				azimuth: "azimuth",
				basefrequency: "baseFrequency",
				baselineshift: "baselineShift",
				"baseline-shift": "baselineShift",
				baseprofile: "baseProfile",
				bbox: "bbox",
				begin: "begin",
				bias: "bias",
				by: "by",
				calcmode: "calcMode",
				capheight: "capHeight",
				"cap-height": "capHeight",
				clip: "clip",
				clippath: "clipPath",
				"clip-path": "clipPath",
				clippathunits: "clipPathUnits",
				cliprule: "clipRule",
				"clip-rule": "clipRule",
				color: "color",
				colorinterpolation: "colorInterpolation",
				"color-interpolation": "colorInterpolation",
				colorinterpolationfilters: "colorInterpolationFilters",
				"color-interpolation-filters": "colorInterpolationFilters",
				colorprofile: "colorProfile",
				"color-profile": "colorProfile",
				colorrendering: "colorRendering",
				"color-rendering": "colorRendering",
				contentscripttype: "contentScriptType",
				contentstyletype: "contentStyleType",
				cursor: "cursor",
				cx: "cx",
				cy: "cy",
				d: "d",
				datatype: "datatype",
				decelerate: "decelerate",
				descent: "descent",
				diffuseconstant: "diffuseConstant",
				direction: "direction",
				display: "display",
				divisor: "divisor",
				dominantbaseline: "dominantBaseline",
				"dominant-baseline": "dominantBaseline",
				dur: "dur",
				dx: "dx",
				dy: "dy",
				edgemode: "edgeMode",
				elevation: "elevation",
				enablebackground: "enableBackground",
				"enable-background": "enableBackground",
				end: "end",
				exponent: "exponent",
				externalresourcesrequired: "externalResourcesRequired",
				fill: "fill",
				fillopacity: "fillOpacity",
				"fill-opacity": "fillOpacity",
				fillrule: "fillRule",
				"fill-rule": "fillRule",
				filter: "filter",
				filterres: "filterRes",
				filterunits: "filterUnits",
				floodopacity: "floodOpacity",
				"flood-opacity": "floodOpacity",
				floodcolor: "floodColor",
				"flood-color": "floodColor",
				focusable: "focusable",
				fontfamily: "fontFamily",
				"font-family": "fontFamily",
				fontsize: "fontSize",
				"font-size": "fontSize",
				fontsizeadjust: "fontSizeAdjust",
				"font-size-adjust": "fontSizeAdjust",
				fontstretch: "fontStretch",
				"font-stretch": "fontStretch",
				fontstyle: "fontStyle",
				"font-style": "fontStyle",
				fontvariant: "fontVariant",
				"font-variant": "fontVariant",
				fontweight: "fontWeight",
				"font-weight": "fontWeight",
				format: "format",
				from: "from",
				fx: "fx",
				fy: "fy",
				g1: "g1",
				g2: "g2",
				glyphname: "glyphName",
				"glyph-name": "glyphName",
				glyphorientationhorizontal: "glyphOrientationHorizontal",
				"glyph-orientation-horizontal": "glyphOrientationHorizontal",
				glyphorientationvertical: "glyphOrientationVertical",
				"glyph-orientation-vertical": "glyphOrientationVertical",
				glyphref: "glyphRef",
				gradienttransform: "gradientTransform",
				gradientunits: "gradientUnits",
				hanging: "hanging",
				horizadvx: "horizAdvX",
				"horiz-adv-x": "horizAdvX",
				horizoriginx: "horizOriginX",
				"horiz-origin-x": "horizOriginX",
				ideographic: "ideographic",
				imagerendering: "imageRendering",
				"image-rendering": "imageRendering",
				in2: "in2",
				in: "in",
				inlist: "inlist",
				intercept: "intercept",
				k1: "k1",
				k2: "k2",
				k3: "k3",
				k4: "k4",
				k: "k",
				kernelmatrix: "kernelMatrix",
				kernelunitlength: "kernelUnitLength",
				kerning: "kerning",
				keypoints: "keyPoints",
				keysplines: "keySplines",
				keytimes: "keyTimes",
				lengthadjust: "lengthAdjust",
				letterspacing: "letterSpacing",
				"letter-spacing": "letterSpacing",
				lightingcolor: "lightingColor",
				"lighting-color": "lightingColor",
				limitingconeangle: "limitingConeAngle",
				local: "local",
				markerend: "markerEnd",
				"marker-end": "markerEnd",
				markerheight: "markerHeight",
				markermid: "markerMid",
				"marker-mid": "markerMid",
				markerstart: "markerStart",
				"marker-start": "markerStart",
				markerunits: "markerUnits",
				markerwidth: "markerWidth",
				mask: "mask",
				maskcontentunits: "maskContentUnits",
				maskunits: "maskUnits",
				mathematical: "mathematical",
				mode: "mode",
				numoctaves: "numOctaves",
				offset: "offset",
				opacity: "opacity",
				operator: "operator",
				order: "order",
				orient: "orient",
				orientation: "orientation",
				origin: "origin",
				overflow: "overflow",
				overlineposition: "overlinePosition",
				"overline-position": "overlinePosition",
				overlinethickness: "overlineThickness",
				"overline-thickness": "overlineThickness",
				paintorder: "paintOrder",
				"paint-order": "paintOrder",
				panose1: "panose1",
				"panose-1": "panose1",
				pathlength: "pathLength",
				patterncontentunits: "patternContentUnits",
				patterntransform: "patternTransform",
				patternunits: "patternUnits",
				pointerevents: "pointerEvents",
				"pointer-events": "pointerEvents",
				points: "points",
				pointsatx: "pointsAtX",
				pointsaty: "pointsAtY",
				pointsatz: "pointsAtZ",
				prefix: "prefix",
				preservealpha: "preserveAlpha",
				preserveaspectratio: "preserveAspectRatio",
				primitiveunits: "primitiveUnits",
				property: "property",
				r: "r",
				radius: "radius",
				refx: "refX",
				refy: "refY",
				renderingintent: "renderingIntent",
				"rendering-intent": "renderingIntent",
				repeatcount: "repeatCount",
				repeatdur: "repeatDur",
				requiredextensions: "requiredExtensions",
				requiredfeatures: "requiredFeatures",
				resource: "resource",
				restart: "restart",
				result: "result",
				results: "results",
				rotate: "rotate",
				rx: "rx",
				ry: "ry",
				scale: "scale",
				security: "security",
				seed: "seed",
				shaperendering: "shapeRendering",
				"shape-rendering": "shapeRendering",
				slope: "slope",
				spacing: "spacing",
				specularconstant: "specularConstant",
				specularexponent: "specularExponent",
				speed: "speed",
				spreadmethod: "spreadMethod",
				startoffset: "startOffset",
				stddeviation: "stdDeviation",
				stemh: "stemh",
				stemv: "stemv",
				stitchtiles: "stitchTiles",
				stopcolor: "stopColor",
				"stop-color": "stopColor",
				stopopacity: "stopOpacity",
				"stop-opacity": "stopOpacity",
				strikethroughposition: "strikethroughPosition",
				"strikethrough-position": "strikethroughPosition",
				strikethroughthickness: "strikethroughThickness",
				"strikethrough-thickness": "strikethroughThickness",
				string: "string",
				stroke: "stroke",
				strokedasharray: "strokeDasharray",
				"stroke-dasharray": "strokeDasharray",
				strokedashoffset: "strokeDashoffset",
				"stroke-dashoffset": "strokeDashoffset",
				strokelinecap: "strokeLinecap",
				"stroke-linecap": "strokeLinecap",
				strokelinejoin: "strokeLinejoin",
				"stroke-linejoin": "strokeLinejoin",
				strokemiterlimit: "strokeMiterlimit",
				"stroke-miterlimit": "strokeMiterlimit",
				strokewidth: "strokeWidth",
				"stroke-width": "strokeWidth",
				strokeopacity: "strokeOpacity",
				"stroke-opacity": "strokeOpacity",
				suppresscontenteditablewarning: "suppressContentEditableWarning",
				suppresshydrationwarning: "suppressHydrationWarning",
				surfacescale: "surfaceScale",
				systemlanguage: "systemLanguage",
				tablevalues: "tableValues",
				targetx: "targetX",
				targety: "targetY",
				textanchor: "textAnchor",
				"text-anchor": "textAnchor",
				textdecoration: "textDecoration",
				"text-decoration": "textDecoration",
				textlength: "textLength",
				textrendering: "textRendering",
				"text-rendering": "textRendering",
				to: "to",
				transform: "transform",
				typeof: "typeof",
				u1: "u1",
				u2: "u2",
				underlineposition: "underlinePosition",
				"underline-position": "underlinePosition",
				underlinethickness: "underlineThickness",
				"underline-thickness": "underlineThickness",
				unicode: "unicode",
				unicodebidi: "unicodeBidi",
				"unicode-bidi": "unicodeBidi",
				unicoderange: "unicodeRange",
				"unicode-range": "unicodeRange",
				unitsperem: "unitsPerEm",
				"units-per-em": "unitsPerEm",
				unselectable: "unselectable",
				valphabetic: "vAlphabetic",
				"v-alphabetic": "vAlphabetic",
				values: "values",
				vectoreffect: "vectorEffect",
				"vector-effect": "vectorEffect",
				version: "version",
				vertadvy: "vertAdvY",
				"vert-adv-y": "vertAdvY",
				vertoriginx: "vertOriginX",
				"vert-origin-x": "vertOriginX",
				vertoriginy: "vertOriginY",
				"vert-origin-y": "vertOriginY",
				vhanging: "vHanging",
				"v-hanging": "vHanging",
				videographic: "vIdeographic",
				"v-ideographic": "vIdeographic",
				viewbox: "viewBox",
				viewtarget: "viewTarget",
				visibility: "visibility",
				vmathematical: "vMathematical",
				"v-mathematical": "vMathematical",
				vocab: "vocab",
				widths: "widths",
				wordspacing: "wordSpacing",
				"word-spacing": "wordSpacing",
				writingmode: "writingMode",
				"writing-mode": "writingMode",
				x1: "x1",
				x2: "x2",
				x: "x",
				xchannelselector: "xChannelSelector",
				xheight: "xHeight",
				"x-height": "xHeight",
				xlinkactuate: "xlinkActuate",
				"xlink:actuate": "xlinkActuate",
				xlinkarcrole: "xlinkArcrole",
				"xlink:arcrole": "xlinkArcrole",
				xlinkhref: "xlinkHref",
				"xlink:href": "xlinkHref",
				xlinkrole: "xlinkRole",
				"xlink:role": "xlinkRole",
				xlinkshow: "xlinkShow",
				"xlink:show": "xlinkShow",
				xlinktitle: "xlinkTitle",
				"xlink:title": "xlinkTitle",
				xlinktype: "xlinkType",
				"xlink:type": "xlinkType",
				xmlbase: "xmlBase",
				"xml:base": "xmlBase",
				xmllang: "xmlLang",
				"xml:lang": "xmlLang",
				xmlns: "xmlns",
				"xml:space": "xmlSpace",
				xmlnsxlink: "xmlnsXlink",
				"xmlns:xlink": "xmlnsXlink",
				xmlspace: "xmlSpace",
				y1: "y1",
				y2: "y2",
				y: "y",
				ychannelselector: "yChannelSelector",
				z: "z",
				zoomandpan: "zoomAndPan"
			};
			var ariaProperties = {
				"aria-current": 0,
				"aria-description": 0,
				"aria-details": 0,
				"aria-disabled": 0,
				"aria-hidden": 0,
				"aria-invalid": 0,
				"aria-keyshortcuts": 0,
				"aria-label": 0,
				"aria-roledescription": 0,
				"aria-autocomplete": 0,
				"aria-checked": 0,
				"aria-expanded": 0,
				"aria-haspopup": 0,
				"aria-level": 0,
				"aria-modal": 0,
				"aria-multiline": 0,
				"aria-multiselectable": 0,
				"aria-orientation": 0,
				"aria-placeholder": 0,
				"aria-pressed": 0,
				"aria-readonly": 0,
				"aria-required": 0,
				"aria-selected": 0,
				"aria-sort": 0,
				"aria-valuemax": 0,
				"aria-valuemin": 0,
				"aria-valuenow": 0,
				"aria-valuetext": 0,
				"aria-atomic": 0,
				"aria-busy": 0,
				"aria-live": 0,
				"aria-relevant": 0,
				"aria-dropeffect": 0,
				"aria-grabbed": 0,
				"aria-activedescendant": 0,
				"aria-colcount": 0,
				"aria-colindex": 0,
				"aria-colspan": 0,
				"aria-controls": 0,
				"aria-describedby": 0,
				"aria-errormessage": 0,
				"aria-flowto": 0,
				"aria-labelledby": 0,
				"aria-owns": 0,
				"aria-posinset": 0,
				"aria-rowcount": 0,
				"aria-rowindex": 0,
				"aria-rowspan": 0,
				"aria-setsize": 0
			};
			var warnedProperties = {};
			var rARIA = new RegExp("^(aria)-[" + ATTRIBUTE_NAME_CHAR + "]*$");
			var rARIACamel = new RegExp("^(aria)[A-Z][" + ATTRIBUTE_NAME_CHAR + "]*$");
			function validateProperty(tagName, name) {
				if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) return true;
				if (rARIACamel.test(name)) {
					var ariaName = "aria-" + name.slice(4).toLowerCase();
					var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null;
					if (correctName == null) {
						error("Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.", name);
						warnedProperties[name] = true;
						return true;
					}
					if (name !== correctName) {
						error("Invalid ARIA attribute `%s`. Did you mean `%s`?", name, correctName);
						warnedProperties[name] = true;
						return true;
					}
				}
				if (rARIA.test(name)) {
					var lowerCasedName = name.toLowerCase();
					var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null;
					if (standardName == null) {
						warnedProperties[name] = true;
						return false;
					}
					if (name !== standardName) {
						error("Unknown ARIA attribute `%s`. Did you mean `%s`?", name, standardName);
						warnedProperties[name] = true;
						return true;
					}
				}
				return true;
			}
			function warnInvalidARIAProps(type, props) {
				var invalidProps = [];
				for (var key in props) if (!validateProperty(type, key)) invalidProps.push(key);
				var unknownPropString = invalidProps.map(function(prop) {
					return "`" + prop + "`";
				}).join(", ");
				if (invalidProps.length === 1) error("Invalid aria prop %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
				else if (invalidProps.length > 1) error("Invalid aria props %s on <%s> tag. For details, see https://reactjs.org/link/invalid-aria-props", unknownPropString, type);
			}
			function validateProperties(type, props) {
				if (isCustomComponent(type, props)) return;
				warnInvalidARIAProps(type, props);
			}
			var didWarnValueNull = false;
			function validateProperties$1(type, props) {
				if (type !== "input" && type !== "textarea" && type !== "select") return;
				if (props != null && props.value === null && !didWarnValueNull) {
					didWarnValueNull = true;
					if (type === "select" && props.multiple) error("`value` prop on `%s` should not be null. Consider using an empty array when `multiple` is set to `true` to clear the component or `undefined` for uncontrolled components.", type);
					else error("`value` prop on `%s` should not be null. Consider using an empty string to clear the component or `undefined` for uncontrolled components.", type);
				}
			}
			var validateProperty$1 = function() {};
			var warnedProperties$1 = {};
			var EVENT_NAME_REGEX = /^on./;
			var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
			var rARIA$1 = new RegExp("^(aria)-[" + ATTRIBUTE_NAME_CHAR + "]*$");
			var rARIACamel$1 = new RegExp("^(aria)[A-Z][" + ATTRIBUTE_NAME_CHAR + "]*$");
			validateProperty$1 = function(tagName, name, value, eventRegistry) {
				if (hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) return true;
				var lowerCasedName = name.toLowerCase();
				if (lowerCasedName === "onfocusin" || lowerCasedName === "onfocusout") {
					error("React uses onFocus and onBlur instead of onFocusIn and onFocusOut. All React events are normalized to bubble, so onFocusIn and onFocusOut are not needed/supported by React.");
					warnedProperties$1[name] = true;
					return true;
				}
				if (eventRegistry != null) {
					var registrationNameDependencies = eventRegistry.registrationNameDependencies, possibleRegistrationNames = eventRegistry.possibleRegistrationNames;
					if (registrationNameDependencies.hasOwnProperty(name)) return true;
					var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;
					if (registrationName != null) {
						error("Invalid event handler property `%s`. Did you mean `%s`?", name, registrationName);
						warnedProperties$1[name] = true;
						return true;
					}
					if (EVENT_NAME_REGEX.test(name)) {
						error("Unknown event handler property `%s`. It will be ignored.", name);
						warnedProperties$1[name] = true;
						return true;
					}
				} else if (EVENT_NAME_REGEX.test(name)) {
					if (INVALID_EVENT_NAME_REGEX.test(name)) error("Invalid event handler property `%s`. React events use the camelCase naming convention, for example `onClick`.", name);
					warnedProperties$1[name] = true;
					return true;
				}
				if (rARIA$1.test(name) || rARIACamel$1.test(name)) return true;
				if (lowerCasedName === "innerhtml") {
					error("Directly setting property `innerHTML` is not permitted. For more information, lookup documentation on `dangerouslySetInnerHTML`.");
					warnedProperties$1[name] = true;
					return true;
				}
				if (lowerCasedName === "aria") {
					error("The `aria` attribute is reserved for future use in React. Pass individual `aria-` attributes instead.");
					warnedProperties$1[name] = true;
					return true;
				}
				if (lowerCasedName === "is" && value !== null && value !== void 0 && typeof value !== "string") {
					error("Received a `%s` for a string attribute `is`. If this is expected, cast the value to a string.", typeof value);
					warnedProperties$1[name] = true;
					return true;
				}
				if (typeof value === "number" && isNaN(value)) {
					error("Received NaN for the `%s` attribute. If this is expected, cast the value to a string.", name);
					warnedProperties$1[name] = true;
					return true;
				}
				var propertyInfo = getPropertyInfo(name);
				var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED;
				if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
					var standardName = possibleStandardNames[lowerCasedName];
					if (standardName !== name) {
						error("Invalid DOM property `%s`. Did you mean `%s`?", name, standardName);
						warnedProperties$1[name] = true;
						return true;
					}
				} else if (!isReserved && name !== lowerCasedName) {
					error("React does not recognize the `%s` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `%s` instead. If you accidentally passed it from a parent component, remove it from the DOM element.", name, lowerCasedName);
					warnedProperties$1[name] = true;
					return true;
				}
				if (typeof value === "boolean" && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
					if (value) error("Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s=\"%s\" or %s={value.toString()}.", value, name, name, value, name);
					else error("Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s=\"%s\" or %s={value.toString()}.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", value, name, name, value, name, name, name);
					warnedProperties$1[name] = true;
					return true;
				}
				if (isReserved) return true;
				if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
					warnedProperties$1[name] = true;
					return false;
				}
				if ((value === "false" || value === "true") && propertyInfo !== null && propertyInfo.type === BOOLEAN) {
					error("Received the string `%s` for the boolean attribute `%s`. %s Did you mean %s={%s}?", value, name, value === "false" ? "The browser will interpret it as a truthy value." : "Although this works, it will not work as expected if you pass the string \"false\".", name, value);
					warnedProperties$1[name] = true;
					return true;
				}
				return true;
			};
			var warnUnknownProperties = function(type, props, eventRegistry) {
				var unknownProps = [];
				for (var key in props) if (!validateProperty$1(type, key, props[key], eventRegistry)) unknownProps.push(key);
				var unknownPropString = unknownProps.map(function(prop) {
					return "`" + prop + "`";
				}).join(", ");
				if (unknownProps.length === 1) error("Invalid value for prop %s on <%s> tag. Either remove it from the element, or pass a string or number value to keep it in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
				else if (unknownProps.length > 1) error("Invalid values for props %s on <%s> tag. Either remove them from the element, or pass a string or number value to keep them in the DOM. For details, see https://reactjs.org/link/attribute-behavior ", unknownPropString, type);
			};
			function validateProperties$2(type, props, eventRegistry) {
				if (isCustomComponent(type, props)) return;
				warnUnknownProperties(type, props, eventRegistry);
			}
			var IS_EVENT_HANDLE_NON_MANAGED_NODE = 1;
			var IS_NON_DELEGATED = 2;
			var IS_CAPTURE_PHASE = 4;
			var SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS = IS_EVENT_HANDLE_NON_MANAGED_NODE | IS_NON_DELEGATED | IS_CAPTURE_PHASE;
			var currentReplayingEvent = null;
			function setReplayingEvent(event) {
				if (currentReplayingEvent !== null) error("Expected currently replaying event to be null. This error is likely caused by a bug in React. Please file an issue.");
				currentReplayingEvent = event;
			}
			function resetReplayingEvent() {
				if (currentReplayingEvent === null) error("Expected currently replaying event to not be null. This error is likely caused by a bug in React. Please file an issue.");
				currentReplayingEvent = null;
			}
			function isReplayingEvent(event) {
				return event === currentReplayingEvent;
			}
			/**
			* Gets the target node from a native browser event by accounting for
			* inconsistencies in browser DOM APIs.
			*
			* @param {object} nativeEvent Native browser event.
			* @return {DOMEventTarget} Target node.
			*/
			function getEventTarget(nativeEvent) {
				var target = nativeEvent.target || nativeEvent.srcElement || window;
				if (target.correspondingUseElement) target = target.correspondingUseElement;
				return target.nodeType === TEXT_NODE ? target.parentNode : target;
			}
			var restoreImpl = null;
			var restoreTarget = null;
			var restoreQueue = null;
			function restoreStateOfTarget(target) {
				var internalInstance = getInstanceFromNode(target);
				if (!internalInstance) return;
				if (typeof restoreImpl !== "function") throw new Error("setRestoreImplementation() needs to be called to handle a target for controlled events. This error is likely caused by a bug in React. Please file an issue.");
				var stateNode = internalInstance.stateNode;
				if (stateNode) {
					var _props = getFiberCurrentPropsFromNode(stateNode);
					restoreImpl(internalInstance.stateNode, internalInstance.type, _props);
				}
			}
			function setRestoreImplementation(impl) {
				restoreImpl = impl;
			}
			function enqueueStateRestore(target) {
				if (restoreTarget) {
					if (restoreQueue) restoreQueue.push(target);
					else restoreQueue = [target];
				} else restoreTarget = target;
			}
			function needsStateRestore() {
				return restoreTarget !== null || restoreQueue !== null;
			}
			function restoreStateIfNeeded() {
				if (!restoreTarget) return;
				var target = restoreTarget;
				var queuedTargets = restoreQueue;
				restoreTarget = null;
				restoreQueue = null;
				restoreStateOfTarget(target);
				if (queuedTargets) for (var i = 0; i < queuedTargets.length; i++) restoreStateOfTarget(queuedTargets[i]);
			}
			var batchedUpdatesImpl = function(fn, bookkeeping) {
				return fn(bookkeeping);
			};
			var flushSyncImpl = function() {};
			var isInsideEventHandler = false;
			function finishEventHandler() {
				if (needsStateRestore()) {
					flushSyncImpl();
					restoreStateIfNeeded();
				}
			}
			function batchedUpdates(fn, a, b) {
				if (isInsideEventHandler) return fn(a, b);
				isInsideEventHandler = true;
				try {
					return batchedUpdatesImpl(fn, a, b);
				} finally {
					isInsideEventHandler = false;
					finishEventHandler();
				}
			}
			function setBatchingImplementation(_batchedUpdatesImpl, _discreteUpdatesImpl, _flushSyncImpl) {
				batchedUpdatesImpl = _batchedUpdatesImpl;
				flushSyncImpl = _flushSyncImpl;
			}
			function isInteractive(tag) {
				return tag === "button" || tag === "input" || tag === "select" || tag === "textarea";
			}
			function shouldPreventMouseEvent(name, type, props) {
				switch (name) {
					case "onClick":
					case "onClickCapture":
					case "onDoubleClick":
					case "onDoubleClickCapture":
					case "onMouseDown":
					case "onMouseDownCapture":
					case "onMouseMove":
					case "onMouseMoveCapture":
					case "onMouseUp":
					case "onMouseUpCapture":
					case "onMouseEnter": return !!(props.disabled && isInteractive(type));
					default: return false;
				}
			}
			/**
			* @param {object} inst The instance, which is the source of events.
			* @param {string} registrationName Name of listener (e.g. `onClick`).
			* @return {?function} The stored callback.
			*/
			function getListener(inst, registrationName) {
				var stateNode = inst.stateNode;
				if (stateNode === null) return null;
				var props = getFiberCurrentPropsFromNode(stateNode);
				if (props === null) return null;
				var listener = props[registrationName];
				if (shouldPreventMouseEvent(registrationName, inst.type, props)) return null;
				if (listener && typeof listener !== "function") throw new Error("Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof listener + "` type.");
				return listener;
			}
			var passiveBrowserEventsSupported = false;
			if (canUseDOM) try {
				var options = {};
				Object.defineProperty(options, "passive", { get: function() {
					passiveBrowserEventsSupported = true;
				} });
				window.addEventListener("test", options, options);
				window.removeEventListener("test", options, options);
			} catch (e) {
				passiveBrowserEventsSupported = false;
			}
			function invokeGuardedCallbackProd(name, func, context, a, b, c, d, e, f) {
				var funcArgs = Array.prototype.slice.call(arguments, 3);
				try {
					func.apply(context, funcArgs);
				} catch (error) {
					this.onError(error);
				}
			}
			var invokeGuardedCallbackImpl = invokeGuardedCallbackProd;
			if (typeof window !== "undefined" && typeof window.dispatchEvent === "function" && typeof document !== "undefined" && typeof document.createEvent === "function") {
				var fakeNode = document.createElement("react");
				invokeGuardedCallbackImpl = function invokeGuardedCallbackDev(name, func, context, a, b, c, d, e, f) {
					if (typeof document === "undefined" || document === null) throw new Error("The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous.");
					var evt = document.createEvent("Event");
					var didCall = false;
					var didError = true;
					var windowEvent = window.event;
					var windowEventDescriptor = Object.getOwnPropertyDescriptor(window, "event");
					function restoreAfterDispatch() {
						fakeNode.removeEventListener(evtType, callCallback, false);
						if (typeof window.event !== "undefined" && window.hasOwnProperty("event")) window.event = windowEvent;
					}
					var funcArgs = Array.prototype.slice.call(arguments, 3);
					function callCallback() {
						didCall = true;
						restoreAfterDispatch();
						func.apply(context, funcArgs);
						didError = false;
					}
					var error;
					var didSetError = false;
					var isCrossOriginError = false;
					function handleWindowError(event) {
						error = event.error;
						didSetError = true;
						if (error === null && event.colno === 0 && event.lineno === 0) isCrossOriginError = true;
						if (event.defaultPrevented) {
							if (error != null && typeof error === "object") try {
								error._suppressLogging = true;
							} catch (inner) {}
						}
					}
					var evtType = "react-" + (name ? name : "invokeguardedcallback");
					window.addEventListener("error", handleWindowError);
					fakeNode.addEventListener(evtType, callCallback, false);
					evt.initEvent(evtType, false, false);
					fakeNode.dispatchEvent(evt);
					if (windowEventDescriptor) Object.defineProperty(window, "event", windowEventDescriptor);
					if (didCall && didError) {
						if (!didSetError) error = /* @__PURE__ */ new Error("An error was thrown inside one of your components, but React doesn't know what it was. This is likely due to browser flakiness. React does its best to preserve the \"Pause on exceptions\" behavior of the DevTools, which requires some DEV-mode only tricks. It's possible that these don't work in your browser. Try triggering the error in production mode, or switching to a modern browser. If you suspect that this is actually an issue with React, please file an issue.");
						else if (isCrossOriginError) error = /* @__PURE__ */ new Error("A cross-origin error was thrown. React doesn't have access to the actual error object in development. See https://reactjs.org/link/crossorigin-error for more information.");
						this.onError(error);
					}
					window.removeEventListener("error", handleWindowError);
					if (!didCall) {
						restoreAfterDispatch();
						return invokeGuardedCallbackProd.apply(this, arguments);
					}
				};
			}
			var invokeGuardedCallbackImpl$1 = invokeGuardedCallbackImpl;
			var hasError = false;
			var caughtError = null;
			var hasRethrowError = false;
			var rethrowError = null;
			var reporter = { onError: function(error) {
				hasError = true;
				caughtError = error;
			} };
			/**
			* Call a function while guarding against errors that happens within it.
			* Returns an error if it throws, otherwise null.
			*
			* In production, this is implemented using a try-catch. The reason we don't
			* use a try-catch directly is so that we can swap out a different
			* implementation in DEV mode.
			*
			* @param {String} name of the guard to use for logging or debugging
			* @param {Function} func The function to invoke
			* @param {*} context The context to use when calling the function
			* @param {...*} args Arguments for function
			*/
			function invokeGuardedCallback(name, func, context, a, b, c, d, e, f) {
				hasError = false;
				caughtError = null;
				invokeGuardedCallbackImpl$1.apply(reporter, arguments);
			}
			/**
			* Same as invokeGuardedCallback, but instead of returning an error, it stores
			* it in a global so it can be rethrown by `rethrowCaughtError` later.
			* TODO: See if caughtError and rethrowError can be unified.
			*
			* @param {String} name of the guard to use for logging or debugging
			* @param {Function} func The function to invoke
			* @param {*} context The context to use when calling the function
			* @param {...*} args Arguments for function
			*/
			function invokeGuardedCallbackAndCatchFirstError(name, func, context, a, b, c, d, e, f) {
				invokeGuardedCallback.apply(this, arguments);
				if (hasError) {
					var error = clearCaughtError();
					if (!hasRethrowError) {
						hasRethrowError = true;
						rethrowError = error;
					}
				}
			}
			/**
			* During execution of guarded functions we will capture the first error which
			* we will rethrow to be handled by the top level error handler.
			*/
			function rethrowCaughtError() {
				if (hasRethrowError) {
					var error = rethrowError;
					hasRethrowError = false;
					rethrowError = null;
					throw error;
				}
			}
			function hasCaughtError() {
				return hasError;
			}
			function clearCaughtError() {
				if (hasError) {
					var error = caughtError;
					hasError = false;
					caughtError = null;
					return error;
				} else throw new Error("clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.");
			}
			/**
			* `ReactInstanceMap` maintains a mapping from a public facing stateful
			* instance (key) and the internal representation (value). This allows public
			* methods to accept the user facing instance as an argument and map them back
			* to internal methods.
			*
			* Note that this module is currently shared and assumed to be stateless.
			* If this becomes an actual Map, that will break.
			*/
			function get(key) {
				return key._reactInternals;
			}
			function has(key) {
				return key._reactInternals !== void 0;
			}
			function set(key, value) {
				key._reactInternals = value;
			}
			var NoFlags = 0;
			var PerformedWork = 1;
			var Placement = 2;
			var Update = 4;
			var ChildDeletion = 16;
			var ContentReset = 32;
			var Callback = 64;
			var DidCapture = 128;
			var ForceClientRender = 256;
			var Ref = 512;
			var Snapshot = 1024;
			var Passive = 2048;
			var Hydrating = 4096;
			var Visibility = 8192;
			var StoreConsistency = 16384;
			var LifecycleEffectMask = Passive | Update | Callback | Ref | Snapshot | StoreConsistency;
			var HostEffectMask = 32767;
			var Incomplete = 32768;
			var ShouldCapture = 65536;
			var ForceUpdateForLegacySuspense = 131072;
			var Forked = 1048576;
			var RefStatic = 2097152;
			var LayoutStatic = 4194304;
			var PassiveStatic = 8388608;
			var MountLayoutDev = 16777216;
			var MountPassiveDev = 33554432;
			var BeforeMutationMask = Update | Snapshot | 0;
			var MutationMask = Placement | Update | ChildDeletion | ContentReset | Ref | Hydrating | Visibility;
			var LayoutMask = Update | Callback | Ref | Visibility;
			var PassiveMask = Passive | ChildDeletion;
			var StaticMask = LayoutStatic | PassiveStatic | RefStatic;
			var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
			function getNearestMountedFiber(fiber) {
				var node = fiber;
				var nearestMounted = fiber;
				if (!fiber.alternate) {
					var nextNode = node;
					do {
						node = nextNode;
						if ((node.flags & (Placement | Hydrating)) !== NoFlags) nearestMounted = node.return;
						nextNode = node.return;
					} while (nextNode);
				} else while (node.return) node = node.return;
				if (node.tag === HostRoot) return nearestMounted;
				return null;
			}
			function getSuspenseInstanceFromFiber(fiber) {
				if (fiber.tag === SuspenseComponent) {
					var suspenseState = fiber.memoizedState;
					if (suspenseState === null) {
						var current = fiber.alternate;
						if (current !== null) suspenseState = current.memoizedState;
					}
					if (suspenseState !== null) return suspenseState.dehydrated;
				}
				return null;
			}
			function getContainerFromFiber(fiber) {
				return fiber.tag === HostRoot ? fiber.stateNode.containerInfo : null;
			}
			function isFiberMounted(fiber) {
				return getNearestMountedFiber(fiber) === fiber;
			}
			function isMounted(component) {
				var owner = ReactCurrentOwner.current;
				if (owner !== null && owner.tag === ClassComponent) {
					var ownerFiber = owner;
					var instance = ownerFiber.stateNode;
					if (!instance._warnedAboutRefsInRender) error("%s is accessing isMounted inside its render() function. render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromFiber(ownerFiber) || "A component");
					instance._warnedAboutRefsInRender = true;
				}
				var fiber = get(component);
				if (!fiber) return false;
				return getNearestMountedFiber(fiber) === fiber;
			}
			function assertIsMounted(fiber) {
				if (getNearestMountedFiber(fiber) !== fiber) throw new Error("Unable to find node on an unmounted component.");
			}
			function findCurrentFiberUsingSlowPath(fiber) {
				var alternate = fiber.alternate;
				if (!alternate) {
					var nearestMounted = getNearestMountedFiber(fiber);
					if (nearestMounted === null) throw new Error("Unable to find node on an unmounted component.");
					if (nearestMounted !== fiber) return null;
					return fiber;
				}
				var a = fiber;
				var b = alternate;
				while (true) {
					var parentA = a.return;
					if (parentA === null) break;
					var parentB = parentA.alternate;
					if (parentB === null) {
						var nextParent = parentA.return;
						if (nextParent !== null) {
							a = b = nextParent;
							continue;
						}
						break;
					}
					if (parentA.child === parentB.child) {
						var child = parentA.child;
						while (child) {
							if (child === a) {
								assertIsMounted(parentA);
								return fiber;
							}
							if (child === b) {
								assertIsMounted(parentA);
								return alternate;
							}
							child = child.sibling;
						}
						throw new Error("Unable to find node on an unmounted component.");
					}
					if (a.return !== b.return) {
						a = parentA;
						b = parentB;
					} else {
						var didFindChild = false;
						var _child = parentA.child;
						while (_child) {
							if (_child === a) {
								didFindChild = true;
								a = parentA;
								b = parentB;
								break;
							}
							if (_child === b) {
								didFindChild = true;
								b = parentA;
								a = parentB;
								break;
							}
							_child = _child.sibling;
						}
						if (!didFindChild) {
							_child = parentB.child;
							while (_child) {
								if (_child === a) {
									didFindChild = true;
									a = parentB;
									b = parentA;
									break;
								}
								if (_child === b) {
									didFindChild = true;
									b = parentB;
									a = parentA;
									break;
								}
								_child = _child.sibling;
							}
							if (!didFindChild) throw new Error("Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.");
						}
					}
					if (a.alternate !== b) throw new Error("Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue.");
				}
				if (a.tag !== HostRoot) throw new Error("Unable to find node on an unmounted component.");
				if (a.stateNode.current === a) return fiber;
				return alternate;
			}
			function findCurrentHostFiber(parent) {
				var currentParent = findCurrentFiberUsingSlowPath(parent);
				return currentParent !== null ? findCurrentHostFiberImpl(currentParent) : null;
			}
			function findCurrentHostFiberImpl(node) {
				if (node.tag === HostComponent || node.tag === HostText) return node;
				var child = node.child;
				while (child !== null) {
					var match = findCurrentHostFiberImpl(child);
					if (match !== null) return match;
					child = child.sibling;
				}
				return null;
			}
			function findCurrentHostFiberWithNoPortals(parent) {
				var currentParent = findCurrentFiberUsingSlowPath(parent);
				return currentParent !== null ? findCurrentHostFiberWithNoPortalsImpl(currentParent) : null;
			}
			function findCurrentHostFiberWithNoPortalsImpl(node) {
				if (node.tag === HostComponent || node.tag === HostText) return node;
				var child = node.child;
				while (child !== null) {
					if (child.tag !== HostPortal) {
						var match = findCurrentHostFiberWithNoPortalsImpl(child);
						if (match !== null) return match;
					}
					child = child.sibling;
				}
				return null;
			}
			var scheduleCallback = Scheduler.unstable_scheduleCallback;
			var cancelCallback = Scheduler.unstable_cancelCallback;
			var shouldYield = Scheduler.unstable_shouldYield;
			var requestPaint = Scheduler.unstable_requestPaint;
			var now = Scheduler.unstable_now;
			var getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel;
			var ImmediatePriority = Scheduler.unstable_ImmediatePriority;
			var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
			var NormalPriority = Scheduler.unstable_NormalPriority;
			var LowPriority = Scheduler.unstable_LowPriority;
			var IdlePriority = Scheduler.unstable_IdlePriority;
			var unstable_yieldValue = Scheduler.unstable_yieldValue;
			var unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue;
			var rendererID = null;
			var injectedHook = null;
			var injectedProfilingHooks = null;
			var hasLoggedError = false;
			var isDevToolsPresent = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined";
			function injectInternals(internals) {
				if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined") return false;
				var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
				if (hook.isDisabled) return true;
				if (!hook.supportsFiber) {
					error("The installed version of React DevTools is too old and will not work with the current version of React. Please update React DevTools. https://reactjs.org/link/react-devtools");
					return true;
				}
				try {
					if (enableSchedulingProfiler) internals = assign({}, internals, {
						getLaneLabelMap,
						injectProfilingHooks
					});
					rendererID = hook.inject(internals);
					injectedHook = hook;
				} catch (err) {
					error("React instrumentation encountered an error: %s.", err);
				}
				if (hook.checkDCE) return true;
				else return false;
			}
			function onScheduleRoot(root, children) {
				if (injectedHook && typeof injectedHook.onScheduleFiberRoot === "function") try {
					injectedHook.onScheduleFiberRoot(rendererID, root, children);
				} catch (err) {
					if (!hasLoggedError) {
						hasLoggedError = true;
						error("React instrumentation encountered an error: %s", err);
					}
				}
			}
			function onCommitRoot(root, eventPriority) {
				if (injectedHook && typeof injectedHook.onCommitFiberRoot === "function") try {
					var didError = (root.current.flags & DidCapture) === DidCapture;
					if (enableProfilerTimer) {
						var schedulerPriority;
						switch (eventPriority) {
							case DiscreteEventPriority:
								schedulerPriority = ImmediatePriority;
								break;
							case ContinuousEventPriority:
								schedulerPriority = UserBlockingPriority;
								break;
							case DefaultEventPriority:
								schedulerPriority = NormalPriority;
								break;
							case IdleEventPriority:
								schedulerPriority = IdlePriority;
								break;
							default: schedulerPriority = NormalPriority;
						}
						injectedHook.onCommitFiberRoot(rendererID, root, schedulerPriority, didError);
					} else injectedHook.onCommitFiberRoot(rendererID, root, void 0, didError);
				} catch (err) {
					if (!hasLoggedError) {
						hasLoggedError = true;
						error("React instrumentation encountered an error: %s", err);
					}
				}
			}
			function onPostCommitRoot(root) {
				if (injectedHook && typeof injectedHook.onPostCommitFiberRoot === "function") try {
					injectedHook.onPostCommitFiberRoot(rendererID, root);
				} catch (err) {
					if (!hasLoggedError) {
						hasLoggedError = true;
						error("React instrumentation encountered an error: %s", err);
					}
				}
			}
			function onCommitUnmount(fiber) {
				if (injectedHook && typeof injectedHook.onCommitFiberUnmount === "function") try {
					injectedHook.onCommitFiberUnmount(rendererID, fiber);
				} catch (err) {
					if (!hasLoggedError) {
						hasLoggedError = true;
						error("React instrumentation encountered an error: %s", err);
					}
				}
			}
			function setIsStrictModeForDevtools(newIsStrictMode) {
				if (typeof unstable_yieldValue === "function") {
					unstable_setDisableYieldValue(newIsStrictMode);
					setSuppressWarning(newIsStrictMode);
				}
				if (injectedHook && typeof injectedHook.setStrictMode === "function") try {
					injectedHook.setStrictMode(rendererID, newIsStrictMode);
				} catch (err) {
					if (!hasLoggedError) {
						hasLoggedError = true;
						error("React instrumentation encountered an error: %s", err);
					}
				}
			}
			function injectProfilingHooks(profilingHooks) {
				injectedProfilingHooks = profilingHooks;
			}
			function getLaneLabelMap() {
				var map = /* @__PURE__ */ new Map();
				var lane = 1;
				for (var index = 0; index < TotalLanes; index++) {
					var label = getLabelForLane(lane);
					map.set(lane, label);
					lane *= 2;
				}
				return map;
			}
			function markCommitStarted(lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStarted === "function") injectedProfilingHooks.markCommitStarted(lanes);
			}
			function markCommitStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markCommitStopped === "function") injectedProfilingHooks.markCommitStopped();
			}
			function markComponentRenderStarted(fiber) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStarted === "function") injectedProfilingHooks.markComponentRenderStarted(fiber);
			}
			function markComponentRenderStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentRenderStopped === "function") injectedProfilingHooks.markComponentRenderStopped();
			}
			function markComponentPassiveEffectMountStarted(fiber) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStarted === "function") injectedProfilingHooks.markComponentPassiveEffectMountStarted(fiber);
			}
			function markComponentPassiveEffectMountStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectMountStopped === "function") injectedProfilingHooks.markComponentPassiveEffectMountStopped();
			}
			function markComponentPassiveEffectUnmountStarted(fiber) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStarted === "function") injectedProfilingHooks.markComponentPassiveEffectUnmountStarted(fiber);
			}
			function markComponentPassiveEffectUnmountStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentPassiveEffectUnmountStopped === "function") injectedProfilingHooks.markComponentPassiveEffectUnmountStopped();
			}
			function markComponentLayoutEffectMountStarted(fiber) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStarted === "function") injectedProfilingHooks.markComponentLayoutEffectMountStarted(fiber);
			}
			function markComponentLayoutEffectMountStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectMountStopped === "function") injectedProfilingHooks.markComponentLayoutEffectMountStopped();
			}
			function markComponentLayoutEffectUnmountStarted(fiber) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStarted === "function") injectedProfilingHooks.markComponentLayoutEffectUnmountStarted(fiber);
			}
			function markComponentLayoutEffectUnmountStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentLayoutEffectUnmountStopped === "function") injectedProfilingHooks.markComponentLayoutEffectUnmountStopped();
			}
			function markComponentErrored(fiber, thrownValue, lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentErrored === "function") injectedProfilingHooks.markComponentErrored(fiber, thrownValue, lanes);
			}
			function markComponentSuspended(fiber, wakeable, lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markComponentSuspended === "function") injectedProfilingHooks.markComponentSuspended(fiber, wakeable, lanes);
			}
			function markLayoutEffectsStarted(lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStarted === "function") injectedProfilingHooks.markLayoutEffectsStarted(lanes);
			}
			function markLayoutEffectsStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markLayoutEffectsStopped === "function") injectedProfilingHooks.markLayoutEffectsStopped();
			}
			function markPassiveEffectsStarted(lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStarted === "function") injectedProfilingHooks.markPassiveEffectsStarted(lanes);
			}
			function markPassiveEffectsStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markPassiveEffectsStopped === "function") injectedProfilingHooks.markPassiveEffectsStopped();
			}
			function markRenderStarted(lanes) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStarted === "function") injectedProfilingHooks.markRenderStarted(lanes);
			}
			function markRenderYielded() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderYielded === "function") injectedProfilingHooks.markRenderYielded();
			}
			function markRenderStopped() {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderStopped === "function") injectedProfilingHooks.markRenderStopped();
			}
			function markRenderScheduled(lane) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markRenderScheduled === "function") injectedProfilingHooks.markRenderScheduled(lane);
			}
			function markForceUpdateScheduled(fiber, lane) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markForceUpdateScheduled === "function") injectedProfilingHooks.markForceUpdateScheduled(fiber, lane);
			}
			function markStateUpdateScheduled(fiber, lane) {
				if (injectedProfilingHooks !== null && typeof injectedProfilingHooks.markStateUpdateScheduled === "function") injectedProfilingHooks.markStateUpdateScheduled(fiber, lane);
			}
			var NoMode = 0;
			var ConcurrentMode = 1;
			var ProfileMode = 2;
			var StrictLegacyMode = 8;
			var StrictEffectsMode = 16;
			var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback;
			var log = Math.log;
			var LN2 = Math.LN2;
			function clz32Fallback(x) {
				var asUint = x >>> 0;
				if (asUint === 0) return 32;
				return 31 - (log(asUint) / LN2 | 0) | 0;
			}
			var TotalLanes = 31;
			var NoLanes = 0;
			var NoLane = 0;
			var SyncLane = 1;
			var InputContinuousHydrationLane = 2;
			var InputContinuousLane = 4;
			var DefaultHydrationLane = 8;
			var DefaultLane = 16;
			var TransitionHydrationLane = 32;
			var TransitionLanes = 4194240;
			var TransitionLane1 = 64;
			var TransitionLane2 = 128;
			var TransitionLane3 = 256;
			var TransitionLane4 = 512;
			var TransitionLane5 = 1024;
			var TransitionLane6 = 2048;
			var TransitionLane7 = 4096;
			var TransitionLane8 = 8192;
			var TransitionLane9 = 16384;
			var TransitionLane10 = 32768;
			var TransitionLane11 = 65536;
			var TransitionLane12 = 131072;
			var TransitionLane13 = 262144;
			var TransitionLane14 = 524288;
			var TransitionLane15 = 1048576;
			var TransitionLane16 = 2097152;
			var RetryLanes = 130023424;
			var RetryLane1 = 4194304;
			var RetryLane2 = 8388608;
			var RetryLane3 = 16777216;
			var RetryLane4 = 33554432;
			var RetryLane5 = 67108864;
			var SomeRetryLane = RetryLane1;
			var SelectiveHydrationLane = 134217728;
			var NonIdleLanes = 268435455;
			var IdleHydrationLane = 268435456;
			var IdleLane = 536870912;
			var OffscreenLane = 1073741824;
			function getLabelForLane(lane) {
				if (lane & SyncLane) return "Sync";
				if (lane & InputContinuousHydrationLane) return "InputContinuousHydration";
				if (lane & InputContinuousLane) return "InputContinuous";
				if (lane & DefaultHydrationLane) return "DefaultHydration";
				if (lane & DefaultLane) return "Default";
				if (lane & TransitionHydrationLane) return "TransitionHydration";
				if (lane & TransitionLanes) return "Transition";
				if (lane & RetryLanes) return "Retry";
				if (lane & SelectiveHydrationLane) return "SelectiveHydration";
				if (lane & IdleHydrationLane) return "IdleHydration";
				if (lane & IdleLane) return "Idle";
				if (lane & OffscreenLane) return "Offscreen";
			}
			var NoTimestamp = -1;
			var nextTransitionLane = TransitionLane1;
			var nextRetryLane = RetryLane1;
			function getHighestPriorityLanes(lanes) {
				switch (getHighestPriorityLane(lanes)) {
					case SyncLane: return SyncLane;
					case InputContinuousHydrationLane: return InputContinuousHydrationLane;
					case InputContinuousLane: return InputContinuousLane;
					case DefaultHydrationLane: return DefaultHydrationLane;
					case DefaultLane: return DefaultLane;
					case TransitionHydrationLane: return TransitionHydrationLane;
					case TransitionLane1:
					case TransitionLane2:
					case TransitionLane3:
					case TransitionLane4:
					case TransitionLane5:
					case TransitionLane6:
					case TransitionLane7:
					case TransitionLane8:
					case TransitionLane9:
					case TransitionLane10:
					case TransitionLane11:
					case TransitionLane12:
					case TransitionLane13:
					case TransitionLane14:
					case TransitionLane15:
					case TransitionLane16: return lanes & TransitionLanes;
					case RetryLane1:
					case RetryLane2:
					case RetryLane3:
					case RetryLane4:
					case RetryLane5: return lanes & RetryLanes;
					case SelectiveHydrationLane: return SelectiveHydrationLane;
					case IdleHydrationLane: return IdleHydrationLane;
					case IdleLane: return IdleLane;
					case OffscreenLane: return OffscreenLane;
					default:
						error("Should have found matching lanes. This is a bug in React.");
						return lanes;
				}
			}
			function getNextLanes(root, wipLanes) {
				var pendingLanes = root.pendingLanes;
				if (pendingLanes === NoLanes) return NoLanes;
				var nextLanes = NoLanes;
				var suspendedLanes = root.suspendedLanes;
				var pingedLanes = root.pingedLanes;
				var nonIdlePendingLanes = pendingLanes & NonIdleLanes;
				if (nonIdlePendingLanes !== NoLanes) {
					var nonIdleUnblockedLanes = nonIdlePendingLanes & ~suspendedLanes;
					if (nonIdleUnblockedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(nonIdleUnblockedLanes);
					else {
						var nonIdlePingedLanes = nonIdlePendingLanes & pingedLanes;
						if (nonIdlePingedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(nonIdlePingedLanes);
					}
				} else {
					var unblockedLanes = pendingLanes & ~suspendedLanes;
					if (unblockedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(unblockedLanes);
					else if (pingedLanes !== NoLanes) nextLanes = getHighestPriorityLanes(pingedLanes);
				}
				if (nextLanes === NoLanes) return NoLanes;
				if (wipLanes !== NoLanes && wipLanes !== nextLanes && (wipLanes & suspendedLanes) === NoLanes) {
					var nextLane = getHighestPriorityLane(nextLanes);
					var wipLane = getHighestPriorityLane(wipLanes);
					if (nextLane >= wipLane || nextLane === DefaultLane && (wipLane & TransitionLanes) !== NoLanes) return wipLanes;
				}
				if ((nextLanes & InputContinuousLane) !== NoLanes) nextLanes |= pendingLanes & DefaultLane;
				var entangledLanes = root.entangledLanes;
				if (entangledLanes !== NoLanes) {
					var entanglements = root.entanglements;
					var lanes = nextLanes & entangledLanes;
					while (lanes > 0) {
						var index = pickArbitraryLaneIndex(lanes);
						var lane = 1 << index;
						nextLanes |= entanglements[index];
						lanes &= ~lane;
					}
				}
				return nextLanes;
			}
			function getMostRecentEventTime(root, lanes) {
				var eventTimes = root.eventTimes;
				var mostRecentEventTime = NoTimestamp;
				while (lanes > 0) {
					var index = pickArbitraryLaneIndex(lanes);
					var lane = 1 << index;
					var eventTime = eventTimes[index];
					if (eventTime > mostRecentEventTime) mostRecentEventTime = eventTime;
					lanes &= ~lane;
				}
				return mostRecentEventTime;
			}
			function computeExpirationTime(lane, currentTime) {
				switch (lane) {
					case SyncLane:
					case InputContinuousHydrationLane:
					case InputContinuousLane: return currentTime + 250;
					case DefaultHydrationLane:
					case DefaultLane:
					case TransitionHydrationLane:
					case TransitionLane1:
					case TransitionLane2:
					case TransitionLane3:
					case TransitionLane4:
					case TransitionLane5:
					case TransitionLane6:
					case TransitionLane7:
					case TransitionLane8:
					case TransitionLane9:
					case TransitionLane10:
					case TransitionLane11:
					case TransitionLane12:
					case TransitionLane13:
					case TransitionLane14:
					case TransitionLane15:
					case TransitionLane16: return currentTime + 5e3;
					case RetryLane1:
					case RetryLane2:
					case RetryLane3:
					case RetryLane4:
					case RetryLane5: return NoTimestamp;
					case SelectiveHydrationLane:
					case IdleHydrationLane:
					case IdleLane:
					case OffscreenLane: return NoTimestamp;
					default:
						error("Should have found matching lanes. This is a bug in React.");
						return NoTimestamp;
				}
			}
			function markStarvedLanesAsExpired(root, currentTime) {
				var pendingLanes = root.pendingLanes;
				var suspendedLanes = root.suspendedLanes;
				var pingedLanes = root.pingedLanes;
				var expirationTimes = root.expirationTimes;
				var lanes = pendingLanes;
				while (lanes > 0) {
					var index = pickArbitraryLaneIndex(lanes);
					var lane = 1 << index;
					var expirationTime = expirationTimes[index];
					if (expirationTime === NoTimestamp) {
						if ((lane & suspendedLanes) === NoLanes || (lane & pingedLanes) !== NoLanes) expirationTimes[index] = computeExpirationTime(lane, currentTime);
					} else if (expirationTime <= currentTime) root.expiredLanes |= lane;
					lanes &= ~lane;
				}
			}
			function getHighestPriorityPendingLanes(root) {
				return getHighestPriorityLanes(root.pendingLanes);
			}
			function getLanesToRetrySynchronouslyOnError(root) {
				var everythingButOffscreen = root.pendingLanes & ~OffscreenLane;
				if (everythingButOffscreen !== NoLanes) return everythingButOffscreen;
				if (everythingButOffscreen & OffscreenLane) return OffscreenLane;
				return NoLanes;
			}
			function includesSyncLane(lanes) {
				return (lanes & SyncLane) !== NoLanes;
			}
			function includesNonIdleWork(lanes) {
				return (lanes & NonIdleLanes) !== NoLanes;
			}
			function includesOnlyRetries(lanes) {
				return (lanes & RetryLanes) === lanes;
			}
			function includesOnlyNonUrgentLanes(lanes) {
				return (lanes & (SyncLane | InputContinuousLane | DefaultLane)) === NoLanes;
			}
			function includesOnlyTransitions(lanes) {
				return (lanes & TransitionLanes) === lanes;
			}
			function includesBlockingLane(root, lanes) {
				return (lanes & (InputContinuousHydrationLane | InputContinuousLane | DefaultHydrationLane | DefaultLane)) !== NoLanes;
			}
			function includesExpiredLane(root, lanes) {
				return (lanes & root.expiredLanes) !== NoLanes;
			}
			function isTransitionLane(lane) {
				return (lane & TransitionLanes) !== NoLanes;
			}
			function claimNextTransitionLane() {
				var lane = nextTransitionLane;
				nextTransitionLane <<= 1;
				if ((nextTransitionLane & TransitionLanes) === NoLanes) nextTransitionLane = TransitionLane1;
				return lane;
			}
			function claimNextRetryLane() {
				var lane = nextRetryLane;
				nextRetryLane <<= 1;
				if ((nextRetryLane & RetryLanes) === NoLanes) nextRetryLane = RetryLane1;
				return lane;
			}
			function getHighestPriorityLane(lanes) {
				return lanes & -lanes;
			}
			function pickArbitraryLane(lanes) {
				return getHighestPriorityLane(lanes);
			}
			function pickArbitraryLaneIndex(lanes) {
				return 31 - clz32(lanes);
			}
			function laneToIndex(lane) {
				return pickArbitraryLaneIndex(lane);
			}
			function includesSomeLane(a, b) {
				return (a & b) !== NoLanes;
			}
			function isSubsetOfLanes(set, subset) {
				return (set & subset) === subset;
			}
			function mergeLanes(a, b) {
				return a | b;
			}
			function removeLanes(set, subset) {
				return set & ~subset;
			}
			function intersectLanes(a, b) {
				return a & b;
			}
			function laneToLanes(lane) {
				return lane;
			}
			function higherPriorityLane(a, b) {
				return a !== NoLane && a < b ? a : b;
			}
			function createLaneMap(initial) {
				var laneMap = [];
				for (var i = 0; i < TotalLanes; i++) laneMap.push(initial);
				return laneMap;
			}
			function markRootUpdated(root, updateLane, eventTime) {
				root.pendingLanes |= updateLane;
				if (updateLane !== IdleLane) {
					root.suspendedLanes = NoLanes;
					root.pingedLanes = NoLanes;
				}
				var eventTimes = root.eventTimes;
				var index = laneToIndex(updateLane);
				eventTimes[index] = eventTime;
			}
			function markRootSuspended(root, suspendedLanes) {
				root.suspendedLanes |= suspendedLanes;
				root.pingedLanes &= ~suspendedLanes;
				var expirationTimes = root.expirationTimes;
				var lanes = suspendedLanes;
				while (lanes > 0) {
					var index = pickArbitraryLaneIndex(lanes);
					var lane = 1 << index;
					expirationTimes[index] = NoTimestamp;
					lanes &= ~lane;
				}
			}
			function markRootPinged(root, pingedLanes, eventTime) {
				root.pingedLanes |= root.suspendedLanes & pingedLanes;
			}
			function markRootFinished(root, remainingLanes) {
				var noLongerPendingLanes = root.pendingLanes & ~remainingLanes;
				root.pendingLanes = remainingLanes;
				root.suspendedLanes = NoLanes;
				root.pingedLanes = NoLanes;
				root.expiredLanes &= remainingLanes;
				root.mutableReadLanes &= remainingLanes;
				root.entangledLanes &= remainingLanes;
				var entanglements = root.entanglements;
				var eventTimes = root.eventTimes;
				var expirationTimes = root.expirationTimes;
				var lanes = noLongerPendingLanes;
				while (lanes > 0) {
					var index = pickArbitraryLaneIndex(lanes);
					var lane = 1 << index;
					entanglements[index] = NoLanes;
					eventTimes[index] = NoTimestamp;
					expirationTimes[index] = NoTimestamp;
					lanes &= ~lane;
				}
			}
			function markRootEntangled(root, entangledLanes) {
				var rootEntangledLanes = root.entangledLanes |= entangledLanes;
				var entanglements = root.entanglements;
				var lanes = rootEntangledLanes;
				while (lanes) {
					var index = pickArbitraryLaneIndex(lanes);
					var lane = 1 << index;
					if (lane & entangledLanes | entanglements[index] & entangledLanes) entanglements[index] |= entangledLanes;
					lanes &= ~lane;
				}
			}
			function getBumpedLaneForHydration(root, renderLanes) {
				var renderLane = getHighestPriorityLane(renderLanes);
				var lane;
				switch (renderLane) {
					case InputContinuousLane:
						lane = InputContinuousHydrationLane;
						break;
					case DefaultLane:
						lane = DefaultHydrationLane;
						break;
					case TransitionLane1:
					case TransitionLane2:
					case TransitionLane3:
					case TransitionLane4:
					case TransitionLane5:
					case TransitionLane6:
					case TransitionLane7:
					case TransitionLane8:
					case TransitionLane9:
					case TransitionLane10:
					case TransitionLane11:
					case TransitionLane12:
					case TransitionLane13:
					case TransitionLane14:
					case TransitionLane15:
					case TransitionLane16:
					case RetryLane1:
					case RetryLane2:
					case RetryLane3:
					case RetryLane4:
					case RetryLane5:
						lane = TransitionHydrationLane;
						break;
					case IdleLane:
						lane = IdleHydrationLane;
						break;
					default: lane = NoLane;
				}
				if ((lane & (root.suspendedLanes | renderLanes)) !== NoLane) return NoLane;
				return lane;
			}
			function addFiberToLanesMap(root, fiber, lanes) {
				if (!isDevToolsPresent) return;
				var pendingUpdatersLaneMap = root.pendingUpdatersLaneMap;
				while (lanes > 0) {
					var index = laneToIndex(lanes);
					var lane = 1 << index;
					pendingUpdatersLaneMap[index].add(fiber);
					lanes &= ~lane;
				}
			}
			function movePendingFibersToMemoized(root, lanes) {
				if (!isDevToolsPresent) return;
				var pendingUpdatersLaneMap = root.pendingUpdatersLaneMap;
				var memoizedUpdaters = root.memoizedUpdaters;
				while (lanes > 0) {
					var index = laneToIndex(lanes);
					var lane = 1 << index;
					var updaters = pendingUpdatersLaneMap[index];
					if (updaters.size > 0) {
						updaters.forEach(function(fiber) {
							var alternate = fiber.alternate;
							if (alternate === null || !memoizedUpdaters.has(alternate)) memoizedUpdaters.add(fiber);
						});
						updaters.clear();
					}
					lanes &= ~lane;
				}
			}
			function getTransitionsForLanes(root, lanes) {
				return null;
			}
			var DiscreteEventPriority = SyncLane;
			var ContinuousEventPriority = InputContinuousLane;
			var DefaultEventPriority = DefaultLane;
			var IdleEventPriority = IdleLane;
			var currentUpdatePriority = NoLane;
			function getCurrentUpdatePriority() {
				return currentUpdatePriority;
			}
			function setCurrentUpdatePriority(newPriority) {
				currentUpdatePriority = newPriority;
			}
			function runWithPriority(priority, fn) {
				var previousPriority = currentUpdatePriority;
				try {
					currentUpdatePriority = priority;
					return fn();
				} finally {
					currentUpdatePriority = previousPriority;
				}
			}
			function higherEventPriority(a, b) {
				return a !== 0 && a < b ? a : b;
			}
			function lowerEventPriority(a, b) {
				return a === 0 || a > b ? a : b;
			}
			function isHigherEventPriority(a, b) {
				return a !== 0 && a < b;
			}
			function lanesToEventPriority(lanes) {
				var lane = getHighestPriorityLane(lanes);
				if (!isHigherEventPriority(DiscreteEventPriority, lane)) return DiscreteEventPriority;
				if (!isHigherEventPriority(ContinuousEventPriority, lane)) return ContinuousEventPriority;
				if (includesNonIdleWork(lane)) return DefaultEventPriority;
				return IdleEventPriority;
			}
			function isRootDehydrated(root) {
				return root.current.memoizedState.isDehydrated;
			}
			var _attemptSynchronousHydration;
			function setAttemptSynchronousHydration(fn) {
				_attemptSynchronousHydration = fn;
			}
			function attemptSynchronousHydration(fiber) {
				_attemptSynchronousHydration(fiber);
			}
			var attemptContinuousHydration;
			function setAttemptContinuousHydration(fn) {
				attemptContinuousHydration = fn;
			}
			var attemptHydrationAtCurrentPriority;
			function setAttemptHydrationAtCurrentPriority(fn) {
				attemptHydrationAtCurrentPriority = fn;
			}
			var getCurrentUpdatePriority$1;
			function setGetCurrentUpdatePriority(fn) {
				getCurrentUpdatePriority$1 = fn;
			}
			var attemptHydrationAtPriority;
			function setAttemptHydrationAtPriority(fn) {
				attemptHydrationAtPriority = fn;
			}
			var hasScheduledReplayAttempt = false;
			var queuedDiscreteEvents = [];
			var queuedFocus = null;
			var queuedDrag = null;
			var queuedMouse = null;
			var queuedPointers = /* @__PURE__ */ new Map();
			var queuedPointerCaptures = /* @__PURE__ */ new Map();
			var queuedExplicitHydrationTargets = [];
			var discreteReplayableEvents = [
				"mousedown",
				"mouseup",
				"touchcancel",
				"touchend",
				"touchstart",
				"auxclick",
				"dblclick",
				"pointercancel",
				"pointerdown",
				"pointerup",
				"dragend",
				"dragstart",
				"drop",
				"compositionend",
				"compositionstart",
				"keydown",
				"keypress",
				"keyup",
				"input",
				"textInput",
				"copy",
				"cut",
				"paste",
				"click",
				"change",
				"contextmenu",
				"reset",
				"submit"
			];
			function isDiscreteEventThatRequiresHydration(eventType) {
				return discreteReplayableEvents.indexOf(eventType) > -1;
			}
			function createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				return {
					blockedOn,
					domEventName,
					eventSystemFlags,
					nativeEvent,
					targetContainers: [targetContainer]
				};
			}
			function clearIfContinuousEvent(domEventName, nativeEvent) {
				switch (domEventName) {
					case "focusin":
					case "focusout":
						queuedFocus = null;
						break;
					case "dragenter":
					case "dragleave":
						queuedDrag = null;
						break;
					case "mouseover":
					case "mouseout":
						queuedMouse = null;
						break;
					case "pointerover":
					case "pointerout":
						var pointerId = nativeEvent.pointerId;
						queuedPointers.delete(pointerId);
						break;
					case "gotpointercapture":
					case "lostpointercapture":
						var _pointerId = nativeEvent.pointerId;
						queuedPointerCaptures.delete(_pointerId);
				}
			}
			function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				if (existingQueuedEvent === null || existingQueuedEvent.nativeEvent !== nativeEvent) {
					var queuedEvent = createQueuedReplayableEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
					if (blockedOn !== null) {
						var _fiber2 = getInstanceFromNode(blockedOn);
						if (_fiber2 !== null) attemptContinuousHydration(_fiber2);
					}
					return queuedEvent;
				}
				existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
				var targetContainers = existingQueuedEvent.targetContainers;
				if (targetContainer !== null && targetContainers.indexOf(targetContainer) === -1) targetContainers.push(targetContainer);
				return existingQueuedEvent;
			}
			function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				switch (domEventName) {
					case "focusin":
						queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(queuedFocus, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
						return true;
					case "dragenter":
						queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(queuedDrag, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
						return true;
					case "mouseover":
						queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(queuedMouse, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent);
						return true;
					case "pointerover":
						var pointerEvent = nativeEvent;
						var pointerId = pointerEvent.pointerId;
						queuedPointers.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, pointerEvent));
						return true;
					case "gotpointercapture":
						var _pointerEvent = nativeEvent;
						var _pointerId2 = _pointerEvent.pointerId;
						queuedPointerCaptures.set(_pointerId2, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointerCaptures.get(_pointerId2) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, _pointerEvent));
						return true;
				}
				return false;
			}
			function attemptExplicitHydrationTarget(queuedTarget) {
				var targetInst = getClosestInstanceFromNode(queuedTarget.target);
				if (targetInst !== null) {
					var nearestMounted = getNearestMountedFiber(targetInst);
					if (nearestMounted !== null) {
						var tag = nearestMounted.tag;
						if (tag === SuspenseComponent) {
							var instance = getSuspenseInstanceFromFiber(nearestMounted);
							if (instance !== null) {
								queuedTarget.blockedOn = instance;
								attemptHydrationAtPriority(queuedTarget.priority, function() {
									attemptHydrationAtCurrentPriority(nearestMounted);
								});
								return;
							}
						} else if (tag === HostRoot) {
							var root = nearestMounted.stateNode;
							if (isRootDehydrated(root)) {
								queuedTarget.blockedOn = getContainerFromFiber(nearestMounted);
								return;
							}
						}
					}
				}
				queuedTarget.blockedOn = null;
			}
			function queueExplicitHydrationTarget(target) {
				var updatePriority = getCurrentUpdatePriority$1();
				var queuedTarget = {
					blockedOn: null,
					target,
					priority: updatePriority
				};
				var i = 0;
				for (; i < queuedExplicitHydrationTargets.length; i++) if (!isHigherEventPriority(updatePriority, queuedExplicitHydrationTargets[i].priority)) break;
				queuedExplicitHydrationTargets.splice(i, 0, queuedTarget);
				if (i === 0) attemptExplicitHydrationTarget(queuedTarget);
			}
			function attemptReplayContinuousQueuedEvent(queuedEvent) {
				if (queuedEvent.blockedOn !== null) return false;
				var targetContainers = queuedEvent.targetContainers;
				while (targetContainers.length > 0) {
					var targetContainer = targetContainers[0];
					var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.domEventName, queuedEvent.eventSystemFlags, targetContainer, queuedEvent.nativeEvent);
					if (nextBlockedOn === null) {
						var nativeEvent = queuedEvent.nativeEvent;
						var nativeEventClone = new nativeEvent.constructor(nativeEvent.type, nativeEvent);
						setReplayingEvent(nativeEventClone);
						nativeEvent.target.dispatchEvent(nativeEventClone);
						resetReplayingEvent();
					} else {
						var _fiber3 = getInstanceFromNode(nextBlockedOn);
						if (_fiber3 !== null) attemptContinuousHydration(_fiber3);
						queuedEvent.blockedOn = nextBlockedOn;
						return false;
					}
					targetContainers.shift();
				}
				return true;
			}
			function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
				if (attemptReplayContinuousQueuedEvent(queuedEvent)) map.delete(key);
			}
			function replayUnblockedEvents() {
				hasScheduledReplayAttempt = false;
				if (queuedFocus !== null && attemptReplayContinuousQueuedEvent(queuedFocus)) queuedFocus = null;
				if (queuedDrag !== null && attemptReplayContinuousQueuedEvent(queuedDrag)) queuedDrag = null;
				if (queuedMouse !== null && attemptReplayContinuousQueuedEvent(queuedMouse)) queuedMouse = null;
				queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
				queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
			}
			function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
				if (queuedEvent.blockedOn === unblocked) {
					queuedEvent.blockedOn = null;
					if (!hasScheduledReplayAttempt) {
						hasScheduledReplayAttempt = true;
						Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents);
					}
				}
			}
			function retryIfBlockedOn(unblocked) {
				if (queuedDiscreteEvents.length > 0) {
					scheduleCallbackIfUnblocked(queuedDiscreteEvents[0], unblocked);
					for (var i = 1; i < queuedDiscreteEvents.length; i++) {
						var queuedEvent = queuedDiscreteEvents[i];
						if (queuedEvent.blockedOn === unblocked) queuedEvent.blockedOn = null;
					}
				}
				if (queuedFocus !== null) scheduleCallbackIfUnblocked(queuedFocus, unblocked);
				if (queuedDrag !== null) scheduleCallbackIfUnblocked(queuedDrag, unblocked);
				if (queuedMouse !== null) scheduleCallbackIfUnblocked(queuedMouse, unblocked);
				var unblock = function(queuedEvent) {
					return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
				};
				queuedPointers.forEach(unblock);
				queuedPointerCaptures.forEach(unblock);
				for (var _i = 0; _i < queuedExplicitHydrationTargets.length; _i++) {
					var queuedTarget = queuedExplicitHydrationTargets[_i];
					if (queuedTarget.blockedOn === unblocked) queuedTarget.blockedOn = null;
				}
				while (queuedExplicitHydrationTargets.length > 0) {
					var nextExplicitTarget = queuedExplicitHydrationTargets[0];
					if (nextExplicitTarget.blockedOn !== null) break;
					else {
						attemptExplicitHydrationTarget(nextExplicitTarget);
						if (nextExplicitTarget.blockedOn === null) queuedExplicitHydrationTargets.shift();
					}
				}
			}
			var ReactCurrentBatchConfig = ReactSharedInternals.ReactCurrentBatchConfig;
			var _enabled = true;
			function setEnabled(enabled) {
				_enabled = !!enabled;
			}
			function isEnabled() {
				return _enabled;
			}
			function createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags) {
				var eventPriority = getEventPriority(domEventName);
				var listenerWrapper;
				switch (eventPriority) {
					case DiscreteEventPriority:
						listenerWrapper = dispatchDiscreteEvent;
						break;
					case ContinuousEventPriority:
						listenerWrapper = dispatchContinuousEvent;
						break;
					case DefaultEventPriority:
					default: listenerWrapper = dispatchEvent;
				}
				return listenerWrapper.bind(null, domEventName, eventSystemFlags, targetContainer);
			}
			function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
				var previousPriority = getCurrentUpdatePriority();
				var prevTransition = ReactCurrentBatchConfig.transition;
				ReactCurrentBatchConfig.transition = null;
				try {
					setCurrentUpdatePriority(DiscreteEventPriority);
					dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
				} finally {
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig.transition = prevTransition;
				}
			}
			function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
				var previousPriority = getCurrentUpdatePriority();
				var prevTransition = ReactCurrentBatchConfig.transition;
				ReactCurrentBatchConfig.transition = null;
				try {
					setCurrentUpdatePriority(ContinuousEventPriority);
					dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
				} finally {
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig.transition = prevTransition;
				}
			}
			function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				if (!_enabled) return;
				dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent);
			}
			function dispatchEventWithEnableCapturePhaseSelectiveHydrationWithoutDiscreteEventReplay(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				var blockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
				if (blockedOn === null) {
					dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
					clearIfContinuousEvent(domEventName, nativeEvent);
					return;
				}
				if (queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent)) {
					nativeEvent.stopPropagation();
					return;
				}
				clearIfContinuousEvent(domEventName, nativeEvent);
				if (eventSystemFlags & IS_CAPTURE_PHASE && isDiscreteEventThatRequiresHydration(domEventName)) {
					while (blockedOn !== null) {
						var fiber = getInstanceFromNode(blockedOn);
						if (fiber !== null) attemptSynchronousHydration(fiber);
						var nextBlockedOn = findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent);
						if (nextBlockedOn === null) dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
						if (nextBlockedOn === blockedOn) break;
						blockedOn = nextBlockedOn;
					}
					if (blockedOn !== null) nativeEvent.stopPropagation();
					return;
				}
				dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, null, targetContainer);
			}
			var return_targetInst = null;
			function findInstanceBlockingEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
				return_targetInst = null;
				var targetInst = getClosestInstanceFromNode(getEventTarget(nativeEvent));
				if (targetInst !== null) {
					var nearestMounted = getNearestMountedFiber(targetInst);
					if (nearestMounted === null) targetInst = null;
					else {
						var tag = nearestMounted.tag;
						if (tag === SuspenseComponent) {
							var instance = getSuspenseInstanceFromFiber(nearestMounted);
							if (instance !== null) return instance;
							targetInst = null;
						} else if (tag === HostRoot) {
							var root = nearestMounted.stateNode;
							if (isRootDehydrated(root)) return getContainerFromFiber(nearestMounted);
							targetInst = null;
						} else if (nearestMounted !== targetInst) targetInst = null;
					}
				}
				return_targetInst = targetInst;
				return null;
			}
			function getEventPriority(domEventName) {
				switch (domEventName) {
					case "cancel":
					case "click":
					case "close":
					case "contextmenu":
					case "copy":
					case "cut":
					case "auxclick":
					case "dblclick":
					case "dragend":
					case "dragstart":
					case "drop":
					case "focusin":
					case "focusout":
					case "input":
					case "invalid":
					case "keydown":
					case "keypress":
					case "keyup":
					case "mousedown":
					case "mouseup":
					case "paste":
					case "pause":
					case "play":
					case "pointercancel":
					case "pointerdown":
					case "pointerup":
					case "ratechange":
					case "reset":
					case "resize":
					case "seeked":
					case "submit":
					case "touchcancel":
					case "touchend":
					case "touchstart":
					case "volumechange":
					case "change":
					case "selectionchange":
					case "textInput":
					case "compositionstart":
					case "compositionend":
					case "compositionupdate":
					case "beforeblur":
					case "afterblur":
					case "beforeinput":
					case "blur":
					case "fullscreenchange":
					case "focus":
					case "hashchange":
					case "popstate":
					case "select":
					case "selectstart": return DiscreteEventPriority;
					case "drag":
					case "dragenter":
					case "dragexit":
					case "dragleave":
					case "dragover":
					case "mousemove":
					case "mouseout":
					case "mouseover":
					case "pointermove":
					case "pointerout":
					case "pointerover":
					case "scroll":
					case "toggle":
					case "touchmove":
					case "wheel":
					case "mouseenter":
					case "mouseleave":
					case "pointerenter":
					case "pointerleave": return ContinuousEventPriority;
					case "message": switch (getCurrentPriorityLevel()) {
						case ImmediatePriority: return DiscreteEventPriority;
						case UserBlockingPriority: return ContinuousEventPriority;
						case NormalPriority:
						case LowPriority: return DefaultEventPriority;
						case IdlePriority: return IdleEventPriority;
						default: return DefaultEventPriority;
					}
					default: return DefaultEventPriority;
				}
			}
			function addEventBubbleListener(target, eventType, listener) {
				target.addEventListener(eventType, listener, false);
				return listener;
			}
			function addEventCaptureListener(target, eventType, listener) {
				target.addEventListener(eventType, listener, true);
				return listener;
			}
			function addEventCaptureListenerWithPassiveFlag(target, eventType, listener, passive) {
				target.addEventListener(eventType, listener, {
					capture: true,
					passive
				});
				return listener;
			}
			function addEventBubbleListenerWithPassiveFlag(target, eventType, listener, passive) {
				target.addEventListener(eventType, listener, { passive });
				return listener;
			}
			/**
			* These variables store information about text content of a target node,
			* allowing comparison of content before and after a given event.
			*
			* Identify the node where selection currently begins, then observe
			* both its text content and its current position in the DOM. Since the
			* browser may natively replace the target node during composition, we can
			* use its position to find its replacement.
			*
			*
			*/
			var root = null;
			var startText = null;
			var fallbackText = null;
			function initialize(nativeEventTarget) {
				root = nativeEventTarget;
				startText = getText();
				return true;
			}
			function reset() {
				root = null;
				startText = null;
				fallbackText = null;
			}
			function getData() {
				if (fallbackText) return fallbackText;
				var start;
				var startValue = startText;
				var startLength = startValue.length;
				var end;
				var endValue = getText();
				var endLength = endValue.length;
				for (start = 0; start < startLength; start++) if (startValue[start] !== endValue[start]) break;
				var minEnd = startLength - start;
				for (end = 1; end <= minEnd; end++) if (startValue[startLength - end] !== endValue[endLength - end]) break;
				var sliceTail = end > 1 ? 1 - end : void 0;
				fallbackText = endValue.slice(start, sliceTail);
				return fallbackText;
			}
			function getText() {
				if ("value" in root) return root.value;
				return root.textContent;
			}
			/**
			* `charCode` represents the actual "character code" and is safe to use with
			* `String.fromCharCode`. As such, only keys that correspond to printable
			* characters produce a valid `charCode`, the only exception to this is Enter.
			* The Tab-key is considered non-printable and does not have a `charCode`,
			* presumably because it does not produce a tab-character in browsers.
			*
			* @param {object} nativeEvent Native browser event.
			* @return {number} Normalized `charCode` property.
			*/
			function getEventCharCode(nativeEvent) {
				var charCode;
				var keyCode = nativeEvent.keyCode;
				if ("charCode" in nativeEvent) {
					charCode = nativeEvent.charCode;
					if (charCode === 0 && keyCode === 13) charCode = 13;
				} else charCode = keyCode;
				if (charCode === 10) charCode = 13;
				if (charCode >= 32 || charCode === 13) return charCode;
				return 0;
			}
			function functionThatReturnsTrue() {
				return true;
			}
			function functionThatReturnsFalse() {
				return false;
			}
			function createSyntheticEvent(Interface) {
				/**
				* Synthetic events are dispatched by event plugins, typically in response to a
				* top-level event delegation handler.
				*
				* These systems should generally use pooling to reduce the frequency of garbage
				* collection. The system should check `isPersistent` to determine whether the
				* event should be released into the pool after being dispatched. Users that
				* need a persisted event should invoke `persist`.
				*
				* Synthetic events (and subclasses) implement the DOM Level 3 Events API by
				* normalizing browser quirks. Subclasses do not necessarily have to implement a
				* DOM interface; custom application-specific events can also subclass this.
				*/
				function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
					this._reactName = reactName;
					this._targetInst = targetInst;
					this.type = reactEventType;
					this.nativeEvent = nativeEvent;
					this.target = nativeEventTarget;
					this.currentTarget = null;
					for (var _propName in Interface) {
						if (!Interface.hasOwnProperty(_propName)) continue;
						var normalize = Interface[_propName];
						if (normalize) this[_propName] = normalize(nativeEvent);
						else this[_propName] = nativeEvent[_propName];
					}
					if (nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false) this.isDefaultPrevented = functionThatReturnsTrue;
					else this.isDefaultPrevented = functionThatReturnsFalse;
					this.isPropagationStopped = functionThatReturnsFalse;
					return this;
				}
				assign(SyntheticBaseEvent.prototype, {
					preventDefault: function() {
						this.defaultPrevented = true;
						var event = this.nativeEvent;
						if (!event) return;
						if (event.preventDefault) event.preventDefault();
						else if (typeof event.returnValue !== "unknown") event.returnValue = false;
						this.isDefaultPrevented = functionThatReturnsTrue;
					},
					stopPropagation: function() {
						var event = this.nativeEvent;
						if (!event) return;
						if (event.stopPropagation) event.stopPropagation();
						else if (typeof event.cancelBubble !== "unknown") event.cancelBubble = true;
						this.isPropagationStopped = functionThatReturnsTrue;
					},
					/**
					* We release all dispatched `SyntheticEvent`s after each event loop, adding
					* them back into the pool. This allows a way to hold onto a reference that
					* won't be added back into the pool.
					*/
					persist: function() {},
					/**
					* Checks if this event should be released back into the pool.
					*
					* @return {boolean} True if this should not be released, false otherwise.
					*/
					isPersistent: functionThatReturnsTrue
				});
				return SyntheticBaseEvent;
			}
			/**
			* @interface Event
			* @see http://www.w3.org/TR/DOM-Level-3-Events/
			*/
			var EventInterface = {
				eventPhase: 0,
				bubbles: 0,
				cancelable: 0,
				timeStamp: function(event) {
					return event.timeStamp || Date.now();
				},
				defaultPrevented: 0,
				isTrusted: 0
			};
			var SyntheticEvent = createSyntheticEvent(EventInterface);
			var UIEventInterface = assign({}, EventInterface, {
				view: 0,
				detail: 0
			});
			var SyntheticUIEvent = createSyntheticEvent(UIEventInterface);
			var lastMovementX;
			var lastMovementY;
			var lastMouseEvent;
			function updateMouseMovementPolyfillState(event) {
				if (event !== lastMouseEvent) {
					if (lastMouseEvent && event.type === "mousemove") {
						lastMovementX = event.screenX - lastMouseEvent.screenX;
						lastMovementY = event.screenY - lastMouseEvent.screenY;
					} else {
						lastMovementX = 0;
						lastMovementY = 0;
					}
					lastMouseEvent = event;
				}
			}
			/**
			* @interface MouseEvent
			* @see http://www.w3.org/TR/DOM-Level-3-Events/
			*/
			var MouseEventInterface = assign({}, UIEventInterface, {
				screenX: 0,
				screenY: 0,
				clientX: 0,
				clientY: 0,
				pageX: 0,
				pageY: 0,
				ctrlKey: 0,
				shiftKey: 0,
				altKey: 0,
				metaKey: 0,
				getModifierState: getEventModifierState,
				button: 0,
				buttons: 0,
				relatedTarget: function(event) {
					if (event.relatedTarget === void 0) return event.fromElement === event.srcElement ? event.toElement : event.fromElement;
					return event.relatedTarget;
				},
				movementX: function(event) {
					if ("movementX" in event) return event.movementX;
					updateMouseMovementPolyfillState(event);
					return lastMovementX;
				},
				movementY: function(event) {
					if ("movementY" in event) return event.movementY;
					return lastMovementY;
				}
			});
			var SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface);
			var SyntheticDragEvent = createSyntheticEvent(assign({}, MouseEventInterface, { dataTransfer: 0 }));
			var SyntheticFocusEvent = createSyntheticEvent(assign({}, UIEventInterface, { relatedTarget: 0 }));
			var SyntheticAnimationEvent = createSyntheticEvent(assign({}, EventInterface, {
				animationName: 0,
				elapsedTime: 0,
				pseudoElement: 0
			}));
			var SyntheticClipboardEvent = createSyntheticEvent(assign({}, EventInterface, { clipboardData: function(event) {
				return "clipboardData" in event ? event.clipboardData : window.clipboardData;
			} }));
			var SyntheticCompositionEvent = createSyntheticEvent(assign({}, EventInterface, { data: 0 }));
			/**
			* @interface Event
			* @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
			*      /#events-inputevents
			*/
			var SyntheticInputEvent = SyntheticCompositionEvent;
			/**
			* Normalization of deprecated HTML5 `key` values
			* @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
			*/
			var normalizeKey = {
				Esc: "Escape",
				Spacebar: " ",
				Left: "ArrowLeft",
				Up: "ArrowUp",
				Right: "ArrowRight",
				Down: "ArrowDown",
				Del: "Delete",
				Win: "OS",
				Menu: "ContextMenu",
				Apps: "ContextMenu",
				Scroll: "ScrollLock",
				MozPrintableKey: "Unidentified"
			};
			/**
			* Translation from legacy `keyCode` to HTML5 `key`
			* Only special keys supported, all others depend on keyboard layout or browser
			* @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
			*/
			var translateToKey = {
				"8": "Backspace",
				"9": "Tab",
				"12": "Clear",
				"13": "Enter",
				"16": "Shift",
				"17": "Control",
				"18": "Alt",
				"19": "Pause",
				"20": "CapsLock",
				"27": "Escape",
				"32": " ",
				"33": "PageUp",
				"34": "PageDown",
				"35": "End",
				"36": "Home",
				"37": "ArrowLeft",
				"38": "ArrowUp",
				"39": "ArrowRight",
				"40": "ArrowDown",
				"45": "Insert",
				"46": "Delete",
				"112": "F1",
				"113": "F2",
				"114": "F3",
				"115": "F4",
				"116": "F5",
				"117": "F6",
				"118": "F7",
				"119": "F8",
				"120": "F9",
				"121": "F10",
				"122": "F11",
				"123": "F12",
				"144": "NumLock",
				"145": "ScrollLock",
				"224": "Meta"
			};
			/**
			* @param {object} nativeEvent Native browser event.
			* @return {string} Normalized `key` property.
			*/
			function getEventKey(nativeEvent) {
				if (nativeEvent.key) {
					var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
					if (key !== "Unidentified") return key;
				}
				if (nativeEvent.type === "keypress") {
					var charCode = getEventCharCode(nativeEvent);
					return charCode === 13 ? "Enter" : String.fromCharCode(charCode);
				}
				if (nativeEvent.type === "keydown" || nativeEvent.type === "keyup") return translateToKey[nativeEvent.keyCode] || "Unidentified";
				return "";
			}
			/**
			* Translation from modifier key to the associated property in the event.
			* @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
			*/
			var modifierKeyToProp = {
				Alt: "altKey",
				Control: "ctrlKey",
				Meta: "metaKey",
				Shift: "shiftKey"
			};
			function modifierStateGetter(keyArg) {
				var nativeEvent = this.nativeEvent;
				if (nativeEvent.getModifierState) return nativeEvent.getModifierState(keyArg);
				var keyProp = modifierKeyToProp[keyArg];
				return keyProp ? !!nativeEvent[keyProp] : false;
			}
			function getEventModifierState(nativeEvent) {
				return modifierStateGetter;
			}
			var SyntheticKeyboardEvent = createSyntheticEvent(assign({}, UIEventInterface, {
				key: getEventKey,
				code: 0,
				location: 0,
				ctrlKey: 0,
				shiftKey: 0,
				altKey: 0,
				metaKey: 0,
				repeat: 0,
				locale: 0,
				getModifierState: getEventModifierState,
				charCode: function(event) {
					if (event.type === "keypress") return getEventCharCode(event);
					return 0;
				},
				keyCode: function(event) {
					if (event.type === "keydown" || event.type === "keyup") return event.keyCode;
					return 0;
				},
				which: function(event) {
					if (event.type === "keypress") return getEventCharCode(event);
					if (event.type === "keydown" || event.type === "keyup") return event.keyCode;
					return 0;
				}
			}));
			var SyntheticPointerEvent = createSyntheticEvent(assign({}, MouseEventInterface, {
				pointerId: 0,
				width: 0,
				height: 0,
				pressure: 0,
				tangentialPressure: 0,
				tiltX: 0,
				tiltY: 0,
				twist: 0,
				pointerType: 0,
				isPrimary: 0
			}));
			var SyntheticTouchEvent = createSyntheticEvent(assign({}, UIEventInterface, {
				touches: 0,
				targetTouches: 0,
				changedTouches: 0,
				altKey: 0,
				metaKey: 0,
				ctrlKey: 0,
				shiftKey: 0,
				getModifierState: getEventModifierState
			}));
			var SyntheticTransitionEvent = createSyntheticEvent(assign({}, EventInterface, {
				propertyName: 0,
				elapsedTime: 0,
				pseudoElement: 0
			}));
			var SyntheticWheelEvent = createSyntheticEvent(assign({}, MouseEventInterface, {
				deltaX: function(event) {
					return "deltaX" in event ? event.deltaX : "wheelDeltaX" in event ? -event.wheelDeltaX : 0;
				},
				deltaY: function(event) {
					return "deltaY" in event ? event.deltaY : "wheelDeltaY" in event ? -event.wheelDeltaY : "wheelDelta" in event ? -event.wheelDelta : 0;
				},
				deltaZ: 0,
				deltaMode: 0
			}));
			var END_KEYCODES = [
				9,
				13,
				27,
				32
			];
			var START_KEYCODE = 229;
			var canUseCompositionEvent = canUseDOM && "CompositionEvent" in window;
			var documentMode = null;
			if (canUseDOM && "documentMode" in document) documentMode = document.documentMode;
			var canUseTextInputEvent = canUseDOM && "TextEvent" in window && !documentMode;
			var useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);
			var SPACEBAR_CODE = 32;
			var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);
			function registerEvents() {
				registerTwoPhaseEvent("onBeforeInput", [
					"compositionend",
					"keypress",
					"textInput",
					"paste"
				]);
				registerTwoPhaseEvent("onCompositionEnd", [
					"compositionend",
					"focusout",
					"keydown",
					"keypress",
					"keyup",
					"mousedown"
				]);
				registerTwoPhaseEvent("onCompositionStart", [
					"compositionstart",
					"focusout",
					"keydown",
					"keypress",
					"keyup",
					"mousedown"
				]);
				registerTwoPhaseEvent("onCompositionUpdate", [
					"compositionupdate",
					"focusout",
					"keydown",
					"keypress",
					"keyup",
					"mousedown"
				]);
			}
			var hasSpaceKeypress = false;
			/**
			* Return whether a native keypress event is assumed to be a command.
			* This is required because Firefox fires `keypress` events for key commands
			* (cut, copy, select-all, etc.) even though no character is inserted.
			*/
			function isKeypressCommand(nativeEvent) {
				return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) && !(nativeEvent.ctrlKey && nativeEvent.altKey);
			}
			/**
			* Translate native top level events into event types.
			*/
			function getCompositionEventType(domEventName) {
				switch (domEventName) {
					case "compositionstart": return "onCompositionStart";
					case "compositionend": return "onCompositionEnd";
					case "compositionupdate": return "onCompositionUpdate";
				}
			}
			/**
			* Does our fallback best-guess model think this event signifies that
			* composition has begun?
			*/
			function isFallbackCompositionStart(domEventName, nativeEvent) {
				return domEventName === "keydown" && nativeEvent.keyCode === START_KEYCODE;
			}
			/**
			* Does our fallback mode think that this event is the end of composition?
			*/
			function isFallbackCompositionEnd(domEventName, nativeEvent) {
				switch (domEventName) {
					case "keyup": return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
					case "keydown": return nativeEvent.keyCode !== START_KEYCODE;
					case "keypress":
					case "mousedown":
					case "focusout": return true;
					default: return false;
				}
			}
			/**
			* Google Input Tools provides composition data via a CustomEvent,
			* with the `data` property populated in the `detail` object. If this
			* is available on the event object, use it. If not, this is a plain
			* composition event and we have nothing special to extract.
			*
			* @param {object} nativeEvent
			* @return {?string}
			*/
			function getDataFromCustomEvent(nativeEvent) {
				var detail = nativeEvent.detail;
				if (typeof detail === "object" && "data" in detail) return detail.data;
				return null;
			}
			/**
			* Check if a composition event was triggered by Korean IME.
			* Our fallback mode does not work well with IE's Korean IME,
			* so just use native composition events when Korean IME is used.
			* Although CompositionEvent.locale property is deprecated,
			* it is available in IE, where our fallback mode is enabled.
			*
			* @param {object} nativeEvent
			* @return {boolean}
			*/
			function isUsingKoreanIME(nativeEvent) {
				return nativeEvent.locale === "ko";
			}
			var isComposing = false;
			/**
			* @return {?object} A SyntheticCompositionEvent.
			*/
			function extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
				var eventType;
				var fallbackData;
				if (canUseCompositionEvent) eventType = getCompositionEventType(domEventName);
				else if (!isComposing) {
					if (isFallbackCompositionStart(domEventName, nativeEvent)) eventType = "onCompositionStart";
				} else if (isFallbackCompositionEnd(domEventName, nativeEvent)) eventType = "onCompositionEnd";
				if (!eventType) return null;
				if (useFallbackCompositionData && !isUsingKoreanIME(nativeEvent)) {
					if (!isComposing && eventType === "onCompositionStart") isComposing = initialize(nativeEventTarget);
					else if (eventType === "onCompositionEnd") {
						if (isComposing) fallbackData = getData();
					}
				}
				var listeners = accumulateTwoPhaseListeners(targetInst, eventType);
				if (listeners.length > 0) {
					var event = new SyntheticCompositionEvent(eventType, domEventName, null, nativeEvent, nativeEventTarget);
					dispatchQueue.push({
						event,
						listeners
					});
					if (fallbackData) event.data = fallbackData;
					else {
						var customData = getDataFromCustomEvent(nativeEvent);
						if (customData !== null) event.data = customData;
					}
				}
			}
			function getNativeBeforeInputChars(domEventName, nativeEvent) {
				switch (domEventName) {
					case "compositionend": return getDataFromCustomEvent(nativeEvent);
					case "keypress":
						if (nativeEvent.which !== SPACEBAR_CODE) return null;
						hasSpaceKeypress = true;
						return SPACEBAR_CHAR;
					case "textInput":
						var chars = nativeEvent.data;
						if (chars === SPACEBAR_CHAR && hasSpaceKeypress) return null;
						return chars;
					default: return null;
				}
			}
			/**
			* For browsers that do not provide the `textInput` event, extract the
			* appropriate string to use for SyntheticInputEvent.
			*/
			function getFallbackBeforeInputChars(domEventName, nativeEvent) {
				if (isComposing) {
					if (domEventName === "compositionend" || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent)) {
						var chars = getData();
						reset();
						isComposing = false;
						return chars;
					}
					return null;
				}
				switch (domEventName) {
					case "paste": return null;
					case "keypress":
						/**
						* As of v27, Firefox may fire keypress events even when no character
						* will be inserted. A few possibilities:
						*
						* - `which` is `0`. Arrow keys, Esc key, etc.
						*
						* - `which` is the pressed key code, but no char is available.
						*   Ex: 'AltGr + d` in Polish. There is no modified character for
						*   this key combination and no character is inserted into the
						*   document, but FF fires the keypress for char code `100` anyway.
						*   No `input` event will occur.
						*
						* - `which` is the pressed key code, but a command combination is
						*   being used. Ex: `Cmd+C`. No character is inserted, and no
						*   `input` event will occur.
						*/
						if (!isKeypressCommand(nativeEvent)) {
							if (nativeEvent.char && nativeEvent.char.length > 1) return nativeEvent.char;
							else if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
						}
						return null;
					case "compositionend": return useFallbackCompositionData && !isUsingKoreanIME(nativeEvent) ? null : nativeEvent.data;
					default: return null;
				}
			}
			/**
			* Extract a SyntheticInputEvent for `beforeInput`, based on either native
			* `textInput` or fallback behavior.
			*
			* @return {?object} A SyntheticInputEvent.
			*/
			function extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget) {
				var chars;
				if (canUseTextInputEvent) chars = getNativeBeforeInputChars(domEventName, nativeEvent);
				else chars = getFallbackBeforeInputChars(domEventName, nativeEvent);
				if (!chars) return null;
				var listeners = accumulateTwoPhaseListeners(targetInst, "onBeforeInput");
				if (listeners.length > 0) {
					var event = new SyntheticInputEvent("onBeforeInput", "beforeinput", null, nativeEvent, nativeEventTarget);
					dispatchQueue.push({
						event,
						listeners
					});
					event.data = chars;
				}
			}
			/**
			* Create an `onBeforeInput` event to match
			* http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
			*
			* This event plugin is based on the native `textInput` event
			* available in Chrome, Safari, Opera, and IE. This event fires after
			* `onKeyPress` and `onCompositionEnd`, but before `onInput`.
			*
			* `beforeInput` is spec'd but not implemented in any browsers, and
			* the `input` event does not provide any useful information about what has
			* actually been added, contrary to the spec. Thus, `textInput` is the best
			* available event to identify the characters that have actually been inserted
			* into the target node.
			*
			* This plugin is also responsible for emitting `composition` events, thus
			* allowing us to share composition fallback code for both `beforeInput` and
			* `composition` event types.
			*/
			function extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				extractCompositionEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
				extractBeforeInputEvent(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
			}
			/**
			* @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
			*/
			var supportedInputTypes = {
				color: true,
				date: true,
				datetime: true,
				"datetime-local": true,
				email: true,
				month: true,
				number: true,
				password: true,
				range: true,
				search: true,
				tel: true,
				text: true,
				time: true,
				url: true,
				week: true
			};
			function isTextInputElement(elem) {
				var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
				if (nodeName === "input") return !!supportedInputTypes[elem.type];
				if (nodeName === "textarea") return true;
				return false;
			}
			/**
			* Checks if an event is supported in the current execution environment.
			*
			* NOTE: This will not work correctly for non-generic events such as `change`,
			* `reset`, `load`, `error`, and `select`.
			*
			* Borrows from Modernizr.
			*
			* @param {string} eventNameSuffix Event name, e.g. "click".
			* @return {boolean} True if the event is supported.
			* @internal
			* @license Modernizr 3.0.0pre (Custom Build) | MIT
			*/
			function isEventSupported(eventNameSuffix) {
				if (!canUseDOM) return false;
				var eventName = "on" + eventNameSuffix;
				var isSupported = eventName in document;
				if (!isSupported) {
					var element = document.createElement("div");
					element.setAttribute(eventName, "return;");
					isSupported = typeof element[eventName] === "function";
				}
				return isSupported;
			}
			function registerEvents$1() {
				registerTwoPhaseEvent("onChange", [
					"change",
					"click",
					"focusin",
					"focusout",
					"input",
					"keydown",
					"keyup",
					"selectionchange"
				]);
			}
			function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
				enqueueStateRestore(target);
				var listeners = accumulateTwoPhaseListeners(inst, "onChange");
				if (listeners.length > 0) {
					var event = new SyntheticEvent("onChange", "change", null, nativeEvent, target);
					dispatchQueue.push({
						event,
						listeners
					});
				}
			}
			/**
			* For IE shims
			*/
			var activeElement = null;
			var activeElementInst = null;
			/**
			* SECTION: handle `change` event
			*/
			function shouldUseChangeEvent(elem) {
				var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
				return nodeName === "select" || nodeName === "input" && elem.type === "file";
			}
			function manualDispatchChangeEvent(nativeEvent) {
				var dispatchQueue = [];
				createAndAccumulateChangeEvent(dispatchQueue, activeElementInst, nativeEvent, getEventTarget(nativeEvent));
				batchedUpdates(runEventInBatch, dispatchQueue);
			}
			function runEventInBatch(dispatchQueue) {
				processDispatchQueue(dispatchQueue, 0);
			}
			function getInstIfValueChanged(targetInst) {
				if (updateValueIfChanged(getNodeFromInstance(targetInst))) return targetInst;
			}
			function getTargetInstForChangeEvent(domEventName, targetInst) {
				if (domEventName === "change") return targetInst;
			}
			/**
			* SECTION: handle `input` event
			*/
			var isInputEventSupported = false;
			if (canUseDOM) isInputEventSupported = isEventSupported("input") && (!document.documentMode || document.documentMode > 9);
			/**
			* (For IE <=9) Starts tracking propertychange events on the passed-in element
			* and override the value property so that we can distinguish user events from
			* value changes in JS.
			*/
			function startWatchingForValueChange(target, targetInst) {
				activeElement = target;
				activeElementInst = targetInst;
				activeElement.attachEvent("onpropertychange", handlePropertyChange);
			}
			/**
			* (For IE <=9) Removes the event listeners from the currently-tracked element,
			* if any exists.
			*/
			function stopWatchingForValueChange() {
				if (!activeElement) return;
				activeElement.detachEvent("onpropertychange", handlePropertyChange);
				activeElement = null;
				activeElementInst = null;
			}
			/**
			* (For IE <=9) Handles a propertychange event, sending a `change` event if
			* the value of the active element has changed.
			*/
			function handlePropertyChange(nativeEvent) {
				if (nativeEvent.propertyName !== "value") return;
				if (getInstIfValueChanged(activeElementInst)) manualDispatchChangeEvent(nativeEvent);
			}
			function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
				if (domEventName === "focusin") {
					stopWatchingForValueChange();
					startWatchingForValueChange(target, targetInst);
				} else if (domEventName === "focusout") stopWatchingForValueChange();
			}
			function getTargetInstForInputEventPolyfill(domEventName, targetInst) {
				if (domEventName === "selectionchange" || domEventName === "keyup" || domEventName === "keydown") return getInstIfValueChanged(activeElementInst);
			}
			/**
			* SECTION: handle `click` event
			*/
			function shouldUseClickEvent(elem) {
				var nodeName = elem.nodeName;
				return nodeName && nodeName.toLowerCase() === "input" && (elem.type === "checkbox" || elem.type === "radio");
			}
			function getTargetInstForClickEvent(domEventName, targetInst) {
				if (domEventName === "click") return getInstIfValueChanged(targetInst);
			}
			function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
				if (domEventName === "input" || domEventName === "change") return getInstIfValueChanged(targetInst);
			}
			function handleControlledInputBlur(node) {
				var state = node._wrapperState;
				if (!state || !state.controlled || node.type !== "number") return;
				setDefaultValue(node, "number", node.value);
			}
			/**
			* This plugin creates an `onChange` event that normalizes change events
			* across form elements. This event fires at a time when it's possible to
			* change the element's value without seeing a flicker.
			*
			* Supported elements are:
			* - input (see `isTextInputElement`)
			* - textarea
			* - select
			*/
			function extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
				var getTargetInstFunc, handleEventFunc;
				if (shouldUseChangeEvent(targetNode)) getTargetInstFunc = getTargetInstForChangeEvent;
				else if (isTextInputElement(targetNode)) {
					if (isInputEventSupported) getTargetInstFunc = getTargetInstForInputOrChangeEvent;
					else {
						getTargetInstFunc = getTargetInstForInputEventPolyfill;
						handleEventFunc = handleEventsForInputEventPolyfill;
					}
				} else if (shouldUseClickEvent(targetNode)) getTargetInstFunc = getTargetInstForClickEvent;
				if (getTargetInstFunc) {
					var inst = getTargetInstFunc(domEventName, targetInst);
					if (inst) {
						createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, nativeEventTarget);
						return;
					}
				}
				if (handleEventFunc) handleEventFunc(domEventName, targetNode, targetInst);
				if (domEventName === "focusout") handleControlledInputBlur(targetNode);
			}
			function registerEvents$2() {
				registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
				registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
				registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
				registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
			}
			/**
			* For almost every interaction we care about, there will be both a top-level
			* `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
			* we do not extract duplicate events. However, moving the mouse into the
			* browser from outside will not fire a `mouseout` event. In this case, we use
			* the `mouseover` top-level event.
			*/
			function extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				var isOverEvent = domEventName === "mouseover" || domEventName === "pointerover";
				var isOutEvent = domEventName === "mouseout" || domEventName === "pointerout";
				if (isOverEvent && !isReplayingEvent(nativeEvent)) {
					var related = nativeEvent.relatedTarget || nativeEvent.fromElement;
					if (related) {
						if (getClosestInstanceFromNode(related) || isContainerMarkedAsRoot(related)) return;
					}
				}
				if (!isOutEvent && !isOverEvent) return;
				var win;
				if (nativeEventTarget.window === nativeEventTarget) win = nativeEventTarget;
				else {
					var doc = nativeEventTarget.ownerDocument;
					if (doc) win = doc.defaultView || doc.parentWindow;
					else win = window;
				}
				var from;
				var to;
				if (isOutEvent) {
					var _related = nativeEvent.relatedTarget || nativeEvent.toElement;
					from = targetInst;
					to = _related ? getClosestInstanceFromNode(_related) : null;
					if (to !== null) {
						var nearestMounted = getNearestMountedFiber(to);
						if (to !== nearestMounted || to.tag !== HostComponent && to.tag !== HostText) to = null;
					}
				} else {
					from = null;
					to = targetInst;
				}
				if (from === to) return;
				var SyntheticEventCtor = SyntheticMouseEvent;
				var leaveEventType = "onMouseLeave";
				var enterEventType = "onMouseEnter";
				var eventTypePrefix = "mouse";
				if (domEventName === "pointerout" || domEventName === "pointerover") {
					SyntheticEventCtor = SyntheticPointerEvent;
					leaveEventType = "onPointerLeave";
					enterEventType = "onPointerEnter";
					eventTypePrefix = "pointer";
				}
				var fromNode = from == null ? win : getNodeFromInstance(from);
				var toNode = to == null ? win : getNodeFromInstance(to);
				var leave = new SyntheticEventCtor(leaveEventType, eventTypePrefix + "leave", from, nativeEvent, nativeEventTarget);
				leave.target = fromNode;
				leave.relatedTarget = toNode;
				var enter = null;
				if (getClosestInstanceFromNode(nativeEventTarget) === targetInst) {
					var enterEvent = new SyntheticEventCtor(enterEventType, eventTypePrefix + "enter", to, nativeEvent, nativeEventTarget);
					enterEvent.target = toNode;
					enterEvent.relatedTarget = fromNode;
					enter = enterEvent;
				}
				accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leave, enter, from, to);
			}
			/**
			* inlined Object.is polyfill to avoid requiring consumers ship their own
			* https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
			*/
			function is(x, y) {
				return x === y && (x !== 0 || 1 / x === 1 / y) || x !== x && y !== y;
			}
			var objectIs = typeof Object.is === "function" ? Object.is : is;
			/**
			* Performs equality by iterating through keys on an object and returning false
			* when any key has values which are not strictly equal between the arguments.
			* Returns true when the values of all keys are strictly equal.
			*/
			function shallowEqual(objA, objB) {
				if (objectIs(objA, objB)) return true;
				if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) return false;
				var keysA = Object.keys(objA);
				var keysB = Object.keys(objB);
				if (keysA.length !== keysB.length) return false;
				for (var i = 0; i < keysA.length; i++) {
					var currentKey = keysA[i];
					if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey])) return false;
				}
				return true;
			}
			/**
			* Given any node return the first leaf node without children.
			*
			* @param {DOMElement|DOMTextNode} node
			* @return {DOMElement|DOMTextNode}
			*/
			function getLeafNode(node) {
				while (node && node.firstChild) node = node.firstChild;
				return node;
			}
			/**
			* Get the next sibling within a container. This will walk up the
			* DOM if a node's siblings have been exhausted.
			*
			* @param {DOMElement|DOMTextNode} node
			* @return {?DOMElement|DOMTextNode}
			*/
			function getSiblingNode(node) {
				while (node) {
					if (node.nextSibling) return node.nextSibling;
					node = node.parentNode;
				}
			}
			/**
			* Get object describing the nodes which contain characters at offset.
			*
			* @param {DOMElement|DOMTextNode} root
			* @param {number} offset
			* @return {?object}
			*/
			function getNodeForCharacterOffset(root, offset) {
				var node = getLeafNode(root);
				var nodeStart = 0;
				var nodeEnd = 0;
				while (node) {
					if (node.nodeType === TEXT_NODE) {
						nodeEnd = nodeStart + node.textContent.length;
						if (nodeStart <= offset && nodeEnd >= offset) return {
							node,
							offset: offset - nodeStart
						};
						nodeStart = nodeEnd;
					}
					node = getLeafNode(getSiblingNode(node));
				}
			}
			/**
			* @param {DOMElement} outerNode
			* @return {?object}
			*/
			function getOffsets(outerNode) {
				var ownerDocument = outerNode.ownerDocument;
				var win = ownerDocument && ownerDocument.defaultView || window;
				var selection = win.getSelection && win.getSelection();
				if (!selection || selection.rangeCount === 0) return null;
				var anchorNode = selection.anchorNode, anchorOffset = selection.anchorOffset, focusNode = selection.focusNode, focusOffset = selection.focusOffset;
				try {
					anchorNode.nodeType;
					focusNode.nodeType;
				} catch (e) {
					return null;
				}
				return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
			}
			/**
			* Returns {start, end} where `start` is the character/codepoint index of
			* (anchorNode, anchorOffset) within the textContent of `outerNode`, and
			* `end` is the index of (focusNode, focusOffset).
			*
			* Returns null if you pass in garbage input but we should probably just crash.
			*
			* Exported only for testing.
			*/
			function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
				var length = 0;
				var start = -1;
				var end = -1;
				var indexWithinAnchor = 0;
				var indexWithinFocus = 0;
				var node = outerNode;
				var parentNode = null;
				outer: while (true) {
					var next = null;
					while (true) {
						if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) start = length + anchorOffset;
						if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) end = length + focusOffset;
						if (node.nodeType === TEXT_NODE) length += node.nodeValue.length;
						if ((next = node.firstChild) === null) break;
						parentNode = node;
						node = next;
					}
					while (true) {
						if (node === outerNode) break outer;
						if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) start = length;
						if (parentNode === focusNode && ++indexWithinFocus === focusOffset) end = length;
						if ((next = node.nextSibling) !== null) break;
						node = parentNode;
						parentNode = node.parentNode;
					}
					node = next;
				}
				if (start === -1 || end === -1) return null;
				return {
					start,
					end
				};
			}
			/**
			* In modern non-IE browsers, we can support both forward and backward
			* selections.
			*
			* Note: IE10+ supports the Selection object, but it does not support
			* the `extend` method, which means that even in modern IE, it's not possible
			* to programmatically create a backward selection. Thus, for all IE
			* versions, we use the old IE API to create our selections.
			*
			* @param {DOMElement|DOMTextNode} node
			* @param {object} offsets
			*/
			function setOffsets(node, offsets) {
				var doc = node.ownerDocument || document;
				var win = doc && doc.defaultView || window;
				if (!win.getSelection) return;
				var selection = win.getSelection();
				var length = node.textContent.length;
				var start = Math.min(offsets.start, length);
				var end = offsets.end === void 0 ? start : Math.min(offsets.end, length);
				if (!selection.extend && start > end) {
					var temp = end;
					end = start;
					start = temp;
				}
				var startMarker = getNodeForCharacterOffset(node, start);
				var endMarker = getNodeForCharacterOffset(node, end);
				if (startMarker && endMarker) {
					if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) return;
					var range = doc.createRange();
					range.setStart(startMarker.node, startMarker.offset);
					selection.removeAllRanges();
					if (start > end) {
						selection.addRange(range);
						selection.extend(endMarker.node, endMarker.offset);
					} else {
						range.setEnd(endMarker.node, endMarker.offset);
						selection.addRange(range);
					}
				}
			}
			function isTextNode(node) {
				return node && node.nodeType === TEXT_NODE;
			}
			function containsNode(outerNode, innerNode) {
				if (!outerNode || !innerNode) return false;
				else if (outerNode === innerNode) return true;
				else if (isTextNode(outerNode)) return false;
				else if (isTextNode(innerNode)) return containsNode(outerNode, innerNode.parentNode);
				else if ("contains" in outerNode) return outerNode.contains(innerNode);
				else if (outerNode.compareDocumentPosition) return !!(outerNode.compareDocumentPosition(innerNode) & 16);
				else return false;
			}
			function isInDocument(node) {
				return node && node.ownerDocument && containsNode(node.ownerDocument.documentElement, node);
			}
			function isSameOriginFrame(iframe) {
				try {
					return typeof iframe.contentWindow.location.href === "string";
				} catch (err) {
					return false;
				}
			}
			function getActiveElementDeep() {
				var win = window;
				var element = getActiveElement();
				while (element instanceof win.HTMLIFrameElement) {
					if (isSameOriginFrame(element)) win = element.contentWindow;
					else return element;
					element = getActiveElement(win.document);
				}
				return element;
			}
			/**
			* @ReactInputSelection: React input selection module. Based on Selection.js,
			* but modified to be suitable for react and has a couple of bug fixes (doesn't
			* assume buttons have range selections allowed).
			* Input selection module for React.
			*/
			/**
			* @hasSelectionCapabilities: we get the element types that support selection
			* from https://html.spec.whatwg.org/#do-not-apply, looking at `selectionStart`
			* and `selectionEnd` rows.
			*/
			function hasSelectionCapabilities(elem) {
				var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
				return nodeName && (nodeName === "input" && (elem.type === "text" || elem.type === "search" || elem.type === "tel" || elem.type === "url" || elem.type === "password") || nodeName === "textarea" || elem.contentEditable === "true");
			}
			function getSelectionInformation() {
				var focusedElem = getActiveElementDeep();
				return {
					focusedElem,
					selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection(focusedElem) : null
				};
			}
			/**
			* @restoreSelection: If any selection information was potentially lost,
			* restore it. This is useful when performing operations that could remove dom
			* nodes and place them back in, resulting in focus being lost.
			*/
			function restoreSelection(priorSelectionInformation) {
				var curFocusedElem = getActiveElementDeep();
				var priorFocusedElem = priorSelectionInformation.focusedElem;
				var priorSelectionRange = priorSelectionInformation.selectionRange;
				if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
					if (priorSelectionRange !== null && hasSelectionCapabilities(priorFocusedElem)) setSelection(priorFocusedElem, priorSelectionRange);
					var ancestors = [];
					var ancestor = priorFocusedElem;
					while (ancestor = ancestor.parentNode) if (ancestor.nodeType === ELEMENT_NODE) ancestors.push({
						element: ancestor,
						left: ancestor.scrollLeft,
						top: ancestor.scrollTop
					});
					if (typeof priorFocusedElem.focus === "function") priorFocusedElem.focus();
					for (var i = 0; i < ancestors.length; i++) {
						var info = ancestors[i];
						info.element.scrollLeft = info.left;
						info.element.scrollTop = info.top;
					}
				}
			}
			/**
			* @getSelection: Gets the selection bounds of a focused textarea, input or
			* contentEditable node.
			* -@input: Look up selection bounds of this input
			* -@return {start: selectionStart, end: selectionEnd}
			*/
			function getSelection(input) {
				var selection;
				if ("selectionStart" in input) selection = {
					start: input.selectionStart,
					end: input.selectionEnd
				};
				else selection = getOffsets(input);
				return selection || {
					start: 0,
					end: 0
				};
			}
			/**
			* @setSelection: Sets the selection bounds of a textarea or input and focuses
			* the input.
			* -@input     Set selection bounds of this input or textarea
			* -@offsets   Object of same form that is returned from get*
			*/
			function setSelection(input, offsets) {
				var start = offsets.start;
				var end = offsets.end;
				if (end === void 0) end = start;
				if ("selectionStart" in input) {
					input.selectionStart = start;
					input.selectionEnd = Math.min(end, input.value.length);
				} else setOffsets(input, offsets);
			}
			var skipSelectionChangeEvent = canUseDOM && "documentMode" in document && document.documentMode <= 11;
			function registerEvents$3() {
				registerTwoPhaseEvent("onSelect", [
					"focusout",
					"contextmenu",
					"dragend",
					"focusin",
					"keydown",
					"keyup",
					"mousedown",
					"mouseup",
					"selectionchange"
				]);
			}
			var activeElement$1 = null;
			var activeElementInst$1 = null;
			var lastSelection = null;
			var mouseDown = false;
			/**
			* Get an object which is a unique representation of the current selection.
			*
			* The return value will not be consistent across nodes or browsers, but
			* two identical selections on the same node will return identical objects.
			*/
			function getSelection$1(node) {
				if ("selectionStart" in node && hasSelectionCapabilities(node)) return {
					start: node.selectionStart,
					end: node.selectionEnd
				};
				else {
					var selection = (node.ownerDocument && node.ownerDocument.defaultView || window).getSelection();
					return {
						anchorNode: selection.anchorNode,
						anchorOffset: selection.anchorOffset,
						focusNode: selection.focusNode,
						focusOffset: selection.focusOffset
					};
				}
			}
			/**
			* Get document associated with the event target.
			*/
			function getEventTargetDocument(eventTarget) {
				return eventTarget.window === eventTarget ? eventTarget.document : eventTarget.nodeType === DOCUMENT_NODE ? eventTarget : eventTarget.ownerDocument;
			}
			/**
			* Poll selection to see whether it's changed.
			*
			* @param {object} nativeEvent
			* @param {object} nativeEventTarget
			* @return {?SyntheticEvent}
			*/
			function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
				var doc = getEventTargetDocument(nativeEventTarget);
				if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement(doc)) return;
				var currentSelection = getSelection$1(activeElement$1);
				if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
					lastSelection = currentSelection;
					var listeners = accumulateTwoPhaseListeners(activeElementInst$1, "onSelect");
					if (listeners.length > 0) {
						var event = new SyntheticEvent("onSelect", "select", null, nativeEvent, nativeEventTarget);
						dispatchQueue.push({
							event,
							listeners
						});
						event.target = activeElement$1;
					}
				}
			}
			/**
			* This plugin creates an `onSelect` event that normalizes select events
			* across form elements.
			*
			* Supported elements are:
			* - input (see `isTextInputElement`)
			* - textarea
			* - contentEditable
			*
			* This differs from native browser implementations in the following ways:
			* - Fires on contentEditable fields as well as inputs.
			* - Fires for collapsed selection.
			* - Fires after user input.
			*/
			function extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				var targetNode = targetInst ? getNodeFromInstance(targetInst) : window;
				switch (domEventName) {
					case "focusin":
						if (isTextInputElement(targetNode) || targetNode.contentEditable === "true") {
							activeElement$1 = targetNode;
							activeElementInst$1 = targetInst;
							lastSelection = null;
						}
						break;
					case "focusout":
						activeElement$1 = null;
						activeElementInst$1 = null;
						lastSelection = null;
						break;
					case "mousedown":
						mouseDown = true;
						break;
					case "contextmenu":
					case "mouseup":
					case "dragend":
						mouseDown = false;
						constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
						break;
					case "selectionchange": if (skipSelectionChangeEvent) break;
					case "keydown":
					case "keyup": constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
				}
			}
			/**
			* Generate a mapping of standard vendor prefixes using the defined style property and event name.
			*
			* @param {string} styleProp
			* @param {string} eventName
			* @returns {object}
			*/
			function makePrefixMap(styleProp, eventName) {
				var prefixes = {};
				prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
				prefixes["Webkit" + styleProp] = "webkit" + eventName;
				prefixes["Moz" + styleProp] = "moz" + eventName;
				return prefixes;
			}
			/**
			* A list of event names to a configurable list of vendor prefixes.
			*/
			var vendorPrefixes = {
				animationend: makePrefixMap("Animation", "AnimationEnd"),
				animationiteration: makePrefixMap("Animation", "AnimationIteration"),
				animationstart: makePrefixMap("Animation", "AnimationStart"),
				transitionend: makePrefixMap("Transition", "TransitionEnd")
			};
			/**
			* Event names that have already been detected and prefixed (if applicable).
			*/
			var prefixedEventNames = {};
			/**
			* Element to check for prefixes on.
			*/
			var style = {};
			/**
			* Bootstrap if a DOM exists.
			*/
			if (canUseDOM) {
				style = document.createElement("div").style;
				if (!("AnimationEvent" in window)) {
					delete vendorPrefixes.animationend.animation;
					delete vendorPrefixes.animationiteration.animation;
					delete vendorPrefixes.animationstart.animation;
				}
				if (!("TransitionEvent" in window)) delete vendorPrefixes.transitionend.transition;
			}
			/**
			* Attempts to determine the correct vendor prefixed event name.
			*
			* @param {string} eventName
			* @returns {string}
			*/
			function getVendorPrefixedEventName(eventName) {
				if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
				else if (!vendorPrefixes[eventName]) return eventName;
				var prefixMap = vendorPrefixes[eventName];
				for (var styleProp in prefixMap) if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) return prefixedEventNames[eventName] = prefixMap[styleProp];
				return eventName;
			}
			var ANIMATION_END = getVendorPrefixedEventName("animationend");
			var ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration");
			var ANIMATION_START = getVendorPrefixedEventName("animationstart");
			var TRANSITION_END = getVendorPrefixedEventName("transitionend");
			var topLevelEventsToReactNames = /* @__PURE__ */ new Map();
			var simpleEventPluginEvents = [
				"abort",
				"auxClick",
				"cancel",
				"canPlay",
				"canPlayThrough",
				"click",
				"close",
				"contextMenu",
				"copy",
				"cut",
				"drag",
				"dragEnd",
				"dragEnter",
				"dragExit",
				"dragLeave",
				"dragOver",
				"dragStart",
				"drop",
				"durationChange",
				"emptied",
				"encrypted",
				"ended",
				"error",
				"gotPointerCapture",
				"input",
				"invalid",
				"keyDown",
				"keyPress",
				"keyUp",
				"load",
				"loadedData",
				"loadedMetadata",
				"loadStart",
				"lostPointerCapture",
				"mouseDown",
				"mouseMove",
				"mouseOut",
				"mouseOver",
				"mouseUp",
				"paste",
				"pause",
				"play",
				"playing",
				"pointerCancel",
				"pointerDown",
				"pointerMove",
				"pointerOut",
				"pointerOver",
				"pointerUp",
				"progress",
				"rateChange",
				"reset",
				"resize",
				"seeked",
				"seeking",
				"stalled",
				"submit",
				"suspend",
				"timeUpdate",
				"touchCancel",
				"touchEnd",
				"touchStart",
				"volumeChange",
				"scroll",
				"toggle",
				"touchMove",
				"waiting",
				"wheel"
			];
			function registerSimpleEvent(domEventName, reactName) {
				topLevelEventsToReactNames.set(domEventName, reactName);
				registerTwoPhaseEvent(reactName, [domEventName]);
			}
			function registerSimpleEvents() {
				for (var i = 0; i < simpleEventPluginEvents.length; i++) {
					var eventName = simpleEventPluginEvents[i];
					registerSimpleEvent(eventName.toLowerCase(), "on" + (eventName[0].toUpperCase() + eventName.slice(1)));
				}
				registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
				registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
				registerSimpleEvent(ANIMATION_START, "onAnimationStart");
				registerSimpleEvent("dblclick", "onDoubleClick");
				registerSimpleEvent("focusin", "onFocus");
				registerSimpleEvent("focusout", "onBlur");
				registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
			}
			function extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				var reactName = topLevelEventsToReactNames.get(domEventName);
				if (reactName === void 0) return;
				var SyntheticEventCtor = SyntheticEvent;
				var reactEventType = domEventName;
				switch (domEventName) {
					case "keypress": if (getEventCharCode(nativeEvent) === 0) return;
					case "keydown":
					case "keyup":
						SyntheticEventCtor = SyntheticKeyboardEvent;
						break;
					case "focusin":
						reactEventType = "focus";
						SyntheticEventCtor = SyntheticFocusEvent;
						break;
					case "focusout":
						reactEventType = "blur";
						SyntheticEventCtor = SyntheticFocusEvent;
						break;
					case "beforeblur":
					case "afterblur":
						SyntheticEventCtor = SyntheticFocusEvent;
						break;
					case "click": if (nativeEvent.button === 2) return;
					case "auxclick":
					case "dblclick":
					case "mousedown":
					case "mousemove":
					case "mouseup":
					case "mouseout":
					case "mouseover":
					case "contextmenu":
						SyntheticEventCtor = SyntheticMouseEvent;
						break;
					case "drag":
					case "dragend":
					case "dragenter":
					case "dragexit":
					case "dragleave":
					case "dragover":
					case "dragstart":
					case "drop":
						SyntheticEventCtor = SyntheticDragEvent;
						break;
					case "touchcancel":
					case "touchend":
					case "touchmove":
					case "touchstart":
						SyntheticEventCtor = SyntheticTouchEvent;
						break;
					case ANIMATION_END:
					case ANIMATION_ITERATION:
					case ANIMATION_START:
						SyntheticEventCtor = SyntheticAnimationEvent;
						break;
					case TRANSITION_END:
						SyntheticEventCtor = SyntheticTransitionEvent;
						break;
					case "scroll":
						SyntheticEventCtor = SyntheticUIEvent;
						break;
					case "wheel":
						SyntheticEventCtor = SyntheticWheelEvent;
						break;
					case "copy":
					case "cut":
					case "paste":
						SyntheticEventCtor = SyntheticClipboardEvent;
						break;
					case "gotpointercapture":
					case "lostpointercapture":
					case "pointercancel":
					case "pointerdown":
					case "pointermove":
					case "pointerout":
					case "pointerover":
					case "pointerup": SyntheticEventCtor = SyntheticPointerEvent;
				}
				var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
				var accumulateTargetOnly = !inCapturePhase && domEventName === "scroll";
				var _listeners = accumulateSinglePhaseListeners(targetInst, reactName, nativeEvent.type, inCapturePhase, accumulateTargetOnly);
				if (_listeners.length > 0) {
					var _event = new SyntheticEventCtor(reactName, reactEventType, null, nativeEvent, nativeEventTarget);
					dispatchQueue.push({
						event: _event,
						listeners: _listeners
					});
				}
			}
			registerSimpleEvents();
			registerEvents$2();
			registerEvents$1();
			registerEvents$3();
			registerEvents();
			function extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, targetContainer) {
				extractEvents$4(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
				if ((eventSystemFlags & SHOULD_NOT_PROCESS_POLYFILL_EVENT_PLUGINS) === 0) {
					extractEvents$2(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
					extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
					extractEvents$3(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
					extractEvents(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
				}
			}
			var mediaEventTypes = [
				"abort",
				"canplay",
				"canplaythrough",
				"durationchange",
				"emptied",
				"encrypted",
				"ended",
				"error",
				"loadeddata",
				"loadedmetadata",
				"loadstart",
				"pause",
				"play",
				"playing",
				"progress",
				"ratechange",
				"resize",
				"seeked",
				"seeking",
				"stalled",
				"suspend",
				"timeupdate",
				"volumechange",
				"waiting"
			];
			var nonDelegatedEvents = new Set([
				"cancel",
				"close",
				"invalid",
				"load",
				"scroll",
				"toggle"
			].concat(mediaEventTypes));
			function executeDispatch(event, listener, currentTarget) {
				var type = event.type || "unknown-event";
				event.currentTarget = currentTarget;
				invokeGuardedCallbackAndCatchFirstError(type, listener, void 0, event);
				event.currentTarget = null;
			}
			function processDispatchQueueItemsInOrder(event, dispatchListeners, inCapturePhase) {
				var previousInstance;
				if (inCapturePhase) for (var i = dispatchListeners.length - 1; i >= 0; i--) {
					var _dispatchListeners$i = dispatchListeners[i], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget, listener = _dispatchListeners$i.listener;
					if (instance !== previousInstance && event.isPropagationStopped()) return;
					executeDispatch(event, listener, currentTarget);
					previousInstance = instance;
				}
				else for (var _i = 0; _i < dispatchListeners.length; _i++) {
					var _dispatchListeners$_i = dispatchListeners[_i], _instance = _dispatchListeners$_i.instance, _currentTarget = _dispatchListeners$_i.currentTarget, _listener = _dispatchListeners$_i.listener;
					if (_instance !== previousInstance && event.isPropagationStopped()) return;
					executeDispatch(event, _listener, _currentTarget);
					previousInstance = _instance;
				}
			}
			function processDispatchQueue(dispatchQueue, eventSystemFlags) {
				var inCapturePhase = (eventSystemFlags & IS_CAPTURE_PHASE) !== 0;
				for (var i = 0; i < dispatchQueue.length; i++) {
					var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event, listeners = _dispatchQueue$i.listeners;
					processDispatchQueueItemsInOrder(event, listeners, inCapturePhase);
				}
				rethrowCaughtError();
			}
			function dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
				var nativeEventTarget = getEventTarget(nativeEvent);
				var dispatchQueue = [];
				extractEvents$5(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
				processDispatchQueue(dispatchQueue, eventSystemFlags);
			}
			function listenToNonDelegatedEvent(domEventName, targetElement) {
				if (!nonDelegatedEvents.has(domEventName)) error("Did not expect a listenToNonDelegatedEvent() call for \"%s\". This is a bug in React. Please file an issue.", domEventName);
				var isCapturePhaseListener = false;
				var listenerSet = getEventListenerSet(targetElement);
				var listenerSetKey = getListenerSetKey(domEventName, isCapturePhaseListener);
				if (!listenerSet.has(listenerSetKey)) {
					addTrappedEventListener(targetElement, domEventName, IS_NON_DELEGATED, isCapturePhaseListener);
					listenerSet.add(listenerSetKey);
				}
			}
			function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
				if (nonDelegatedEvents.has(domEventName) && !isCapturePhaseListener) error("Did not expect a listenToNativeEvent() call for \"%s\" in the bubble phase. This is a bug in React. Please file an issue.", domEventName);
				var eventSystemFlags = 0;
				if (isCapturePhaseListener) eventSystemFlags |= IS_CAPTURE_PHASE;
				addTrappedEventListener(target, domEventName, eventSystemFlags, isCapturePhaseListener);
			}
			var listeningMarker = "_reactListening" + Math.random().toString(36).slice(2);
			function listenToAllSupportedEvents(rootContainerElement) {
				if (!rootContainerElement[listeningMarker]) {
					rootContainerElement[listeningMarker] = true;
					allNativeEvents.forEach(function(domEventName) {
						if (domEventName !== "selectionchange") {
							if (!nonDelegatedEvents.has(domEventName)) listenToNativeEvent(domEventName, false, rootContainerElement);
							listenToNativeEvent(domEventName, true, rootContainerElement);
						}
					});
					var ownerDocument = rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
					if (ownerDocument !== null) {
						if (!ownerDocument[listeningMarker]) {
							ownerDocument[listeningMarker] = true;
							listenToNativeEvent("selectionchange", false, ownerDocument);
						}
					}
				}
			}
			function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener, isDeferredListenerForLegacyFBSupport) {
				var listener = createEventListenerWrapperWithPriority(targetContainer, domEventName, eventSystemFlags);
				var isPassiveListener = void 0;
				if (passiveBrowserEventsSupported) {
					if (domEventName === "touchstart" || domEventName === "touchmove" || domEventName === "wheel") isPassiveListener = true;
				}
				targetContainer = targetContainer;
				if (isCapturePhaseListener) {
					if (isPassiveListener !== void 0) addEventCaptureListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
					else addEventCaptureListener(targetContainer, domEventName, listener);
				} else if (isPassiveListener !== void 0) addEventBubbleListenerWithPassiveFlag(targetContainer, domEventName, listener, isPassiveListener);
				else addEventBubbleListener(targetContainer, domEventName, listener);
			}
			function isMatchingRootContainer(grandContainer, targetContainer) {
				return grandContainer === targetContainer || grandContainer.nodeType === COMMENT_NODE && grandContainer.parentNode === targetContainer;
			}
			function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst, targetContainer) {
				var ancestorInst = targetInst;
				if ((eventSystemFlags & IS_EVENT_HANDLE_NON_MANAGED_NODE) === 0 && (eventSystemFlags & IS_NON_DELEGATED) === 0) {
					var targetContainerNode = targetContainer;
					if (targetInst !== null) {
						var node = targetInst;
						mainLoop: while (true) {
							if (node === null) return;
							var nodeTag = node.tag;
							if (nodeTag === HostRoot || nodeTag === HostPortal) {
								var container = node.stateNode.containerInfo;
								if (isMatchingRootContainer(container, targetContainerNode)) break;
								if (nodeTag === HostPortal) {
									var grandNode = node.return;
									while (grandNode !== null) {
										var grandTag = grandNode.tag;
										if (grandTag === HostRoot || grandTag === HostPortal) {
											var grandContainer = grandNode.stateNode.containerInfo;
											if (isMatchingRootContainer(grandContainer, targetContainerNode)) return;
										}
										grandNode = grandNode.return;
									}
								}
								while (container !== null) {
									var parentNode = getClosestInstanceFromNode(container);
									if (parentNode === null) return;
									var parentTag = parentNode.tag;
									if (parentTag === HostComponent || parentTag === HostText) {
										node = ancestorInst = parentNode;
										continue mainLoop;
									}
									container = container.parentNode;
								}
							}
							node = node.return;
						}
					}
				}
				batchedUpdates(function() {
					return dispatchEventsForPlugins(domEventName, eventSystemFlags, nativeEvent, ancestorInst);
				});
			}
			function createDispatchListener(instance, listener, currentTarget) {
				return {
					instance,
					listener,
					currentTarget
				};
			}
			function accumulateSinglePhaseListeners(targetFiber, reactName, nativeEventType, inCapturePhase, accumulateTargetOnly, nativeEvent) {
				var captureName = reactName !== null ? reactName + "Capture" : null;
				var reactEventName = inCapturePhase ? captureName : reactName;
				var listeners = [];
				var instance = targetFiber;
				var lastHostComponent = null;
				while (instance !== null) {
					var _instance2 = instance, stateNode = _instance2.stateNode;
					if (_instance2.tag === HostComponent && stateNode !== null) {
						lastHostComponent = stateNode;
						if (reactEventName !== null) {
							var listener = getListener(instance, reactEventName);
							if (listener != null) listeners.push(createDispatchListener(instance, listener, lastHostComponent));
						}
					}
					if (accumulateTargetOnly) break;
					instance = instance.return;
				}
				return listeners;
			}
			function accumulateTwoPhaseListeners(targetFiber, reactName) {
				var captureName = reactName + "Capture";
				var listeners = [];
				var instance = targetFiber;
				while (instance !== null) {
					var _instance3 = instance, stateNode = _instance3.stateNode;
					if (_instance3.tag === HostComponent && stateNode !== null) {
						var currentTarget = stateNode;
						var captureListener = getListener(instance, captureName);
						if (captureListener != null) listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
						var bubbleListener = getListener(instance, reactName);
						if (bubbleListener != null) listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
					}
					instance = instance.return;
				}
				return listeners;
			}
			function getParent(inst) {
				if (inst === null) return null;
				do
					inst = inst.return;
				while (inst && inst.tag !== HostComponent);
				if (inst) return inst;
				return null;
			}
			/**
			* Return the lowest common ancestor of A and B, or null if they are in
			* different trees.
			*/
			function getLowestCommonAncestor(instA, instB) {
				var nodeA = instA;
				var nodeB = instB;
				var depthA = 0;
				for (var tempA = nodeA; tempA; tempA = getParent(tempA)) depthA++;
				var depthB = 0;
				for (var tempB = nodeB; tempB; tempB = getParent(tempB)) depthB++;
				while (depthA - depthB > 0) {
					nodeA = getParent(nodeA);
					depthA--;
				}
				while (depthB - depthA > 0) {
					nodeB = getParent(nodeB);
					depthB--;
				}
				var depth = depthA;
				while (depth--) {
					if (nodeA === nodeB || nodeB !== null && nodeA === nodeB.alternate) return nodeA;
					nodeA = getParent(nodeA);
					nodeB = getParent(nodeB);
				}
				return null;
			}
			function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
				var registrationName = event._reactName;
				var listeners = [];
				var instance = target;
				while (instance !== null) {
					if (instance === common) break;
					var _instance4 = instance, alternate = _instance4.alternate, stateNode = _instance4.stateNode, tag = _instance4.tag;
					if (alternate !== null && alternate === common) break;
					if (tag === HostComponent && stateNode !== null) {
						var currentTarget = stateNode;
						if (inCapturePhase) {
							var captureListener = getListener(instance, registrationName);
							if (captureListener != null) listeners.unshift(createDispatchListener(instance, captureListener, currentTarget));
						} else if (!inCapturePhase) {
							var bubbleListener = getListener(instance, registrationName);
							if (bubbleListener != null) listeners.push(createDispatchListener(instance, bubbleListener, currentTarget));
						}
					}
					instance = instance.return;
				}
				if (listeners.length !== 0) dispatchQueue.push({
					event,
					listeners
				});
			}
			function accumulateEnterLeaveTwoPhaseListeners(dispatchQueue, leaveEvent, enterEvent, from, to) {
				var common = from && to ? getLowestCommonAncestor(from, to) : null;
				if (from !== null) accumulateEnterLeaveListenersForEvent(dispatchQueue, leaveEvent, from, common, false);
				if (to !== null && enterEvent !== null) accumulateEnterLeaveListenersForEvent(dispatchQueue, enterEvent, to, common, true);
			}
			function getListenerSetKey(domEventName, capture) {
				return domEventName + "__" + (capture ? "capture" : "bubble");
			}
			var didWarnInvalidHydration = false;
			var DANGEROUSLY_SET_INNER_HTML = "dangerouslySetInnerHTML";
			var SUPPRESS_CONTENT_EDITABLE_WARNING = "suppressContentEditableWarning";
			var SUPPRESS_HYDRATION_WARNING = "suppressHydrationWarning";
			var AUTOFOCUS = "autoFocus";
			var CHILDREN = "children";
			var STYLE = "style";
			var HTML$1 = "__html";
			var warnedUnknownTags;
			var validatePropertiesInDevelopment;
			var warnForPropDifference;
			var warnForExtraAttributes;
			var warnForInvalidEventListener;
			var canDiffStyleForHydrationWarning;
			var normalizeHTML;
			warnedUnknownTags = {
				dialog: true,
				webview: true
			};
			validatePropertiesInDevelopment = function(type, props) {
				validateProperties(type, props);
				validateProperties$1(type, props);
				validateProperties$2(type, props, {
					registrationNameDependencies,
					possibleRegistrationNames
				});
			};
			canDiffStyleForHydrationWarning = canUseDOM && !document.documentMode;
			warnForPropDifference = function(propName, serverValue, clientValue) {
				if (didWarnInvalidHydration) return;
				var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
				var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
				if (normalizedServerValue === normalizedClientValue) return;
				didWarnInvalidHydration = true;
				error("Prop `%s` did not match. Server: %s Client: %s", propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
			};
			warnForExtraAttributes = function(attributeNames) {
				if (didWarnInvalidHydration) return;
				didWarnInvalidHydration = true;
				var names = [];
				attributeNames.forEach(function(name) {
					names.push(name);
				});
				error("Extra attributes from the server: %s", names);
			};
			warnForInvalidEventListener = function(registrationName, listener) {
				if (listener === false) error("Expected `%s` listener to be a function, instead got `false`.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", registrationName, registrationName, registrationName);
				else error("Expected `%s` listener to be a function, instead got a value of `%s` type.", registrationName, typeof listener);
			};
			normalizeHTML = function(parent, html) {
				var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
				testElement.innerHTML = html;
				return testElement.innerHTML;
			};
			var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
			var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
			function normalizeMarkupForTextOrAttribute(markup) {
				checkHtmlStringCoercion(markup);
				return (typeof markup === "string" ? markup : "" + markup).replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
			}
			function checkForUnmatchedText(serverText, clientText, isConcurrentMode, shouldWarnDev) {
				var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
				var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
				if (normalizedServerText === normalizedClientText) return;
				if (shouldWarnDev) {
					if (!didWarnInvalidHydration) {
						didWarnInvalidHydration = true;
						error("Text content did not match. Server: \"%s\" Client: \"%s\"", normalizedServerText, normalizedClientText);
					}
				}
				if (isConcurrentMode && enableClientRenderFallbackOnTextMismatch) throw new Error("Text content does not match server-rendered HTML.");
			}
			function getOwnerDocumentFromRootContainer(rootContainerElement) {
				return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
			}
			function noop() {}
			function trapClickOnNonInteractiveElement(node) {
				node.onclick = noop;
			}
			function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
				for (var propKey in nextProps) {
					if (!nextProps.hasOwnProperty(propKey)) continue;
					var nextProp = nextProps[propKey];
					if (propKey === STYLE) {
						if (nextProp) Object.freeze(nextProp);
						setValueForStyles(domElement, nextProp);
					} else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
						var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
						if (nextHtml != null) setInnerHTML(domElement, nextHtml);
					} else if (propKey === CHILDREN) {
						if (typeof nextProp === "string") {
							if (tag !== "textarea" || nextProp !== "") setTextContent(domElement, nextProp);
						} else if (typeof nextProp === "number") setTextContent(domElement, "" + nextProp);
					} else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING);
					else if (propKey === AUTOFOCUS);
					else if (registrationNameDependencies.hasOwnProperty(propKey)) {
						if (nextProp != null) {
							if (typeof nextProp !== "function") warnForInvalidEventListener(propKey, nextProp);
							if (propKey === "onScroll") listenToNonDelegatedEvent("scroll", domElement);
						}
					} else if (nextProp != null) setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
				}
			}
			function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
				for (var i = 0; i < updatePayload.length; i += 2) {
					var propKey = updatePayload[i];
					var propValue = updatePayload[i + 1];
					if (propKey === STYLE) setValueForStyles(domElement, propValue);
					else if (propKey === DANGEROUSLY_SET_INNER_HTML) setInnerHTML(domElement, propValue);
					else if (propKey === CHILDREN) setTextContent(domElement, propValue);
					else setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
				}
			}
			function createElement(type, props, rootContainerElement, parentNamespace) {
				var isCustomComponentTag;
				var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
				var domElement;
				var namespaceURI = parentNamespace;
				if (namespaceURI === HTML_NAMESPACE) namespaceURI = getIntrinsicNamespace(type);
				if (namespaceURI === HTML_NAMESPACE) {
					isCustomComponentTag = isCustomComponent(type, props);
					if (!isCustomComponentTag && type !== type.toLowerCase()) error("<%s /> is using incorrect casing. Use PascalCase for React components, or lowercase for HTML elements.", type);
					if (type === "script") {
						var div = ownerDocument.createElement("div");
						div.innerHTML = "<script><\/script>";
						var firstChild = div.firstChild;
						domElement = div.removeChild(firstChild);
					} else if (typeof props.is === "string") domElement = ownerDocument.createElement(type, { is: props.is });
					else {
						domElement = ownerDocument.createElement(type);
						if (type === "select") {
							var node = domElement;
							if (props.multiple) node.multiple = true;
							else if (props.size) node.size = props.size;
						}
					}
				} else domElement = ownerDocument.createElementNS(namespaceURI, type);
				if (namespaceURI === HTML_NAMESPACE) {
					if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === "[object HTMLUnknownElement]" && !hasOwnProperty.call(warnedUnknownTags, type)) {
						warnedUnknownTags[type] = true;
						error("The tag <%s> is unrecognized in this browser. If you meant to render a React component, start its name with an uppercase letter.", type);
					}
				}
				return domElement;
			}
			function createTextNode(text, rootContainerElement) {
				return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
			}
			function setInitialProperties(domElement, tag, rawProps, rootContainerElement) {
				var isCustomComponentTag = isCustomComponent(tag, rawProps);
				validatePropertiesInDevelopment(tag, rawProps);
				var props;
				switch (tag) {
					case "dialog":
						listenToNonDelegatedEvent("cancel", domElement);
						listenToNonDelegatedEvent("close", domElement);
						props = rawProps;
						break;
					case "iframe":
					case "object":
					case "embed":
						listenToNonDelegatedEvent("load", domElement);
						props = rawProps;
						break;
					case "video":
					case "audio":
						for (var i = 0; i < mediaEventTypes.length; i++) listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
						props = rawProps;
						break;
					case "source":
						listenToNonDelegatedEvent("error", domElement);
						props = rawProps;
						break;
					case "img":
					case "image":
					case "link":
						listenToNonDelegatedEvent("error", domElement);
						listenToNonDelegatedEvent("load", domElement);
						props = rawProps;
						break;
					case "details":
						listenToNonDelegatedEvent("toggle", domElement);
						props = rawProps;
						break;
					case "input":
						initWrapperState(domElement, rawProps);
						props = getHostProps(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
						break;
					case "option":
						validateProps(domElement, rawProps);
						props = rawProps;
						break;
					case "select":
						initWrapperState$1(domElement, rawProps);
						props = getHostProps$1(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
						break;
					case "textarea":
						initWrapperState$2(domElement, rawProps);
						props = getHostProps$2(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
						break;
					default: props = rawProps;
				}
				assertValidProps(tag, props);
				setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);
				switch (tag) {
					case "input":
						track(domElement);
						postMountWrapper(domElement, rawProps, false);
						break;
					case "textarea":
						track(domElement);
						postMountWrapper$3(domElement);
						break;
					case "option":
						postMountWrapper$1(domElement, rawProps);
						break;
					case "select":
						postMountWrapper$2(domElement, rawProps);
						break;
					default: if (typeof props.onClick === "function") trapClickOnNonInteractiveElement(domElement);
				}
			}
			function diffProperties(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
				validatePropertiesInDevelopment(tag, nextRawProps);
				var updatePayload = null;
				var lastProps;
				var nextProps;
				switch (tag) {
					case "input":
						lastProps = getHostProps(domElement, lastRawProps);
						nextProps = getHostProps(domElement, nextRawProps);
						updatePayload = [];
						break;
					case "select":
						lastProps = getHostProps$1(domElement, lastRawProps);
						nextProps = getHostProps$1(domElement, nextRawProps);
						updatePayload = [];
						break;
					case "textarea":
						lastProps = getHostProps$2(domElement, lastRawProps);
						nextProps = getHostProps$2(domElement, nextRawProps);
						updatePayload = [];
						break;
					default:
						lastProps = lastRawProps;
						nextProps = nextRawProps;
						if (typeof lastProps.onClick !== "function" && typeof nextProps.onClick === "function") trapClickOnNonInteractiveElement(domElement);
				}
				assertValidProps(tag, nextProps);
				var propKey;
				var styleName;
				var styleUpdates = null;
				for (propKey in lastProps) {
					if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) continue;
					if (propKey === STYLE) {
						var lastStyle = lastProps[propKey];
						for (styleName in lastStyle) if (lastStyle.hasOwnProperty(styleName)) {
							if (!styleUpdates) styleUpdates = {};
							styleUpdates[styleName] = "";
						}
					} else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN);
					else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING);
					else if (propKey === AUTOFOCUS);
					else if (registrationNameDependencies.hasOwnProperty(propKey)) {
						if (!updatePayload) updatePayload = [];
					} else (updatePayload = updatePayload || []).push(propKey, null);
				}
				for (propKey in nextProps) {
					var nextProp = nextProps[propKey];
					var lastProp = lastProps != null ? lastProps[propKey] : void 0;
					if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) continue;
					if (propKey === STYLE) {
						if (nextProp) Object.freeze(nextProp);
						if (lastProp) {
							for (styleName in lastProp) if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
								if (!styleUpdates) styleUpdates = {};
								styleUpdates[styleName] = "";
							}
							for (styleName in nextProp) if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
								if (!styleUpdates) styleUpdates = {};
								styleUpdates[styleName] = nextProp[styleName];
							}
						} else {
							if (!styleUpdates) {
								if (!updatePayload) updatePayload = [];
								updatePayload.push(propKey, styleUpdates);
							}
							styleUpdates = nextProp;
						}
					} else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
						var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
						var lastHtml = lastProp ? lastProp[HTML$1] : void 0;
						if (nextHtml != null) {
							if (lastHtml !== nextHtml) (updatePayload = updatePayload || []).push(propKey, nextHtml);
						}
					} else if (propKey === CHILDREN) {
						if (typeof nextProp === "string" || typeof nextProp === "number") (updatePayload = updatePayload || []).push(propKey, "" + nextProp);
					} else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING);
					else if (registrationNameDependencies.hasOwnProperty(propKey)) {
						if (nextProp != null) {
							if (typeof nextProp !== "function") warnForInvalidEventListener(propKey, nextProp);
							if (propKey === "onScroll") listenToNonDelegatedEvent("scroll", domElement);
						}
						if (!updatePayload && lastProp !== nextProp) updatePayload = [];
					} else (updatePayload = updatePayload || []).push(propKey, nextProp);
				}
				if (styleUpdates) {
					validateShorthandPropertyCollisionInDev(styleUpdates, nextProps[STYLE]);
					(updatePayload = updatePayload || []).push(STYLE, styleUpdates);
				}
				return updatePayload;
			}
			function updateProperties(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
				if (tag === "input" && nextRawProps.type === "radio" && nextRawProps.name != null) updateChecked(domElement, nextRawProps);
				updateDOMProperties(domElement, updatePayload, isCustomComponent(tag, lastRawProps), isCustomComponent(tag, nextRawProps));
				switch (tag) {
					case "input":
						updateWrapper(domElement, nextRawProps);
						break;
					case "textarea":
						updateWrapper$1(domElement, nextRawProps);
						break;
					case "select": postUpdateWrapper(domElement, nextRawProps);
				}
			}
			function getPossibleStandardName(propName) {
				var lowerCasedName = propName.toLowerCase();
				if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) return null;
				return possibleStandardNames[lowerCasedName] || null;
			}
			function diffHydratedProperties(domElement, tag, rawProps, parentNamespace, rootContainerElement, isConcurrentMode, shouldWarnDev) {
				var isCustomComponentTag;
				var extraAttributeNames;
				isCustomComponentTag = isCustomComponent(tag, rawProps);
				validatePropertiesInDevelopment(tag, rawProps);
				switch (tag) {
					case "dialog":
						listenToNonDelegatedEvent("cancel", domElement);
						listenToNonDelegatedEvent("close", domElement);
						break;
					case "iframe":
					case "object":
					case "embed":
						listenToNonDelegatedEvent("load", domElement);
						break;
					case "video":
					case "audio":
						for (var i = 0; i < mediaEventTypes.length; i++) listenToNonDelegatedEvent(mediaEventTypes[i], domElement);
						break;
					case "source":
						listenToNonDelegatedEvent("error", domElement);
						break;
					case "img":
					case "image":
					case "link":
						listenToNonDelegatedEvent("error", domElement);
						listenToNonDelegatedEvent("load", domElement);
						break;
					case "details":
						listenToNonDelegatedEvent("toggle", domElement);
						break;
					case "input":
						initWrapperState(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
						break;
					case "option":
						validateProps(domElement, rawProps);
						break;
					case "select":
						initWrapperState$1(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
						break;
					case "textarea":
						initWrapperState$2(domElement, rawProps);
						listenToNonDelegatedEvent("invalid", domElement);
				}
				assertValidProps(tag, rawProps);
				extraAttributeNames = /* @__PURE__ */ new Set();
				var attributes = domElement.attributes;
				for (var _i = 0; _i < attributes.length; _i++) switch (attributes[_i].name.toLowerCase()) {
					case "value": break;
					case "checked": break;
					case "selected": break;
					default: extraAttributeNames.add(attributes[_i].name);
				}
				var updatePayload = null;
				for (var propKey in rawProps) {
					if (!rawProps.hasOwnProperty(propKey)) continue;
					var nextProp = rawProps[propKey];
					if (propKey === CHILDREN) {
						if (typeof nextProp === "string") {
							if (domElement.textContent !== nextProp) {
								if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
								updatePayload = [CHILDREN, nextProp];
							}
						} else if (typeof nextProp === "number") {
							if (domElement.textContent !== "" + nextProp) {
								if (rawProps[SUPPRESS_HYDRATION_WARNING] !== true) checkForUnmatchedText(domElement.textContent, nextProp, isConcurrentMode, shouldWarnDev);
								updatePayload = [CHILDREN, "" + nextProp];
							}
						}
					} else if (registrationNameDependencies.hasOwnProperty(propKey)) {
						if (nextProp != null) {
							if (typeof nextProp !== "function") warnForInvalidEventListener(propKey, nextProp);
							if (propKey === "onScroll") listenToNonDelegatedEvent("scroll", domElement);
						}
					} else if (shouldWarnDev && typeof isCustomComponentTag === "boolean") {
						var serverValue = void 0;
						var propertyInfo = isCustomComponentTag && enableCustomElementPropertySupport ? null : getPropertyInfo(propKey);
						if (rawProps[SUPPRESS_HYDRATION_WARNING] === true);
						else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING || propKey === "value" || propKey === "checked" || propKey === "selected");
						else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
							var serverHTML = domElement.innerHTML;
							var nextHtml = nextProp ? nextProp[HTML$1] : void 0;
							if (nextHtml != null) {
								var expectedHTML = normalizeHTML(domElement, nextHtml);
								if (expectedHTML !== serverHTML) warnForPropDifference(propKey, serverHTML, expectedHTML);
							}
						} else if (propKey === STYLE) {
							extraAttributeNames.delete(propKey);
							if (canDiffStyleForHydrationWarning) {
								var expectedStyle = createDangerousStringForStyles(nextProp);
								serverValue = domElement.getAttribute("style");
								if (expectedStyle !== serverValue) warnForPropDifference(propKey, serverValue, expectedStyle);
							}
						} else if (isCustomComponentTag && !enableCustomElementPropertySupport) {
							extraAttributeNames.delete(propKey.toLowerCase());
							serverValue = getValueForAttribute(domElement, propKey, nextProp);
							if (nextProp !== serverValue) warnForPropDifference(propKey, serverValue, nextProp);
						} else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
							var isMismatchDueToBadCasing = false;
							if (propertyInfo !== null) {
								extraAttributeNames.delete(propertyInfo.attributeName);
								serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
							} else {
								var ownNamespace = parentNamespace;
								if (ownNamespace === HTML_NAMESPACE) ownNamespace = getIntrinsicNamespace(tag);
								if (ownNamespace === HTML_NAMESPACE) extraAttributeNames.delete(propKey.toLowerCase());
								else {
									var standardName = getPossibleStandardName(propKey);
									if (standardName !== null && standardName !== propKey) {
										isMismatchDueToBadCasing = true;
										extraAttributeNames.delete(standardName);
									}
									extraAttributeNames.delete(propKey);
								}
								serverValue = getValueForAttribute(domElement, propKey, nextProp);
							}
							if (!enableCustomElementPropertySupport && nextProp !== serverValue && !isMismatchDueToBadCasing) warnForPropDifference(propKey, serverValue, nextProp);
						}
					}
				}
				if (shouldWarnDev) {
					if (extraAttributeNames.size > 0 && rawProps[SUPPRESS_HYDRATION_WARNING] !== true) warnForExtraAttributes(extraAttributeNames);
				}
				switch (tag) {
					case "input":
						track(domElement);
						postMountWrapper(domElement, rawProps, true);
						break;
					case "textarea":
						track(domElement);
						postMountWrapper$3(domElement);
						break;
					case "select":
					case "option": break;
					default: if (typeof rawProps.onClick === "function") trapClickOnNonInteractiveElement(domElement);
				}
				return updatePayload;
			}
			function diffHydratedText(textNode, text, isConcurrentMode) {
				return textNode.nodeValue !== text;
			}
			function warnForDeletedHydratableElement(parentNode, child) {
				if (didWarnInvalidHydration) return;
				didWarnInvalidHydration = true;
				error("Did not expect server HTML to contain a <%s> in <%s>.", child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
			}
			function warnForDeletedHydratableText(parentNode, child) {
				if (didWarnInvalidHydration) return;
				didWarnInvalidHydration = true;
				error("Did not expect server HTML to contain the text node \"%s\" in <%s>.", child.nodeValue, parentNode.nodeName.toLowerCase());
			}
			function warnForInsertedHydratedElement(parentNode, tag, props) {
				if (didWarnInvalidHydration) return;
				didWarnInvalidHydration = true;
				error("Expected server HTML to contain a matching <%s> in <%s>.", tag, parentNode.nodeName.toLowerCase());
			}
			function warnForInsertedHydratedText(parentNode, text) {
				if (text === "") return;
				if (didWarnInvalidHydration) return;
				didWarnInvalidHydration = true;
				error("Expected server HTML to contain a matching text node for \"%s\" in <%s>.", text, parentNode.nodeName.toLowerCase());
			}
			function restoreControlledState$3(domElement, tag, props) {
				switch (tag) {
					case "input":
						restoreControlledState(domElement, props);
						return;
					case "textarea":
						restoreControlledState$2(domElement, props);
						return;
					case "select":
						restoreControlledState$1(domElement, props);
						return;
				}
			}
			var validateDOMNesting = function() {};
			var updatedAncestorInfo = function() {};
			var specialTags = [
				"address",
				"applet",
				"area",
				"article",
				"aside",
				"base",
				"basefont",
				"bgsound",
				"blockquote",
				"body",
				"br",
				"button",
				"caption",
				"center",
				"col",
				"colgroup",
				"dd",
				"details",
				"dir",
				"div",
				"dl",
				"dt",
				"embed",
				"fieldset",
				"figcaption",
				"figure",
				"footer",
				"form",
				"frame",
				"frameset",
				"h1",
				"h2",
				"h3",
				"h4",
				"h5",
				"h6",
				"head",
				"header",
				"hgroup",
				"hr",
				"html",
				"iframe",
				"img",
				"input",
				"isindex",
				"li",
				"link",
				"listing",
				"main",
				"marquee",
				"menu",
				"menuitem",
				"meta",
				"nav",
				"noembed",
				"noframes",
				"noscript",
				"object",
				"ol",
				"p",
				"param",
				"plaintext",
				"pre",
				"script",
				"section",
				"select",
				"source",
				"style",
				"summary",
				"table",
				"tbody",
				"td",
				"template",
				"textarea",
				"tfoot",
				"th",
				"thead",
				"title",
				"tr",
				"track",
				"ul",
				"wbr",
				"xmp"
			];
			var inScopeTags = [
				"applet",
				"caption",
				"html",
				"table",
				"td",
				"th",
				"marquee",
				"object",
				"template",
				"foreignObject",
				"desc",
				"title"
			];
			var buttonScopeTags = inScopeTags.concat(["button"]);
			var impliedEndTags = [
				"dd",
				"dt",
				"li",
				"option",
				"optgroup",
				"p",
				"rp",
				"rt"
			];
			var emptyAncestorInfo = {
				current: null,
				formTag: null,
				aTagInScope: null,
				buttonTagInScope: null,
				nobrTagInScope: null,
				pTagInButtonScope: null,
				listItemTagAutoclosing: null,
				dlItemTagAutoclosing: null
			};
			updatedAncestorInfo = function(oldInfo, tag) {
				var ancestorInfo = assign({}, oldInfo || emptyAncestorInfo);
				var info = { tag };
				if (inScopeTags.indexOf(tag) !== -1) {
					ancestorInfo.aTagInScope = null;
					ancestorInfo.buttonTagInScope = null;
					ancestorInfo.nobrTagInScope = null;
				}
				if (buttonScopeTags.indexOf(tag) !== -1) ancestorInfo.pTagInButtonScope = null;
				if (specialTags.indexOf(tag) !== -1 && tag !== "address" && tag !== "div" && tag !== "p") {
					ancestorInfo.listItemTagAutoclosing = null;
					ancestorInfo.dlItemTagAutoclosing = null;
				}
				ancestorInfo.current = info;
				if (tag === "form") ancestorInfo.formTag = info;
				if (tag === "a") ancestorInfo.aTagInScope = info;
				if (tag === "button") ancestorInfo.buttonTagInScope = info;
				if (tag === "nobr") ancestorInfo.nobrTagInScope = info;
				if (tag === "p") ancestorInfo.pTagInButtonScope = info;
				if (tag === "li") ancestorInfo.listItemTagAutoclosing = info;
				if (tag === "dd" || tag === "dt") ancestorInfo.dlItemTagAutoclosing = info;
				return ancestorInfo;
			};
			/**
			* Returns whether
			*/
			var isTagValidWithParent = function(tag, parentTag) {
				switch (parentTag) {
					case "select": return tag === "option" || tag === "optgroup" || tag === "#text";
					case "optgroup": return tag === "option" || tag === "#text";
					case "option": return tag === "#text";
					case "tr": return tag === "th" || tag === "td" || tag === "style" || tag === "script" || tag === "template";
					case "tbody":
					case "thead":
					case "tfoot": return tag === "tr" || tag === "style" || tag === "script" || tag === "template";
					case "colgroup": return tag === "col" || tag === "template";
					case "table": return tag === "caption" || tag === "colgroup" || tag === "tbody" || tag === "tfoot" || tag === "thead" || tag === "style" || tag === "script" || tag === "template";
					case "head": return tag === "base" || tag === "basefont" || tag === "bgsound" || tag === "link" || tag === "meta" || tag === "title" || tag === "noscript" || tag === "noframes" || tag === "style" || tag === "script" || tag === "template";
					case "html": return tag === "head" || tag === "body" || tag === "frameset";
					case "frameset": return tag === "frame";
					case "#document": return tag === "html";
				}
				switch (tag) {
					case "h1":
					case "h2":
					case "h3":
					case "h4":
					case "h5":
					case "h6": return parentTag !== "h1" && parentTag !== "h2" && parentTag !== "h3" && parentTag !== "h4" && parentTag !== "h5" && parentTag !== "h6";
					case "rp":
					case "rt": return impliedEndTags.indexOf(parentTag) === -1;
					case "body":
					case "caption":
					case "col":
					case "colgroup":
					case "frameset":
					case "frame":
					case "head":
					case "html":
					case "tbody":
					case "td":
					case "tfoot":
					case "th":
					case "thead":
					case "tr": return parentTag == null;
				}
				return true;
			};
			/**
			* Returns whether
			*/
			var findInvalidAncestorForTag = function(tag, ancestorInfo) {
				switch (tag) {
					case "address":
					case "article":
					case "aside":
					case "blockquote":
					case "center":
					case "details":
					case "dialog":
					case "dir":
					case "div":
					case "dl":
					case "fieldset":
					case "figcaption":
					case "figure":
					case "footer":
					case "header":
					case "hgroup":
					case "main":
					case "menu":
					case "nav":
					case "ol":
					case "p":
					case "section":
					case "summary":
					case "ul":
					case "pre":
					case "listing":
					case "table":
					case "hr":
					case "xmp":
					case "h1":
					case "h2":
					case "h3":
					case "h4":
					case "h5":
					case "h6": return ancestorInfo.pTagInButtonScope;
					case "form": return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;
					case "li": return ancestorInfo.listItemTagAutoclosing;
					case "dd":
					case "dt": return ancestorInfo.dlItemTagAutoclosing;
					case "button": return ancestorInfo.buttonTagInScope;
					case "a": return ancestorInfo.aTagInScope;
					case "nobr": return ancestorInfo.nobrTagInScope;
				}
				return null;
			};
			var didWarn$1 = {};
			validateDOMNesting = function(childTag, childText, ancestorInfo) {
				ancestorInfo = ancestorInfo || emptyAncestorInfo;
				var parentInfo = ancestorInfo.current;
				var parentTag = parentInfo && parentInfo.tag;
				if (childText != null) {
					if (childTag != null) error("validateDOMNesting: when childText is passed, childTag should be null");
					childTag = "#text";
				}
				var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
				var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
				var invalidParentOrAncestor = invalidParent || invalidAncestor;
				if (!invalidParentOrAncestor) return;
				var ancestorTag = invalidParentOrAncestor.tag;
				var warnKey = !!invalidParent + "|" + childTag + "|" + ancestorTag;
				if (didWarn$1[warnKey]) return;
				didWarn$1[warnKey] = true;
				var tagDisplayName = childTag;
				var whitespaceInfo = "";
				if (childTag === "#text") {
					if (/\S/.test(childText)) tagDisplayName = "Text nodes";
					else {
						tagDisplayName = "Whitespace text nodes";
						whitespaceInfo = " Make sure you don't have any extra whitespace between tags on each line of your source code.";
					}
				} else tagDisplayName = "<" + childTag + ">";
				if (invalidParent) {
					var info = "";
					if (ancestorTag === "table" && childTag === "tr") info += " Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by the browser.";
					error("validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s", tagDisplayName, ancestorTag, whitespaceInfo, info);
				} else error("validateDOMNesting(...): %s cannot appear as a descendant of <%s>.", tagDisplayName, ancestorTag);
			};
			var SUPPRESS_HYDRATION_WARNING$1 = "suppressHydrationWarning";
			var SUSPENSE_START_DATA = "$";
			var SUSPENSE_END_DATA = "/$";
			var SUSPENSE_PENDING_START_DATA = "$?";
			var SUSPENSE_FALLBACK_START_DATA = "$!";
			var STYLE$1 = "style";
			var eventsEnabled = null;
			var selectionInformation = null;
			function getRootHostContext(rootContainerInstance) {
				var type;
				var namespace;
				var nodeType = rootContainerInstance.nodeType;
				switch (nodeType) {
					case DOCUMENT_NODE:
					case DOCUMENT_FRAGMENT_NODE:
						type = nodeType === DOCUMENT_NODE ? "#document" : "#fragment";
						var root = rootContainerInstance.documentElement;
						namespace = root ? root.namespaceURI : getChildNamespace(null, "");
						break;
					default:
						var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
						var ownNamespace = container.namespaceURI || null;
						type = container.tagName;
						namespace = getChildNamespace(ownNamespace, type);
				}
				var validatedTag = type.toLowerCase();
				var ancestorInfo = updatedAncestorInfo(null, validatedTag);
				return {
					namespace,
					ancestorInfo
				};
			}
			function getChildHostContext(parentHostContext, type, rootContainerInstance) {
				var parentHostContextDev = parentHostContext;
				return {
					namespace: getChildNamespace(parentHostContextDev.namespace, type),
					ancestorInfo: updatedAncestorInfo(parentHostContextDev.ancestorInfo, type)
				};
			}
			function getPublicInstance(instance) {
				return instance;
			}
			function prepareForCommit(containerInfo) {
				eventsEnabled = isEnabled();
				selectionInformation = getSelectionInformation();
				var activeInstance = null;
				setEnabled(false);
				return activeInstance;
			}
			function resetAfterCommit(containerInfo) {
				restoreSelection(selectionInformation);
				setEnabled(eventsEnabled);
				eventsEnabled = null;
				selectionInformation = null;
			}
			function createInstance(type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
				var parentNamespace;
				var hostContextDev = hostContext;
				validateDOMNesting(type, null, hostContextDev.ancestorInfo);
				if (typeof props.children === "string" || typeof props.children === "number") {
					var string = "" + props.children;
					var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
					validateDOMNesting(null, string, ownAncestorInfo);
				}
				parentNamespace = hostContextDev.namespace;
				var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
				precacheFiberNode(internalInstanceHandle, domElement);
				updateFiberProps(domElement, props);
				return domElement;
			}
			function appendInitialChild(parentInstance, child) {
				parentInstance.appendChild(child);
			}
			function finalizeInitialChildren(domElement, type, props, rootContainerInstance, hostContext) {
				setInitialProperties(domElement, type, props, rootContainerInstance);
				switch (type) {
					case "button":
					case "input":
					case "select":
					case "textarea": return !!props.autoFocus;
					case "img": return true;
					default: return false;
				}
			}
			function prepareUpdate(domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
				var hostContextDev = hostContext;
				if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === "string" || typeof newProps.children === "number")) {
					var string = "" + newProps.children;
					var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
					validateDOMNesting(null, string, ownAncestorInfo);
				}
				return diffProperties(domElement, type, oldProps, newProps);
			}
			function shouldSetTextContent(type, props) {
				return type === "textarea" || type === "noscript" || typeof props.children === "string" || typeof props.children === "number" || typeof props.dangerouslySetInnerHTML === "object" && props.dangerouslySetInnerHTML !== null && props.dangerouslySetInnerHTML.__html != null;
			}
			function createTextInstance(text, rootContainerInstance, hostContext, internalInstanceHandle) {
				validateDOMNesting(null, text, hostContext.ancestorInfo);
				var textNode = createTextNode(text, rootContainerInstance);
				precacheFiberNode(internalInstanceHandle, textNode);
				return textNode;
			}
			function getCurrentEventPriority() {
				var currentEvent = window.event;
				if (currentEvent === void 0) return DefaultEventPriority;
				return getEventPriority(currentEvent.type);
			}
			var scheduleTimeout = typeof setTimeout === "function" ? setTimeout : void 0;
			var cancelTimeout = typeof clearTimeout === "function" ? clearTimeout : void 0;
			var noTimeout = -1;
			var localPromise = typeof Promise === "function" ? Promise : void 0;
			var scheduleMicrotask = typeof queueMicrotask === "function" ? queueMicrotask : typeof localPromise !== "undefined" ? function(callback) {
				return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
			} : scheduleTimeout;
			function handleErrorInNextTick(error) {
				setTimeout(function() {
					throw error;
				});
			}
			function commitMount(domElement, type, newProps, internalInstanceHandle) {
				switch (type) {
					case "button":
					case "input":
					case "select":
					case "textarea":
						if (newProps.autoFocus) domElement.focus();
						return;
					case "img":
						if (newProps.src) domElement.src = newProps.src;
						return;
				}
			}
			function commitUpdate(domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
				updateProperties(domElement, updatePayload, type, oldProps, newProps);
				updateFiberProps(domElement, newProps);
			}
			function resetTextContent(domElement) {
				setTextContent(domElement, "");
			}
			function commitTextUpdate(textInstance, oldText, newText) {
				textInstance.nodeValue = newText;
			}
			function appendChild(parentInstance, child) {
				parentInstance.appendChild(child);
			}
			function appendChildToContainer(container, child) {
				var parentNode;
				if (container.nodeType === COMMENT_NODE) {
					parentNode = container.parentNode;
					parentNode.insertBefore(child, container);
				} else {
					parentNode = container;
					parentNode.appendChild(child);
				}
				var reactRootContainer = container._reactRootContainer;
				if ((reactRootContainer === null || reactRootContainer === void 0) && parentNode.onclick === null) trapClickOnNonInteractiveElement(parentNode);
			}
			function insertBefore(parentInstance, child, beforeChild) {
				parentInstance.insertBefore(child, beforeChild);
			}
			function insertInContainerBefore(container, child, beforeChild) {
				if (container.nodeType === COMMENT_NODE) container.parentNode.insertBefore(child, beforeChild);
				else container.insertBefore(child, beforeChild);
			}
			function removeChild(parentInstance, child) {
				parentInstance.removeChild(child);
			}
			function removeChildFromContainer(container, child) {
				if (container.nodeType === COMMENT_NODE) container.parentNode.removeChild(child);
				else container.removeChild(child);
			}
			function clearSuspenseBoundary(parentInstance, suspenseInstance) {
				var node = suspenseInstance;
				var depth = 0;
				do {
					var nextNode = node.nextSibling;
					parentInstance.removeChild(node);
					if (nextNode && nextNode.nodeType === COMMENT_NODE) {
						var data = nextNode.data;
						if (data === SUSPENSE_END_DATA) {
							if (depth === 0) {
								parentInstance.removeChild(nextNode);
								retryIfBlockedOn(suspenseInstance);
								return;
							} else depth--;
						} else if (data === SUSPENSE_START_DATA || data === SUSPENSE_PENDING_START_DATA || data === SUSPENSE_FALLBACK_START_DATA) depth++;
					}
					node = nextNode;
				} while (node);
				retryIfBlockedOn(suspenseInstance);
			}
			function clearSuspenseBoundaryFromContainer(container, suspenseInstance) {
				if (container.nodeType === COMMENT_NODE) clearSuspenseBoundary(container.parentNode, suspenseInstance);
				else if (container.nodeType === ELEMENT_NODE) clearSuspenseBoundary(container, suspenseInstance);
				retryIfBlockedOn(container);
			}
			function hideInstance(instance) {
				instance = instance;
				var style = instance.style;
				if (typeof style.setProperty === "function") style.setProperty("display", "none", "important");
				else style.display = "none";
			}
			function hideTextInstance(textInstance) {
				textInstance.nodeValue = "";
			}
			function unhideInstance(instance, props) {
				instance = instance;
				var styleProp = props[STYLE$1];
				var display = styleProp !== void 0 && styleProp !== null && styleProp.hasOwnProperty("display") ? styleProp.display : null;
				instance.style.display = dangerousStyleValue("display", display);
			}
			function unhideTextInstance(textInstance, text) {
				textInstance.nodeValue = text;
			}
			function clearContainer(container) {
				if (container.nodeType === ELEMENT_NODE) container.textContent = "";
				else if (container.nodeType === DOCUMENT_NODE) {
					if (container.documentElement) container.removeChild(container.documentElement);
				}
			}
			function canHydrateInstance(instance, type, props) {
				if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) return null;
				return instance;
			}
			function canHydrateTextInstance(instance, text) {
				if (text === "" || instance.nodeType !== TEXT_NODE) return null;
				return instance;
			}
			function canHydrateSuspenseInstance(instance) {
				if (instance.nodeType !== COMMENT_NODE) return null;
				return instance;
			}
			function isSuspenseInstancePending(instance) {
				return instance.data === SUSPENSE_PENDING_START_DATA;
			}
			function isSuspenseInstanceFallback(instance) {
				return instance.data === SUSPENSE_FALLBACK_START_DATA;
			}
			function getSuspenseInstanceFallbackErrorDetails(instance) {
				var dataset = instance.nextSibling && instance.nextSibling.dataset;
				var digest, message, stack;
				if (dataset) {
					digest = dataset.dgst;
					message = dataset.msg;
					stack = dataset.stck;
				}
				return {
					message,
					digest,
					stack
				};
			}
			function registerSuspenseInstanceRetry(instance, callback) {
				instance._reactRetry = callback;
			}
			function getNextHydratable(node) {
				for (; node != null; node = node.nextSibling) {
					var nodeType = node.nodeType;
					if (nodeType === ELEMENT_NODE || nodeType === TEXT_NODE) break;
					if (nodeType === COMMENT_NODE) {
						var nodeData = node.data;
						if (nodeData === SUSPENSE_START_DATA || nodeData === SUSPENSE_FALLBACK_START_DATA || nodeData === SUSPENSE_PENDING_START_DATA) break;
						if (nodeData === SUSPENSE_END_DATA) return null;
					}
				}
				return node;
			}
			function getNextHydratableSibling(instance) {
				return getNextHydratable(instance.nextSibling);
			}
			function getFirstHydratableChild(parentInstance) {
				return getNextHydratable(parentInstance.firstChild);
			}
			function getFirstHydratableChildWithinContainer(parentContainer) {
				return getNextHydratable(parentContainer.firstChild);
			}
			function getFirstHydratableChildWithinSuspenseInstance(parentInstance) {
				return getNextHydratable(parentInstance.nextSibling);
			}
			function hydrateInstance(instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle, shouldWarnDev) {
				precacheFiberNode(internalInstanceHandle, instance);
				updateFiberProps(instance, props);
				var parentNamespace = hostContext.namespace;
				return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance, (internalInstanceHandle.mode & ConcurrentMode) !== NoMode, shouldWarnDev);
			}
			function hydrateTextInstance(textInstance, text, internalInstanceHandle, shouldWarnDev) {
				precacheFiberNode(internalInstanceHandle, textInstance);
				internalInstanceHandle.mode & ConcurrentMode;
				return diffHydratedText(textInstance, text);
			}
			function hydrateSuspenseInstance(suspenseInstance, internalInstanceHandle) {
				precacheFiberNode(internalInstanceHandle, suspenseInstance);
			}
			function getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance) {
				var node = suspenseInstance.nextSibling;
				var depth = 0;
				while (node) {
					if (node.nodeType === COMMENT_NODE) {
						var data = node.data;
						if (data === SUSPENSE_END_DATA) {
							if (depth === 0) return getNextHydratableSibling(node);
							else depth--;
						} else if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) depth++;
					}
					node = node.nextSibling;
				}
				return null;
			}
			function getParentSuspenseInstance(targetInstance) {
				var node = targetInstance.previousSibling;
				var depth = 0;
				while (node) {
					if (node.nodeType === COMMENT_NODE) {
						var data = node.data;
						if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
							if (depth === 0) return node;
							else depth--;
						} else if (data === SUSPENSE_END_DATA) depth++;
					}
					node = node.previousSibling;
				}
				return null;
			}
			function commitHydratedContainer(container) {
				retryIfBlockedOn(container);
			}
			function commitHydratedSuspenseInstance(suspenseInstance) {
				retryIfBlockedOn(suspenseInstance);
			}
			function shouldDeleteUnhydratedTailInstances(parentType) {
				return parentType !== "head" && parentType !== "body";
			}
			function didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, text, isConcurrentMode) {
				checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, true);
			}
			function didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, text, isConcurrentMode) {
				if (parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) checkForUnmatchedText(textInstance.nodeValue, text, isConcurrentMode, true);
			}
			function didNotHydrateInstanceWithinContainer(parentContainer, instance) {
				if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentContainer, instance);
				else if (instance.nodeType === COMMENT_NODE);
				else warnForDeletedHydratableText(parentContainer, instance);
			}
			function didNotHydrateInstanceWithinSuspenseInstance(parentInstance, instance) {
				var parentNode = parentInstance.parentNode;
				if (parentNode !== null) {
					if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentNode, instance);
					else if (instance.nodeType === COMMENT_NODE);
					else warnForDeletedHydratableText(parentNode, instance);
				}
			}
			function didNotHydrateInstance(parentType, parentProps, parentInstance, instance, isConcurrentMode) {
				if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
					if (instance.nodeType === ELEMENT_NODE) warnForDeletedHydratableElement(parentInstance, instance);
					else if (instance.nodeType === COMMENT_NODE);
					else warnForDeletedHydratableText(parentInstance, instance);
				}
			}
			function didNotFindHydratableInstanceWithinContainer(parentContainer, type, props) {
				warnForInsertedHydratedElement(parentContainer, type);
			}
			function didNotFindHydratableTextInstanceWithinContainer(parentContainer, text) {
				warnForInsertedHydratedText(parentContainer, text);
			}
			function didNotFindHydratableInstanceWithinSuspenseInstance(parentInstance, type, props) {
				var parentNode = parentInstance.parentNode;
				if (parentNode !== null) warnForInsertedHydratedElement(parentNode, type);
			}
			function didNotFindHydratableTextInstanceWithinSuspenseInstance(parentInstance, text) {
				var parentNode = parentInstance.parentNode;
				if (parentNode !== null) warnForInsertedHydratedText(parentNode, text);
			}
			function didNotFindHydratableInstance(parentType, parentProps, parentInstance, type, props, isConcurrentMode) {
				if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) warnForInsertedHydratedElement(parentInstance, type);
			}
			function didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, text, isConcurrentMode) {
				if (isConcurrentMode || parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) warnForInsertedHydratedText(parentInstance, text);
			}
			function errorHydratingContainer(parentContainer) {
				error("An error occurred during hydration. The server HTML was replaced with client content in <%s>.", parentContainer.nodeName.toLowerCase());
			}
			function preparePortalMount(portalInstance) {
				listenToAllSupportedEvents(portalInstance);
			}
			var randomKey = Math.random().toString(36).slice(2);
			var internalInstanceKey = "__reactFiber$" + randomKey;
			var internalPropsKey = "__reactProps$" + randomKey;
			var internalContainerInstanceKey = "__reactContainer$" + randomKey;
			var internalEventHandlersKey = "__reactEvents$" + randomKey;
			var internalEventHandlerListenersKey = "__reactListeners$" + randomKey;
			var internalEventHandlesSetKey = "__reactHandles$" + randomKey;
			function detachDeletedInstance(node) {
				delete node[internalInstanceKey];
				delete node[internalPropsKey];
				delete node[internalEventHandlersKey];
				delete node[internalEventHandlerListenersKey];
				delete node[internalEventHandlesSetKey];
			}
			function precacheFiberNode(hostInst, node) {
				node[internalInstanceKey] = hostInst;
			}
			function markContainerAsRoot(hostRoot, node) {
				node[internalContainerInstanceKey] = hostRoot;
			}
			function unmarkContainerAsRoot(node) {
				node[internalContainerInstanceKey] = null;
			}
			function isContainerMarkedAsRoot(node) {
				return !!node[internalContainerInstanceKey];
			}
			function getClosestInstanceFromNode(targetNode) {
				var targetInst = targetNode[internalInstanceKey];
				if (targetInst) return targetInst;
				var parentNode = targetNode.parentNode;
				while (parentNode) {
					targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey];
					if (targetInst) {
						var alternate = targetInst.alternate;
						if (targetInst.child !== null || alternate !== null && alternate.child !== null) {
							var suspenseInstance = getParentSuspenseInstance(targetNode);
							while (suspenseInstance !== null) {
								var targetSuspenseInst = suspenseInstance[internalInstanceKey];
								if (targetSuspenseInst) return targetSuspenseInst;
								suspenseInstance = getParentSuspenseInstance(suspenseInstance);
							}
						}
						return targetInst;
					}
					targetNode = parentNode;
					parentNode = targetNode.parentNode;
				}
				return null;
			}
			/**
			* Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
			* instance, or null if the node was not rendered by this React.
			*/
			function getInstanceFromNode(node) {
				var inst = node[internalInstanceKey] || node[internalContainerInstanceKey];
				if (inst) {
					if (inst.tag === HostComponent || inst.tag === HostText || inst.tag === SuspenseComponent || inst.tag === HostRoot) return inst;
					else return null;
				}
				return null;
			}
			/**
			* Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
			* DOM node.
			*/
			function getNodeFromInstance(inst) {
				if (inst.tag === HostComponent || inst.tag === HostText) return inst.stateNode;
				throw new Error("getNodeFromInstance: Invalid argument.");
			}
			function getFiberCurrentPropsFromNode(node) {
				return node[internalPropsKey] || null;
			}
			function updateFiberProps(node, props) {
				node[internalPropsKey] = props;
			}
			function getEventListenerSet(node) {
				var elementListenerSet = node[internalEventHandlersKey];
				if (elementListenerSet === void 0) elementListenerSet = node[internalEventHandlersKey] = /* @__PURE__ */ new Set();
				return elementListenerSet;
			}
			var loggedTypeFailures = {};
			var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
			function setCurrentlyValidatingElement(element) {
				if (element) {
					var owner = element._owner;
					var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
					ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
				} else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
			}
			function checkPropTypes(typeSpecs, values, location, componentName, element) {
				var has = Function.call.bind(hasOwnProperty);
				for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
					var error$1 = void 0;
					try {
						if (typeof typeSpecs[typeSpecName] !== "function") {
							var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
							err.name = "Invariant Violation";
							throw err;
						}
						error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
					} catch (ex) {
						error$1 = ex;
					}
					if (error$1 && !(error$1 instanceof Error)) {
						setCurrentlyValidatingElement(element);
						error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
						setCurrentlyValidatingElement(null);
					}
					if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
						loggedTypeFailures[error$1.message] = true;
						setCurrentlyValidatingElement(element);
						error("Failed %s type: %s", location, error$1.message);
						setCurrentlyValidatingElement(null);
					}
				}
			}
			var valueStack = [];
			var fiberStack = [];
			var index = -1;
			function createCursor(defaultValue) {
				return { current: defaultValue };
			}
			function pop(cursor, fiber) {
				if (index < 0) {
					error("Unexpected pop.");
					return;
				}
				if (fiber !== fiberStack[index]) error("Unexpected Fiber popped.");
				cursor.current = valueStack[index];
				valueStack[index] = null;
				fiberStack[index] = null;
				index--;
			}
			function push(cursor, value, fiber) {
				index++;
				valueStack[index] = cursor.current;
				fiberStack[index] = fiber;
				cursor.current = value;
			}
			var warnedAboutMissingGetChildContext = {};
			var emptyContextObject = {};
			Object.freeze(emptyContextObject);
			var contextStackCursor = createCursor(emptyContextObject);
			var didPerformWorkStackCursor = createCursor(false);
			var previousContext = emptyContextObject;
			function getUnmaskedContext(workInProgress, Component, didPushOwnContextIfProvider) {
				if (didPushOwnContextIfProvider && isContextProvider(Component)) return previousContext;
				return contextStackCursor.current;
			}
			function cacheContext(workInProgress, unmaskedContext, maskedContext) {
				var instance = workInProgress.stateNode;
				instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
				instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
			}
			function getMaskedContext(workInProgress, unmaskedContext) {
				var contextTypes = workInProgress.type.contextTypes;
				if (!contextTypes) return emptyContextObject;
				var instance = workInProgress.stateNode;
				if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) return instance.__reactInternalMemoizedMaskedChildContext;
				var context = {};
				for (var key in contextTypes) context[key] = unmaskedContext[key];
				checkPropTypes(contextTypes, context, "context", getComponentNameFromFiber(workInProgress) || "Unknown");
				if (instance) cacheContext(workInProgress, unmaskedContext, context);
				return context;
			}
			function hasContextChanged() {
				return didPerformWorkStackCursor.current;
			}
			function isContextProvider(type) {
				var childContextTypes = type.childContextTypes;
				return childContextTypes !== null && childContextTypes !== void 0;
			}
			function popContext(fiber) {
				pop(didPerformWorkStackCursor, fiber);
				pop(contextStackCursor, fiber);
			}
			function popTopLevelContextObject(fiber) {
				pop(didPerformWorkStackCursor, fiber);
				pop(contextStackCursor, fiber);
			}
			function pushTopLevelContextObject(fiber, context, didChange) {
				if (contextStackCursor.current !== emptyContextObject) throw new Error("Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.");
				push(contextStackCursor, context, fiber);
				push(didPerformWorkStackCursor, didChange, fiber);
			}
			function processChildContext(fiber, type, parentContext) {
				var instance = fiber.stateNode;
				var childContextTypes = type.childContextTypes;
				if (typeof instance.getChildContext !== "function") {
					var componentName = getComponentNameFromFiber(fiber) || "Unknown";
					if (!warnedAboutMissingGetChildContext[componentName]) {
						warnedAboutMissingGetChildContext[componentName] = true;
						error("%s.childContextTypes is specified but there is no getChildContext() method on the instance. You can either define getChildContext() on %s or remove childContextTypes from it.", componentName, componentName);
					}
					return parentContext;
				}
				var childContext = instance.getChildContext();
				for (var contextKey in childContext) if (!(contextKey in childContextTypes)) throw new Error((getComponentNameFromFiber(fiber) || "Unknown") + ".getChildContext(): key \"" + contextKey + "\" is not defined in childContextTypes.");
				checkPropTypes(childContextTypes, childContext, "child context", getComponentNameFromFiber(fiber) || "Unknown");
				return assign({}, parentContext, childContext);
			}
			function pushContextProvider(workInProgress) {
				var instance = workInProgress.stateNode;
				var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyContextObject;
				previousContext = contextStackCursor.current;
				push(contextStackCursor, memoizedMergedChildContext, workInProgress);
				push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);
				return true;
			}
			function invalidateContextProvider(workInProgress, type, didChange) {
				var instance = workInProgress.stateNode;
				if (!instance) throw new Error("Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.");
				if (didChange) {
					var mergedContext = processChildContext(workInProgress, type, previousContext);
					instance.__reactInternalMemoizedMergedChildContext = mergedContext;
					pop(didPerformWorkStackCursor, workInProgress);
					pop(contextStackCursor, workInProgress);
					push(contextStackCursor, mergedContext, workInProgress);
					push(didPerformWorkStackCursor, didChange, workInProgress);
				} else {
					pop(didPerformWorkStackCursor, workInProgress);
					push(didPerformWorkStackCursor, didChange, workInProgress);
				}
			}
			function findCurrentUnmaskedContext(fiber) {
				if (!isFiberMounted(fiber) || fiber.tag !== ClassComponent) throw new Error("Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.");
				var node = fiber;
				do {
					switch (node.tag) {
						case HostRoot: return node.stateNode.context;
						case ClassComponent:
							var Component = node.type;
							if (isContextProvider(Component)) return node.stateNode.__reactInternalMemoizedMergedChildContext;
					}
					node = node.return;
				} while (node !== null);
				throw new Error("Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.");
			}
			var LegacyRoot = 0;
			var ConcurrentRoot = 1;
			var syncQueue = null;
			var includesLegacySyncCallbacks = false;
			var isFlushingSyncQueue = false;
			function scheduleSyncCallback(callback) {
				if (syncQueue === null) syncQueue = [callback];
				else syncQueue.push(callback);
			}
			function scheduleLegacySyncCallback(callback) {
				includesLegacySyncCallbacks = true;
				scheduleSyncCallback(callback);
			}
			function flushSyncCallbacksOnlyInLegacyMode() {
				if (includesLegacySyncCallbacks) flushSyncCallbacks();
			}
			function flushSyncCallbacks() {
				if (!isFlushingSyncQueue && syncQueue !== null) {
					isFlushingSyncQueue = true;
					var i = 0;
					var previousUpdatePriority = getCurrentUpdatePriority();
					try {
						var isSync = true;
						var queue = syncQueue;
						setCurrentUpdatePriority(DiscreteEventPriority);
						for (; i < queue.length; i++) {
							var callback = queue[i];
							do
								callback = callback(isSync);
							while (callback !== null);
						}
						syncQueue = null;
						includesLegacySyncCallbacks = false;
					} catch (error) {
						if (syncQueue !== null) syncQueue = syncQueue.slice(i + 1);
						scheduleCallback(ImmediatePriority, flushSyncCallbacks);
						throw error;
					} finally {
						setCurrentUpdatePriority(previousUpdatePriority);
						isFlushingSyncQueue = false;
					}
				}
				return null;
			}
			var forkStack = [];
			var forkStackIndex = 0;
			var treeForkProvider = null;
			var treeForkCount = 0;
			var idStack = [];
			var idStackIndex = 0;
			var treeContextProvider = null;
			var treeContextId = 1;
			var treeContextOverflow = "";
			function isForkedChild(workInProgress) {
				warnIfNotHydrating();
				return (workInProgress.flags & Forked) !== NoFlags;
			}
			function getForksAtLevel(workInProgress) {
				warnIfNotHydrating();
				return treeForkCount;
			}
			function getTreeId() {
				var overflow = treeContextOverflow;
				var idWithLeadingBit = treeContextId;
				return (idWithLeadingBit & ~getLeadingBit(idWithLeadingBit)).toString(32) + overflow;
			}
			function pushTreeFork(workInProgress, totalChildren) {
				warnIfNotHydrating();
				forkStack[forkStackIndex++] = treeForkCount;
				forkStack[forkStackIndex++] = treeForkProvider;
				treeForkProvider = workInProgress;
				treeForkCount = totalChildren;
			}
			function pushTreeId(workInProgress, totalChildren, index) {
				warnIfNotHydrating();
				idStack[idStackIndex++] = treeContextId;
				idStack[idStackIndex++] = treeContextOverflow;
				idStack[idStackIndex++] = treeContextProvider;
				treeContextProvider = workInProgress;
				var baseIdWithLeadingBit = treeContextId;
				var baseOverflow = treeContextOverflow;
				var baseLength = getBitLength(baseIdWithLeadingBit) - 1;
				var baseId = baseIdWithLeadingBit & ~(1 << baseLength);
				var slot = index + 1;
				var length = getBitLength(totalChildren) + baseLength;
				if (length > 30) {
					var numberOfOverflowBits = baseLength - baseLength % 5;
					var newOverflow = (baseId & (1 << numberOfOverflowBits) - 1).toString(32);
					var restOfBaseId = baseId >> numberOfOverflowBits;
					var restOfBaseLength = baseLength - numberOfOverflowBits;
					var restOfLength = getBitLength(totalChildren) + restOfBaseLength;
					var id = slot << restOfBaseLength | restOfBaseId;
					var overflow = newOverflow + baseOverflow;
					treeContextId = 1 << restOfLength | id;
					treeContextOverflow = overflow;
				} else {
					var _id = slot << baseLength | baseId;
					var _overflow = baseOverflow;
					treeContextId = 1 << length | _id;
					treeContextOverflow = _overflow;
				}
			}
			function pushMaterializedTreeId(workInProgress) {
				warnIfNotHydrating();
				if (workInProgress.return !== null) {
					var numberOfForks = 1;
					var slotIndex = 0;
					pushTreeFork(workInProgress, numberOfForks);
					pushTreeId(workInProgress, numberOfForks, slotIndex);
				}
			}
			function getBitLength(number) {
				return 32 - clz32(number);
			}
			function getLeadingBit(id) {
				return 1 << getBitLength(id) - 1;
			}
			function popTreeContext(workInProgress) {
				while (workInProgress === treeForkProvider) {
					treeForkProvider = forkStack[--forkStackIndex];
					forkStack[forkStackIndex] = null;
					treeForkCount = forkStack[--forkStackIndex];
					forkStack[forkStackIndex] = null;
				}
				while (workInProgress === treeContextProvider) {
					treeContextProvider = idStack[--idStackIndex];
					idStack[idStackIndex] = null;
					treeContextOverflow = idStack[--idStackIndex];
					idStack[idStackIndex] = null;
					treeContextId = idStack[--idStackIndex];
					idStack[idStackIndex] = null;
				}
			}
			function getSuspendedTreeContext() {
				warnIfNotHydrating();
				if (treeContextProvider !== null) return {
					id: treeContextId,
					overflow: treeContextOverflow
				};
				else return null;
			}
			function restoreSuspendedTreeContext(workInProgress, suspendedContext) {
				warnIfNotHydrating();
				idStack[idStackIndex++] = treeContextId;
				idStack[idStackIndex++] = treeContextOverflow;
				idStack[idStackIndex++] = treeContextProvider;
				treeContextId = suspendedContext.id;
				treeContextOverflow = suspendedContext.overflow;
				treeContextProvider = workInProgress;
			}
			function warnIfNotHydrating() {
				if (!getIsHydrating()) error("Expected to be hydrating. This is a bug in React. Please file an issue.");
			}
			var hydrationParentFiber = null;
			var nextHydratableInstance = null;
			var isHydrating = false;
			var didSuspendOrErrorDEV = false;
			var hydrationErrors = null;
			function warnIfHydrating() {
				if (isHydrating) error("We should not be hydrating here. This is a bug in React. Please file a bug.");
			}
			function markDidThrowWhileHydratingDEV() {
				didSuspendOrErrorDEV = true;
			}
			function didSuspendOrErrorWhileHydratingDEV() {
				return didSuspendOrErrorDEV;
			}
			function enterHydrationState(fiber) {
				var parentInstance = fiber.stateNode.containerInfo;
				nextHydratableInstance = getFirstHydratableChildWithinContainer(parentInstance);
				hydrationParentFiber = fiber;
				isHydrating = true;
				hydrationErrors = null;
				didSuspendOrErrorDEV = false;
				return true;
			}
			function reenterHydrationStateFromDehydratedSuspenseInstance(fiber, suspenseInstance, treeContext) {
				nextHydratableInstance = getFirstHydratableChildWithinSuspenseInstance(suspenseInstance);
				hydrationParentFiber = fiber;
				isHydrating = true;
				hydrationErrors = null;
				didSuspendOrErrorDEV = false;
				if (treeContext !== null) restoreSuspendedTreeContext(fiber, treeContext);
				return true;
			}
			function warnUnhydratedInstance(returnFiber, instance) {
				switch (returnFiber.tag) {
					case HostRoot:
						didNotHydrateInstanceWithinContainer(returnFiber.stateNode.containerInfo, instance);
						break;
					case HostComponent:
						var isConcurrentMode = (returnFiber.mode & ConcurrentMode) !== NoMode;
						didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance, isConcurrentMode);
						break;
					case SuspenseComponent:
						var suspenseState = returnFiber.memoizedState;
						if (suspenseState.dehydrated !== null) didNotHydrateInstanceWithinSuspenseInstance(suspenseState.dehydrated, instance);
				}
			}
			function deleteHydratableInstance(returnFiber, instance) {
				warnUnhydratedInstance(returnFiber, instance);
				var childToDelete = createFiberFromHostInstanceForDeletion();
				childToDelete.stateNode = instance;
				childToDelete.return = returnFiber;
				var deletions = returnFiber.deletions;
				if (deletions === null) {
					returnFiber.deletions = [childToDelete];
					returnFiber.flags |= ChildDeletion;
				} else deletions.push(childToDelete);
			}
			function warnNonhydratedInstance(returnFiber, fiber) {
				if (didSuspendOrErrorDEV) return;
				switch (returnFiber.tag) {
					case HostRoot:
						var parentContainer = returnFiber.stateNode.containerInfo;
						switch (fiber.tag) {
							case HostComponent:
								var type = fiber.type;
								fiber.pendingProps;
								didNotFindHydratableInstanceWithinContainer(parentContainer, type);
								break;
							case HostText:
								var text = fiber.pendingProps;
								didNotFindHydratableTextInstanceWithinContainer(parentContainer, text);
						}
						break;
					case HostComponent:
						var parentType = returnFiber.type;
						var parentProps = returnFiber.memoizedProps;
						var parentInstance = returnFiber.stateNode;
						switch (fiber.tag) {
							case HostComponent:
								var _type = fiber.type;
								var _props = fiber.pendingProps;
								didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props, (returnFiber.mode & ConcurrentMode) !== NoMode);
								break;
							case HostText:
								var _text = fiber.pendingProps;
								didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text, (returnFiber.mode & ConcurrentMode) !== NoMode);
						}
						break;
					case SuspenseComponent:
						var _parentInstance = returnFiber.memoizedState.dehydrated;
						if (_parentInstance !== null) switch (fiber.tag) {
							case HostComponent:
								var _type2 = fiber.type;
								fiber.pendingProps;
								didNotFindHydratableInstanceWithinSuspenseInstance(_parentInstance, _type2);
								break;
							case HostText:
								var _text2 = fiber.pendingProps;
								didNotFindHydratableTextInstanceWithinSuspenseInstance(_parentInstance, _text2);
						}
						break;
					default: return;
				}
			}
			function insertNonHydratedInstance(returnFiber, fiber) {
				fiber.flags = fiber.flags & ~Hydrating | Placement;
				warnNonhydratedInstance(returnFiber, fiber);
			}
			function tryHydrate(fiber, nextInstance) {
				switch (fiber.tag) {
					case HostComponent:
						var type = fiber.type;
						fiber.pendingProps;
						var instance = canHydrateInstance(nextInstance, type);
						if (instance !== null) {
							fiber.stateNode = instance;
							hydrationParentFiber = fiber;
							nextHydratableInstance = getFirstHydratableChild(instance);
							return true;
						}
						return false;
					case HostText:
						var text = fiber.pendingProps;
						var textInstance = canHydrateTextInstance(nextInstance, text);
						if (textInstance !== null) {
							fiber.stateNode = textInstance;
							hydrationParentFiber = fiber;
							nextHydratableInstance = null;
							return true;
						}
						return false;
					case SuspenseComponent:
						var suspenseInstance = canHydrateSuspenseInstance(nextInstance);
						if (suspenseInstance !== null) {
							fiber.memoizedState = {
								dehydrated: suspenseInstance,
								treeContext: getSuspendedTreeContext(),
								retryLane: OffscreenLane
							};
							var dehydratedFragment = createFiberFromDehydratedFragment(suspenseInstance);
							dehydratedFragment.return = fiber;
							fiber.child = dehydratedFragment;
							hydrationParentFiber = fiber;
							nextHydratableInstance = null;
							return true;
						}
						return false;
					default: return false;
				}
			}
			function shouldClientRenderOnMismatch(fiber) {
				return (fiber.mode & ConcurrentMode) !== NoMode && (fiber.flags & DidCapture) === NoFlags;
			}
			function throwOnHydrationMismatch(fiber) {
				throw new Error("Hydration failed because the initial UI does not match what was rendered on the server.");
			}
			function tryToClaimNextHydratableInstance(fiber) {
				if (!isHydrating) return;
				var nextInstance = nextHydratableInstance;
				if (!nextInstance) {
					if (shouldClientRenderOnMismatch(fiber)) {
						warnNonhydratedInstance(hydrationParentFiber, fiber);
						throwOnHydrationMismatch();
					}
					insertNonHydratedInstance(hydrationParentFiber, fiber);
					isHydrating = false;
					hydrationParentFiber = fiber;
					return;
				}
				var firstAttemptedInstance = nextInstance;
				if (!tryHydrate(fiber, nextInstance)) {
					if (shouldClientRenderOnMismatch(fiber)) {
						warnNonhydratedInstance(hydrationParentFiber, fiber);
						throwOnHydrationMismatch();
					}
					nextInstance = getNextHydratableSibling(firstAttemptedInstance);
					var prevHydrationParentFiber = hydrationParentFiber;
					if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
						insertNonHydratedInstance(hydrationParentFiber, fiber);
						isHydrating = false;
						hydrationParentFiber = fiber;
						return;
					}
					deleteHydratableInstance(prevHydrationParentFiber, firstAttemptedInstance);
				}
			}
			function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
				var instance = fiber.stateNode;
				var shouldWarnIfMismatchDev = !didSuspendOrErrorDEV;
				var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber, shouldWarnIfMismatchDev);
				fiber.updateQueue = updatePayload;
				if (updatePayload !== null) return true;
				return false;
			}
			function prepareToHydrateHostTextInstance(fiber) {
				var textInstance = fiber.stateNode;
				var textContent = fiber.memoizedProps;
				var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
				if (shouldUpdate) {
					var returnFiber = hydrationParentFiber;
					if (returnFiber !== null) switch (returnFiber.tag) {
						case HostRoot:
							var parentContainer = returnFiber.stateNode.containerInfo;
							didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent, (returnFiber.mode & ConcurrentMode) !== NoMode);
							break;
						case HostComponent:
							var parentType = returnFiber.type;
							var parentProps = returnFiber.memoizedProps;
							var parentInstance = returnFiber.stateNode;
							didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent, (returnFiber.mode & ConcurrentMode) !== NoMode);
					}
				}
				return shouldUpdate;
			}
			function prepareToHydrateHostSuspenseInstance(fiber) {
				var suspenseState = fiber.memoizedState;
				var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
				if (!suspenseInstance) throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
				hydrateSuspenseInstance(suspenseInstance, fiber);
			}
			function skipPastDehydratedSuspenseInstance(fiber) {
				var suspenseState = fiber.memoizedState;
				var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;
				if (!suspenseInstance) throw new Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
				return getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance);
			}
			function popToNextHostParent(fiber) {
				var parent = fiber.return;
				while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot && parent.tag !== SuspenseComponent) parent = parent.return;
				hydrationParentFiber = parent;
			}
			function popHydrationState(fiber) {
				if (fiber !== hydrationParentFiber) return false;
				if (!isHydrating) {
					popToNextHostParent(fiber);
					isHydrating = true;
					return false;
				}
				if (fiber.tag !== HostRoot && (fiber.tag !== HostComponent || shouldDeleteUnhydratedTailInstances(fiber.type) && !shouldSetTextContent(fiber.type, fiber.memoizedProps))) {
					var nextInstance = nextHydratableInstance;
					if (nextInstance) {
						if (shouldClientRenderOnMismatch(fiber)) {
							warnIfUnhydratedTailNodes(fiber);
							throwOnHydrationMismatch();
						} else while (nextInstance) {
							deleteHydratableInstance(fiber, nextInstance);
							nextInstance = getNextHydratableSibling(nextInstance);
						}
					}
				}
				popToNextHostParent(fiber);
				if (fiber.tag === SuspenseComponent) nextHydratableInstance = skipPastDehydratedSuspenseInstance(fiber);
				else nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
				return true;
			}
			function hasUnhydratedTailNodes() {
				return isHydrating && nextHydratableInstance !== null;
			}
			function warnIfUnhydratedTailNodes(fiber) {
				var nextInstance = nextHydratableInstance;
				while (nextInstance) {
					warnUnhydratedInstance(fiber, nextInstance);
					nextInstance = getNextHydratableSibling(nextInstance);
				}
			}
			function resetHydrationState() {
				hydrationParentFiber = null;
				nextHydratableInstance = null;
				isHydrating = false;
				didSuspendOrErrorDEV = false;
			}
			function upgradeHydrationErrorsToRecoverable() {
				if (hydrationErrors !== null) {
					queueRecoverableErrors(hydrationErrors);
					hydrationErrors = null;
				}
			}
			function getIsHydrating() {
				return isHydrating;
			}
			function queueHydrationError(error) {
				if (hydrationErrors === null) hydrationErrors = [error];
				else hydrationErrors.push(error);
			}
			var ReactCurrentBatchConfig$1 = ReactSharedInternals.ReactCurrentBatchConfig;
			var NoTransition = null;
			function requestCurrentTransition() {
				return ReactCurrentBatchConfig$1.transition;
			}
			var ReactStrictModeWarnings = {
				recordUnsafeLifecycleWarnings: function(fiber, instance) {},
				flushPendingUnsafeLifecycleWarnings: function() {},
				recordLegacyContextWarning: function(fiber, instance) {},
				flushLegacyContextWarning: function() {},
				discardPendingWarnings: function() {}
			};
			var findStrictRoot = function(fiber) {
				var maybeStrictRoot = null;
				var node = fiber;
				while (node !== null) {
					if (node.mode & StrictLegacyMode) maybeStrictRoot = node;
					node = node.return;
				}
				return maybeStrictRoot;
			};
			var setToSortedString = function(set) {
				var array = [];
				set.forEach(function(value) {
					array.push(value);
				});
				return array.sort().join(", ");
			};
			var pendingComponentWillMountWarnings = [];
			var pendingUNSAFE_ComponentWillMountWarnings = [];
			var pendingComponentWillReceivePropsWarnings = [];
			var pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
			var pendingComponentWillUpdateWarnings = [];
			var pendingUNSAFE_ComponentWillUpdateWarnings = [];
			var didWarnAboutUnsafeLifecycles = /* @__PURE__ */ new Set();
			ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function(fiber, instance) {
				if (didWarnAboutUnsafeLifecycles.has(fiber.type)) return;
				if (typeof instance.componentWillMount === "function" && instance.componentWillMount.__suppressDeprecationWarning !== true) pendingComponentWillMountWarnings.push(fiber);
				if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillMount === "function") pendingUNSAFE_ComponentWillMountWarnings.push(fiber);
				if (typeof instance.componentWillReceiveProps === "function" && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) pendingComponentWillReceivePropsWarnings.push(fiber);
				if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillReceiveProps === "function") pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber);
				if (typeof instance.componentWillUpdate === "function" && instance.componentWillUpdate.__suppressDeprecationWarning !== true) pendingComponentWillUpdateWarnings.push(fiber);
				if (fiber.mode & StrictLegacyMode && typeof instance.UNSAFE_componentWillUpdate === "function") pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber);
			};
			ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function() {
				var componentWillMountUniqueNames = /* @__PURE__ */ new Set();
				if (pendingComponentWillMountWarnings.length > 0) {
					pendingComponentWillMountWarnings.forEach(function(fiber) {
						componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingComponentWillMountWarnings = [];
				}
				var UNSAFE_componentWillMountUniqueNames = /* @__PURE__ */ new Set();
				if (pendingUNSAFE_ComponentWillMountWarnings.length > 0) {
					pendingUNSAFE_ComponentWillMountWarnings.forEach(function(fiber) {
						UNSAFE_componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingUNSAFE_ComponentWillMountWarnings = [];
				}
				var componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
				if (pendingComponentWillReceivePropsWarnings.length > 0) {
					pendingComponentWillReceivePropsWarnings.forEach(function(fiber) {
						componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingComponentWillReceivePropsWarnings = [];
				}
				var UNSAFE_componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
				if (pendingUNSAFE_ComponentWillReceivePropsWarnings.length > 0) {
					pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function(fiber) {
						UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
				}
				var componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
				if (pendingComponentWillUpdateWarnings.length > 0) {
					pendingComponentWillUpdateWarnings.forEach(function(fiber) {
						componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingComponentWillUpdateWarnings = [];
				}
				var UNSAFE_componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
				if (pendingUNSAFE_ComponentWillUpdateWarnings.length > 0) {
					pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function(fiber) {
						UNSAFE_componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutUnsafeLifecycles.add(fiber.type);
					});
					pendingUNSAFE_ComponentWillUpdateWarnings = [];
				}
				if (UNSAFE_componentWillMountUniqueNames.size > 0) error("Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n\nPlease update the following components: %s", setToSortedString(UNSAFE_componentWillMountUniqueNames));
				if (UNSAFE_componentWillReceivePropsUniqueNames.size > 0) error("Using UNSAFE_componentWillReceiveProps in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n\nPlease update the following components: %s", setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames));
				if (UNSAFE_componentWillUpdateUniqueNames.size > 0) error("Using UNSAFE_componentWillUpdate in strict mode is not recommended and may indicate bugs in your code. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n\nPlease update the following components: %s", setToSortedString(UNSAFE_componentWillUpdateUniqueNames));
				if (componentWillMountUniqueNames.size > 0) warn("componentWillMount has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n* Rename componentWillMount to UNSAFE_componentWillMount to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", setToSortedString(componentWillMountUniqueNames));
				if (componentWillReceivePropsUniqueNames.size > 0) warn("componentWillReceiveProps has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://reactjs.org/link/derived-state\n* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", setToSortedString(componentWillReceivePropsUniqueNames));
				if (componentWillUpdateUniqueNames.size > 0) warn("componentWillUpdate has been renamed, and is not recommended for use. See https://reactjs.org/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", setToSortedString(componentWillUpdateUniqueNames));
			};
			var pendingLegacyContextWarning = /* @__PURE__ */ new Map();
			var didWarnAboutLegacyContext = /* @__PURE__ */ new Set();
			ReactStrictModeWarnings.recordLegacyContextWarning = function(fiber, instance) {
				var strictRoot = findStrictRoot(fiber);
				if (strictRoot === null) {
					error("Expected to find a StrictMode component in a strict mode tree. This error is likely caused by a bug in React. Please file an issue.");
					return;
				}
				if (didWarnAboutLegacyContext.has(fiber.type)) return;
				var warningsForRoot = pendingLegacyContextWarning.get(strictRoot);
				if (fiber.type.contextTypes != null || fiber.type.childContextTypes != null || instance !== null && typeof instance.getChildContext === "function") {
					if (warningsForRoot === void 0) {
						warningsForRoot = [];
						pendingLegacyContextWarning.set(strictRoot, warningsForRoot);
					}
					warningsForRoot.push(fiber);
				}
			};
			ReactStrictModeWarnings.flushLegacyContextWarning = function() {
				pendingLegacyContextWarning.forEach(function(fiberArray, strictRoot) {
					if (fiberArray.length === 0) return;
					var firstFiber = fiberArray[0];
					var uniqueNames = /* @__PURE__ */ new Set();
					fiberArray.forEach(function(fiber) {
						uniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutLegacyContext.add(fiber.type);
					});
					var sortedNames = setToSortedString(uniqueNames);
					try {
						setCurrentFiber(firstFiber);
						error("Legacy context API has been detected within a strict-mode tree.\n\nThe old API will be supported in all 16.x releases, but applications using it should migrate to the new version.\n\nPlease update the following components: %s\n\nLearn more about this warning here: https://reactjs.org/link/legacy-context", sortedNames);
					} finally {
						resetCurrentFiber();
					}
				});
			};
			ReactStrictModeWarnings.discardPendingWarnings = function() {
				pendingComponentWillMountWarnings = [];
				pendingUNSAFE_ComponentWillMountWarnings = [];
				pendingComponentWillReceivePropsWarnings = [];
				pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
				pendingComponentWillUpdateWarnings = [];
				pendingUNSAFE_ComponentWillUpdateWarnings = [];
				pendingLegacyContextWarning = /* @__PURE__ */ new Map();
			};
			var didWarnAboutMaps;
			var didWarnAboutGenerators;
			var didWarnAboutStringRefs;
			var ownerHasKeyUseWarning;
			var ownerHasFunctionTypeWarning;
			var warnForMissingKey = function(child, returnFiber) {};
			didWarnAboutMaps = false;
			didWarnAboutGenerators = false;
			didWarnAboutStringRefs = {};
			/**
			* Warn if there's no key explicitly set on dynamic arrays of children or
			* object keys are not valid. This allows us to keep track of children between
			* updates.
			*/
			ownerHasKeyUseWarning = {};
			ownerHasFunctionTypeWarning = {};
			warnForMissingKey = function(child, returnFiber) {
				if (child === null || typeof child !== "object") return;
				if (!child._store || child._store.validated || child.key != null) return;
				if (typeof child._store !== "object") throw new Error("React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.");
				child._store.validated = true;
				var componentName = getComponentNameFromFiber(returnFiber) || "Component";
				if (ownerHasKeyUseWarning[componentName]) return;
				ownerHasKeyUseWarning[componentName] = true;
				error("Each child in a list should have a unique \"key\" prop. See https://reactjs.org/link/warning-keys for more information.");
			};
			function isReactClass(type) {
				return type.prototype && type.prototype.isReactComponent;
			}
			function coerceRef(returnFiber, current, element) {
				var mixedRef = element.ref;
				if (mixedRef !== null && typeof mixedRef !== "function" && typeof mixedRef !== "object") {
					if ((returnFiber.mode & StrictLegacyMode || warnAboutStringRefs) && !(element._owner && element._self && element._owner.stateNode !== element._self) && !(element._owner && element._owner.tag !== ClassComponent) && !(typeof element.type === "function" && !isReactClass(element.type)) && element._owner) {
						var componentName = getComponentNameFromFiber(returnFiber) || "Component";
						if (!didWarnAboutStringRefs[componentName]) {
							error("Component \"%s\" contains the string ref \"%s\". Support for string refs will be removed in a future major release. We recommend using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref", componentName, mixedRef);
							didWarnAboutStringRefs[componentName] = true;
						}
					}
					if (element._owner) {
						var owner = element._owner;
						var inst;
						if (owner) {
							var ownerFiber = owner;
							if (ownerFiber.tag !== ClassComponent) throw new Error("Function components cannot have string refs. We recommend using useRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref");
							inst = ownerFiber.stateNode;
						}
						if (!inst) throw new Error("Missing owner for string ref " + mixedRef + ". This error is likely caused by a bug in React. Please file an issue.");
						var resolvedInst = inst;
						checkPropStringCoercion(mixedRef, "ref");
						var stringRef = "" + mixedRef;
						if (current !== null && current.ref !== null && typeof current.ref === "function" && current.ref._stringRef === stringRef) return current.ref;
						var ref = function(value) {
							var refs = resolvedInst.refs;
							if (value === null) delete refs[stringRef];
							else refs[stringRef] = value;
						};
						ref._stringRef = stringRef;
						return ref;
					} else {
						if (typeof mixedRef !== "string") throw new Error("Expected ref to be a function, a string, an object returned by React.createRef(), or null.");
						if (!element._owner) throw new Error("Element ref was specified as a string (" + mixedRef + ") but no owner was set. This could happen for one of the following reasons:\n1. You may be adding a ref to a function component\n2. You may be adding a ref to a component that was not created inside a component's render method\n3. You have multiple copies of React loaded\nSee https://reactjs.org/link/refs-must-have-owner for more information.");
					}
				}
				return mixedRef;
			}
			function throwOnInvalidObjectType(returnFiber, newChild) {
				var childString = Object.prototype.toString.call(newChild);
				throw new Error("Objects are not valid as a React child (found: " + (childString === "[object Object]" ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : childString) + "). If you meant to render a collection of children, use an array instead.");
			}
			function warnOnFunctionType(returnFiber) {
				var componentName = getComponentNameFromFiber(returnFiber) || "Component";
				if (ownerHasFunctionTypeWarning[componentName]) return;
				ownerHasFunctionTypeWarning[componentName] = true;
				error("Functions are not valid as a React child. This may happen if you return a Component instead of <Component /> from render. Or maybe you meant to call this function rather than return it.");
			}
			function resolveLazy(lazyType) {
				var payload = lazyType._payload;
				var init = lazyType._init;
				return init(payload);
			}
			function ChildReconciler(shouldTrackSideEffects) {
				function deleteChild(returnFiber, childToDelete) {
					if (!shouldTrackSideEffects) return;
					var deletions = returnFiber.deletions;
					if (deletions === null) {
						returnFiber.deletions = [childToDelete];
						returnFiber.flags |= ChildDeletion;
					} else deletions.push(childToDelete);
				}
				function deleteRemainingChildren(returnFiber, currentFirstChild) {
					if (!shouldTrackSideEffects) return null;
					var childToDelete = currentFirstChild;
					while (childToDelete !== null) {
						deleteChild(returnFiber, childToDelete);
						childToDelete = childToDelete.sibling;
					}
					return null;
				}
				function mapRemainingChildren(returnFiber, currentFirstChild) {
					var existingChildren = /* @__PURE__ */ new Map();
					var existingChild = currentFirstChild;
					while (existingChild !== null) {
						if (existingChild.key !== null) existingChildren.set(existingChild.key, existingChild);
						else existingChildren.set(existingChild.index, existingChild);
						existingChild = existingChild.sibling;
					}
					return existingChildren;
				}
				function useFiber(fiber, pendingProps) {
					var clone = createWorkInProgress(fiber, pendingProps);
					clone.index = 0;
					clone.sibling = null;
					return clone;
				}
				function placeChild(newFiber, lastPlacedIndex, newIndex) {
					newFiber.index = newIndex;
					if (!shouldTrackSideEffects) {
						newFiber.flags |= Forked;
						return lastPlacedIndex;
					}
					var current = newFiber.alternate;
					if (current !== null) {
						var oldIndex = current.index;
						if (oldIndex < lastPlacedIndex) {
							newFiber.flags |= Placement;
							return lastPlacedIndex;
						} else return oldIndex;
					} else {
						newFiber.flags |= Placement;
						return lastPlacedIndex;
					}
				}
				function placeSingleChild(newFiber) {
					if (shouldTrackSideEffects && newFiber.alternate === null) newFiber.flags |= Placement;
					return newFiber;
				}
				function updateTextNode(returnFiber, current, textContent, lanes) {
					if (current === null || current.tag !== HostText) {
						var created = createFiberFromText(textContent, returnFiber.mode, lanes);
						created.return = returnFiber;
						return created;
					} else {
						var existing = useFiber(current, textContent);
						existing.return = returnFiber;
						return existing;
					}
				}
				function updateElement(returnFiber, current, element, lanes) {
					var elementType = element.type;
					if (elementType === REACT_FRAGMENT_TYPE) return updateFragment(returnFiber, current, element.props.children, lanes, element.key);
					if (current !== null) {
						if (current.elementType === elementType || isCompatibleFamilyForHotReloading(current, element) || typeof elementType === "object" && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current.type) {
							var existing = useFiber(current, element.props);
							existing.ref = coerceRef(returnFiber, current, element);
							existing.return = returnFiber;
							existing._debugSource = element._source;
							existing._debugOwner = element._owner;
							return existing;
						}
					}
					var created = createFiberFromElement(element, returnFiber.mode, lanes);
					created.ref = coerceRef(returnFiber, current, element);
					created.return = returnFiber;
					return created;
				}
				function updatePortal(returnFiber, current, portal, lanes) {
					if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
						var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
						created.return = returnFiber;
						return created;
					} else {
						var existing = useFiber(current, portal.children || []);
						existing.return = returnFiber;
						return existing;
					}
				}
				function updateFragment(returnFiber, current, fragment, lanes, key) {
					if (current === null || current.tag !== Fragment) {
						var created = createFiberFromFragment(fragment, returnFiber.mode, lanes, key);
						created.return = returnFiber;
						return created;
					} else {
						var existing = useFiber(current, fragment);
						existing.return = returnFiber;
						return existing;
					}
				}
				function createChild(returnFiber, newChild, lanes) {
					if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
						var created = createFiberFromText("" + newChild, returnFiber.mode, lanes);
						created.return = returnFiber;
						return created;
					}
					if (typeof newChild === "object" && newChild !== null) {
						switch (newChild.$$typeof) {
							case REACT_ELEMENT_TYPE:
								var _created = createFiberFromElement(newChild, returnFiber.mode, lanes);
								_created.ref = coerceRef(returnFiber, null, newChild);
								_created.return = returnFiber;
								return _created;
							case REACT_PORTAL_TYPE:
								var _created2 = createFiberFromPortal(newChild, returnFiber.mode, lanes);
								_created2.return = returnFiber;
								return _created2;
							case REACT_LAZY_TYPE:
								var payload = newChild._payload;
								var init = newChild._init;
								return createChild(returnFiber, init(payload), lanes);
						}
						if (isArray(newChild) || getIteratorFn(newChild)) {
							var _created3 = createFiberFromFragment(newChild, returnFiber.mode, lanes, null);
							_created3.return = returnFiber;
							return _created3;
						}
						throwOnInvalidObjectType(returnFiber, newChild);
					}
					if (typeof newChild === "function") warnOnFunctionType(returnFiber);
					return null;
				}
				function updateSlot(returnFiber, oldFiber, newChild, lanes) {
					var key = oldFiber !== null ? oldFiber.key : null;
					if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") {
						if (key !== null) return null;
						return updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
					}
					if (typeof newChild === "object" && newChild !== null) {
						switch (newChild.$$typeof) {
							case REACT_ELEMENT_TYPE: if (newChild.key === key) return updateElement(returnFiber, oldFiber, newChild, lanes);
							else return null;
							case REACT_PORTAL_TYPE: if (newChild.key === key) return updatePortal(returnFiber, oldFiber, newChild, lanes);
							else return null;
							case REACT_LAZY_TYPE:
								var payload = newChild._payload;
								var init = newChild._init;
								return updateSlot(returnFiber, oldFiber, init(payload), lanes);
						}
						if (isArray(newChild) || getIteratorFn(newChild)) {
							if (key !== null) return null;
							return updateFragment(returnFiber, oldFiber, newChild, lanes, null);
						}
						throwOnInvalidObjectType(returnFiber, newChild);
					}
					if (typeof newChild === "function") warnOnFunctionType(returnFiber);
					return null;
				}
				function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
					if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") return updateTextNode(returnFiber, existingChildren.get(newIdx) || null, "" + newChild, lanes);
					if (typeof newChild === "object" && newChild !== null) {
						switch (newChild.$$typeof) {
							case REACT_ELEMENT_TYPE: return updateElement(returnFiber, existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null, newChild, lanes);
							case REACT_PORTAL_TYPE: return updatePortal(returnFiber, existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null, newChild, lanes);
							case REACT_LAZY_TYPE:
								var payload = newChild._payload;
								var init = newChild._init;
								return updateFromMap(existingChildren, returnFiber, newIdx, init(payload), lanes);
						}
						if (isArray(newChild) || getIteratorFn(newChild)) return updateFragment(returnFiber, existingChildren.get(newIdx) || null, newChild, lanes, null);
						throwOnInvalidObjectType(returnFiber, newChild);
					}
					if (typeof newChild === "function") warnOnFunctionType(returnFiber);
					return null;
				}
				/**
				* Warns if there is a duplicate or missing key
				*/
				function warnOnInvalidKey(child, knownKeys, returnFiber) {
					if (typeof child !== "object" || child === null) return knownKeys;
					switch (child.$$typeof) {
						case REACT_ELEMENT_TYPE:
						case REACT_PORTAL_TYPE:
							warnForMissingKey(child, returnFiber);
							var key = child.key;
							if (typeof key !== "string") break;
							if (knownKeys === null) {
								knownKeys = /* @__PURE__ */ new Set();
								knownKeys.add(key);
								break;
							}
							if (!knownKeys.has(key)) {
								knownKeys.add(key);
								break;
							}
							error("Encountered two children with the same key, `%s`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.", key);
							break;
						case REACT_LAZY_TYPE:
							var payload = child._payload;
							var init = child._init;
							warnOnInvalidKey(init(payload), knownKeys, returnFiber);
					}
					return knownKeys;
				}
				function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
					var knownKeys = null;
					for (var i = 0; i < newChildren.length; i++) {
						var child = newChildren[i];
						knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
					}
					var resultingFirstChild = null;
					var previousNewFiber = null;
					var oldFiber = currentFirstChild;
					var lastPlacedIndex = 0;
					var newIdx = 0;
					var nextOldFiber = null;
					for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
						if (oldFiber.index > newIdx) {
							nextOldFiber = oldFiber;
							oldFiber = null;
						} else nextOldFiber = oldFiber.sibling;
						var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], lanes);
						if (newFiber === null) {
							if (oldFiber === null) oldFiber = nextOldFiber;
							break;
						}
						if (shouldTrackSideEffects) {
							if (oldFiber && newFiber.alternate === null) deleteChild(returnFiber, oldFiber);
						}
						lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
						if (previousNewFiber === null) resultingFirstChild = newFiber;
						else previousNewFiber.sibling = newFiber;
						previousNewFiber = newFiber;
						oldFiber = nextOldFiber;
					}
					if (newIdx === newChildren.length) {
						deleteRemainingChildren(returnFiber, oldFiber);
						if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
						return resultingFirstChild;
					}
					if (oldFiber === null) {
						for (; newIdx < newChildren.length; newIdx++) {
							var _newFiber = createChild(returnFiber, newChildren[newIdx], lanes);
							if (_newFiber === null) continue;
							lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
							if (previousNewFiber === null) resultingFirstChild = _newFiber;
							else previousNewFiber.sibling = _newFiber;
							previousNewFiber = _newFiber;
						}
						if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
						return resultingFirstChild;
					}
					var existingChildren = mapRemainingChildren(returnFiber, oldFiber);
					for (; newIdx < newChildren.length; newIdx++) {
						var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], lanes);
						if (_newFiber2 !== null) {
							if (shouldTrackSideEffects) {
								if (_newFiber2.alternate !== null) existingChildren.delete(_newFiber2.key === null ? newIdx : _newFiber2.key);
							}
							lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
							if (previousNewFiber === null) resultingFirstChild = _newFiber2;
							else previousNewFiber.sibling = _newFiber2;
							previousNewFiber = _newFiber2;
						}
					}
					if (shouldTrackSideEffects) existingChildren.forEach(function(child) {
						return deleteChild(returnFiber, child);
					});
					if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
					return resultingFirstChild;
				}
				function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, lanes) {
					var iteratorFn = getIteratorFn(newChildrenIterable);
					if (typeof iteratorFn !== "function") throw new Error("An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.");
					if (typeof Symbol === "function" && newChildrenIterable[Symbol.toStringTag] === "Generator") {
						if (!didWarnAboutGenerators) error("Using Generators as children is unsupported and will likely yield unexpected results because enumerating a generator mutates it. You may convert it to an array with `Array.from()` or the `[...spread]` operator before rendering. Keep in mind you might need to polyfill these features for older browsers.");
						didWarnAboutGenerators = true;
					}
					if (newChildrenIterable.entries === iteratorFn) {
						if (!didWarnAboutMaps) error("Using Maps as children is not supported. Use an array of keyed ReactElements instead.");
						didWarnAboutMaps = true;
					}
					var _newChildren = iteratorFn.call(newChildrenIterable);
					if (_newChildren) {
						var knownKeys = null;
						var _step = _newChildren.next();
						for (; !_step.done; _step = _newChildren.next()) {
							var child = _step.value;
							knownKeys = warnOnInvalidKey(child, knownKeys, returnFiber);
						}
					}
					var newChildren = iteratorFn.call(newChildrenIterable);
					if (newChildren == null) throw new Error("An iterable object provided no iterator.");
					var resultingFirstChild = null;
					var previousNewFiber = null;
					var oldFiber = currentFirstChild;
					var lastPlacedIndex = 0;
					var newIdx = 0;
					var nextOldFiber = null;
					var step = newChildren.next();
					for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
						if (oldFiber.index > newIdx) {
							nextOldFiber = oldFiber;
							oldFiber = null;
						} else nextOldFiber = oldFiber.sibling;
						var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
						if (newFiber === null) {
							if (oldFiber === null) oldFiber = nextOldFiber;
							break;
						}
						if (shouldTrackSideEffects) {
							if (oldFiber && newFiber.alternate === null) deleteChild(returnFiber, oldFiber);
						}
						lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
						if (previousNewFiber === null) resultingFirstChild = newFiber;
						else previousNewFiber.sibling = newFiber;
						previousNewFiber = newFiber;
						oldFiber = nextOldFiber;
					}
					if (step.done) {
						deleteRemainingChildren(returnFiber, oldFiber);
						if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
						return resultingFirstChild;
					}
					if (oldFiber === null) {
						for (; !step.done; newIdx++, step = newChildren.next()) {
							var _newFiber3 = createChild(returnFiber, step.value, lanes);
							if (_newFiber3 === null) continue;
							lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
							if (previousNewFiber === null) resultingFirstChild = _newFiber3;
							else previousNewFiber.sibling = _newFiber3;
							previousNewFiber = _newFiber3;
						}
						if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
						return resultingFirstChild;
					}
					var existingChildren = mapRemainingChildren(returnFiber, oldFiber);
					for (; !step.done; newIdx++, step = newChildren.next()) {
						var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, lanes);
						if (_newFiber4 !== null) {
							if (shouldTrackSideEffects) {
								if (_newFiber4.alternate !== null) existingChildren.delete(_newFiber4.key === null ? newIdx : _newFiber4.key);
							}
							lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
							if (previousNewFiber === null) resultingFirstChild = _newFiber4;
							else previousNewFiber.sibling = _newFiber4;
							previousNewFiber = _newFiber4;
						}
					}
					if (shouldTrackSideEffects) existingChildren.forEach(function(child) {
						return deleteChild(returnFiber, child);
					});
					if (getIsHydrating()) pushTreeFork(returnFiber, newIdx);
					return resultingFirstChild;
				}
				function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, lanes) {
					if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
						deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
						var existing = useFiber(currentFirstChild, textContent);
						existing.return = returnFiber;
						return existing;
					}
					deleteRemainingChildren(returnFiber, currentFirstChild);
					var created = createFiberFromText(textContent, returnFiber.mode, lanes);
					created.return = returnFiber;
					return created;
				}
				function reconcileSingleElement(returnFiber, currentFirstChild, element, lanes) {
					var key = element.key;
					var child = currentFirstChild;
					while (child !== null) {
						if (child.key === key) {
							var elementType = element.type;
							if (elementType === REACT_FRAGMENT_TYPE) {
								if (child.tag === Fragment) {
									deleteRemainingChildren(returnFiber, child.sibling);
									var existing = useFiber(child, element.props.children);
									existing.return = returnFiber;
									existing._debugSource = element._source;
									existing._debugOwner = element._owner;
									return existing;
								}
							} else if (child.elementType === elementType || isCompatibleFamilyForHotReloading(child, element) || typeof elementType === "object" && elementType !== null && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === child.type) {
								deleteRemainingChildren(returnFiber, child.sibling);
								var _existing = useFiber(child, element.props);
								_existing.ref = coerceRef(returnFiber, child, element);
								_existing.return = returnFiber;
								_existing._debugSource = element._source;
								_existing._debugOwner = element._owner;
								return _existing;
							}
							deleteRemainingChildren(returnFiber, child);
							break;
						} else deleteChild(returnFiber, child);
						child = child.sibling;
					}
					if (element.type === REACT_FRAGMENT_TYPE) {
						var created = createFiberFromFragment(element.props.children, returnFiber.mode, lanes, element.key);
						created.return = returnFiber;
						return created;
					} else {
						var _created4 = createFiberFromElement(element, returnFiber.mode, lanes);
						_created4.ref = coerceRef(returnFiber, currentFirstChild, element);
						_created4.return = returnFiber;
						return _created4;
					}
				}
				function reconcileSinglePortal(returnFiber, currentFirstChild, portal, lanes) {
					var key = portal.key;
					var child = currentFirstChild;
					while (child !== null) {
						if (child.key === key) {
							if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
								deleteRemainingChildren(returnFiber, child.sibling);
								var existing = useFiber(child, portal.children || []);
								existing.return = returnFiber;
								return existing;
							} else {
								deleteRemainingChildren(returnFiber, child);
								break;
							}
						} else deleteChild(returnFiber, child);
						child = child.sibling;
					}
					var created = createFiberFromPortal(portal, returnFiber.mode, lanes);
					created.return = returnFiber;
					return created;
				}
				function reconcileChildFibers(returnFiber, currentFirstChild, newChild, lanes) {
					if (typeof newChild === "object" && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null) newChild = newChild.props.children;
					if (typeof newChild === "object" && newChild !== null) {
						switch (newChild.$$typeof) {
							case REACT_ELEMENT_TYPE: return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, lanes));
							case REACT_PORTAL_TYPE: return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, lanes));
							case REACT_LAZY_TYPE:
								var payload = newChild._payload;
								var init = newChild._init;
								return reconcileChildFibers(returnFiber, currentFirstChild, init(payload), lanes);
						}
						if (isArray(newChild)) return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, lanes);
						if (getIteratorFn(newChild)) return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, lanes);
						throwOnInvalidObjectType(returnFiber, newChild);
					}
					if (typeof newChild === "string" && newChild !== "" || typeof newChild === "number") return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, "" + newChild, lanes));
					if (typeof newChild === "function") warnOnFunctionType(returnFiber);
					return deleteRemainingChildren(returnFiber, currentFirstChild);
				}
				return reconcileChildFibers;
			}
			var reconcileChildFibers = ChildReconciler(true);
			var mountChildFibers = ChildReconciler(false);
			function cloneChildFibers(current, workInProgress) {
				if (current !== null && workInProgress.child !== current.child) throw new Error("Resuming work not yet implemented.");
				if (workInProgress.child === null) return;
				var currentChild = workInProgress.child;
				var newChild = createWorkInProgress(currentChild, currentChild.pendingProps);
				workInProgress.child = newChild;
				newChild.return = workInProgress;
				while (currentChild.sibling !== null) {
					currentChild = currentChild.sibling;
					newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps);
					newChild.return = workInProgress;
				}
				newChild.sibling = null;
			}
			function resetChildFibers(workInProgress, lanes) {
				var child = workInProgress.child;
				while (child !== null) {
					resetWorkInProgress(child, lanes);
					child = child.sibling;
				}
			}
			var valueCursor = createCursor(null);
			var rendererSigil = {};
			var currentlyRenderingFiber = null;
			var lastContextDependency = null;
			var lastFullyObservedContext = null;
			var isDisallowedContextReadInDEV = false;
			function resetContextDependencies() {
				currentlyRenderingFiber = null;
				lastContextDependency = null;
				lastFullyObservedContext = null;
				isDisallowedContextReadInDEV = false;
			}
			function enterDisallowedContextReadInDEV() {
				isDisallowedContextReadInDEV = true;
			}
			function exitDisallowedContextReadInDEV() {
				isDisallowedContextReadInDEV = false;
			}
			function pushProvider(providerFiber, context, nextValue) {
				push(valueCursor, context._currentValue, providerFiber);
				context._currentValue = nextValue;
				if (context._currentRenderer !== void 0 && context._currentRenderer !== null && context._currentRenderer !== rendererSigil) error("Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported.");
				context._currentRenderer = rendererSigil;
			}
			function popProvider(context, providerFiber) {
				var currentValue = valueCursor.current;
				pop(valueCursor, providerFiber);
				context._currentValue = currentValue;
			}
			function scheduleContextWorkOnParentPath(parent, renderLanes, propagationRoot) {
				var node = parent;
				while (node !== null) {
					var alternate = node.alternate;
					if (!isSubsetOfLanes(node.childLanes, renderLanes)) {
						node.childLanes = mergeLanes(node.childLanes, renderLanes);
						if (alternate !== null) alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes);
					} else if (alternate !== null && !isSubsetOfLanes(alternate.childLanes, renderLanes)) alternate.childLanes = mergeLanes(alternate.childLanes, renderLanes);
					if (node === propagationRoot) break;
					node = node.return;
				}
				if (node !== propagationRoot) error("Expected to find the propagation root when scheduling context work. This error is likely caused by a bug in React. Please file an issue.");
			}
			function propagateContextChange(workInProgress, context, renderLanes) {
				propagateContextChange_eager(workInProgress, context, renderLanes);
			}
			function propagateContextChange_eager(workInProgress, context, renderLanes) {
				var fiber = workInProgress.child;
				if (fiber !== null) fiber.return = workInProgress;
				while (fiber !== null) {
					var nextFiber = void 0;
					var list = fiber.dependencies;
					if (list !== null) {
						nextFiber = fiber.child;
						var dependency = list.firstContext;
						while (dependency !== null) {
							if (dependency.context === context) {
								if (fiber.tag === ClassComponent) {
									var update = createUpdate(NoTimestamp, pickArbitraryLane(renderLanes));
									update.tag = ForceUpdate;
									var updateQueue = fiber.updateQueue;
									if (updateQueue === null);
									else {
										var sharedQueue = updateQueue.shared;
										var pending = sharedQueue.pending;
										if (pending === null) update.next = update;
										else {
											update.next = pending.next;
											pending.next = update;
										}
										sharedQueue.pending = update;
									}
								}
								fiber.lanes = mergeLanes(fiber.lanes, renderLanes);
								var alternate = fiber.alternate;
								if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, renderLanes);
								scheduleContextWorkOnParentPath(fiber.return, renderLanes, workInProgress);
								list.lanes = mergeLanes(list.lanes, renderLanes);
								break;
							}
							dependency = dependency.next;
						}
					} else if (fiber.tag === ContextProvider) nextFiber = fiber.type === workInProgress.type ? null : fiber.child;
					else if (fiber.tag === DehydratedFragment) {
						var parentSuspense = fiber.return;
						if (parentSuspense === null) throw new Error("We just came from a parent so we must have had a parent. This is a bug in React.");
						parentSuspense.lanes = mergeLanes(parentSuspense.lanes, renderLanes);
						var _alternate = parentSuspense.alternate;
						if (_alternate !== null) _alternate.lanes = mergeLanes(_alternate.lanes, renderLanes);
						scheduleContextWorkOnParentPath(parentSuspense, renderLanes, workInProgress);
						nextFiber = fiber.sibling;
					} else nextFiber = fiber.child;
					if (nextFiber !== null) nextFiber.return = fiber;
					else {
						nextFiber = fiber;
						while (nextFiber !== null) {
							if (nextFiber === workInProgress) {
								nextFiber = null;
								break;
							}
							var sibling = nextFiber.sibling;
							if (sibling !== null) {
								sibling.return = nextFiber.return;
								nextFiber = sibling;
								break;
							}
							nextFiber = nextFiber.return;
						}
					}
					fiber = nextFiber;
				}
			}
			function prepareToReadContext(workInProgress, renderLanes) {
				currentlyRenderingFiber = workInProgress;
				lastContextDependency = null;
				lastFullyObservedContext = null;
				var dependencies = workInProgress.dependencies;
				if (dependencies !== null) {
					if (dependencies.firstContext !== null) {
						if (includesSomeLane(dependencies.lanes, renderLanes)) markWorkInProgressReceivedUpdate();
						dependencies.firstContext = null;
					}
				}
			}
			function readContext(context) {
				if (isDisallowedContextReadInDEV) error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
				var value = context._currentValue;
				if (lastFullyObservedContext === context);
				else {
					var contextItem = {
						context,
						memoizedValue: value,
						next: null
					};
					if (lastContextDependency === null) {
						if (currentlyRenderingFiber === null) throw new Error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
						lastContextDependency = contextItem;
						currentlyRenderingFiber.dependencies = {
							lanes: NoLanes,
							firstContext: contextItem
						};
					} else lastContextDependency = lastContextDependency.next = contextItem;
				}
				return value;
			}
			var concurrentQueues = null;
			function pushConcurrentUpdateQueue(queue) {
				if (concurrentQueues === null) concurrentQueues = [queue];
				else concurrentQueues.push(queue);
			}
			function finishQueueingConcurrentUpdates() {
				if (concurrentQueues !== null) {
					for (var i = 0; i < concurrentQueues.length; i++) {
						var queue = concurrentQueues[i];
						var lastInterleavedUpdate = queue.interleaved;
						if (lastInterleavedUpdate !== null) {
							queue.interleaved = null;
							var firstInterleavedUpdate = lastInterleavedUpdate.next;
							var lastPendingUpdate = queue.pending;
							if (lastPendingUpdate !== null) {
								var firstPendingUpdate = lastPendingUpdate.next;
								lastPendingUpdate.next = firstInterleavedUpdate;
								lastInterleavedUpdate.next = firstPendingUpdate;
							}
							queue.pending = lastInterleavedUpdate;
						}
					}
					concurrentQueues = null;
				}
			}
			function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
				var interleaved = queue.interleaved;
				if (interleaved === null) {
					update.next = update;
					pushConcurrentUpdateQueue(queue);
				} else {
					update.next = interleaved.next;
					interleaved.next = update;
				}
				queue.interleaved = update;
				return markUpdateLaneFromFiberToRoot(fiber, lane);
			}
			function enqueueConcurrentHookUpdateAndEagerlyBailout(fiber, queue, update, lane) {
				var interleaved = queue.interleaved;
				if (interleaved === null) {
					update.next = update;
					pushConcurrentUpdateQueue(queue);
				} else {
					update.next = interleaved.next;
					interleaved.next = update;
				}
				queue.interleaved = update;
			}
			function enqueueConcurrentClassUpdate(fiber, queue, update, lane) {
				var interleaved = queue.interleaved;
				if (interleaved === null) {
					update.next = update;
					pushConcurrentUpdateQueue(queue);
				} else {
					update.next = interleaved.next;
					interleaved.next = update;
				}
				queue.interleaved = update;
				return markUpdateLaneFromFiberToRoot(fiber, lane);
			}
			function enqueueConcurrentRenderForLane(fiber, lane) {
				return markUpdateLaneFromFiberToRoot(fiber, lane);
			}
			var unsafe_markUpdateLaneFromFiberToRoot = markUpdateLaneFromFiberToRoot;
			function markUpdateLaneFromFiberToRoot(sourceFiber, lane) {
				sourceFiber.lanes = mergeLanes(sourceFiber.lanes, lane);
				var alternate = sourceFiber.alternate;
				if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, lane);
				if (alternate === null && (sourceFiber.flags & (Placement | Hydrating)) !== NoFlags) warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
				var node = sourceFiber;
				var parent = sourceFiber.return;
				while (parent !== null) {
					parent.childLanes = mergeLanes(parent.childLanes, lane);
					alternate = parent.alternate;
					if (alternate !== null) alternate.childLanes = mergeLanes(alternate.childLanes, lane);
					else if ((parent.flags & (Placement | Hydrating)) !== NoFlags) warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
					node = parent;
					parent = parent.return;
				}
				if (node.tag === HostRoot) return node.stateNode;
				else return null;
			}
			var UpdateState = 0;
			var ReplaceState = 1;
			var ForceUpdate = 2;
			var CaptureUpdate = 3;
			var hasForceUpdate = false;
			var didWarnUpdateInsideUpdate;
			var currentlyProcessingQueue;
			didWarnUpdateInsideUpdate = false;
			currentlyProcessingQueue = null;
			function initializeUpdateQueue(fiber) {
				fiber.updateQueue = {
					baseState: fiber.memoizedState,
					firstBaseUpdate: null,
					lastBaseUpdate: null,
					shared: {
						pending: null,
						interleaved: null,
						lanes: NoLanes
					},
					effects: null
				};
			}
			function cloneUpdateQueue(current, workInProgress) {
				var queue = workInProgress.updateQueue;
				var currentQueue = current.updateQueue;
				if (queue === currentQueue) workInProgress.updateQueue = {
					baseState: currentQueue.baseState,
					firstBaseUpdate: currentQueue.firstBaseUpdate,
					lastBaseUpdate: currentQueue.lastBaseUpdate,
					shared: currentQueue.shared,
					effects: currentQueue.effects
				};
			}
			function createUpdate(eventTime, lane) {
				return {
					eventTime,
					lane,
					tag: UpdateState,
					payload: null,
					callback: null,
					next: null
				};
			}
			function enqueueUpdate(fiber, update, lane) {
				var updateQueue = fiber.updateQueue;
				if (updateQueue === null) return null;
				var sharedQueue = updateQueue.shared;
				if (currentlyProcessingQueue === sharedQueue && !didWarnUpdateInsideUpdate) {
					error("An update (setState, replaceState, or forceUpdate) was scheduled from inside an update function. Update functions should be pure, with zero side-effects. Consider using componentDidUpdate or a callback.");
					didWarnUpdateInsideUpdate = true;
				}
				if (isUnsafeClassRenderPhaseUpdate()) {
					var pending = sharedQueue.pending;
					if (pending === null) update.next = update;
					else {
						update.next = pending.next;
						pending.next = update;
					}
					sharedQueue.pending = update;
					return unsafe_markUpdateLaneFromFiberToRoot(fiber, lane);
				} else return enqueueConcurrentClassUpdate(fiber, sharedQueue, update, lane);
			}
			function entangleTransitions(root, fiber, lane) {
				var updateQueue = fiber.updateQueue;
				if (updateQueue === null) return;
				var sharedQueue = updateQueue.shared;
				if (isTransitionLane(lane)) {
					var queueLanes = sharedQueue.lanes;
					queueLanes = intersectLanes(queueLanes, root.pendingLanes);
					var newQueueLanes = mergeLanes(queueLanes, lane);
					sharedQueue.lanes = newQueueLanes;
					markRootEntangled(root, newQueueLanes);
				}
			}
			function enqueueCapturedUpdate(workInProgress, capturedUpdate) {
				var queue = workInProgress.updateQueue;
				var current = workInProgress.alternate;
				if (current !== null) {
					var currentQueue = current.updateQueue;
					if (queue === currentQueue) {
						var newFirst = null;
						var newLast = null;
						var firstBaseUpdate = queue.firstBaseUpdate;
						if (firstBaseUpdate !== null) {
							var update = firstBaseUpdate;
							do {
								var clone = {
									eventTime: update.eventTime,
									lane: update.lane,
									tag: update.tag,
									payload: update.payload,
									callback: update.callback,
									next: null
								};
								if (newLast === null) newFirst = newLast = clone;
								else {
									newLast.next = clone;
									newLast = clone;
								}
								update = update.next;
							} while (update !== null);
							if (newLast === null) newFirst = newLast = capturedUpdate;
							else {
								newLast.next = capturedUpdate;
								newLast = capturedUpdate;
							}
						} else newFirst = newLast = capturedUpdate;
						queue = {
							baseState: currentQueue.baseState,
							firstBaseUpdate: newFirst,
							lastBaseUpdate: newLast,
							shared: currentQueue.shared,
							effects: currentQueue.effects
						};
						workInProgress.updateQueue = queue;
						return;
					}
				}
				var lastBaseUpdate = queue.lastBaseUpdate;
				if (lastBaseUpdate === null) queue.firstBaseUpdate = capturedUpdate;
				else lastBaseUpdate.next = capturedUpdate;
				queue.lastBaseUpdate = capturedUpdate;
			}
			function getStateFromUpdate(workInProgress, queue, update, prevState, nextProps, instance) {
				switch (update.tag) {
					case ReplaceState:
						var payload = update.payload;
						if (typeof payload === "function") {
							enterDisallowedContextReadInDEV();
							var nextState = payload.call(instance, prevState, nextProps);
							if (workInProgress.mode & StrictLegacyMode) {
								setIsStrictModeForDevtools(true);
								try {
									payload.call(instance, prevState, nextProps);
								} finally {
									setIsStrictModeForDevtools(false);
								}
							}
							exitDisallowedContextReadInDEV();
							return nextState;
						}
						return payload;
					case CaptureUpdate: workInProgress.flags = workInProgress.flags & ~ShouldCapture | DidCapture;
					case UpdateState:
						var _payload = update.payload;
						var partialState;
						if (typeof _payload === "function") {
							enterDisallowedContextReadInDEV();
							partialState = _payload.call(instance, prevState, nextProps);
							if (workInProgress.mode & StrictLegacyMode) {
								setIsStrictModeForDevtools(true);
								try {
									_payload.call(instance, prevState, nextProps);
								} finally {
									setIsStrictModeForDevtools(false);
								}
							}
							exitDisallowedContextReadInDEV();
						} else partialState = _payload;
						if (partialState === null || partialState === void 0) return prevState;
						return assign({}, prevState, partialState);
					case ForceUpdate:
						hasForceUpdate = true;
						return prevState;
				}
				return prevState;
			}
			function processUpdateQueue(workInProgress, props, instance, renderLanes) {
				var queue = workInProgress.updateQueue;
				hasForceUpdate = false;
				currentlyProcessingQueue = queue.shared;
				var firstBaseUpdate = queue.firstBaseUpdate;
				var lastBaseUpdate = queue.lastBaseUpdate;
				var pendingQueue = queue.shared.pending;
				if (pendingQueue !== null) {
					queue.shared.pending = null;
					var lastPendingUpdate = pendingQueue;
					var firstPendingUpdate = lastPendingUpdate.next;
					lastPendingUpdate.next = null;
					if (lastBaseUpdate === null) firstBaseUpdate = firstPendingUpdate;
					else lastBaseUpdate.next = firstPendingUpdate;
					lastBaseUpdate = lastPendingUpdate;
					var current = workInProgress.alternate;
					if (current !== null) {
						var currentQueue = current.updateQueue;
						var currentLastBaseUpdate = currentQueue.lastBaseUpdate;
						if (currentLastBaseUpdate !== lastBaseUpdate) {
							if (currentLastBaseUpdate === null) currentQueue.firstBaseUpdate = firstPendingUpdate;
							else currentLastBaseUpdate.next = firstPendingUpdate;
							currentQueue.lastBaseUpdate = lastPendingUpdate;
						}
					}
				}
				if (firstBaseUpdate !== null) {
					var newState = queue.baseState;
					var newLanes = NoLanes;
					var newBaseState = null;
					var newFirstBaseUpdate = null;
					var newLastBaseUpdate = null;
					var update = firstBaseUpdate;
					do {
						var updateLane = update.lane;
						var updateEventTime = update.eventTime;
						if (!isSubsetOfLanes(renderLanes, updateLane)) {
							var clone = {
								eventTime: updateEventTime,
								lane: updateLane,
								tag: update.tag,
								payload: update.payload,
								callback: update.callback,
								next: null
							};
							if (newLastBaseUpdate === null) {
								newFirstBaseUpdate = newLastBaseUpdate = clone;
								newBaseState = newState;
							} else newLastBaseUpdate = newLastBaseUpdate.next = clone;
							newLanes = mergeLanes(newLanes, updateLane);
						} else {
							if (newLastBaseUpdate !== null) {
								var _clone = {
									eventTime: updateEventTime,
									lane: NoLane,
									tag: update.tag,
									payload: update.payload,
									callback: update.callback,
									next: null
								};
								newLastBaseUpdate = newLastBaseUpdate.next = _clone;
							}
							newState = getStateFromUpdate(workInProgress, queue, update, newState, props, instance);
							if (update.callback !== null && update.lane !== NoLane) {
								workInProgress.flags |= Callback;
								var effects = queue.effects;
								if (effects === null) queue.effects = [update];
								else effects.push(update);
							}
						}
						update = update.next;
						if (update === null) {
							pendingQueue = queue.shared.pending;
							if (pendingQueue === null) break;
							else {
								var _lastPendingUpdate = pendingQueue;
								var _firstPendingUpdate = _lastPendingUpdate.next;
								_lastPendingUpdate.next = null;
								update = _firstPendingUpdate;
								queue.lastBaseUpdate = _lastPendingUpdate;
								queue.shared.pending = null;
							}
						}
					} while (true);
					if (newLastBaseUpdate === null) newBaseState = newState;
					queue.baseState = newBaseState;
					queue.firstBaseUpdate = newFirstBaseUpdate;
					queue.lastBaseUpdate = newLastBaseUpdate;
					var lastInterleaved = queue.shared.interleaved;
					if (lastInterleaved !== null) {
						var interleaved = lastInterleaved;
						do {
							newLanes = mergeLanes(newLanes, interleaved.lane);
							interleaved = interleaved.next;
						} while (interleaved !== lastInterleaved);
					} else if (firstBaseUpdate === null) queue.shared.lanes = NoLanes;
					markSkippedUpdateLanes(newLanes);
					workInProgress.lanes = newLanes;
					workInProgress.memoizedState = newState;
				}
				currentlyProcessingQueue = null;
			}
			function callCallback(callback, context) {
				if (typeof callback !== "function") throw new Error("Invalid argument passed as callback. Expected a function. Instead " + ("received: " + callback));
				callback.call(context);
			}
			function resetHasForceUpdateBeforeProcessing() {
				hasForceUpdate = false;
			}
			function checkHasForceUpdateAfterProcessing() {
				return hasForceUpdate;
			}
			function commitUpdateQueue(finishedWork, finishedQueue, instance) {
				var effects = finishedQueue.effects;
				finishedQueue.effects = null;
				if (effects !== null) for (var i = 0; i < effects.length; i++) {
					var effect = effects[i];
					var callback = effect.callback;
					if (callback !== null) {
						effect.callback = null;
						callCallback(callback, instance);
					}
				}
			}
			var NO_CONTEXT = {};
			var contextStackCursor$1 = createCursor(NO_CONTEXT);
			var contextFiberStackCursor = createCursor(NO_CONTEXT);
			var rootInstanceStackCursor = createCursor(NO_CONTEXT);
			function requiredContext(c) {
				if (c === NO_CONTEXT) throw new Error("Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.");
				return c;
			}
			function getRootHostContainer() {
				return requiredContext(rootInstanceStackCursor.current);
			}
			function pushHostContainer(fiber, nextRootInstance) {
				push(rootInstanceStackCursor, nextRootInstance, fiber);
				push(contextFiberStackCursor, fiber, fiber);
				push(contextStackCursor$1, NO_CONTEXT, fiber);
				var nextRootContext = getRootHostContext(nextRootInstance);
				pop(contextStackCursor$1, fiber);
				push(contextStackCursor$1, nextRootContext, fiber);
			}
			function popHostContainer(fiber) {
				pop(contextStackCursor$1, fiber);
				pop(contextFiberStackCursor, fiber);
				pop(rootInstanceStackCursor, fiber);
			}
			function getHostContext() {
				return requiredContext(contextStackCursor$1.current);
			}
			function pushHostContext(fiber) {
				requiredContext(rootInstanceStackCursor.current);
				var context = requiredContext(contextStackCursor$1.current);
				var nextContext = getChildHostContext(context, fiber.type);
				if (context === nextContext) return;
				push(contextFiberStackCursor, fiber, fiber);
				push(contextStackCursor$1, nextContext, fiber);
			}
			function popHostContext(fiber) {
				if (contextFiberStackCursor.current !== fiber) return;
				pop(contextStackCursor$1, fiber);
				pop(contextFiberStackCursor, fiber);
			}
			var DefaultSuspenseContext = 0;
			var SubtreeSuspenseContextMask = 1;
			var InvisibleParentSuspenseContext = 1;
			var ForceSuspenseFallback = 2;
			var suspenseStackCursor = createCursor(DefaultSuspenseContext);
			function hasSuspenseContext(parentContext, flag) {
				return (parentContext & flag) !== 0;
			}
			function setDefaultShallowSuspenseContext(parentContext) {
				return parentContext & SubtreeSuspenseContextMask;
			}
			function setShallowSuspenseContext(parentContext, shallowContext) {
				return parentContext & SubtreeSuspenseContextMask | shallowContext;
			}
			function addSubtreeSuspenseContext(parentContext, subtreeContext) {
				return parentContext | subtreeContext;
			}
			function pushSuspenseContext(fiber, newContext) {
				push(suspenseStackCursor, newContext, fiber);
			}
			function popSuspenseContext(fiber) {
				pop(suspenseStackCursor, fiber);
			}
			function shouldCaptureSuspense(workInProgress, hasInvisibleParent) {
				var nextState = workInProgress.memoizedState;
				if (nextState !== null) {
					if (nextState.dehydrated !== null) return true;
					return false;
				}
				workInProgress.memoizedProps;
				return true;
			}
			function findFirstSuspended(row) {
				var node = row;
				while (node !== null) {
					if (node.tag === SuspenseComponent) {
						var state = node.memoizedState;
						if (state !== null) {
							var dehydrated = state.dehydrated;
							if (dehydrated === null || isSuspenseInstancePending(dehydrated) || isSuspenseInstanceFallback(dehydrated)) return node;
						}
					} else if (node.tag === SuspenseListComponent && node.memoizedProps.revealOrder !== void 0) {
						if ((node.flags & DidCapture) !== NoFlags) return node;
					} else if (node.child !== null) {
						node.child.return = node;
						node = node.child;
						continue;
					}
					if (node === row) return null;
					while (node.sibling === null) {
						if (node.return === null || node.return === row) return null;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
				return null;
			}
			var NoFlags$1 = 0;
			var HasEffect = 1;
			var Insertion = 2;
			var Layout = 4;
			var Passive$1 = 8;
			var workInProgressSources = [];
			function resetWorkInProgressVersions() {
				for (var i = 0; i < workInProgressSources.length; i++) {
					var mutableSource = workInProgressSources[i];
					mutableSource._workInProgressVersionPrimary = null;
				}
				workInProgressSources.length = 0;
			}
			function registerMutableSourceForHydration(root, mutableSource) {
				var getVersion = mutableSource._getVersion;
				var version = getVersion(mutableSource._source);
				if (root.mutableSourceEagerHydrationData == null) root.mutableSourceEagerHydrationData = [mutableSource, version];
				else root.mutableSourceEagerHydrationData.push(mutableSource, version);
			}
			var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentBatchConfig$2 = ReactSharedInternals.ReactCurrentBatchConfig;
			var didWarnAboutMismatchedHooksForComponent;
			var didWarnUncachedGetSnapshot;
			didWarnAboutMismatchedHooksForComponent = /* @__PURE__ */ new Set();
			var renderLanes = NoLanes;
			var currentlyRenderingFiber$1 = null;
			var currentHook = null;
			var workInProgressHook = null;
			var didScheduleRenderPhaseUpdate = false;
			var didScheduleRenderPhaseUpdateDuringThisPass = false;
			var localIdCounter = 0;
			var globalClientIdCounter = 0;
			var RE_RENDER_LIMIT = 25;
			var currentHookNameInDev = null;
			var hookTypesDev = null;
			var hookTypesUpdateIndexDev = -1;
			var ignorePreviousDependencies = false;
			function mountHookTypesDev() {
				var hookName = currentHookNameInDev;
				if (hookTypesDev === null) hookTypesDev = [hookName];
				else hookTypesDev.push(hookName);
			}
			function updateHookTypesDev() {
				var hookName = currentHookNameInDev;
				if (hookTypesDev !== null) {
					hookTypesUpdateIndexDev++;
					if (hookTypesDev[hookTypesUpdateIndexDev] !== hookName) warnOnHookMismatchInDev(hookName);
				}
			}
			function checkDepsAreArrayDev(deps) {
				if (deps !== void 0 && deps !== null && !isArray(deps)) error("%s received a final argument that is not an array (instead, received `%s`). When specified, the final argument must be an array.", currentHookNameInDev, typeof deps);
			}
			function warnOnHookMismatchInDev(currentHookName) {
				var componentName = getComponentNameFromFiber(currentlyRenderingFiber$1);
				if (!didWarnAboutMismatchedHooksForComponent.has(componentName)) {
					didWarnAboutMismatchedHooksForComponent.add(componentName);
					if (hookTypesDev !== null) {
						var table = "";
						var secondColumnStart = 30;
						for (var i = 0; i <= hookTypesUpdateIndexDev; i++) {
							var oldHookName = hookTypesDev[i];
							var newHookName = i === hookTypesUpdateIndexDev ? currentHookName : oldHookName;
							var row = i + 1 + ". " + oldHookName;
							while (row.length < secondColumnStart) row += " ";
							row += newHookName + "\n";
							table += row;
						}
						error("React has detected a change in the order of Hooks called by %s. This will lead to bugs and errors if not fixed. For more information, read the Rules of Hooks: https://reactjs.org/link/rules-of-hooks\n\n   Previous render            Next render\n   ------------------------------------------------------\n%s   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n", componentName, table);
					}
				}
			}
			function throwInvalidHookError() {
				throw new Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
			}
			function areHookInputsEqual(nextDeps, prevDeps) {
				if (ignorePreviousDependencies) return false;
				if (prevDeps === null) {
					error("%s received a final argument during this render, but not during the previous render. Even though the final argument is optional, its type cannot change between renders.", currentHookNameInDev);
					return false;
				}
				if (nextDeps.length !== prevDeps.length) error("The final argument passed to %s changed size between renders. The order and size of this array must remain constant.\n\nPrevious: %s\nIncoming: %s", currentHookNameInDev, "[" + prevDeps.join(", ") + "]", "[" + nextDeps.join(", ") + "]");
				for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++) {
					if (objectIs(nextDeps[i], prevDeps[i])) continue;
					return false;
				}
				return true;
			}
			function renderWithHooks(current, workInProgress, Component, props, secondArg, nextRenderLanes) {
				renderLanes = nextRenderLanes;
				currentlyRenderingFiber$1 = workInProgress;
				hookTypesDev = current !== null ? current._debugHookTypes : null;
				hookTypesUpdateIndexDev = -1;
				ignorePreviousDependencies = current !== null && current.type !== workInProgress.type;
				workInProgress.memoizedState = null;
				workInProgress.updateQueue = null;
				workInProgress.lanes = NoLanes;
				if (current !== null && current.memoizedState !== null) ReactCurrentDispatcher$1.current = HooksDispatcherOnUpdateInDEV;
				else if (hookTypesDev !== null) ReactCurrentDispatcher$1.current = HooksDispatcherOnMountWithHookTypesInDEV;
				else ReactCurrentDispatcher$1.current = HooksDispatcherOnMountInDEV;
				var children = Component(props, secondArg);
				if (didScheduleRenderPhaseUpdateDuringThisPass) {
					var numberOfReRenders = 0;
					do {
						didScheduleRenderPhaseUpdateDuringThisPass = false;
						localIdCounter = 0;
						if (numberOfReRenders >= RE_RENDER_LIMIT) throw new Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
						numberOfReRenders += 1;
						ignorePreviousDependencies = false;
						currentHook = null;
						workInProgressHook = null;
						workInProgress.updateQueue = null;
						hookTypesUpdateIndexDev = -1;
						ReactCurrentDispatcher$1.current = HooksDispatcherOnRerenderInDEV;
						children = Component(props, secondArg);
					} while (didScheduleRenderPhaseUpdateDuringThisPass);
				}
				ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
				workInProgress._debugHookTypes = hookTypesDev;
				var didRenderTooFewHooks = currentHook !== null && currentHook.next !== null;
				renderLanes = NoLanes;
				currentlyRenderingFiber$1 = null;
				currentHook = null;
				workInProgressHook = null;
				currentHookNameInDev = null;
				hookTypesDev = null;
				hookTypesUpdateIndexDev = -1;
				if (current !== null && (current.flags & StaticMask) !== (workInProgress.flags & StaticMask) && (current.mode & ConcurrentMode) !== NoMode) error("Internal React error: Expected static flag was missing. Please notify the React team.");
				didScheduleRenderPhaseUpdate = false;
				if (didRenderTooFewHooks) throw new Error("Rendered fewer hooks than expected. This may be caused by an accidental early return statement.");
				return children;
			}
			function checkDidRenderIdHook() {
				var didRenderIdHook = localIdCounter !== 0;
				localIdCounter = 0;
				return didRenderIdHook;
			}
			function bailoutHooks(current, workInProgress, lanes) {
				workInProgress.updateQueue = current.updateQueue;
				if ((workInProgress.mode & StrictEffectsMode) !== NoMode) workInProgress.flags &= ~(MountPassiveDev | MountLayoutDev | Passive | Update);
				else workInProgress.flags &= ~(Passive | Update);
				current.lanes = removeLanes(current.lanes, lanes);
			}
			function resetHooksAfterThrow() {
				ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;
				if (didScheduleRenderPhaseUpdate) {
					var hook = currentlyRenderingFiber$1.memoizedState;
					while (hook !== null) {
						var queue = hook.queue;
						if (queue !== null) queue.pending = null;
						hook = hook.next;
					}
					didScheduleRenderPhaseUpdate = false;
				}
				renderLanes = NoLanes;
				currentlyRenderingFiber$1 = null;
				currentHook = null;
				workInProgressHook = null;
				hookTypesDev = null;
				hookTypesUpdateIndexDev = -1;
				currentHookNameInDev = null;
				isUpdatingOpaqueValueInRenderPhase = false;
				didScheduleRenderPhaseUpdateDuringThisPass = false;
				localIdCounter = 0;
			}
			function mountWorkInProgressHook() {
				var hook = {
					memoizedState: null,
					baseState: null,
					baseQueue: null,
					queue: null,
					next: null
				};
				if (workInProgressHook === null) currentlyRenderingFiber$1.memoizedState = workInProgressHook = hook;
				else workInProgressHook = workInProgressHook.next = hook;
				return workInProgressHook;
			}
			function updateWorkInProgressHook() {
				var nextCurrentHook;
				if (currentHook === null) {
					var current = currentlyRenderingFiber$1.alternate;
					if (current !== null) nextCurrentHook = current.memoizedState;
					else nextCurrentHook = null;
				} else nextCurrentHook = currentHook.next;
				var nextWorkInProgressHook;
				if (workInProgressHook === null) nextWorkInProgressHook = currentlyRenderingFiber$1.memoizedState;
				else nextWorkInProgressHook = workInProgressHook.next;
				if (nextWorkInProgressHook !== null) {
					workInProgressHook = nextWorkInProgressHook;
					nextWorkInProgressHook = workInProgressHook.next;
					currentHook = nextCurrentHook;
				} else {
					if (nextCurrentHook === null) throw new Error("Rendered more hooks than during the previous render.");
					currentHook = nextCurrentHook;
					var newHook = {
						memoizedState: currentHook.memoizedState,
						baseState: currentHook.baseState,
						baseQueue: currentHook.baseQueue,
						queue: currentHook.queue,
						next: null
					};
					if (workInProgressHook === null) currentlyRenderingFiber$1.memoizedState = workInProgressHook = newHook;
					else workInProgressHook = workInProgressHook.next = newHook;
				}
				return workInProgressHook;
			}
			function createFunctionComponentUpdateQueue() {
				return {
					lastEffect: null,
					stores: null
				};
			}
			function basicStateReducer(state, action) {
				return typeof action === "function" ? action(state) : action;
			}
			function mountReducer(reducer, initialArg, init) {
				var hook = mountWorkInProgressHook();
				var initialState;
				if (init !== void 0) initialState = init(initialArg);
				else initialState = initialArg;
				hook.memoizedState = hook.baseState = initialState;
				var queue = {
					pending: null,
					interleaved: null,
					lanes: NoLanes,
					dispatch: null,
					lastRenderedReducer: reducer,
					lastRenderedState: initialState
				};
				hook.queue = queue;
				var dispatch = queue.dispatch = dispatchReducerAction.bind(null, currentlyRenderingFiber$1, queue);
				return [hook.memoizedState, dispatch];
			}
			function updateReducer(reducer, initialArg, init) {
				var hook = updateWorkInProgressHook();
				var queue = hook.queue;
				if (queue === null) throw new Error("Should have a queue. This is likely a bug in React. Please file an issue.");
				queue.lastRenderedReducer = reducer;
				var current = currentHook;
				var baseQueue = current.baseQueue;
				var pendingQueue = queue.pending;
				if (pendingQueue !== null) {
					if (baseQueue !== null) {
						var baseFirst = baseQueue.next;
						var pendingFirst = pendingQueue.next;
						baseQueue.next = pendingFirst;
						pendingQueue.next = baseFirst;
					}
					if (current.baseQueue !== baseQueue) error("Internal error: Expected work-in-progress queue to be a clone. This is a bug in React.");
					current.baseQueue = baseQueue = pendingQueue;
					queue.pending = null;
				}
				if (baseQueue !== null) {
					var first = baseQueue.next;
					var newState = current.baseState;
					var newBaseState = null;
					var newBaseQueueFirst = null;
					var newBaseQueueLast = null;
					var update = first;
					do {
						var updateLane = update.lane;
						if (!isSubsetOfLanes(renderLanes, updateLane)) {
							var clone = {
								lane: updateLane,
								action: update.action,
								hasEagerState: update.hasEagerState,
								eagerState: update.eagerState,
								next: null
							};
							if (newBaseQueueLast === null) {
								newBaseQueueFirst = newBaseQueueLast = clone;
								newBaseState = newState;
							} else newBaseQueueLast = newBaseQueueLast.next = clone;
							currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, updateLane);
							markSkippedUpdateLanes(updateLane);
						} else {
							if (newBaseQueueLast !== null) {
								var _clone = {
									lane: NoLane,
									action: update.action,
									hasEagerState: update.hasEagerState,
									eagerState: update.eagerState,
									next: null
								};
								newBaseQueueLast = newBaseQueueLast.next = _clone;
							}
							if (update.hasEagerState) newState = update.eagerState;
							else {
								var action = update.action;
								newState = reducer(newState, action);
							}
						}
						update = update.next;
					} while (update !== null && update !== first);
					if (newBaseQueueLast === null) newBaseState = newState;
					else newBaseQueueLast.next = newBaseQueueFirst;
					if (!objectIs(newState, hook.memoizedState)) markWorkInProgressReceivedUpdate();
					hook.memoizedState = newState;
					hook.baseState = newBaseState;
					hook.baseQueue = newBaseQueueLast;
					queue.lastRenderedState = newState;
				}
				var lastInterleaved = queue.interleaved;
				if (lastInterleaved !== null) {
					var interleaved = lastInterleaved;
					do {
						var interleavedLane = interleaved.lane;
						currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, interleavedLane);
						markSkippedUpdateLanes(interleavedLane);
						interleaved = interleaved.next;
					} while (interleaved !== lastInterleaved);
				} else if (baseQueue === null) queue.lanes = NoLanes;
				var dispatch = queue.dispatch;
				return [hook.memoizedState, dispatch];
			}
			function rerenderReducer(reducer, initialArg, init) {
				var hook = updateWorkInProgressHook();
				var queue = hook.queue;
				if (queue === null) throw new Error("Should have a queue. This is likely a bug in React. Please file an issue.");
				queue.lastRenderedReducer = reducer;
				var dispatch = queue.dispatch;
				var lastRenderPhaseUpdate = queue.pending;
				var newState = hook.memoizedState;
				if (lastRenderPhaseUpdate !== null) {
					queue.pending = null;
					var firstRenderPhaseUpdate = lastRenderPhaseUpdate.next;
					var update = firstRenderPhaseUpdate;
					do {
						var action = update.action;
						newState = reducer(newState, action);
						update = update.next;
					} while (update !== firstRenderPhaseUpdate);
					if (!objectIs(newState, hook.memoizedState)) markWorkInProgressReceivedUpdate();
					hook.memoizedState = newState;
					if (hook.baseQueue === null) hook.baseState = newState;
					queue.lastRenderedState = newState;
				}
				return [newState, dispatch];
			}
			function mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
				var fiber = currentlyRenderingFiber$1;
				var hook = mountWorkInProgressHook();
				var nextSnapshot;
				if (getIsHydrating()) {
					if (getServerSnapshot === void 0) throw new Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
					nextSnapshot = getServerSnapshot();
					if (!didWarnUncachedGetSnapshot) {
						if (nextSnapshot !== getServerSnapshot()) {
							error("The result of getServerSnapshot should be cached to avoid an infinite loop");
							didWarnUncachedGetSnapshot = true;
						}
					}
				} else {
					nextSnapshot = getSnapshot();
					if (!didWarnUncachedGetSnapshot) {
						var cachedSnapshot = getSnapshot();
						if (!objectIs(nextSnapshot, cachedSnapshot)) {
							error("The result of getSnapshot should be cached to avoid an infinite loop");
							didWarnUncachedGetSnapshot = true;
						}
					}
					var root = getWorkInProgressRoot();
					if (root === null) throw new Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
					if (!includesBlockingLane(root, renderLanes)) pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
				}
				hook.memoizedState = nextSnapshot;
				var inst = {
					value: nextSnapshot,
					getSnapshot
				};
				hook.queue = inst;
				mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [subscribe]);
				fiber.flags |= Passive;
				pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), void 0, null);
				return nextSnapshot;
			}
			function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
				var fiber = currentlyRenderingFiber$1;
				var hook = updateWorkInProgressHook();
				var nextSnapshot = getSnapshot();
				if (!didWarnUncachedGetSnapshot) {
					if (!objectIs(nextSnapshot, getSnapshot())) {
						error("The result of getSnapshot should be cached to avoid an infinite loop");
						didWarnUncachedGetSnapshot = true;
					}
				}
				var prevSnapshot = hook.memoizedState;
				var snapshotChanged = !objectIs(prevSnapshot, nextSnapshot);
				if (snapshotChanged) {
					hook.memoizedState = nextSnapshot;
					markWorkInProgressReceivedUpdate();
				}
				var inst = hook.queue;
				updateEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [subscribe]);
				if (inst.getSnapshot !== getSnapshot || snapshotChanged || workInProgressHook !== null && workInProgressHook.memoizedState.tag & HasEffect) {
					fiber.flags |= Passive;
					pushEffect(HasEffect | Passive$1, updateStoreInstance.bind(null, fiber, inst, nextSnapshot, getSnapshot), void 0, null);
					var root = getWorkInProgressRoot();
					if (root === null) throw new Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
					if (!includesBlockingLane(root, renderLanes)) pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
				}
				return nextSnapshot;
			}
			function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
				fiber.flags |= StoreConsistency;
				var check = {
					getSnapshot,
					value: renderedSnapshot
				};
				var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
				if (componentUpdateQueue === null) {
					componentUpdateQueue = createFunctionComponentUpdateQueue();
					currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
					componentUpdateQueue.stores = [check];
				} else {
					var stores = componentUpdateQueue.stores;
					if (stores === null) componentUpdateQueue.stores = [check];
					else stores.push(check);
				}
			}
			function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
				inst.value = nextSnapshot;
				inst.getSnapshot = getSnapshot;
				if (checkIfSnapshotChanged(inst)) forceStoreRerender(fiber);
			}
			function subscribeToStore(fiber, inst, subscribe) {
				var handleStoreChange = function() {
					if (checkIfSnapshotChanged(inst)) forceStoreRerender(fiber);
				};
				return subscribe(handleStoreChange);
			}
			function checkIfSnapshotChanged(inst) {
				var latestGetSnapshot = inst.getSnapshot;
				var prevValue = inst.value;
				try {
					return !objectIs(prevValue, latestGetSnapshot());
				} catch (error) {
					return true;
				}
			}
			function forceStoreRerender(fiber) {
				var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
			}
			function mountState(initialState) {
				var hook = mountWorkInProgressHook();
				if (typeof initialState === "function") initialState = initialState();
				hook.memoizedState = hook.baseState = initialState;
				var queue = {
					pending: null,
					interleaved: null,
					lanes: NoLanes,
					dispatch: null,
					lastRenderedReducer: basicStateReducer,
					lastRenderedState: initialState
				};
				hook.queue = queue;
				var dispatch = queue.dispatch = dispatchSetState.bind(null, currentlyRenderingFiber$1, queue);
				return [hook.memoizedState, dispatch];
			}
			function updateState(initialState) {
				return updateReducer(basicStateReducer);
			}
			function rerenderState(initialState) {
				return rerenderReducer(basicStateReducer);
			}
			function pushEffect(tag, create, destroy, deps) {
				var effect = {
					tag,
					create,
					destroy,
					deps,
					next: null
				};
				var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;
				if (componentUpdateQueue === null) {
					componentUpdateQueue = createFunctionComponentUpdateQueue();
					currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
					componentUpdateQueue.lastEffect = effect.next = effect;
				} else {
					var lastEffect = componentUpdateQueue.lastEffect;
					if (lastEffect === null) componentUpdateQueue.lastEffect = effect.next = effect;
					else {
						var firstEffect = lastEffect.next;
						lastEffect.next = effect;
						effect.next = firstEffect;
						componentUpdateQueue.lastEffect = effect;
					}
				}
				return effect;
			}
			function mountRef(initialValue) {
				var hook = mountWorkInProgressHook();
				var _ref2 = { current: initialValue };
				hook.memoizedState = _ref2;
				return _ref2;
			}
			function updateRef(initialValue) {
				return updateWorkInProgressHook().memoizedState;
			}
			function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
				var hook = mountWorkInProgressHook();
				var nextDeps = deps === void 0 ? null : deps;
				currentlyRenderingFiber$1.flags |= fiberFlags;
				hook.memoizedState = pushEffect(HasEffect | hookFlags, create, void 0, nextDeps);
			}
			function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
				var hook = updateWorkInProgressHook();
				var nextDeps = deps === void 0 ? null : deps;
				var destroy = void 0;
				if (currentHook !== null) {
					var prevEffect = currentHook.memoizedState;
					destroy = prevEffect.destroy;
					if (nextDeps !== null) {
						var prevDeps = prevEffect.deps;
						if (areHookInputsEqual(nextDeps, prevDeps)) {
							hook.memoizedState = pushEffect(hookFlags, create, destroy, nextDeps);
							return;
						}
					}
				}
				currentlyRenderingFiber$1.flags |= fiberFlags;
				hook.memoizedState = pushEffect(HasEffect | hookFlags, create, destroy, nextDeps);
			}
			function mountEffect(create, deps) {
				if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) return mountEffectImpl(MountPassiveDev | Passive | PassiveStatic, Passive$1, create, deps);
				else return mountEffectImpl(Passive | PassiveStatic, Passive$1, create, deps);
			}
			function updateEffect(create, deps) {
				return updateEffectImpl(Passive, Passive$1, create, deps);
			}
			function mountInsertionEffect(create, deps) {
				return mountEffectImpl(Update, Insertion, create, deps);
			}
			function updateInsertionEffect(create, deps) {
				return updateEffectImpl(Update, Insertion, create, deps);
			}
			function mountLayoutEffect(create, deps) {
				var fiberFlags = Update;
				fiberFlags |= LayoutStatic;
				if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
				return mountEffectImpl(fiberFlags, Layout, create, deps);
			}
			function updateLayoutEffect(create, deps) {
				return updateEffectImpl(Update, Layout, create, deps);
			}
			function imperativeHandleEffect(create, ref) {
				if (typeof ref === "function") {
					var refCallback = ref;
					refCallback(create());
					return function() {
						refCallback(null);
					};
				} else if (ref !== null && ref !== void 0) {
					var refObject = ref;
					if (!refObject.hasOwnProperty("current")) error("Expected useImperativeHandle() first argument to either be a ref callback or React.createRef() object. Instead received: %s.", "an object with keys {" + Object.keys(refObject).join(", ") + "}");
					refObject.current = create();
					return function() {
						refObject.current = null;
					};
				}
			}
			function mountImperativeHandle(ref, create, deps) {
				if (typeof create !== "function") error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : "null");
				var effectDeps = deps !== null && deps !== void 0 ? deps.concat([ref]) : null;
				var fiberFlags = Update;
				fiberFlags |= LayoutStatic;
				if ((currentlyRenderingFiber$1.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
				return mountEffectImpl(fiberFlags, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
			}
			function updateImperativeHandle(ref, create, deps) {
				if (typeof create !== "function") error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", create !== null ? typeof create : "null");
				var effectDeps = deps !== null && deps !== void 0 ? deps.concat([ref]) : null;
				return updateEffectImpl(Update, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
			}
			function mountDebugValue(value, formatterFn) {}
			var updateDebugValue = mountDebugValue;
			function mountCallback(callback, deps) {
				var hook = mountWorkInProgressHook();
				hook.memoizedState = [callback, deps === void 0 ? null : deps];
				return callback;
			}
			function updateCallback(callback, deps) {
				var hook = updateWorkInProgressHook();
				var nextDeps = deps === void 0 ? null : deps;
				var prevState = hook.memoizedState;
				if (prevState !== null) {
					if (nextDeps !== null) {
						var prevDeps = prevState[1];
						if (areHookInputsEqual(nextDeps, prevDeps)) return prevState[0];
					}
				}
				hook.memoizedState = [callback, nextDeps];
				return callback;
			}
			function mountMemo(nextCreate, deps) {
				var hook = mountWorkInProgressHook();
				var nextDeps = deps === void 0 ? null : deps;
				var nextValue = nextCreate();
				hook.memoizedState = [nextValue, nextDeps];
				return nextValue;
			}
			function updateMemo(nextCreate, deps) {
				var hook = updateWorkInProgressHook();
				var nextDeps = deps === void 0 ? null : deps;
				var prevState = hook.memoizedState;
				if (prevState !== null) {
					if (nextDeps !== null) {
						var prevDeps = prevState[1];
						if (areHookInputsEqual(nextDeps, prevDeps)) return prevState[0];
					}
				}
				var nextValue = nextCreate();
				hook.memoizedState = [nextValue, nextDeps];
				return nextValue;
			}
			function mountDeferredValue(value) {
				var hook = mountWorkInProgressHook();
				hook.memoizedState = value;
				return value;
			}
			function updateDeferredValue(value) {
				var hook = updateWorkInProgressHook();
				var prevValue = currentHook.memoizedState;
				return updateDeferredValueImpl(hook, prevValue, value);
			}
			function rerenderDeferredValue(value) {
				var hook = updateWorkInProgressHook();
				if (currentHook === null) {
					hook.memoizedState = value;
					return value;
				} else {
					var prevValue = currentHook.memoizedState;
					return updateDeferredValueImpl(hook, prevValue, value);
				}
			}
			function updateDeferredValueImpl(hook, prevValue, value) {
				if (!includesOnlyNonUrgentLanes(renderLanes)) {
					if (!objectIs(value, prevValue)) {
						var deferredLane = claimNextTransitionLane();
						currentlyRenderingFiber$1.lanes = mergeLanes(currentlyRenderingFiber$1.lanes, deferredLane);
						markSkippedUpdateLanes(deferredLane);
						hook.baseState = true;
					}
					return prevValue;
				} else {
					if (hook.baseState) {
						hook.baseState = false;
						markWorkInProgressReceivedUpdate();
					}
					hook.memoizedState = value;
					return value;
				}
			}
			function startTransition(setPending, callback, options) {
				var previousPriority = getCurrentUpdatePriority();
				setCurrentUpdatePriority(higherEventPriority(previousPriority, ContinuousEventPriority));
				setPending(true);
				var prevTransition = ReactCurrentBatchConfig$2.transition;
				ReactCurrentBatchConfig$2.transition = {};
				var currentTransition = ReactCurrentBatchConfig$2.transition;
				ReactCurrentBatchConfig$2.transition._updatedFibers = /* @__PURE__ */ new Set();
				try {
					setPending(false);
					callback();
				} finally {
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig$2.transition = prevTransition;
					if (prevTransition === null && currentTransition._updatedFibers) {
						if (currentTransition._updatedFibers.size > 10) warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.");
						currentTransition._updatedFibers.clear();
					}
				}
			}
			function mountTransition() {
				var _mountState = mountState(false), isPending = _mountState[0], setPending = _mountState[1];
				var start = startTransition.bind(null, setPending);
				var hook = mountWorkInProgressHook();
				hook.memoizedState = start;
				return [isPending, start];
			}
			function updateTransition() {
				return [updateState()[0], updateWorkInProgressHook().memoizedState];
			}
			function rerenderTransition() {
				return [rerenderState()[0], updateWorkInProgressHook().memoizedState];
			}
			var isUpdatingOpaqueValueInRenderPhase = false;
			function getIsUpdatingOpaqueValueInRenderPhaseInDEV() {
				return isUpdatingOpaqueValueInRenderPhase;
			}
			function mountId() {
				var hook = mountWorkInProgressHook();
				var identifierPrefix = getWorkInProgressRoot().identifierPrefix;
				var id;
				if (getIsHydrating()) {
					var treeId = getTreeId();
					id = ":" + identifierPrefix + "R" + treeId;
					var localId = localIdCounter++;
					if (localId > 0) id += "H" + localId.toString(32);
					id += ":";
				} else {
					var globalClientId = globalClientIdCounter++;
					id = ":" + identifierPrefix + "r" + globalClientId.toString(32) + ":";
				}
				hook.memoizedState = id;
				return id;
			}
			function updateId() {
				return updateWorkInProgressHook().memoizedState;
			}
			function dispatchReducerAction(fiber, queue, action) {
				if (typeof arguments[3] === "function") error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
				var lane = requestUpdateLane(fiber);
				var update = {
					lane,
					action,
					hasEagerState: false,
					eagerState: null,
					next: null
				};
				if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
				else {
					var root = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
					if (root !== null) {
						scheduleUpdateOnFiber(root, fiber, lane, requestEventTime());
						entangleTransitionUpdate(root, queue, lane);
					}
				}
				markUpdateInDevTools(fiber, lane);
			}
			function dispatchSetState(fiber, queue, action) {
				if (typeof arguments[3] === "function") error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
				var lane = requestUpdateLane(fiber);
				var update = {
					lane,
					action,
					hasEagerState: false,
					eagerState: null,
					next: null
				};
				if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
				else {
					var alternate = fiber.alternate;
					if (fiber.lanes === NoLanes && (alternate === null || alternate.lanes === NoLanes)) {
						var lastRenderedReducer = queue.lastRenderedReducer;
						if (lastRenderedReducer !== null) {
							var prevDispatcher = ReactCurrentDispatcher$1.current;
							ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
							try {
								var currentState = queue.lastRenderedState;
								var eagerState = lastRenderedReducer(currentState, action);
								update.hasEagerState = true;
								update.eagerState = eagerState;
								if (objectIs(eagerState, currentState)) {
									enqueueConcurrentHookUpdateAndEagerlyBailout(fiber, queue, update, lane);
									return;
								}
							} catch (error) {} finally {
								ReactCurrentDispatcher$1.current = prevDispatcher;
							}
						}
					}
					var root = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
					if (root !== null) {
						scheduleUpdateOnFiber(root, fiber, lane, requestEventTime());
						entangleTransitionUpdate(root, queue, lane);
					}
				}
				markUpdateInDevTools(fiber, lane);
			}
			function isRenderPhaseUpdate(fiber) {
				var alternate = fiber.alternate;
				return fiber === currentlyRenderingFiber$1 || alternate !== null && alternate === currentlyRenderingFiber$1;
			}
			function enqueueRenderPhaseUpdate(queue, update) {
				didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
				var pending = queue.pending;
				if (pending === null) update.next = update;
				else {
					update.next = pending.next;
					pending.next = update;
				}
				queue.pending = update;
			}
			function entangleTransitionUpdate(root, queue, lane) {
				if (isTransitionLane(lane)) {
					var queueLanes = queue.lanes;
					queueLanes = intersectLanes(queueLanes, root.pendingLanes);
					var newQueueLanes = mergeLanes(queueLanes, lane);
					queue.lanes = newQueueLanes;
					markRootEntangled(root, newQueueLanes);
				}
			}
			function markUpdateInDevTools(fiber, lane, action) {
				markStateUpdateScheduled(fiber, lane);
			}
			var ContextOnlyDispatcher = {
				readContext,
				useCallback: throwInvalidHookError,
				useContext: throwInvalidHookError,
				useEffect: throwInvalidHookError,
				useImperativeHandle: throwInvalidHookError,
				useInsertionEffect: throwInvalidHookError,
				useLayoutEffect: throwInvalidHookError,
				useMemo: throwInvalidHookError,
				useReducer: throwInvalidHookError,
				useRef: throwInvalidHookError,
				useState: throwInvalidHookError,
				useDebugValue: throwInvalidHookError,
				useDeferredValue: throwInvalidHookError,
				useTransition: throwInvalidHookError,
				useMutableSource: throwInvalidHookError,
				useSyncExternalStore: throwInvalidHookError,
				useId: throwInvalidHookError,
				unstable_isNewReconciler: enableNewReconciler
			};
			var HooksDispatcherOnMountInDEV = null;
			var HooksDispatcherOnMountWithHookTypesInDEV = null;
			var HooksDispatcherOnUpdateInDEV = null;
			var HooksDispatcherOnRerenderInDEV = null;
			var InvalidNestedHooksDispatcherOnMountInDEV = null;
			var InvalidNestedHooksDispatcherOnUpdateInDEV = null;
			var InvalidNestedHooksDispatcherOnRerenderInDEV = null;
			var warnInvalidContextAccess = function() {
				error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
			};
			var warnInvalidHookAccess = function() {
				error("Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. You can only call Hooks at the top level of your React function. For more information, see https://reactjs.org/link/rules-of-hooks");
			};
			HooksDispatcherOnMountInDEV = {
				readContext: function(context) {
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					return mountCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					mountHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					return mountEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					return mountImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					return mountInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					return mountLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					mountHookTypesDev();
					checkDepsAreArrayDev(deps);
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					mountHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					mountHookTypesDev();
					return mountRef(initialValue);
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					mountHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					mountHookTypesDev();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					mountHookTypesDev();
					return mountDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					mountHookTypesDev();
					return mountTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					mountHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					mountHookTypesDev();
					return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					mountHookTypesDev();
					return mountId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			HooksDispatcherOnMountWithHookTypesInDEV = {
				readContext: function(context) {
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					updateHookTypesDev();
					return mountCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					updateHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					updateHookTypesDev();
					return mountEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					updateHookTypesDev();
					return mountImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					updateHookTypesDev();
					return mountInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					updateHookTypesDev();
					return mountLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					updateHookTypesDev();
					return mountRef(initialValue);
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					updateHookTypesDev();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					updateHookTypesDev();
					return mountDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					updateHookTypesDev();
					return mountTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					updateHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					updateHookTypesDev();
					return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					updateHookTypesDev();
					return mountId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			HooksDispatcherOnUpdateInDEV = {
				readContext: function(context) {
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					updateHookTypesDev();
					return updateCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					updateHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					updateHookTypesDev();
					return updateEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					updateHookTypesDev();
					return updateImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					updateHookTypesDev();
					return updateInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					updateHookTypesDev();
					return updateLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					updateHookTypesDev();
					return updateRef();
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					updateHookTypesDev();
					return updateDebugValue();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					updateHookTypesDev();
					return updateDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					updateHookTypesDev();
					return updateTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					updateHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					updateHookTypesDev();
					return updateSyncExternalStore(subscribe, getSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					updateHookTypesDev();
					return updateId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			HooksDispatcherOnRerenderInDEV = {
				readContext: function(context) {
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					updateHookTypesDev();
					return updateCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					updateHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					updateHookTypesDev();
					return updateEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					updateHookTypesDev();
					return updateImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					updateHookTypesDev();
					return updateInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					updateHookTypesDev();
					return updateLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
					try {
						return updateMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
					try {
						return rerenderReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					updateHookTypesDev();
					return updateRef();
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnRerenderInDEV;
					try {
						return rerenderState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					updateHookTypesDev();
					return updateDebugValue();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					updateHookTypesDev();
					return rerenderDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					updateHookTypesDev();
					return rerenderTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					updateHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					updateHookTypesDev();
					return updateSyncExternalStore(subscribe, getSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					updateHookTypesDev();
					return updateId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			InvalidNestedHooksDispatcherOnMountInDEV = {
				readContext: function(context) {
					warnInvalidContextAccess();
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					warnInvalidHookAccess();
					mountHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					warnInvalidHookAccess();
					mountHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountRef(initialValue);
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					warnInvalidHookAccess();
					mountHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnMountInDEV;
					try {
						return mountState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					warnInvalidHookAccess();
					mountHookTypesDev();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					warnInvalidHookAccess();
					mountHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					warnInvalidHookAccess();
					mountHookTypesDev();
					return mountId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			InvalidNestedHooksDispatcherOnUpdateInDEV = {
				readContext: function(context) {
					warnInvalidContextAccess();
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateRef();
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateDebugValue();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					warnInvalidHookAccess();
					updateHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateSyncExternalStore(subscribe, getSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			InvalidNestedHooksDispatcherOnRerenderInDEV = {
				readContext: function(context) {
					warnInvalidContextAccess();
					return readContext(context);
				},
				useCallback: function(callback, deps) {
					currentHookNameInDev = "useCallback";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateCallback(callback, deps);
				},
				useContext: function(context) {
					currentHookNameInDev = "useContext";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return readContext(context);
				},
				useEffect: function(create, deps) {
					currentHookNameInDev = "useEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateEffect(create, deps);
				},
				useImperativeHandle: function(ref, create, deps) {
					currentHookNameInDev = "useImperativeHandle";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateImperativeHandle(ref, create, deps);
				},
				useInsertionEffect: function(create, deps) {
					currentHookNameInDev = "useInsertionEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateInsertionEffect(create, deps);
				},
				useLayoutEffect: function(create, deps) {
					currentHookNameInDev = "useLayoutEffect";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateLayoutEffect(create, deps);
				},
				useMemo: function(create, deps) {
					currentHookNameInDev = "useMemo";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return updateMemo(create, deps);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useReducer: function(reducer, initialArg, init) {
					currentHookNameInDev = "useReducer";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return rerenderReducer(reducer, initialArg, init);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useRef: function(initialValue) {
					currentHookNameInDev = "useRef";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateRef();
				},
				useState: function(initialState) {
					currentHookNameInDev = "useState";
					warnInvalidHookAccess();
					updateHookTypesDev();
					var prevDispatcher = ReactCurrentDispatcher$1.current;
					ReactCurrentDispatcher$1.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						return rerenderState(initialState);
					} finally {
						ReactCurrentDispatcher$1.current = prevDispatcher;
					}
				},
				useDebugValue: function(value, formatterFn) {
					currentHookNameInDev = "useDebugValue";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateDebugValue();
				},
				useDeferredValue: function(value) {
					currentHookNameInDev = "useDeferredValue";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return rerenderDeferredValue(value);
				},
				useTransition: function() {
					currentHookNameInDev = "useTransition";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return rerenderTransition();
				},
				useMutableSource: function(source, getSnapshot, subscribe) {
					currentHookNameInDev = "useMutableSource";
					warnInvalidHookAccess();
					updateHookTypesDev();
				},
				useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
					currentHookNameInDev = "useSyncExternalStore";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateSyncExternalStore(subscribe, getSnapshot);
				},
				useId: function() {
					currentHookNameInDev = "useId";
					warnInvalidHookAccess();
					updateHookTypesDev();
					return updateId();
				},
				unstable_isNewReconciler: enableNewReconciler
			};
			var now$1 = Scheduler.unstable_now;
			var commitTime = 0;
			var layoutEffectStartTime = -1;
			var profilerStartTime = -1;
			var passiveEffectStartTime = -1;
			/**
			* Tracks whether the current update was a nested/cascading update (scheduled from a layout effect).
			*
			* The overall sequence is:
			*   1. render
			*   2. commit (and call `onRender`, `onCommit`)
			*   3. check for nested updates
			*   4. flush passive effects (and call `onPostCommit`)
			*
			* Nested updates are identified in step 3 above,
			* but step 4 still applies to the work that was just committed.
			* We use two flags to track nested updates then:
			* one tracks whether the upcoming update is a nested update,
			* and the other tracks whether the current update was a nested update.
			* The first value gets synced to the second at the start of the render phase.
			*/
			var currentUpdateIsNested = false;
			var nestedUpdateScheduled = false;
			function isCurrentUpdateNested() {
				return currentUpdateIsNested;
			}
			function markNestedUpdateScheduled() {
				nestedUpdateScheduled = true;
			}
			function resetNestedUpdateFlag() {
				currentUpdateIsNested = false;
				nestedUpdateScheduled = false;
			}
			function syncNestedUpdateFlag() {
				currentUpdateIsNested = nestedUpdateScheduled;
				nestedUpdateScheduled = false;
			}
			function getCommitTime() {
				return commitTime;
			}
			function recordCommitTime() {
				commitTime = now$1();
			}
			function startProfilerTimer(fiber) {
				profilerStartTime = now$1();
				if (fiber.actualStartTime < 0) fiber.actualStartTime = now$1();
			}
			function stopProfilerTimerIfRunning(fiber) {
				profilerStartTime = -1;
			}
			function stopProfilerTimerIfRunningAndRecordDelta(fiber, overrideBaseTime) {
				if (profilerStartTime >= 0) {
					var elapsedTime = now$1() - profilerStartTime;
					fiber.actualDuration += elapsedTime;
					if (overrideBaseTime) fiber.selfBaseDuration = elapsedTime;
					profilerStartTime = -1;
				}
			}
			function recordLayoutEffectDuration(fiber) {
				if (layoutEffectStartTime >= 0) {
					var elapsedTime = now$1() - layoutEffectStartTime;
					layoutEffectStartTime = -1;
					var parentFiber = fiber.return;
					while (parentFiber !== null) {
						switch (parentFiber.tag) {
							case HostRoot:
								var root = parentFiber.stateNode;
								root.effectDuration += elapsedTime;
								return;
							case Profiler:
								var parentStateNode = parentFiber.stateNode;
								parentStateNode.effectDuration += elapsedTime;
								return;
						}
						parentFiber = parentFiber.return;
					}
				}
			}
			function recordPassiveEffectDuration(fiber) {
				if (passiveEffectStartTime >= 0) {
					var elapsedTime = now$1() - passiveEffectStartTime;
					passiveEffectStartTime = -1;
					var parentFiber = fiber.return;
					while (parentFiber !== null) {
						switch (parentFiber.tag) {
							case HostRoot:
								var root = parentFiber.stateNode;
								if (root !== null) root.passiveEffectDuration += elapsedTime;
								return;
							case Profiler:
								var parentStateNode = parentFiber.stateNode;
								if (parentStateNode !== null) parentStateNode.passiveEffectDuration += elapsedTime;
								return;
						}
						parentFiber = parentFiber.return;
					}
				}
			}
			function startLayoutEffectTimer() {
				layoutEffectStartTime = now$1();
			}
			function startPassiveEffectTimer() {
				passiveEffectStartTime = now$1();
			}
			function transferActualDuration(fiber) {
				var child = fiber.child;
				while (child) {
					fiber.actualDuration += child.actualDuration;
					child = child.sibling;
				}
			}
			function resolveDefaultProps(Component, baseProps) {
				if (Component && Component.defaultProps) {
					var props = assign({}, baseProps);
					var defaultProps = Component.defaultProps;
					for (var propName in defaultProps) if (props[propName] === void 0) props[propName] = defaultProps[propName];
					return props;
				}
				return baseProps;
			}
			var fakeInternalInstance = {};
			var didWarnAboutStateAssignmentForComponent;
			var didWarnAboutUninitializedState;
			var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate;
			var didWarnAboutLegacyLifecyclesAndDerivedState;
			var didWarnAboutUndefinedDerivedState;
			var warnOnUndefinedDerivedState;
			var warnOnInvalidCallback;
			var didWarnAboutDirectlyAssigningPropsToState;
			var didWarnAboutContextTypeAndContextTypes;
			var didWarnAboutInvalidateContextType;
			var didWarnAboutLegacyContext$1;
			didWarnAboutStateAssignmentForComponent = /* @__PURE__ */ new Set();
			didWarnAboutUninitializedState = /* @__PURE__ */ new Set();
			didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = /* @__PURE__ */ new Set();
			didWarnAboutLegacyLifecyclesAndDerivedState = /* @__PURE__ */ new Set();
			didWarnAboutDirectlyAssigningPropsToState = /* @__PURE__ */ new Set();
			didWarnAboutUndefinedDerivedState = /* @__PURE__ */ new Set();
			didWarnAboutContextTypeAndContextTypes = /* @__PURE__ */ new Set();
			didWarnAboutInvalidateContextType = /* @__PURE__ */ new Set();
			didWarnAboutLegacyContext$1 = /* @__PURE__ */ new Set();
			var didWarnOnInvalidCallback = /* @__PURE__ */ new Set();
			warnOnInvalidCallback = function(callback, callerName) {
				if (callback === null || typeof callback === "function") return;
				var key = callerName + "_" + callback;
				if (!didWarnOnInvalidCallback.has(key)) {
					didWarnOnInvalidCallback.add(key);
					error("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
				}
			};
			warnOnUndefinedDerivedState = function(type, partialState) {
				if (partialState === void 0) {
					var componentName = getComponentNameFromType(type) || "Component";
					if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
						didWarnAboutUndefinedDerivedState.add(componentName);
						error("%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. You have returned undefined.", componentName);
					}
				}
			};
			Object.defineProperty(fakeInternalInstance, "_processChildContext", {
				enumerable: false,
				value: function() {
					throw new Error("_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn't supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).");
				}
			});
			Object.freeze(fakeInternalInstance);
			function applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, nextProps) {
				var prevState = workInProgress.memoizedState;
				var partialState = getDerivedStateFromProps(nextProps, prevState);
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(true);
					try {
						partialState = getDerivedStateFromProps(nextProps, prevState);
					} finally {
						setIsStrictModeForDevtools(false);
					}
				}
				warnOnUndefinedDerivedState(ctor, partialState);
				var memoizedState = partialState === null || partialState === void 0 ? prevState : assign({}, prevState, partialState);
				workInProgress.memoizedState = memoizedState;
				if (workInProgress.lanes === NoLanes) {
					var updateQueue = workInProgress.updateQueue;
					updateQueue.baseState = memoizedState;
				}
			}
			var classComponentUpdater = {
				isMounted,
				enqueueSetState: function(inst, payload, callback) {
					var fiber = get(inst);
					var eventTime = requestEventTime();
					var lane = requestUpdateLane(fiber);
					var update = createUpdate(eventTime, lane);
					update.payload = payload;
					if (callback !== void 0 && callback !== null) {
						warnOnInvalidCallback(callback, "setState");
						update.callback = callback;
					}
					var root = enqueueUpdate(fiber, update, lane);
					if (root !== null) {
						scheduleUpdateOnFiber(root, fiber, lane, eventTime);
						entangleTransitions(root, fiber, lane);
					}
					markStateUpdateScheduled(fiber, lane);
				},
				enqueueReplaceState: function(inst, payload, callback) {
					var fiber = get(inst);
					var eventTime = requestEventTime();
					var lane = requestUpdateLane(fiber);
					var update = createUpdate(eventTime, lane);
					update.tag = ReplaceState;
					update.payload = payload;
					if (callback !== void 0 && callback !== null) {
						warnOnInvalidCallback(callback, "replaceState");
						update.callback = callback;
					}
					var root = enqueueUpdate(fiber, update, lane);
					if (root !== null) {
						scheduleUpdateOnFiber(root, fiber, lane, eventTime);
						entangleTransitions(root, fiber, lane);
					}
					markStateUpdateScheduled(fiber, lane);
				},
				enqueueForceUpdate: function(inst, callback) {
					var fiber = get(inst);
					var eventTime = requestEventTime();
					var lane = requestUpdateLane(fiber);
					var update = createUpdate(eventTime, lane);
					update.tag = ForceUpdate;
					if (callback !== void 0 && callback !== null) {
						warnOnInvalidCallback(callback, "forceUpdate");
						update.callback = callback;
					}
					var root = enqueueUpdate(fiber, update, lane);
					if (root !== null) {
						scheduleUpdateOnFiber(root, fiber, lane, eventTime);
						entangleTransitions(root, fiber, lane);
					}
					markForceUpdateScheduled(fiber, lane);
				}
			};
			function checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) {
				var instance = workInProgress.stateNode;
				if (typeof instance.shouldComponentUpdate === "function") {
					var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
					if (workInProgress.mode & StrictLegacyMode) {
						setIsStrictModeForDevtools(true);
						try {
							shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
						} finally {
							setIsStrictModeForDevtools(false);
						}
					}
					if (shouldUpdate === void 0) error("%s.shouldComponentUpdate(): Returned undefined instead of a boolean value. Make sure to return true or false.", getComponentNameFromType(ctor) || "Component");
					return shouldUpdate;
				}
				if (ctor.prototype && ctor.prototype.isPureReactComponent) return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
				return true;
			}
			function checkClassInstance(workInProgress, ctor, newProps) {
				var instance = workInProgress.stateNode;
				var name = getComponentNameFromType(ctor) || "Component";
				if (!instance.render) {
					if (ctor.prototype && typeof ctor.prototype.render === "function") error("%s(...): No `render` method found on the returned component instance: did you accidentally return an object from the constructor?", name);
					else error("%s(...): No `render` method found on the returned component instance: you may have forgotten to define `render`.", name);
				}
				if (instance.getInitialState && !instance.getInitialState.isReactClassApproved && !instance.state) error("getInitialState was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Did you mean to define a state property instead?", name);
				if (instance.getDefaultProps && !instance.getDefaultProps.isReactClassApproved) error("getDefaultProps was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Use a static property to define defaultProps instead.", name);
				if (instance.propTypes) error("propTypes was defined as an instance property on %s. Use a static property to define propTypes instead.", name);
				if (instance.contextType) error("contextType was defined as an instance property on %s. Use a static property to define contextType instead.", name);
				if (ctor.childContextTypes && !didWarnAboutLegacyContext$1.has(ctor) && (workInProgress.mode & StrictLegacyMode) === NoMode) {
					didWarnAboutLegacyContext$1.add(ctor);
					error("%s uses the legacy childContextTypes API which is no longer supported and will be removed in the next major release. Use React.createContext() instead\n\n.Learn more about this warning here: https://reactjs.org/link/legacy-context", name);
				}
				if (ctor.contextTypes && !didWarnAboutLegacyContext$1.has(ctor) && (workInProgress.mode & StrictLegacyMode) === NoMode) {
					didWarnAboutLegacyContext$1.add(ctor);
					error("%s uses the legacy contextTypes API which is no longer supported and will be removed in the next major release. Use React.createContext() with static contextType instead.\n\nLearn more about this warning here: https://reactjs.org/link/legacy-context", name);
				}
				if (instance.contextTypes) error("contextTypes was defined as an instance property on %s. Use a static property to define contextTypes instead.", name);
				if (ctor.contextType && ctor.contextTypes && !didWarnAboutContextTypeAndContextTypes.has(ctor)) {
					didWarnAboutContextTypeAndContextTypes.add(ctor);
					error("%s declares both contextTypes and contextType static properties. The legacy contextTypes property will be ignored.", name);
				}
				if (typeof instance.componentShouldUpdate === "function") error("%s has a method called componentShouldUpdate(). Did you mean shouldComponentUpdate()? The name is phrased as a question because the function is expected to return a value.", name);
				if (ctor.prototype && ctor.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== "undefined") error("%s has a method called shouldComponentUpdate(). shouldComponentUpdate should not be used when extending React.PureComponent. Please extend React.Component if shouldComponentUpdate is used.", getComponentNameFromType(ctor) || "A pure component");
				if (typeof instance.componentDidUnmount === "function") error("%s has a method called componentDidUnmount(). But there is no such lifecycle method. Did you mean componentWillUnmount()?", name);
				if (typeof instance.componentDidReceiveProps === "function") error("%s has a method called componentDidReceiveProps(). But there is no such lifecycle method. If you meant to update the state in response to changing props, use componentWillReceiveProps(). If you meant to fetch data or run side-effects or mutations after React has updated the UI, use componentDidUpdate().", name);
				if (typeof instance.componentWillRecieveProps === "function") error("%s has a method called componentWillRecieveProps(). Did you mean componentWillReceiveProps()?", name);
				if (typeof instance.UNSAFE_componentWillRecieveProps === "function") error("%s has a method called UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?", name);
				var hasMutatedProps = instance.props !== newProps;
				if (instance.props !== void 0 && hasMutatedProps) error("%s(...): When calling super() in `%s`, make sure to pass up the same props that your component's constructor was passed.", name, name);
				if (instance.defaultProps) error("Setting defaultProps as an instance property on %s is not supported and will be ignored. Instead, define defaultProps as a static property on %s.", name, name);
				if (typeof instance.getSnapshotBeforeUpdate === "function" && typeof instance.componentDidUpdate !== "function" && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(ctor)) {
					didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(ctor);
					error("%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). This component defines getSnapshotBeforeUpdate() only.", getComponentNameFromType(ctor));
				}
				if (typeof instance.getDerivedStateFromProps === "function") error("%s: getDerivedStateFromProps() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
				if (typeof instance.getDerivedStateFromError === "function") error("%s: getDerivedStateFromError() is defined as an instance method and will be ignored. Instead, declare it as a static method.", name);
				if (typeof ctor.getSnapshotBeforeUpdate === "function") error("%s: getSnapshotBeforeUpdate() is defined as a static method and will be ignored. Instead, declare it as an instance method.", name);
				var _state = instance.state;
				if (_state && (typeof _state !== "object" || isArray(_state))) error("%s.state: must be set to an object or null", name);
				if (typeof instance.getChildContext === "function" && typeof ctor.childContextTypes !== "object") error("%s.getChildContext(): childContextTypes must be defined in order to use getChildContext().", name);
			}
			function adoptClassInstance(workInProgress, instance) {
				instance.updater = classComponentUpdater;
				workInProgress.stateNode = instance;
				set(instance, workInProgress);
				instance._reactInternalInstance = fakeInternalInstance;
			}
			function constructClassInstance(workInProgress, ctor, props) {
				var isLegacyContextConsumer = false;
				var unmaskedContext = emptyContextObject;
				var context = emptyContextObject;
				var contextType = ctor.contextType;
				if ("contextType" in ctor) {
					if (!(contextType === null || contextType !== void 0 && contextType.$$typeof === REACT_CONTEXT_TYPE && contextType._context === void 0) && !didWarnAboutInvalidateContextType.has(ctor)) {
						didWarnAboutInvalidateContextType.add(ctor);
						var addendum = "";
						if (contextType === void 0) addendum = " However, it is set to undefined. This can be caused by a typo or by mixing up named and default imports. This can also happen due to a circular dependency, so try moving the createContext() call to a separate file.";
						else if (typeof contextType !== "object") addendum = " However, it is set to a " + typeof contextType + ".";
						else if (contextType.$$typeof === REACT_PROVIDER_TYPE) addendum = " Did you accidentally pass the Context.Provider instead?";
						else if (contextType._context !== void 0) addendum = " Did you accidentally pass the Context.Consumer instead?";
						else addendum = " However, it is set to an object with keys {" + Object.keys(contextType).join(", ") + "}.";
						error("%s defines an invalid contextType. contextType should point to the Context object returned by React.createContext().%s", getComponentNameFromType(ctor) || "Component", addendum);
					}
				}
				if (typeof contextType === "object" && contextType !== null) context = readContext(contextType);
				else {
					unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
					var contextTypes = ctor.contextTypes;
					isLegacyContextConsumer = contextTypes !== null && contextTypes !== void 0;
					context = isLegacyContextConsumer ? getMaskedContext(workInProgress, unmaskedContext) : emptyContextObject;
				}
				var instance = new ctor(props, context);
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(true);
					try {
						instance = new ctor(props, context);
					} finally {
						setIsStrictModeForDevtools(false);
					}
				}
				var state = workInProgress.memoizedState = instance.state !== null && instance.state !== void 0 ? instance.state : null;
				adoptClassInstance(workInProgress, instance);
				if (typeof ctor.getDerivedStateFromProps === "function" && state === null) {
					var componentName = getComponentNameFromType(ctor) || "Component";
					if (!didWarnAboutUninitializedState.has(componentName)) {
						didWarnAboutUninitializedState.add(componentName);
						error("`%s` uses `getDerivedStateFromProps` but its initial state is %s. This is not recommended. Instead, define the initial state by assigning an object to `this.state` in the constructor of `%s`. This ensures that `getDerivedStateFromProps` arguments have a consistent shape.", componentName, instance.state === null ? "null" : "undefined", componentName);
					}
				}
				if (typeof ctor.getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function") {
					var foundWillMountName = null;
					var foundWillReceivePropsName = null;
					var foundWillUpdateName = null;
					if (typeof instance.componentWillMount === "function" && instance.componentWillMount.__suppressDeprecationWarning !== true) foundWillMountName = "componentWillMount";
					else if (typeof instance.UNSAFE_componentWillMount === "function") foundWillMountName = "UNSAFE_componentWillMount";
					if (typeof instance.componentWillReceiveProps === "function" && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) foundWillReceivePropsName = "componentWillReceiveProps";
					else if (typeof instance.UNSAFE_componentWillReceiveProps === "function") foundWillReceivePropsName = "UNSAFE_componentWillReceiveProps";
					if (typeof instance.componentWillUpdate === "function" && instance.componentWillUpdate.__suppressDeprecationWarning !== true) foundWillUpdateName = "componentWillUpdate";
					else if (typeof instance.UNSAFE_componentWillUpdate === "function") foundWillUpdateName = "UNSAFE_componentWillUpdate";
					if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
						var _componentName = getComponentNameFromType(ctor) || "Component";
						var newApiName = typeof ctor.getDerivedStateFromProps === "function" ? "getDerivedStateFromProps()" : "getSnapshotBeforeUpdate()";
						if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
							didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);
							error("Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\nThe above lifecycles should be removed. Learn more about this warning here:\nhttps://reactjs.org/link/unsafe-component-lifecycles", _componentName, newApiName, foundWillMountName !== null ? "\n  " + foundWillMountName : "", foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : "", foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : "");
						}
					}
				}
				if (isLegacyContextConsumer) cacheContext(workInProgress, unmaskedContext, context);
				return instance;
			}
			function callComponentWillMount(workInProgress, instance) {
				var oldState = instance.state;
				if (typeof instance.componentWillMount === "function") instance.componentWillMount();
				if (typeof instance.UNSAFE_componentWillMount === "function") instance.UNSAFE_componentWillMount();
				if (oldState !== instance.state) {
					error("%s.componentWillMount(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", getComponentNameFromFiber(workInProgress) || "Component");
					classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
				}
			}
			function callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext) {
				var oldState = instance.state;
				if (typeof instance.componentWillReceiveProps === "function") instance.componentWillReceiveProps(newProps, nextContext);
				if (typeof instance.UNSAFE_componentWillReceiveProps === "function") instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
				if (instance.state !== oldState) {
					var componentName = getComponentNameFromFiber(workInProgress) || "Component";
					if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
						didWarnAboutStateAssignmentForComponent.add(componentName);
						error("%s.componentWillReceiveProps(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", componentName);
					}
					classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
				}
			}
			function mountClassInstance(workInProgress, ctor, newProps, renderLanes) {
				checkClassInstance(workInProgress, ctor, newProps);
				var instance = workInProgress.stateNode;
				instance.props = newProps;
				instance.state = workInProgress.memoizedState;
				instance.refs = {};
				initializeUpdateQueue(workInProgress);
				var contextType = ctor.contextType;
				if (typeof contextType === "object" && contextType !== null) instance.context = readContext(contextType);
				else instance.context = getMaskedContext(workInProgress, getUnmaskedContext(workInProgress, ctor, true));
				if (instance.state === newProps) {
					var componentName = getComponentNameFromType(ctor) || "Component";
					if (!didWarnAboutDirectlyAssigningPropsToState.has(componentName)) {
						didWarnAboutDirectlyAssigningPropsToState.add(componentName);
						error("%s: It is not recommended to assign props directly to state because updates to props won't be reflected in state. In most cases, it is better to use props directly.", componentName);
					}
				}
				if (workInProgress.mode & StrictLegacyMode) ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, instance);
				ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, instance);
				instance.state = workInProgress.memoizedState;
				var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
				if (typeof getDerivedStateFromProps === "function") {
					applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
					instance.state = workInProgress.memoizedState;
				}
				if (typeof ctor.getDerivedStateFromProps !== "function" && typeof instance.getSnapshotBeforeUpdate !== "function" && (typeof instance.UNSAFE_componentWillMount === "function" || typeof instance.componentWillMount === "function")) {
					callComponentWillMount(workInProgress, instance);
					processUpdateQueue(workInProgress, newProps, instance, renderLanes);
					instance.state = workInProgress.memoizedState;
				}
				if (typeof instance.componentDidMount === "function") {
					var fiberFlags = Update;
					fiberFlags |= LayoutStatic;
					if ((workInProgress.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
					workInProgress.flags |= fiberFlags;
				}
			}
			function resumeMountClassInstance(workInProgress, ctor, newProps, renderLanes) {
				var instance = workInProgress.stateNode;
				var oldProps = workInProgress.memoizedProps;
				instance.props = oldProps;
				var oldContext = instance.context;
				var contextType = ctor.contextType;
				var nextContext = emptyContextObject;
				if (typeof contextType === "object" && contextType !== null) nextContext = readContext(contextType);
				else nextContext = getMaskedContext(workInProgress, getUnmaskedContext(workInProgress, ctor, true));
				var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
				var hasNewLifecycles = typeof getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function";
				if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === "function" || typeof instance.componentWillReceiveProps === "function")) {
					if (oldProps !== newProps || oldContext !== nextContext) callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
				}
				resetHasForceUpdateBeforeProcessing();
				var oldState = workInProgress.memoizedState;
				var newState = instance.state = oldState;
				processUpdateQueue(workInProgress, newProps, instance, renderLanes);
				newState = workInProgress.memoizedState;
				if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
					if (typeof instance.componentDidMount === "function") {
						var fiberFlags = Update;
						fiberFlags |= LayoutStatic;
						if ((workInProgress.mode & StrictEffectsMode) !== NoMode) fiberFlags |= MountLayoutDev;
						workInProgress.flags |= fiberFlags;
					}
					return false;
				}
				if (typeof getDerivedStateFromProps === "function") {
					applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
					newState = workInProgress.memoizedState;
				}
				var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);
				if (shouldUpdate) {
					if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === "function" || typeof instance.componentWillMount === "function")) {
						if (typeof instance.componentWillMount === "function") instance.componentWillMount();
						if (typeof instance.UNSAFE_componentWillMount === "function") instance.UNSAFE_componentWillMount();
					}
					if (typeof instance.componentDidMount === "function") {
						var _fiberFlags = Update;
						_fiberFlags |= LayoutStatic;
						if ((workInProgress.mode & StrictEffectsMode) !== NoMode) _fiberFlags |= MountLayoutDev;
						workInProgress.flags |= _fiberFlags;
					}
				} else {
					if (typeof instance.componentDidMount === "function") {
						var _fiberFlags2 = Update;
						_fiberFlags2 |= LayoutStatic;
						if ((workInProgress.mode & StrictEffectsMode) !== NoMode) _fiberFlags2 |= MountLayoutDev;
						workInProgress.flags |= _fiberFlags2;
					}
					workInProgress.memoizedProps = newProps;
					workInProgress.memoizedState = newState;
				}
				instance.props = newProps;
				instance.state = newState;
				instance.context = nextContext;
				return shouldUpdate;
			}
			function updateClassInstance(current, workInProgress, ctor, newProps, renderLanes) {
				var instance = workInProgress.stateNode;
				cloneUpdateQueue(current, workInProgress);
				var unresolvedOldProps = workInProgress.memoizedProps;
				var oldProps = workInProgress.type === workInProgress.elementType ? unresolvedOldProps : resolveDefaultProps(workInProgress.type, unresolvedOldProps);
				instance.props = oldProps;
				var unresolvedNewProps = workInProgress.pendingProps;
				var oldContext = instance.context;
				var contextType = ctor.contextType;
				var nextContext = emptyContextObject;
				if (typeof contextType === "object" && contextType !== null) nextContext = readContext(contextType);
				else nextContext = getMaskedContext(workInProgress, getUnmaskedContext(workInProgress, ctor, true));
				var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
				var hasNewLifecycles = typeof getDerivedStateFromProps === "function" || typeof instance.getSnapshotBeforeUpdate === "function";
				if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === "function" || typeof instance.componentWillReceiveProps === "function")) {
					if (unresolvedOldProps !== unresolvedNewProps || oldContext !== nextContext) callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
				}
				resetHasForceUpdateBeforeProcessing();
				var oldState = workInProgress.memoizedState;
				var newState = instance.state = oldState;
				processUpdateQueue(workInProgress, newProps, instance, renderLanes);
				newState = workInProgress.memoizedState;
				if (unresolvedOldProps === unresolvedNewProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing() && !enableLazyContextPropagation) {
					if (typeof instance.componentDidUpdate === "function") {
						if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Update;
					}
					if (typeof instance.getSnapshotBeforeUpdate === "function") {
						if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Snapshot;
					}
					return false;
				}
				if (typeof getDerivedStateFromProps === "function") {
					applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
					newState = workInProgress.memoizedState;
				}
				var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) || enableLazyContextPropagation;
				if (shouldUpdate) {
					if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === "function" || typeof instance.componentWillUpdate === "function")) {
						if (typeof instance.componentWillUpdate === "function") instance.componentWillUpdate(newProps, newState, nextContext);
						if (typeof instance.UNSAFE_componentWillUpdate === "function") instance.UNSAFE_componentWillUpdate(newProps, newState, nextContext);
					}
					if (typeof instance.componentDidUpdate === "function") workInProgress.flags |= Update;
					if (typeof instance.getSnapshotBeforeUpdate === "function") workInProgress.flags |= Snapshot;
				} else {
					if (typeof instance.componentDidUpdate === "function") {
						if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Update;
					}
					if (typeof instance.getSnapshotBeforeUpdate === "function") {
						if (unresolvedOldProps !== current.memoizedProps || oldState !== current.memoizedState) workInProgress.flags |= Snapshot;
					}
					workInProgress.memoizedProps = newProps;
					workInProgress.memoizedState = newState;
				}
				instance.props = newProps;
				instance.state = newState;
				instance.context = nextContext;
				return shouldUpdate;
			}
			function createCapturedValueAtFiber(value, source) {
				return {
					value,
					source,
					stack: getStackByFiberInDevAndProd(source),
					digest: null
				};
			}
			function createCapturedValue(value, digest, stack) {
				return {
					value,
					source: null,
					stack: stack != null ? stack : null,
					digest: digest != null ? digest : null
				};
			}
			function showErrorDialog(boundary, errorInfo) {
				return true;
			}
			function logCapturedError(boundary, errorInfo) {
				try {
					if (showErrorDialog(boundary, errorInfo) === false) return;
					var error = errorInfo.value;
					var source = errorInfo.source;
					var stack = errorInfo.stack;
					var componentStack = stack !== null ? stack : "";
					if (error != null && error._suppressLogging) {
						if (boundary.tag === ClassComponent) return;
						console["error"](error);
					}
					var componentName = source ? getComponentNameFromFiber(source) : null;
					var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component:" : "The above error occurred in one of your React components:";
					var errorBoundaryMessage;
					if (boundary.tag === HostRoot) errorBoundaryMessage = "Consider adding an error boundary to your tree to customize error handling behavior.\nVisit https://reactjs.org/link/error-boundaries to learn more about error boundaries.";
					else errorBoundaryMessage = "React will try to recreate this component tree from scratch " + ("using the error boundary you provided, " + (getComponentNameFromFiber(boundary) || "Anonymous") + ".");
					var combinedMessage = componentNameMessage + "\n" + componentStack + "\n\n" + ("" + errorBoundaryMessage);
					console["error"](combinedMessage);
				} catch (e) {
					setTimeout(function() {
						throw e;
					});
				}
			}
			var PossiblyWeakMap$1 = typeof WeakMap === "function" ? WeakMap : Map;
			function createRootErrorUpdate(fiber, errorInfo, lane) {
				var update = createUpdate(NoTimestamp, lane);
				update.tag = CaptureUpdate;
				update.payload = { element: null };
				var error = errorInfo.value;
				update.callback = function() {
					onUncaughtError(error);
					logCapturedError(fiber, errorInfo);
				};
				return update;
			}
			function createClassErrorUpdate(fiber, errorInfo, lane) {
				var update = createUpdate(NoTimestamp, lane);
				update.tag = CaptureUpdate;
				var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
				if (typeof getDerivedStateFromError === "function") {
					var error$1 = errorInfo.value;
					update.payload = function() {
						return getDerivedStateFromError(error$1);
					};
					update.callback = function() {
						markFailedErrorBoundaryForHotReloading(fiber);
						logCapturedError(fiber, errorInfo);
					};
				}
				var inst = fiber.stateNode;
				if (inst !== null && typeof inst.componentDidCatch === "function") update.callback = function callback() {
					markFailedErrorBoundaryForHotReloading(fiber);
					logCapturedError(fiber, errorInfo);
					if (typeof getDerivedStateFromError !== "function") markLegacyErrorBoundaryAsFailed(this);
					var error$1 = errorInfo.value;
					var stack = errorInfo.stack;
					this.componentDidCatch(error$1, { componentStack: stack !== null ? stack : "" });
					if (typeof getDerivedStateFromError !== "function") {
						if (!includesSomeLane(fiber.lanes, SyncLane)) error("%s: Error boundaries should implement getDerivedStateFromError(). In that method, return a state update to display an error message or fallback UI.", getComponentNameFromFiber(fiber) || "Unknown");
					}
				};
				return update;
			}
			function attachPingListener(root, wakeable, lanes) {
				var pingCache = root.pingCache;
				var threadIDs;
				if (pingCache === null) {
					pingCache = root.pingCache = new PossiblyWeakMap$1();
					threadIDs = /* @__PURE__ */ new Set();
					pingCache.set(wakeable, threadIDs);
				} else {
					threadIDs = pingCache.get(wakeable);
					if (threadIDs === void 0) {
						threadIDs = /* @__PURE__ */ new Set();
						pingCache.set(wakeable, threadIDs);
					}
				}
				if (!threadIDs.has(lanes)) {
					threadIDs.add(lanes);
					var ping = pingSuspendedRoot.bind(null, root, wakeable, lanes);
					if (isDevToolsPresent) restorePendingUpdaters(root, lanes);
					wakeable.then(ping, ping);
				}
			}
			function attachRetryListener(suspenseBoundary, root, wakeable, lanes) {
				var wakeables = suspenseBoundary.updateQueue;
				if (wakeables === null) {
					var updateQueue = /* @__PURE__ */ new Set();
					updateQueue.add(wakeable);
					suspenseBoundary.updateQueue = updateQueue;
				} else wakeables.add(wakeable);
			}
			function resetSuspendedComponent(sourceFiber, rootRenderLanes) {
				var tag = sourceFiber.tag;
				if ((sourceFiber.mode & ConcurrentMode) === NoMode && (tag === FunctionComponent || tag === ForwardRef || tag === SimpleMemoComponent)) {
					var currentSource = sourceFiber.alternate;
					if (currentSource) {
						sourceFiber.updateQueue = currentSource.updateQueue;
						sourceFiber.memoizedState = currentSource.memoizedState;
						sourceFiber.lanes = currentSource.lanes;
					} else {
						sourceFiber.updateQueue = null;
						sourceFiber.memoizedState = null;
					}
				}
			}
			function getNearestSuspenseBoundaryToCapture(returnFiber) {
				var node = returnFiber;
				do {
					if (node.tag === SuspenseComponent && shouldCaptureSuspense(node)) return node;
					node = node.return;
				} while (node !== null);
				return null;
			}
			function markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes) {
				if ((suspenseBoundary.mode & ConcurrentMode) === NoMode) {
					if (suspenseBoundary === returnFiber) suspenseBoundary.flags |= ShouldCapture;
					else {
						suspenseBoundary.flags |= DidCapture;
						sourceFiber.flags |= ForceUpdateForLegacySuspense;
						sourceFiber.flags &= ~(LifecycleEffectMask | Incomplete);
						if (sourceFiber.tag === ClassComponent) {
							if (sourceFiber.alternate === null) sourceFiber.tag = IncompleteClassComponent;
							else {
								var update = createUpdate(NoTimestamp, SyncLane);
								update.tag = ForceUpdate;
								enqueueUpdate(sourceFiber, update, SyncLane);
							}
						}
						sourceFiber.lanes = mergeLanes(sourceFiber.lanes, SyncLane);
					}
					return suspenseBoundary;
				}
				suspenseBoundary.flags |= ShouldCapture;
				suspenseBoundary.lanes = rootRenderLanes;
				return suspenseBoundary;
			}
			function throwException(root, returnFiber, sourceFiber, value, rootRenderLanes) {
				sourceFiber.flags |= Incomplete;
				if (isDevToolsPresent) restorePendingUpdaters(root, rootRenderLanes);
				if (value !== null && typeof value === "object" && typeof value.then === "function") {
					var wakeable = value;
					resetSuspendedComponent(sourceFiber);
					if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) markDidThrowWhileHydratingDEV();
					var suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber);
					if (suspenseBoundary !== null) {
						suspenseBoundary.flags &= ~ForceClientRender;
						markSuspenseBoundaryShouldCapture(suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes);
						if (suspenseBoundary.mode & ConcurrentMode) attachPingListener(root, wakeable, rootRenderLanes);
						attachRetryListener(suspenseBoundary, root, wakeable);
						return;
					} else {
						if (!includesSyncLane(rootRenderLanes)) {
							attachPingListener(root, wakeable, rootRenderLanes);
							renderDidSuspendDelayIfPossible();
							return;
						}
						value = /* @__PURE__ */ new Error("A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition.");
					}
				} else if (getIsHydrating() && sourceFiber.mode & ConcurrentMode) {
					markDidThrowWhileHydratingDEV();
					var _suspenseBoundary = getNearestSuspenseBoundaryToCapture(returnFiber);
					if (_suspenseBoundary !== null) {
						if ((_suspenseBoundary.flags & ShouldCapture) === NoFlags) _suspenseBoundary.flags |= ForceClientRender;
						markSuspenseBoundaryShouldCapture(_suspenseBoundary, returnFiber, sourceFiber, root, rootRenderLanes);
						queueHydrationError(createCapturedValueAtFiber(value, sourceFiber));
						return;
					}
				}
				value = createCapturedValueAtFiber(value, sourceFiber);
				renderDidError(value);
				var workInProgress = returnFiber;
				do {
					switch (workInProgress.tag) {
						case HostRoot:
							var _errorInfo = value;
							workInProgress.flags |= ShouldCapture;
							var lane = pickArbitraryLane(rootRenderLanes);
							workInProgress.lanes = mergeLanes(workInProgress.lanes, lane);
							var update = createRootErrorUpdate(workInProgress, _errorInfo, lane);
							enqueueCapturedUpdate(workInProgress, update);
							return;
						case ClassComponent:
							var errorInfo = value;
							var ctor = workInProgress.type;
							var instance = workInProgress.stateNode;
							if ((workInProgress.flags & DidCapture) === NoFlags && (typeof ctor.getDerivedStateFromError === "function" || instance !== null && typeof instance.componentDidCatch === "function" && !isAlreadyFailedLegacyErrorBoundary(instance))) {
								workInProgress.flags |= ShouldCapture;
								var _lane = pickArbitraryLane(rootRenderLanes);
								workInProgress.lanes = mergeLanes(workInProgress.lanes, _lane);
								var _update = createClassErrorUpdate(workInProgress, errorInfo, _lane);
								enqueueCapturedUpdate(workInProgress, _update);
								return;
							}
					}
					workInProgress = workInProgress.return;
				} while (workInProgress !== null);
			}
			function getSuspendedCache() {
				return null;
			}
			var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
			var didReceiveUpdate = false;
			var didWarnAboutBadClass;
			var didWarnAboutModulePatternComponent;
			var didWarnAboutContextTypeOnFunctionComponent;
			var didWarnAboutGetDerivedStateOnFunctionComponent;
			var didWarnAboutFunctionRefs;
			var didWarnAboutReassigningProps;
			var didWarnAboutRevealOrder;
			var didWarnAboutTailOptions;
			var didWarnAboutDefaultPropsOnFunctionComponent;
			didWarnAboutBadClass = {};
			didWarnAboutModulePatternComponent = {};
			didWarnAboutContextTypeOnFunctionComponent = {};
			didWarnAboutGetDerivedStateOnFunctionComponent = {};
			didWarnAboutFunctionRefs = {};
			didWarnAboutReassigningProps = false;
			didWarnAboutRevealOrder = {};
			didWarnAboutTailOptions = {};
			didWarnAboutDefaultPropsOnFunctionComponent = {};
			function reconcileChildren(current, workInProgress, nextChildren, renderLanes) {
				if (current === null) workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderLanes);
				else workInProgress.child = reconcileChildFibers(workInProgress, current.child, nextChildren, renderLanes);
			}
			function forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderLanes) {
				workInProgress.child = reconcileChildFibers(workInProgress, current.child, null, renderLanes);
				workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderLanes);
			}
			function updateForwardRef(current, workInProgress, Component, nextProps, renderLanes) {
				if (workInProgress.type !== workInProgress.elementType) {
					var innerPropTypes = Component.propTypes;
					if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, "prop", getComponentNameFromType(Component));
				}
				var render = Component.render;
				var ref = workInProgress.ref;
				var nextChildren;
				var hasId;
				prepareToReadContext(workInProgress, renderLanes);
				markComponentRenderStarted(workInProgress);
				ReactCurrentOwner$1.current = workInProgress;
				setIsRendering(true);
				nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderLanes);
				hasId = checkDidRenderIdHook();
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(true);
					try {
						nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderLanes);
						hasId = checkDidRenderIdHook();
					} finally {
						setIsStrictModeForDevtools(false);
					}
				}
				setIsRendering(false);
				markComponentRenderStopped();
				if (current !== null && !didReceiveUpdate) {
					bailoutHooks(current, workInProgress, renderLanes);
					return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
				}
				if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
				workInProgress.flags |= PerformedWork;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
				if (current === null) {
					var type = Component.type;
					if (isSimpleFunctionComponent(type) && Component.compare === null && Component.defaultProps === void 0) {
						var resolvedType = type;
						resolvedType = resolveFunctionForHotReloading(type);
						workInProgress.tag = SimpleMemoComponent;
						workInProgress.type = resolvedType;
						validateFunctionComponentInDev(workInProgress, type);
						return updateSimpleMemoComponent(current, workInProgress, resolvedType, nextProps, renderLanes);
					}
					var innerPropTypes = type.propTypes;
					if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, "prop", getComponentNameFromType(type));
					if (Component.defaultProps !== void 0) {
						var componentName = getComponentNameFromType(type) || "Unknown";
						if (!didWarnAboutDefaultPropsOnFunctionComponent[componentName]) {
							error("%s: Support for defaultProps will be removed from memo components in a future major release. Use JavaScript default parameters instead.", componentName);
							didWarnAboutDefaultPropsOnFunctionComponent[componentName] = true;
						}
					}
					var child = createFiberFromTypeAndProps(Component.type, null, nextProps, workInProgress, workInProgress.mode, renderLanes);
					child.ref = workInProgress.ref;
					child.return = workInProgress;
					workInProgress.child = child;
					return child;
				}
				var _type = Component.type;
				var _innerPropTypes = _type.propTypes;
				if (_innerPropTypes) checkPropTypes(_innerPropTypes, nextProps, "prop", getComponentNameFromType(_type));
				var currentChild = current.child;
				if (!checkScheduledUpdateOrContext(current, renderLanes)) {
					var prevProps = currentChild.memoizedProps;
					var compare = Component.compare;
					compare = compare !== null ? compare : shallowEqual;
					if (compare(prevProps, nextProps) && current.ref === workInProgress.ref) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
				}
				workInProgress.flags |= PerformedWork;
				var newChild = createWorkInProgress(currentChild, nextProps);
				newChild.ref = workInProgress.ref;
				newChild.return = workInProgress;
				workInProgress.child = newChild;
				return newChild;
			}
			function updateSimpleMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
				if (workInProgress.type !== workInProgress.elementType) {
					var outerMemoType = workInProgress.elementType;
					if (outerMemoType.$$typeof === REACT_LAZY_TYPE) {
						var lazyComponent = outerMemoType;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							outerMemoType = init(payload);
						} catch (x) {
							outerMemoType = null;
						}
						var outerPropTypes = outerMemoType && outerMemoType.propTypes;
						if (outerPropTypes) checkPropTypes(outerPropTypes, nextProps, "prop", getComponentNameFromType(outerMemoType));
					}
				}
				if (current !== null) {
					var prevProps = current.memoizedProps;
					if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress.ref && workInProgress.type === current.type) {
						didReceiveUpdate = false;
						workInProgress.pendingProps = nextProps = prevProps;
						if (!checkScheduledUpdateOrContext(current, renderLanes)) {
							workInProgress.lanes = current.lanes;
							return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
						} else if ((current.flags & ForceUpdateForLegacySuspense) !== NoFlags) didReceiveUpdate = true;
					}
				}
				return updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes);
			}
			function updateOffscreenComponent(current, workInProgress, renderLanes) {
				var nextProps = workInProgress.pendingProps;
				var nextChildren = nextProps.children;
				var prevState = current !== null ? current.memoizedState : null;
				if (nextProps.mode === "hidden" || enableLegacyHidden) {
					if ((workInProgress.mode & ConcurrentMode) === NoMode) {
						workInProgress.memoizedState = {
							baseLanes: NoLanes,
							cachePool: null,
							transitions: null
						};
						pushRenderLanes(workInProgress, renderLanes);
					} else if (!includesSomeLane(renderLanes, OffscreenLane)) {
						var spawnedCachePool = null;
						var nextBaseLanes;
						if (prevState !== null) {
							var prevBaseLanes = prevState.baseLanes;
							nextBaseLanes = mergeLanes(prevBaseLanes, renderLanes);
						} else nextBaseLanes = renderLanes;
						workInProgress.lanes = workInProgress.childLanes = laneToLanes(OffscreenLane);
						workInProgress.memoizedState = {
							baseLanes: nextBaseLanes,
							cachePool: spawnedCachePool,
							transitions: null
						};
						workInProgress.updateQueue = null;
						pushRenderLanes(workInProgress, nextBaseLanes);
						return null;
					} else {
						workInProgress.memoizedState = {
							baseLanes: NoLanes,
							cachePool: null,
							transitions: null
						};
						pushRenderLanes(workInProgress, prevState !== null ? prevState.baseLanes : renderLanes);
					}
				} else {
					var _subtreeRenderLanes;
					if (prevState !== null) {
						_subtreeRenderLanes = mergeLanes(prevState.baseLanes, renderLanes);
						workInProgress.memoizedState = null;
					} else _subtreeRenderLanes = renderLanes;
					pushRenderLanes(workInProgress, _subtreeRenderLanes);
				}
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateFragment(current, workInProgress, renderLanes) {
				var nextChildren = workInProgress.pendingProps;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateMode(current, workInProgress, renderLanes) {
				var nextChildren = workInProgress.pendingProps.children;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateProfiler(current, workInProgress, renderLanes) {
				workInProgress.flags |= Update;
				var stateNode = workInProgress.stateNode;
				stateNode.effectDuration = 0;
				stateNode.passiveEffectDuration = 0;
				var nextChildren = workInProgress.pendingProps.children;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function markRef(current, workInProgress) {
				var ref = workInProgress.ref;
				if (current === null && ref !== null || current !== null && current.ref !== ref) {
					workInProgress.flags |= Ref;
					workInProgress.flags |= RefStatic;
				}
			}
			function updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes) {
				if (workInProgress.type !== workInProgress.elementType) {
					var innerPropTypes = Component.propTypes;
					if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, "prop", getComponentNameFromType(Component));
				}
				var context = getMaskedContext(workInProgress, getUnmaskedContext(workInProgress, Component, true));
				var nextChildren;
				var hasId;
				prepareToReadContext(workInProgress, renderLanes);
				markComponentRenderStarted(workInProgress);
				ReactCurrentOwner$1.current = workInProgress;
				setIsRendering(true);
				nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderLanes);
				hasId = checkDidRenderIdHook();
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(true);
					try {
						nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderLanes);
						hasId = checkDidRenderIdHook();
					} finally {
						setIsStrictModeForDevtools(false);
					}
				}
				setIsRendering(false);
				markComponentRenderStopped();
				if (current !== null && !didReceiveUpdate) {
					bailoutHooks(current, workInProgress, renderLanes);
					return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
				}
				if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
				workInProgress.flags |= PerformedWork;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateClassComponent(current, workInProgress, Component, nextProps, renderLanes) {
				switch (shouldError(workInProgress)) {
					case false:
						var _instance = workInProgress.stateNode;
						var ctor = workInProgress.type;
						var state = new ctor(workInProgress.memoizedProps, _instance.context).state;
						_instance.updater.enqueueSetState(_instance, state, null);
						break;
					case true:
						workInProgress.flags |= DidCapture;
						workInProgress.flags |= ShouldCapture;
						var error$1 = /* @__PURE__ */ new Error("Simulated error coming from DevTools");
						var lane = pickArbitraryLane(renderLanes);
						workInProgress.lanes = mergeLanes(workInProgress.lanes, lane);
						enqueueCapturedUpdate(workInProgress, createClassErrorUpdate(workInProgress, createCapturedValueAtFiber(error$1, workInProgress), lane));
				}
				if (workInProgress.type !== workInProgress.elementType) {
					var innerPropTypes = Component.propTypes;
					if (innerPropTypes) checkPropTypes(innerPropTypes, nextProps, "prop", getComponentNameFromType(Component));
				}
				var hasContext;
				if (isContextProvider(Component)) {
					hasContext = true;
					pushContextProvider(workInProgress);
				} else hasContext = false;
				prepareToReadContext(workInProgress, renderLanes);
				var instance = workInProgress.stateNode;
				var shouldUpdate;
				if (instance === null) {
					resetSuspendedCurrentOnMountInLegacyMode(current, workInProgress);
					constructClassInstance(workInProgress, Component, nextProps);
					mountClassInstance(workInProgress, Component, nextProps, renderLanes);
					shouldUpdate = true;
				} else if (current === null) shouldUpdate = resumeMountClassInstance(workInProgress, Component, nextProps, renderLanes);
				else shouldUpdate = updateClassInstance(current, workInProgress, Component, nextProps, renderLanes);
				var nextUnitOfWork = finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderLanes);
				var inst = workInProgress.stateNode;
				if (shouldUpdate && inst.props !== nextProps) {
					if (!didWarnAboutReassigningProps) error("It looks like %s is reassigning its own `this.props` while rendering. This is not supported and can lead to confusing bugs.", getComponentNameFromFiber(workInProgress) || "a component");
					didWarnAboutReassigningProps = true;
				}
				return nextUnitOfWork;
			}
			function finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderLanes) {
				markRef(current, workInProgress);
				var didCaptureError = (workInProgress.flags & DidCapture) !== NoFlags;
				if (!shouldUpdate && !didCaptureError) {
					if (hasContext) invalidateContextProvider(workInProgress, Component, false);
					return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
				}
				var instance = workInProgress.stateNode;
				ReactCurrentOwner$1.current = workInProgress;
				var nextChildren;
				if (didCaptureError && typeof Component.getDerivedStateFromError !== "function") {
					nextChildren = null;
					stopProfilerTimerIfRunning();
				} else {
					markComponentRenderStarted(workInProgress);
					setIsRendering(true);
					nextChildren = instance.render();
					if (workInProgress.mode & StrictLegacyMode) {
						setIsStrictModeForDevtools(true);
						try {
							instance.render();
						} finally {
							setIsStrictModeForDevtools(false);
						}
					}
					setIsRendering(false);
					markComponentRenderStopped();
				}
				workInProgress.flags |= PerformedWork;
				if (current !== null && didCaptureError) forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderLanes);
				else reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				workInProgress.memoizedState = instance.state;
				if (hasContext) invalidateContextProvider(workInProgress, Component, true);
				return workInProgress.child;
			}
			function pushHostRootContext(workInProgress) {
				var root = workInProgress.stateNode;
				if (root.pendingContext) pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
				else if (root.context) pushTopLevelContextObject(workInProgress, root.context, false);
				pushHostContainer(workInProgress, root.containerInfo);
			}
			function updateHostRoot(current, workInProgress, renderLanes) {
				pushHostRootContext(workInProgress);
				if (current === null) throw new Error("Should have a current fiber. This is a bug in React.");
				var nextProps = workInProgress.pendingProps;
				var prevState = workInProgress.memoizedState;
				var prevChildren = prevState.element;
				cloneUpdateQueue(current, workInProgress);
				processUpdateQueue(workInProgress, nextProps, null, renderLanes);
				var nextState = workInProgress.memoizedState;
				workInProgress.stateNode;
				var nextChildren = nextState.element;
				if (prevState.isDehydrated) {
					var overrideState = {
						element: nextChildren,
						isDehydrated: false,
						cache: nextState.cache,
						pendingSuspenseBoundaries: nextState.pendingSuspenseBoundaries,
						transitions: nextState.transitions
					};
					var updateQueue = workInProgress.updateQueue;
					updateQueue.baseState = overrideState;
					workInProgress.memoizedState = overrideState;
					if (workInProgress.flags & ForceClientRender) return mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, createCapturedValueAtFiber(/* @__PURE__ */ new Error("There was an error while hydrating. Because the error happened outside of a Suspense boundary, the entire root will switch to client rendering."), workInProgress));
					else if (nextChildren !== prevChildren) return mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, createCapturedValueAtFiber(/* @__PURE__ */ new Error("This root received an early update, before anything was able hydrate. Switched the entire root to client rendering."), workInProgress));
					else {
						enterHydrationState(workInProgress);
						var child = mountChildFibers(workInProgress, null, nextChildren, renderLanes);
						workInProgress.child = child;
						var node = child;
						while (node) {
							node.flags = node.flags & ~Placement | Hydrating;
							node = node.sibling;
						}
					}
				} else {
					resetHydrationState();
					if (nextChildren === prevChildren) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
					reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				}
				return workInProgress.child;
			}
			function mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes, recoverableError) {
				resetHydrationState();
				queueHydrationError(recoverableError);
				workInProgress.flags |= ForceClientRender;
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateHostComponent(current, workInProgress, renderLanes) {
				pushHostContext(workInProgress);
				if (current === null) tryToClaimNextHydratableInstance(workInProgress);
				var type = workInProgress.type;
				var nextProps = workInProgress.pendingProps;
				var prevProps = current !== null ? current.memoizedProps : null;
				var nextChildren = nextProps.children;
				if (shouldSetTextContent(type, nextProps)) nextChildren = null;
				else if (prevProps !== null && shouldSetTextContent(type, prevProps)) workInProgress.flags |= ContentReset;
				markRef(current, workInProgress);
				reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			function updateHostText(current, workInProgress) {
				if (current === null) tryToClaimNextHydratableInstance(workInProgress);
				return null;
			}
			function mountLazyComponent(_current, workInProgress, elementType, renderLanes) {
				resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress);
				var props = workInProgress.pendingProps;
				var lazyComponent = elementType;
				var payload = lazyComponent._payload;
				var init = lazyComponent._init;
				var Component = init(payload);
				workInProgress.type = Component;
				var resolvedTag = workInProgress.tag = resolveLazyComponentTag(Component);
				var resolvedProps = resolveDefaultProps(Component, props);
				var child;
				switch (resolvedTag) {
					case FunctionComponent:
						validateFunctionComponentInDev(workInProgress, Component);
						workInProgress.type = Component = resolveFunctionForHotReloading(Component);
						child = updateFunctionComponent(null, workInProgress, Component, resolvedProps, renderLanes);
						return child;
					case ClassComponent:
						workInProgress.type = Component = resolveClassForHotReloading(Component);
						child = updateClassComponent(null, workInProgress, Component, resolvedProps, renderLanes);
						return child;
					case ForwardRef:
						workInProgress.type = Component = resolveForwardRefForHotReloading(Component);
						child = updateForwardRef(null, workInProgress, Component, resolvedProps, renderLanes);
						return child;
					case MemoComponent:
						if (workInProgress.type !== workInProgress.elementType) {
							var outerPropTypes = Component.propTypes;
							if (outerPropTypes) checkPropTypes(outerPropTypes, resolvedProps, "prop", getComponentNameFromType(Component));
						}
						child = updateMemoComponent(null, workInProgress, Component, resolveDefaultProps(Component.type, resolvedProps), renderLanes);
						return child;
				}
				var hint = "";
				if (Component !== null && typeof Component === "object" && Component.$$typeof === REACT_LAZY_TYPE) hint = " Did you wrap a component in React.lazy() more than once?";
				throw new Error("Element type is invalid. Received a promise that resolves to: " + Component + ". " + ("Lazy element type must resolve to a class or function." + hint));
			}
			function mountIncompleteClassComponent(_current, workInProgress, Component, nextProps, renderLanes) {
				resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress);
				workInProgress.tag = ClassComponent;
				var hasContext;
				if (isContextProvider(Component)) {
					hasContext = true;
					pushContextProvider(workInProgress);
				} else hasContext = false;
				prepareToReadContext(workInProgress, renderLanes);
				constructClassInstance(workInProgress, Component, nextProps);
				mountClassInstance(workInProgress, Component, nextProps, renderLanes);
				return finishClassComponent(null, workInProgress, Component, true, hasContext, renderLanes);
			}
			function mountIndeterminateComponent(_current, workInProgress, Component, renderLanes) {
				resetSuspendedCurrentOnMountInLegacyMode(_current, workInProgress);
				var props = workInProgress.pendingProps;
				var context = getMaskedContext(workInProgress, getUnmaskedContext(workInProgress, Component, false));
				prepareToReadContext(workInProgress, renderLanes);
				var value;
				var hasId;
				markComponentRenderStarted(workInProgress);
				if (Component.prototype && typeof Component.prototype.render === "function") {
					var componentName = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutBadClass[componentName]) {
						error("The <%s /> component appears to have a render method, but doesn't extend React.Component. This is likely to cause errors. Change %s to extend React.Component instead.", componentName, componentName);
						didWarnAboutBadClass[componentName] = true;
					}
				}
				if (workInProgress.mode & StrictLegacyMode) ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, null);
				setIsRendering(true);
				ReactCurrentOwner$1.current = workInProgress;
				value = renderWithHooks(null, workInProgress, Component, props, context, renderLanes);
				hasId = checkDidRenderIdHook();
				setIsRendering(false);
				markComponentRenderStopped();
				workInProgress.flags |= PerformedWork;
				if (typeof value === "object" && value !== null && typeof value.render === "function" && value.$$typeof === void 0) {
					var _componentName = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutModulePatternComponent[_componentName]) {
						error("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName, _componentName, _componentName);
						didWarnAboutModulePatternComponent[_componentName] = true;
					}
				}
				if (typeof value === "object" && value !== null && typeof value.render === "function" && value.$$typeof === void 0) {
					var _componentName2 = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutModulePatternComponent[_componentName2]) {
						error("The <%s /> component appears to be a function component that returns a class instance. Change %s to a class that extends React.Component instead. If you can't use a class try assigning the prototype on the function as a workaround. `%s.prototype = React.Component.prototype`. Don't use an arrow function since it cannot be called with `new` by React.", _componentName2, _componentName2, _componentName2);
						didWarnAboutModulePatternComponent[_componentName2] = true;
					}
					workInProgress.tag = ClassComponent;
					workInProgress.memoizedState = null;
					workInProgress.updateQueue = null;
					var hasContext = false;
					if (isContextProvider(Component)) {
						hasContext = true;
						pushContextProvider(workInProgress);
					} else hasContext = false;
					workInProgress.memoizedState = value.state !== null && value.state !== void 0 ? value.state : null;
					initializeUpdateQueue(workInProgress);
					adoptClassInstance(workInProgress, value);
					mountClassInstance(workInProgress, Component, props, renderLanes);
					return finishClassComponent(null, workInProgress, Component, true, hasContext, renderLanes);
				} else {
					workInProgress.tag = FunctionComponent;
					if (workInProgress.mode & StrictLegacyMode) {
						setIsStrictModeForDevtools(true);
						try {
							value = renderWithHooks(null, workInProgress, Component, props, context, renderLanes);
							hasId = checkDidRenderIdHook();
						} finally {
							setIsStrictModeForDevtools(false);
						}
					}
					if (getIsHydrating() && hasId) pushMaterializedTreeId(workInProgress);
					reconcileChildren(null, workInProgress, value, renderLanes);
					validateFunctionComponentInDev(workInProgress, Component);
					return workInProgress.child;
				}
			}
			function validateFunctionComponentInDev(workInProgress, Component) {
				if (Component) {
					if (Component.childContextTypes) error("%s(...): childContextTypes cannot be defined on a function component.", Component.displayName || Component.name || "Component");
				}
				if (workInProgress.ref !== null) {
					var info = "";
					var ownerName = getCurrentFiberOwnerNameInDevOrNull();
					if (ownerName) info += "\n\nCheck the render method of `" + ownerName + "`.";
					var warningKey = ownerName || "";
					var debugSource = workInProgress._debugSource;
					if (debugSource) warningKey = debugSource.fileName + ":" + debugSource.lineNumber;
					if (!didWarnAboutFunctionRefs[warningKey]) {
						didWarnAboutFunctionRefs[warningKey] = true;
						error("Function components cannot be given refs. Attempts to access this ref will fail. Did you mean to use React.forwardRef()?%s", info);
					}
				}
				if (Component.defaultProps !== void 0) {
					var componentName = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutDefaultPropsOnFunctionComponent[componentName]) {
						error("%s: Support for defaultProps will be removed from function components in a future major release. Use JavaScript default parameters instead.", componentName);
						didWarnAboutDefaultPropsOnFunctionComponent[componentName] = true;
					}
				}
				if (typeof Component.getDerivedStateFromProps === "function") {
					var _componentName3 = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3]) {
						error("%s: Function components do not support getDerivedStateFromProps.", _componentName3);
						didWarnAboutGetDerivedStateOnFunctionComponent[_componentName3] = true;
					}
				}
				if (typeof Component.contextType === "object" && Component.contextType !== null) {
					var _componentName4 = getComponentNameFromType(Component) || "Unknown";
					if (!didWarnAboutContextTypeOnFunctionComponent[_componentName4]) {
						error("%s: Function components do not support contextType.", _componentName4);
						didWarnAboutContextTypeOnFunctionComponent[_componentName4] = true;
					}
				}
			}
			var SUSPENDED_MARKER = {
				dehydrated: null,
				treeContext: null,
				retryLane: NoLane
			};
			function mountSuspenseOffscreenState(renderLanes) {
				return {
					baseLanes: renderLanes,
					cachePool: getSuspendedCache(),
					transitions: null
				};
			}
			function updateSuspenseOffscreenState(prevOffscreenState, renderLanes) {
				return {
					baseLanes: mergeLanes(prevOffscreenState.baseLanes, renderLanes),
					cachePool: null,
					transitions: prevOffscreenState.transitions
				};
			}
			function shouldRemainOnFallback(suspenseContext, current, workInProgress, renderLanes) {
				if (current !== null) {
					if (current.memoizedState === null) return false;
				}
				return hasSuspenseContext(suspenseContext, ForceSuspenseFallback);
			}
			function getRemainingWorkInPrimaryTree(current, renderLanes) {
				return removeLanes(current.childLanes, renderLanes);
			}
			function updateSuspenseComponent(current, workInProgress, renderLanes) {
				var nextProps = workInProgress.pendingProps;
				if (shouldSuspend(workInProgress)) workInProgress.flags |= DidCapture;
				var suspenseContext = suspenseStackCursor.current;
				var showFallback = false;
				var didSuspend = (workInProgress.flags & DidCapture) !== NoFlags;
				if (didSuspend || shouldRemainOnFallback(suspenseContext, current)) {
					showFallback = true;
					workInProgress.flags &= ~DidCapture;
				} else if (current === null || current.memoizedState !== null) suspenseContext = addSubtreeSuspenseContext(suspenseContext, InvisibleParentSuspenseContext);
				suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
				pushSuspenseContext(workInProgress, suspenseContext);
				if (current === null) {
					tryToClaimNextHydratableInstance(workInProgress);
					var suspenseState = workInProgress.memoizedState;
					if (suspenseState !== null) {
						var dehydrated = suspenseState.dehydrated;
						if (dehydrated !== null) return mountDehydratedSuspenseComponent(workInProgress, dehydrated);
					}
					var nextPrimaryChildren = nextProps.children;
					var nextFallbackChildren = nextProps.fallback;
					if (showFallback) {
						var fallbackFragment = mountSuspenseFallbackChildren(workInProgress, nextPrimaryChildren, nextFallbackChildren, renderLanes);
						var primaryChildFragment = workInProgress.child;
						primaryChildFragment.memoizedState = mountSuspenseOffscreenState(renderLanes);
						workInProgress.memoizedState = SUSPENDED_MARKER;
						return fallbackFragment;
					} else return mountSuspensePrimaryChildren(workInProgress, nextPrimaryChildren);
				} else {
					var prevState = current.memoizedState;
					if (prevState !== null) {
						var _dehydrated = prevState.dehydrated;
						if (_dehydrated !== null) return updateDehydratedSuspenseComponent(current, workInProgress, didSuspend, nextProps, _dehydrated, prevState, renderLanes);
					}
					if (showFallback) {
						var _nextFallbackChildren = nextProps.fallback;
						var _nextPrimaryChildren = nextProps.children;
						var fallbackChildFragment = updateSuspenseFallbackChildren(current, workInProgress, _nextPrimaryChildren, _nextFallbackChildren, renderLanes);
						var _primaryChildFragment2 = workInProgress.child;
						var prevOffscreenState = current.child.memoizedState;
						_primaryChildFragment2.memoizedState = prevOffscreenState === null ? mountSuspenseOffscreenState(renderLanes) : updateSuspenseOffscreenState(prevOffscreenState, renderLanes);
						_primaryChildFragment2.childLanes = getRemainingWorkInPrimaryTree(current, renderLanes);
						workInProgress.memoizedState = SUSPENDED_MARKER;
						return fallbackChildFragment;
					} else {
						var _nextPrimaryChildren2 = nextProps.children;
						var _primaryChildFragment3 = updateSuspensePrimaryChildren(current, workInProgress, _nextPrimaryChildren2, renderLanes);
						workInProgress.memoizedState = null;
						return _primaryChildFragment3;
					}
				}
			}
			function mountSuspensePrimaryChildren(workInProgress, primaryChildren, renderLanes) {
				var mode = workInProgress.mode;
				var primaryChildFragment = mountWorkInProgressOffscreenFiber({
					mode: "visible",
					children: primaryChildren
				}, mode);
				primaryChildFragment.return = workInProgress;
				workInProgress.child = primaryChildFragment;
				return primaryChildFragment;
			}
			function mountSuspenseFallbackChildren(workInProgress, primaryChildren, fallbackChildren, renderLanes) {
				var mode = workInProgress.mode;
				var progressedPrimaryFragment = workInProgress.child;
				var primaryChildProps = {
					mode: "hidden",
					children: primaryChildren
				};
				var primaryChildFragment;
				var fallbackChildFragment;
				if ((mode & ConcurrentMode) === NoMode && progressedPrimaryFragment !== null) {
					primaryChildFragment = progressedPrimaryFragment;
					primaryChildFragment.childLanes = NoLanes;
					primaryChildFragment.pendingProps = primaryChildProps;
					if (workInProgress.mode & ProfileMode) {
						primaryChildFragment.actualDuration = 0;
						primaryChildFragment.actualStartTime = -1;
						primaryChildFragment.selfBaseDuration = 0;
						primaryChildFragment.treeBaseDuration = 0;
					}
					fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null);
				} else {
					primaryChildFragment = mountWorkInProgressOffscreenFiber(primaryChildProps, mode);
					fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null);
				}
				primaryChildFragment.return = workInProgress;
				fallbackChildFragment.return = workInProgress;
				primaryChildFragment.sibling = fallbackChildFragment;
				workInProgress.child = primaryChildFragment;
				return fallbackChildFragment;
			}
			function mountWorkInProgressOffscreenFiber(offscreenProps, mode, renderLanes) {
				return createFiberFromOffscreen(offscreenProps, mode, NoLanes, null);
			}
			function updateWorkInProgressOffscreenFiber(current, offscreenProps) {
				return createWorkInProgress(current, offscreenProps);
			}
			function updateSuspensePrimaryChildren(current, workInProgress, primaryChildren, renderLanes) {
				var currentPrimaryChildFragment = current.child;
				var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
				var primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, {
					mode: "visible",
					children: primaryChildren
				});
				if ((workInProgress.mode & ConcurrentMode) === NoMode) primaryChildFragment.lanes = renderLanes;
				primaryChildFragment.return = workInProgress;
				primaryChildFragment.sibling = null;
				if (currentFallbackChildFragment !== null) {
					var deletions = workInProgress.deletions;
					if (deletions === null) {
						workInProgress.deletions = [currentFallbackChildFragment];
						workInProgress.flags |= ChildDeletion;
					} else deletions.push(currentFallbackChildFragment);
				}
				workInProgress.child = primaryChildFragment;
				return primaryChildFragment;
			}
			function updateSuspenseFallbackChildren(current, workInProgress, primaryChildren, fallbackChildren, renderLanes) {
				var mode = workInProgress.mode;
				var currentPrimaryChildFragment = current.child;
				var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;
				var primaryChildProps = {
					mode: "hidden",
					children: primaryChildren
				};
				var primaryChildFragment;
				if ((mode & ConcurrentMode) === NoMode && workInProgress.child !== currentPrimaryChildFragment) {
					primaryChildFragment = workInProgress.child;
					primaryChildFragment.childLanes = NoLanes;
					primaryChildFragment.pendingProps = primaryChildProps;
					if (workInProgress.mode & ProfileMode) {
						primaryChildFragment.actualDuration = 0;
						primaryChildFragment.actualStartTime = -1;
						primaryChildFragment.selfBaseDuration = currentPrimaryChildFragment.selfBaseDuration;
						primaryChildFragment.treeBaseDuration = currentPrimaryChildFragment.treeBaseDuration;
					}
					workInProgress.deletions = null;
				} else {
					primaryChildFragment = updateWorkInProgressOffscreenFiber(currentPrimaryChildFragment, primaryChildProps);
					primaryChildFragment.subtreeFlags = currentPrimaryChildFragment.subtreeFlags & StaticMask;
				}
				var fallbackChildFragment;
				if (currentFallbackChildFragment !== null) fallbackChildFragment = createWorkInProgress(currentFallbackChildFragment, fallbackChildren);
				else {
					fallbackChildFragment = createFiberFromFragment(fallbackChildren, mode, renderLanes, null);
					fallbackChildFragment.flags |= Placement;
				}
				fallbackChildFragment.return = workInProgress;
				primaryChildFragment.return = workInProgress;
				primaryChildFragment.sibling = fallbackChildFragment;
				workInProgress.child = primaryChildFragment;
				return fallbackChildFragment;
			}
			function retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, recoverableError) {
				if (recoverableError !== null) queueHydrationError(recoverableError);
				reconcileChildFibers(workInProgress, current.child, null, renderLanes);
				var primaryChildren = workInProgress.pendingProps.children;
				var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress, primaryChildren);
				primaryChildFragment.flags |= Placement;
				workInProgress.memoizedState = null;
				return primaryChildFragment;
			}
			function mountSuspenseFallbackAfterRetryWithoutHydrating(current, workInProgress, primaryChildren, fallbackChildren, renderLanes) {
				var fiberMode = workInProgress.mode;
				var primaryChildFragment = mountWorkInProgressOffscreenFiber({
					mode: "visible",
					children: primaryChildren
				}, fiberMode);
				var fallbackChildFragment = createFiberFromFragment(fallbackChildren, fiberMode, renderLanes, null);
				fallbackChildFragment.flags |= Placement;
				primaryChildFragment.return = workInProgress;
				fallbackChildFragment.return = workInProgress;
				primaryChildFragment.sibling = fallbackChildFragment;
				workInProgress.child = primaryChildFragment;
				if ((workInProgress.mode & ConcurrentMode) !== NoMode) reconcileChildFibers(workInProgress, current.child, null, renderLanes);
				return fallbackChildFragment;
			}
			function mountDehydratedSuspenseComponent(workInProgress, suspenseInstance, renderLanes) {
				if ((workInProgress.mode & ConcurrentMode) === NoMode) {
					error("Cannot hydrate Suspense in legacy mode. Switch from ReactDOM.hydrate(element, container) to ReactDOMClient.hydrateRoot(container, <App />).render(element) or remove the Suspense components from the server rendered components.");
					workInProgress.lanes = laneToLanes(SyncLane);
				} else if (isSuspenseInstanceFallback(suspenseInstance)) workInProgress.lanes = laneToLanes(DefaultHydrationLane);
				else workInProgress.lanes = laneToLanes(OffscreenLane);
				return null;
			}
			function updateDehydratedSuspenseComponent(current, workInProgress, didSuspend, nextProps, suspenseInstance, suspenseState, renderLanes) {
				if (!didSuspend) {
					warnIfHydrating();
					if ((workInProgress.mode & ConcurrentMode) === NoMode) return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, null);
					if (isSuspenseInstanceFallback(suspenseInstance)) {
						var digest, message, stack;
						var _getSuspenseInstanceF = getSuspenseInstanceFallbackErrorDetails(suspenseInstance);
						digest = _getSuspenseInstanceF.digest;
						message = _getSuspenseInstanceF.message;
						stack = _getSuspenseInstanceF.stack;
						var error;
						if (message) error = new Error(message);
						else error = /* @__PURE__ */ new Error("The server could not finish this Suspense boundary, likely due to an error during server rendering. Switched to client rendering.");
						return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, createCapturedValue(error, digest, stack));
					}
					var hasContextChanged = includesSomeLane(renderLanes, current.childLanes);
					if (didReceiveUpdate || hasContextChanged) {
						var root = getWorkInProgressRoot();
						if (root !== null) {
							var attemptHydrationAtLane = getBumpedLaneForHydration(root, renderLanes);
							if (attemptHydrationAtLane !== NoLane && attemptHydrationAtLane !== suspenseState.retryLane) {
								suspenseState.retryLane = attemptHydrationAtLane;
								var eventTime = NoTimestamp;
								enqueueConcurrentRenderForLane(current, attemptHydrationAtLane);
								scheduleUpdateOnFiber(root, current, attemptHydrationAtLane, eventTime);
							}
						}
						renderDidSuspendDelayIfPossible();
						return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, createCapturedValue(/* @__PURE__ */ new Error("This Suspense boundary received an update before it finished hydrating. This caused the boundary to switch to client rendering. The usual way to fix this is to wrap the original update in startTransition.")));
					} else if (isSuspenseInstancePending(suspenseInstance)) {
						workInProgress.flags |= DidCapture;
						workInProgress.child = current.child;
						registerSuspenseInstanceRetry(suspenseInstance, retryDehydratedSuspenseBoundary.bind(null, current));
						return null;
					} else {
						reenterHydrationStateFromDehydratedSuspenseInstance(workInProgress, suspenseInstance, suspenseState.treeContext);
						var primaryChildren = nextProps.children;
						var primaryChildFragment = mountSuspensePrimaryChildren(workInProgress, primaryChildren);
						primaryChildFragment.flags |= Hydrating;
						return primaryChildFragment;
					}
				} else if (workInProgress.flags & ForceClientRender) {
					workInProgress.flags &= ~ForceClientRender;
					return retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes, createCapturedValue(/* @__PURE__ */ new Error("There was an error while hydrating this Suspense boundary. Switched to client rendering.")));
				} else if (workInProgress.memoizedState !== null) {
					workInProgress.child = current.child;
					workInProgress.flags |= DidCapture;
					return null;
				} else {
					var nextPrimaryChildren = nextProps.children;
					var nextFallbackChildren = nextProps.fallback;
					var fallbackChildFragment = mountSuspenseFallbackAfterRetryWithoutHydrating(current, workInProgress, nextPrimaryChildren, nextFallbackChildren, renderLanes);
					var _primaryChildFragment4 = workInProgress.child;
					_primaryChildFragment4.memoizedState = mountSuspenseOffscreenState(renderLanes);
					workInProgress.memoizedState = SUSPENDED_MARKER;
					return fallbackChildFragment;
				}
			}
			function scheduleSuspenseWorkOnFiber(fiber, renderLanes, propagationRoot) {
				fiber.lanes = mergeLanes(fiber.lanes, renderLanes);
				var alternate = fiber.alternate;
				if (alternate !== null) alternate.lanes = mergeLanes(alternate.lanes, renderLanes);
				scheduleContextWorkOnParentPath(fiber.return, renderLanes, propagationRoot);
			}
			function propagateSuspenseContextChange(workInProgress, firstChild, renderLanes) {
				var node = firstChild;
				while (node !== null) {
					if (node.tag === SuspenseComponent) {
						if (node.memoizedState !== null) scheduleSuspenseWorkOnFiber(node, renderLanes, workInProgress);
					} else if (node.tag === SuspenseListComponent) scheduleSuspenseWorkOnFiber(node, renderLanes, workInProgress);
					else if (node.child !== null) {
						node.child.return = node;
						node = node.child;
						continue;
					}
					if (node === workInProgress) return;
					while (node.sibling === null) {
						if (node.return === null || node.return === workInProgress) return;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
			}
			function findLastContentRow(firstChild) {
				var row = firstChild;
				var lastContentRow = null;
				while (row !== null) {
					var currentRow = row.alternate;
					if (currentRow !== null && findFirstSuspended(currentRow) === null) lastContentRow = row;
					row = row.sibling;
				}
				return lastContentRow;
			}
			function validateRevealOrder(revealOrder) {
				if (revealOrder !== void 0 && revealOrder !== "forwards" && revealOrder !== "backwards" && revealOrder !== "together" && !didWarnAboutRevealOrder[revealOrder]) {
					didWarnAboutRevealOrder[revealOrder] = true;
					if (typeof revealOrder === "string") switch (revealOrder.toLowerCase()) {
						case "together":
						case "forwards":
						case "backwards":
							error("\"%s\" is not a valid value for revealOrder on <SuspenseList />. Use lowercase \"%s\" instead.", revealOrder, revealOrder.toLowerCase());
							break;
						case "forward":
						case "backward":
							error("\"%s\" is not a valid value for revealOrder on <SuspenseList />. React uses the -s suffix in the spelling. Use \"%ss\" instead.", revealOrder, revealOrder.toLowerCase());
							break;
						default: error("\"%s\" is not a supported revealOrder on <SuspenseList />. Did you mean \"together\", \"forwards\" or \"backwards\"?", revealOrder);
					}
					else error("%s is not a supported value for revealOrder on <SuspenseList />. Did you mean \"together\", \"forwards\" or \"backwards\"?", revealOrder);
				}
			}
			function validateTailOptions(tailMode, revealOrder) {
				if (tailMode !== void 0 && !didWarnAboutTailOptions[tailMode]) {
					if (tailMode !== "collapsed" && tailMode !== "hidden") {
						didWarnAboutTailOptions[tailMode] = true;
						error("\"%s\" is not a supported value for tail on <SuspenseList />. Did you mean \"collapsed\" or \"hidden\"?", tailMode);
					} else if (revealOrder !== "forwards" && revealOrder !== "backwards") {
						didWarnAboutTailOptions[tailMode] = true;
						error("<SuspenseList tail=\"%s\" /> is only valid if revealOrder is \"forwards\" or \"backwards\". Did you mean to specify revealOrder=\"forwards\"?", tailMode);
					}
				}
			}
			function validateSuspenseListNestedChild(childSlot, index) {
				var isAnArray = isArray(childSlot);
				var isIterable = !isAnArray && typeof getIteratorFn(childSlot) === "function";
				if (isAnArray || isIterable) {
					var type = isAnArray ? "array" : "iterable";
					error("A nested %s was passed to row #%s in <SuspenseList />. Wrap it in an additional SuspenseList to configure its revealOrder: <SuspenseList revealOrder=...> ... <SuspenseList revealOrder=...>{%s}</SuspenseList> ... </SuspenseList>", type, index, type);
					return false;
				}
				return true;
			}
			function validateSuspenseListChildren(children, revealOrder) {
				if ((revealOrder === "forwards" || revealOrder === "backwards") && children !== void 0 && children !== null && children !== false) {
					if (isArray(children)) {
						for (var i = 0; i < children.length; i++) if (!validateSuspenseListNestedChild(children[i], i)) return;
					} else {
						var iteratorFn = getIteratorFn(children);
						if (typeof iteratorFn === "function") {
							var childrenIterator = iteratorFn.call(children);
							if (childrenIterator) {
								var step = childrenIterator.next();
								var _i = 0;
								for (; !step.done; step = childrenIterator.next()) {
									if (!validateSuspenseListNestedChild(step.value, _i)) return;
									_i++;
								}
							}
						} else error("A single row was passed to a <SuspenseList revealOrder=\"%s\" />. This is not useful since it needs multiple rows. Did you mean to pass multiple children or an array?", revealOrder);
					}
				}
			}
			function initSuspenseListRenderState(workInProgress, isBackwards, tail, lastContentRow, tailMode) {
				var renderState = workInProgress.memoizedState;
				if (renderState === null) workInProgress.memoizedState = {
					isBackwards,
					rendering: null,
					renderingStartTime: 0,
					last: lastContentRow,
					tail,
					tailMode
				};
				else {
					renderState.isBackwards = isBackwards;
					renderState.rendering = null;
					renderState.renderingStartTime = 0;
					renderState.last = lastContentRow;
					renderState.tail = tail;
					renderState.tailMode = tailMode;
				}
			}
			function updateSuspenseListComponent(current, workInProgress, renderLanes) {
				var nextProps = workInProgress.pendingProps;
				var revealOrder = nextProps.revealOrder;
				var tailMode = nextProps.tail;
				var newChildren = nextProps.children;
				validateRevealOrder(revealOrder);
				validateTailOptions(tailMode, revealOrder);
				validateSuspenseListChildren(newChildren, revealOrder);
				reconcileChildren(current, workInProgress, newChildren, renderLanes);
				var suspenseContext = suspenseStackCursor.current;
				if (hasSuspenseContext(suspenseContext, ForceSuspenseFallback)) {
					suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
					workInProgress.flags |= DidCapture;
				} else {
					if (current !== null && (current.flags & DidCapture) !== NoFlags) propagateSuspenseContextChange(workInProgress, workInProgress.child, renderLanes);
					suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
				}
				pushSuspenseContext(workInProgress, suspenseContext);
				if ((workInProgress.mode & ConcurrentMode) === NoMode) workInProgress.memoizedState = null;
				else switch (revealOrder) {
					case "forwards":
						var lastContentRow = findLastContentRow(workInProgress.child);
						var tail;
						if (lastContentRow === null) {
							tail = workInProgress.child;
							workInProgress.child = null;
						} else {
							tail = lastContentRow.sibling;
							lastContentRow.sibling = null;
						}
						initSuspenseListRenderState(workInProgress, false, tail, lastContentRow, tailMode);
						break;
					case "backwards":
						var _tail = null;
						var row = workInProgress.child;
						workInProgress.child = null;
						while (row !== null) {
							var currentRow = row.alternate;
							if (currentRow !== null && findFirstSuspended(currentRow) === null) {
								workInProgress.child = row;
								break;
							}
							var nextRow = row.sibling;
							row.sibling = _tail;
							_tail = row;
							row = nextRow;
						}
						initSuspenseListRenderState(workInProgress, true, _tail, null, tailMode);
						break;
					case "together":
						initSuspenseListRenderState(workInProgress, false, null, null, void 0);
						break;
					default: workInProgress.memoizedState = null;
				}
				return workInProgress.child;
			}
			function updatePortalComponent(current, workInProgress, renderLanes) {
				pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
				var nextChildren = workInProgress.pendingProps;
				if (current === null) workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderLanes);
				else reconcileChildren(current, workInProgress, nextChildren, renderLanes);
				return workInProgress.child;
			}
			var hasWarnedAboutUsingNoValuePropOnContextProvider = false;
			function updateContextProvider(current, workInProgress, renderLanes) {
				var context = workInProgress.type._context;
				var newProps = workInProgress.pendingProps;
				var oldProps = workInProgress.memoizedProps;
				var newValue = newProps.value;
				if (!("value" in newProps)) {
					if (!hasWarnedAboutUsingNoValuePropOnContextProvider) {
						hasWarnedAboutUsingNoValuePropOnContextProvider = true;
						error("The `value` prop is required for the `<Context.Provider>`. Did you misspell it or forget to pass it?");
					}
				}
				var providerPropTypes = workInProgress.type.propTypes;
				if (providerPropTypes) checkPropTypes(providerPropTypes, newProps, "prop", "Context.Provider");
				pushProvider(workInProgress, context, newValue);
				if (oldProps !== null) {
					var oldValue = oldProps.value;
					if (objectIs(oldValue, newValue)) {
						if (oldProps.children === newProps.children && !hasContextChanged()) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
					} else propagateContextChange(workInProgress, context, renderLanes);
				}
				var newChildren = newProps.children;
				reconcileChildren(current, workInProgress, newChildren, renderLanes);
				return workInProgress.child;
			}
			var hasWarnedAboutUsingContextAsConsumer = false;
			function updateContextConsumer(current, workInProgress, renderLanes) {
				var context = workInProgress.type;
				if (context._context === void 0) {
					if (context !== context.Consumer) {
						if (!hasWarnedAboutUsingContextAsConsumer) {
							hasWarnedAboutUsingContextAsConsumer = true;
							error("Rendering <Context> directly is not supported and will be removed in a future major release. Did you mean to render <Context.Consumer> instead?");
						}
					}
				} else context = context._context;
				var render = workInProgress.pendingProps.children;
				if (typeof render !== "function") error("A context consumer was rendered with multiple children, or a child that isn't a function. A context consumer expects a single child that is a function. If you did pass a function, make sure there is no trailing or leading whitespace around it.");
				prepareToReadContext(workInProgress, renderLanes);
				var newValue = readContext(context);
				markComponentRenderStarted(workInProgress);
				var newChildren;
				ReactCurrentOwner$1.current = workInProgress;
				setIsRendering(true);
				newChildren = render(newValue);
				setIsRendering(false);
				markComponentRenderStopped();
				workInProgress.flags |= PerformedWork;
				reconcileChildren(current, workInProgress, newChildren, renderLanes);
				return workInProgress.child;
			}
			function markWorkInProgressReceivedUpdate() {
				didReceiveUpdate = true;
			}
			function resetSuspendedCurrentOnMountInLegacyMode(current, workInProgress) {
				if ((workInProgress.mode & ConcurrentMode) === NoMode) {
					if (current !== null) {
						current.alternate = null;
						workInProgress.alternate = null;
						workInProgress.flags |= Placement;
					}
				}
			}
			function bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes) {
				if (current !== null) workInProgress.dependencies = current.dependencies;
				stopProfilerTimerIfRunning();
				markSkippedUpdateLanes(workInProgress.lanes);
				if (!includesSomeLane(renderLanes, workInProgress.childLanes)) return null;
				cloneChildFibers(current, workInProgress);
				return workInProgress.child;
			}
			function remountFiber(current, oldWorkInProgress, newWorkInProgress) {
				var returnFiber = oldWorkInProgress.return;
				if (returnFiber === null) throw new Error("Cannot swap the root fiber.");
				current.alternate = null;
				oldWorkInProgress.alternate = null;
				newWorkInProgress.index = oldWorkInProgress.index;
				newWorkInProgress.sibling = oldWorkInProgress.sibling;
				newWorkInProgress.return = oldWorkInProgress.return;
				newWorkInProgress.ref = oldWorkInProgress.ref;
				if (oldWorkInProgress === returnFiber.child) returnFiber.child = newWorkInProgress;
				else {
					var prevSibling = returnFiber.child;
					if (prevSibling === null) throw new Error("Expected parent to have a child.");
					while (prevSibling.sibling !== oldWorkInProgress) {
						prevSibling = prevSibling.sibling;
						if (prevSibling === null) throw new Error("Expected to find the previous sibling.");
					}
					prevSibling.sibling = newWorkInProgress;
				}
				var deletions = returnFiber.deletions;
				if (deletions === null) {
					returnFiber.deletions = [current];
					returnFiber.flags |= ChildDeletion;
				} else deletions.push(current);
				newWorkInProgress.flags |= Placement;
				return newWorkInProgress;
			}
			function checkScheduledUpdateOrContext(current, renderLanes) {
				var updateLanes = current.lanes;
				if (includesSomeLane(updateLanes, renderLanes)) return true;
				return false;
			}
			function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes) {
				switch (workInProgress.tag) {
					case HostRoot:
						pushHostRootContext(workInProgress);
						workInProgress.stateNode;
						resetHydrationState();
						break;
					case HostComponent:
						pushHostContext(workInProgress);
						break;
					case ClassComponent:
						var Component = workInProgress.type;
						if (isContextProvider(Component)) pushContextProvider(workInProgress);
						break;
					case HostPortal:
						pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
						break;
					case ContextProvider:
						var newValue = workInProgress.memoizedProps.value;
						var context = workInProgress.type._context;
						pushProvider(workInProgress, context, newValue);
						break;
					case Profiler:
						if (includesSomeLane(renderLanes, workInProgress.childLanes)) workInProgress.flags |= Update;
						var stateNode = workInProgress.stateNode;
						stateNode.effectDuration = 0;
						stateNode.passiveEffectDuration = 0;
						break;
					case SuspenseComponent:
						var state = workInProgress.memoizedState;
						if (state !== null) {
							if (state.dehydrated !== null) {
								pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
								workInProgress.flags |= DidCapture;
								return null;
							}
							var primaryChildLanes = workInProgress.child.childLanes;
							if (includesSomeLane(renderLanes, primaryChildLanes)) return updateSuspenseComponent(current, workInProgress, renderLanes);
							else {
								pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
								var child = bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
								if (child !== null) return child.sibling;
								else return null;
							}
						} else pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
						break;
					case SuspenseListComponent:
						var didSuspendBefore = (current.flags & DidCapture) !== NoFlags;
						var _hasChildWork = includesSomeLane(renderLanes, workInProgress.childLanes);
						if (didSuspendBefore) {
							if (_hasChildWork) return updateSuspenseListComponent(current, workInProgress, renderLanes);
							workInProgress.flags |= DidCapture;
						}
						var renderState = workInProgress.memoizedState;
						if (renderState !== null) {
							renderState.rendering = null;
							renderState.tail = null;
							renderState.lastEffect = null;
						}
						pushSuspenseContext(workInProgress, suspenseStackCursor.current);
						if (_hasChildWork) break;
						else return null;
					case OffscreenComponent:
					case LegacyHiddenComponent:
						workInProgress.lanes = NoLanes;
						return updateOffscreenComponent(current, workInProgress, renderLanes);
				}
				return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			}
			function beginWork(current, workInProgress, renderLanes) {
				if (workInProgress._debugNeedsRemount && current !== null) return remountFiber(current, workInProgress, createFiberFromTypeAndProps(workInProgress.type, workInProgress.key, workInProgress.pendingProps, workInProgress._debugOwner || null, workInProgress.mode, workInProgress.lanes));
				if (current !== null) {
					if (current.memoizedProps !== workInProgress.pendingProps || hasContextChanged() || workInProgress.type !== current.type) didReceiveUpdate = true;
					else {
						if (!checkScheduledUpdateOrContext(current, renderLanes) && (workInProgress.flags & DidCapture) === NoFlags) {
							didReceiveUpdate = false;
							return attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes);
						}
						if ((current.flags & ForceUpdateForLegacySuspense) !== NoFlags) didReceiveUpdate = true;
						else didReceiveUpdate = false;
					}
				} else {
					didReceiveUpdate = false;
					if (getIsHydrating() && isForkedChild(workInProgress)) {
						var slotIndex = workInProgress.index;
						pushTreeId(workInProgress, getForksAtLevel(), slotIndex);
					}
				}
				workInProgress.lanes = NoLanes;
				switch (workInProgress.tag) {
					case IndeterminateComponent: return mountIndeterminateComponent(current, workInProgress, workInProgress.type, renderLanes);
					case LazyComponent:
						var elementType = workInProgress.elementType;
						return mountLazyComponent(current, workInProgress, elementType, renderLanes);
					case FunctionComponent:
						var Component = workInProgress.type;
						var unresolvedProps = workInProgress.pendingProps;
						return updateFunctionComponent(current, workInProgress, Component, workInProgress.elementType === Component ? unresolvedProps : resolveDefaultProps(Component, unresolvedProps), renderLanes);
					case ClassComponent:
						var _Component = workInProgress.type;
						var _unresolvedProps = workInProgress.pendingProps;
						return updateClassComponent(current, workInProgress, _Component, workInProgress.elementType === _Component ? _unresolvedProps : resolveDefaultProps(_Component, _unresolvedProps), renderLanes);
					case HostRoot: return updateHostRoot(current, workInProgress, renderLanes);
					case HostComponent: return updateHostComponent(current, workInProgress, renderLanes);
					case HostText: return updateHostText(current, workInProgress);
					case SuspenseComponent: return updateSuspenseComponent(current, workInProgress, renderLanes);
					case HostPortal: return updatePortalComponent(current, workInProgress, renderLanes);
					case ForwardRef:
						var type = workInProgress.type;
						var _unresolvedProps2 = workInProgress.pendingProps;
						return updateForwardRef(current, workInProgress, type, workInProgress.elementType === type ? _unresolvedProps2 : resolveDefaultProps(type, _unresolvedProps2), renderLanes);
					case Fragment: return updateFragment(current, workInProgress, renderLanes);
					case Mode: return updateMode(current, workInProgress, renderLanes);
					case Profiler: return updateProfiler(current, workInProgress, renderLanes);
					case ContextProvider: return updateContextProvider(current, workInProgress, renderLanes);
					case ContextConsumer: return updateContextConsumer(current, workInProgress, renderLanes);
					case MemoComponent:
						var _type2 = workInProgress.type;
						var _unresolvedProps3 = workInProgress.pendingProps;
						var _resolvedProps3 = resolveDefaultProps(_type2, _unresolvedProps3);
						if (workInProgress.type !== workInProgress.elementType) {
							var outerPropTypes = _type2.propTypes;
							if (outerPropTypes) checkPropTypes(outerPropTypes, _resolvedProps3, "prop", getComponentNameFromType(_type2));
						}
						_resolvedProps3 = resolveDefaultProps(_type2.type, _resolvedProps3);
						return updateMemoComponent(current, workInProgress, _type2, _resolvedProps3, renderLanes);
					case SimpleMemoComponent: return updateSimpleMemoComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
					case IncompleteClassComponent:
						var _Component2 = workInProgress.type;
						var _unresolvedProps4 = workInProgress.pendingProps;
						return mountIncompleteClassComponent(current, workInProgress, _Component2, workInProgress.elementType === _Component2 ? _unresolvedProps4 : resolveDefaultProps(_Component2, _unresolvedProps4), renderLanes);
					case SuspenseListComponent: return updateSuspenseListComponent(current, workInProgress, renderLanes);
					case ScopeComponent: break;
					case OffscreenComponent: return updateOffscreenComponent(current, workInProgress, renderLanes);
				}
				throw new Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue.");
			}
			function markUpdate(workInProgress) {
				workInProgress.flags |= Update;
			}
			function markRef$1(workInProgress) {
				workInProgress.flags |= Ref;
				workInProgress.flags |= RefStatic;
			}
			var appendAllChildren;
			var updateHostContainer;
			var updateHostComponent$1;
			var updateHostText$1;
			appendAllChildren = function(parent, workInProgress, needsVisibilityToggle, isHidden) {
				var node = workInProgress.child;
				while (node !== null) {
					if (node.tag === HostComponent || node.tag === HostText) appendInitialChild(parent, node.stateNode);
					else if (node.tag === HostPortal);
					else if (node.child !== null) {
						node.child.return = node;
						node = node.child;
						continue;
					}
					if (node === workInProgress) return;
					while (node.sibling === null) {
						if (node.return === null || node.return === workInProgress) return;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
			};
			updateHostContainer = function(current, workInProgress) {};
			updateHostComponent$1 = function(current, workInProgress, type, newProps, rootContainerInstance) {
				var oldProps = current.memoizedProps;
				if (oldProps === newProps) return;
				var instance = workInProgress.stateNode;
				var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, getHostContext());
				workInProgress.updateQueue = updatePayload;
				if (updatePayload) markUpdate(workInProgress);
			};
			updateHostText$1 = function(current, workInProgress, oldText, newText) {
				if (oldText !== newText) markUpdate(workInProgress);
			};
			function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
				if (getIsHydrating()) return;
				switch (renderState.tailMode) {
					case "hidden":
						var tailNode = renderState.tail;
						var lastTailNode = null;
						while (tailNode !== null) {
							if (tailNode.alternate !== null) lastTailNode = tailNode;
							tailNode = tailNode.sibling;
						}
						if (lastTailNode === null) renderState.tail = null;
						else lastTailNode.sibling = null;
						break;
					case "collapsed":
						var _tailNode = renderState.tail;
						var _lastTailNode = null;
						while (_tailNode !== null) {
							if (_tailNode.alternate !== null) _lastTailNode = _tailNode;
							_tailNode = _tailNode.sibling;
						}
						if (_lastTailNode === null) {
							if (!hasRenderedATailFallback && renderState.tail !== null) renderState.tail.sibling = null;
							else renderState.tail = null;
						} else _lastTailNode.sibling = null;
				}
			}
			function bubbleProperties(completedWork) {
				var didBailout = completedWork.alternate !== null && completedWork.alternate.child === completedWork.child;
				var newChildLanes = NoLanes;
				var subtreeFlags = NoFlags;
				if (!didBailout) {
					if ((completedWork.mode & ProfileMode) !== NoMode) {
						var actualDuration = completedWork.actualDuration;
						var treeBaseDuration = completedWork.selfBaseDuration;
						var child = completedWork.child;
						while (child !== null) {
							newChildLanes = mergeLanes(newChildLanes, mergeLanes(child.lanes, child.childLanes));
							subtreeFlags |= child.subtreeFlags;
							subtreeFlags |= child.flags;
							actualDuration += child.actualDuration;
							treeBaseDuration += child.treeBaseDuration;
							child = child.sibling;
						}
						completedWork.actualDuration = actualDuration;
						completedWork.treeBaseDuration = treeBaseDuration;
					} else {
						var _child = completedWork.child;
						while (_child !== null) {
							newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child.lanes, _child.childLanes));
							subtreeFlags |= _child.subtreeFlags;
							subtreeFlags |= _child.flags;
							_child.return = completedWork;
							_child = _child.sibling;
						}
					}
					completedWork.subtreeFlags |= subtreeFlags;
				} else {
					if ((completedWork.mode & ProfileMode) !== NoMode) {
						var _treeBaseDuration = completedWork.selfBaseDuration;
						var _child2 = completedWork.child;
						while (_child2 !== null) {
							newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child2.lanes, _child2.childLanes));
							subtreeFlags |= _child2.subtreeFlags & StaticMask;
							subtreeFlags |= _child2.flags & StaticMask;
							_treeBaseDuration += _child2.treeBaseDuration;
							_child2 = _child2.sibling;
						}
						completedWork.treeBaseDuration = _treeBaseDuration;
					} else {
						var _child3 = completedWork.child;
						while (_child3 !== null) {
							newChildLanes = mergeLanes(newChildLanes, mergeLanes(_child3.lanes, _child3.childLanes));
							subtreeFlags |= _child3.subtreeFlags & StaticMask;
							subtreeFlags |= _child3.flags & StaticMask;
							_child3.return = completedWork;
							_child3 = _child3.sibling;
						}
					}
					completedWork.subtreeFlags |= subtreeFlags;
				}
				completedWork.childLanes = newChildLanes;
				return didBailout;
			}
			function completeDehydratedSuspenseBoundary(current, workInProgress, nextState) {
				if (hasUnhydratedTailNodes() && (workInProgress.mode & ConcurrentMode) !== NoMode && (workInProgress.flags & DidCapture) === NoFlags) {
					warnIfUnhydratedTailNodes(workInProgress);
					resetHydrationState();
					workInProgress.flags |= ForceClientRender | Incomplete | ShouldCapture;
					return false;
				}
				var wasHydrated = popHydrationState(workInProgress);
				if (nextState !== null && nextState.dehydrated !== null) {
					if (current === null) {
						if (!wasHydrated) throw new Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React.");
						prepareToHydrateHostSuspenseInstance(workInProgress);
						bubbleProperties(workInProgress);
						if ((workInProgress.mode & ProfileMode) !== NoMode) {
							if (nextState !== null) {
								var primaryChildFragment = workInProgress.child;
								if (primaryChildFragment !== null) workInProgress.treeBaseDuration -= primaryChildFragment.treeBaseDuration;
							}
						}
						return false;
					} else {
						resetHydrationState();
						if ((workInProgress.flags & DidCapture) === NoFlags) workInProgress.memoizedState = null;
						workInProgress.flags |= Update;
						bubbleProperties(workInProgress);
						if ((workInProgress.mode & ProfileMode) !== NoMode) {
							if (nextState !== null) {
								var _primaryChildFragment = workInProgress.child;
								if (_primaryChildFragment !== null) workInProgress.treeBaseDuration -= _primaryChildFragment.treeBaseDuration;
							}
						}
						return false;
					}
				} else {
					upgradeHydrationErrorsToRecoverable();
					return true;
				}
			}
			function completeWork(current, workInProgress, renderLanes) {
				var newProps = workInProgress.pendingProps;
				popTreeContext(workInProgress);
				switch (workInProgress.tag) {
					case IndeterminateComponent:
					case LazyComponent:
					case SimpleMemoComponent:
					case FunctionComponent:
					case ForwardRef:
					case Fragment:
					case Mode:
					case Profiler:
					case ContextConsumer:
					case MemoComponent:
						bubbleProperties(workInProgress);
						return null;
					case ClassComponent:
						var Component = workInProgress.type;
						if (isContextProvider(Component)) popContext(workInProgress);
						bubbleProperties(workInProgress);
						return null;
					case HostRoot:
						var fiberRoot = workInProgress.stateNode;
						popHostContainer(workInProgress);
						popTopLevelContextObject(workInProgress);
						resetWorkInProgressVersions();
						if (fiberRoot.pendingContext) {
							fiberRoot.context = fiberRoot.pendingContext;
							fiberRoot.pendingContext = null;
						}
						if (current === null || current.child === null) {
							if (popHydrationState(workInProgress)) markUpdate(workInProgress);
							else if (current !== null) {
								if (!current.memoizedState.isDehydrated || (workInProgress.flags & ForceClientRender) !== NoFlags) {
									workInProgress.flags |= Snapshot;
									upgradeHydrationErrorsToRecoverable();
								}
							}
						}
						updateHostContainer(current, workInProgress);
						bubbleProperties(workInProgress);
						return null;
					case HostComponent:
						popHostContext(workInProgress);
						var rootContainerInstance = getRootHostContainer();
						var type = workInProgress.type;
						if (current !== null && workInProgress.stateNode != null) {
							updateHostComponent$1(current, workInProgress, type, newProps, rootContainerInstance);
							if (current.ref !== workInProgress.ref) markRef$1(workInProgress);
						} else {
							if (!newProps) {
								if (workInProgress.stateNode === null) throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
								bubbleProperties(workInProgress);
								return null;
							}
							var currentHostContext = getHostContext();
							if (popHydrationState(workInProgress)) {
								if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, currentHostContext)) markUpdate(workInProgress);
							} else {
								var instance = createInstance(type, newProps, rootContainerInstance, currentHostContext, workInProgress);
								appendAllChildren(instance, workInProgress, false, false);
								workInProgress.stateNode = instance;
								if (finalizeInitialChildren(instance, type, newProps, rootContainerInstance)) markUpdate(workInProgress);
							}
							if (workInProgress.ref !== null) markRef$1(workInProgress);
						}
						bubbleProperties(workInProgress);
						return null;
					case HostText:
						var newText = newProps;
						if (current && workInProgress.stateNode != null) {
							var oldText = current.memoizedProps;
							updateHostText$1(current, workInProgress, oldText, newText);
						} else {
							if (typeof newText !== "string") {
								if (workInProgress.stateNode === null) throw new Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
							}
							var _rootContainerInstance = getRootHostContainer();
							var _currentHostContext = getHostContext();
							if (popHydrationState(workInProgress)) {
								if (prepareToHydrateHostTextInstance(workInProgress)) markUpdate(workInProgress);
							} else workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext, workInProgress);
						}
						bubbleProperties(workInProgress);
						return null;
					case SuspenseComponent:
						popSuspenseContext(workInProgress);
						var nextState = workInProgress.memoizedState;
						if (current === null || current.memoizedState !== null && current.memoizedState.dehydrated !== null) {
							if (!completeDehydratedSuspenseBoundary(current, workInProgress, nextState)) {
								if (workInProgress.flags & ShouldCapture) return workInProgress;
								else return null;
							}
						}
						if ((workInProgress.flags & DidCapture) !== NoFlags) {
							workInProgress.lanes = renderLanes;
							if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
							return workInProgress;
						}
						var nextDidTimeout = nextState !== null;
						if (nextDidTimeout !== (current !== null && current.memoizedState !== null)) {
							if (nextDidTimeout) {
								var _offscreenFiber2 = workInProgress.child;
								_offscreenFiber2.flags |= Visibility;
								if ((workInProgress.mode & ConcurrentMode) !== NoMode) {
									if (current === null && (workInProgress.memoizedProps.unstable_avoidThisFallback !== true || !enableSuspenseAvoidThisFallback) || hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext)) renderDidSuspend();
									else renderDidSuspendDelayIfPossible();
								}
							}
						}
						if (workInProgress.updateQueue !== null) workInProgress.flags |= Update;
						bubbleProperties(workInProgress);
						if ((workInProgress.mode & ProfileMode) !== NoMode) {
							if (nextDidTimeout) {
								var primaryChildFragment = workInProgress.child;
								if (primaryChildFragment !== null) workInProgress.treeBaseDuration -= primaryChildFragment.treeBaseDuration;
							}
						}
						return null;
					case HostPortal:
						popHostContainer(workInProgress);
						updateHostContainer(current, workInProgress);
						if (current === null) preparePortalMount(workInProgress.stateNode.containerInfo);
						bubbleProperties(workInProgress);
						return null;
					case ContextProvider:
						var context = workInProgress.type._context;
						popProvider(context, workInProgress);
						bubbleProperties(workInProgress);
						return null;
					case IncompleteClassComponent:
						var _Component = workInProgress.type;
						if (isContextProvider(_Component)) popContext(workInProgress);
						bubbleProperties(workInProgress);
						return null;
					case SuspenseListComponent:
						popSuspenseContext(workInProgress);
						var renderState = workInProgress.memoizedState;
						if (renderState === null) {
							bubbleProperties(workInProgress);
							return null;
						}
						var didSuspendAlready = (workInProgress.flags & DidCapture) !== NoFlags;
						var renderedTail = renderState.rendering;
						if (renderedTail === null) {
							if (!didSuspendAlready) {
								if (!(renderHasNotSuspendedYet() && (current === null || (current.flags & DidCapture) === NoFlags))) {
									var row = workInProgress.child;
									while (row !== null) {
										var suspended = findFirstSuspended(row);
										if (suspended !== null) {
											didSuspendAlready = true;
											workInProgress.flags |= DidCapture;
											cutOffTailIfNeeded(renderState, false);
											var newThenables = suspended.updateQueue;
											if (newThenables !== null) {
												workInProgress.updateQueue = newThenables;
												workInProgress.flags |= Update;
											}
											workInProgress.subtreeFlags = NoFlags;
											resetChildFibers(workInProgress, renderLanes);
											pushSuspenseContext(workInProgress, setShallowSuspenseContext(suspenseStackCursor.current, ForceSuspenseFallback));
											return workInProgress.child;
										}
										row = row.sibling;
									}
								}
								if (renderState.tail !== null && now() > getRenderTargetTime()) {
									workInProgress.flags |= DidCapture;
									didSuspendAlready = true;
									cutOffTailIfNeeded(renderState, false);
									workInProgress.lanes = SomeRetryLane;
								}
							} else cutOffTailIfNeeded(renderState, false);
						} else {
							if (!didSuspendAlready) {
								var _suspended = findFirstSuspended(renderedTail);
								if (_suspended !== null) {
									workInProgress.flags |= DidCapture;
									didSuspendAlready = true;
									var _newThenables = _suspended.updateQueue;
									if (_newThenables !== null) {
										workInProgress.updateQueue = _newThenables;
										workInProgress.flags |= Update;
									}
									cutOffTailIfNeeded(renderState, true);
									if (renderState.tail === null && renderState.tailMode === "hidden" && !renderedTail.alternate && !getIsHydrating()) {
										bubbleProperties(workInProgress);
										return null;
									}
								} else if (now() * 2 - renderState.renderingStartTime > getRenderTargetTime() && renderLanes !== OffscreenLane) {
									workInProgress.flags |= DidCapture;
									didSuspendAlready = true;
									cutOffTailIfNeeded(renderState, false);
									workInProgress.lanes = SomeRetryLane;
								}
							}
							if (renderState.isBackwards) {
								renderedTail.sibling = workInProgress.child;
								workInProgress.child = renderedTail;
							} else {
								var previousSibling = renderState.last;
								if (previousSibling !== null) previousSibling.sibling = renderedTail;
								else workInProgress.child = renderedTail;
								renderState.last = renderedTail;
							}
						}
						if (renderState.tail !== null) {
							var next = renderState.tail;
							renderState.rendering = next;
							renderState.tail = next.sibling;
							renderState.renderingStartTime = now();
							next.sibling = null;
							var suspenseContext = suspenseStackCursor.current;
							if (didSuspendAlready) suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
							else suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
							pushSuspenseContext(workInProgress, suspenseContext);
							return next;
						}
						bubbleProperties(workInProgress);
						return null;
					case ScopeComponent: break;
					case OffscreenComponent:
					case LegacyHiddenComponent:
						popRenderLanes(workInProgress);
						var nextIsHidden = workInProgress.memoizedState !== null;
						if (current !== null) {
							if (current.memoizedState !== null !== nextIsHidden && !enableLegacyHidden) workInProgress.flags |= Visibility;
						}
						if (!nextIsHidden || (workInProgress.mode & ConcurrentMode) === NoMode) bubbleProperties(workInProgress);
						else if (includesSomeLane(subtreeRenderLanes, OffscreenLane)) {
							bubbleProperties(workInProgress);
							if (workInProgress.subtreeFlags & (Placement | Update)) workInProgress.flags |= Visibility;
						}
						return null;
					case CacheComponent: return null;
					case TracingMarkerComponent: return null;
				}
				throw new Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue.");
			}
			function unwindWork(current, workInProgress, renderLanes) {
				popTreeContext(workInProgress);
				switch (workInProgress.tag) {
					case ClassComponent:
						var Component = workInProgress.type;
						if (isContextProvider(Component)) popContext(workInProgress);
						var flags = workInProgress.flags;
						if (flags & ShouldCapture) {
							workInProgress.flags = flags & ~ShouldCapture | DidCapture;
							if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
							return workInProgress;
						}
						return null;
					case HostRoot:
						workInProgress.stateNode;
						popHostContainer(workInProgress);
						popTopLevelContextObject(workInProgress);
						resetWorkInProgressVersions();
						var _flags = workInProgress.flags;
						if ((_flags & ShouldCapture) !== NoFlags && (_flags & DidCapture) === NoFlags) {
							workInProgress.flags = _flags & ~ShouldCapture | DidCapture;
							return workInProgress;
						}
						return null;
					case HostComponent:
						popHostContext(workInProgress);
						return null;
					case SuspenseComponent:
						popSuspenseContext(workInProgress);
						var suspenseState = workInProgress.memoizedState;
						if (suspenseState !== null && suspenseState.dehydrated !== null) {
							if (workInProgress.alternate === null) throw new Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue.");
							resetHydrationState();
						}
						var _flags2 = workInProgress.flags;
						if (_flags2 & ShouldCapture) {
							workInProgress.flags = _flags2 & ~ShouldCapture | DidCapture;
							if ((workInProgress.mode & ProfileMode) !== NoMode) transferActualDuration(workInProgress);
							return workInProgress;
						}
						return null;
					case SuspenseListComponent:
						popSuspenseContext(workInProgress);
						return null;
					case HostPortal:
						popHostContainer(workInProgress);
						return null;
					case ContextProvider:
						var context = workInProgress.type._context;
						popProvider(context, workInProgress);
						return null;
					case OffscreenComponent:
					case LegacyHiddenComponent:
						popRenderLanes(workInProgress);
						return null;
					case CacheComponent: return null;
					default: return null;
				}
			}
			function unwindInterruptedWork(current, interruptedWork, renderLanes) {
				popTreeContext(interruptedWork);
				switch (interruptedWork.tag) {
					case ClassComponent:
						var childContextTypes = interruptedWork.type.childContextTypes;
						if (childContextTypes !== null && childContextTypes !== void 0) popContext(interruptedWork);
						break;
					case HostRoot:
						interruptedWork.stateNode;
						popHostContainer(interruptedWork);
						popTopLevelContextObject(interruptedWork);
						resetWorkInProgressVersions();
						break;
					case HostComponent:
						popHostContext(interruptedWork);
						break;
					case HostPortal:
						popHostContainer(interruptedWork);
						break;
					case SuspenseComponent:
						popSuspenseContext(interruptedWork);
						break;
					case SuspenseListComponent:
						popSuspenseContext(interruptedWork);
						break;
					case ContextProvider:
						var context = interruptedWork.type._context;
						popProvider(context, interruptedWork);
						break;
					case OffscreenComponent:
					case LegacyHiddenComponent: popRenderLanes(interruptedWork);
				}
			}
			var didWarnAboutUndefinedSnapshotBeforeUpdate = null;
			didWarnAboutUndefinedSnapshotBeforeUpdate = /* @__PURE__ */ new Set();
			var offscreenSubtreeIsHidden = false;
			var offscreenSubtreeWasHidden = false;
			var PossiblyWeakSet = typeof WeakSet === "function" ? WeakSet : Set;
			var nextEffect = null;
			var inProgressLanes = null;
			var inProgressRoot = null;
			function reportUncaughtErrorInDEV(error) {
				invokeGuardedCallback(null, function() {
					throw error;
				});
				clearCaughtError();
			}
			var callComponentWillUnmountWithTimer = function(current, instance) {
				instance.props = current.memoizedProps;
				instance.state = current.memoizedState;
				if (current.mode & ProfileMode) try {
					startLayoutEffectTimer();
					instance.componentWillUnmount();
				} finally {
					recordLayoutEffectDuration(current);
				}
				else instance.componentWillUnmount();
			};
			function safelyCallCommitHookLayoutEffectListMount(current, nearestMountedAncestor) {
				try {
					commitHookEffectListMount(Layout, current);
				} catch (error) {
					captureCommitPhaseError(current, nearestMountedAncestor, error);
				}
			}
			function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
				try {
					callComponentWillUnmountWithTimer(current, instance);
				} catch (error) {
					captureCommitPhaseError(current, nearestMountedAncestor, error);
				}
			}
			function safelyCallComponentDidMount(current, nearestMountedAncestor, instance) {
				try {
					instance.componentDidMount();
				} catch (error) {
					captureCommitPhaseError(current, nearestMountedAncestor, error);
				}
			}
			function safelyAttachRef(current, nearestMountedAncestor) {
				try {
					commitAttachRef(current);
				} catch (error) {
					captureCommitPhaseError(current, nearestMountedAncestor, error);
				}
			}
			function safelyDetachRef(current, nearestMountedAncestor) {
				var ref = current.ref;
				if (ref !== null) {
					if (typeof ref === "function") {
						var retVal;
						try {
							if (enableProfilerTimer && enableProfilerCommitHooks && current.mode & ProfileMode) try {
								startLayoutEffectTimer();
								retVal = ref(null);
							} finally {
								recordLayoutEffectDuration(current);
							}
							else retVal = ref(null);
						} catch (error) {
							captureCommitPhaseError(current, nearestMountedAncestor, error);
						}
						if (typeof retVal === "function") error("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(current));
					} else ref.current = null;
				}
			}
			function safelyCallDestroy(current, nearestMountedAncestor, destroy) {
				try {
					destroy();
				} catch (error) {
					captureCommitPhaseError(current, nearestMountedAncestor, error);
				}
			}
			var shouldFireAfterActiveInstanceBlur = false;
			function commitBeforeMutationEffects(root, firstChild) {
				prepareForCommit(root.containerInfo);
				nextEffect = firstChild;
				commitBeforeMutationEffects_begin();
				var shouldFire = shouldFireAfterActiveInstanceBlur;
				shouldFireAfterActiveInstanceBlur = false;
				return shouldFire;
			}
			function commitBeforeMutationEffects_begin() {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var child = fiber.child;
					if ((fiber.subtreeFlags & BeforeMutationMask) !== NoFlags && child !== null) {
						child.return = fiber;
						nextEffect = child;
					} else commitBeforeMutationEffects_complete();
				}
			}
			function commitBeforeMutationEffects_complete() {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					setCurrentFiber(fiber);
					try {
						commitBeforeMutationEffectsOnFiber(fiber);
					} catch (error) {
						captureCommitPhaseError(fiber, fiber.return, error);
					}
					resetCurrentFiber();
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function commitBeforeMutationEffectsOnFiber(finishedWork) {
				var current = finishedWork.alternate;
				if ((finishedWork.flags & Snapshot) !== NoFlags) {
					setCurrentFiber(finishedWork);
					switch (finishedWork.tag) {
						case FunctionComponent:
						case ForwardRef:
						case SimpleMemoComponent: break;
						case ClassComponent:
							if (current !== null) {
								var prevProps = current.memoizedProps;
								var prevState = current.memoizedState;
								var instance = finishedWork.stateNode;
								if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
									if (instance.props !== finishedWork.memoizedProps) error("Expected %s props to match memoized props before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
									if (instance.state !== finishedWork.memoizedState) error("Expected %s state to match memoized state before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
								}
								var snapshot = instance.getSnapshotBeforeUpdate(finishedWork.elementType === finishedWork.type ? prevProps : resolveDefaultProps(finishedWork.type, prevProps), prevState);
								var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;
								if (snapshot === void 0 && !didWarnSet.has(finishedWork.type)) {
									didWarnSet.add(finishedWork.type);
									error("%s.getSnapshotBeforeUpdate(): A snapshot value (or null) must be returned. You have returned undefined.", getComponentNameFromFiber(finishedWork));
								}
								instance.__reactInternalSnapshotBeforeUpdate = snapshot;
							}
							break;
						case HostRoot:
							var root = finishedWork.stateNode;
							clearContainer(root.containerInfo);
							break;
						case HostComponent:
						case HostText:
						case HostPortal:
						case IncompleteClassComponent: break;
						default: throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
					}
					resetCurrentFiber();
				}
			}
			function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor) {
				var updateQueue = finishedWork.updateQueue;
				var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
				if (lastEffect !== null) {
					var firstEffect = lastEffect.next;
					var effect = firstEffect;
					do {
						if ((effect.tag & flags) === flags) {
							var destroy = effect.destroy;
							effect.destroy = void 0;
							if (destroy !== void 0) {
								if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectUnmountStarted(finishedWork);
								else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectUnmountStarted(finishedWork);
								if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(true);
								safelyCallDestroy(finishedWork, nearestMountedAncestor, destroy);
								if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(false);
								if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectUnmountStopped();
								else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectUnmountStopped();
							}
						}
						effect = effect.next;
					} while (effect !== firstEffect);
				}
			}
			function commitHookEffectListMount(flags, finishedWork) {
				var updateQueue = finishedWork.updateQueue;
				var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;
				if (lastEffect !== null) {
					var firstEffect = lastEffect.next;
					var effect = firstEffect;
					do {
						if ((effect.tag & flags) === flags) {
							if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectMountStarted(finishedWork);
							else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectMountStarted(finishedWork);
							var create = effect.create;
							if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(true);
							effect.destroy = create();
							if ((flags & Insertion) !== NoFlags$1) setIsRunningInsertionEffect(false);
							if ((flags & Passive$1) !== NoFlags$1) markComponentPassiveEffectMountStopped();
							else if ((flags & Layout) !== NoFlags$1) markComponentLayoutEffectMountStopped();
							var destroy = effect.destroy;
							if (destroy !== void 0 && typeof destroy !== "function") {
								var hookName = void 0;
								if ((effect.tag & Layout) !== NoFlags) hookName = "useLayoutEffect";
								else if ((effect.tag & Insertion) !== NoFlags) hookName = "useInsertionEffect";
								else hookName = "useEffect";
								var addendum = void 0;
								if (destroy === null) addendum = " You returned null. If your effect does not require clean up, return undefined (or nothing).";
								else if (typeof destroy.then === "function") addendum = "\n\nIt looks like you wrote " + hookName + "(async () => ...) or returned a Promise. Instead, write the async function inside your effect and call it immediately:\n\n" + hookName + "(() => {\n  async function fetchData() {\n    // You can await here\n    const response = await MyAPI.getData(someId);\n    // ...\n  }\n  fetchData();\n}, [someId]); // Or [] if effect doesn't need props or state\n\nLearn more about data fetching with Hooks: https://reactjs.org/link/hooks-data-fetching";
								else addendum = " You returned: " + destroy;
								error("%s must not return anything besides a function, which is used for clean-up.%s", hookName, addendum);
							}
						}
						effect = effect.next;
					} while (effect !== firstEffect);
				}
			}
			function commitPassiveEffectDurations(finishedRoot, finishedWork) {
				if ((finishedWork.flags & Update) !== NoFlags) switch (finishedWork.tag) {
					case Profiler:
						var passiveEffectDuration = finishedWork.stateNode.passiveEffectDuration;
						var _finishedWork$memoize = finishedWork.memoizedProps, id = _finishedWork$memoize.id, onPostCommit = _finishedWork$memoize.onPostCommit;
						var commitTime = getCommitTime();
						var phase = finishedWork.alternate === null ? "mount" : "update";
						if (isCurrentUpdateNested()) phase = "nested-update";
						if (typeof onPostCommit === "function") onPostCommit(id, phase, passiveEffectDuration, commitTime);
						var parentFiber = finishedWork.return;
						outer: while (parentFiber !== null) {
							switch (parentFiber.tag) {
								case HostRoot:
									var root = parentFiber.stateNode;
									root.passiveEffectDuration += passiveEffectDuration;
									break outer;
								case Profiler:
									var parentStateNode = parentFiber.stateNode;
									parentStateNode.passiveEffectDuration += passiveEffectDuration;
									break outer;
							}
							parentFiber = parentFiber.return;
						}
				}
			}
			function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork, committedLanes) {
				if ((finishedWork.flags & LayoutMask) !== NoFlags) switch (finishedWork.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent:
						if (!offscreenSubtreeWasHidden) {
							if (finishedWork.mode & ProfileMode) try {
								startLayoutEffectTimer();
								commitHookEffectListMount(Layout | HasEffect, finishedWork);
							} finally {
								recordLayoutEffectDuration(finishedWork);
							}
							else commitHookEffectListMount(Layout | HasEffect, finishedWork);
						}
						break;
					case ClassComponent:
						var instance = finishedWork.stateNode;
						if (finishedWork.flags & Update) {
							if (!offscreenSubtreeWasHidden) {
								if (current === null) {
									if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
										if (instance.props !== finishedWork.memoizedProps) error("Expected %s props to match memoized props before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
										if (instance.state !== finishedWork.memoizedState) error("Expected %s state to match memoized state before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
									}
									if (finishedWork.mode & ProfileMode) try {
										startLayoutEffectTimer();
										instance.componentDidMount();
									} finally {
										recordLayoutEffectDuration(finishedWork);
									}
									else instance.componentDidMount();
								} else {
									var prevProps = finishedWork.elementType === finishedWork.type ? current.memoizedProps : resolveDefaultProps(finishedWork.type, current.memoizedProps);
									var prevState = current.memoizedState;
									if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
										if (instance.props !== finishedWork.memoizedProps) error("Expected %s props to match memoized props before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
										if (instance.state !== finishedWork.memoizedState) error("Expected %s state to match memoized state before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
									}
									if (finishedWork.mode & ProfileMode) try {
										startLayoutEffectTimer();
										instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
									} finally {
										recordLayoutEffectDuration(finishedWork);
									}
									else instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
								}
							}
						}
						var updateQueue = finishedWork.updateQueue;
						if (updateQueue !== null) {
							if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
								if (instance.props !== finishedWork.memoizedProps) error("Expected %s props to match memoized props before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
								if (instance.state !== finishedWork.memoizedState) error("Expected %s state to match memoized state before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance");
							}
							commitUpdateQueue(finishedWork, updateQueue, instance);
						}
						break;
					case HostRoot:
						var _updateQueue = finishedWork.updateQueue;
						if (_updateQueue !== null) {
							var _instance = null;
							if (finishedWork.child !== null) switch (finishedWork.child.tag) {
								case HostComponent:
									_instance = getPublicInstance(finishedWork.child.stateNode);
									break;
								case ClassComponent: _instance = finishedWork.child.stateNode;
							}
							commitUpdateQueue(finishedWork, _updateQueue, _instance);
						}
						break;
					case HostComponent:
						var _instance2 = finishedWork.stateNode;
						if (current === null && finishedWork.flags & Update) {
							var type = finishedWork.type;
							var props = finishedWork.memoizedProps;
							commitMount(_instance2, type, props);
						}
						break;
					case HostText: break;
					case HostPortal: break;
					case Profiler:
						var _finishedWork$memoize2 = finishedWork.memoizedProps, onCommit = _finishedWork$memoize2.onCommit, onRender = _finishedWork$memoize2.onRender;
						var effectDuration = finishedWork.stateNode.effectDuration;
						var commitTime = getCommitTime();
						var phase = current === null ? "mount" : "update";
						if (isCurrentUpdateNested()) phase = "nested-update";
						if (typeof onRender === "function") onRender(finishedWork.memoizedProps.id, phase, finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, commitTime);
						if (typeof onCommit === "function") onCommit(finishedWork.memoizedProps.id, phase, effectDuration, commitTime);
						enqueuePendingPassiveProfilerEffect(finishedWork);
						var parentFiber = finishedWork.return;
						outer: while (parentFiber !== null) {
							switch (parentFiber.tag) {
								case HostRoot:
									var root = parentFiber.stateNode;
									root.effectDuration += effectDuration;
									break outer;
								case Profiler:
									var parentStateNode = parentFiber.stateNode;
									parentStateNode.effectDuration += effectDuration;
									break outer;
							}
							parentFiber = parentFiber.return;
						}
						break;
					case SuspenseComponent:
						commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
						break;
					case SuspenseListComponent:
					case IncompleteClassComponent:
					case ScopeComponent:
					case OffscreenComponent:
					case LegacyHiddenComponent:
					case TracingMarkerComponent: break;
					default: throw new Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
				}
				if (!offscreenSubtreeWasHidden) {
					if (finishedWork.flags & Ref) commitAttachRef(finishedWork);
				}
			}
			function reappearLayoutEffectsOnFiber(node) {
				switch (node.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent:
						if (node.mode & ProfileMode) try {
							startLayoutEffectTimer();
							safelyCallCommitHookLayoutEffectListMount(node, node.return);
						} finally {
							recordLayoutEffectDuration(node);
						}
						else safelyCallCommitHookLayoutEffectListMount(node, node.return);
						break;
					case ClassComponent:
						var instance = node.stateNode;
						if (typeof instance.componentDidMount === "function") safelyCallComponentDidMount(node, node.return, instance);
						safelyAttachRef(node, node.return);
						break;
					case HostComponent: safelyAttachRef(node, node.return);
				}
			}
			function hideOrUnhideAllChildren(finishedWork, isHidden) {
				var hostSubtreeRoot = null;
				var node = finishedWork;
				while (true) {
					if (node.tag === HostComponent) {
						if (hostSubtreeRoot === null) {
							hostSubtreeRoot = node;
							try {
								var instance = node.stateNode;
								if (isHidden) hideInstance(instance);
								else unhideInstance(node.stateNode, node.memoizedProps);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
						}
					} else if (node.tag === HostText) {
						if (hostSubtreeRoot === null) try {
							var _instance3 = node.stateNode;
							if (isHidden) hideTextInstance(_instance3);
							else unhideTextInstance(_instance3, node.memoizedProps);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					} else if ((node.tag === OffscreenComponent || node.tag === LegacyHiddenComponent) && node.memoizedState !== null && node !== finishedWork);
					else if (node.child !== null) {
						node.child.return = node;
						node = node.child;
						continue;
					}
					if (node === finishedWork) return;
					while (node.sibling === null) {
						if (node.return === null || node.return === finishedWork) return;
						if (hostSubtreeRoot === node) hostSubtreeRoot = null;
						node = node.return;
					}
					if (hostSubtreeRoot === node) hostSubtreeRoot = null;
					node.sibling.return = node.return;
					node = node.sibling;
				}
			}
			function commitAttachRef(finishedWork) {
				var ref = finishedWork.ref;
				if (ref !== null) {
					var instance = finishedWork.stateNode;
					var instanceToUse;
					switch (finishedWork.tag) {
						case HostComponent:
							instanceToUse = getPublicInstance(instance);
							break;
						default: instanceToUse = instance;
					}
					if (typeof ref === "function") {
						var retVal;
						if (finishedWork.mode & ProfileMode) try {
							startLayoutEffectTimer();
							retVal = ref(instanceToUse);
						} finally {
							recordLayoutEffectDuration(finishedWork);
						}
						else retVal = ref(instanceToUse);
						if (typeof retVal === "function") error("Unexpected return value from a callback ref in %s. A callback ref should not return a function.", getComponentNameFromFiber(finishedWork));
					} else {
						if (!ref.hasOwnProperty("current")) error("Unexpected ref object provided for %s. Use either a ref-setter function or React.createRef().", getComponentNameFromFiber(finishedWork));
						ref.current = instanceToUse;
					}
				}
			}
			function detachFiberMutation(fiber) {
				var alternate = fiber.alternate;
				if (alternate !== null) alternate.return = null;
				fiber.return = null;
			}
			function detachFiberAfterEffects(fiber) {
				var alternate = fiber.alternate;
				if (alternate !== null) {
					fiber.alternate = null;
					detachFiberAfterEffects(alternate);
				}
				fiber.child = null;
				fiber.deletions = null;
				fiber.sibling = null;
				if (fiber.tag === HostComponent) {
					var hostInstance = fiber.stateNode;
					if (hostInstance !== null) detachDeletedInstance(hostInstance);
				}
				fiber.stateNode = null;
				fiber._debugOwner = null;
				fiber.return = null;
				fiber.dependencies = null;
				fiber.memoizedProps = null;
				fiber.memoizedState = null;
				fiber.pendingProps = null;
				fiber.stateNode = null;
				fiber.updateQueue = null;
			}
			function getHostParentFiber(fiber) {
				var parent = fiber.return;
				while (parent !== null) {
					if (isHostParent(parent)) return parent;
					parent = parent.return;
				}
				throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
			}
			function isHostParent(fiber) {
				return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
			}
			function getHostSibling(fiber) {
				var node = fiber;
				siblings: while (true) {
					while (node.sibling === null) {
						if (node.return === null || isHostParent(node.return)) return null;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
					while (node.tag !== HostComponent && node.tag !== HostText && node.tag !== DehydratedFragment) {
						if (node.flags & Placement) continue siblings;
						if (node.child === null || node.tag === HostPortal) continue siblings;
						else {
							node.child.return = node;
							node = node.child;
						}
					}
					if (!(node.flags & Placement)) return node.stateNode;
				}
			}
			function commitPlacement(finishedWork) {
				var parentFiber = getHostParentFiber(finishedWork);
				switch (parentFiber.tag) {
					case HostComponent:
						var parent = parentFiber.stateNode;
						if (parentFiber.flags & ContentReset) {
							resetTextContent(parent);
							parentFiber.flags &= ~ContentReset;
						}
						insertOrAppendPlacementNode(finishedWork, getHostSibling(finishedWork), parent);
						break;
					case HostRoot:
					case HostPortal:
						var _parent = parentFiber.stateNode.containerInfo;
						insertOrAppendPlacementNodeIntoContainer(finishedWork, getHostSibling(finishedWork), _parent);
						break;
					default: throw new Error("Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.");
				}
			}
			function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
				var tag = node.tag;
				if (tag === HostComponent || tag === HostText) {
					var stateNode = node.stateNode;
					if (before) insertInContainerBefore(parent, stateNode, before);
					else appendChildToContainer(parent, stateNode);
				} else if (tag === HostPortal);
				else {
					var child = node.child;
					if (child !== null) {
						insertOrAppendPlacementNodeIntoContainer(child, before, parent);
						var sibling = child.sibling;
						while (sibling !== null) {
							insertOrAppendPlacementNodeIntoContainer(sibling, before, parent);
							sibling = sibling.sibling;
						}
					}
				}
			}
			function insertOrAppendPlacementNode(node, before, parent) {
				var tag = node.tag;
				if (tag === HostComponent || tag === HostText) {
					var stateNode = node.stateNode;
					if (before) insertBefore(parent, stateNode, before);
					else appendChild(parent, stateNode);
				} else if (tag === HostPortal);
				else {
					var child = node.child;
					if (child !== null) {
						insertOrAppendPlacementNode(child, before, parent);
						var sibling = child.sibling;
						while (sibling !== null) {
							insertOrAppendPlacementNode(sibling, before, parent);
							sibling = sibling.sibling;
						}
					}
				}
			}
			var hostParent = null;
			var hostParentIsContainer = false;
			function commitDeletionEffects(root, returnFiber, deletedFiber) {
				var parent = returnFiber;
				findParent: while (parent !== null) {
					switch (parent.tag) {
						case HostComponent:
							hostParent = parent.stateNode;
							hostParentIsContainer = false;
							break findParent;
						case HostRoot:
							hostParent = parent.stateNode.containerInfo;
							hostParentIsContainer = true;
							break findParent;
						case HostPortal:
							hostParent = parent.stateNode.containerInfo;
							hostParentIsContainer = true;
							break findParent;
					}
					parent = parent.return;
				}
				if (hostParent === null) throw new Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
				commitDeletionEffectsOnFiber(root, returnFiber, deletedFiber);
				hostParent = null;
				hostParentIsContainer = false;
				detachFiberMutation(deletedFiber);
			}
			function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
				var child = parent.child;
				while (child !== null) {
					commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, child);
					child = child.sibling;
				}
			}
			function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
				onCommitUnmount(deletedFiber);
				switch (deletedFiber.tag) {
					case HostComponent: if (!offscreenSubtreeWasHidden) safelyDetachRef(deletedFiber, nearestMountedAncestor);
					case HostText:
						var prevHostParent = hostParent;
						var prevHostParentIsContainer = hostParentIsContainer;
						hostParent = null;
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						hostParent = prevHostParent;
						hostParentIsContainer = prevHostParentIsContainer;
						if (hostParent !== null) {
							if (hostParentIsContainer) removeChildFromContainer(hostParent, deletedFiber.stateNode);
							else removeChild(hostParent, deletedFiber.stateNode);
						}
						return;
					case DehydratedFragment:
						if (hostParent !== null) {
							if (hostParentIsContainer) clearSuspenseBoundaryFromContainer(hostParent, deletedFiber.stateNode);
							else clearSuspenseBoundary(hostParent, deletedFiber.stateNode);
						}
						return;
					case HostPortal:
						var _prevHostParent = hostParent;
						var _prevHostParentIsContainer = hostParentIsContainer;
						hostParent = deletedFiber.stateNode.containerInfo;
						hostParentIsContainer = true;
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						hostParent = _prevHostParent;
						hostParentIsContainer = _prevHostParentIsContainer;
						return;
					case FunctionComponent:
					case ForwardRef:
					case MemoComponent:
					case SimpleMemoComponent:
						if (!offscreenSubtreeWasHidden) {
							var updateQueue = deletedFiber.updateQueue;
							if (updateQueue !== null) {
								var lastEffect = updateQueue.lastEffect;
								if (lastEffect !== null) {
									var firstEffect = lastEffect.next;
									var effect = firstEffect;
									do {
										var _effect = effect, destroy = _effect.destroy, tag = _effect.tag;
										if (destroy !== void 0) {
											if ((tag & Insertion) !== NoFlags$1) safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
											else if ((tag & Layout) !== NoFlags$1) {
												markComponentLayoutEffectUnmountStarted(deletedFiber);
												if (deletedFiber.mode & ProfileMode) {
													startLayoutEffectTimer();
													safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
													recordLayoutEffectDuration(deletedFiber);
												} else safelyCallDestroy(deletedFiber, nearestMountedAncestor, destroy);
												markComponentLayoutEffectUnmountStopped();
											}
										}
										effect = effect.next;
									} while (effect !== firstEffect);
								}
							}
						}
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						return;
					case ClassComponent:
						if (!offscreenSubtreeWasHidden) {
							safelyDetachRef(deletedFiber, nearestMountedAncestor);
							var instance = deletedFiber.stateNode;
							if (typeof instance.componentWillUnmount === "function") safelyCallComponentWillUnmount(deletedFiber, nearestMountedAncestor, instance);
						}
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						return;
					case ScopeComponent:
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						return;
					case OffscreenComponent:
						if (deletedFiber.mode & ConcurrentMode) {
							var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
							offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || deletedFiber.memoizedState !== null;
							recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
							offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
						} else recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						break;
					default:
						recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
						return;
				}
			}
			function commitSuspenseCallback(finishedWork) {
				finishedWork.memoizedState;
			}
			function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
				if (finishedWork.memoizedState === null) {
					var current = finishedWork.alternate;
					if (current !== null) {
						var prevState = current.memoizedState;
						if (prevState !== null) {
							var suspenseInstance = prevState.dehydrated;
							if (suspenseInstance !== null) commitHydratedSuspenseInstance(suspenseInstance);
						}
					}
				}
			}
			function attachSuspenseRetryListeners(finishedWork) {
				var wakeables = finishedWork.updateQueue;
				if (wakeables !== null) {
					finishedWork.updateQueue = null;
					var retryCache = finishedWork.stateNode;
					if (retryCache === null) retryCache = finishedWork.stateNode = new PossiblyWeakSet();
					wakeables.forEach(function(wakeable) {
						var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
						if (!retryCache.has(wakeable)) {
							retryCache.add(wakeable);
							if (isDevToolsPresent) {
								if (inProgressLanes !== null && inProgressRoot !== null) restorePendingUpdaters(inProgressRoot, inProgressLanes);
								else throw Error("Expected finished root and lanes to be set. This is a bug in React.");
							}
							wakeable.then(retry, retry);
						}
					});
				}
			}
			function commitMutationEffects(root, finishedWork, committedLanes) {
				inProgressLanes = committedLanes;
				inProgressRoot = root;
				setCurrentFiber(finishedWork);
				commitMutationEffectsOnFiber(finishedWork, root);
				setCurrentFiber(finishedWork);
				inProgressLanes = null;
				inProgressRoot = null;
			}
			function recursivelyTraverseMutationEffects(root, parentFiber, lanes) {
				var deletions = parentFiber.deletions;
				if (deletions !== null) for (var i = 0; i < deletions.length; i++) {
					var childToDelete = deletions[i];
					try {
						commitDeletionEffects(root, parentFiber, childToDelete);
					} catch (error) {
						captureCommitPhaseError(childToDelete, parentFiber, error);
					}
				}
				var prevDebugFiber = getCurrentFiber();
				if (parentFiber.subtreeFlags & MutationMask) {
					var child = parentFiber.child;
					while (child !== null) {
						setCurrentFiber(child);
						commitMutationEffectsOnFiber(child, root);
						child = child.sibling;
					}
				}
				setCurrentFiber(prevDebugFiber);
			}
			function commitMutationEffectsOnFiber(finishedWork, root, lanes) {
				var current = finishedWork.alternate;
				var flags = finishedWork.flags;
				switch (finishedWork.tag) {
					case FunctionComponent:
					case ForwardRef:
					case MemoComponent:
					case SimpleMemoComponent:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Update) {
							try {
								commitHookEffectListUnmount(Insertion | HasEffect, finishedWork, finishedWork.return);
								commitHookEffectListMount(Insertion | HasEffect, finishedWork);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
							if (finishedWork.mode & ProfileMode) {
								try {
									startLayoutEffectTimer();
									commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
								} catch (error) {
									captureCommitPhaseError(finishedWork, finishedWork.return, error);
								}
								recordLayoutEffectDuration(finishedWork);
							} else try {
								commitHookEffectListUnmount(Layout | HasEffect, finishedWork, finishedWork.return);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
						}
						return;
					case ClassComponent:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Ref) {
							if (current !== null) safelyDetachRef(current, current.return);
						}
						return;
					case HostComponent:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Ref) {
							if (current !== null) safelyDetachRef(current, current.return);
						}
						if (finishedWork.flags & ContentReset) {
							var instance = finishedWork.stateNode;
							try {
								resetTextContent(instance);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
						}
						if (flags & Update) {
							var _instance4 = finishedWork.stateNode;
							if (_instance4 != null) {
								var newProps = finishedWork.memoizedProps;
								var oldProps = current !== null ? current.memoizedProps : newProps;
								var type = finishedWork.type;
								var updatePayload = finishedWork.updateQueue;
								finishedWork.updateQueue = null;
								if (updatePayload !== null) try {
									commitUpdate(_instance4, updatePayload, type, oldProps, newProps, finishedWork);
								} catch (error) {
									captureCommitPhaseError(finishedWork, finishedWork.return, error);
								}
							}
						}
						return;
					case HostText:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Update) {
							if (finishedWork.stateNode === null) throw new Error("This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.");
							var textInstance = finishedWork.stateNode;
							var newText = finishedWork.memoizedProps;
							var oldText = current !== null ? current.memoizedProps : newText;
							try {
								commitTextUpdate(textInstance, oldText, newText);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
						}
						return;
					case HostRoot:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Update) {
							if (current !== null) {
								if (current.memoizedState.isDehydrated) try {
									commitHydratedContainer(root.containerInfo);
								} catch (error) {
									captureCommitPhaseError(finishedWork, finishedWork.return, error);
								}
							}
						}
						return;
					case HostPortal:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						return;
					case SuspenseComponent:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						var offscreenFiber = finishedWork.child;
						if (offscreenFiber.flags & Visibility) {
							var offscreenInstance = offscreenFiber.stateNode;
							var isHidden = offscreenFiber.memoizedState !== null;
							offscreenInstance.isHidden = isHidden;
							if (isHidden) {
								if (!(offscreenFiber.alternate !== null && offscreenFiber.alternate.memoizedState !== null)) markCommitTimeOfFallback();
							}
						}
						if (flags & Update) {
							try {
								commitSuspenseCallback(finishedWork);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
							attachSuspenseRetryListeners(finishedWork);
						}
						return;
					case OffscreenComponent:
						var _wasHidden = current !== null && current.memoizedState !== null;
						if (finishedWork.mode & ConcurrentMode) {
							var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
							offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || _wasHidden;
							recursivelyTraverseMutationEffects(root, finishedWork);
							offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
						} else recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Visibility) {
							var _offscreenInstance = finishedWork.stateNode;
							var _isHidden = finishedWork.memoizedState !== null;
							var offscreenBoundary = finishedWork;
							_offscreenInstance.isHidden = _isHidden;
							if (_isHidden) {
								if (!_wasHidden) {
									if ((offscreenBoundary.mode & ConcurrentMode) !== NoMode) {
										nextEffect = offscreenBoundary;
										var offscreenChild = offscreenBoundary.child;
										while (offscreenChild !== null) {
											nextEffect = offscreenChild;
											disappearLayoutEffects_begin(offscreenChild);
											offscreenChild = offscreenChild.sibling;
										}
									}
								}
							}
							hideOrUnhideAllChildren(offscreenBoundary, _isHidden);
						}
						return;
					case SuspenseListComponent:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						if (flags & Update) attachSuspenseRetryListeners(finishedWork);
						return;
					case ScopeComponent: return;
					default:
						recursivelyTraverseMutationEffects(root, finishedWork);
						commitReconciliationEffects(finishedWork);
						return;
				}
			}
			function commitReconciliationEffects(finishedWork) {
				var flags = finishedWork.flags;
				if (flags & Placement) {
					try {
						commitPlacement(finishedWork);
					} catch (error) {
						captureCommitPhaseError(finishedWork, finishedWork.return, error);
					}
					finishedWork.flags &= ~Placement;
				}
				if (flags & Hydrating) finishedWork.flags &= ~Hydrating;
			}
			function commitLayoutEffects(finishedWork, root, committedLanes) {
				inProgressLanes = committedLanes;
				inProgressRoot = root;
				nextEffect = finishedWork;
				commitLayoutEffects_begin(finishedWork, root, committedLanes);
				inProgressLanes = null;
				inProgressRoot = null;
			}
			function commitLayoutEffects_begin(subtreeRoot, root, committedLanes) {
				var isModernRoot = (subtreeRoot.mode & ConcurrentMode) !== NoMode;
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var firstChild = fiber.child;
					if (fiber.tag === OffscreenComponent && isModernRoot) {
						var newOffscreenSubtreeIsHidden = fiber.memoizedState !== null || offscreenSubtreeIsHidden;
						if (newOffscreenSubtreeIsHidden) {
							commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
							continue;
						} else {
							var current = fiber.alternate;
							var newOffscreenSubtreeWasHidden = current !== null && current.memoizedState !== null || offscreenSubtreeWasHidden;
							var prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden;
							var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
							offscreenSubtreeIsHidden = newOffscreenSubtreeIsHidden;
							offscreenSubtreeWasHidden = newOffscreenSubtreeWasHidden;
							if (offscreenSubtreeWasHidden && !prevOffscreenSubtreeWasHidden) {
								nextEffect = fiber;
								reappearLayoutEffects_begin(fiber);
							}
							var child = firstChild;
							while (child !== null) {
								nextEffect = child;
								commitLayoutEffects_begin(child, root, committedLanes);
								child = child.sibling;
							}
							nextEffect = fiber;
							offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
							offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
							commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
							continue;
						}
					}
					if ((fiber.subtreeFlags & LayoutMask) !== NoFlags && firstChild !== null) {
						firstChild.return = fiber;
						nextEffect = firstChild;
					} else commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes);
				}
			}
			function commitLayoutMountEffects_complete(subtreeRoot, root, committedLanes) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					if ((fiber.flags & LayoutMask) !== NoFlags) {
						var current = fiber.alternate;
						setCurrentFiber(fiber);
						try {
							commitLayoutEffectOnFiber(root, current, fiber, committedLanes);
						} catch (error) {
							captureCommitPhaseError(fiber, fiber.return, error);
						}
						resetCurrentFiber();
					}
					if (fiber === subtreeRoot) {
						nextEffect = null;
						return;
					}
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function disappearLayoutEffects_begin(subtreeRoot) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var firstChild = fiber.child;
					switch (fiber.tag) {
						case FunctionComponent:
						case ForwardRef:
						case MemoComponent:
						case SimpleMemoComponent:
							if (fiber.mode & ProfileMode) try {
								startLayoutEffectTimer();
								commitHookEffectListUnmount(Layout, fiber, fiber.return);
							} finally {
								recordLayoutEffectDuration(fiber);
							}
							else commitHookEffectListUnmount(Layout, fiber, fiber.return);
							break;
						case ClassComponent:
							safelyDetachRef(fiber, fiber.return);
							var instance = fiber.stateNode;
							if (typeof instance.componentWillUnmount === "function") safelyCallComponentWillUnmount(fiber, fiber.return, instance);
							break;
						case HostComponent:
							safelyDetachRef(fiber, fiber.return);
							break;
						case OffscreenComponent: if (fiber.memoizedState !== null) {
							disappearLayoutEffects_complete(subtreeRoot);
							continue;
						}
					}
					if (firstChild !== null) {
						firstChild.return = fiber;
						nextEffect = firstChild;
					} else disappearLayoutEffects_complete(subtreeRoot);
				}
			}
			function disappearLayoutEffects_complete(subtreeRoot) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					if (fiber === subtreeRoot) {
						nextEffect = null;
						return;
					}
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function reappearLayoutEffects_begin(subtreeRoot) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var firstChild = fiber.child;
					if (fiber.tag === OffscreenComponent) {
						if (fiber.memoizedState !== null) {
							reappearLayoutEffects_complete(subtreeRoot);
							continue;
						}
					}
					if (firstChild !== null) {
						firstChild.return = fiber;
						nextEffect = firstChild;
					} else reappearLayoutEffects_complete(subtreeRoot);
				}
			}
			function reappearLayoutEffects_complete(subtreeRoot) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					setCurrentFiber(fiber);
					try {
						reappearLayoutEffectsOnFiber(fiber);
					} catch (error) {
						captureCommitPhaseError(fiber, fiber.return, error);
					}
					resetCurrentFiber();
					if (fiber === subtreeRoot) {
						nextEffect = null;
						return;
					}
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function commitPassiveMountEffects(root, finishedWork, committedLanes, committedTransitions) {
				nextEffect = finishedWork;
				commitPassiveMountEffects_begin(finishedWork, root, committedLanes, committedTransitions);
			}
			function commitPassiveMountEffects_begin(subtreeRoot, root, committedLanes, committedTransitions) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var firstChild = fiber.child;
					if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && firstChild !== null) {
						firstChild.return = fiber;
						nextEffect = firstChild;
					} else commitPassiveMountEffects_complete(subtreeRoot, root, committedLanes, committedTransitions);
				}
			}
			function commitPassiveMountEffects_complete(subtreeRoot, root, committedLanes, committedTransitions) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					if ((fiber.flags & Passive) !== NoFlags) {
						setCurrentFiber(fiber);
						try {
							commitPassiveMountOnFiber(root, fiber, committedLanes, committedTransitions);
						} catch (error) {
							captureCommitPhaseError(fiber, fiber.return, error);
						}
						resetCurrentFiber();
					}
					if (fiber === subtreeRoot) {
						nextEffect = null;
						return;
					}
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
				switch (finishedWork.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent: if (finishedWork.mode & ProfileMode) {
						startPassiveEffectTimer();
						try {
							commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
						} finally {
							recordPassiveEffectDuration(finishedWork);
						}
					} else commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
				}
			}
			function commitPassiveUnmountEffects(firstChild) {
				nextEffect = firstChild;
				commitPassiveUnmountEffects_begin();
			}
			function commitPassiveUnmountEffects_begin() {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var child = fiber.child;
					if ((nextEffect.flags & ChildDeletion) !== NoFlags) {
						var deletions = fiber.deletions;
						if (deletions !== null) {
							for (var i = 0; i < deletions.length; i++) {
								var fiberToDelete = deletions[i];
								nextEffect = fiberToDelete;
								commitPassiveUnmountEffectsInsideOfDeletedTree_begin(fiberToDelete, fiber);
							}
							var previousFiber = fiber.alternate;
							if (previousFiber !== null) {
								var detachedChild = previousFiber.child;
								if (detachedChild !== null) {
									previousFiber.child = null;
									do {
										var detachedSibling = detachedChild.sibling;
										detachedChild.sibling = null;
										detachedChild = detachedSibling;
									} while (detachedChild !== null);
								}
							}
							nextEffect = fiber;
						}
					}
					if ((fiber.subtreeFlags & PassiveMask) !== NoFlags && child !== null) {
						child.return = fiber;
						nextEffect = child;
					} else commitPassiveUnmountEffects_complete();
				}
			}
			function commitPassiveUnmountEffects_complete() {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					if ((fiber.flags & Passive) !== NoFlags) {
						setCurrentFiber(fiber);
						commitPassiveUnmountOnFiber(fiber);
						resetCurrentFiber();
					}
					var sibling = fiber.sibling;
					if (sibling !== null) {
						sibling.return = fiber.return;
						nextEffect = sibling;
						return;
					}
					nextEffect = fiber.return;
				}
			}
			function commitPassiveUnmountOnFiber(finishedWork) {
				switch (finishedWork.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent: if (finishedWork.mode & ProfileMode) {
						startPassiveEffectTimer();
						commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
						recordPassiveEffectDuration(finishedWork);
					} else commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork, finishedWork.return);
				}
			}
			function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					setCurrentFiber(fiber);
					commitPassiveUnmountInsideDeletedTreeOnFiber(fiber, nearestMountedAncestor);
					resetCurrentFiber();
					var child = fiber.child;
					if (child !== null) {
						child.return = fiber;
						nextEffect = child;
					} else commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot);
				}
			}
			function commitPassiveUnmountEffectsInsideOfDeletedTree_complete(deletedSubtreeRoot) {
				while (nextEffect !== null) {
					var fiber = nextEffect;
					var sibling = fiber.sibling;
					var returnFiber = fiber.return;
					detachFiberAfterEffects(fiber);
					if (fiber === deletedSubtreeRoot) {
						nextEffect = null;
						return;
					}
					if (sibling !== null) {
						sibling.return = returnFiber;
						nextEffect = sibling;
						return;
					}
					nextEffect = returnFiber;
				}
			}
			function commitPassiveUnmountInsideDeletedTreeOnFiber(current, nearestMountedAncestor) {
				switch (current.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent: if (current.mode & ProfileMode) {
						startPassiveEffectTimer();
						commitHookEffectListUnmount(Passive$1, current, nearestMountedAncestor);
						recordPassiveEffectDuration(current);
					} else commitHookEffectListUnmount(Passive$1, current, nearestMountedAncestor);
				}
			}
			function invokeLayoutEffectMountInDEV(fiber) {
				switch (fiber.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent:
						try {
							commitHookEffectListMount(Layout | HasEffect, fiber);
						} catch (error) {
							captureCommitPhaseError(fiber, fiber.return, error);
						}
						break;
					case ClassComponent:
						var instance = fiber.stateNode;
						try {
							instance.componentDidMount();
						} catch (error) {
							captureCommitPhaseError(fiber, fiber.return, error);
						}
				}
			}
			function invokePassiveEffectMountInDEV(fiber) {
				switch (fiber.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent: try {
						commitHookEffectListMount(Passive$1 | HasEffect, fiber);
					} catch (error) {
						captureCommitPhaseError(fiber, fiber.return, error);
					}
				}
			}
			function invokeLayoutEffectUnmountInDEV(fiber) {
				switch (fiber.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent:
						try {
							commitHookEffectListUnmount(Layout | HasEffect, fiber, fiber.return);
						} catch (error) {
							captureCommitPhaseError(fiber, fiber.return, error);
						}
						break;
					case ClassComponent:
						var instance = fiber.stateNode;
						if (typeof instance.componentWillUnmount === "function") safelyCallComponentWillUnmount(fiber, fiber.return, instance);
				}
			}
			function invokePassiveEffectUnmountInDEV(fiber) {
				switch (fiber.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent: try {
						commitHookEffectListUnmount(Passive$1 | HasEffect, fiber, fiber.return);
					} catch (error) {
						captureCommitPhaseError(fiber, fiber.return, error);
					}
				}
			}
			if (typeof Symbol === "function" && Symbol.for) {
				var symbolFor = Symbol.for;
				symbolFor("selector.component");
				symbolFor("selector.has_pseudo_class");
				symbolFor("selector.role");
				symbolFor("selector.test_id");
				symbolFor("selector.text");
			}
			var commitHooks = [];
			function onCommitRoot$1() {
				commitHooks.forEach(function(commitHook) {
					return commitHook();
				});
			}
			var ReactCurrentActQueue = ReactSharedInternals.ReactCurrentActQueue;
			function isLegacyActEnvironment(fiber) {
				var isReactActEnvironmentGlobal = typeof IS_REACT_ACT_ENVIRONMENT !== "undefined" ? IS_REACT_ACT_ENVIRONMENT : void 0;
				return typeof jest !== "undefined" && isReactActEnvironmentGlobal !== false;
			}
			function isConcurrentActEnvironment() {
				var isReactActEnvironmentGlobal = typeof IS_REACT_ACT_ENVIRONMENT !== "undefined" ? IS_REACT_ACT_ENVIRONMENT : void 0;
				if (!isReactActEnvironmentGlobal && ReactCurrentActQueue.current !== null) error("The current testing environment is not configured to support act(...)");
				return isReactActEnvironmentGlobal;
			}
			var ceil = Math.ceil;
			var ReactCurrentDispatcher$2 = ReactSharedInternals.ReactCurrentDispatcher, ReactCurrentOwner$2 = ReactSharedInternals.ReactCurrentOwner, ReactCurrentBatchConfig$3 = ReactSharedInternals.ReactCurrentBatchConfig, ReactCurrentActQueue$1 = ReactSharedInternals.ReactCurrentActQueue;
			var NoContext = 0;
			var BatchedContext = 1;
			var RenderContext = 2;
			var CommitContext = 4;
			var RootInProgress = 0;
			var RootFatalErrored = 1;
			var RootErrored = 2;
			var RootSuspended = 3;
			var RootSuspendedWithDelay = 4;
			var RootCompleted = 5;
			var RootDidNotComplete = 6;
			var executionContext = NoContext;
			var workInProgressRoot = null;
			var workInProgress = null;
			var workInProgressRootRenderLanes = NoLanes;
			var subtreeRenderLanes = NoLanes;
			var subtreeRenderLanesCursor = createCursor(NoLanes);
			var workInProgressRootExitStatus = RootInProgress;
			var workInProgressRootFatalError = null;
			var workInProgressRootIncludedLanes = NoLanes;
			var workInProgressRootSkippedLanes = NoLanes;
			var workInProgressRootInterleavedUpdatedLanes = NoLanes;
			var workInProgressRootPingedLanes = NoLanes;
			var workInProgressRootConcurrentErrors = null;
			var workInProgressRootRecoverableErrors = null;
			var globalMostRecentFallbackTime = 0;
			var FALLBACK_THROTTLE_MS = 500;
			var workInProgressRootRenderTargetTime = Infinity;
			var RENDER_TIMEOUT_MS = 500;
			var workInProgressTransitions = null;
			function resetRenderTimer() {
				workInProgressRootRenderTargetTime = now() + RENDER_TIMEOUT_MS;
			}
			function getRenderTargetTime() {
				return workInProgressRootRenderTargetTime;
			}
			var hasUncaughtError = false;
			var firstUncaughtError = null;
			var legacyErrorBoundariesThatAlreadyFailed = null;
			var rootDoesHavePassiveEffects = false;
			var rootWithPendingPassiveEffects = null;
			var pendingPassiveEffectsLanes = NoLanes;
			var pendingPassiveProfilerEffects = [];
			var pendingPassiveTransitions = null;
			var NESTED_UPDATE_LIMIT = 50;
			var nestedUpdateCount = 0;
			var rootWithNestedUpdates = null;
			var isFlushingPassiveEffects = false;
			var didScheduleUpdateDuringPassiveEffects = false;
			var NESTED_PASSIVE_UPDATE_LIMIT = 50;
			var nestedPassiveUpdateCount = 0;
			var rootWithPassiveNestedUpdates = null;
			var currentEventTime = NoTimestamp;
			var currentEventTransitionLane = NoLanes;
			var isRunningInsertionEffect = false;
			function getWorkInProgressRoot() {
				return workInProgressRoot;
			}
			function requestEventTime() {
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) return now();
				if (currentEventTime !== NoTimestamp) return currentEventTime;
				currentEventTime = now();
				return currentEventTime;
			}
			function requestUpdateLane(fiber) {
				if ((fiber.mode & ConcurrentMode) === NoMode) return SyncLane;
				else if ((executionContext & RenderContext) !== NoContext && workInProgressRootRenderLanes !== NoLanes) return pickArbitraryLane(workInProgressRootRenderLanes);
				if (requestCurrentTransition() !== NoTransition) {
					if (ReactCurrentBatchConfig$3.transition !== null) {
						var transition = ReactCurrentBatchConfig$3.transition;
						if (!transition._updatedFibers) transition._updatedFibers = /* @__PURE__ */ new Set();
						transition._updatedFibers.add(fiber);
					}
					if (currentEventTransitionLane === NoLane) currentEventTransitionLane = claimNextTransitionLane();
					return currentEventTransitionLane;
				}
				var updateLane = getCurrentUpdatePriority();
				if (updateLane !== NoLane) return updateLane;
				return getCurrentEventPriority();
			}
			function requestRetryLane(fiber) {
				if ((fiber.mode & ConcurrentMode) === NoMode) return SyncLane;
				return claimNextRetryLane();
			}
			function scheduleUpdateOnFiber(root, fiber, lane, eventTime) {
				checkForNestedUpdates();
				if (isRunningInsertionEffect) error("useInsertionEffect must not schedule updates.");
				if (isFlushingPassiveEffects) didScheduleUpdateDuringPassiveEffects = true;
				markRootUpdated(root, lane, eventTime);
				if ((executionContext & RenderContext) !== NoLanes && root === workInProgressRoot) warnAboutRenderPhaseUpdatesInDEV(fiber);
				else {
					if (isDevToolsPresent) addFiberToLanesMap(root, fiber, lane);
					warnIfUpdatesNotWrappedWithActDEV(fiber);
					if (root === workInProgressRoot) {
						if ((executionContext & RenderContext) === NoContext) workInProgressRootInterleavedUpdatedLanes = mergeLanes(workInProgressRootInterleavedUpdatedLanes, lane);
						if (workInProgressRootExitStatus === RootSuspendedWithDelay) markRootSuspended$1(root, workInProgressRootRenderLanes);
					}
					ensureRootIsScheduled(root, eventTime);
					if (lane === SyncLane && executionContext === NoContext && (fiber.mode & ConcurrentMode) === NoMode && !ReactCurrentActQueue$1.isBatchingLegacy) {
						resetRenderTimer();
						flushSyncCallbacksOnlyInLegacyMode();
					}
				}
			}
			function scheduleInitialHydrationOnRoot(root, lane, eventTime) {
				var current = root.current;
				current.lanes = lane;
				markRootUpdated(root, lane, eventTime);
				ensureRootIsScheduled(root, eventTime);
			}
			function isUnsafeClassRenderPhaseUpdate(fiber) {
				return (executionContext & RenderContext) !== NoContext;
			}
			function ensureRootIsScheduled(root, currentTime) {
				var existingCallbackNode = root.callbackNode;
				markStarvedLanesAsExpired(root, currentTime);
				var nextLanes = getNextLanes(root, root === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
				if (nextLanes === NoLanes) {
					if (existingCallbackNode !== null) cancelCallback$1(existingCallbackNode);
					root.callbackNode = null;
					root.callbackPriority = NoLane;
					return;
				}
				var newCallbackPriority = getHighestPriorityLane(nextLanes);
				var existingCallbackPriority = root.callbackPriority;
				if (existingCallbackPriority === newCallbackPriority && !(ReactCurrentActQueue$1.current !== null && existingCallbackNode !== fakeActCallbackNode)) {
					if (existingCallbackNode == null && existingCallbackPriority !== SyncLane) error("Expected scheduled callback to exist. This error is likely caused by a bug in React. Please file an issue.");
					return;
				}
				if (existingCallbackNode != null) cancelCallback$1(existingCallbackNode);
				var newCallbackNode;
				if (newCallbackPriority === SyncLane) {
					if (root.tag === LegacyRoot) {
						if (ReactCurrentActQueue$1.isBatchingLegacy !== null) ReactCurrentActQueue$1.didScheduleLegacyUpdate = true;
						scheduleLegacySyncCallback(performSyncWorkOnRoot.bind(null, root));
					} else scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
					if (ReactCurrentActQueue$1.current !== null) ReactCurrentActQueue$1.current.push(flushSyncCallbacks);
					else scheduleMicrotask(function() {
						if ((executionContext & (RenderContext | CommitContext)) === NoContext) flushSyncCallbacks();
					});
					newCallbackNode = null;
				} else {
					var schedulerPriorityLevel;
					switch (lanesToEventPriority(nextLanes)) {
						case DiscreteEventPriority:
							schedulerPriorityLevel = ImmediatePriority;
							break;
						case ContinuousEventPriority:
							schedulerPriorityLevel = UserBlockingPriority;
							break;
						case DefaultEventPriority:
							schedulerPriorityLevel = NormalPriority;
							break;
						case IdleEventPriority:
							schedulerPriorityLevel = IdlePriority;
							break;
						default: schedulerPriorityLevel = NormalPriority;
					}
					newCallbackNode = scheduleCallback$1(schedulerPriorityLevel, performConcurrentWorkOnRoot.bind(null, root));
				}
				root.callbackPriority = newCallbackPriority;
				root.callbackNode = newCallbackNode;
			}
			function performConcurrentWorkOnRoot(root, didTimeout) {
				resetNestedUpdateFlag();
				currentEventTime = NoTimestamp;
				currentEventTransitionLane = NoLanes;
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error("Should not already be working.");
				var originalCallbackNode = root.callbackNode;
				if (flushPassiveEffects()) {
					if (root.callbackNode !== originalCallbackNode) return null;
				}
				var lanes = getNextLanes(root, root === workInProgressRoot ? workInProgressRootRenderLanes : NoLanes);
				if (lanes === NoLanes) return null;
				var exitStatus = !includesBlockingLane(root, lanes) && !includesExpiredLane(root, lanes) && !didTimeout ? renderRootConcurrent(root, lanes) : renderRootSync(root, lanes);
				if (exitStatus !== RootInProgress) {
					if (exitStatus === RootErrored) {
						var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
						if (errorRetryLanes !== NoLanes) {
							lanes = errorRetryLanes;
							exitStatus = recoverFromConcurrentError(root, errorRetryLanes);
						}
					}
					if (exitStatus === RootFatalErrored) {
						var fatalError = workInProgressRootFatalError;
						prepareFreshStack(root, NoLanes);
						markRootSuspended$1(root, lanes);
						ensureRootIsScheduled(root, now());
						throw fatalError;
					}
					if (exitStatus === RootDidNotComplete) markRootSuspended$1(root, lanes);
					else {
						var renderWasConcurrent = !includesBlockingLane(root, lanes);
						var finishedWork = root.current.alternate;
						if (renderWasConcurrent && !isRenderConsistentWithExternalStores(finishedWork)) {
							exitStatus = renderRootSync(root, lanes);
							if (exitStatus === RootErrored) {
								var _errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
								if (_errorRetryLanes !== NoLanes) {
									lanes = _errorRetryLanes;
									exitStatus = recoverFromConcurrentError(root, _errorRetryLanes);
								}
							}
							if (exitStatus === RootFatalErrored) {
								var _fatalError = workInProgressRootFatalError;
								prepareFreshStack(root, NoLanes);
								markRootSuspended$1(root, lanes);
								ensureRootIsScheduled(root, now());
								throw _fatalError;
							}
						}
						root.finishedWork = finishedWork;
						root.finishedLanes = lanes;
						finishConcurrentRender(root, exitStatus, lanes);
					}
				}
				ensureRootIsScheduled(root, now());
				if (root.callbackNode === originalCallbackNode) return performConcurrentWorkOnRoot.bind(null, root);
				return null;
			}
			function recoverFromConcurrentError(root, errorRetryLanes) {
				var errorsFromFirstAttempt = workInProgressRootConcurrentErrors;
				if (isRootDehydrated(root)) {
					var rootWorkInProgress = prepareFreshStack(root, errorRetryLanes);
					rootWorkInProgress.flags |= ForceClientRender;
					errorHydratingContainer(root.containerInfo);
				}
				var exitStatus = renderRootSync(root, errorRetryLanes);
				if (exitStatus !== RootErrored) {
					var errorsFromSecondAttempt = workInProgressRootRecoverableErrors;
					workInProgressRootRecoverableErrors = errorsFromFirstAttempt;
					if (errorsFromSecondAttempt !== null) queueRecoverableErrors(errorsFromSecondAttempt);
				}
				return exitStatus;
			}
			function queueRecoverableErrors(errors) {
				if (workInProgressRootRecoverableErrors === null) workInProgressRootRecoverableErrors = errors;
				else workInProgressRootRecoverableErrors.push.apply(workInProgressRootRecoverableErrors, errors);
			}
			function finishConcurrentRender(root, exitStatus, lanes) {
				switch (exitStatus) {
					case RootInProgress:
					case RootFatalErrored: throw new Error("Root did not complete. This is a bug in React.");
					case RootErrored:
						commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
						break;
					case RootSuspended:
						markRootSuspended$1(root, lanes);
						if (includesOnlyRetries(lanes) && !shouldForceFlushFallbacksInDEV()) {
							var msUntilTimeout = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now();
							if (msUntilTimeout > 10) {
								if (getNextLanes(root, NoLanes) !== NoLanes) break;
								var suspendedLanes = root.suspendedLanes;
								if (!isSubsetOfLanes(suspendedLanes, lanes)) {
									requestEventTime();
									markRootPinged(root, suspendedLanes);
									break;
								}
								root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root, workInProgressRootRecoverableErrors, workInProgressTransitions), msUntilTimeout);
								break;
							}
						}
						commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
						break;
					case RootSuspendedWithDelay:
						markRootSuspended$1(root, lanes);
						if (includesOnlyTransitions(lanes)) break;
						if (!shouldForceFlushFallbacksInDEV()) {
							var eventTimeMs = getMostRecentEventTime(root, lanes);
							var timeElapsedMs = now() - eventTimeMs;
							var _msUntilTimeout = jnd(timeElapsedMs) - timeElapsedMs;
							if (_msUntilTimeout > 10) {
								root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root, workInProgressRootRecoverableErrors, workInProgressTransitions), _msUntilTimeout);
								break;
							}
						}
						commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
						break;
					case RootCompleted:
						commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
						break;
					default: throw new Error("Unknown root exit status.");
				}
			}
			function isRenderConsistentWithExternalStores(finishedWork) {
				var node = finishedWork;
				while (true) {
					if (node.flags & StoreConsistency) {
						var updateQueue = node.updateQueue;
						if (updateQueue !== null) {
							var checks = updateQueue.stores;
							if (checks !== null) for (var i = 0; i < checks.length; i++) {
								var check = checks[i];
								var getSnapshot = check.getSnapshot;
								var renderedValue = check.value;
								try {
									if (!objectIs(getSnapshot(), renderedValue)) return false;
								} catch (error) {
									return false;
								}
							}
						}
					}
					var child = node.child;
					if (node.subtreeFlags & StoreConsistency && child !== null) {
						child.return = node;
						node = child;
						continue;
					}
					if (node === finishedWork) return true;
					while (node.sibling === null) {
						if (node.return === null || node.return === finishedWork) return true;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
				return true;
			}
			function markRootSuspended$1(root, suspendedLanes) {
				suspendedLanes = removeLanes(suspendedLanes, workInProgressRootPingedLanes);
				suspendedLanes = removeLanes(suspendedLanes, workInProgressRootInterleavedUpdatedLanes);
				markRootSuspended(root, suspendedLanes);
			}
			function performSyncWorkOnRoot(root) {
				syncNestedUpdateFlag();
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error("Should not already be working.");
				flushPassiveEffects();
				var lanes = getNextLanes(root, NoLanes);
				if (!includesSomeLane(lanes, SyncLane)) {
					ensureRootIsScheduled(root, now());
					return null;
				}
				var exitStatus = renderRootSync(root, lanes);
				if (root.tag !== LegacyRoot && exitStatus === RootErrored) {
					var errorRetryLanes = getLanesToRetrySynchronouslyOnError(root);
					if (errorRetryLanes !== NoLanes) {
						lanes = errorRetryLanes;
						exitStatus = recoverFromConcurrentError(root, errorRetryLanes);
					}
				}
				if (exitStatus === RootFatalErrored) {
					var fatalError = workInProgressRootFatalError;
					prepareFreshStack(root, NoLanes);
					markRootSuspended$1(root, lanes);
					ensureRootIsScheduled(root, now());
					throw fatalError;
				}
				if (exitStatus === RootDidNotComplete) throw new Error("Root did not complete. This is a bug in React.");
				root.finishedWork = root.current.alternate;
				root.finishedLanes = lanes;
				commitRoot(root, workInProgressRootRecoverableErrors, workInProgressTransitions);
				ensureRootIsScheduled(root, now());
				return null;
			}
			function flushRoot(root, lanes) {
				if (lanes !== NoLanes) {
					markRootEntangled(root, mergeLanes(lanes, SyncLane));
					ensureRootIsScheduled(root, now());
					if ((executionContext & (RenderContext | CommitContext)) === NoContext) {
						resetRenderTimer();
						flushSyncCallbacks();
					}
				}
			}
			function batchedUpdates$1(fn, a) {
				var prevExecutionContext = executionContext;
				executionContext |= BatchedContext;
				try {
					return fn(a);
				} finally {
					executionContext = prevExecutionContext;
					if (executionContext === NoContext && !ReactCurrentActQueue$1.isBatchingLegacy) {
						resetRenderTimer();
						flushSyncCallbacksOnlyInLegacyMode();
					}
				}
			}
			function discreteUpdates(fn, a, b, c, d) {
				var previousPriority = getCurrentUpdatePriority();
				var prevTransition = ReactCurrentBatchConfig$3.transition;
				try {
					ReactCurrentBatchConfig$3.transition = null;
					setCurrentUpdatePriority(DiscreteEventPriority);
					return fn(a, b, c, d);
				} finally {
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig$3.transition = prevTransition;
					if (executionContext === NoContext) resetRenderTimer();
				}
			}
			function flushSync(fn) {
				if (rootWithPendingPassiveEffects !== null && rootWithPendingPassiveEffects.tag === LegacyRoot && (executionContext & (RenderContext | CommitContext)) === NoContext) flushPassiveEffects();
				var prevExecutionContext = executionContext;
				executionContext |= BatchedContext;
				var prevTransition = ReactCurrentBatchConfig$3.transition;
				var previousPriority = getCurrentUpdatePriority();
				try {
					ReactCurrentBatchConfig$3.transition = null;
					setCurrentUpdatePriority(DiscreteEventPriority);
					if (fn) return fn();
					else return;
				} finally {
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig$3.transition = prevTransition;
					executionContext = prevExecutionContext;
					if ((executionContext & (RenderContext | CommitContext)) === NoContext) flushSyncCallbacks();
				}
			}
			function isAlreadyRendering() {
				return (executionContext & (RenderContext | CommitContext)) !== NoContext;
			}
			function pushRenderLanes(fiber, lanes) {
				push(subtreeRenderLanesCursor, subtreeRenderLanes, fiber);
				subtreeRenderLanes = mergeLanes(subtreeRenderLanes, lanes);
				workInProgressRootIncludedLanes = mergeLanes(workInProgressRootIncludedLanes, lanes);
			}
			function popRenderLanes(fiber) {
				subtreeRenderLanes = subtreeRenderLanesCursor.current;
				pop(subtreeRenderLanesCursor, fiber);
			}
			function prepareFreshStack(root, lanes) {
				root.finishedWork = null;
				root.finishedLanes = NoLanes;
				var timeoutHandle = root.timeoutHandle;
				if (timeoutHandle !== noTimeout) {
					root.timeoutHandle = noTimeout;
					cancelTimeout(timeoutHandle);
				}
				if (workInProgress !== null) {
					var interruptedWork = workInProgress.return;
					while (interruptedWork !== null) {
						var current = interruptedWork.alternate;
						unwindInterruptedWork(current, interruptedWork);
						interruptedWork = interruptedWork.return;
					}
				}
				workInProgressRoot = root;
				var rootWorkInProgress = createWorkInProgress(root.current, null);
				workInProgress = rootWorkInProgress;
				workInProgressRootRenderLanes = subtreeRenderLanes = workInProgressRootIncludedLanes = lanes;
				workInProgressRootExitStatus = RootInProgress;
				workInProgressRootFatalError = null;
				workInProgressRootSkippedLanes = NoLanes;
				workInProgressRootInterleavedUpdatedLanes = NoLanes;
				workInProgressRootPingedLanes = NoLanes;
				workInProgressRootConcurrentErrors = null;
				workInProgressRootRecoverableErrors = null;
				finishQueueingConcurrentUpdates();
				ReactStrictModeWarnings.discardPendingWarnings();
				return rootWorkInProgress;
			}
			function handleError(root, thrownValue) {
				do {
					var erroredWork = workInProgress;
					try {
						resetContextDependencies();
						resetHooksAfterThrow();
						resetCurrentFiber();
						ReactCurrentOwner$2.current = null;
						if (erroredWork === null || erroredWork.return === null) {
							workInProgressRootExitStatus = RootFatalErrored;
							workInProgressRootFatalError = thrownValue;
							workInProgress = null;
							return;
						}
						if (enableProfilerTimer && erroredWork.mode & ProfileMode) stopProfilerTimerIfRunningAndRecordDelta(erroredWork, true);
						if (enableSchedulingProfiler) {
							markComponentRenderStopped();
							if (thrownValue !== null && typeof thrownValue === "object" && typeof thrownValue.then === "function") markComponentSuspended(erroredWork, thrownValue, workInProgressRootRenderLanes);
							else markComponentErrored(erroredWork, thrownValue, workInProgressRootRenderLanes);
						}
						throwException(root, erroredWork.return, erroredWork, thrownValue, workInProgressRootRenderLanes);
						completeUnitOfWork(erroredWork);
					} catch (yetAnotherThrownValue) {
						thrownValue = yetAnotherThrownValue;
						if (workInProgress === erroredWork && erroredWork !== null) {
							erroredWork = erroredWork.return;
							workInProgress = erroredWork;
						} else erroredWork = workInProgress;
						continue;
					}
					return;
				} while (true);
			}
			function pushDispatcher() {
				var prevDispatcher = ReactCurrentDispatcher$2.current;
				ReactCurrentDispatcher$2.current = ContextOnlyDispatcher;
				if (prevDispatcher === null) return ContextOnlyDispatcher;
				else return prevDispatcher;
			}
			function popDispatcher(prevDispatcher) {
				ReactCurrentDispatcher$2.current = prevDispatcher;
			}
			function markCommitTimeOfFallback() {
				globalMostRecentFallbackTime = now();
			}
			function markSkippedUpdateLanes(lane) {
				workInProgressRootSkippedLanes = mergeLanes(lane, workInProgressRootSkippedLanes);
			}
			function renderDidSuspend() {
				if (workInProgressRootExitStatus === RootInProgress) workInProgressRootExitStatus = RootSuspended;
			}
			function renderDidSuspendDelayIfPossible() {
				if (workInProgressRootExitStatus === RootInProgress || workInProgressRootExitStatus === RootSuspended || workInProgressRootExitStatus === RootErrored) workInProgressRootExitStatus = RootSuspendedWithDelay;
				if (workInProgressRoot !== null && (includesNonIdleWork(workInProgressRootSkippedLanes) || includesNonIdleWork(workInProgressRootInterleavedUpdatedLanes))) markRootSuspended$1(workInProgressRoot, workInProgressRootRenderLanes);
			}
			function renderDidError(error) {
				if (workInProgressRootExitStatus !== RootSuspendedWithDelay) workInProgressRootExitStatus = RootErrored;
				if (workInProgressRootConcurrentErrors === null) workInProgressRootConcurrentErrors = [error];
				else workInProgressRootConcurrentErrors.push(error);
			}
			function renderHasNotSuspendedYet() {
				return workInProgressRootExitStatus === RootInProgress;
			}
			function renderRootSync(root, lanes) {
				var prevExecutionContext = executionContext;
				executionContext |= RenderContext;
				var prevDispatcher = pushDispatcher();
				if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
					if (isDevToolsPresent) {
						var memoizedUpdaters = root.memoizedUpdaters;
						if (memoizedUpdaters.size > 0) {
							restorePendingUpdaters(root, workInProgressRootRenderLanes);
							memoizedUpdaters.clear();
						}
						movePendingFibersToMemoized(root, lanes);
					}
					workInProgressTransitions = getTransitionsForLanes();
					prepareFreshStack(root, lanes);
				}
				markRenderStarted(lanes);
				do
					try {
						workLoopSync();
						break;
					} catch (thrownValue) {
						handleError(root, thrownValue);
					}
				while (true);
				resetContextDependencies();
				executionContext = prevExecutionContext;
				popDispatcher(prevDispatcher);
				if (workInProgress !== null) throw new Error("Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue.");
				markRenderStopped();
				workInProgressRoot = null;
				workInProgressRootRenderLanes = NoLanes;
				return workInProgressRootExitStatus;
			}
			/** @noinline */
			function workLoopSync() {
				while (workInProgress !== null) performUnitOfWork(workInProgress);
			}
			function renderRootConcurrent(root, lanes) {
				var prevExecutionContext = executionContext;
				executionContext |= RenderContext;
				var prevDispatcher = pushDispatcher();
				if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
					if (isDevToolsPresent) {
						var memoizedUpdaters = root.memoizedUpdaters;
						if (memoizedUpdaters.size > 0) {
							restorePendingUpdaters(root, workInProgressRootRenderLanes);
							memoizedUpdaters.clear();
						}
						movePendingFibersToMemoized(root, lanes);
					}
					workInProgressTransitions = getTransitionsForLanes();
					resetRenderTimer();
					prepareFreshStack(root, lanes);
				}
				markRenderStarted(lanes);
				do
					try {
						workLoopConcurrent();
						break;
					} catch (thrownValue) {
						handleError(root, thrownValue);
					}
				while (true);
				resetContextDependencies();
				popDispatcher(prevDispatcher);
				executionContext = prevExecutionContext;
				if (workInProgress !== null) {
					markRenderYielded();
					return RootInProgress;
				} else {
					markRenderStopped();
					workInProgressRoot = null;
					workInProgressRootRenderLanes = NoLanes;
					return workInProgressRootExitStatus;
				}
			}
			/** @noinline */
			function workLoopConcurrent() {
				while (workInProgress !== null && !shouldYield()) performUnitOfWork(workInProgress);
			}
			function performUnitOfWork(unitOfWork) {
				var current = unitOfWork.alternate;
				setCurrentFiber(unitOfWork);
				var next;
				if ((unitOfWork.mode & ProfileMode) !== NoMode) {
					startProfilerTimer(unitOfWork);
					next = beginWork$1(current, unitOfWork, subtreeRenderLanes);
					stopProfilerTimerIfRunningAndRecordDelta(unitOfWork, true);
				} else next = beginWork$1(current, unitOfWork, subtreeRenderLanes);
				resetCurrentFiber();
				unitOfWork.memoizedProps = unitOfWork.pendingProps;
				if (next === null) completeUnitOfWork(unitOfWork);
				else workInProgress = next;
				ReactCurrentOwner$2.current = null;
			}
			function completeUnitOfWork(unitOfWork) {
				var completedWork = unitOfWork;
				do {
					var current = completedWork.alternate;
					var returnFiber = completedWork.return;
					if ((completedWork.flags & Incomplete) === NoFlags) {
						setCurrentFiber(completedWork);
						var next = void 0;
						if ((completedWork.mode & ProfileMode) === NoMode) next = completeWork(current, completedWork, subtreeRenderLanes);
						else {
							startProfilerTimer(completedWork);
							next = completeWork(current, completedWork, subtreeRenderLanes);
							stopProfilerTimerIfRunningAndRecordDelta(completedWork, false);
						}
						resetCurrentFiber();
						if (next !== null) {
							workInProgress = next;
							return;
						}
					} else {
						var _next = unwindWork(current, completedWork);
						if (_next !== null) {
							_next.flags &= HostEffectMask;
							workInProgress = _next;
							return;
						}
						if ((completedWork.mode & ProfileMode) !== NoMode) {
							stopProfilerTimerIfRunningAndRecordDelta(completedWork, false);
							var actualDuration = completedWork.actualDuration;
							var child = completedWork.child;
							while (child !== null) {
								actualDuration += child.actualDuration;
								child = child.sibling;
							}
							completedWork.actualDuration = actualDuration;
						}
						if (returnFiber !== null) {
							returnFiber.flags |= Incomplete;
							returnFiber.subtreeFlags = NoFlags;
							returnFiber.deletions = null;
						} else {
							workInProgressRootExitStatus = RootDidNotComplete;
							workInProgress = null;
							return;
						}
					}
					var siblingFiber = completedWork.sibling;
					if (siblingFiber !== null) {
						workInProgress = siblingFiber;
						return;
					}
					completedWork = returnFiber;
					workInProgress = completedWork;
				} while (completedWork !== null);
				if (workInProgressRootExitStatus === RootInProgress) workInProgressRootExitStatus = RootCompleted;
			}
			function commitRoot(root, recoverableErrors, transitions) {
				var previousUpdateLanePriority = getCurrentUpdatePriority();
				var prevTransition = ReactCurrentBatchConfig$3.transition;
				try {
					ReactCurrentBatchConfig$3.transition = null;
					setCurrentUpdatePriority(DiscreteEventPriority);
					commitRootImpl(root, recoverableErrors, transitions, previousUpdateLanePriority);
				} finally {
					ReactCurrentBatchConfig$3.transition = prevTransition;
					setCurrentUpdatePriority(previousUpdateLanePriority);
				}
				return null;
			}
			function commitRootImpl(root, recoverableErrors, transitions, renderPriorityLevel) {
				do
					flushPassiveEffects();
				while (rootWithPendingPassiveEffects !== null);
				flushRenderPhaseStrictModeWarningsInDEV();
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error("Should not already be working.");
				var finishedWork = root.finishedWork;
				var lanes = root.finishedLanes;
				markCommitStarted(lanes);
				if (finishedWork === null) {
					markCommitStopped();
					return null;
				} else if (lanes === NoLanes) error("root.finishedLanes should not be empty during a commit. This is a bug in React.");
				root.finishedWork = null;
				root.finishedLanes = NoLanes;
				if (finishedWork === root.current) throw new Error("Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue.");
				root.callbackNode = null;
				root.callbackPriority = NoLane;
				var remainingLanes = mergeLanes(finishedWork.lanes, finishedWork.childLanes);
				markRootFinished(root, remainingLanes);
				if (root === workInProgressRoot) {
					workInProgressRoot = null;
					workInProgress = null;
					workInProgressRootRenderLanes = NoLanes;
				}
				if ((finishedWork.subtreeFlags & PassiveMask) !== NoFlags || (finishedWork.flags & PassiveMask) !== NoFlags) {
					if (!rootDoesHavePassiveEffects) {
						rootDoesHavePassiveEffects = true;
						pendingPassiveTransitions = transitions;
						scheduleCallback$1(NormalPriority, function() {
							flushPassiveEffects();
							return null;
						});
					}
				}
				var subtreeHasEffects = (finishedWork.subtreeFlags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
				var rootHasEffect = (finishedWork.flags & (BeforeMutationMask | MutationMask | LayoutMask | PassiveMask)) !== NoFlags;
				if (subtreeHasEffects || rootHasEffect) {
					var prevTransition = ReactCurrentBatchConfig$3.transition;
					ReactCurrentBatchConfig$3.transition = null;
					var previousPriority = getCurrentUpdatePriority();
					setCurrentUpdatePriority(DiscreteEventPriority);
					var prevExecutionContext = executionContext;
					executionContext |= CommitContext;
					ReactCurrentOwner$2.current = null;
					commitBeforeMutationEffects(root, finishedWork);
					recordCommitTime();
					commitMutationEffects(root, finishedWork, lanes);
					resetAfterCommit(root.containerInfo);
					root.current = finishedWork;
					markLayoutEffectsStarted(lanes);
					commitLayoutEffects(finishedWork, root, lanes);
					markLayoutEffectsStopped();
					requestPaint();
					executionContext = prevExecutionContext;
					setCurrentUpdatePriority(previousPriority);
					ReactCurrentBatchConfig$3.transition = prevTransition;
				} else {
					root.current = finishedWork;
					recordCommitTime();
				}
				var rootDidHavePassiveEffects = rootDoesHavePassiveEffects;
				if (rootDoesHavePassiveEffects) {
					rootDoesHavePassiveEffects = false;
					rootWithPendingPassiveEffects = root;
					pendingPassiveEffectsLanes = lanes;
				} else {
					nestedPassiveUpdateCount = 0;
					rootWithPassiveNestedUpdates = null;
				}
				remainingLanes = root.pendingLanes;
				if (remainingLanes === NoLanes) legacyErrorBoundariesThatAlreadyFailed = null;
				if (!rootDidHavePassiveEffects) commitDoubleInvokeEffectsInDEV(root.current, false);
				onCommitRoot(finishedWork.stateNode, renderPriorityLevel);
				if (isDevToolsPresent) root.memoizedUpdaters.clear();
				onCommitRoot$1();
				ensureRootIsScheduled(root, now());
				if (recoverableErrors !== null) {
					var onRecoverableError = root.onRecoverableError;
					for (var i = 0; i < recoverableErrors.length; i++) {
						var recoverableError = recoverableErrors[i];
						var componentStack = recoverableError.stack;
						var digest = recoverableError.digest;
						onRecoverableError(recoverableError.value, {
							componentStack,
							digest
						});
					}
				}
				if (hasUncaughtError) {
					hasUncaughtError = false;
					var error$1 = firstUncaughtError;
					firstUncaughtError = null;
					throw error$1;
				}
				if (includesSomeLane(pendingPassiveEffectsLanes, SyncLane) && root.tag !== LegacyRoot) flushPassiveEffects();
				remainingLanes = root.pendingLanes;
				if (includesSomeLane(remainingLanes, SyncLane)) {
					markNestedUpdateScheduled();
					if (root === rootWithNestedUpdates) nestedUpdateCount++;
					else {
						nestedUpdateCount = 0;
						rootWithNestedUpdates = root;
					}
				} else nestedUpdateCount = 0;
				flushSyncCallbacks();
				markCommitStopped();
				return null;
			}
			function flushPassiveEffects() {
				if (rootWithPendingPassiveEffects !== null) {
					var priority = lowerEventPriority(DefaultEventPriority, lanesToEventPriority(pendingPassiveEffectsLanes));
					var prevTransition = ReactCurrentBatchConfig$3.transition;
					var previousPriority = getCurrentUpdatePriority();
					try {
						ReactCurrentBatchConfig$3.transition = null;
						setCurrentUpdatePriority(priority);
						return flushPassiveEffectsImpl();
					} finally {
						setCurrentUpdatePriority(previousPriority);
						ReactCurrentBatchConfig$3.transition = prevTransition;
					}
				}
				return false;
			}
			function enqueuePendingPassiveProfilerEffect(fiber) {
				pendingPassiveProfilerEffects.push(fiber);
				if (!rootDoesHavePassiveEffects) {
					rootDoesHavePassiveEffects = true;
					scheduleCallback$1(NormalPriority, function() {
						flushPassiveEffects();
						return null;
					});
				}
			}
			function flushPassiveEffectsImpl() {
				if (rootWithPendingPassiveEffects === null) return false;
				var transitions = pendingPassiveTransitions;
				pendingPassiveTransitions = null;
				var root = rootWithPendingPassiveEffects;
				var lanes = pendingPassiveEffectsLanes;
				rootWithPendingPassiveEffects = null;
				pendingPassiveEffectsLanes = NoLanes;
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw new Error("Cannot flush passive effects while already rendering.");
				isFlushingPassiveEffects = true;
				didScheduleUpdateDuringPassiveEffects = false;
				markPassiveEffectsStarted(lanes);
				var prevExecutionContext = executionContext;
				executionContext |= CommitContext;
				commitPassiveUnmountEffects(root.current);
				commitPassiveMountEffects(root, root.current, lanes, transitions);
				var profilerEffects = pendingPassiveProfilerEffects;
				pendingPassiveProfilerEffects = [];
				for (var i = 0; i < profilerEffects.length; i++) {
					var _fiber = profilerEffects[i];
					commitPassiveEffectDurations(root, _fiber);
				}
				markPassiveEffectsStopped();
				commitDoubleInvokeEffectsInDEV(root.current, true);
				executionContext = prevExecutionContext;
				flushSyncCallbacks();
				if (didScheduleUpdateDuringPassiveEffects) {
					if (root === rootWithPassiveNestedUpdates) nestedPassiveUpdateCount++;
					else {
						nestedPassiveUpdateCount = 0;
						rootWithPassiveNestedUpdates = root;
					}
				} else nestedPassiveUpdateCount = 0;
				isFlushingPassiveEffects = false;
				didScheduleUpdateDuringPassiveEffects = false;
				onPostCommitRoot(root);
				var stateNode = root.current.stateNode;
				stateNode.effectDuration = 0;
				stateNode.passiveEffectDuration = 0;
				return true;
			}
			function isAlreadyFailedLegacyErrorBoundary(instance) {
				return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
			}
			function markLegacyErrorBoundaryAsFailed(instance) {
				if (legacyErrorBoundariesThatAlreadyFailed === null) legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([instance]);
				else legacyErrorBoundariesThatAlreadyFailed.add(instance);
			}
			function prepareToThrowUncaughtError(error) {
				if (!hasUncaughtError) {
					hasUncaughtError = true;
					firstUncaughtError = error;
				}
			}
			var onUncaughtError = prepareToThrowUncaughtError;
			function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
				var root = enqueueUpdate(rootFiber, createRootErrorUpdate(rootFiber, createCapturedValueAtFiber(error, sourceFiber), SyncLane), SyncLane);
				var eventTime = requestEventTime();
				if (root !== null) {
					markRootUpdated(root, SyncLane, eventTime);
					ensureRootIsScheduled(root, eventTime);
				}
			}
			function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error$1) {
				reportUncaughtErrorInDEV(error$1);
				setIsRunningInsertionEffect(false);
				if (sourceFiber.tag === HostRoot) {
					captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error$1);
					return;
				}
				var fiber = null;
				fiber = nearestMountedAncestor;
				while (fiber !== null) {
					if (fiber.tag === HostRoot) {
						captureCommitPhaseErrorOnRoot(fiber, sourceFiber, error$1);
						return;
					} else if (fiber.tag === ClassComponent) {
						var ctor = fiber.type;
						var instance = fiber.stateNode;
						if (typeof ctor.getDerivedStateFromError === "function" || typeof instance.componentDidCatch === "function" && !isAlreadyFailedLegacyErrorBoundary(instance)) {
							var errorInfo = createCapturedValueAtFiber(error$1, sourceFiber);
							var update = createClassErrorUpdate(fiber, errorInfo, SyncLane);
							var root = enqueueUpdate(fiber, update, SyncLane);
							var eventTime = requestEventTime();
							if (root !== null) {
								markRootUpdated(root, SyncLane, eventTime);
								ensureRootIsScheduled(root, eventTime);
							}
							return;
						}
					}
					fiber = fiber.return;
				}
				error("Internal React error: Attempted to capture a commit phase error inside a detached tree. This indicates a bug in React. Likely causes include deleting the same fiber more than once, committing an already-finished tree, or an inconsistent return pointer.\n\nError message:\n\n%s", error$1);
			}
			function pingSuspendedRoot(root, wakeable, pingedLanes) {
				var pingCache = root.pingCache;
				if (pingCache !== null) pingCache.delete(wakeable);
				var eventTime = requestEventTime();
				markRootPinged(root, pingedLanes);
				warnIfSuspenseResolutionNotWrappedWithActDEV(root);
				if (workInProgressRoot === root && isSubsetOfLanes(workInProgressRootRenderLanes, pingedLanes)) {
					if (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && includesOnlyRetries(workInProgressRootRenderLanes) && now() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS) prepareFreshStack(root, NoLanes);
					else workInProgressRootPingedLanes = mergeLanes(workInProgressRootPingedLanes, pingedLanes);
				}
				ensureRootIsScheduled(root, eventTime);
			}
			function retryTimedOutBoundary(boundaryFiber, retryLane) {
				if (retryLane === NoLane) retryLane = requestRetryLane(boundaryFiber);
				var eventTime = requestEventTime();
				var root = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
				if (root !== null) {
					markRootUpdated(root, retryLane, eventTime);
					ensureRootIsScheduled(root, eventTime);
				}
			}
			function retryDehydratedSuspenseBoundary(boundaryFiber) {
				var suspenseState = boundaryFiber.memoizedState;
				var retryLane = NoLane;
				if (suspenseState !== null) retryLane = suspenseState.retryLane;
				retryTimedOutBoundary(boundaryFiber, retryLane);
			}
			function resolveRetryWakeable(boundaryFiber, wakeable) {
				var retryLane = NoLane;
				var retryCache;
				switch (boundaryFiber.tag) {
					case SuspenseComponent:
						retryCache = boundaryFiber.stateNode;
						var suspenseState = boundaryFiber.memoizedState;
						if (suspenseState !== null) retryLane = suspenseState.retryLane;
						break;
					case SuspenseListComponent:
						retryCache = boundaryFiber.stateNode;
						break;
					default: throw new Error("Pinged unknown suspense boundary type. This is probably a bug in React.");
				}
				if (retryCache !== null) retryCache.delete(wakeable);
				retryTimedOutBoundary(boundaryFiber, retryLane);
			}
			function jnd(timeElapsed) {
				return timeElapsed < 120 ? 120 : timeElapsed < 480 ? 480 : timeElapsed < 1080 ? 1080 : timeElapsed < 1920 ? 1920 : timeElapsed < 3e3 ? 3e3 : timeElapsed < 4320 ? 4320 : ceil(timeElapsed / 1960) * 1960;
			}
			function checkForNestedUpdates() {
				if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
					nestedUpdateCount = 0;
					rootWithNestedUpdates = null;
					throw new Error("Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.");
				}
				if (nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT) {
					nestedPassiveUpdateCount = 0;
					rootWithPassiveNestedUpdates = null;
					error("Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.");
				}
			}
			function flushRenderPhaseStrictModeWarningsInDEV() {
				ReactStrictModeWarnings.flushLegacyContextWarning();
				ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
			}
			function commitDoubleInvokeEffectsInDEV(fiber, hasPassiveEffects) {
				setCurrentFiber(fiber);
				invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectUnmountInDEV);
				if (hasPassiveEffects) invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectUnmountInDEV);
				invokeEffectsInDev(fiber, MountLayoutDev, invokeLayoutEffectMountInDEV);
				if (hasPassiveEffects) invokeEffectsInDev(fiber, MountPassiveDev, invokePassiveEffectMountInDEV);
				resetCurrentFiber();
			}
			function invokeEffectsInDev(firstChild, fiberFlags, invokeEffectFn) {
				var current = firstChild;
				var subtreeRoot = null;
				while (current !== null) {
					var primarySubtreeFlag = current.subtreeFlags & fiberFlags;
					if (current !== subtreeRoot && current.child !== null && primarySubtreeFlag !== NoFlags) current = current.child;
					else {
						if ((current.flags & fiberFlags) !== NoFlags) invokeEffectFn(current);
						if (current.sibling !== null) current = current.sibling;
						else current = subtreeRoot = current.return;
					}
				}
			}
			var didWarnStateUpdateForNotYetMountedComponent = null;
			function warnAboutUpdateOnNotYetMountedFiberInDEV(fiber) {
				if ((executionContext & RenderContext) !== NoContext) return;
				if (!(fiber.mode & ConcurrentMode)) return;
				var tag = fiber.tag;
				if (tag !== IndeterminateComponent && tag !== HostRoot && tag !== ClassComponent && tag !== FunctionComponent && tag !== ForwardRef && tag !== MemoComponent && tag !== SimpleMemoComponent) return;
				var componentName = getComponentNameFromFiber(fiber) || "ReactComponent";
				if (didWarnStateUpdateForNotYetMountedComponent !== null) {
					if (didWarnStateUpdateForNotYetMountedComponent.has(componentName)) return;
					didWarnStateUpdateForNotYetMountedComponent.add(componentName);
				} else didWarnStateUpdateForNotYetMountedComponent = /* @__PURE__ */ new Set([componentName]);
				var previousFiber = current;
				try {
					setCurrentFiber(fiber);
					error("Can't perform a React state update on a component that hasn't mounted yet. This indicates that you have a side-effect in your render function that asynchronously later calls tries to update the component. Move this work to useEffect instead.");
				} finally {
					if (previousFiber) setCurrentFiber(fiber);
					else resetCurrentFiber();
				}
			}
			var beginWork$1;
			var dummyFiber = null;
			beginWork$1 = function(current, unitOfWork, lanes) {
				var originalWorkInProgressCopy = assignFiberPropertiesInDEV(dummyFiber, unitOfWork);
				try {
					return beginWork(current, unitOfWork, lanes);
				} catch (originalError) {
					if (didSuspendOrErrorWhileHydratingDEV() || originalError !== null && typeof originalError === "object" && typeof originalError.then === "function") throw originalError;
					resetContextDependencies();
					resetHooksAfterThrow();
					unwindInterruptedWork(current, unitOfWork);
					assignFiberPropertiesInDEV(unitOfWork, originalWorkInProgressCopy);
					if (unitOfWork.mode & ProfileMode) startProfilerTimer(unitOfWork);
					invokeGuardedCallback(null, beginWork, null, current, unitOfWork, lanes);
					if (hasCaughtError()) {
						var replayError = clearCaughtError();
						if (typeof replayError === "object" && replayError !== null && replayError._suppressLogging && typeof originalError === "object" && originalError !== null && !originalError._suppressLogging) originalError._suppressLogging = true;
					}
					throw originalError;
				}
			};
			var didWarnAboutUpdateInRender = false;
			var didWarnAboutUpdateInRenderForAnotherComponent = /* @__PURE__ */ new Set();
			function warnAboutRenderPhaseUpdatesInDEV(fiber) {
				if (isRendering && !getIsUpdatingOpaqueValueInRenderPhaseInDEV()) switch (fiber.tag) {
					case FunctionComponent:
					case ForwardRef:
					case SimpleMemoComponent:
						var renderingComponentName = workInProgress && getComponentNameFromFiber(workInProgress) || "Unknown";
						var dedupeKey = renderingComponentName;
						if (!didWarnAboutUpdateInRenderForAnotherComponent.has(dedupeKey)) {
							didWarnAboutUpdateInRenderForAnotherComponent.add(dedupeKey);
							error("Cannot update a component (`%s`) while rendering a different component (`%s`). To locate the bad setState() call inside `%s`, follow the stack trace as described in https://reactjs.org/link/setstate-in-render", getComponentNameFromFiber(fiber) || "Unknown", renderingComponentName, renderingComponentName);
						}
						break;
					case ClassComponent: if (!didWarnAboutUpdateInRender) {
						error("Cannot update during an existing state transition (such as within `render`). Render methods should be a pure function of props and state.");
						didWarnAboutUpdateInRender = true;
					}
				}
			}
			function restorePendingUpdaters(root, lanes) {
				if (isDevToolsPresent) root.memoizedUpdaters.forEach(function(schedulingFiber) {
					addFiberToLanesMap(root, schedulingFiber, lanes);
				});
			}
			var fakeActCallbackNode = {};
			function scheduleCallback$1(priorityLevel, callback) {
				var actQueue = ReactCurrentActQueue$1.current;
				if (actQueue !== null) {
					actQueue.push(callback);
					return fakeActCallbackNode;
				} else return scheduleCallback(priorityLevel, callback);
			}
			function cancelCallback$1(callbackNode) {
				if (callbackNode === fakeActCallbackNode) return;
				return cancelCallback(callbackNode);
			}
			function shouldForceFlushFallbacksInDEV() {
				return ReactCurrentActQueue$1.current !== null;
			}
			function warnIfUpdatesNotWrappedWithActDEV(fiber) {
				if (fiber.mode & ConcurrentMode) {
					if (!isConcurrentActEnvironment()) return;
				} else {
					if (!isLegacyActEnvironment()) return;
					if (executionContext !== NoContext) return;
					if (fiber.tag !== FunctionComponent && fiber.tag !== ForwardRef && fiber.tag !== SimpleMemoComponent) return;
				}
				if (ReactCurrentActQueue$1.current === null) {
					var previousFiber = current;
					try {
						setCurrentFiber(fiber);
						error("An update to %s inside a test was not wrapped in act(...).\n\nWhen testing, code that causes React state updates should be wrapped into act(...):\n\nact(() => {\n  /* fire events that update state */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act", getComponentNameFromFiber(fiber));
					} finally {
						if (previousFiber) setCurrentFiber(fiber);
						else resetCurrentFiber();
					}
				}
			}
			function warnIfSuspenseResolutionNotWrappedWithActDEV(root) {
				if (root.tag !== LegacyRoot && isConcurrentActEnvironment() && ReactCurrentActQueue$1.current === null) error("A suspended resource finished loading inside a test, but the event was not wrapped in act(...).\n\nWhen testing, code that resolves suspended data should be wrapped into act(...):\n\nact(() => {\n  /* finish loading suspended data */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://reactjs.org/link/wrap-tests-with-act");
			}
			function setIsRunningInsertionEffect(isRunning) {
				isRunningInsertionEffect = isRunning;
			}
			var resolveFamily = null;
			var failedBoundaries = null;
			var setRefreshHandler = function(handler) {
				resolveFamily = handler;
			};
			function resolveFunctionForHotReloading(type) {
				if (resolveFamily === null) return type;
				var family = resolveFamily(type);
				if (family === void 0) return type;
				return family.current;
			}
			function resolveClassForHotReloading(type) {
				return resolveFunctionForHotReloading(type);
			}
			function resolveForwardRefForHotReloading(type) {
				if (resolveFamily === null) return type;
				var family = resolveFamily(type);
				if (family === void 0) {
					if (type !== null && type !== void 0 && typeof type.render === "function") {
						var currentRender = resolveFunctionForHotReloading(type.render);
						if (type.render !== currentRender) {
							var syntheticType = {
								$$typeof: REACT_FORWARD_REF_TYPE,
								render: currentRender
							};
							if (type.displayName !== void 0) syntheticType.displayName = type.displayName;
							return syntheticType;
						}
					}
					return type;
				}
				return family.current;
			}
			function isCompatibleFamilyForHotReloading(fiber, element) {
				if (resolveFamily === null) return false;
				var prevType = fiber.elementType;
				var nextType = element.type;
				var needsCompareFamilies = false;
				var $$typeofNextType = typeof nextType === "object" && nextType !== null ? nextType.$$typeof : null;
				switch (fiber.tag) {
					case ClassComponent:
						if (typeof nextType === "function") needsCompareFamilies = true;
						break;
					case FunctionComponent:
						if (typeof nextType === "function") needsCompareFamilies = true;
						else if ($$typeofNextType === REACT_LAZY_TYPE) needsCompareFamilies = true;
						break;
					case ForwardRef:
						if ($$typeofNextType === REACT_FORWARD_REF_TYPE) needsCompareFamilies = true;
						else if ($$typeofNextType === REACT_LAZY_TYPE) needsCompareFamilies = true;
						break;
					case MemoComponent:
					case SimpleMemoComponent:
						if ($$typeofNextType === REACT_MEMO_TYPE) needsCompareFamilies = true;
						else if ($$typeofNextType === REACT_LAZY_TYPE) needsCompareFamilies = true;
						break;
					default: return false;
				}
				if (needsCompareFamilies) {
					var prevFamily = resolveFamily(prevType);
					if (prevFamily !== void 0 && prevFamily === resolveFamily(nextType)) return true;
				}
				return false;
			}
			function markFailedErrorBoundaryForHotReloading(fiber) {
				if (resolveFamily === null) return;
				if (typeof WeakSet !== "function") return;
				if (failedBoundaries === null) failedBoundaries = /* @__PURE__ */ new WeakSet();
				failedBoundaries.add(fiber);
			}
			var scheduleRefresh = function(root, update) {
				if (resolveFamily === null) return;
				var staleFamilies = update.staleFamilies, updatedFamilies = update.updatedFamilies;
				flushPassiveEffects();
				flushSync(function() {
					scheduleFibersWithFamiliesRecursively(root.current, updatedFamilies, staleFamilies);
				});
			};
			var scheduleRoot = function(root, element) {
				if (root.context !== emptyContextObject) return;
				flushPassiveEffects();
				flushSync(function() {
					updateContainer(element, root, null, null);
				});
			};
			function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
				var alternate = fiber.alternate, child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
				var candidateType = null;
				switch (tag) {
					case FunctionComponent:
					case SimpleMemoComponent:
					case ClassComponent:
						candidateType = type;
						break;
					case ForwardRef: candidateType = type.render;
				}
				if (resolveFamily === null) throw new Error("Expected resolveFamily to be set during hot reload.");
				var needsRender = false;
				var needsRemount = false;
				if (candidateType !== null) {
					var family = resolveFamily(candidateType);
					if (family !== void 0) {
						if (staleFamilies.has(family)) needsRemount = true;
						else if (updatedFamilies.has(family)) {
							if (tag === ClassComponent) needsRemount = true;
							else needsRender = true;
						}
					}
				}
				if (failedBoundaries !== null) {
					if (failedBoundaries.has(fiber) || alternate !== null && failedBoundaries.has(alternate)) needsRemount = true;
				}
				if (needsRemount) fiber._debugNeedsRemount = true;
				if (needsRemount || needsRender) {
					var _root = enqueueConcurrentRenderForLane(fiber, SyncLane);
					if (_root !== null) scheduleUpdateOnFiber(_root, fiber, SyncLane, NoTimestamp);
				}
				if (child !== null && !needsRemount) scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
				if (sibling !== null) scheduleFibersWithFamiliesRecursively(sibling, updatedFamilies, staleFamilies);
			}
			var findHostInstancesForRefresh = function(root, families) {
				var hostInstances = /* @__PURE__ */ new Set();
				var types = new Set(families.map(function(family) {
					return family.current;
				}));
				findHostInstancesForMatchingFibersRecursively(root.current, types, hostInstances);
				return hostInstances;
			};
			function findHostInstancesForMatchingFibersRecursively(fiber, types, hostInstances) {
				var child = fiber.child, sibling = fiber.sibling, tag = fiber.tag, type = fiber.type;
				var candidateType = null;
				switch (tag) {
					case FunctionComponent:
					case SimpleMemoComponent:
					case ClassComponent:
						candidateType = type;
						break;
					case ForwardRef: candidateType = type.render;
				}
				var didMatch = false;
				if (candidateType !== null) {
					if (types.has(candidateType)) didMatch = true;
				}
				if (didMatch) findHostInstancesForFiberShallowly(fiber, hostInstances);
				else if (child !== null) findHostInstancesForMatchingFibersRecursively(child, types, hostInstances);
				if (sibling !== null) findHostInstancesForMatchingFibersRecursively(sibling, types, hostInstances);
			}
			function findHostInstancesForFiberShallowly(fiber, hostInstances) {
				if (findChildHostInstancesForFiberShallowly(fiber, hostInstances)) return;
				var node = fiber;
				while (true) {
					switch (node.tag) {
						case HostComponent:
							hostInstances.add(node.stateNode);
							return;
						case HostPortal:
							hostInstances.add(node.stateNode.containerInfo);
							return;
						case HostRoot:
							hostInstances.add(node.stateNode.containerInfo);
							return;
					}
					if (node.return === null) throw new Error("Expected to reach root first.");
					node = node.return;
				}
			}
			function findChildHostInstancesForFiberShallowly(fiber, hostInstances) {
				var node = fiber;
				var foundHostInstances = false;
				while (true) {
					if (node.tag === HostComponent) {
						foundHostInstances = true;
						hostInstances.add(node.stateNode);
					} else if (node.child !== null) {
						node.child.return = node;
						node = node.child;
						continue;
					}
					if (node === fiber) return foundHostInstances;
					while (node.sibling === null) {
						if (node.return === null || node.return === fiber) return foundHostInstances;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
				return false;
			}
			var hasBadMapPolyfill = false;
			try {
				Object.preventExtensions({});
			} catch (e) {
				hasBadMapPolyfill = true;
			}
			function FiberNode(tag, pendingProps, key, mode) {
				this.tag = tag;
				this.key = key;
				this.elementType = null;
				this.type = null;
				this.stateNode = null;
				this.return = null;
				this.child = null;
				this.sibling = null;
				this.index = 0;
				this.ref = null;
				this.pendingProps = pendingProps;
				this.memoizedProps = null;
				this.updateQueue = null;
				this.memoizedState = null;
				this.dependencies = null;
				this.mode = mode;
				this.flags = NoFlags;
				this.subtreeFlags = NoFlags;
				this.deletions = null;
				this.lanes = NoLanes;
				this.childLanes = NoLanes;
				this.alternate = null;
				this.actualDuration = NaN;
				this.actualStartTime = NaN;
				this.selfBaseDuration = NaN;
				this.treeBaseDuration = NaN;
				this.actualDuration = 0;
				this.actualStartTime = -1;
				this.selfBaseDuration = 0;
				this.treeBaseDuration = 0;
				this._debugSource = null;
				this._debugOwner = null;
				this._debugNeedsRemount = false;
				this._debugHookTypes = null;
				if (!hasBadMapPolyfill && typeof Object.preventExtensions === "function") Object.preventExtensions(this);
			}
			var createFiber = function(tag, pendingProps, key, mode) {
				return new FiberNode(tag, pendingProps, key, mode);
			};
			function shouldConstruct$1(Component) {
				var prototype = Component.prototype;
				return !!(prototype && prototype.isReactComponent);
			}
			function isSimpleFunctionComponent(type) {
				return typeof type === "function" && !shouldConstruct$1(type) && type.defaultProps === void 0;
			}
			function resolveLazyComponentTag(Component) {
				if (typeof Component === "function") return shouldConstruct$1(Component) ? ClassComponent : FunctionComponent;
				else if (Component !== void 0 && Component !== null) {
					var $$typeof = Component.$$typeof;
					if ($$typeof === REACT_FORWARD_REF_TYPE) return ForwardRef;
					if ($$typeof === REACT_MEMO_TYPE) return MemoComponent;
				}
				return IndeterminateComponent;
			}
			function createWorkInProgress(current, pendingProps) {
				var workInProgress = current.alternate;
				if (workInProgress === null) {
					workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode);
					workInProgress.elementType = current.elementType;
					workInProgress.type = current.type;
					workInProgress.stateNode = current.stateNode;
					workInProgress._debugSource = current._debugSource;
					workInProgress._debugOwner = current._debugOwner;
					workInProgress._debugHookTypes = current._debugHookTypes;
					workInProgress.alternate = current;
					current.alternate = workInProgress;
				} else {
					workInProgress.pendingProps = pendingProps;
					workInProgress.type = current.type;
					workInProgress.flags = NoFlags;
					workInProgress.subtreeFlags = NoFlags;
					workInProgress.deletions = null;
					workInProgress.actualDuration = 0;
					workInProgress.actualStartTime = -1;
				}
				workInProgress.flags = current.flags & StaticMask;
				workInProgress.childLanes = current.childLanes;
				workInProgress.lanes = current.lanes;
				workInProgress.child = current.child;
				workInProgress.memoizedProps = current.memoizedProps;
				workInProgress.memoizedState = current.memoizedState;
				workInProgress.updateQueue = current.updateQueue;
				var currentDependencies = current.dependencies;
				workInProgress.dependencies = currentDependencies === null ? null : {
					lanes: currentDependencies.lanes,
					firstContext: currentDependencies.firstContext
				};
				workInProgress.sibling = current.sibling;
				workInProgress.index = current.index;
				workInProgress.ref = current.ref;
				workInProgress.selfBaseDuration = current.selfBaseDuration;
				workInProgress.treeBaseDuration = current.treeBaseDuration;
				workInProgress._debugNeedsRemount = current._debugNeedsRemount;
				switch (workInProgress.tag) {
					case IndeterminateComponent:
					case FunctionComponent:
					case SimpleMemoComponent:
						workInProgress.type = resolveFunctionForHotReloading(current.type);
						break;
					case ClassComponent:
						workInProgress.type = resolveClassForHotReloading(current.type);
						break;
					case ForwardRef: workInProgress.type = resolveForwardRefForHotReloading(current.type);
				}
				return workInProgress;
			}
			function resetWorkInProgress(workInProgress, renderLanes) {
				workInProgress.flags &= StaticMask | Placement;
				var current = workInProgress.alternate;
				if (current === null) {
					workInProgress.childLanes = NoLanes;
					workInProgress.lanes = renderLanes;
					workInProgress.child = null;
					workInProgress.subtreeFlags = NoFlags;
					workInProgress.memoizedProps = null;
					workInProgress.memoizedState = null;
					workInProgress.updateQueue = null;
					workInProgress.dependencies = null;
					workInProgress.stateNode = null;
					workInProgress.selfBaseDuration = 0;
					workInProgress.treeBaseDuration = 0;
				} else {
					workInProgress.childLanes = current.childLanes;
					workInProgress.lanes = current.lanes;
					workInProgress.child = current.child;
					workInProgress.subtreeFlags = NoFlags;
					workInProgress.deletions = null;
					workInProgress.memoizedProps = current.memoizedProps;
					workInProgress.memoizedState = current.memoizedState;
					workInProgress.updateQueue = current.updateQueue;
					workInProgress.type = current.type;
					var currentDependencies = current.dependencies;
					workInProgress.dependencies = currentDependencies === null ? null : {
						lanes: currentDependencies.lanes,
						firstContext: currentDependencies.firstContext
					};
					workInProgress.selfBaseDuration = current.selfBaseDuration;
					workInProgress.treeBaseDuration = current.treeBaseDuration;
				}
				return workInProgress;
			}
			function createHostRootFiber(tag, isStrictMode, concurrentUpdatesByDefaultOverride) {
				var mode;
				if (tag === ConcurrentRoot) {
					mode = ConcurrentMode;
					if (isStrictMode === true) {
						mode |= StrictLegacyMode;
						mode |= StrictEffectsMode;
					}
				} else mode = NoMode;
				if (isDevToolsPresent) mode |= ProfileMode;
				return createFiber(HostRoot, null, null, mode);
			}
			function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
				var fiberTag = IndeterminateComponent;
				var resolvedType = type;
				if (typeof type === "function") {
					if (shouldConstruct$1(type)) {
						fiberTag = ClassComponent;
						resolvedType = resolveClassForHotReloading(resolvedType);
					} else resolvedType = resolveFunctionForHotReloading(resolvedType);
				} else if (typeof type === "string") fiberTag = HostComponent;
				else getTag: switch (type) {
					case REACT_FRAGMENT_TYPE: return createFiberFromFragment(pendingProps.children, mode, lanes, key);
					case REACT_STRICT_MODE_TYPE:
						fiberTag = Mode;
						mode |= StrictLegacyMode;
						if ((mode & ConcurrentMode) !== NoMode) mode |= StrictEffectsMode;
						break;
					case REACT_PROFILER_TYPE: return createFiberFromProfiler(pendingProps, mode, lanes, key);
					case REACT_SUSPENSE_TYPE: return createFiberFromSuspense(pendingProps, mode, lanes, key);
					case REACT_SUSPENSE_LIST_TYPE: return createFiberFromSuspenseList(pendingProps, mode, lanes, key);
					case REACT_OFFSCREEN_TYPE: return createFiberFromOffscreen(pendingProps, mode, lanes, key);
					case REACT_LEGACY_HIDDEN_TYPE:
					case REACT_SCOPE_TYPE:
					case REACT_CACHE_TYPE:
					case REACT_TRACING_MARKER_TYPE:
					case REACT_DEBUG_TRACING_MODE_TYPE:
					default:
						if (typeof type === "object" && type !== null) switch (type.$$typeof) {
							case REACT_PROVIDER_TYPE:
								fiberTag = ContextProvider;
								break getTag;
							case REACT_CONTEXT_TYPE:
								fiberTag = ContextConsumer;
								break getTag;
							case REACT_FORWARD_REF_TYPE:
								fiberTag = ForwardRef;
								resolvedType = resolveForwardRefForHotReloading(resolvedType);
								break getTag;
							case REACT_MEMO_TYPE:
								fiberTag = MemoComponent;
								break getTag;
							case REACT_LAZY_TYPE:
								fiberTag = LazyComponent;
								resolvedType = null;
								break getTag;
						}
						var info = "";
						if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
						var ownerName = owner ? getComponentNameFromFiber(owner) : null;
						if (ownerName) info += "\n\nCheck the render method of `" + ownerName + "`.";
						throw new Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) " + ("but got: " + (type == null ? type : typeof type) + "." + info));
				}
				var fiber = createFiber(fiberTag, pendingProps, key, mode);
				fiber.elementType = type;
				fiber.type = resolvedType;
				fiber.lanes = lanes;
				fiber._debugOwner = owner;
				return fiber;
			}
			function createFiberFromElement(element, mode, lanes) {
				var owner = null;
				owner = element._owner;
				var type = element.type;
				var key = element.key;
				var pendingProps = element.props;
				var fiber = createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes);
				fiber._debugSource = element._source;
				fiber._debugOwner = element._owner;
				return fiber;
			}
			function createFiberFromFragment(elements, mode, lanes, key) {
				var fiber = createFiber(Fragment, elements, key, mode);
				fiber.lanes = lanes;
				return fiber;
			}
			function createFiberFromProfiler(pendingProps, mode, lanes, key) {
				if (typeof pendingProps.id !== "string") error("Profiler must specify an \"id\" of type `string` as a prop. Received the type `%s` instead.", typeof pendingProps.id);
				var fiber = createFiber(Profiler, pendingProps, key, mode | ProfileMode);
				fiber.elementType = REACT_PROFILER_TYPE;
				fiber.lanes = lanes;
				fiber.stateNode = {
					effectDuration: 0,
					passiveEffectDuration: 0
				};
				return fiber;
			}
			function createFiberFromSuspense(pendingProps, mode, lanes, key) {
				var fiber = createFiber(SuspenseComponent, pendingProps, key, mode);
				fiber.elementType = REACT_SUSPENSE_TYPE;
				fiber.lanes = lanes;
				return fiber;
			}
			function createFiberFromSuspenseList(pendingProps, mode, lanes, key) {
				var fiber = createFiber(SuspenseListComponent, pendingProps, key, mode);
				fiber.elementType = REACT_SUSPENSE_LIST_TYPE;
				fiber.lanes = lanes;
				return fiber;
			}
			function createFiberFromOffscreen(pendingProps, mode, lanes, key) {
				var fiber = createFiber(OffscreenComponent, pendingProps, key, mode);
				fiber.elementType = REACT_OFFSCREEN_TYPE;
				fiber.lanes = lanes;
				fiber.stateNode = { isHidden: false };
				return fiber;
			}
			function createFiberFromText(content, mode, lanes) {
				var fiber = createFiber(HostText, content, null, mode);
				fiber.lanes = lanes;
				return fiber;
			}
			function createFiberFromHostInstanceForDeletion() {
				var fiber = createFiber(HostComponent, null, null, NoMode);
				fiber.elementType = "DELETED";
				return fiber;
			}
			function createFiberFromDehydratedFragment(dehydratedNode) {
				var fiber = createFiber(DehydratedFragment, null, null, NoMode);
				fiber.stateNode = dehydratedNode;
				return fiber;
			}
			function createFiberFromPortal(portal, mode, lanes) {
				var fiber = createFiber(HostPortal, portal.children !== null ? portal.children : [], portal.key, mode);
				fiber.lanes = lanes;
				fiber.stateNode = {
					containerInfo: portal.containerInfo,
					pendingChildren: null,
					implementation: portal.implementation
				};
				return fiber;
			}
			function assignFiberPropertiesInDEV(target, source) {
				if (target === null) target = createFiber(IndeterminateComponent, null, null, NoMode);
				target.tag = source.tag;
				target.key = source.key;
				target.elementType = source.elementType;
				target.type = source.type;
				target.stateNode = source.stateNode;
				target.return = source.return;
				target.child = source.child;
				target.sibling = source.sibling;
				target.index = source.index;
				target.ref = source.ref;
				target.pendingProps = source.pendingProps;
				target.memoizedProps = source.memoizedProps;
				target.updateQueue = source.updateQueue;
				target.memoizedState = source.memoizedState;
				target.dependencies = source.dependencies;
				target.mode = source.mode;
				target.flags = source.flags;
				target.subtreeFlags = source.subtreeFlags;
				target.deletions = source.deletions;
				target.lanes = source.lanes;
				target.childLanes = source.childLanes;
				target.alternate = source.alternate;
				target.actualDuration = source.actualDuration;
				target.actualStartTime = source.actualStartTime;
				target.selfBaseDuration = source.selfBaseDuration;
				target.treeBaseDuration = source.treeBaseDuration;
				target._debugSource = source._debugSource;
				target._debugOwner = source._debugOwner;
				target._debugNeedsRemount = source._debugNeedsRemount;
				target._debugHookTypes = source._debugHookTypes;
				return target;
			}
			function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onRecoverableError) {
				this.tag = tag;
				this.containerInfo = containerInfo;
				this.pendingChildren = null;
				this.current = null;
				this.pingCache = null;
				this.finishedWork = null;
				this.timeoutHandle = noTimeout;
				this.context = null;
				this.pendingContext = null;
				this.callbackNode = null;
				this.callbackPriority = NoLane;
				this.eventTimes = createLaneMap(NoLanes);
				this.expirationTimes = createLaneMap(NoTimestamp);
				this.pendingLanes = NoLanes;
				this.suspendedLanes = NoLanes;
				this.pingedLanes = NoLanes;
				this.expiredLanes = NoLanes;
				this.mutableReadLanes = NoLanes;
				this.finishedLanes = NoLanes;
				this.entangledLanes = NoLanes;
				this.entanglements = createLaneMap(NoLanes);
				this.identifierPrefix = identifierPrefix;
				this.onRecoverableError = onRecoverableError;
				this.mutableSourceEagerHydrationData = null;
				this.effectDuration = 0;
				this.passiveEffectDuration = 0;
				this.memoizedUpdaters = /* @__PURE__ */ new Set();
				var pendingUpdatersLaneMap = this.pendingUpdatersLaneMap = [];
				for (var _i = 0; _i < TotalLanes; _i++) pendingUpdatersLaneMap.push(/* @__PURE__ */ new Set());
				switch (tag) {
					case ConcurrentRoot:
						this._debugRootType = hydrate ? "hydrateRoot()" : "createRoot()";
						break;
					case LegacyRoot: this._debugRootType = hydrate ? "hydrate()" : "render()";
				}
			}
			function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
				var root = new FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onRecoverableError);
				var uninitializedFiber = createHostRootFiber(tag, isStrictMode);
				root.current = uninitializedFiber;
				uninitializedFiber.stateNode = root;
				uninitializedFiber.memoizedState = {
					element: initialChildren,
					isDehydrated: hydrate,
					cache: null,
					transitions: null,
					pendingSuspenseBoundaries: null
				};
				initializeUpdateQueue(uninitializedFiber);
				return root;
			}
			var ReactVersion = "18.3.1";
			function createPortal(children, containerInfo, implementation) {
				var key = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null;
				checkKeyStringCoercion(key);
				return {
					$$typeof: REACT_PORTAL_TYPE,
					key: key == null ? null : "" + key,
					children,
					containerInfo,
					implementation
				};
			}
			var didWarnAboutNestedUpdates;
			var didWarnAboutFindNodeInStrictMode;
			didWarnAboutNestedUpdates = false;
			didWarnAboutFindNodeInStrictMode = {};
			function getContextForSubtree(parentComponent) {
				if (!parentComponent) return emptyContextObject;
				var fiber = get(parentComponent);
				var parentContext = findCurrentUnmaskedContext(fiber);
				if (fiber.tag === ClassComponent) {
					var Component = fiber.type;
					if (isContextProvider(Component)) return processChildContext(fiber, Component, parentContext);
				}
				return parentContext;
			}
			function findHostInstanceWithWarning(component, methodName) {
				var fiber = get(component);
				if (fiber === void 0) {
					if (typeof component.render === "function") throw new Error("Unable to find node on an unmounted component.");
					else {
						var keys = Object.keys(component).join(",");
						throw new Error("Argument appears to not be a ReactComponent. Keys: " + keys);
					}
				}
				var hostFiber = findCurrentHostFiber(fiber);
				if (hostFiber === null) return null;
				if (hostFiber.mode & StrictLegacyMode) {
					var componentName = getComponentNameFromFiber(fiber) || "Component";
					if (!didWarnAboutFindNodeInStrictMode[componentName]) {
						didWarnAboutFindNodeInStrictMode[componentName] = true;
						var previousFiber = current;
						try {
							setCurrentFiber(hostFiber);
							if (fiber.mode & StrictLegacyMode) error("%s is deprecated in StrictMode. %s was passed an instance of %s which is inside StrictMode. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
							else error("%s is deprecated in StrictMode. %s was passed an instance of %s which renders StrictMode children. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node", methodName, methodName, componentName);
						} finally {
							if (previousFiber) setCurrentFiber(previousFiber);
							else resetCurrentFiber();
						}
					}
				}
				return hostFiber.stateNode;
			}
			function createContainer(containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
				return createFiberRoot(containerInfo, tag, false, null, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
			}
			function createHydrationContainer(initialChildren, callback, containerInfo, tag, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError, transitionCallbacks) {
				var root = createFiberRoot(containerInfo, tag, true, initialChildren, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
				root.context = getContextForSubtree(null);
				var current = root.current;
				var eventTime = requestEventTime();
				var lane = requestUpdateLane(current);
				var update = createUpdate(eventTime, lane);
				update.callback = callback !== void 0 && callback !== null ? callback : null;
				enqueueUpdate(current, update, lane);
				scheduleInitialHydrationOnRoot(root, lane, eventTime);
				return root;
			}
			function updateContainer(element, container, parentComponent, callback) {
				onScheduleRoot(container, element);
				var current$1 = container.current;
				var eventTime = requestEventTime();
				var lane = requestUpdateLane(current$1);
				markRenderScheduled(lane);
				var context = getContextForSubtree(parentComponent);
				if (container.context === null) container.context = context;
				else container.pendingContext = context;
				if (isRendering && current !== null && !didWarnAboutNestedUpdates) {
					didWarnAboutNestedUpdates = true;
					error("Render methods should be a pure function of props and state; triggering nested component updates from render is not allowed. If necessary, trigger nested updates in componentDidUpdate.\n\nCheck the render method of %s.", getComponentNameFromFiber(current) || "Unknown");
				}
				var update = createUpdate(eventTime, lane);
				update.payload = { element };
				callback = callback === void 0 ? null : callback;
				if (callback !== null) {
					if (typeof callback !== "function") error("render(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callback);
					update.callback = callback;
				}
				var root = enqueueUpdate(current$1, update, lane);
				if (root !== null) {
					scheduleUpdateOnFiber(root, current$1, lane, eventTime);
					entangleTransitions(root, current$1, lane);
				}
				return lane;
			}
			function getPublicRootInstance(container) {
				var containerFiber = container.current;
				if (!containerFiber.child) return null;
				switch (containerFiber.child.tag) {
					case HostComponent: return getPublicInstance(containerFiber.child.stateNode);
					default: return containerFiber.child.stateNode;
				}
			}
			function attemptSynchronousHydration$1(fiber) {
				switch (fiber.tag) {
					case HostRoot:
						var root = fiber.stateNode;
						if (isRootDehydrated(root)) flushRoot(root, getHighestPriorityPendingLanes(root));
						break;
					case SuspenseComponent:
						flushSync(function() {
							var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
							if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, requestEventTime());
						});
						markRetryLaneIfNotHydrated(fiber, SyncLane);
				}
			}
			function markRetryLaneImpl(fiber, retryLane) {
				var suspenseState = fiber.memoizedState;
				if (suspenseState !== null && suspenseState.dehydrated !== null) suspenseState.retryLane = higherPriorityLane(suspenseState.retryLane, retryLane);
			}
			function markRetryLaneIfNotHydrated(fiber, retryLane) {
				markRetryLaneImpl(fiber, retryLane);
				var alternate = fiber.alternate;
				if (alternate) markRetryLaneImpl(alternate, retryLane);
			}
			function attemptContinuousHydration$1(fiber) {
				if (fiber.tag !== SuspenseComponent) return;
				var lane = SelectiveHydrationLane;
				var root = enqueueConcurrentRenderForLane(fiber, lane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, lane, requestEventTime());
				markRetryLaneIfNotHydrated(fiber, lane);
			}
			function attemptHydrationAtCurrentPriority$1(fiber) {
				if (fiber.tag !== SuspenseComponent) return;
				var lane = requestUpdateLane(fiber);
				var root = enqueueConcurrentRenderForLane(fiber, lane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, lane, requestEventTime());
				markRetryLaneIfNotHydrated(fiber, lane);
			}
			function findHostInstanceWithNoPortals(fiber) {
				var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
				if (hostFiber === null) return null;
				return hostFiber.stateNode;
			}
			var shouldErrorImpl = function(fiber) {
				return null;
			};
			function shouldError(fiber) {
				return shouldErrorImpl(fiber);
			}
			var shouldSuspendImpl = function(fiber) {
				return false;
			};
			function shouldSuspend(fiber) {
				return shouldSuspendImpl(fiber);
			}
			var overrideHookState = null;
			var overrideHookStateDeletePath = null;
			var overrideHookStateRenamePath = null;
			var overrideProps = null;
			var overridePropsDeletePath = null;
			var overridePropsRenamePath = null;
			var scheduleUpdate = null;
			var setErrorHandler = null;
			var setSuspenseHandler = null;
			var copyWithDeleteImpl = function(obj, path, index) {
				var key = path[index];
				var updated = isArray(obj) ? obj.slice() : assign({}, obj);
				if (index + 1 === path.length) {
					if (isArray(updated)) updated.splice(key, 1);
					else delete updated[key];
					return updated;
				}
				updated[key] = copyWithDeleteImpl(obj[key], path, index + 1);
				return updated;
			};
			var copyWithDelete = function(obj, path) {
				return copyWithDeleteImpl(obj, path, 0);
			};
			var copyWithRenameImpl = function(obj, oldPath, newPath, index) {
				var oldKey = oldPath[index];
				var updated = isArray(obj) ? obj.slice() : assign({}, obj);
				if (index + 1 === oldPath.length) {
					var newKey = newPath[index];
					updated[newKey] = updated[oldKey];
					if (isArray(updated)) updated.splice(oldKey, 1);
					else delete updated[oldKey];
				} else updated[oldKey] = copyWithRenameImpl(obj[oldKey], oldPath, newPath, index + 1);
				return updated;
			};
			var copyWithRename = function(obj, oldPath, newPath) {
				if (oldPath.length !== newPath.length) {
					warn("copyWithRename() expects paths of the same length");
					return;
				} else for (var i = 0; i < newPath.length - 1; i++) if (oldPath[i] !== newPath[i]) {
					warn("copyWithRename() expects paths to be the same except for the deepest key");
					return;
				}
				return copyWithRenameImpl(obj, oldPath, newPath, 0);
			};
			var copyWithSetImpl = function(obj, path, index, value) {
				if (index >= path.length) return value;
				var key = path[index];
				var updated = isArray(obj) ? obj.slice() : assign({}, obj);
				updated[key] = copyWithSetImpl(obj[key], path, index + 1, value);
				return updated;
			};
			var copyWithSet = function(obj, path, value) {
				return copyWithSetImpl(obj, path, 0, value);
			};
			var findHook = function(fiber, id) {
				var currentHook = fiber.memoizedState;
				while (currentHook !== null && id > 0) {
					currentHook = currentHook.next;
					id--;
				}
				return currentHook;
			};
			overrideHookState = function(fiber, id, path, value) {
				var hook = findHook(fiber, id);
				if (hook !== null) {
					var newState = copyWithSet(hook.memoizedState, path, value);
					hook.memoizedState = newState;
					hook.baseState = newState;
					fiber.memoizedProps = assign({}, fiber.memoizedProps);
					var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
					if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
				}
			};
			overrideHookStateDeletePath = function(fiber, id, path) {
				var hook = findHook(fiber, id);
				if (hook !== null) {
					var newState = copyWithDelete(hook.memoizedState, path);
					hook.memoizedState = newState;
					hook.baseState = newState;
					fiber.memoizedProps = assign({}, fiber.memoizedProps);
					var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
					if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
				}
			};
			overrideHookStateRenamePath = function(fiber, id, oldPath, newPath) {
				var hook = findHook(fiber, id);
				if (hook !== null) {
					var newState = copyWithRename(hook.memoizedState, oldPath, newPath);
					hook.memoizedState = newState;
					hook.baseState = newState;
					fiber.memoizedProps = assign({}, fiber.memoizedProps);
					var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
					if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
				}
			};
			overrideProps = function(fiber, path, value) {
				fiber.pendingProps = copyWithSet(fiber.memoizedProps, path, value);
				if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
				var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
			};
			overridePropsDeletePath = function(fiber, path) {
				fiber.pendingProps = copyWithDelete(fiber.memoizedProps, path);
				if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
				var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
			};
			overridePropsRenamePath = function(fiber, oldPath, newPath) {
				fiber.pendingProps = copyWithRename(fiber.memoizedProps, oldPath, newPath);
				if (fiber.alternate) fiber.alternate.pendingProps = fiber.pendingProps;
				var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
			};
			scheduleUpdate = function(fiber) {
				var root = enqueueConcurrentRenderForLane(fiber, SyncLane);
				if (root !== null) scheduleUpdateOnFiber(root, fiber, SyncLane, NoTimestamp);
			};
			setErrorHandler = function(newShouldErrorImpl) {
				shouldErrorImpl = newShouldErrorImpl;
			};
			setSuspenseHandler = function(newShouldSuspendImpl) {
				shouldSuspendImpl = newShouldSuspendImpl;
			};
			function findHostInstanceByFiber(fiber) {
				var hostFiber = findCurrentHostFiber(fiber);
				if (hostFiber === null) return null;
				return hostFiber.stateNode;
			}
			function emptyFindFiberByHostInstance(instance) {
				return null;
			}
			function getCurrentFiberForDevTools() {
				return current;
			}
			function injectIntoDevTools(devToolsConfig) {
				var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;
				var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
				return injectInternals({
					bundleType: devToolsConfig.bundleType,
					version: devToolsConfig.version,
					rendererPackageName: devToolsConfig.rendererPackageName,
					rendererConfig: devToolsConfig.rendererConfig,
					overrideHookState,
					overrideHookStateDeletePath,
					overrideHookStateRenamePath,
					overrideProps,
					overridePropsDeletePath,
					overridePropsRenamePath,
					setErrorHandler,
					setSuspenseHandler,
					scheduleUpdate,
					currentDispatcherRef: ReactCurrentDispatcher,
					findHostInstanceByFiber,
					findFiberByHostInstance: findFiberByHostInstance || emptyFindFiberByHostInstance,
					findHostInstancesForRefresh,
					scheduleRefresh,
					scheduleRoot,
					setRefreshHandler,
					getCurrentFiber: getCurrentFiberForDevTools,
					reconcilerVersion: ReactVersion
				});
			}
			var defaultOnRecoverableError = typeof reportError === "function" ? reportError : function(error) {
				console["error"](error);
			};
			function ReactDOMRoot(internalRoot) {
				this._internalRoot = internalRoot;
			}
			ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
				var root = this._internalRoot;
				if (root === null) throw new Error("Cannot update an unmounted root.");
				if (typeof arguments[1] === "function") error("render(...): does not support the second callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
				else if (isValidContainer(arguments[1])) error("You passed a container to the second argument of root.render(...). You don't need to pass it again since you already passed it to create the root.");
				else if (typeof arguments[1] !== "undefined") error("You passed a second argument to root.render(...) but it only accepts one argument.");
				var container = root.containerInfo;
				if (container.nodeType !== COMMENT_NODE) {
					var hostInstance = findHostInstanceWithNoPortals(root.current);
					if (hostInstance) {
						if (hostInstance.parentNode !== container) error("render(...): It looks like the React-rendered content of the root container was removed without using React. This is not supported and will cause errors. Instead, call root.unmount() to empty a root's container.");
					}
				}
				updateContainer(children, root, null, null);
			};
			ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
				if (typeof arguments[0] === "function") error("unmount(...): does not support a callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
				var root = this._internalRoot;
				if (root !== null) {
					this._internalRoot = null;
					var container = root.containerInfo;
					if (isAlreadyRendering()) error("Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.");
					flushSync(function() {
						updateContainer(null, root, null, null);
					});
					unmarkContainerAsRoot(container);
				}
			};
			function createRoot(container, options) {
				if (!isValidContainer(container)) throw new Error("createRoot(...): Target container is not a DOM element.");
				warnIfReactDOMContainerInDEV(container);
				var isStrictMode = false;
				var concurrentUpdatesByDefaultOverride = false;
				var identifierPrefix = "";
				var onRecoverableError = defaultOnRecoverableError;
				if (options !== null && options !== void 0) {
					if (options.hydrate) warn("hydrate through createRoot is deprecated. Use ReactDOMClient.hydrateRoot(container, <App />) instead.");
					else if (typeof options === "object" && options !== null && options.$$typeof === REACT_ELEMENT_TYPE) error("You passed a JSX element to createRoot. You probably meant to call root.render instead. Example usage:\n\n  let root = createRoot(domContainer);\n  root.render(<App />);");
					if (options.unstable_strictMode === true) isStrictMode = true;
					if (options.identifierPrefix !== void 0) identifierPrefix = options.identifierPrefix;
					if (options.onRecoverableError !== void 0) onRecoverableError = options.onRecoverableError;
					if (options.transitionCallbacks !== void 0) options.transitionCallbacks;
				}
				var root = createContainer(container, ConcurrentRoot, null, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
				markContainerAsRoot(root.current, container);
				listenToAllSupportedEvents(container.nodeType === COMMENT_NODE ? container.parentNode : container);
				return new ReactDOMRoot(root);
			}
			function ReactDOMHydrationRoot(internalRoot) {
				this._internalRoot = internalRoot;
			}
			function scheduleHydration(target) {
				if (target) queueExplicitHydrationTarget(target);
			}
			ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = scheduleHydration;
			function hydrateRoot(container, initialChildren, options) {
				if (!isValidContainer(container)) throw new Error("hydrateRoot(...): Target container is not a DOM element.");
				warnIfReactDOMContainerInDEV(container);
				if (initialChildren === void 0) error("Must provide initial children as second argument to hydrateRoot. Example usage: hydrateRoot(domContainer, <App />)");
				var hydrationCallbacks = options != null ? options : null;
				var mutableSources = options != null && options.hydratedSources || null;
				var isStrictMode = false;
				var concurrentUpdatesByDefaultOverride = false;
				var identifierPrefix = "";
				var onRecoverableError = defaultOnRecoverableError;
				if (options !== null && options !== void 0) {
					if (options.unstable_strictMode === true) isStrictMode = true;
					if (options.identifierPrefix !== void 0) identifierPrefix = options.identifierPrefix;
					if (options.onRecoverableError !== void 0) onRecoverableError = options.onRecoverableError;
				}
				var root = createHydrationContainer(initialChildren, null, container, ConcurrentRoot, hydrationCallbacks, isStrictMode, concurrentUpdatesByDefaultOverride, identifierPrefix, onRecoverableError);
				markContainerAsRoot(root.current, container);
				listenToAllSupportedEvents(container);
				if (mutableSources) for (var i = 0; i < mutableSources.length; i++) {
					var mutableSource = mutableSources[i];
					registerMutableSourceForHydration(root, mutableSource);
				}
				return new ReactDOMHydrationRoot(root);
			}
			function isValidContainer(node) {
				return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || !disableCommentsAsDOMContainers));
			}
			function isValidContainerLegacy(node) {
				return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === " react-mount-point-unstable "));
			}
			function warnIfReactDOMContainerInDEV(container) {
				if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === "BODY") error("createRoot(): Creating roots directly with document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try using a container element created for your app.");
				if (isContainerMarkedAsRoot(container)) {
					if (container._reactRootContainer) error("You are calling ReactDOMClient.createRoot() on a container that was previously passed to ReactDOM.render(). This is not supported.");
					else error("You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before. Instead, call root.render() on the existing root instead if you want to update it.");
				}
			}
			var ReactCurrentOwner$3 = ReactSharedInternals.ReactCurrentOwner;
			var topLevelUpdateWarnings = function(container) {
				if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
					var hostInstance = findHostInstanceWithNoPortals(container._reactRootContainer.current);
					if (hostInstance) {
						if (hostInstance.parentNode !== container) error("render(...): It looks like the React-rendered content of this container was removed without using React. This is not supported and will cause errors. Instead, call ReactDOM.unmountComponentAtNode to empty a container.");
					}
				}
				var isRootRenderedBySomeReact = !!container._reactRootContainer;
				var rootEl = getReactRootElementInContainer(container);
				if (!!(rootEl && getInstanceFromNode(rootEl)) && !isRootRenderedBySomeReact) error("render(...): Replacing React-rendered children with a new root component. If you intended to update the children of this node, you should instead have the existing children update their state and render the new components instead of calling ReactDOM.render.");
				if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === "BODY") error("render(): Rendering components directly into document.body is discouraged, since its children are often manipulated by third-party scripts and browser extensions. This may lead to subtle reconciliation issues. Try rendering into a container element created for your app.");
			};
			function getReactRootElementInContainer(container) {
				if (!container) return null;
				if (container.nodeType === DOCUMENT_NODE) return container.documentElement;
				else return container.firstChild;
			}
			function noopOnRecoverableError() {}
			function legacyCreateRootFromDOMContainer(container, initialChildren, parentComponent, callback, isHydrationContainer) {
				if (isHydrationContainer) {
					if (typeof callback === "function") {
						var originalCallback = callback;
						callback = function() {
							var instance = getPublicRootInstance(root);
							originalCallback.call(instance);
						};
					}
					var root = createHydrationContainer(initialChildren, callback, container, LegacyRoot, null, false, false, "", noopOnRecoverableError);
					container._reactRootContainer = root;
					markContainerAsRoot(root.current, container);
					listenToAllSupportedEvents(container.nodeType === COMMENT_NODE ? container.parentNode : container);
					flushSync();
					return root;
				} else {
					var rootSibling;
					while (rootSibling = container.lastChild) container.removeChild(rootSibling);
					if (typeof callback === "function") {
						var _originalCallback = callback;
						callback = function() {
							var instance = getPublicRootInstance(_root);
							_originalCallback.call(instance);
						};
					}
					var _root = createContainer(container, LegacyRoot, null, false, false, "", noopOnRecoverableError);
					container._reactRootContainer = _root;
					markContainerAsRoot(_root.current, container);
					listenToAllSupportedEvents(container.nodeType === COMMENT_NODE ? container.parentNode : container);
					flushSync(function() {
						updateContainer(initialChildren, _root, parentComponent, callback);
					});
					return _root;
				}
			}
			function warnOnInvalidCallback$1(callback, callerName) {
				if (callback !== null && typeof callback !== "function") error("%s(...): Expected the last optional `callback` argument to be a function. Instead received: %s.", callerName, callback);
			}
			function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
				topLevelUpdateWarnings(container);
				warnOnInvalidCallback$1(callback === void 0 ? null : callback, "render");
				var maybeRoot = container._reactRootContainer;
				var root;
				if (!maybeRoot) root = legacyCreateRootFromDOMContainer(container, children, parentComponent, callback, forceHydrate);
				else {
					root = maybeRoot;
					if (typeof callback === "function") {
						var originalCallback = callback;
						callback = function() {
							var instance = getPublicRootInstance(root);
							originalCallback.call(instance);
						};
					}
					updateContainer(children, root, parentComponent, callback);
				}
				return getPublicRootInstance(root);
			}
			var didWarnAboutFindDOMNode = false;
			function findDOMNode(componentOrElement) {
				if (!didWarnAboutFindDOMNode) {
					didWarnAboutFindDOMNode = true;
					error("findDOMNode is deprecated and will be removed in the next major release. Instead, add a ref directly to the element you want to reference. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-find-node");
				}
				var owner = ReactCurrentOwner$3.current;
				if (owner !== null && owner.stateNode !== null) {
					if (!owner.stateNode._warnedAboutRefsInRender) error("%s is accessing findDOMNode inside its render(). render() should be a pure function of props and state. It should never access something that requires stale data from the previous render, such as refs. Move this logic to componentDidMount and componentDidUpdate instead.", getComponentNameFromType(owner.type) || "A component");
					owner.stateNode._warnedAboutRefsInRender = true;
				}
				if (componentOrElement == null) return null;
				if (componentOrElement.nodeType === ELEMENT_NODE) return componentOrElement;
				return findHostInstanceWithWarning(componentOrElement, "findDOMNode");
			}
			function hydrate(element, container, callback) {
				error("ReactDOM.hydrate is no longer supported in React 18. Use hydrateRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
				if (!isValidContainerLegacy(container)) throw new Error("Target container is not a DOM element.");
				if (isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0) error("You are calling ReactDOM.hydrate() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call hydrateRoot(container, element)?");
				return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
			}
			function render(element, container, callback) {
				error("ReactDOM.render is no longer supported in React 18. Use createRoot instead. Until you switch to the new API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
				if (!isValidContainerLegacy(container)) throw new Error("Target container is not a DOM element.");
				if (isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0) error("You are calling ReactDOM.render() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.render(element)?");
				return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
			}
			function unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
				error("ReactDOM.unstable_renderSubtreeIntoContainer() is no longer supported in React 18. Consider using a portal instead. Until you switch to the createRoot API, your app will behave as if it's running React 17. Learn more: https://reactjs.org/link/switch-to-createroot");
				if (!isValidContainerLegacy(containerNode)) throw new Error("Target container is not a DOM element.");
				if (parentComponent == null || !has(parentComponent)) throw new Error("parentComponent must be a valid React Component");
				return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
			}
			var didWarnAboutUnmountComponentAtNode = false;
			function unmountComponentAtNode(container) {
				if (!didWarnAboutUnmountComponentAtNode) {
					didWarnAboutUnmountComponentAtNode = true;
					error("unmountComponentAtNode is deprecated and will be removed in the next major release. Switch to the createRoot API. Learn more: https://reactjs.org/link/switch-to-createroot");
				}
				if (!isValidContainerLegacy(container)) throw new Error("unmountComponentAtNode(...): Target container is not a DOM element.");
				if (isContainerMarkedAsRoot(container) && container._reactRootContainer === void 0) error("You are calling ReactDOM.unmountComponentAtNode() on a container that was previously passed to ReactDOMClient.createRoot(). This is not supported. Did you mean to call root.unmount()?");
				if (container._reactRootContainer) {
					var rootEl = getReactRootElementInContainer(container);
					if (rootEl && !getInstanceFromNode(rootEl)) error("unmountComponentAtNode(): The node you're attempting to unmount was rendered by another copy of React.");
					flushSync(function() {
						legacyRenderSubtreeIntoContainer(null, null, container, false, function() {
							container._reactRootContainer = null;
							unmarkContainerAsRoot(container);
						});
					});
					return true;
				} else {
					var _rootEl = getReactRootElementInContainer(container);
					var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode(_rootEl));
					var isContainerReactRoot = container.nodeType === ELEMENT_NODE && isValidContainerLegacy(container.parentNode) && !!container.parentNode._reactRootContainer;
					if (hasNonRootReactChild) error("unmountComponentAtNode(): The node you're attempting to unmount was rendered by React and is not a top-level container. %s", isContainerReactRoot ? "You may have accidentally passed in a React root node instead of its container." : "Instead, have the parent component update its state and rerender in order to remove this component.");
					return false;
				}
			}
			setAttemptSynchronousHydration(attemptSynchronousHydration$1);
			setAttemptContinuousHydration(attemptContinuousHydration$1);
			setAttemptHydrationAtCurrentPriority(attemptHydrationAtCurrentPriority$1);
			setGetCurrentUpdatePriority(getCurrentUpdatePriority);
			setAttemptHydrationAtPriority(runWithPriority);
			if (typeof Map !== "function" || Map.prototype == null || typeof Map.prototype.forEach !== "function" || typeof Set !== "function" || Set.prototype == null || typeof Set.prototype.clear !== "function" || typeof Set.prototype.forEach !== "function") error("React depends on Map and Set built-in types. Make sure that you load a polyfill in older browsers. https://reactjs.org/link/react-polyfills");
			setRestoreImplementation(restoreControlledState$3);
			setBatchingImplementation(batchedUpdates$1, discreteUpdates, flushSync);
			function createPortal$1(children, container) {
				var key = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
				if (!isValidContainer(container)) throw new Error("Target container is not a DOM element.");
				return createPortal(children, container, null, key);
			}
			function renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
				return unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback);
			}
			var Internals = {
				usingClientEntryPoint: false,
				Events: [
					getInstanceFromNode,
					getNodeFromInstance,
					getFiberCurrentPropsFromNode,
					enqueueStateRestore,
					restoreStateIfNeeded,
					batchedUpdates$1
				]
			};
			function createRoot$1(container, options) {
				if (!Internals.usingClientEntryPoint && true) error("You are importing createRoot from \"react-dom\" which is not supported. You should instead import it from \"react-dom/client\".");
				return createRoot(container, options);
			}
			function hydrateRoot$1(container, initialChildren, options) {
				if (!Internals.usingClientEntryPoint && true) error("You are importing hydrateRoot from \"react-dom\" which is not supported. You should instead import it from \"react-dom/client\".");
				return hydrateRoot(container, initialChildren, options);
			}
			function flushSync$1(fn) {
				if (isAlreadyRendering()) error("flushSync was called from inside a lifecycle method. React cannot flush when React is already rendering. Consider moving this call to a scheduler task or micro task.");
				return flushSync(fn);
			}
			if (!injectIntoDevTools({
				findFiberByHostInstance: getClosestInstanceFromNode,
				bundleType: 1,
				version: ReactVersion,
				rendererPackageName: "react-dom"
			}) && canUseDOM && window.top === window.self) {
				if (navigator.userAgent.indexOf("Chrome") > -1 && navigator.userAgent.indexOf("Edge") === -1 || navigator.userAgent.indexOf("Firefox") > -1) {
					var protocol = window.location.protocol;
					if (/^(https?|file):$/.test(protocol)) console.info("%cDownload the React DevTools for a better development experience: https://reactjs.org/link/react-devtools" + (protocol === "file:" ? "\nYou might need to use a local HTTP server (instead of file://): https://reactjs.org/link/react-devtools-faq" : ""), "font-weight:bold");
				}
			}
			exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = Internals;
			exports.createPortal = createPortal$1;
			exports.createRoot = createRoot$1;
			exports.findDOMNode = findDOMNode;
			exports.flushSync = flushSync$1;
			exports.hydrate = hydrate;
			exports.hydrateRoot = hydrateRoot$1;
			exports.render = render;
			exports.unmountComponentAtNode = unmountComponentAtNode;
			exports.unstable_batchedUpdates = batchedUpdates$1;
			exports.unstable_renderSubtreeIntoContainer = renderSubtreeIntoContainer;
			exports.version = ReactVersion;
			if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(/* @__PURE__ */ new Error());
		})();
	}));
	//#endregion
	//#region ../node_modules/react-dom/index.js
	var require_react_dom = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_react_dom_development();
	}));
	//#endregion
	//#region ../node_modules/react-dom/client.js
	var require_client = /* @__PURE__ */ __commonJSMin(((exports) => {
		var m = require_react_dom();
		var i = m.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
		exports.createRoot = function(c, o) {
			i.usingClientEntryPoint = true;
			try {
				return m.createRoot(c, o);
			} finally {
				i.usingClientEntryPoint = false;
			}
		};
	}));
	//#endregion
	//#region ../node_modules/react-router/dist/development/chunk-BV7QT456.mjs
	/**
	* react-router v7.18.3
	*
	* Copyright (c) Remix Software Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE.md file in the root directory of this source tree.
	*
	* @license MIT
	*/
	function normalizeProtocolRelativeUrl(url, protocol) {
		return protocol + url.replace(/\\/g, "/");
	}
	function isLocation(obj) {
		return typeof obj === "object" && obj != null && "pathname" in obj && "search" in obj && "hash" in obj && "state" in obj && "key" in obj;
	}
	function createMemoryHistory(options = {}) {
		let { initialEntries = ["/"], initialIndex, v5Compat = false } = options;
		let entries;
		entries = initialEntries.map((entry, index2) => createMemoryLocation(entry, typeof entry === "string" ? null : entry.state, index2 === 0 ? "default" : void 0, typeof entry === "string" ? void 0 : entry.mask));
		let index = clampIndex(initialIndex == null ? entries.length - 1 : initialIndex);
		let action = "POP";
		let listener = null;
		function clampIndex(n) {
			return Math.min(Math.max(n, 0), entries.length - 1);
		}
		function getCurrentLocation() {
			return entries[index];
		}
		function createMemoryLocation(to, state = null, key, mask) {
			let location = createLocation(entries ? getCurrentLocation().pathname : "/", to, state, key, mask);
			warning(location.pathname.charAt(0) === "/", `relative pathnames are not supported in memory history: ${JSON.stringify(to)}`);
			return location;
		}
		function createHref2(to) {
			return typeof to === "string" ? to : createPath(to);
		}
		return {
			get index() {
				return index;
			},
			get action() {
				return action;
			},
			get location() {
				return getCurrentLocation();
			},
			createHref: createHref2,
			createURL(to) {
				return new URL(createHref2(to), "http://localhost");
			},
			encodeLocation(to) {
				let path = typeof to === "string" ? parsePath(to) : to;
				return {
					pathname: path.pathname || "",
					search: path.search || "",
					hash: path.hash || ""
				};
			},
			push(to, state) {
				action = "PUSH";
				let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
				index += 1;
				entries.splice(index, entries.length, nextLocation);
				if (v5Compat && listener) listener({
					action,
					location: nextLocation,
					delta: 1
				});
			},
			replace(to, state) {
				action = "REPLACE";
				let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
				entries[index] = nextLocation;
				if (v5Compat && listener) listener({
					action,
					location: nextLocation,
					delta: 0
				});
			},
			go(delta) {
				action = "POP";
				let nextIndex = clampIndex(index + delta);
				let nextLocation = entries[nextIndex];
				index = nextIndex;
				if (listener) listener({
					action,
					location: nextLocation,
					delta
				});
			},
			listen(fn) {
				listener = fn;
				return () => {
					listener = null;
				};
			}
		};
	}
	function invariant(value, message) {
		if (value === false || value === null || typeof value === "undefined") throw new Error(message);
	}
	function warning(cond, message) {
		if (!cond) {
			if (typeof console !== "undefined") console.warn(message);
			try {
				throw new Error(message);
			} catch (e) {}
		}
	}
	function createKey() {
		return Math.random().toString(36).substring(2, 10);
	}
	function createLocation(current, to, state = null, key, mask) {
		return {
			pathname: typeof current === "string" ? current : current.pathname,
			search: "",
			hash: "",
			...typeof to === "string" ? parsePath(to) : to,
			state,
			key: to && to.key || key || createKey(),
			mask
		};
	}
	function createPath({ pathname = "/", search = "", hash = "" }) {
		if (search && search !== "?") pathname += search.charAt(0) === "?" ? search : "?" + search;
		if (hash && hash !== "#") pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
		return pathname;
	}
	function parsePath(path) {
		let parsedPath = {};
		if (path) {
			let hashIndex = path.indexOf("#");
			if (hashIndex >= 0) {
				parsedPath.hash = path.substring(hashIndex);
				path = path.substring(0, hashIndex);
			}
			let searchIndex = path.indexOf("?");
			if (searchIndex >= 0) {
				parsedPath.search = path.substring(searchIndex);
				path = path.substring(0, searchIndex);
			}
			if (path) parsedPath.pathname = path;
		}
		return parsedPath;
	}
	function matchRoutes(routes, locationArg, basename = "/") {
		return matchRoutesImpl(routes, locationArg, basename, false);
	}
	function matchRoutesImpl(routes, locationArg, basename, allowPartial, precomputedBranches) {
		let pathname = stripBasename((typeof locationArg === "string" ? parsePath(locationArg) : locationArg).pathname || "/", basename);
		if (pathname == null) return null;
		let branches = precomputedBranches ?? flattenAndRankRoutes(routes);
		let matches = null;
		let decoded = decodePath(pathname);
		for (let i = 0; matches == null && i < branches.length; ++i) matches = matchRouteBranch(branches[i], decoded, allowPartial);
		return matches;
	}
	function convertRouteMatchToUiMatch(match, loaderData) {
		let { route, pathname, params } = match;
		return {
			id: route.id,
			pathname,
			params,
			data: loaderData[route.id],
			loaderData: loaderData[route.id],
			handle: route.handle
		};
	}
	function flattenAndRankRoutes(routes) {
		let branches = flattenRoutes(routes);
		rankRouteBranches(branches);
		return branches;
	}
	function flattenRoutes(routes, branches = [], parentsMeta = [], parentPath = "", _hasParentOptionalSegments = false) {
		let flattenRoute = (route, index, hasParentOptionalSegments = _hasParentOptionalSegments, relativePath) => {
			let meta = {
				relativePath: relativePath === void 0 ? route.path || "" : relativePath,
				caseSensitive: route.caseSensitive === true,
				childrenIndex: index,
				route
			};
			if (meta.relativePath.startsWith("/")) {
				if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) return;
				invariant(meta.relativePath.startsWith(parentPath), `Absolute route path "${meta.relativePath}" nested under path "${parentPath}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`);
				meta.relativePath = meta.relativePath.slice(parentPath.length);
			}
			let path = joinPaths([parentPath, meta.relativePath]);
			let routesMeta = parentsMeta.concat(meta);
			if (route.children && route.children.length > 0) {
				invariant(route.index !== true, `Index routes must not have child routes. Please remove all child routes from route path "${path}".`);
				flattenRoutes(route.children, branches, routesMeta, path, hasParentOptionalSegments);
			}
			if (route.path == null && !route.index) return;
			branches.push({
				path,
				score: computeScore(path, route.index),
				routesMeta: routesMeta.map((meta2, i) => {
					let [matcher, params] = compilePath(meta2.relativePath, meta2.caseSensitive, i === routesMeta.length - 1);
					return {
						...meta2,
						matcher,
						compiledParams: params
					};
				})
			});
		};
		routes.forEach((route, index) => {
			if (route.path === "" || !route.path?.includes("?")) flattenRoute(route, index);
			else for (let exploded of explodeOptionalSegments(route.path)) flattenRoute(route, index, true, exploded);
		});
		return branches;
	}
	function explodeOptionalSegments(path) {
		let segments = path.split("/");
		if (segments.length === 0) return [];
		let [first, ...rest] = segments;
		let isOptional = first.endsWith("?");
		let required = first.replace(/\?$/, "");
		if (rest.length === 0) return isOptional ? [required, ""] : [required];
		let restExploded = explodeOptionalSegments(rest.join("/"));
		let result = [];
		result.push(...restExploded.map((subpath) => subpath === "" ? required : [required, subpath].join("/")));
		if (isOptional) result.push(...restExploded);
		return result.map((exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded);
	}
	function rankRouteBranches(branches) {
		branches.sort((a, b) => a.score !== b.score ? b.score - a.score : compareIndexes(a.routesMeta.map((meta) => meta.childrenIndex), b.routesMeta.map((meta) => meta.childrenIndex)));
	}
	function computeScore(path, index) {
		let segments = path.split("/");
		let initialScore = segments.length;
		if (segments.some(isSplat)) initialScore += splatPenalty;
		if (index) initialScore += indexRouteValue;
		return segments.filter((s) => !isSplat(s)).reduce((score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue), initialScore);
	}
	function compareIndexes(a, b) {
		return a.length === b.length && a.slice(0, -1).every((n, i) => n === b[i]) ? a[a.length - 1] - b[b.length - 1] : 0;
	}
	function matchRouteBranch(branch, pathname, allowPartial = false) {
		let { routesMeta } = branch;
		let matchedParams = {};
		let matchedPathname = "/";
		let matches = [];
		for (let i = 0; i < routesMeta.length; ++i) {
			let meta = routesMeta[i];
			let end = i === routesMeta.length - 1;
			let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
			let pattern = {
				path: meta.relativePath,
				caseSensitive: meta.caseSensitive,
				end
			};
			let match = meta.matcher && meta.compiledParams ? matchPathImpl(pattern, remainingPathname, meta.matcher, meta.compiledParams) : matchPath(pattern, remainingPathname);
			let route = meta.route;
			if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) match = matchPath({
				path: meta.relativePath,
				caseSensitive: meta.caseSensitive,
				end: false
			}, remainingPathname);
			if (!match) return null;
			Object.assign(matchedParams, match.params);
			matches.push({
				params: matchedParams,
				pathname: joinPaths([matchedPathname, match.pathname]),
				pathnameBase: normalizePathname(joinPaths([matchedPathname, match.pathnameBase])),
				route
			});
			if (match.pathnameBase !== "/") matchedPathname = joinPaths([matchedPathname, match.pathnameBase]);
		}
		return matches;
	}
	function matchPath(pattern, pathname) {
		if (typeof pattern === "string") pattern = {
			path: pattern,
			caseSensitive: false,
			end: true
		};
		let [matcher, compiledParams] = compilePath(pattern.path, pattern.caseSensitive, pattern.end);
		return matchPathImpl(pattern, pathname, matcher, compiledParams);
	}
	function matchPathImpl(pattern, pathname, matcher, compiledParams) {
		let match = pathname.match(matcher);
		if (!match) return null;
		let matchedPathname = match[0];
		let pathnameBase = removeTrailingSlash(matchedPathname, 1);
		let captureGroups = match.slice(1);
		return {
			params: compiledParams.reduce((memo2, { paramName, isOptional }, index) => {
				if (paramName === "*") {
					let splatValue = captureGroups[index] || "";
					pathnameBase = removeTrailingSlash(matchedPathname.slice(0, matchedPathname.length - splatValue.length), 1);
				}
				const value = captureGroups[index];
				if (isOptional && !value) memo2[paramName] = void 0;
				else memo2[paramName] = (value || "").replace(/%2F/g, "/");
				return memo2;
			}, {}),
			pathname: matchedPathname,
			pathnameBase,
			pattern
		};
	}
	function compilePath(path, caseSensitive = false, end = true) {
		warning(path === "*" || !path.endsWith("*") || path.endsWith("/*"), `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`);
		let params = [];
		let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(/\/:([\w-]+)(\?)?/g, (match, paramName, isOptional, index, str) => {
			params.push({
				paramName,
				isOptional: isOptional != null
			});
			if (isOptional) {
				let nextChar = str.charAt(index + match.length);
				if (nextChar && nextChar !== "/") return "/([^\\/]*)";
				return "(?:/([^\\/]*))?";
			}
			return "/([^\\/]+)";
		}).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
		if (path.endsWith("*")) {
			params.push({ paramName: "*" });
			regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
		} else if (end) regexpSource += "\\/*$";
		else if (path !== "" && path !== "/") regexpSource += "(?:(?=\\/|$))";
		return [new RegExp(regexpSource, caseSensitive ? void 0 : "i"), params];
	}
	function decodePath(value) {
		try {
			return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
		} catch (error) {
			warning(false, `The URL path "${value}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${error}).`);
			return value;
		}
	}
	function stripBasename(pathname, basename) {
		if (basename === "/") return pathname;
		if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) return null;
		let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
		let nextChar = pathname.charAt(startIndex);
		if (nextChar && nextChar !== "/") return null;
		return pathname.slice(startIndex) || "/";
	}
	function resolvePath(to, fromPathname = "/") {
		let { pathname: toPathname, search = "", hash = "" } = typeof to === "string" ? parsePath(to) : to;
		let pathname;
		if (toPathname) {
			toPathname = removeDoubleSlashes(toPathname);
			if (toPathname.startsWith("/") || toPathname.startsWith("\\")) pathname = resolvePathname(toPathname.substring(1), "/");
			else pathname = resolvePathname(toPathname, fromPathname);
		} else pathname = fromPathname;
		return {
			pathname,
			search: normalizeSearch(search),
			hash: normalizeHash(hash)
		};
	}
	function resolvePathname(relativePath, fromPathname) {
		let segments = removeTrailingSlash(fromPathname).split("/");
		relativePath.split("/").forEach((segment) => {
			if (segment === "..") {
				if (segments.length > 1) segments.pop();
			} else if (segment !== ".") segments.push(segment);
		});
		return segments.length > 1 ? segments.join("/") : "/";
	}
	function getInvalidPathError(char, field, dest, path) {
		return `Cannot include a '${char}' character in a manually specified \`to.${field}\` field [${JSON.stringify(path)}].  Please separate it out to the \`to.${dest}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
	}
	function getPathContributingMatches(matches) {
		return matches.filter((match, index) => index === 0 || match.route.path && match.route.path.length > 0);
	}
	function getResolveToMatches(matches) {
		let pathMatches = getPathContributingMatches(matches);
		return pathMatches.map((match, idx) => idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase);
	}
	function resolveTo(toArg, routePathnames, locationPathname, isPathRelative = false) {
		let to;
		if (typeof toArg === "string") to = parsePath(toArg);
		else {
			to = { ...toArg };
			invariant(!to.pathname || !to.pathname.includes("?"), getInvalidPathError("?", "pathname", "search", to));
			invariant(!to.pathname || !to.pathname.includes("#"), getInvalidPathError("#", "pathname", "hash", to));
			invariant(!to.search || !to.search.includes("#"), getInvalidPathError("#", "search", "hash", to));
		}
		let isEmptyPath = toArg === "" || to.pathname === "";
		let toPathname = isEmptyPath ? "/" : to.pathname;
		let from;
		if (toPathname == null) from = locationPathname;
		else {
			let routePathnameIndex = routePathnames.length - 1;
			if (!isPathRelative && toPathname.startsWith("..")) {
				let toSegments = toPathname.split("/");
				while (toSegments[0] === "..") {
					toSegments.shift();
					routePathnameIndex -= 1;
				}
				to.pathname = toSegments.join("/");
			}
			from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
		}
		let path = resolvePath(to, from);
		let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
		let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
		if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) path.pathname += "/";
		return path;
	}
	function removeTrailingSlash(path, minLength = 0) {
		let end = path.length;
		while (end > minLength && path.charCodeAt(end - 1) === 47) end--;
		return end === path.length ? path : path.slice(0, end);
	}
	function isRouteErrorResponse(error) {
		return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
	}
	function getRoutePattern(matches) {
		return joinPaths(matches.map((m) => m.route.path).filter(Boolean)) || "/";
	}
	function parseToInfo(_to, basename) {
		let to = _to;
		if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) return {
			absoluteURL: void 0,
			isExternal: false,
			to
		};
		let absoluteURL = to;
		let isExternal = false;
		if (isBrowser) try {
			let currentUrl = new URL(window.location.href);
			let targetUrl = PROTOCOL_RELATIVE_URL_REGEX.test(to) ? new URL(normalizeProtocolRelativeUrl(to, currentUrl.protocol)) : new URL(to);
			let path = stripBasename(targetUrl.pathname, basename);
			if (targetUrl.origin === currentUrl.origin && path != null) to = path + targetUrl.search + targetUrl.hash;
			else isExternal = true;
		} catch (e) {
			warning(false, `<Link to="${to}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`);
		}
		return {
			absoluteURL,
			isExternal,
			to
		};
	}
	function getNavigatorCurrentUrl(navigator) {
		if (navigator.createURL) return navigator.createURL("/");
		try {
			return new URL(navigator.createHref("/"), DEFAULT_NAVIGATION_URL);
		} catch {
			return DEFAULT_NAVIGATION_URL;
		}
	}
	function isSameOrigin(a, b) {
		return a.origin === b.origin && (a.origin !== "null" || a.protocol === b.protocol && a.host === b.host);
	}
	function isExplicitUrl(destination, target) {
		if (destination.startsWith("//")) return true;
		let protocol = target.protocol.toLowerCase();
		if (!destination.toLowerCase().startsWith(protocol)) return false;
		return target.host === "" || destination.slice(protocol.length).startsWith("//");
	}
	function validateNavigationTarget(original, resolved, currentUrl, externalPolicy) {
		let originalUrl = null;
		try {
			originalUrl = original == null ? null : new URL(original, currentUrl);
		} catch {}
		let resolvedUrl = new URL(resolved, currentUrl);
		let originalIsExternal = originalUrl != null && !isSameOrigin(originalUrl, currentUrl);
		let resolvedIsExternal = !isSameOrigin(resolvedUrl, currentUrl);
		if (externalPolicy === "reject") {
			if (originalIsExternal || resolvedIsExternal) throw new Error("External navigation is not allowed");
		} else if (resolvedIsExternal) {
			if (originalUrl == null || !isExplicitUrl(original, originalUrl) || !isSameOrigin(originalUrl, resolvedUrl)) throw new Error("External navigation is not allowed");
		}
	}
	function hasInvalidProtocol(location) {
		try {
			return invalidProtocols.includes(new URL(location).protocol);
		} catch {
			return false;
		}
	}
	function useIsRSCRouterContext() {
		return import_react$24.useContext(RSCRouterContext);
	}
	function decodeRedirectErrorDigest(digest) {
		if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_REDIRECT}:{`)) try {
			let parsed = JSON.parse(digest.slice(28));
			if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") return parsed;
		} catch {}
	}
	function decodeRouteErrorResponseDigest(digest) {
		if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_ROUTE_ERROR_RESPONSE}:{`)) try {
			let parsed = JSON.parse(digest.slice(40));
			if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") return new ErrorResponseImpl(parsed.status, parsed.statusText, parsed.data);
		} catch {}
	}
	function useHref(to, { relative } = {}) {
		invariant(useInRouterContext(), `useHref() may be used only in the context of a <Router> component.`);
		let { basename, navigator } = import_react$25.useContext(NavigationContext);
		let { hash, pathname, search } = useResolvedPath(to, { relative });
		let joinedPathname = pathname;
		if (basename !== "/") joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
		return navigator.createHref({
			pathname: joinedPathname,
			search,
			hash
		});
	}
	function useInRouterContext() {
		return import_react$25.useContext(LocationContext) != null;
	}
	function useLocation() {
		invariant(useInRouterContext(), `useLocation() may be used only in the context of a <Router> component.`);
		return import_react$25.useContext(LocationContext).location;
	}
	function useIsomorphicLayoutEffect(cb) {
		if (!import_react$25.useContext(NavigationContext).static) import_react$25.useLayoutEffect(cb);
	}
	function useNavigate() {
		let { isDataRoute } = import_react$25.useContext(RouteContext);
		return isDataRoute ? useNavigateStable() : useNavigateUnstable();
	}
	function useNavigateUnstable() {
		invariant(useInRouterContext(), `useNavigate() may be used only in the context of a <Router> component.`);
		let dataRouterContext = import_react$25.useContext(DataRouterContext);
		let { basename, navigator } = import_react$25.useContext(NavigationContext);
		let { matches } = import_react$25.useContext(RouteContext);
		let { pathname: locationPathname } = useLocation();
		let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
		let activeRef = import_react$25.useRef(false);
		useIsomorphicLayoutEffect(() => {
			activeRef.current = true;
		});
		return import_react$25.useCallback((to, options = {}) => {
			warning(activeRef.current, navigateEffectWarning);
			if (!activeRef.current) return;
			if (typeof to === "number") {
				navigator.go(to);
				return;
			}
			let path = resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, options.relative === "path");
			if (dataRouterContext == null && basename !== "/") path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
			validateNavigationTarget(typeof to === "string" ? to : createPath(to), navigator.createHref(path), getNavigatorCurrentUrl(navigator), "reject");
			(!!options.replace ? navigator.replace : navigator.push)(path, options.state, options);
		}, [
			basename,
			navigator,
			routePathnamesJson,
			locationPathname,
			dataRouterContext
		]);
	}
	function useParams() {
		let { matches } = import_react$25.useContext(RouteContext);
		return matches[matches.length - 1]?.params ?? {};
	}
	function useResolvedPath(to, { relative } = {}) {
		let { matches } = import_react$25.useContext(RouteContext);
		let { pathname: locationPathname } = useLocation();
		let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
		return import_react$25.useMemo(() => resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, relative === "path"), [
			to,
			routePathnamesJson,
			locationPathname,
			relative
		]);
	}
	function useRoutes(routes, locationArg) {
		return useRoutesImpl(routes, locationArg);
	}
	function useRoutesImpl(routes, locationArg, dataRouterOpts) {
		invariant(useInRouterContext(), `useRoutes() may be used only in the context of a <Router> component.`);
		let { navigator } = import_react$25.useContext(NavigationContext);
		let { matches: parentMatches } = import_react$25.useContext(RouteContext);
		let routeMatch = parentMatches[parentMatches.length - 1];
		let parentParams = routeMatch ? routeMatch.params : {};
		let parentPathname = routeMatch ? routeMatch.pathname : "/";
		let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
		let parentRoute = routeMatch && routeMatch.route;
		{
			let parentPath = parentRoute && parentRoute.path || "";
			warningOnce(parentPathname, !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"), `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${parentPathname}" (under <Route path="${parentPath}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${parentPath}"> to <Route path="${parentPath === "/" ? "*" : `${parentPath}/*`}">.`);
		}
		let locationFromContext = useLocation();
		let location;
		if (locationArg) {
			let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
			invariant(parentPathnameBase === "/" || parsedLocationArg.pathname?.startsWith(parentPathnameBase), `When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${parentPathnameBase}" but pathname "${parsedLocationArg.pathname}" was given in the \`location\` prop.`);
			location = parsedLocationArg;
		} else location = locationFromContext;
		let pathname = location.pathname || "/";
		let remainingPathname = pathname;
		if (parentPathnameBase !== "/") {
			let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
			remainingPathname = "/" + pathname.replace(/^\//, "").split("/").slice(parentSegments.length).join("/");
		}
		let matches = dataRouterOpts && dataRouterOpts.state.matches.length ? dataRouterOpts.state.matches.map((m) => Object.assign(m, { route: dataRouterOpts.manifest[m.route.id] || m.route })) : matchRoutes(routes, { pathname: remainingPathname });
		warning(parentRoute || matches != null, `No routes matched location "${location.pathname}${location.search}${location.hash}" `);
		warning(matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0, `Matched leaf route at location "${location.pathname}${location.search}${location.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`);
		let renderedMatches = _renderMatches(matches && matches.map((match) => Object.assign({}, match, {
			params: Object.assign({}, parentParams, match.params),
			pathname: joinPaths([parentPathnameBase, navigator.encodeLocation ? navigator.encodeLocation(match.pathname.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")).pathname : match.pathname]),
			pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([parentPathnameBase, navigator.encodeLocation ? navigator.encodeLocation(match.pathnameBase.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")).pathname : match.pathnameBase])
		})), parentMatches, dataRouterOpts);
		if (locationArg && renderedMatches) return /* @__PURE__ */ import_react$25.createElement(LocationContext.Provider, { value: {
			location: {
				pathname: "/",
				search: "",
				hash: "",
				state: null,
				key: "default",
				mask: void 0,
				...location
			},
			navigationType: "POP"
		} }, renderedMatches);
		return renderedMatches;
	}
	function DefaultErrorComponent() {
		let error = useRouteError();
		let message = isRouteErrorResponse(error) ? `${error.status} ${error.statusText}` : error instanceof Error ? error.message : JSON.stringify(error);
		let stack = error instanceof Error ? error.stack : null;
		let lightgrey = "rgba(200,200,200, 0.5)";
		let preStyles = {
			padding: "0.5rem",
			backgroundColor: lightgrey
		};
		let codeStyles = {
			padding: "2px 4px",
			backgroundColor: lightgrey
		};
		let devInfo = null;
		console.error("Error handled by React Router default ErrorBoundary:", error);
		devInfo = /* @__PURE__ */ import_react$25.createElement(import_react$25.Fragment, null, /* @__PURE__ */ import_react$25.createElement("p", null, "💿 Hey developer 👋"), /* @__PURE__ */ import_react$25.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ import_react$25.createElement("code", { style: codeStyles }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ import_react$25.createElement("code", { style: codeStyles }, "errorElement"), " prop on your route."));
		return /* @__PURE__ */ import_react$25.createElement(import_react$25.Fragment, null, /* @__PURE__ */ import_react$25.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ import_react$25.createElement("h3", { style: { fontStyle: "italic" } }, message), stack ? /* @__PURE__ */ import_react$25.createElement("pre", { style: preStyles }, stack) : null, devInfo);
	}
	function RSCErrorHandler({ children, error }) {
		let { basename, navigator } = import_react$25.useContext(NavigationContext);
		if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
			let redirect2 = decodeRedirectErrorDigest(error.digest);
			if (redirect2) {
				let existingRedirect = errorRedirectHandledMap.get(error);
				if (existingRedirect) throw existingRedirect;
				let parsed = parseToInfo(redirect2.location, basename);
				let target = parsed.absoluteURL || parsed.to;
				validateNavigationTarget(redirect2.location, target, getNavigatorCurrentUrl(navigator), "allow-explicit");
				if (hasInvalidProtocol(target)) throw new Error("Invalid redirect location");
				if (isBrowser && !errorRedirectHandledMap.get(error)) {
					if (parsed.isExternal || redirect2.reloadDocument) window.location.href = target;
					else {
						const redirectPromise = Promise.resolve().then(() => window.__reactRouterDataRouter.navigate(parsed.to, { replace: redirect2.replace }));
						errorRedirectHandledMap.set(error, redirectPromise);
						throw redirectPromise;
					}
				}
				return /* @__PURE__ */ import_react$25.createElement("meta", {
					httpEquiv: "refresh",
					content: `0;url=${target}`
				});
			}
		}
		return children;
	}
	function RenderedRoute({ routeContext, match, children }) {
		let dataRouterContext = import_react$25.useContext(DataRouterContext);
		if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
		return /* @__PURE__ */ import_react$25.createElement(RouteContext.Provider, { value: routeContext }, children);
	}
	function _renderMatches(matches, parentMatches = [], dataRouterOpts) {
		let dataRouterState = dataRouterOpts?.state;
		if (matches == null) {
			if (!dataRouterState) return null;
			if (dataRouterState.errors) matches = dataRouterState.matches;
			else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) matches = dataRouterState.matches;
			else return null;
		}
		let renderedMatches = matches;
		let errors = dataRouterState?.errors;
		if (errors != null) {
			let errorIndex = renderedMatches.findIndex((m) => m.route.id && errors?.[m.route.id] !== void 0);
			invariant(errorIndex >= 0, `Could not find a matching route for errors on route IDs: ${Object.keys(errors).join(",")}`);
			renderedMatches = renderedMatches.slice(0, Math.min(renderedMatches.length, errorIndex + 1));
		}
		let renderFallback = false;
		let fallbackIndex = -1;
		if (dataRouterOpts && dataRouterState) {
			renderFallback = dataRouterState.renderFallback;
			for (let i = 0; i < renderedMatches.length; i++) {
				let match = renderedMatches[i];
				if (match.route.HydrateFallback || match.route.hydrateFallbackElement) fallbackIndex = i;
				if (match.route.id) {
					let { loaderData, errors: errors2 } = dataRouterState;
					let needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
					if (match.route.lazy || needsToRunLoader) {
						if (dataRouterOpts.isStatic) renderFallback = true;
						if (fallbackIndex >= 0) renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
						else renderedMatches = [renderedMatches[0]];
						break;
					}
				}
			}
		}
		let onErrorHandler = dataRouterOpts?.onError;
		let onError = dataRouterState && onErrorHandler ? (error, errorInfo) => {
			onErrorHandler(error, {
				location: dataRouterState.location,
				params: dataRouterState.matches?.[0]?.params ?? {},
				pattern: getRoutePattern(dataRouterState.matches),
				errorInfo
			});
		} : void 0;
		return renderedMatches.reduceRight((outlet, match, index) => {
			let error;
			let shouldRenderHydrateFallback = false;
			let errorElement = null;
			let hydrateFallbackElement = null;
			if (dataRouterState) {
				error = errors && match.route.id ? errors[match.route.id] : void 0;
				errorElement = match.route.errorElement || defaultErrorElement;
				if (renderFallback) {
					if (fallbackIndex < 0 && index === 0) {
						warningOnce("route-fallback", false, "No `HydrateFallback` element provided to render during initial hydration");
						shouldRenderHydrateFallback = true;
						hydrateFallbackElement = null;
					} else if (fallbackIndex === index) {
						shouldRenderHydrateFallback = true;
						hydrateFallbackElement = match.route.hydrateFallbackElement || null;
					}
				}
			}
			let matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
			let getChildren = () => {
				let children;
				if (error) children = errorElement;
				else if (shouldRenderHydrateFallback) children = hydrateFallbackElement;
				else if (match.route.Component) children = /* @__PURE__ */ import_react$25.createElement(match.route.Component, null);
				else if (match.route.element) children = match.route.element;
				else children = outlet;
				return /* @__PURE__ */ import_react$25.createElement(RenderedRoute, {
					match,
					routeContext: {
						outlet,
						matches: matches2,
						isDataRoute: dataRouterState != null
					},
					children
				});
			};
			return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ import_react$25.createElement(RenderErrorBoundary, {
				location: dataRouterState.location,
				revalidation: dataRouterState.revalidation,
				component: errorElement,
				error,
				children: getChildren(),
				routeContext: {
					outlet: null,
					matches: matches2,
					isDataRoute: true
				},
				onError
			}) : getChildren();
		}, null);
	}
	function getDataRouterConsoleError(hookName) {
		return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
	}
	function useDataRouterContext(hookName) {
		let ctx = import_react$25.useContext(DataRouterContext);
		invariant(ctx, getDataRouterConsoleError(hookName));
		return ctx;
	}
	function useDataRouterState(hookName) {
		let state = import_react$25.useContext(DataRouterStateContext);
		invariant(state, getDataRouterConsoleError(hookName));
		return state;
	}
	function useRouteContext(hookName) {
		let route = import_react$25.useContext(RouteContext);
		invariant(route, getDataRouterConsoleError(hookName));
		return route;
	}
	function useCurrentRouteId(hookName) {
		let route = useRouteContext(hookName);
		let thisRoute = route.matches[route.matches.length - 1];
		invariant(thisRoute.route.id, `${hookName} can only be used on routes that contain a unique "id"`);
		return thisRoute.route.id;
	}
	function useRouteId() {
		return useCurrentRouteId("useRouteId");
	}
	function useNavigation() {
		let state = useDataRouterState("useNavigation");
		return import_react$25.useMemo(() => {
			let { matches, historyAction, ...rest } = state.navigation;
			return rest;
		}, [state.navigation]);
	}
	function useMatches() {
		let { matches, loaderData } = useDataRouterState("useMatches");
		return import_react$25.useMemo(() => matches.map((m) => convertRouteMatchToUiMatch(m, loaderData)), [matches, loaderData]);
	}
	function useRouteError() {
		let error = import_react$25.useContext(RouteErrorContext);
		let state = useDataRouterState("useRouteError");
		let routeId = useCurrentRouteId("useRouteError");
		if (error !== void 0) return error;
		return state.errors?.[routeId];
	}
	function useNavigateStable() {
		let { router } = useDataRouterContext("useNavigate");
		let id = useCurrentRouteId("useNavigate");
		let activeRef = import_react$25.useRef(false);
		useIsomorphicLayoutEffect(() => {
			activeRef.current = true;
		});
		return import_react$25.useCallback(async (to, options = {}) => {
			warning(activeRef.current, navigateEffectWarning);
			if (!activeRef.current) return;
			if (typeof to === "number") await router.navigate(to);
			else await router.navigate(to, {
				fromRouteId: id,
				...options
			});
		}, [router, id]);
	}
	function warningOnce(key, cond, message) {
		if (!cond && !alreadyWarned[key]) {
			alreadyWarned[key] = true;
			warning(false, message);
		}
	}
	function DataRoutes2({ routes, manifest, future, state, isStatic, onError }) {
		return useRoutesImpl(routes, void 0, {
			manifest,
			state,
			isStatic,
			onError,
			future
		});
	}
	function MemoryRouter({ basename, children, initialEntries, initialIndex, useTransitions }) {
		let historyRef = import_react$26.useRef();
		if (historyRef.current == null) historyRef.current = createMemoryHistory({
			initialEntries,
			initialIndex,
			v5Compat: true
		});
		let history = historyRef.current;
		let [state, setStateImpl] = import_react$26.useState({
			action: history.action,
			location: history.location
		});
		let setState = import_react$26.useCallback((newState) => {
			if (useTransitions === false) setStateImpl(newState);
			else import_react$26.startTransition(() => setStateImpl(newState));
		}, [useTransitions]);
		import_react$26.useLayoutEffect(() => history.listen(setState), [history, setState]);
		return /* @__PURE__ */ import_react$26.createElement(Router, {
			basename,
			children,
			location: state.location,
			navigationType: state.action,
			navigator: history,
			useTransitions
		});
	}
	function Navigate({ to, replace: replace2, state, relative }) {
		invariant(useInRouterContext(), `<Navigate> may be used only in the context of a <Router> component.`);
		let { static: isStatic, navigator } = import_react$26.useContext(NavigationContext);
		warning(!isStatic, `<Navigate> must not be used on the initial render in a <StaticRouter>. This is a no-op, but you should modify your code so the <Navigate> is only ever rendered in response to some user interaction or state change.`);
		let { matches } = import_react$26.useContext(RouteContext);
		let { pathname: locationPathname } = useLocation();
		let navigate = useNavigate();
		let path = resolveTo(to, getResolveToMatches(matches), locationPathname, relative === "path");
		validateNavigationTarget(typeof to === "string" ? to : createPath(to), navigator.createHref(path), getNavigatorCurrentUrl(navigator), "reject");
		let jsonPath = JSON.stringify(path);
		import_react$26.useEffect(() => {
			navigate(JSON.parse(jsonPath), {
				replace: replace2,
				state,
				relative
			});
		}, [
			navigate,
			jsonPath,
			relative,
			replace2,
			state
		]);
		return null;
	}
	function Route(props) {
		invariant(false, `A <Route> is only ever to be used as the child of <Routes> element, never rendered directly. Please wrap your <Route> in a <Routes>.`);
	}
	function Router({ basename: basenameProp = "/", children = null, location: locationProp, navigationType = "POP", navigator, static: staticProp = false, useTransitions }) {
		invariant(!useInRouterContext(), `You cannot render a <Router> inside another <Router>. You should never have more than one in your app.`);
		let basename = basenameProp.replace(/^\/*/, "/");
		let navigationContext = import_react$26.useMemo(() => ({
			basename,
			navigator,
			static: staticProp,
			useTransitions,
			future: {}
		}), [
			basename,
			navigator,
			staticProp,
			useTransitions
		]);
		if (typeof locationProp === "string") locationProp = parsePath(locationProp);
		let { pathname = "/", search = "", hash = "", state = null, key = "default", mask } = locationProp;
		let locationContext = import_react$26.useMemo(() => {
			let trailingPathname = stripBasename(pathname, basename);
			if (trailingPathname == null) return null;
			return {
				location: {
					pathname: trailingPathname,
					search,
					hash,
					state,
					key,
					mask
				},
				navigationType
			};
		}, [
			basename,
			pathname,
			search,
			hash,
			state,
			key,
			navigationType,
			mask
		]);
		warning(locationContext != null, `<Router basename="${basename}"> is not able to match the URL "${pathname}${search}${hash}" because it does not start with the basename, so the <Router> won't render anything.`);
		if (locationContext == null) return null;
		return /* @__PURE__ */ import_react$26.createElement(NavigationContext.Provider, { value: navigationContext }, /* @__PURE__ */ import_react$26.createElement(LocationContext.Provider, {
			children,
			value: locationContext
		}));
	}
	function Routes({ children, location }) {
		return useRoutes(createRoutesFromChildren(children), location);
	}
	function createRoutesFromChildren(children, parentPath = []) {
		let routes = [];
		import_react$26.Children.forEach(children, (element, index) => {
			if (!import_react$26.isValidElement(element)) return;
			let treePath = [...parentPath, index];
			if (element.type === import_react$26.Fragment) {
				routes.push.apply(routes, createRoutesFromChildren(element.props.children, treePath));
				return;
			}
			invariant(element.type === Route, `[${typeof element.type === "string" ? element.type : element.type.name}] is not a <Route> component. All component children of <Routes> must be a <Route> or <React.Fragment>`);
			invariant(!element.props.index || !element.props.children, "An index route cannot have child routes.");
			let route = {
				id: element.props.id || treePath.join("-"),
				caseSensitive: element.props.caseSensitive,
				element: element.props.element,
				Component: element.props.Component,
				index: element.props.index,
				path: element.props.path,
				middleware: element.props.middleware,
				loader: element.props.loader,
				action: element.props.action,
				hydrateFallbackElement: element.props.hydrateFallbackElement,
				HydrateFallback: element.props.HydrateFallback,
				errorElement: element.props.errorElement,
				ErrorBoundary: element.props.ErrorBoundary,
				hasErrorBoundary: element.props.hasErrorBoundary === true || element.props.ErrorBoundary != null || element.props.errorElement != null,
				shouldRevalidate: element.props.shouldRevalidate,
				handle: element.props.handle,
				lazy: element.props.lazy
			};
			if (element.props.children) route.children = createRoutesFromChildren(element.props.children, treePath);
			routes.push(route);
		});
		return routes;
	}
	function isHtmlElement(object) {
		return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
	}
	function isButtonElement(object) {
		return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
	}
	function isFormElement(object) {
		return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
	}
	function isInputElement(object) {
		return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
	}
	function isModifiedEvent(event) {
		return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
	}
	function shouldProcessLinkClick(event, target) {
		return event.button === 0 && (!target || target === "_self") && !isModifiedEvent(event);
	}
	function createSearchParams(init = "") {
		return new URLSearchParams(typeof init === "string" || Array.isArray(init) || init instanceof URLSearchParams ? init : Object.keys(init).reduce((memo2, key) => {
			let value = init[key];
			return memo2.concat(Array.isArray(value) ? value.map((v) => [key, v]) : [[key, value]]);
		}, []));
	}
	function getSearchParamsForLocation(locationSearch, defaultSearchParams) {
		let searchParams = createSearchParams(locationSearch);
		if (defaultSearchParams) defaultSearchParams.forEach((_, key) => {
			if (!searchParams.has(key)) defaultSearchParams.getAll(key).forEach((value) => {
				searchParams.append(key, value);
			});
		});
		return searchParams;
	}
	function isFormDataSubmitterSupported() {
		if (_formDataSupportsSubmitter === null) try {
			new FormData(document.createElement("form"), 0);
			_formDataSupportsSubmitter = false;
		} catch (e) {
			_formDataSupportsSubmitter = true;
		}
		return _formDataSupportsSubmitter;
	}
	function getFormEncType(encType) {
		if (encType != null && !supportedFormEncTypes.has(encType)) {
			warning(false, `"${encType}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${defaultEncType}"`);
			return null;
		}
		return encType;
	}
	function getFormSubmissionInfo(target, basename) {
		let method;
		let action;
		let encType;
		let formData;
		let body;
		if (isFormElement(target)) {
			let attr = target.getAttribute("action");
			action = attr ? stripBasename(attr, basename) : null;
			method = target.getAttribute("method") || defaultMethod;
			encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
			formData = new FormData(target);
		} else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
			let form = target.form;
			if (form == null) throw new Error(`Cannot submit a <button> or <input type="submit"> without a <form>`);
			let attr = target.getAttribute("formaction") || form.getAttribute("action");
			action = attr ? stripBasename(attr, basename) : null;
			method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
			encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
			formData = new FormData(form, target);
			if (!isFormDataSubmitterSupported()) {
				let { name, type, value } = target;
				if (type === "image") {
					let prefix = name ? `${name}.` : "";
					formData.append(`${prefix}x`, "0");
					formData.append(`${prefix}y`, "0");
				} else if (name) formData.append(name, value);
			}
		} else if (isHtmlElement(target)) throw new Error(`Cannot submit element that is not <form>, <button>, or <input type="submit|image">`);
		else {
			method = defaultMethod;
			action = null;
			encType = defaultEncType;
			body = target;
		}
		if (formData && encType === "text/plain") {
			body = formData;
			formData = void 0;
		}
		return {
			action,
			method: method.toLowerCase(),
			encType,
			formData,
			body
		};
	}
	function escapeHtml(html) {
		return html.replace(ESCAPE_REGEX, (match) => ESCAPE_LOOKUP[match]);
	}
	function invariant2(value, message) {
		if (value === false || value === null || typeof value === "undefined") throw new Error(message);
	}
	function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
		let url = typeof reqUrl === "string" ? new URL(reqUrl, typeof window === "undefined" ? "server://singlefetch/" : window.location.origin) : reqUrl;
		if (trailingSlashAware) {
			if (url.pathname.endsWith("/")) url.pathname = `${url.pathname}_.${extension}`;
			else url.pathname = `${url.pathname}.${extension}`;
		} else if (url.pathname === "/") url.pathname = `_root.${extension}`;
		else if (basename && stripBasename(url.pathname, basename) === "/") url.pathname = `${removeTrailingSlash(basename)}/_root.${extension}`;
		else url.pathname = `${removeTrailingSlash(url.pathname)}.${extension}`;
		return url;
	}
	async function loadRouteModule(route, routeModulesCache) {
		if (route.id in routeModulesCache) return routeModulesCache[route.id];
		try {
			let routeModule = await import(
				/* @vite-ignore */
				/* webpackIgnore: true */
				route.module
);
			routeModulesCache[route.id] = routeModule;
			return routeModule;
		} catch (error) {
			console.error(`Error loading route module \`${route.module}\`, reloading page...`);
			console.error(error);
			if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && void 0);
			window.location.reload();
			return new Promise(() => {});
		}
	}
	function isPageLinkDescriptor(object) {
		return object != null && typeof object.page === "string";
	}
	function isHtmlLinkDescriptor(object) {
		if (object == null) return false;
		if (object.href == null) return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
		return typeof object.rel === "string" && typeof object.href === "string";
	}
	async function getKeyedPrefetchLinks(matches, manifest, routeModules) {
		return dedupeLinkDescriptors((await Promise.all(matches.map(async (match) => {
			let route = manifest.routes[match.route.id];
			if (route) {
				let mod = await loadRouteModule(route, routeModules);
				return mod.links ? mod.links() : [];
			}
			return [];
		}))).flat(1).filter(isHtmlLinkDescriptor).filter((link) => link.rel === "stylesheet" || link.rel === "preload").map((link) => link.rel === "stylesheet" ? {
			...link,
			rel: "prefetch",
			as: "style"
		} : {
			...link,
			rel: "prefetch"
		}));
	}
	function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
		let isNew = (match, index) => {
			if (!currentMatches[index]) return true;
			return match.route.id !== currentMatches[index].route.id;
		};
		let matchPathChanged = (match, index) => {
			return currentMatches[index].pathname !== match.pathname || currentMatches[index].route.path?.endsWith("*") && currentMatches[index].params["*"] !== match.params["*"];
		};
		if (mode === "assets") return nextMatches.filter((match, index) => isNew(match, index) || matchPathChanged(match, index));
		if (mode === "data") return nextMatches.filter((match, index) => {
			let manifestRoute = manifest.routes[match.route.id];
			if (!manifestRoute || !manifestRoute.hasLoader) return false;
			if (isNew(match, index) || matchPathChanged(match, index)) return true;
			if (match.route.shouldRevalidate) {
				let routeChoice = match.route.shouldRevalidate({
					currentUrl: new URL(location.pathname + location.search + location.hash, window.origin),
					currentParams: currentMatches[0]?.params || {},
					nextUrl: new URL(page, window.origin),
					nextParams: match.params,
					defaultShouldRevalidate: true
				});
				if (typeof routeChoice === "boolean") return routeChoice;
			}
			return true;
		});
		return [];
	}
	function getModuleLinkHrefs(matches, manifest, { includeHydrateFallback } = {}) {
		return dedupeHrefs(matches.map((match) => {
			let route = manifest.routes[match.route.id];
			if (!route) return [];
			let hrefs = [route.module];
			if (route.clientActionModule) hrefs = hrefs.concat(route.clientActionModule);
			if (route.clientLoaderModule) hrefs = hrefs.concat(route.clientLoaderModule);
			if (includeHydrateFallback && route.hydrateFallbackModule) hrefs = hrefs.concat(route.hydrateFallbackModule);
			if (route.imports) hrefs = hrefs.concat(route.imports);
			return hrefs;
		}).flat(1));
	}
	function dedupeHrefs(hrefs) {
		return [...new Set(hrefs)];
	}
	function sortKeys(obj) {
		let sorted = {};
		let keys = Object.keys(obj).sort();
		for (let key of keys) sorted[key] = obj[key];
		return sorted;
	}
	function dedupeLinkDescriptors(descriptors, preloads) {
		let set = /* @__PURE__ */ new Set();
		let preloadsSet = new Set(preloads);
		return descriptors.reduce((deduped, descriptor) => {
			if (preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href)) return deduped;
			let key = JSON.stringify(sortKeys(descriptor));
			if (!set.has(key)) {
				set.add(key);
				deduped.push({
					key,
					link: descriptor
				});
			}
			return deduped;
		}, []);
	}
	function useDataRouterContext2() {
		let context = import_react$29.useContext(DataRouterContext);
		invariant2(context, "You must render this element inside a <DataRouterContext.Provider> element");
		return context;
	}
	function useDataRouterStateContext() {
		let context = import_react$29.useContext(DataRouterStateContext);
		invariant2(context, "You must render this element inside a <DataRouterStateContext.Provider> element");
		return context;
	}
	function useFrameworkContext() {
		let context = import_react$29.useContext(FrameworkContext);
		invariant2(context, "You must render this element inside a <HydratedRouter> element");
		return context;
	}
	function usePrefetchBehavior(prefetch, theirElementProps) {
		let frameworkContext = import_react$29.useContext(FrameworkContext);
		let [maybePrefetch, setMaybePrefetch] = import_react$29.useState(false);
		let [shouldPrefetch, setShouldPrefetch] = import_react$29.useState(false);
		let { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
		let ref = import_react$29.useRef(null);
		import_react$29.useEffect(() => {
			if (prefetch === "render") setShouldPrefetch(true);
			if (prefetch === "viewport") {
				let callback = (entries) => {
					entries.forEach((entry) => {
						setShouldPrefetch(entry.isIntersecting);
					});
				};
				let observer = new IntersectionObserver(callback, { threshold: .5 });
				if (ref.current) observer.observe(ref.current);
				return () => {
					observer.disconnect();
				};
			}
		}, [prefetch]);
		import_react$29.useEffect(() => {
			if (maybePrefetch) {
				let id = setTimeout(() => {
					setShouldPrefetch(true);
				}, 100);
				return () => {
					clearTimeout(id);
				};
			}
		}, [maybePrefetch]);
		let setIntent = () => {
			setMaybePrefetch(true);
		};
		let cancelIntent = () => {
			setMaybePrefetch(false);
			setShouldPrefetch(false);
		};
		if (!frameworkContext) return [
			false,
			ref,
			{}
		];
		if (prefetch !== "intent") return [
			shouldPrefetch,
			ref,
			{}
		];
		return [
			shouldPrefetch,
			ref,
			{
				onFocus: composeEventHandlers(onFocus, setIntent),
				onBlur: composeEventHandlers(onBlur, cancelIntent),
				onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
				onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
				onTouchStart: composeEventHandlers(onTouchStart, setIntent)
			}
		];
	}
	function composeEventHandlers(theirHandler, ourHandler) {
		return (event) => {
			theirHandler && theirHandler(event);
			if (!event.defaultPrevented) ourHandler(event);
		};
	}
	function PrefetchPageLinks({ page, ...linkProps }) {
		let rsc = useIsRSCRouterContext();
		let { nonce: contextNonce } = useFrameworkContext();
		let { router } = useDataRouterContext2();
		let matches = import_react$29.useMemo(() => matchRoutes(router.routes, page, router.basename), [
			router.routes,
			page,
			router.basename
		]);
		if (!matches) return null;
		if (linkProps.nonce == null && contextNonce) linkProps = {
			...linkProps,
			nonce: contextNonce
		};
		if (rsc) return /* @__PURE__ */ import_react$29.createElement(RSCPrefetchPageLinksImpl, {
			page,
			matches,
			...linkProps
		});
		return /* @__PURE__ */ import_react$29.createElement(PrefetchPageLinksImpl, {
			page,
			matches,
			...linkProps
		});
	}
	function useKeyedPrefetchLinks(matches) {
		let { manifest, routeModules } = useFrameworkContext();
		let [keyedPrefetchLinks, setKeyedPrefetchLinks] = import_react$29.useState([]);
		import_react$29.useEffect(() => {
			let interrupted = false;
			getKeyedPrefetchLinks(matches, manifest, routeModules).then((links) => {
				if (!interrupted) setKeyedPrefetchLinks(links);
			});
			return () => {
				interrupted = true;
			};
		}, [
			matches,
			manifest,
			routeModules
		]);
		return keyedPrefetchLinks;
	}
	function RSCPrefetchPageLinksImpl({ page, matches: nextMatches, ...linkProps }) {
		let location = useLocation();
		let { future } = useFrameworkContext();
		let { basename } = useDataRouterContext2();
		let dataHrefs = import_react$29.useMemo(() => {
			if (page === location.pathname + location.search + location.hash) return [];
			let url = singleFetchUrl(page, basename, future.v8_trailingSlashAwareDataRequests, "rsc");
			let hasSomeRoutesWithShouldRevalidate = false;
			let targetRoutes = [];
			for (let match of nextMatches) if (typeof match.route.shouldRevalidate === "function") hasSomeRoutesWithShouldRevalidate = true;
			else targetRoutes.push(match.route.id);
			if (hasSomeRoutesWithShouldRevalidate && targetRoutes.length > 0) url.searchParams.set("_routes", targetRoutes.join(","));
			return [url.pathname + url.search];
		}, [
			basename,
			future.v8_trailingSlashAwareDataRequests,
			page,
			location,
			nextMatches
		]);
		return /* @__PURE__ */ import_react$29.createElement(import_react$29.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ import_react$29.createElement("link", {
			key: href,
			rel: "prefetch",
			as: "fetch",
			href,
			...linkProps
		})));
	}
	function PrefetchPageLinksImpl({ page, matches: nextMatches, ...linkProps }) {
		let location = useLocation();
		let { future, manifest, routeModules } = useFrameworkContext();
		let { basename } = useDataRouterContext2();
		let { loaderData, matches } = useDataRouterStateContext();
		let newMatchesForData = import_react$29.useMemo(() => getNewMatchesForLinks(page, nextMatches, matches, manifest, location, "data"), [
			page,
			nextMatches,
			matches,
			manifest,
			location
		]);
		let newMatchesForAssets = import_react$29.useMemo(() => getNewMatchesForLinks(page, nextMatches, matches, manifest, location, "assets"), [
			page,
			nextMatches,
			matches,
			manifest,
			location
		]);
		let dataHrefs = import_react$29.useMemo(() => {
			if (page === location.pathname + location.search + location.hash) return [];
			let routesParams = /* @__PURE__ */ new Set();
			let foundOptOutRoute = false;
			nextMatches.forEach((m) => {
				let manifestRoute = manifest.routes[m.route.id];
				if (!manifestRoute || !manifestRoute.hasLoader) return;
				if (!newMatchesForData.some((m2) => m2.route.id === m.route.id) && m.route.id in loaderData && routeModules[m.route.id]?.shouldRevalidate) foundOptOutRoute = true;
				else if (manifestRoute.hasClientLoader) foundOptOutRoute = true;
				else routesParams.add(m.route.id);
			});
			if (routesParams.size === 0) return [];
			let url = singleFetchUrl(page, basename, future.v8_trailingSlashAwareDataRequests, "data");
			if (foundOptOutRoute && routesParams.size > 0) url.searchParams.set("_routes", nextMatches.filter((m) => routesParams.has(m.route.id)).map((m) => m.route.id).join(","));
			return [url.pathname + url.search];
		}, [
			basename,
			future.v8_trailingSlashAwareDataRequests,
			loaderData,
			location,
			manifest,
			newMatchesForData,
			nextMatches,
			page,
			routeModules
		]);
		let moduleHrefs = import_react$29.useMemo(() => getModuleLinkHrefs(newMatchesForAssets, manifest), [newMatchesForAssets, manifest]);
		let keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
		return /* @__PURE__ */ import_react$29.createElement(import_react$29.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ import_react$29.createElement("link", {
			key: href,
			rel: "prefetch",
			as: "fetch",
			href,
			...linkProps
		})), moduleHrefs.map((href) => /* @__PURE__ */ import_react$29.createElement("link", {
			key: href,
			rel: "modulepreload",
			href,
			...linkProps
		})), keyedPrefetchLinks.map(({ key, link }) => /* @__PURE__ */ import_react$29.createElement("link", {
			key,
			nonce: linkProps.nonce,
			...link,
			crossOrigin: link.crossOrigin ?? linkProps.crossOrigin
		})));
	}
	function mergeRefs(...refs) {
		return (value) => {
			refs.forEach((ref) => {
				if (typeof ref === "function") ref(value);
				else if (ref != null) ref.current = value;
			});
		};
	}
	function HistoryRouter({ basename, children, history, useTransitions }) {
		let [state, setStateImpl] = import_react$33.useState({
			action: history.action,
			location: history.location
		});
		let setState = import_react$33.useCallback((newState) => {
			if (useTransitions === false) setStateImpl(newState);
			else import_react$33.startTransition(() => setStateImpl(newState));
		}, [useTransitions]);
		import_react$33.useLayoutEffect(() => history.listen(setState), [history, setState]);
		return /* @__PURE__ */ import_react$33.createElement(Router, {
			basename,
			children,
			location: state.location,
			navigationType: state.action,
			navigator: history,
			useTransitions
		});
	}
	function ScrollRestoration({ getKey, storageKey, ...props }) {
		let remixContext = import_react$33.useContext(FrameworkContext);
		let { basename } = import_react$33.useContext(NavigationContext);
		let location = useLocation();
		let matches = useMatches();
		useScrollRestoration({
			getKey,
			storageKey
		});
		let ssrKey = import_react$33.useMemo(() => {
			if (!remixContext || !getKey) return null;
			let userKey = getScrollRestorationKey(location, matches, basename, getKey);
			return userKey !== location.key ? userKey : null;
		}, []);
		if (!remixContext || remixContext.isSpaMode) return null;
		let restoreScroll = ((storageKey2, restoreKey) => {
			if (!window.history.state || !window.history.state.key) {
				let key = Math.random().toString(32).slice(2);
				window.history.replaceState({ key }, "");
			}
			try {
				let storedY = JSON.parse(sessionStorage.getItem(storageKey2) || "{}")[restoreKey || window.history.state.key];
				if (typeof storedY === "number") window.scrollTo(0, storedY);
			} catch (error) {
				console.error(error);
				sessionStorage.removeItem(storageKey2);
			}
		}).toString();
		if (props.nonce == null && remixContext?.nonce) props.nonce = remixContext.nonce;
		return /* @__PURE__ */ import_react$33.createElement("script", {
			...props,
			suppressHydrationWarning: true,
			dangerouslySetInnerHTML: { __html: `(${restoreScroll})(${escapeHtml(JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY))}, ${escapeHtml(JSON.stringify(ssrKey))})` }
		});
	}
	function getDataRouterConsoleError2(hookName) {
		return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
	}
	function useDataRouterContext3(hookName) {
		let ctx = import_react$33.useContext(DataRouterContext);
		invariant(ctx, getDataRouterConsoleError2(hookName));
		return ctx;
	}
	function useDataRouterState2(hookName) {
		let state = import_react$33.useContext(DataRouterStateContext);
		invariant(state, getDataRouterConsoleError2(hookName));
		return state;
	}
	function useLinkClickHandler(to, { target, replace: replaceProp, mask, state, preventScrollReset, relative, viewTransition, defaultShouldRevalidate, useTransitions } = {}) {
		let navigate = useNavigate();
		let location = useLocation();
		let path = useResolvedPath(to, { relative });
		return import_react$33.useCallback((event) => {
			if (shouldProcessLinkClick(event, target)) {
				event.preventDefault();
				let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
				let doNavigate = () => navigate(to, {
					replace: replace2,
					mask,
					state,
					preventScrollReset,
					relative,
					viewTransition,
					defaultShouldRevalidate
				});
				if (useTransitions) import_react$33.startTransition(() => doNavigate());
				else doNavigate();
			}
		}, [
			location,
			navigate,
			path,
			replaceProp,
			mask,
			state,
			target,
			to,
			preventScrollReset,
			relative,
			viewTransition,
			defaultShouldRevalidate,
			useTransitions
		]);
	}
	function useSearchParams(defaultInit) {
		warning(typeof URLSearchParams !== "undefined", `You cannot use the \`useSearchParams\` hook in a browser that does not support the URLSearchParams API. If you need to support Internet Explorer 11, we recommend you load a polyfill such as https://github.com/ungap/url-search-params.`);
		let defaultSearchParamsRef = import_react$33.useRef(createSearchParams(defaultInit));
		let hasSetSearchParamsRef = import_react$33.useRef(false);
		let location = useLocation();
		let searchParams = import_react$33.useMemo(() => getSearchParamsForLocation(location.search, hasSetSearchParamsRef.current ? null : defaultSearchParamsRef.current), [location.search]);
		let navigate = useNavigate();
		return [searchParams, import_react$33.useCallback((nextInit, navigateOptions) => {
			const newSearchParams = createSearchParams(typeof nextInit === "function" ? nextInit(new URLSearchParams(searchParams)) : nextInit);
			hasSetSearchParamsRef.current = true;
			navigate("?" + newSearchParams, navigateOptions);
		}, [navigate, searchParams])];
	}
	function useSubmit() {
		let { router } = useDataRouterContext3("useSubmit");
		let { basename } = import_react$33.useContext(NavigationContext);
		let currentRouteId = useRouteId();
		let routerFetch = router.fetch;
		let routerNavigate = router.navigate;
		return import_react$33.useCallback(async (target, options = {}) => {
			let { action, method, encType, formData, body } = getFormSubmissionInfo(target, basename);
			if (options.navigate === false) {
				let key = options.fetcherKey || getUniqueFetcherId();
				await routerFetch(key, currentRouteId, options.action || action, {
					defaultShouldRevalidate: options.defaultShouldRevalidate,
					preventScrollReset: options.preventScrollReset,
					formData,
					body,
					formMethod: options.method || method,
					formEncType: options.encType || encType,
					flushSync: options.flushSync
				});
			} else await routerNavigate(options.action || action, {
				defaultShouldRevalidate: options.defaultShouldRevalidate,
				preventScrollReset: options.preventScrollReset,
				formData,
				body,
				formMethod: options.method || method,
				formEncType: options.encType || encType,
				replace: options.replace,
				state: options.state,
				fromRouteId: currentRouteId,
				flushSync: options.flushSync,
				viewTransition: options.viewTransition
			});
		}, [
			routerFetch,
			routerNavigate,
			basename,
			currentRouteId
		]);
	}
	function useFormAction(action, { relative } = {}) {
		let { basename } = import_react$33.useContext(NavigationContext);
		let routeContext = import_react$33.useContext(RouteContext);
		invariant(routeContext, "useFormAction must be used inside a RouteContext");
		let [match] = routeContext.matches.slice(-1);
		let path = { ...useResolvedPath(action ? action : ".", { relative }) };
		let location = useLocation();
		if (action == null) {
			path.search = location.search;
			let params = new URLSearchParams(path.search);
			let indexValues = params.getAll("index");
			if (indexValues.some((v) => v === "")) {
				params.delete("index");
				indexValues.filter((v) => v).forEach((v) => params.append("index", v));
				let qs = params.toString();
				path.search = qs ? `?${qs}` : "";
			}
		}
		if ((!action || action === ".") && match.route.index) path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
		if (basename !== "/") path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
		return createPath(path);
	}
	function getScrollRestorationKey(location, matches, basename, getKey) {
		let key = null;
		if (getKey) {
			if (basename !== "/") key = getKey({
				...location,
				pathname: stripBasename(location.pathname, basename) || location.pathname
			}, matches);
			else key = getKey(location, matches);
		}
		if (key == null) key = location.key;
		return key;
	}
	function useScrollRestoration({ getKey, storageKey } = {}) {
		let { router } = useDataRouterContext3("useScrollRestoration");
		let { restoreScrollPosition, preventScrollReset } = useDataRouterState2("useScrollRestoration");
		let { basename } = import_react$33.useContext(NavigationContext);
		let location = useLocation();
		let matches = useMatches();
		let navigation = useNavigation();
		import_react$33.useEffect(() => {
			window.history.scrollRestoration = "manual";
			return () => {
				window.history.scrollRestoration = "auto";
			};
		}, []);
		usePageHide(import_react$33.useCallback(() => {
			if (navigation.state === "idle") {
				let key = getScrollRestorationKey(location, matches, basename, getKey);
				savedScrollPositions[key] = window.scrollY;
			}
			try {
				sessionStorage.setItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY, JSON.stringify(savedScrollPositions));
			} catch (error) {
				warning(false, `Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (${error}).`);
			}
			window.history.scrollRestoration = "auto";
		}, [
			navigation.state,
			getKey,
			basename,
			location,
			matches,
			storageKey
		]));
		if (typeof document !== "undefined") {
			import_react$33.useLayoutEffect(() => {
				try {
					let sessionPositions = sessionStorage.getItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY);
					if (sessionPositions) savedScrollPositions = JSON.parse(sessionPositions);
				} catch (e) {}
			}, [storageKey]);
			import_react$33.useLayoutEffect(() => {
				let disableScrollRestoration = router?.enableScrollRestoration(savedScrollPositions, () => window.scrollY, getKey ? (location2, matches2) => getScrollRestorationKey(location2, matches2, basename, getKey) : void 0);
				return () => disableScrollRestoration && disableScrollRestoration();
			}, [
				router,
				basename,
				getKey
			]);
			import_react$33.useLayoutEffect(() => {
				if (restoreScrollPosition === false) return;
				if (typeof restoreScrollPosition === "number") {
					window.scrollTo(0, restoreScrollPosition);
					return;
				}
				try {
					if (location.hash) {
						let el = document.getElementById(decodeURIComponent(location.hash.slice(1)));
						if (el) {
							el.scrollIntoView();
							return;
						}
					}
				} catch {
					warning(false, `"${location.hash.slice(1)}" is not a decodable element ID. The view will not scroll to it.`);
				}
				if (preventScrollReset === true) return;
				window.scrollTo(0, 0);
			}, [
				location,
				restoreScrollPosition,
				preventScrollReset
			]);
		}
	}
	function usePageHide(callback, options) {
		let { capture } = options || {};
		import_react$33.useEffect(() => {
			let opts = capture != null ? { capture } : void 0;
			window.addEventListener("pagehide", callback, opts);
			return () => {
				window.removeEventListener("pagehide", callback, opts);
			};
		}, [callback, capture]);
	}
	function useViewTransitionState(to, { relative } = {}) {
		let vtContext = import_react$33.useContext(ViewTransitionContext);
		invariant(vtContext != null, "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?");
		let { basename } = useDataRouterContext3("useViewTransitionState");
		let path = useResolvedPath(to, { relative });
		if (!vtContext.isTransitioning) return false;
		let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
		let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
		return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
	}
	var import_react$24, import_react$25, import_react$26, import_react$28, import_react$29, import_react$33, ABSOLUTE_URL_REGEX, PROTOCOL_RELATIVE_URL_REGEX, paramRe, dynamicSegmentValue, indexRouteValue, emptySegmentValue, staticSegmentValue, splatPenalty, isSplat, removeDoubleSlashes, joinPaths, normalizePathname, normalizeSearch, normalizeHash, ErrorResponseImpl, isBrowser, DEFAULT_NAVIGATION_URL, validMutationMethodsArr, validRequestMethodsArr, invalidProtocols, DataRouterContext, DataRouterStateContext, RSCRouterContext, ViewTransitionContext, FetchersContext, AwaitContext, NavigationContext, LocationContext, RouteContext, RouteErrorContext, ERROR_DIGEST_BASE, ERROR_DIGEST_REDIRECT, ERROR_DIGEST_ROUTE_ERROR_RESPONSE, navigateEffectWarning, defaultErrorElement, RenderErrorBoundary, errorRedirectHandledMap, alreadyWarned, defaultMethod, defaultEncType, _formDataSupportsSubmitter, supportedFormEncTypes, ESCAPE_LOOKUP, ESCAPE_REGEX, FrameworkContext, isBrowser2, Link, NavLink, Form, fetcherId, getUniqueFetcherId, SCROLL_RESTORATION_STORAGE_KEY, savedScrollPositions;
	var init_chunk_BV7QT456 = __esmMin((() => {
		import_react$24 = /* @__PURE__ */ __toESM(require_react(), 1);
		import_react$25 = /* @__PURE__ */ __toESM(require_react(), 1);
		import_react$26 = /* @__PURE__ */ __toESM(require_react(), 1);
		require_react();
		import_react$28 = /* @__PURE__ */ __toESM(require_react(), 1);
		import_react$29 = /* @__PURE__ */ __toESM(require_react(), 1);
		require_react();
		require_react();
		require_react();
		import_react$33 = /* @__PURE__ */ __toESM(require_react(), 1);
		require_react();
		ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|[\\/]{2})/i;
		PROTOCOL_RELATIVE_URL_REGEX = /^[\\/]{2}/;
		paramRe = /^:[\w-]+$/;
		dynamicSegmentValue = 3;
		indexRouteValue = 2;
		emptySegmentValue = 1;
		staticSegmentValue = 10;
		splatPenalty = -2;
		isSplat = (s) => s === "*";
		removeDoubleSlashes = (path) => path.replace(/[\\/]{2,}/g, "/");
		joinPaths = (paths) => removeDoubleSlashes(paths.join("/"));
		normalizePathname = (pathname) => removeTrailingSlash(pathname).replace(/^\/*/, "/");
		normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
		normalizeHash = (hash) => !hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
		ErrorResponseImpl = class {
			constructor(status, statusText, data2, internal = false) {
				this.status = status;
				this.statusText = statusText || "";
				this.internal = internal;
				if (data2 instanceof Error) {
					this.data = data2.toString();
					this.error = data2;
				} else this.data = data2;
			}
		};
		isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
		Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
		DEFAULT_NAVIGATION_URL = new URL("http://localhost");
		validMutationMethodsArr = [
			"POST",
			"PUT",
			"PATCH",
			"DELETE"
		];
		new Set(validMutationMethodsArr);
		validRequestMethodsArr = ["GET", ...validMutationMethodsArr];
		new Set(validRequestMethodsArr);
		invalidProtocols = [
			"about:",
			"blob:",
			"chrome:",
			"chrome-untrusted:",
			"content:",
			"data:",
			"devtools:",
			"file:",
			"filesystem:",
			"javascript:"
		];
		DataRouterContext = import_react$24.createContext(null);
		DataRouterContext.displayName = "DataRouter";
		DataRouterStateContext = import_react$24.createContext(null);
		DataRouterStateContext.displayName = "DataRouterState";
		RSCRouterContext = import_react$24.createContext(false);
		ViewTransitionContext = import_react$24.createContext({ isTransitioning: false });
		ViewTransitionContext.displayName = "ViewTransition";
		FetchersContext = import_react$24.createContext(/* @__PURE__ */ new Map());
		FetchersContext.displayName = "Fetchers";
		AwaitContext = import_react$24.createContext(null);
		AwaitContext.displayName = "Await";
		NavigationContext = import_react$24.createContext(null);
		NavigationContext.displayName = "Navigation";
		LocationContext = import_react$24.createContext(null);
		LocationContext.displayName = "Location";
		RouteContext = import_react$24.createContext({
			outlet: null,
			matches: [],
			isDataRoute: false
		});
		RouteContext.displayName = "Route";
		RouteErrorContext = import_react$24.createContext(null);
		RouteErrorContext.displayName = "RouteError";
		ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
		ERROR_DIGEST_REDIRECT = "REDIRECT";
		ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
		navigateEffectWarning = `You should call navigate() in a React.useEffect(), not when your component is first rendered.`;
		import_react$25.createContext(null);
		defaultErrorElement = /* @__PURE__ */ import_react$25.createElement(DefaultErrorComponent, null);
		RenderErrorBoundary = class extends import_react$25.Component {
			constructor(props) {
				super(props);
				this.state = {
					location: props.location,
					revalidation: props.revalidation,
					error: props.error
				};
			}
			static getDerivedStateFromError(error) {
				return { error };
			}
			static getDerivedStateFromProps(props, state) {
				if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") return {
					error: props.error,
					location: props.location,
					revalidation: props.revalidation
				};
				return {
					error: props.error !== void 0 ? props.error : state.error,
					location: state.location,
					revalidation: props.revalidation || state.revalidation
				};
			}
			componentDidCatch(error, errorInfo) {
				if (this.props.onError) this.props.onError(error, errorInfo);
				else console.error("React Router caught the following error during render", error);
			}
			render() {
				let error = this.state.error;
				if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
					const decoded = decodeRouteErrorResponseDigest(error.digest);
					if (decoded) error = decoded;
				}
				let result = error !== void 0 ? /* @__PURE__ */ import_react$25.createElement(RouteContext.Provider, { value: this.props.routeContext }, /* @__PURE__ */ import_react$25.createElement(RouteErrorContext.Provider, {
					value: error,
					children: this.props.component
				})) : this.props.children;
				if (this.context) return /* @__PURE__ */ import_react$25.createElement(RSCErrorHandler, { error }, result);
				return result;
			}
		};
		RenderErrorBoundary.contextType = RSCRouterContext;
		errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
		alreadyWarned = {};
		import_react$26.memo(DataRoutes2);
		import_react$26.Component;
		defaultMethod = "get";
		defaultEncType = "application/x-www-form-urlencoded";
		_formDataSupportsSubmitter = null;
		supportedFormEncTypes = /* @__PURE__ */ new Set([
			"application/x-www-form-urlencoded",
			"multipart/form-data",
			"text/plain"
		]);
		Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
		ESCAPE_LOOKUP = {
			"&": "\\u0026",
			">": "\\u003e",
			"<": "\\u003c",
			"\u2028": "\\u2028",
			"\u2029": "\\u2029"
		};
		ESCAPE_REGEX = /[&><\u2028\u2029]/g;
		FrameworkContext = import_react$29.createContext(void 0);
		FrameworkContext.displayName = "FrameworkContext";
		import_react$28.Component;
		isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
		try {
			if (isBrowser2) window.__reactRouterVersion = "7.18.3";
		} catch (e) {}
		HistoryRouter.displayName = "unstable_HistoryRouter";
		Link = import_react$33.forwardRef(function LinkWithRef({ onClick, discover = "render", prefetch = "none", relative, reloadDocument, replace: replace2, mask, state, target, to, preventScrollReset, viewTransition, defaultShouldRevalidate, ...rest }, forwardedRef) {
			let { basename, navigator, useTransitions } = import_react$33.useContext(NavigationContext);
			let isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX.test(to);
			let parsed = parseToInfo(to, basename);
			to = parsed.to;
			let href = useHref(to, { relative });
			let location = useLocation();
			let maskedHref = null;
			if (mask) {
				let resolved = resolveTo(mask, [], location.mask ? location.mask.pathname : "/", true);
				if (basename !== "/") resolved.pathname = resolved.pathname === "/" ? basename : joinPaths([basename, resolved.pathname]);
				maskedHref = navigator.createHref(resolved);
			}
			let [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(prefetch, rest);
			let internalOnClick = useLinkClickHandler(to, {
				replace: replace2,
				mask,
				state,
				target,
				preventScrollReset,
				relative,
				viewTransition,
				defaultShouldRevalidate,
				useTransitions
			});
			function handleClick(event) {
				if (onClick) onClick(event);
				if (!event.defaultPrevented) internalOnClick(event);
			}
			let isSpaLink = !(parsed.isExternal || reloadDocument);
			let link = /* @__PURE__ */ import_react$33.createElement("a", {
				...rest,
				...prefetchHandlers,
				href: (isSpaLink ? maskedHref : void 0) || parsed.absoluteURL || href,
				onClick: isSpaLink ? handleClick : onClick,
				ref: mergeRefs(forwardedRef, prefetchRef),
				target,
				"data-discover": !isAbsolute && discover === "render" ? "true" : void 0
			});
			return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ import_react$33.createElement(import_react$33.Fragment, null, link, /* @__PURE__ */ import_react$33.createElement(PrefetchPageLinks, { page: href })) : link;
		});
		Link.displayName = "Link";
		NavLink = import_react$33.forwardRef(function NavLinkWithRef({ "aria-current": ariaCurrentProp = "page", caseSensitive = false, className: classNameProp = "", end = false, style: styleProp, to, viewTransition, children, ...rest }, ref) {
			let path = useResolvedPath(to, { relative: rest.relative });
			let location = useLocation();
			let routerState = import_react$33.useContext(DataRouterStateContext);
			let { navigator, basename } = import_react$33.useContext(NavigationContext);
			let isTransitioning = routerState != null && useViewTransitionState(path) && viewTransition === true;
			let toPathname = navigator.encodeLocation ? navigator.encodeLocation(path).pathname : path.pathname;
			let locationPathname = location.pathname;
			let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
			if (!caseSensitive) {
				locationPathname = locationPathname.toLowerCase();
				nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
				toPathname = toPathname.toLowerCase();
			}
			if (nextLocationPathname && basename) nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
			const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
			let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
			let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
			let renderProps = {
				isActive,
				isPending,
				isTransitioning
			};
			let ariaCurrent = isActive ? ariaCurrentProp : void 0;
			let className;
			if (typeof classNameProp === "function") className = classNameProp(renderProps);
			else className = [
				classNameProp,
				isActive ? "active" : null,
				isPending ? "pending" : null,
				isTransitioning ? "transitioning" : null
			].filter(Boolean).join(" ");
			let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
			return /* @__PURE__ */ import_react$33.createElement(Link, {
				...rest,
				"aria-current": ariaCurrent,
				className,
				ref,
				style,
				to,
				viewTransition
			}, typeof children === "function" ? children(renderProps) : children);
		});
		NavLink.displayName = "NavLink";
		Form = import_react$33.forwardRef(({ discover = "render", fetcherKey, navigate, reloadDocument, replace: replace2, state, method = defaultMethod, action, onSubmit, relative, preventScrollReset, viewTransition, defaultShouldRevalidate, ...props }, forwardedRef) => {
			let { useTransitions } = import_react$33.useContext(NavigationContext);
			let submit = useSubmit();
			let formAction = useFormAction(action, { relative });
			let formMethod = method.toLowerCase() === "get" ? "get" : "post";
			let isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX.test(action);
			let submitHandler = (event) => {
				onSubmit && onSubmit(event);
				if (event.defaultPrevented) return;
				event.preventDefault();
				let submitter = event.nativeEvent.submitter;
				let submitMethod = submitter?.getAttribute("formmethod") || method;
				let doSubmit = () => submit(submitter || event.currentTarget, {
					fetcherKey,
					method: submitMethod,
					navigate,
					replace: replace2,
					state,
					relative,
					preventScrollReset,
					viewTransition,
					defaultShouldRevalidate
				});
				if (useTransitions && navigate !== false) import_react$33.startTransition(() => doSubmit());
				else doSubmit();
			};
			return /* @__PURE__ */ import_react$33.createElement("form", {
				ref: forwardedRef,
				method: formMethod,
				action: formAction,
				onSubmit: reloadDocument ? onSubmit : submitHandler,
				...props,
				"data-discover": !isAbsolute && discover === "render" ? "true" : void 0
			});
		});
		Form.displayName = "Form";
		ScrollRestoration.displayName = "ScrollRestoration";
		fetcherId = 0;
		getUniqueFetcherId = () => `__${String(++fetcherId)}__`;
		SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
		savedScrollPositions = {};
	}));
	//#endregion
	//#region ../node_modules/react-router/dist/development/index.mjs
	/**
	* react-router v7.18.3
	*
	* Copyright (c) Remix Software Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE.md file in the root directory of this source tree.
	*
	* @license MIT
	*/
	var init_development = __esmMin((() => {
		init_chunk_BV7QT456();
	}));
	//#endregion
	//#region ../node_modules/react-router-dom/dist/index.mjs
	var init_dist = __esmMin((() => {
		init_development();
	}));
	/**
	* react-router-dom v7.18.3
	*
	* Copyright (c) Remix Software Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE.md file in the root directory of this source tree.
	*
	* @license MIT
	*/
	//#endregion
	//#region src/api/client.js
	var client_exports = /* @__PURE__ */ __exportAll({
		ApiError: () => ApiError,
		api: () => api,
		attachments: () => attachments,
		audit: () => audit,
		auth: () => auth,
		dashboard: () => dashboard,
		equipment: () => equipment,
		faults: () => faults,
		getCsrf: () => getCsrf,
		maintenance: () => maintenance,
		notifications: () => notifications,
		parts: () => parts,
		publicApi: () => publicApi,
		reference: () => reference,
		reports: () => reports,
		setBearer: () => setBearer,
		setCsrf: () => setCsrf,
		setUnauthorizedHandler: () => setUnauthorizedHandler,
		users: () => users
	});
	async function request(path, { method = "GET", body, formData, headers = {}, signal, raw = false } = {}) {
		const isWrite = method !== "GET" && method !== "HEAD";
		const opts = {
			method,
			credentials: "same-origin",
			headers: {
				accept: "application/json",
				...bearer ? { authorization: `Bearer ${bearer}` } : {},
				...isWrite && csrfToken ? { "x-bm-csrf": csrfToken } : {},
				...headers
			},
			signal
		};
		if (formData) opts.body = formData;
		else if (body !== void 0) {
			opts.headers["content-type"] = "application/json";
			opts.body = JSON.stringify(body);
		}
		let res;
		try {
			res = await fetch(`${BASE}${path}`, opts);
		} catch (err) {
			if (err?.name === "AbortError") throw err;
			throw new ApiError(0, "network_error", "The server could not be reached. Check the network connection, then try again.", {});
		}
		if (res.status === 204) return null;
		if (raw) return res;
		const text = await res.text();
		let payload = null;
		if (text) try {
			payload = JSON.parse(text);
		} catch {
			payload = { error: {
				code: "bad_response",
				message: text.slice(0, 200)
			} };
		}
		if (!res.ok) {
			const e = payload?.error ?? {};
			if (res.status === 401) onUnauthorized();
			throw new ApiError(res.status, e.code ?? "error", e.message ?? `Request failed (${res.status})`, e.details);
		}
		return payload;
	}
	/** Drops empty strings/nulls so a blank filter box does not become `?q=`. */
	function clean(params = {}) {
		const out = {};
		for (const [k, v] of Object.entries(params)) {
			if (v === void 0 || v === null || v === "") continue;
			out[k] = typeof v === "boolean" ? v ? "1" : "0" : v;
		}
		return out;
	}
	var BASE, csrfToken, onUnauthorized, ApiError, setCsrf, getCsrf, setUnauthorizedHandler, bearer, setBearer, api, auth, equipment, faults, maintenance, dashboard, notifications, reports, reference, users, parts, audit, publicApi, attachments;
	var init_client = __esmMin((() => {
		BASE = "/api/v1";
		csrfToken = null;
		onUnauthorized = () => {};
		ApiError = class extends Error {
			constructor(status, code, message, details) {
				super(message || "Request failed");
				this.name = "ApiError";
				this.status = status;
				this.code = code;
				this.details = details ?? {};
				this.fieldErrors = details?.fields ?? {};
			}
			/** Message for one field, if the server complained about it. */
			errorFor(field) {
				const list = this.fieldErrors[field];
				return Array.isArray(list) && list.length ? list.join(" ") : null;
			}
			get isAuth() {
				return this.status === 401;
			}
			get isForbidden() {
				return this.status === 403;
			}
		};
		setCsrf = (token) => {
			csrfToken = token ?? null;
		};
		getCsrf = () => csrfToken;
		setUnauthorizedHandler = (fn) => {
			onUnauthorized = fn ?? (() => {});
		};
		bearer = null;
		setBearer = (t) => {
			bearer = t ?? null;
		};
		api = {
			get: (path, opts) => request(path, {
				...opts,
				method: "GET"
			}),
			post: (path, body, opts) => request(path, {
				...opts,
				method: "POST",
				body
			}),
			put: (path, body, opts) => request(path, {
				...opts,
				method: "PUT",
				body
			}),
			patch: (path, body, opts) => request(path, {
				...opts,
				method: "PATCH",
				body
			}),
			del: (path, body, opts) => request(path, {
				...opts,
				method: "DELETE",
				body
			}),
			upload: (path, formData, opts) => request(path, {
				...opts,
				method: "POST",
				formData
			}),
			raw: (path, opts) => request(path, {
				...opts,
				method: "GET",
				raw: true
			})
		};
		auth = {
			login: (email, password, remember) => request("/auth/login", {
				method: "POST",
				body: {
					email,
					password,
					remember
				}
			}),
			logout: () => request("/auth/logout", { method: "POST" }),
			me: () => request("/auth/me"),
			policy: () => request("/auth/policy"),
			changePassword: (currentPassword, newPassword) => request("/auth/change-password", {
				method: "POST",
				body: {
					currentPassword,
					newPassword
				}
			}),
			sessions: () => request("/auth/sessions"),
			revokeSession: (id) => request(`/auth/sessions/${id}`, { method: "DELETE" }),
			revokeAll: () => request("/auth/sessions/revoke-all", { method: "POST" })
		};
		equipment = {
			list: (params) => request(`/equipment?${new URLSearchParams(clean(params))}`),
			get: (id) => request(`/equipment/${id}`),
			create: (body) => request("/equipment", {
				method: "POST",
				body
			}),
			update: (id, body) => request(`/equipment/${id}`, {
				method: "PATCH",
				body
			}),
			remove: (id, body) => request(`/equipment/${id}`, {
				method: "DELETE",
				body
			}),
			setStatus: (id, status, reason) => request(`/equipment/${id}/status`, {
				method: "POST",
				body: {
					status,
					reason
				}
			}),
			activate: (id) => request(`/equipment/${id}/activate`, {
				method: "POST",
				body: {}
			}),
			deactivate: (id, reason) => request(`/equipment/${id}/deactivate`, {
				method: "POST",
				body: { reason }
			}),
			history: (id, params) => request(`/equipment/${id}/history?${new URLSearchParams(clean(params))}`),
			vocabulary: () => request("/equipment/vocabulary"),
			needsAttention: (limit = 10) => request(`/equipment/needs-attention?limit=${limit}`),
			uploadImage: (id, file) => {
				const fd = new FormData();
				fd.append("file", file);
				return request(`/equipment/${id}/image`, {
					method: "POST",
					formData: fd
				});
			},
			removeImage: (id) => request(`/equipment/${id}/image`, { method: "DELETE" }),
			labels: (ids) => request("/equipment/labels", {
				method: "POST",
				body: { ids }
			}),
			qrUrl: (id, kind = "png") => `${BASE}/equipment/${id}/qr.${kind}`
		};
		faults = {
			list: (params) => request(`/faults?${new URLSearchParams(clean(params))}`),
			get: (id) => request(`/faults/${id}`),
			vocabulary: () => request("/faults/vocabulary"),
			create: (formData) => request("/faults", {
				method: "POST",
				formData
			}),
			update: (id, body) => request(`/faults/${id}`, {
				method: "PATCH",
				body
			}),
			assign: (id, technicianId, note) => request(`/faults/${id}/assign`, {
				method: "POST",
				body: {
					technicianId,
					note
				}
			}),
			transition: (id, targetStatus, comment) => request(`/faults/${id}/transition`, {
				method: "POST",
				body: {
					targetStatus,
					comment
				}
			}),
			note: (id, comment) => request(`/faults/${id}/notes`, {
				method: "POST",
				body: { comment }
			}),
			reopen: (id, comment, reason) => request(`/faults/${id}/reopen`, {
				method: "POST",
				body: {
					comment,
					reason
				}
			}),
			repair: (id) => request(`/faults/${id}/repair`),
			saveRepair: (id, body) => request(`/faults/${id}/repair`, {
				method: "PUT",
				body
			}),
			uploadRepairPhotos: (id, files, kind, caption) => {
				const fd = new FormData();
				fd.append("kind", kind);
				if (caption) fd.append("caption", caption);
				for (const f of files) fd.append("files", f);
				return request(`/faults/${id}/repair/photos`, {
					method: "POST",
					formData: fd
				});
			},
			attach: (id, files, kind, caption) => {
				const fd = new FormData();
				fd.append("kind", kind);
				if (caption) fd.append("caption", caption);
				for (const f of files) fd.append("files", f);
				return request(`/faults/${id}/attachments`, {
					method: "POST",
					formData: fd
				});
			}
		};
		maintenance = {
			schedules: (params) => request(`/maintenance/schedules?${new URLSearchParams(clean(params))}`),
			schedule: (id) => request(`/maintenance/schedules/${id}`),
			createSchedule: (body) => request("/maintenance/schedules", {
				method: "POST",
				body
			}),
			updateSchedule: (id, body) => request(`/maintenance/schedules/${id}`, {
				method: "PATCH",
				body
			}),
			deleteSchedule: (id, reason) => request(`/maintenance/schedules/${id}`, {
				method: "DELETE",
				body: { reason }
			}),
			complete: (id, body) => request(`/maintenance/schedules/${id}/complete`, {
				method: "POST",
				body
			}),
			records: (params) => request(`/maintenance/records?${new URLSearchParams(clean(params))}`),
			record: (id) => request(`/maintenance/records/${id}`),
			createRecord: (body) => request("/maintenance/records", {
				method: "POST",
				body
			}),
			deleteRecord: (id, reason) => request(`/maintenance/records/${id}`, {
				method: "DELETE",
				body: { reason }
			}),
			dueBoard: (params) => request(`/maintenance/due-board?${new URLSearchParams(clean(params))}`),
			compliance: (params) => request(`/maintenance/compliance?${new URLSearchParams(clean(params))}`),
			vocabulary: () => request("/maintenance/vocabulary"),
			reminders: () => request("/maintenance/reminders", {
				method: "POST",
				body: {}
			})
		};
		dashboard = {
			get: (params) => request(`/dashboard?${new URLSearchParams(clean(params))}`),
			risk: (params) => request(`/dashboard/risk?${new URLSearchParams(clean(params))}`),
			riskFor: (id) => request(`/dashboard/risk/${id}`),
			riskModel: () => request("/dashboard/risk/model")
		};
		notifications = {
			list: (params) => request(`/notifications?${new URLSearchParams(clean(params))}`),
			unread: () => request("/notifications/unread-count"),
			markRead: (ids) => request("/notifications/read", {
				method: "POST",
				body: ids?.length ? { ids } : {}
			}),
			remove: (id) => request(`/notifications/${id}`, { method: "DELETE" }),
			deliveries: (id) => request(`/notifications/${id}/deliveries`)
		};
		reports = {
			list: () => request("/reports"),
			run: (key, params) => request(`/reports/${key}?${new URLSearchParams(clean(params))}`),
			csvUrl: (key, params) => `${BASE}/reports/export/${key}/csv?${new URLSearchParams(clean(params))}`,
			printUrl: (key, params) => `${BASE}/reports/export/${key}/print?${new URLSearchParams(clean(params))}`
		};
		reference = {
			picklists: () => request("/reference/picklists"),
			categories: () => request("/reference/categories"),
			locations: () => request("/reference/locations"),
			faultCategories: () => request("/reference/fault-categories"),
			create: (kind, body) => request(`/reference/${kind}`, {
				method: "POST",
				body
			}),
			update: (kind, id, body) => request(`/reference/${kind}/${id}`, {
				method: "PATCH",
				body
			}),
			remove: (kind, id, body) => request(`/reference/${kind}/${id}`, {
				method: "DELETE",
				body: body ?? {}
			}),
			settings: () => request("/reference/settings"),
			saveSettings: (body) => request("/reference/settings", {
				method: "PATCH",
				body
			})
		};
		users = {
			list: (params) => request(`/users?${new URLSearchParams(clean(params))}`),
			get: (id) => request(`/users/${id}`),
			create: (body) => request("/users", {
				method: "POST",
				body
			}),
			update: (id, body) => request(`/users/${id}`, {
				method: "PATCH",
				body
			}),
			resetPassword: (id, body) => request(`/users/${id}/reset-password`, {
				method: "POST",
				body: body ?? {}
			}),
			signOut: (id) => request(`/users/${id}/sign-out`, {
				method: "POST",
				body: {}
			}),
			remove: (id, body) => request(`/users/${id}`, {
				method: "DELETE",
				body
			}),
			lookup: () => request("/users/lookup"),
			strength: (candidate, fullName, email) => request(`/users/password-strength?${new URLSearchParams({
				candidate,
				fullName: fullName ?? "",
				email: email ?? ""
			})}`)
		};
		parts = {
			list: (params) => request(`/parts?${new URLSearchParams(clean(params))}`),
			upsert: (body) => request("/parts", {
				method: "POST",
				body
			})
		};
		audit = { list: (params) => request(`/audit?${new URLSearchParams(clean(params))}`) };
		publicApi = {
			config: () => request("/public/config"),
			equipment: (tag) => request(`/public/equipment/${encodeURIComponent(tag)}`)
		};
		attachments = {
			viewUrl: (id) => `${BASE}/attachments/${id}`,
			remove: (id) => request(`/attachments/${id}`, { method: "DELETE" })
		};
	}));
	//#endregion
	//#region ../node_modules/react/cjs/react-jsx-runtime.development.js
	/**
	* @license React
	* react-jsx-runtime.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_react_jsx_runtime_development = /* @__PURE__ */ __commonJSMin(((exports) => {
		(function() {
			"use strict";
			var React = require_react();
			var REACT_ELEMENT_TYPE = Symbol.for("react.element");
			var REACT_PORTAL_TYPE = Symbol.for("react.portal");
			var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
			var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
			var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
			var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
			var REACT_CONTEXT_TYPE = Symbol.for("react.context");
			var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
			var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
			var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
			var REACT_MEMO_TYPE = Symbol.for("react.memo");
			var REACT_LAZY_TYPE = Symbol.for("react.lazy");
			var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
			var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
			var FAUX_ITERATOR_SYMBOL = "@@iterator";
			function getIteratorFn(maybeIterable) {
				if (maybeIterable === null || typeof maybeIterable !== "object") return null;
				var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
				if (typeof maybeIterator === "function") return maybeIterator;
				return null;
			}
			var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
			function error(format) {
				for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) args[_key2 - 1] = arguments[_key2];
				printWarning("error", format, args);
			}
			function printWarning(level, format, args) {
				var stack = ReactSharedInternals.ReactDebugCurrentFrame.getStackAddendum();
				if (stack !== "") {
					format += "%s";
					args = args.concat([stack]);
				}
				var argsWithFormat = args.map(function(item) {
					return String(item);
				});
				argsWithFormat.unshift("Warning: " + format);
				Function.prototype.apply.call(console[level], console, argsWithFormat);
			}
			var enableScopeAPI = false;
			var enableCacheElement = false;
			var enableTransitionTracing = false;
			var enableLegacyHidden = false;
			var enableDebugTracing = false;
			var REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
			function isValidElementType(type) {
				if (typeof type === "string" || typeof type === "function") return true;
				if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) return true;
				if (typeof type === "object" && type !== null) {
					if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) return true;
				}
				return false;
			}
			function getWrappedName(outerType, innerType, wrapperName) {
				var displayName = outerType.displayName;
				if (displayName) return displayName;
				var functionName = innerType.displayName || innerType.name || "";
				return functionName !== "" ? wrapperName + "(" + functionName + ")" : wrapperName;
			}
			function getContextName(type) {
				return type.displayName || "Context";
			}
			function getComponentNameFromType(type) {
				if (type == null) return null;
				if (typeof type.tag === "number") error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue.");
				if (typeof type === "function") return type.displayName || type.name || null;
				if (typeof type === "string") return type;
				switch (type) {
					case REACT_FRAGMENT_TYPE: return "Fragment";
					case REACT_PORTAL_TYPE: return "Portal";
					case REACT_PROFILER_TYPE: return "Profiler";
					case REACT_STRICT_MODE_TYPE: return "StrictMode";
					case REACT_SUSPENSE_TYPE: return "Suspense";
					case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_CONTEXT_TYPE: return getContextName(type) + ".Consumer";
					case REACT_PROVIDER_TYPE: return getContextName(type._context) + ".Provider";
					case REACT_FORWARD_REF_TYPE: return getWrappedName(type, type.render, "ForwardRef");
					case REACT_MEMO_TYPE:
						var outerName = type.displayName || null;
						if (outerName !== null) return outerName;
						return getComponentNameFromType(type.type) || "Memo";
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return getComponentNameFromType(init(payload));
						} catch (x) {
							return null;
						}
				}
				return null;
			}
			var assign = Object.assign;
			var disabledDepth = 0;
			var prevLog;
			var prevInfo;
			var prevWarn;
			var prevError;
			var prevGroup;
			var prevGroupCollapsed;
			var prevGroupEnd;
			function disabledLog() {}
			disabledLog.__reactDisabledLog = true;
			function disableLogs() {
				if (disabledDepth === 0) {
					prevLog = console.log;
					prevInfo = console.info;
					prevWarn = console.warn;
					prevError = console.error;
					prevGroup = console.group;
					prevGroupCollapsed = console.groupCollapsed;
					prevGroupEnd = console.groupEnd;
					var props = {
						configurable: true,
						enumerable: true,
						value: disabledLog,
						writable: true
					};
					Object.defineProperties(console, {
						info: props,
						log: props,
						warn: props,
						error: props,
						group: props,
						groupCollapsed: props,
						groupEnd: props
					});
				}
				disabledDepth++;
			}
			function reenableLogs() {
				disabledDepth--;
				if (disabledDepth === 0) {
					var props = {
						configurable: true,
						enumerable: true,
						writable: true
					};
					Object.defineProperties(console, {
						log: assign({}, props, { value: prevLog }),
						info: assign({}, props, { value: prevInfo }),
						warn: assign({}, props, { value: prevWarn }),
						error: assign({}, props, { value: prevError }),
						group: assign({}, props, { value: prevGroup }),
						groupCollapsed: assign({}, props, { value: prevGroupCollapsed }),
						groupEnd: assign({}, props, { value: prevGroupEnd })
					});
				}
				if (disabledDepth < 0) error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
			}
			var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
			var prefix;
			function describeBuiltInComponentFrame(name, source, ownerFn) {
				if (prefix === void 0) try {
					throw Error();
				} catch (x) {
					var match = x.stack.trim().match(/\n( *(at )?)/);
					prefix = match && match[1] || "";
				}
				return "\n" + prefix + name;
			}
			var reentry = false;
			var componentFrameCache = new (typeof WeakMap === "function" ? WeakMap : Map)();
			function describeNativeComponentFrame(fn, construct) {
				if (!fn || reentry) return "";
				var frame = componentFrameCache.get(fn);
				if (frame !== void 0) return frame;
				var control;
				reentry = true;
				var previousPrepareStackTrace = Error.prepareStackTrace;
				Error.prepareStackTrace = void 0;
				var previousDispatcher = ReactCurrentDispatcher.current;
				ReactCurrentDispatcher.current = null;
				disableLogs();
				try {
					if (construct) {
						var Fake = function() {
							throw Error();
						};
						Object.defineProperty(Fake.prototype, "props", { set: function() {
							throw Error();
						} });
						if (typeof Reflect === "object" && Reflect.construct) {
							try {
								Reflect.construct(Fake, []);
							} catch (x) {
								control = x;
							}
							Reflect.construct(fn, [], Fake);
						} else {
							try {
								Fake.call();
							} catch (x) {
								control = x;
							}
							fn.call(Fake.prototype);
						}
					} else {
						try {
							throw Error();
						} catch (x) {
							control = x;
						}
						fn();
					}
				} catch (sample) {
					if (sample && control && typeof sample.stack === "string") {
						var sampleLines = sample.stack.split("\n");
						var controlLines = control.stack.split("\n");
						var s = sampleLines.length - 1;
						var c = controlLines.length - 1;
						while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) c--;
						for (; s >= 1 && c >= 0; s--, c--) if (sampleLines[s] !== controlLines[c]) {
							if (s !== 1 || c !== 1) do {
								s--;
								c--;
								if (c < 0 || sampleLines[s] !== controlLines[c]) {
									var _frame = "\n" + sampleLines[s].replace(" at new ", " at ");
									if (fn.displayName && _frame.includes("<anonymous>")) _frame = _frame.replace("<anonymous>", fn.displayName);
									if (typeof fn === "function") componentFrameCache.set(fn, _frame);
									return _frame;
								}
							} while (s >= 1 && c >= 0);
							break;
						}
					}
				} finally {
					reentry = false;
					ReactCurrentDispatcher.current = previousDispatcher;
					reenableLogs();
					Error.prepareStackTrace = previousPrepareStackTrace;
				}
				var name = fn ? fn.displayName || fn.name : "";
				var syntheticFrame = name ? describeBuiltInComponentFrame(name) : "";
				if (typeof fn === "function") componentFrameCache.set(fn, syntheticFrame);
				return syntheticFrame;
			}
			function describeFunctionComponentFrame(fn, source, ownerFn) {
				return describeNativeComponentFrame(fn, false);
			}
			function shouldConstruct(Component) {
				var prototype = Component.prototype;
				return !!(prototype && prototype.isReactComponent);
			}
			function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {
				if (type == null) return "";
				if (typeof type === "function") return describeNativeComponentFrame(type, shouldConstruct(type));
				if (typeof type === "string") return describeBuiltInComponentFrame(type);
				switch (type) {
					case REACT_SUSPENSE_TYPE: return describeBuiltInComponentFrame("Suspense");
					case REACT_SUSPENSE_LIST_TYPE: return describeBuiltInComponentFrame("SuspenseList");
				}
				if (typeof type === "object") switch (type.$$typeof) {
					case REACT_FORWARD_REF_TYPE: return describeFunctionComponentFrame(type.render);
					case REACT_MEMO_TYPE: return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);
					case REACT_LAZY_TYPE:
						var lazyComponent = type;
						var payload = lazyComponent._payload;
						var init = lazyComponent._init;
						try {
							return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
						} catch (x) {}
				}
				return "";
			}
			var hasOwnProperty = Object.prototype.hasOwnProperty;
			var loggedTypeFailures = {};
			var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
			function setCurrentlyValidatingElement(element) {
				if (element) {
					var owner = element._owner;
					var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
					ReactDebugCurrentFrame.setExtraStackFrame(stack);
				} else ReactDebugCurrentFrame.setExtraStackFrame(null);
			}
			function checkPropTypes(typeSpecs, values, location, componentName, element) {
				var has = Function.call.bind(hasOwnProperty);
				for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
					var error$1 = void 0;
					try {
						if (typeof typeSpecs[typeSpecName] !== "function") {
							var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
							err.name = "Invariant Violation";
							throw err;
						}
						error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
					} catch (ex) {
						error$1 = ex;
					}
					if (error$1 && !(error$1 instanceof Error)) {
						setCurrentlyValidatingElement(element);
						error("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", componentName || "React class", location, typeSpecName, typeof error$1);
						setCurrentlyValidatingElement(null);
					}
					if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
						loggedTypeFailures[error$1.message] = true;
						setCurrentlyValidatingElement(element);
						error("Failed %s type: %s", location, error$1.message);
						setCurrentlyValidatingElement(null);
					}
				}
			}
			var isArrayImpl = Array.isArray;
			function isArray(a) {
				return isArrayImpl(a);
			}
			function typeName(value) {
				return typeof Symbol === "function" && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
			}
			function willCoercionThrow(value) {
				try {
					testStringCoercion(value);
					return false;
				} catch (e) {
					return true;
				}
			}
			function testStringCoercion(value) {
				return "" + value;
			}
			function checkKeyStringCoercion(value) {
				if (willCoercionThrow(value)) {
					error("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", typeName(value));
					return testStringCoercion(value);
				}
			}
			var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
			var RESERVED_PROPS = {
				key: true,
				ref: true,
				__self: true,
				__source: true
			};
			var specialPropKeyWarningShown;
			var specialPropRefWarningShown;
			var didWarnAboutStringRefs = {};
			function hasValidRef(config) {
				if (hasOwnProperty.call(config, "ref")) {
					var getter = Object.getOwnPropertyDescriptor(config, "ref").get;
					if (getter && getter.isReactWarning) return false;
				}
				return config.ref !== void 0;
			}
			function hasValidKey(config) {
				if (hasOwnProperty.call(config, "key")) {
					var getter = Object.getOwnPropertyDescriptor(config, "key").get;
					if (getter && getter.isReactWarning) return false;
				}
				return config.key !== void 0;
			}
			function warnIfStringRefCannotBeAutoConverted(config, self) {
				if (typeof config.ref === "string" && ReactCurrentOwner.current && self && ReactCurrentOwner.current.stateNode !== self) {
					var componentName = getComponentNameFromType(ReactCurrentOwner.current.type);
					if (!didWarnAboutStringRefs[componentName]) {
						error("Component \"%s\" contains the string ref \"%s\". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref", getComponentNameFromType(ReactCurrentOwner.current.type), config.ref);
						didWarnAboutStringRefs[componentName] = true;
					}
				}
			}
			function defineKeyPropWarningGetter(props, displayName) {
				var warnAboutAccessingKey = function() {
					if (!specialPropKeyWarningShown) {
						specialPropKeyWarningShown = true;
						error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
					}
				};
				warnAboutAccessingKey.isReactWarning = true;
				Object.defineProperty(props, "key", {
					get: warnAboutAccessingKey,
					configurable: true
				});
			}
			function defineRefPropWarningGetter(props, displayName) {
				var warnAboutAccessingRef = function() {
					if (!specialPropRefWarningShown) {
						specialPropRefWarningShown = true;
						error("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", displayName);
					}
				};
				warnAboutAccessingRef.isReactWarning = true;
				Object.defineProperty(props, "ref", {
					get: warnAboutAccessingRef,
					configurable: true
				});
			}
			/**
			* Factory method to create a new React element. This no longer adheres to
			* the class pattern, so do not use new to call it. Also, instanceof check
			* will not work. Instead test $$typeof field against Symbol.for('react.element') to check
			* if something is a React Element.
			*
			* @param {*} type
			* @param {*} props
			* @param {*} key
			* @param {string|object} ref
			* @param {*} owner
			* @param {*} self A *temporary* helper to detect places where `this` is
			* different from the `owner` when React.createElement is called, so that we
			* can warn. We want to get rid of owner and replace string `ref`s with arrow
			* functions, and as long as `this` and owner are the same, there will be no
			* change in behavior.
			* @param {*} source An annotation object (added by a transpiler or otherwise)
			* indicating filename, line number, and/or other information.
			* @internal
			*/
			var ReactElement = function(type, key, ref, self, source, owner, props) {
				var element = {
					$$typeof: REACT_ELEMENT_TYPE,
					type,
					key,
					ref,
					props,
					_owner: owner
				};
				element._store = {};
				Object.defineProperty(element._store, "validated", {
					configurable: false,
					enumerable: false,
					writable: true,
					value: false
				});
				Object.defineProperty(element, "_self", {
					configurable: false,
					enumerable: false,
					writable: false,
					value: self
				});
				Object.defineProperty(element, "_source", {
					configurable: false,
					enumerable: false,
					writable: false,
					value: source
				});
				if (Object.freeze) {
					Object.freeze(element.props);
					Object.freeze(element);
				}
				return element;
			};
			/**
			* https://github.com/reactjs/rfcs/pull/107
			* @param {*} type
			* @param {object} props
			* @param {string} key
			*/
			function jsxDEV(type, config, maybeKey, source, self) {
				var propName;
				var props = {};
				var key = null;
				var ref = null;
				if (maybeKey !== void 0) {
					checkKeyStringCoercion(maybeKey);
					key = "" + maybeKey;
				}
				if (hasValidKey(config)) {
					checkKeyStringCoercion(config.key);
					key = "" + config.key;
				}
				if (hasValidRef(config)) {
					ref = config.ref;
					warnIfStringRefCannotBeAutoConverted(config, self);
				}
				for (propName in config) if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) props[propName] = config[propName];
				if (type && type.defaultProps) {
					var defaultProps = type.defaultProps;
					for (propName in defaultProps) if (props[propName] === void 0) props[propName] = defaultProps[propName];
				}
				if (key || ref) {
					var displayName = typeof type === "function" ? type.displayName || type.name || "Unknown" : type;
					if (key) defineKeyPropWarningGetter(props, displayName);
					if (ref) defineRefPropWarningGetter(props, displayName);
				}
				return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
			}
			var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
			var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
			function setCurrentlyValidatingElement$1(element) {
				if (element) {
					var owner = element._owner;
					var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
					ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
				} else ReactDebugCurrentFrame$1.setExtraStackFrame(null);
			}
			var propTypesMisspellWarningShown = false;
			/**
			* Verifies the object is a ReactElement.
			* See https://reactjs.org/docs/react-api.html#isvalidelement
			* @param {?object} object
			* @return {boolean} True if `object` is a ReactElement.
			* @final
			*/
			function isValidElement(object) {
				return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
			}
			function getDeclarationErrorAddendum() {
				if (ReactCurrentOwner$1.current) {
					var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);
					if (name) return "\n\nCheck the render method of `" + name + "`.";
				}
				return "";
			}
			function getSourceInfoErrorAddendum(source) {
				if (source !== void 0) {
					var fileName = source.fileName.replace(/^.*[\\\/]/, "");
					var lineNumber = source.lineNumber;
					return "\n\nCheck your code at " + fileName + ":" + lineNumber + ".";
				}
				return "";
			}
			/**
			* Warn if there's no key explicitly set on dynamic arrays of children or
			* object keys are not valid. This allows us to keep track of children between
			* updates.
			*/
			var ownerHasKeyUseWarning = {};
			function getCurrentComponentErrorInfo(parentType) {
				var info = getDeclarationErrorAddendum();
				if (!info) {
					var parentName = typeof parentType === "string" ? parentType : parentType.displayName || parentType.name;
					if (parentName) info = "\n\nCheck the top-level render call using <" + parentName + ">.";
				}
				return info;
			}
			/**
			* Warn if the element doesn't have an explicit key assigned to it.
			* This element is in an array. The array could grow and shrink or be
			* reordered. All children that haven't already been validated are required to
			* have a "key" property assigned to it. Error statuses are cached so a warning
			* will only be shown once.
			*
			* @internal
			* @param {ReactElement} element Element that requires a key.
			* @param {*} parentType element's parent's type.
			*/
			function validateExplicitKey(element, parentType) {
				if (!element._store || element._store.validated || element.key != null) return;
				element._store.validated = true;
				var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
				if (ownerHasKeyUseWarning[currentComponentErrorInfo]) return;
				ownerHasKeyUseWarning[currentComponentErrorInfo] = true;
				var childOwner = "";
				if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
				setCurrentlyValidatingElement$1(element);
				error("Each child in a list should have a unique \"key\" prop.%s%s See https://reactjs.org/link/warning-keys for more information.", currentComponentErrorInfo, childOwner);
				setCurrentlyValidatingElement$1(null);
			}
			/**
			* Ensure that every element either is passed in a static location, in an
			* array with an explicit keys property defined, or in an object literal
			* with valid key property.
			*
			* @internal
			* @param {ReactNode} node Statically passed child of any type.
			* @param {*} parentType node's parent's type.
			*/
			function validateChildKeys(node, parentType) {
				if (typeof node !== "object") return;
				if (isArray(node)) for (var i = 0; i < node.length; i++) {
					var child = node[i];
					if (isValidElement(child)) validateExplicitKey(child, parentType);
				}
				else if (isValidElement(node)) {
					if (node._store) node._store.validated = true;
				} else if (node) {
					var iteratorFn = getIteratorFn(node);
					if (typeof iteratorFn === "function") {
						if (iteratorFn !== node.entries) {
							var iterator = iteratorFn.call(node);
							var step;
							while (!(step = iterator.next()).done) if (isValidElement(step.value)) validateExplicitKey(step.value, parentType);
						}
					}
				}
			}
			/**
			* Given an element, validate that its props follow the propTypes definition,
			* provided by the type.
			*
			* @param {ReactElement} element
			*/
			function validatePropTypes(element) {
				var type = element.type;
				if (type === null || type === void 0 || typeof type === "string") return;
				var propTypes;
				if (typeof type === "function") propTypes = type.propTypes;
				else if (typeof type === "object" && (type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_MEMO_TYPE)) propTypes = type.propTypes;
				else return;
				if (propTypes) {
					var name = getComponentNameFromType(type);
					checkPropTypes(propTypes, element.props, "prop", name, element);
				} else if (type.PropTypes !== void 0 && !propTypesMisspellWarningShown) {
					propTypesMisspellWarningShown = true;
					error("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", getComponentNameFromType(type) || "Unknown");
				}
				if (typeof type.getDefaultProps === "function" && !type.getDefaultProps.isReactClassApproved) error("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
			}
			/**
			* Given a fragment, validate that it can only be provided with fragment props
			* @param {ReactElement} fragment
			*/
			function validateFragmentProps(fragment) {
				var keys = Object.keys(fragment.props);
				for (var i = 0; i < keys.length; i++) {
					var key = keys[i];
					if (key !== "children" && key !== "key") {
						setCurrentlyValidatingElement$1(fragment);
						error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", key);
						setCurrentlyValidatingElement$1(null);
						break;
					}
				}
				if (fragment.ref !== null) {
					setCurrentlyValidatingElement$1(fragment);
					error("Invalid attribute `ref` supplied to `React.Fragment`.");
					setCurrentlyValidatingElement$1(null);
				}
			}
			var didWarnAboutKeySpread = {};
			function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
				var validType = isValidElementType(type);
				if (!validType) {
					var info = "";
					if (type === void 0 || typeof type === "object" && type !== null && Object.keys(type).length === 0) info += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
					var sourceInfo = getSourceInfoErrorAddendum(source);
					if (sourceInfo) info += sourceInfo;
					else info += getDeclarationErrorAddendum();
					var typeString;
					if (type === null) typeString = "null";
					else if (isArray(type)) typeString = "array";
					else if (type !== void 0 && type.$$typeof === REACT_ELEMENT_TYPE) {
						typeString = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />";
						info = " Did you accidentally export a JSX literal instead of a component?";
					} else typeString = typeof type;
					error("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", typeString, info);
				}
				var element = jsxDEV(type, props, key, source, self);
				if (element == null) return element;
				if (validType) {
					var children = props.children;
					if (children !== void 0) {
						if (isStaticChildren) {
							if (isArray(children)) {
								for (var i = 0; i < children.length; i++) validateChildKeys(children[i], type);
								if (Object.freeze) Object.freeze(children);
							} else error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
						} else validateChildKeys(children, type);
					}
				}
				if (hasOwnProperty.call(props, "key")) {
					var componentName = getComponentNameFromType(type);
					var keys = Object.keys(props).filter(function(k) {
						return k !== "key";
					});
					var beforeExample = keys.length > 0 ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
					if (!didWarnAboutKeySpread[componentName + beforeExample]) {
						error("A props object containing a \"key\" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />", beforeExample, componentName, keys.length > 0 ? "{" + keys.join(": ..., ") + ": ...}" : "{}", componentName);
						didWarnAboutKeySpread[componentName + beforeExample] = true;
					}
				}
				if (type === REACT_FRAGMENT_TYPE) validateFragmentProps(element);
				else validatePropTypes(element);
				return element;
			}
			function jsxWithValidationStatic(type, props, key) {
				return jsxWithValidation(type, props, key, true);
			}
			function jsxWithValidationDynamic(type, props, key) {
				return jsxWithValidation(type, props, key, false);
			}
			var jsx = jsxWithValidationDynamic;
			var jsxs = jsxWithValidationStatic;
			exports.Fragment = REACT_FRAGMENT_TYPE;
			exports.jsx = jsx;
			exports.jsxs = jsxs;
		})();
	}));
	//#endregion
	//#region ../node_modules/react/jsx-runtime.js
	var require_jsx_runtime = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_react_jsx_runtime_development();
	}));
	//#endregion
	//#region src/auth/AuthContext.jsx
	/**
	* Session state for the whole app.
	*
	* The user object (including `capabilities`) comes from the server; this context only caches
	* it for rendering.  Hiding a button is a courtesy, never a control — every route re-checks
	* on the server (docs/SECURITY.md).
	*/
	function AuthProvider({ children }) {
		const [user, setUser] = (0, import_react$23.useState)(null);
		const [status, setStatus] = (0, import_react$23.useState)("loading");
		const [notice, setNotice] = (0, import_react$23.useState)(null);
		const applySession = (0, import_react$23.useCallback)((session) => {
			setBearer(session?.token ?? null);
			setCsrf(session?.csrfToken ?? null);
			setUser(session?.user ?? session ?? null);
		}, []);
		const bootstrap = (0, import_react$23.useCallback)(async () => {
			try {
				const me = await auth.me();
				setCsrf(me.csrfToken ?? null);
				setUser(me);
				setStatus("authenticated");
			} catch (err) {
				setCsrf(null);
				setUser(null);
				setStatus("anonymous");
			}
		}, []);
		(0, import_react$23.useEffect)(() => {
			bootstrap();
			setUnauthorizedHandler(() => {
				setStatus((current) => {
					if (current !== "authenticated") return current;
					setNotice("Your session expired. Sign in again to continue where you left off.");
					setUser(null);
					setCsrf(null);
					setBearer(null);
					return "anonymous";
				});
			});
			return () => setUnauthorizedHandler(null);
		}, [bootstrap]);
		const login = (0, import_react$23.useCallback)(async ({ email, password, remember }) => {
			const session = await auth.login(email, password, !!remember);
			applySession(session);
			setNotice(null);
			setStatus("authenticated");
			return session;
		}, [applySession]);
		const logout = (0, import_react$23.useCallback)(async () => {
			try {
				await auth.logout();
			} catch {}
			setUser(null);
			setCsrf(null);
			setBearer(null);
			setStatus("anonymous");
		}, []);
		const refresh = (0, import_react$23.useCallback)(async () => {
			try {
				setUser(await auth.me());
			} catch {}
		}, []);
		const value = (0, import_react$23.useMemo)(() => ({
			user,
			status,
			notice,
			clearNotice: () => setNotice(null),
			login,
			logout,
			refresh,
			isAuthenticated: status === "authenticated" && !!user,
			/** UI-level permission check. Mirrors the server, never replaces it. */
			can: (capability) => !!user?.capabilities?.includes(capability),
			is: (role) => user?.roleCode === role,
			role: user?.roleCode ?? null
		}), [
			user,
			status,
			notice,
			login,
			logout,
			refresh
		]);
		return /* @__PURE__ */ (0, import_jsx_runtime$25.jsx)(AuthContext.Provider, {
			value,
			children
		});
	}
	function useAuth() {
		const ctx = (0, import_react$23.useContext)(AuthContext);
		if (!ctx) throw new Error("useAuth must be used inside <AuthProvider>");
		return ctx;
	}
	var import_react$23, import_jsx_runtime$25, AuthContext;
	var init_AuthContext = __esmMin((() => {
		import_react$23 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_client();
		import_jsx_runtime$25 = require_jsx_runtime();
		AuthContext = (0, import_react$23.createContext)(null);
	}));
	//#endregion
	//#region src/utils/format.js
	function formatDateTime(value, { withSeconds = false } = {}) {
		if (!value) return "—";
		const d = new Date(value);
		if (Number.isNaN(d.getTime())) return String(value);
		const opts = {
			year: "numeric",
			month: "short",
			day: "numeric",
			hour: "2-digit",
			minute: "2-digit",
			hour12: false
		};
		if (withSeconds) opts.second = "2-digit";
		return new Intl.DateTimeFormat(void 0, opts).format(d);
	}
	function formatDate(value) {
		if (!value) return "—";
		const s = String(value).slice(0, 10);
		const d = /* @__PURE__ */ new Date(`${s}T00:00:00Z`);
		if (Number.isNaN(d.getTime())) return s;
		return new Intl.DateTimeFormat(void 0, {
			year: "numeric",
			month: "short",
			day: "numeric",
			timeZone: "UTC"
		}).format(d);
	}
	/** "3 h ago" for timelines; falls back to a date beyond a week. */
	function relativeTime(value) {
		if (!value) return "—";
		const then = new Date(value).getTime();
		if (Number.isNaN(then)) return String(value);
		const mins = Math.round((Date.now() - then) / 6e4);
		if (mins < 1) return "just now";
		if (mins < 60) return `${mins} min ago`;
		const hours = Math.round(mins / 60);
		if (hours < 24) return `${hours} h ago`;
		const days = Math.round(hours / 24);
		if (days <= 7) return days === 1 ? "yesterday" : `${days} days ago`;
		return formatDate(value);
	}
	/** Minutes → "2 h 15 m"; the unit is part of the label so the number stays readable. */
	function formatDuration(minutes) {
		if (minutes === null || minutes === void 0 || Number.isNaN(Number(minutes))) return "—";
		const m = Math.round(Number(minutes));
		if (m < 60) return `${m} min`;
		const h = Math.floor(m / 60);
		const rem = m % 60;
		if (h < 48) return rem ? `${h} h ${rem} m` : `${h} h`;
		const d = Math.floor(h / 24);
		const hrs = h % 24;
		return hrs ? `${d} d ${hrs} h` : `${d} d`;
	}
	function formatHours(hours) {
		if (hours === null || hours === void 0 || Number.isNaN(Number(hours))) return "—";
		return formatDuration(Number(hours) * 60);
	}
	function formatMoney(amount, currency = "USD") {
		if (amount === null || amount === void 0) return "—";
		try {
			return new Intl.NumberFormat(void 0, {
				style: "currency",
				currency: currency || "USD",
				maximumFractionDigits: 2
			}).format(Number(amount) || 0);
		} catch {
			return `${currency} ${Number(amount).toFixed(2)}`;
		}
	}
	function formatBytes(bytes) {
		if (!bytes && bytes !== 0) return "—";
		const units = [
			"B",
			"KB",
			"MB",
			"GB"
		];
		let i = 0;
		let n = Number(bytes);
		while (n >= 1024 && i < units.length - 1) {
			n /= 1024;
			i += 1;
		}
		return `${n >= 10 || i === 0 ? Math.round(n) : n.toFixed(1)} ${units[i]}`;
	}
	/** Month axis labels: "2026-09" → "Sep". Keeps charts readable without a date library. */
	function monthLabel(key) {
		if (!key) return "";
		const [y, m] = String(key).split("-");
		const d = new Date(Date.UTC(Number(y), Number(m) - 1, 1));
		if (Number.isNaN(d.getTime())) return key;
		return new Intl.DateTimeFormat(void 0, {
			month: "short",
			timeZone: "UTC"
		}).format(d);
	}
	var cx, toDateInput, toDateTimeLocal, pluralise;
	var init_format = __esmMin((() => {
		cx = (...parts) => parts.filter(Boolean).join(" ");
		toDateInput = (value) => value ? String(value).slice(0, 10) : "";
		toDateTimeLocal = (value) => {
			if (!value) return "";
			const d = new Date(value);
			if (Number.isNaN(d.getTime())) return "";
			const pad = (n) => String(n).padStart(2, "0");
			return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`;
		};
		pluralise = (n, one, many = `${one}s`) => `${n} ${n === 1 ? one : many}`;
	}));
	//#endregion
	//#region src/components/Toast.jsx
	/**
	* Application toasts + a `<Callout>` primitive for in-page messages.
	* Toasts are deliberately transient and never the only channel: anything the user must be
	* able to act on later is also written to the notification centre by the server.
	*/
	function ToastProvider({ children }) {
		const [items, setItems] = (0, import_react$22.useState)([]);
		const next = (0, import_react$22.useRef)(1);
		const dismiss = (0, import_react$22.useCallback)((id) => setItems((list) => list.filter((t) => t.id !== id)), []);
		const push = (0, import_react$22.useCallback)((tone, message, opts = {}) => {
			const id = next.current++;
			setItems((list) => [...list.slice(-3), {
				id,
				tone,
				message,
				detail: opts.detail,
				sticky: !!opts.sticky
			}]);
			if (!opts.sticky) setTimeout(() => dismiss(id), opts.duration ?? 5200);
			return id;
		}, [dismiss]);
		const value = (0, import_react$22.useMemo)(() => ({
			push,
			success: (m, o) => push("ok", m, o),
			error: (m, o) => push("bad", m, {
				duration: 9e3,
				...o
			}),
			info: (m, o) => push("info", m, o),
			warn: (m, o) => push("warn", m, o),
			dismiss
		}), [push, dismiss]);
		return /* @__PURE__ */ (0, import_jsx_runtime$24.jsxs)(ToastContext.Provider, {
			value,
			children: [children, /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("div", {
				className: "toasts",
				role: "status",
				"aria-live": "polite",
				"aria-atomic": "false",
				children: items.map((t) => /* @__PURE__ */ (0, import_jsx_runtime$24.jsxs)("div", {
					className: cx("toast", `toast--${t.tone}`),
					children: [/* @__PURE__ */ (0, import_jsx_runtime$24.jsxs)("div", {
						className: "toast__body",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("p", {
							className: "toast__msg",
							children: t.message
						}), t.detail ? /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("p", {
							className: "toast__detail",
							children: t.detail
						}) : null]
					}), /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("button", {
						type: "button",
						className: "toast__close",
						onClick: () => dismiss(t.id),
						"aria-label": "Dismiss notification",
						children: "×"
					})]
				}, t.id))
			})]
		});
	}
	function useToast() {
		const ctx = (0, import_react$22.useContext)(ToastContext);
		if (!ctx) throw new Error("useToast must be used inside <ToastProvider>");
		return ctx;
	}
	function Callout({ tone = "info", title, children, actions, className, icon }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$24.jsxs)("div", {
			className: cx("callout", `callout--${tone}`, className),
			role: tone === "bad" ? "alert" : "note",
			children: [icon ? /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("span", {
				className: "callout__icon",
				"aria-hidden": "true",
				children: icon
			}) : null, /* @__PURE__ */ (0, import_jsx_runtime$24.jsxs)("div", {
				className: "callout__main",
				children: [
					title ? /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("p", {
						className: "callout__title",
						children: title
					}) : null,
					children ? /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("div", {
						className: "callout__body",
						children
					}) : null,
					actions ? /* @__PURE__ */ (0, import_jsx_runtime$24.jsx)("div", {
						className: "callout__actions",
						children: actions
					}) : null
				]
			})]
		});
	}
	/** Turns any thrown error into a sentence a lab user can act on. */
	function errorText(err) {
		if (!err) return "Something went wrong.";
		if (err.fieldErrors && Object.keys(err.fieldErrors).length) {
			const first = Object.entries(err.fieldErrors)[0];
			return `${first[0]}: ${Array.isArray(first[1]) ? first[1].join(" ") : first[1]}`;
		}
		if (err.status === 0) return "The server could not be reached. Check your connection and try again.";
		if (err.status === 401) return "Your session has expired. Sign in again.";
		if (err.status === 403) return err.message || "Your role is not permitted to do that.";
		if (err.status === 429) return err.message || "Too many requests — wait a moment and retry.";
		return err.message || "The request could not be completed.";
	}
	var import_react$22, import_jsx_runtime$24, ToastContext;
	var init_Toast = __esmMin((() => {
		import_react$22 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_format();
		import_jsx_runtime$24 = require_jsx_runtime();
		ToastContext = (0, import_react$22.createContext)(null);
	}));
	//#endregion
	//#region src/utils/constants.js
	var EQUIPMENT_STATUS, FAULT_STATUS, SEVERITY, MAINTENANCE_STATE, RISK_LEVEL, SAFETY_NOTICE;
	var init_constants = __esmMin((() => {
		EQUIPMENT_STATUS = {
			operational: {
				label: "Operational",
				tone: "ok",
				hint: "Ready for use"
			},
			reported_fault: {
				label: "Reported Fault",
				tone: "warn",
				hint: "A user has reported a problem"
			},
			under_inspection: {
				label: "Under Inspection",
				tone: "info",
				hint: "Being assessed by a technician"
			},
			under_repair: {
				label: "Under Repair",
				tone: "warn",
				hint: "Work in progress on the bench"
			},
			awaiting_parts: {
				label: "Awaiting Parts",
				tone: "warn",
				hint: "Blocked on a spare part"
			},
			out_of_service: {
				label: "Out of Service",
				tone: "bad",
				hint: "Deliberately unavailable"
			},
			decommissioned: {
				label: "Decommissioned",
				tone: "neutral",
				hint: "Written off / no longer maintained"
			}
		};
		FAULT_STATUS = {
			reported: {
				label: "Reported",
				tone: "warn",
				step: 1
			},
			assigned: {
				label: "Assigned",
				tone: "info",
				step: 2
			},
			acknowledged: {
				label: "Acknowledged",
				tone: "info",
				step: 3
			},
			under_inspection: {
				label: "Under Inspection",
				tone: "info",
				step: 4
			},
			under_repair: {
				label: "Under Repair",
				tone: "warn",
				step: 5
			},
			awaiting_parts: {
				label: "Awaiting Parts",
				tone: "warn",
				step: 6
			},
			repaired: {
				label: "Repaired",
				tone: "ok",
				step: 7
			},
			verified: {
				label: "Verified",
				tone: "ok",
				step: 8
			},
			closed: {
				label: "Closed",
				tone: "neutral",
				step: 9
			}
		};
		SEVERITY = {
			low: {
				label: "Low",
				tone: "neutral",
				hint: "Usable with care; fix when convenient"
			},
			medium: {
				label: "Medium",
				tone: "info",
				hint: "Affected teaching or use, not safety"
			},
			high: {
				label: "High",
				tone: "warn",
				hint: "Cannot be used as intended; someone is affected today"
			},
			critical: {
				label: "Critical",
				tone: "bad",
				hint: "Unsafe to use or a safety function is lost — stop using it and tell the desk now"
			}
		};
		MAINTENANCE_STATE = {
			up_to_date: {
				label: "Maintenance up to date",
				light: "🟢",
				tone: "ok"
			},
			due_soon: {
				label: "Maintenance due soon",
				light: "🟡",
				tone: "warn"
			},
			overdue: {
				label: "Maintenance overdue",
				light: "🔴",
				tone: "bad"
			},
			not_scheduled: {
				label: "No schedule configured",
				light: "⚪",
				tone: "neutral"
			}
		};
		RISK_LEVEL = {
			low: {
				label: "Low Risk",
				tone: "ok"
			},
			moderate: {
				label: "Moderate Risk",
				tone: "warn"
			},
			high: {
				label: "High Risk",
				tone: "bad"
			}
		};
		SAFETY_NOTICE = "This system records equipment status and maintenance history for the department. It does not assess patient safety, does not diagnose equipment faults, and does not replace inspection by a qualified biomedical engineer.";
	}));
	//#endregion
	//#region src/components/display.jsx
	/** Read-only display primitives: status is always a colour *and* a word, never colour alone. */
	function Badge({ tone = "neutral", children, dot, size = "md", className, title }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
			className: cx("badge", `badge--${tone}`, `badge--${size}`, className),
			title,
			children: [dot ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
				className: "badge__dot",
				"aria-hidden": "true"
			}) : null, children]
		});
	}
	function EquipmentStatusPill({ status, size = "md" }) {
		const meta = EQUIPMENT_STATUS[status] ?? {
			label: status ?? "Unknown",
			tone: "neutral"
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)(Badge, {
			tone: meta.tone,
			dot: true,
			size,
			title: meta.hint,
			children: meta.label
		});
	}
	function FaultStatusPill({ status, size = "md" }) {
		const meta = FAULT_STATUS[status] ?? {
			label: status,
			tone: "neutral"
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)(Badge, {
			tone: meta.tone,
			size,
			children: meta.label
		});
	}
	function SeverityPill({ severity, size = "md", showHint = false }) {
		const meta = SEVERITY[severity] ?? {
			label: severity,
			tone: "neutral"
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)(Badge, {
			tone: meta.tone,
			dot: true,
			size,
			title: showHint ? void 0 : meta.hint,
			children: [meta.label, showHint ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
				className: "sev-hint",
				children: meta.hint
			}) : null]
		});
	}
	function MaintenanceLight({ state, daysUntil, label, size = "md" }) {
		const meta = MAINTENANCE_STATE[state] ?? MAINTENANCE_STATE.not_scheduled;
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
			className: cx("pm-light", `pm-light--${meta.tone}`, `pm-light--${size}`),
			title: label ?? meta.label,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
					className: "pm-light__icon",
					"aria-hidden": "true",
					children: meta.light
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
					className: "pm-light__text",
					children: label ?? meta.label
				}),
				daysUntil !== void 0 && daysUntil !== null && state !== "not_scheduled" ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
					className: "pm-light__num",
					children: daysUntil < 0 ? `${Math.abs(daysUntil)} d late` : `${daysUntil} d left`
				}) : null
			]
		});
	}
	function RiskBadge({ level, score, size = "md" }) {
		const meta = RISK_LEVEL[level] ?? {
			label: "Not assessed",
			tone: "neutral"
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
			className: cx("risk", `risk--${meta.tone}`, `risk--${size}`),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)(RiskDial, {
				score,
				level,
				size: size === "lg" ? 62 : 34
			}), /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
				className: "risk__text",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
					className: "risk__level",
					children: meta.label
				}), score !== null && score !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
					className: "risk__score",
					children: [score, "/100"]
				}) : null]
			})]
		});
	}
	function RiskDial({ score, level, size = 40 }) {
		const value = Math.max(0, Math.min(100, Number(score ?? 0)));
		const r = size / 2 - 3;
		const c = 2 * Math.PI * r;
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("svg", {
			className: cx("dial", `dial--${level === "high" ? "bad" : level === "moderate" ? "warn" : "ok"}`),
			width: size,
			height: size,
			viewBox: `0 0 ${size} ${size}`,
			role: "img",
			"aria-label": `Maintenance risk score ${value} out of 100, ${level ?? "unknown"} risk`,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("circle", {
					cx: size / 2,
					cy: size / 2,
					r,
					className: "dial__track",
					strokeWidth: "4",
					fill: "none"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("circle", {
					cx: size / 2,
					cy: size / 2,
					r,
					className: "dial__value",
					strokeWidth: "4",
					fill: "none",
					strokeLinecap: "round",
					strokeDasharray: `${value / 100 * c} ${c}`,
					transform: `rotate(-90 ${size / 2} ${size / 2})`
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("text", {
					x: "50%",
					y: "52%",
					dominantBaseline: "middle",
					textAnchor: "middle",
					className: "dial__num",
					children: Math.round(value)
				})
			]
		});
	}
	function Card({ title, subtitle, actions, children, className, pad = true, as: Tag = "section", footer }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)(Tag, {
			className: cx("card", className),
			children: [
				title || actions ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("header", {
					className: "card__head",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("div", {
						className: "card__heading",
						children: [title ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("h2", {
							className: "card__title",
							children: title
						}) : null, subtitle ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("p", {
							className: "card__subtitle",
							children: subtitle
						}) : null]
					}), actions ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("div", {
						className: "card__actions",
						children: actions
					}) : null]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("div", {
					className: cx("card__body", !pad && "card__body--flush"),
					children
				}),
				footer ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("footer", {
					className: "card__foot",
					children: footer
				}) : null
			]
		});
	}
	function KeyValue({ items, columns = 2, className }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("dl", {
			className: cx("kv", `kv--${columns}`, className),
			children: items.filter(Boolean).map(([label, value, opts = {}]) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("div", {
				className: cx("kv__item", opts.wide && "kv__item--wide"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("dt", { children: label }), /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("dd", {
					className: opts.mono ? "mono" : void 0,
					children: value === null || value === void 0 || value === "" ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
						className: "muted",
						children: "Not recorded"
					}) : value
				})]
			}, label))
		});
	}
	function DataTable({ columns, rows, keyOf = (r, i) => r.id ?? i, onRowClick, loading, empty, className, dense, footer, caption }) {
		if (loading) return /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("div", {
			className: "table-skeleton",
			role: "status",
			"aria-live": "polite",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
				className: "sr-only",
				children: "Loading table"
			}), Array.from({ length: 5 }).map((_, i) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("div", {
				className: "skeleton skeleton--row",
				style: { width: `${96 - i * 7}%` }
			}, i))]
		});
		if (!rows?.length) return empty ?? null;
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("div", {
			className: cx("table-wrap", dense && "table-wrap--dense", className),
			children: /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("table", {
				className: "table",
				children: [
					caption ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("caption", {
						className: "sr-only",
						children: caption
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("tr", { children: columns.map((c) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("th", {
						scope: "col",
						className: cx(c.align && `is-${c.align}`, c.sortable && "is-sortable"),
						style: c.width ? { width: c.width } : void 0,
						children: c.sortable && c.onSort ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("button", {
							type: "button",
							className: "th-sort",
							onClick: () => c.onSort(c.key),
							"aria-label": `Sort by ${c.label}`,
							children: [
								c.label,
								" ",
								/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
									"aria-hidden": "true",
									children: c.dir === "asc" ? "▲" : c.dir === "desc" ? "▼" : "↕"
								})
							]
						}) : c.label
					}, c.key)) }) }),
					/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("tbody", { children: rows.map((row, i) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("tr", {
						className: cx(onRowClick && "is-clickable", row.rowClassName),
						onClick: onRowClick ? (e) => {
							if (!e.target.closest("a,button")) onRowClick(row);
						} : void 0,
						tabIndex: onRowClick ? 0 : void 0,
						onKeyDown: onRowClick ? (e) => {
							if (e.key === "Enter") onRowClick(row);
						} : void 0,
						children: columns.map((c) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("td", {
							className: cx(c.align && `is-${c.align}`, c.mono && "mono"),
							"data-label": c.label,
							children: c.render ? c.render(row, i) : row[c.key] ?? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
								className: "muted",
								children: "—"
							})
						}, c.key))
					}, keyOf(row, i))) }),
					footer ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("tfoot", { children: footer }) : null
				]
			})
		});
	}
	function Money({ amount, currency }) {
		if (amount === null || amount === void 0) return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
			className: "muted",
			children: "—"
		});
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
			className: "mono",
			children: formatMoney(amount, currency)
		});
	}
	function Timeline({ items, className }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("ol", {
			className: cx("timeline", className),
			children: items.map((item, i) => /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("li", {
				className: cx("timeline__item", item.tone && `timeline__item--${item.tone}`, item.isNote && "timeline__item--note"),
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("span", {
						className: "timeline__marker",
						"aria-hidden": "true",
						children: item.icon ?? "●"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("div", {
						className: "timeline__body",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("p", {
								className: "timeline__title",
								children: [item.title, item.actor ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsxs)("span", {
									className: "timeline__actor",
									children: [" · ", item.actor]
								}) : null]
							}),
							item.detail ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("p", {
								className: "timeline__detail",
								children: item.detail
							}) : null,
							item.meta ? /* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("p", {
								className: "timeline__meta",
								children: item.meta
							}) : null
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$23.jsx)("time", {
						className: "timeline__time",
						dateTime: item.at,
						title: formatDateTime(item.at, { withSeconds: true }),
						children: formatDateTime(item.at)
					})
				]
			}, item.id ?? i))
		});
	}
	var import_jsx_runtime$23;
	var init_display = __esmMin((() => {
		init_format();
		init_constants();
		import_jsx_runtime$23 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/components/ui.jsx
	/**
	* Form + action primitives.  Everything here is a real <button>/<input>, uses native
	* validation affordances where they help, and takes an explicit `invalid`/`describedBy`
	* contract so server-side field errors render the same way as client-side ones.
	*/
	function Button({ as: Tag = "button", tone = "primary", size = "md", icon, loading, children, className, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)(Tag, {
			...Tag === "button" ? { type: rest.type ?? "button" } : {},
			className: cx("btn", `btn--${tone}`, `btn--${size}`, loading && "btn--loading", className),
			"aria-busy": loading || void 0,
			disabled: Tag === "button" && (rest.disabled || loading),
			...rest,
			children: [
				icon ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "btn__icon",
					"aria-hidden": "true",
					children: icon
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "btn__label",
					children
				}),
				loading ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "btn__spinner",
					"aria-hidden": "true"
				}) : null
			]
		});
	}
	function IconButton({ label, children, tone = "ghost", className, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("button", {
			type: "button",
			className: cx("iconbtn", `iconbtn--${tone}`, className),
			"aria-label": label,
			title: label,
			...rest,
			children
		});
	}
	/** Label + control + error + help, wired with ids so screen readers get the whole story. */
	function Field({ label, hint, error, required, children, htmlFor, className, addon, counter }) {
		const id = (0, import_react$21.useId)();
		const inputId = htmlFor ?? id;
		const describedBy = [hint ? `${inputId}-hint` : null, error ? `${inputId}-error` : null].filter(Boolean).join(" ") || void 0;
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("field", error && "field--error", className),
			children: [
				label ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("label", {
					className: "field__label",
					htmlFor: inputId,
					children: [
						label,
						required ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
							className: "field__req",
							"aria-hidden": "true",
							children: " *"
						}) : null,
						required ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
							className: "sr-only",
							children: " (required)"
						}) : null
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
					className: "field__control",
					children: [typeof children === "function" ? children({
						id: inputId,
						describedBy
					}) : children, addon ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
						className: "field__addon",
						children: addon
					}) : null]
				}),
				hint && !error ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "field__hint",
					id: `${inputId}-hint`,
					children: hint
				}) : null,
				error ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "field__error",
					id: `${inputId}-error`,
					role: "alert",
					children: error
				}) : null,
				counter ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: cx("field__counter", counter.over && "field__counter--over"),
					children: counter.text
				}) : null
			]
		});
	}
	function TextInput({ error, className, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("input", {
			className: cx("input", error && "input--error", className),
			"aria-invalid": error ? "true" : void 0,
			...rest
		});
	}
	function TextArea({ error, rows = 4, className, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("textarea", {
			rows,
			className: cx("input", "textarea", error && "input--error", className),
			"aria-invalid": error ? "true" : void 0,
			...rest
		});
	}
	function Select({ error, options = [], placeholder, className, children, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("select", {
			className: cx("input", "select", error && "input--error", className),
			"aria-invalid": error ? "true" : void 0,
			...rest,
			children: [
				placeholder ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("option", {
					value: "",
					children: placeholder
				}) : null,
				(options || []).map((o) => {
					const value = typeof o === "object" ? o.value : o;
					const label = typeof o === "object" ? o.label : o;
					return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("option", {
						value: value ?? "",
						children: label
					}, String(value));
				}),
				children
			]
		});
	}
	function Checkbox({ label, hint, checked, onChange, disabled, className, ...rest }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("label", {
			className: cx("check", disabled && "check--disabled", className),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("input", {
					type: "checkbox",
					checked: !!checked,
					disabled,
					onChange: (e) => onChange?.(e.target.checked),
					...rest
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "check__box",
					"aria-hidden": "true"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("span", {
					className: "check__text",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
						className: "check__label",
						children: label
					}), hint ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
						className: "check__hint",
						children: hint
					}) : null]
				})
			]
		});
	}
	/** Segmented choice for short lists (severity, status) — much faster on a phone than a select. */
	function ChoiceChips({ name, value, onChange, options, size = "md", ariaLabel }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
			className: cx("chips", `chips--${size}`),
			role: "radiogroup",
			"aria-label": ariaLabel ?? name,
			children: options.map((o) => {
				const val = typeof o === "object" ? o.value : o;
				const opt = typeof o === "object" ? o : {
					value: o,
					label: o
				};
				const active = value === val;
				return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("button", {
					type: "button",
					role: "radio",
					"aria-checked": active,
					className: cx("chip", `chip--${opt.tone ?? "neutral"}`, active && "chip--active"),
					onClick: () => onChange(val),
					children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
						className: "chip__label",
						children: opt.label
					}), opt.hint ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
						className: "chip__hint",
						children: opt.hint
					}) : null]
				}, String(val));
			})
		});
	}
	function Spinner({ label = "Loading", size = "md" }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("span", {
			className: cx("spinner", `spinner--${size}`),
			role: "status",
			"aria-live": "polite",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
				className: "spinner__ring",
				"aria-hidden": "true"
			}), /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
				className: "sr-only",
				children: label
			})]
		});
	}
	function Loading({ label = "Loading", rows = 3, className } = {}) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("loading-block", className),
			role: "status",
			"aria-live": "polite",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
				className: "sr-only",
				children: label
			}), Array.from({ length: rows }).map((_, i) => /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
				className: "skeleton skeleton--row",
				style: { width: `${100 - i * 9}%` }
			}, i))]
		});
	}
	function EmptyState({ icon = "📋", title, description, action, className }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("empty", className),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "empty__icon",
					"aria-hidden": "true",
					children: icon
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "empty__title",
					children: title
				}),
				description ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "empty__desc",
					children: description
				}) : null,
				action ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
					className: "empty__action",
					children: action
				}) : null
			]
		});
	}
	function ErrorState({ error, onRetry, className }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("callout", "callout--bad", className),
			role: "alert",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
				className: "callout__icon",
				"aria-hidden": "true",
				children: "⚠"
			}), /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
				className: "callout__main",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
						className: "callout__title",
						children: error?.message || "This could not be loaded."
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
						className: "callout__body",
						children: /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", { children: error?.status ? `Server responded ${error.status}.` : "Check your connection and try again." })
					}),
					onRetry ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
						className: "callout__actions",
						children: /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
							tone: "secondary",
							size: "sm",
							onClick: onRetry,
							children: "Try again"
						})
					}) : null
				]
			})]
		});
	}
	/** Modal dialog: focus-trapped, Escape to close, returns focus to the opener. */
	function Modal({ open, title, description, onClose, children, footer, size = "md", labelId }) {
		const panel = (0, import_react$21.useRef)(null);
		const previouslyFocused = (0, import_react$21.useRef)(null);
		(0, import_react$21.useEffect)(() => {
			if (!open) return void 0;
			previouslyFocused.current = document.activeElement;
			const { body } = document;
			const prevOverflow = body.style.overflow;
			body.style.overflow = "hidden";
			const onKey = (e) => {
				if (e.key === "Escape") {
					e.preventDefault();
					onClose?.();
					return;
				}
				if (e.key !== "Tab" || !panel.current) return;
				const focusables = panel.current.querySelectorAll("a[href],button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex=\"-1\"])");
				if (!focusables.length) return;
				const first = focusables[0];
				const last = focusables[focusables.length - 1];
				if (e.shiftKey && document.activeElement === first) {
					e.preventDefault();
					last.focus();
				} else if (!e.shiftKey && document.activeElement === last) {
					e.preventDefault();
					first.focus();
				}
			};
			document.addEventListener("keydown", onKey);
			const t = setTimeout(() => panel.current?.querySelector("[data-autofocus],input,select,textarea,button")?.focus(), 20);
			return () => {
				document.removeEventListener("keydown", onKey);
				body.style.overflow = prevOverflow;
				clearTimeout(t);
				previouslyFocused.current?.focus?.();
			};
		}, [open, onClose]);
		if (!open) return null;
		return (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
			className: "modal",
			onMouseDown: (e) => {
				if (e.target === e.currentTarget) onClose?.();
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
				className: cx("modal__panel", `modal__panel--${size}`),
				role: "dialog",
				"aria-modal": "true",
				"aria-labelledby": labelId ?? "modal-title",
				ref: panel,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("header", {
						className: "modal__head",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("h2", {
							id: labelId ?? "modal-title",
							className: "modal__title",
							children: title
						}), description ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
							className: "modal__desc",
							children: description
						}) : null] }), /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(IconButton, {
							label: "Close dialog",
							onClick: () => onClose?.(),
							children: "×"
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
						className: "modal__body",
						children
					}),
					footer ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("footer", {
						className: "modal__foot",
						children: footer
					}) : null
				]
			})
		}), document.body);
	}
	/**
	* Destructive-action confirmation.  `confirmText` forces the user to type a word (asset tag,
	* email) for the irreversible cases — deleting a record, removing a user — because a stray
	* click on "Delete" should not be able to erase a department's history.
	*/
	function ConfirmDialog({ open, title = "Are you sure?", tone = "bad", body, children, confirmLabel = "Confirm", cancelLabel = "Cancel", requireText, busy, error, onConfirm, onCancel }) {
		const [typed, setTyped] = (0, import_react$21.useState)("");
		(0, import_react$21.useEffect)(() => {
			if (open) setTyped("");
		}, [open]);
		const ready = !requireText || typed.trim().toLowerCase() === String(requireText).trim().toLowerCase();
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)(Modal, {
			open,
			onClose: () => !busy && onCancel?.(),
			title,
			size: "sm",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)(import_jsx_runtime$22.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
				tone: "ghost",
				onClick: () => onCancel?.(),
				disabled: busy,
				children: cancelLabel
			}), /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
				tone: tone === "bad" ? "danger" : "primary",
				onClick: () => onConfirm?.({ confirmText: typed }),
				disabled: !ready || busy,
				loading: busy,
				"data-autofocus": true,
				children: confirmLabel
			})] }),
			children: [
				body ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "form-note",
					children: body
				}) : null,
				children,
				requireText ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Field, {
					label: `Type “${requireText}” to confirm`,
					hint: "This guard prevents losing a record by clicking the wrong button.",
					error: !ready && typed ? "That does not match." : null,
					children: /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(TextInput, {
						value: typed,
						onChange: (e) => setTyped(e.target.value),
						placeholder: requireText,
						autoComplete: "off"
					})
				}) : null,
				error ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
					className: "form-error",
					role: "alert",
					children: error
				}) : null
			]
		});
	}
	function Tabs({ tabs, active, onChange, className }) {
		const listRef = (0, import_react$21.useRef)(null);
		const onKeyDown = (e) => {
			if (![
				"ArrowRight",
				"ArrowLeft",
				"Home",
				"End"
			].includes(e.key)) return;
			const items = tabs.map((t) => t.id);
			const i = items.indexOf(active);
			const nextIndex = e.key === "ArrowRight" ? (i + 1) % items.length : e.key === "ArrowLeft" ? (i - 1 + items.length) % items.length : e.key === "Home" ? 0 : items.length - 1;
			e.preventDefault();
			onChange(items[nextIndex]);
			listRef.current?.querySelectorAll("[role=\"tab\"]")[nextIndex]?.focus();
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
			className: cx("tabs", className),
			role: "tablist",
			ref: listRef,
			onKeyDown,
			children: tabs.map((t) => /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("button", {
				type: "button",
				role: "tab",
				id: `tab-${t.id}`,
				"aria-selected": active === t.id,
				"aria-controls": `panel-${t.id}`,
				tabIndex: active === t.id ? 0 : -1,
				className: cx("tabs__item", active === t.id && "tabs__item--active"),
				onClick: () => onChange(t.id),
				children: [t.label, t.count !== void 0 && t.count !== null ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "tabs__count",
					children: t.count
				}) : null]
			}, t.id))
		});
	}
	function Pagination({ pagination, onPage, className }) {
		if (!pagination || pagination.pages <= 1) return null;
		const { page, pages, total, perPage } = pagination;
		const from = (page - 1) * perPage + 1;
		const to = Math.min(total, page * perPage);
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("nav", {
			className: cx("pager", className),
			"aria-label": "Pagination",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("p", {
				className: "pager__summary",
				children: total === 0 ? "No results" : /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)(import_jsx_runtime$22.Fragment, { children: [
					"Showing ",
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("b", { children: [
						from,
						"–",
						to
					] }),
					" of ",
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("b", { children: total })
				] })
			}), /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
				className: "pager__buttons",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
						size: "sm",
						tone: "ghost",
						disabled: page <= 1,
						onClick: () => onPage(page - 1),
						children: "← Previous"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("span", {
						className: "pager__page",
						"aria-live": "polite",
						children: [
							"Page ",
							page,
							" of ",
							pages
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
						size: "sm",
						tone: "ghost",
						disabled: page >= pages,
						onClick: () => onPage(page + 1),
						children: "Next →"
					})
				]
			})]
		});
	}
	/** Collapsible filter panel: full width on a phone, inline row on a desktop. */
	function FilterBar({ children, active, onClear, className }) {
		const [open, setOpen] = (0, import_react$21.useState)(false);
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("filters", className),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
				className: "filters__bar",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)(Button, {
					size: "sm",
					tone: active ? "primary" : "ghost",
					icon: "⚙",
					onClick: () => setOpen((v) => !v),
					"aria-expanded": open,
					"aria-controls": "filter-panel",
					children: ["Filters", active ? " · active" : ""]
				}), active ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(Button, {
					size: "sm",
					tone: "ghost",
					onClick: onClear,
					children: "Clear"
				}) : null]
			}), open ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("div", {
				id: "filter-panel",
				className: "filters__panel",
				children
			}) : null]
		});
	}
	/**
	* Debounced search input with an explicit submit, so a fast typist on a phone does not fire
	* six requests per word.
	*/
	function SearchInput({ value, onSearch, placeholder = "Search…", delay = 450, className }) {
		const [local, setLocal] = (0, import_react$21.useState)(value ?? "");
		const timer = (0, import_react$21.useRef)();
		(0, import_react$21.useEffect)(() => {
			setLocal(value ?? "");
		}, [value]);
		const commit = (0, import_react$21.useCallback)(() => {
			clearTimeout(timer.current);
			onSearch(local.trim());
		}, [local, onSearch]);
		return /* @__PURE__ */ (0, import_jsx_runtime$22.jsxs)("div", {
			className: cx("search", className),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("span", {
					className: "search__icon",
					"aria-hidden": "true",
					children: "⌕"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$22.jsx)("input", {
					type: "search",
					className: "input search__input",
					value: local,
					placeholder,
					"aria-label": placeholder,
					onChange: (e) => {
						const v = e.target.value;
						setLocal(v);
						clearTimeout(timer.current);
						timer.current = setTimeout(() => onSearch(v.trim()), delay);
					},
					onKeyDown: (e) => {
						if (e.key === "Enter") {
							e.preventDefault();
							commit();
						}
					}
				}),
				local ? /* @__PURE__ */ (0, import_jsx_runtime$22.jsx)(IconButton, {
					label: "Clear search",
					onClick: () => {
						setLocal("");
						onSearch("");
					},
					children: "×"
				}) : null
			]
		});
	}
	function useCopy() {
		const [copied, setCopied] = (0, import_react$21.useState)(false);
		return {
			copied,
			copy: (0, import_react$21.useCallback)(async (text) => {
				try {
					await navigator.clipboard.writeText(text);
					setCopied(true);
					setTimeout(() => setCopied(false), 1600);
				} catch {
					setCopied(false);
				}
			}, [])
		};
	}
	var import_react$21, import_react_dom, import_jsx_runtime$22;
	var init_ui = __esmMin((() => {
		import_react$21 = /* @__PURE__ */ __toESM(require_react(), 1);
		import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
		init_format();
		import_jsx_runtime$22 = require_jsx_runtime();
		init_Toast();
		init_display();
	}));
	//#endregion
	//#region src/test-stubs/jsqr.js
	var jsqr_exports = /* @__PURE__ */ __exportAll({ default: () => jsQRStub });
	/** Test-only stub: jsQR is a 90 KB decoder that cannot run on a jsdom canvas. */
	function jsQRStub() {
		return null;
	}
	var init_jsqr = __esmMin((() => {}));
	//#endregion
	//#region src/components/QrScanner.jsx
	/**
	* QR / manual identification (Phase 3).
	*
	* Strategy, in order of what the device supports:
	*   1. `BarcodeDetector` — the native decoder on modern Android Chrome: fast, no bundle cost.
	*   2. jsQR on a canvas — works in Safari/Firefox and in the "take a photo instead" path.
	*   3. Typing the code — the label always prints the asset tag under the QR, so a cracked
	*      lens or a dark cupboard is never a blocker.
	*
	* The camera stream is only ever touched here; frames are decoded in-memory and discarded.
	*/
	function QrScanner({ open, onClose, onFound, title = "Scan the equipment label" }) {
		const videoRef = (0, import_react$20.useRef)(null);
		const canvasRef = (0, import_react$20.useRef)(null);
		const streamRef = (0, import_react$20.useRef)(null);
		const rafRef = (0, import_react$20.useRef)(0);
		const [mode, setMode] = (0, import_react$20.useState)("camera");
		const [error, setError] = (0, import_react$20.useState)(null);
		const [supported, setSupported] = (0, import_react$20.useState)(false);
		const [manual, setManual] = (0, import_react$20.useState)("");
		const [busy, setBusy] = (0, import_react$20.useState)(false);
		const detectorRef = (0, import_react$20.useRef)(null);
		const stop = (0, import_react$20.useCallback)(() => {
			cancelAnimationFrame(rafRef.current);
			streamRef.current?.getTracks().forEach((t) => t.stop());
			streamRef.current = null;
		}, []);
		(0, import_react$20.useEffect)(() => {
			if (!open) {
				stop();
				return;
			}
			setError(null);
			setManual("");
			const hasNative = typeof window !== "undefined" && "BarcodeDetector" in window;
			setSupported(hasNative);
			setMode(hasNative ? "camera" : "photo");
			if (hasNative) try {
				detectorRef.current = new window.BarcodeDetector({ formats: ["qr_code"] });
			} catch {
				detectorRef.current = null;
			}
			return stop;
		}, [open, stop]);
		const startCamera = (0, import_react$20.useCallback)(async () => {
			setError(null);
			if (!navigator.mediaDevices?.getUserMedia) {
				setError("This browser cannot open the camera. Take a photo of the label instead, or type the code.");
				setMode("photo");
				return;
			}
			try {
				const stream = await navigator.mediaDevices.getUserMedia({
					video: {
						facingMode: { ideal: "environment" },
						width: { ideal: 1280 }
					},
					audio: false
				});
				streamRef.current = stream;
				const video = videoRef.current;
				if (video) {
					video.srcObject = stream;
					await video.play().catch(() => {});
				}
				tick();
			} catch (err) {
				const name = err?.name ?? "";
				setError(name === "NotAllowedError" ? "Camera permission was declined. Allow it for this site, or use “Photo” / “Type code” below." : name === "NotFoundError" ? "No camera was found on this device. Use “Photo” or “Type code”." : "The camera could not be started. Use “Photo” or “Type code”.");
				setMode("photo");
			}
		}, []);
		const decoderRef = (0, import_react$20.useRef)(null);
		const loadDecoder = (0, import_react$20.useCallback)(async () => {
			if (!decoderRef.current) {
				const mod = await Promise.resolve().then(() => (init_jsqr(), jsqr_exports));
				decoderRef.current = mod.default ?? mod;
			}
			return decoderRef.current;
		}, []);
		const handleDetected = (0, import_react$20.useCallback)((text) => {
			stop();
			const tag = parseTag(text);
			if (!tag) {
				setError("That does not look like an equipment label from this department. Try again or type the code.");
				return;
			}
			onFound?.(tag, text);
		}, [onFound, stop]);
		(0, import_react$20.useEffect)(() => {
			if (!open) return void 0;
			if (mode === "camera") startCamera();
			else stop();
			return stop;
		}, [
			open,
			mode,
			startCamera,
			stop
		]);
		const tick = (0, import_react$20.useCallback)(async () => {
			const video = videoRef.current;
			const canvas = canvasRef.current;
			if (!video || !canvas || video.readyState < 2) {
				rafRef.current = requestAnimationFrame(tick);
				return;
			}
			const jsQR = detectorRef.current ? null : await loadDecoder();
			const w = 480;
			const ratio = video.videoWidth ? w / video.videoWidth : 1;
			const h = Math.max(1, Math.round((video.videoHeight || w * .75) * ratio));
			canvas.width = w;
			canvas.height = h;
			const ctx = canvas.getContext("2d", { willReadFrequently: true });
			ctx.drawImage(video, 0, 0, w, h);
			try {
				if (detectorRef.current) {
					const found = await detectorRef.current.detect(video);
					if (found?.length) {
						handleDetected(found[0].rawValue ?? found[0].value);
						return;
					}
				} else {
					const result = jsQR(ctx.getImageData(0, 0, w, h).data, w, h, { inversionAttempts: "dontInvert" });
					if (result?.data) {
						handleDetected(result.data);
						return;
					}
				}
			} catch {}
			rafRef.current = requestAnimationFrame(tick);
		}, [handleDetected, loadDecoder]);
		const decodeFile = (0, import_react$20.useCallback)(async (file) => {
			if (!file) return;
			setBusy(true);
			setError(null);
			try {
				const bitmap = await createImageBitmap(file);
				const canvas = document.createElement("canvas");
				const scale = Math.min(1, 900 / Math.max(bitmap.width, bitmap.height));
				canvas.width = Math.round(bitmap.width * scale);
				canvas.height = Math.round(bitmap.height * scale);
				const ctx = canvas.getContext("2d", { willReadFrequently: true });
				ctx.drawImage(bitmap, 0, 0, canvas.width, canvas.height);
				const img = ctx.getImageData(0, 0, canvas.width, canvas.height);
				const result = (await loadDecoder())(img.data, canvas.width, canvas.height, { inversionAttempts: "attemptBoth" });
				if (!result?.data) throw new Error("no-code");
				handleDetected(result.data);
			} catch (err) {
				setError(err?.message === "no-code" ? "No QR code was found in that photo. Move closer to the label, avoid glare, and try again." : "That image could not be read. Try again, or type the code instead.");
			} finally {
				setBusy(false);
			}
		}, [handleDetected, loadDecoder]);
		return /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)(Modal, {
			open,
			onClose: () => {
				stop();
				onClose?.();
			},
			title,
			description: "Point the camera at the QR label on the equipment. The label also prints the asset tag if scanning is awkward.",
			size: "md",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)(import_jsx_runtime$21.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("div", {
				className: "segbar",
				role: "tablist",
				"aria-label": "Scanning method",
				children: [
					[
						"camera",
						"Camera",
						supported
					],
					[
						"photo",
						"Photo",
						true
					],
					[
						"manual",
						"Type code",
						true
					]
				].map(([key, label, enabled]) => /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)("button", {
					type: "button",
					role: "tab",
					"aria-selected": mode === key,
					disabled: !enabled,
					className: cx("segbar__btn", mode === key && "segbar__btn--active"),
					onClick: () => setMode(key),
					children: [label, !enabled ? " (unavailable)" : ""]
				}, key))
			}), /* @__PURE__ */ (0, import_jsx_runtime$21.jsx)(Button, {
				tone: "ghost",
				onClick: () => {
					stop();
					onClose?.();
				},
				children: "Close"
			})] }),
			children: [
				mode === "camera" ? /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)("div", {
					className: "scan-view",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("video", {
							ref: videoRef,
							playsInline: true,
							muted: true,
							className: "scan-view__video",
							"aria-label": "Camera preview of the equipment label"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("div", {
							className: "scan-view__frame",
							"aria-hidden": "true"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("canvas", {
							ref: canvasRef,
							className: "sr-only"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("p", {
							className: "scan-view__hint",
							children: "Hold steady · about 20 cm · fill the frame with the code"
						})
					]
				}) : null,
				mode === "photo" ? /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)("div", {
					className: "scan-alt",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("p", {
						className: "form-note",
						children: "Take or choose a clear photo of the label; the code is decoded on this device, nothing is uploaded."
					}), /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)("label", {
						className: "btn btn--primary scan-alt__btn",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("input", {
							type: "file",
							accept: "image/*",
							capture: "environment",
							className: "sr-only",
							onChange: (e) => decodeFile(e.target.files?.[0]),
							disabled: busy
						}), busy ? "Reading…" : "Use camera / choose photo"]
					})]
				}) : null,
				mode === "manual" ? /* @__PURE__ */ (0, import_jsx_runtime$21.jsxs)("form", {
					className: "scan-manual",
					onSubmit: (e) => {
						e.preventDefault();
						if (manual.trim()) handleDetected(manual.trim());
					},
					children: [/* @__PURE__ */ (0, import_jsx_runtime$21.jsx)(Field, {
						label: "Asset tag or scan URL",
						hint: "Printed under the QR code, for example BMU-ECG-0014.",
						error: null,
						children: /* @__PURE__ */ (0, import_jsx_runtime$21.jsx)(TextInput, {
							value: manual,
							onChange: (e) => setManual(e.target.value),
							placeholder: "BMU-ECG-0014",
							autoComplete: "off",
							autoCapitalize: "characters",
							spellCheck: false,
							"data-autofocus": true
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime$21.jsx)(Button, {
						type: "submit",
						tone: "primary",
						disabled: !manual.trim(),
						children: "Find equipment"
					})]
				}) : null,
				error ? /* @__PURE__ */ (0, import_jsx_runtime$21.jsx)("p", {
					className: "form-error",
					role: "alert",
					children: error
				}) : null
			]
		});
	}
	/** Accepts a bare tag, a full URL from the label, or a `BMEQ:<tag>` payload. */
	function parseTag(raw) {
		const text = String(raw ?? "").trim();
		if (!text) return null;
		const fromUrl = /\/e\/([^/?#]+)/i.exec(text);
		if (fromUrl) return decodeURIComponent(fromUrl[1]);
		const prefixed = /^BMEQ:\s*([A-Za-z0-9-]+)/i.exec(text);
		if (prefixed) return prefixed[1].toUpperCase();
		if (/^[A-Z]{2,4}-[A-Z0-9]{2,8}-\d{1,6}$/i.exec(text)) return text.toUpperCase();
		if (/^[A-Za-z0-9_-]{3,40}$/.test(text)) return text.toUpperCase();
		return null;
	}
	var import_react$20, import_jsx_runtime$21;
	var init_QrScanner = __esmMin((() => {
		import_react$20 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_ui();
		init_format();
		import_jsx_runtime$21 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/components/AppShell.jsx
	function AppShell({ children }) {
		const { user, can, logout, is, notice, clearNotice } = useAuth();
		const navigate = useNavigate();
		const location = useLocation();
		const [menuOpen, setMenuOpen] = (0, import_react$19.useState)(false);
		const [scanOpen, setScanOpen] = (0, import_react$19.useState)(false);
		const [bellOpen, setBellOpen] = (0, import_react$19.useState)(false);
		const [unread, setUnread] = (0, import_react$19.useState)(0);
		const [inbox, setInbox] = (0, import_react$19.useState)([]);
		const [demo, setDemo] = (0, import_react$19.useState)(null);
		const [query, setQuery] = (0, import_react$19.useState)("");
		const [userOpen, setUserOpen] = (0, import_react$19.useState)(false);
		const bellRef = (0, import_react$19.useRef)(null);
		const userRef = (0, import_react$19.useRef)(null);
		const items = (0, import_react$19.useMemo)(() => {
			const visible = NAV.filter((n) => (n.cap ? can(n.cap) : true) && (!n.reporterOnly || is("reporter") && !can("fault.view.any")));
			return {
				main: visible.filter((n) => n.group === "main" || n.group === "work"),
				admin: visible.filter((n) => n.group === "admin"),
				all: visible
			};
		}, [can, is]);
		const refreshUnread = (0, import_react$19.useCallback)(async () => {
			try {
				setUnread((await notifications.unread()).unread ?? 0);
			} catch {}
		}, []);
		(0, import_react$19.useEffect)(() => {
			refreshUnread();
			const id = setInterval(refreshUnread, 6e4);
			return () => clearInterval(id);
		}, [refreshUnread, location.pathname]);
		(0, import_react$19.useEffect)(() => {
			let alive = true;
			publicApi.config().then((c) => {
				if (alive) setDemo(c);
			}).catch(() => {});
			return () => {
				alive = false;
			};
		}, []);
		(0, import_react$19.useEffect)(() => {
			setMenuOpen(false);
			setBellOpen(false);
		}, [location.pathname]);
		(0, import_react$19.useEffect)(() => {
			if (!bellOpen && !userOpen) return void 0;
			const onDown = (e) => {
				if (bellOpen && bellRef.current && !bellRef.current.contains(e.target)) setBellOpen(false);
				if (userOpen && userRef.current && !userRef.current.contains(e.target)) setUserOpen(false);
			};
			document.addEventListener("mousedown", onDown);
			notifications.list({ limit: 12 }).then((r) => setInbox(r.items ?? [])).catch(() => {});
			return () => document.removeEventListener("mousedown", onDown);
		}, [bellOpen]);
		const onFound = (tag) => {
			setScanOpen(false);
			navigate(`/e/${encodeURIComponent(tag)}`);
		};
		const submitSearch = (e) => {
			e.preventDefault();
			const term = query.trim();
			if (!term) return;
			const tag = parseTag(term);
			if (tag && /^[A-Z]{2,4}-[A-Z0-9]{2,8}-\d+$/i.test(tag)) navigate(`/e/${encodeURIComponent(tag)}`);
			else navigate(`/equipment?q=${encodeURIComponent(term)}`);
			setQuery("");
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
			className: "shell",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("a", {
					className: "skip-link",
					href: "#main",
					children: "Skip to main content"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("aside", {
					className: cx("sidebar", menuOpen && "sidebar--open"),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
							className: "sidebar__brand",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(Link, {
								to: "/",
								className: "brand",
								"aria-label": "BEM-FRS dashboard",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
									className: "brand__mark",
									"aria-hidden": "true",
									children: /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("svg", {
										viewBox: "0 0 32 32",
										width: "26",
										height: "26",
										children: /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("path", {
											d: "M3 17h5l2.4-7 3.3 13.4L17 13l2 4h10",
											fill: "none",
											stroke: "currentColor",
											strokeWidth: "2.4",
											strokeLinecap: "round",
											strokeLinejoin: "round"
										})
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("span", {
									className: "brand__text",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("strong", { children: "BEM-FRS" }), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("small", { children: demo?.department ?? "Biomedical Engineering" })]
								})]
							}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(IconButton, {
								label: "Close menu",
								className: "sidebar__close",
								onClick: () => setMenuOpen(false),
								children: "×"
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("nav", {
							className: "sidebar__nav",
							"aria-label": "Main navigation",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(NavGroup, { items: items.main }), items.admin.length ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(import_jsx_runtime$20.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("p", {
								className: "sidebar__group-label",
								children: "Administration"
							}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(NavGroup, { items: items.admin })] }) : null]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
							className: "sidebar__foot",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(Button, {
								tone: "primary",
								className: "sidebar__scan",
								icon: "▣",
								onClick: () => setScanOpen(true),
								children: "Scan equipment label"
							}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("p", {
								className: "sidebar__notice",
								children: [SAFETY_NOTICE.split(".")[0], "."]
							})]
						})
					]
				}),
				menuOpen ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("div", {
					className: "scrim",
					onClick: () => setMenuOpen(false),
					"aria-hidden": "true"
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
					className: "shell__main",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("header", {
							className: "topbar",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(IconButton, {
									label: "Open menu",
									className: "topbar__menu",
									onClick: () => setMenuOpen(true),
									children: "☰"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("form", {
									className: "topbar__search",
									onSubmit: submitSearch,
									role: "search",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
										className: "topbar__search-icon",
										"aria-hidden": "true",
										children: "⌕"
									}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("input", {
										className: "input topbar__search-input",
										type: "search",
										value: query,
										onChange: (e) => setQuery(e.target.value),
										placeholder: "Search equipment, asset tag, serial…",
										"aria-label": "Search equipment by name, tag or serial number"
									})]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
									className: "topbar__right",
									children: [
										/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(IconButton, {
											label: "Scan a QR label",
											className: "topbar__scan",
											onClick: () => setScanOpen(true),
											children: "▣"
										}),
										/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
											className: "bell",
											ref: bellRef,
											children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("button", {
												type: "button",
												className: "bell__btn",
												onClick: () => setBellOpen((v) => !v),
												"aria-expanded": bellOpen,
												"aria-haspopup": "true",
												"aria-label": `Notifications${unread ? `, ${unread} unread` : ""}`,
												children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
													"aria-hidden": "true",
													children: "◍"
												}), unread > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
													className: "bell__count",
													children: unread > 99 ? "99+" : unread
												}) : null]
											}), bellOpen ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
												className: "bell__panel",
												role: "dialog",
												"aria-label": "Recent notifications",
												children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
													className: "bell__head",
													children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("strong", { children: "Notifications" }), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(Link, {
														to: "/notifications",
														children: "View all"
													})]
												}), inbox.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("p", {
													className: "bell__empty",
													children: "Nothing waiting. That is usually good news."
												}) : /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("ul", {
													className: "bell__list",
													children: inbox.map((n) => /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(Link, {
														to: n.link ?? "/notifications",
														className: cx("bell__item", n.unread && "bell__item--unread"),
														children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
															className: cx("bell__tone", `bell__tone--${n.severity}`),
															"aria-hidden": "true"
														}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("span", {
															className: "bell__body",
															children: [
																/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
																	className: "bell__title",
																	children: n.title
																}),
																/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
																	className: "bell__text",
																	children: (n.body ?? "").slice(0, 110)
																}),
																/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
																	className: "bell__time",
																	children: relativeTime(n.createdAt)
																})
															]
														})]
													}) }, n.id))
												})]
											}) : null]
										}),
										/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
											className: "usermenu",
											ref: userRef,
											children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("button", {
												type: "button",
												className: "usermenu__btn",
												onClick: () => setUserOpen((v) => !v),
												"aria-expanded": userOpen,
												"aria-haspopup": "true",
												"aria-label": "Account menu",
												children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
													className: "avatar",
													"aria-hidden": "true",
													children: (user?.fullName ?? "?").split(" ").map((w) => w[0]).slice(0, 2).join("")
												}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("span", {
													className: "usermenu__text",
													children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("strong", { children: user?.fullName }), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("small", { children: user?.roleLabel })]
												})]
											}), userOpen ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
												className: "usermenu__panel",
												children: [
													/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("p", {
														className: "usermenu__who",
														children: [
															user?.email,
															/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("br", {}),
															/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
																className: "muted",
																children: user?.jobTitle ?? user?.department
															})
														]
													}),
													/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(Link, {
														to: "/profile",
														children: "Profile & security"
													}),
													/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(Link, {
														to: "/profile?tab=sessions",
														children: [
															"Active sessions (",
															user?.activeSessions ?? "…",
															")"
														]
													}),
													/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("button", {
														type: "button",
														onClick: async () => {
															await logout();
															navigate("/login");
														},
														children: "Sign out"
													})
												]
											}) : null]
										})
									]
								})
							]
						}),
						notice ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
							className: "banner banner--warn",
							role: "alert",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", { children: notice }), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("button", {
								type: "button",
								onClick: clearNotice,
								"aria-label": "Dismiss message",
								children: "×"
							})]
						}) : null,
						demo?.demoData ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
							className: "banner banner--demo",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("strong", { children: "DEMO DATA" }),
								" — every item, fault and cost below is fictional sample data installed for evaluation.",
								/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(Link, {
									to: "/reference?tab=settings",
									children: "Turn the banner off"
								})
							]
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("main", {
							id: "main",
							className: "shell__content",
							children
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("nav", {
							className: "tabbar",
							"aria-label": "Primary",
							children: [items.main.slice(0, 4).map((n) => /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(NavLink, {
								to: n.to,
								end: n.exact,
								className: ({ isActive }) => cx("tabbar__item", isActive && "tabbar__item--active"),
								children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
									"aria-hidden": "true",
									children: n.icon
								}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", { children: n.label.split(" ")[0] })]
							}, n.to)), /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("button", {
								type: "button",
								className: "tabbar__item tabbar__item--scan",
								onClick: () => setScanOpen(true),
								children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
									"aria-hidden": "true",
									children: "▣"
								}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", { children: "Scan" })]
							})]
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)(QrScanner, {
					open: scanOpen,
					onClose: () => setScanOpen(false),
					onFound
				})
			]
		});
	}
	function NavGroup({ items }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("ul", {
			className: "navlist",
			children: items.map((n) => /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(NavLink, {
				to: n.to,
				end: n.exact,
				className: ({ isActive }) => cx("navlink", isActive && "navlink--active"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
					className: "navlink__icon",
					"aria-hidden": "true",
					children: n.icon
				}), /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("span", {
					className: "navlink__label",
					children: n.label
				})]
			}) }, n.to))
		});
	}
	function PageHeader({ eyebrow, title, description, actions, back }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
			className: "page-head",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)("div", {
				className: "page-head__main",
				children: [
					back ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsxs)(Link, {
						to: back.to,
						className: "page-head__back",
						children: ["← ", back.label]
					}) : null,
					eyebrow ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("p", {
						className: "page-head__eyebrow",
						children: eyebrow
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("h1", { children: title }),
					description ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("p", {
						className: "page-head__desc",
						children: description
					}) : null
				]
			}), actions ? /* @__PURE__ */ (0, import_jsx_runtime$20.jsx)("div", {
				className: "page-head__actions",
				children: actions
			}) : null]
		});
	}
	var import_react$19, import_jsx_runtime$20, NAV;
	var init_AppShell = __esmMin((() => {
		import_react$19 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_AuthContext();
		init_client();
		init_ui();
		init_QrScanner();
		init_format();
		init_constants();
		import_jsx_runtime$20 = require_jsx_runtime();
		NAV = [
			{
				to: "/",
				label: "Dashboard",
				icon: "◧",
				exact: true,
				cap: "dashboard.view",
				group: "main"
			},
			{
				to: "/equipment",
				label: "Equipment",
				icon: "⚕",
				cap: "equipment.view",
				group: "main"
			},
			{
				to: "/faults",
				label: "Fault reports",
				icon: "⚑",
				cap: "fault.create",
				group: "main"
			},
			{
				to: "/my-reports",
				label: "My reports",
				icon: "☰",
				cap: "fault.view.own",
				group: "main",
				reporterOnly: true
			},
			{
				to: "/work",
				label: "Work queue",
				icon: "⚒",
				cap: "fault.transition.technical",
				group: "work"
			},
			{
				to: "/maintenance",
				label: "Maintenance",
				icon: "◷",
				cap: "maintenance.view",
				group: "work"
			},
			{
				to: "/risk",
				label: "Risk register",
				icon: "△",
				cap: "risk.view",
				group: "work"
			},
			{
				to: "/reports",
				label: "Reports",
				icon: "▤",
				cap: "report.generate",
				group: "admin"
			},
			{
				to: "/users",
				label: "Users",
				icon: "☺",
				cap: "user.manage",
				group: "admin"
			},
			{
				to: "/reference",
				label: "Configuration",
				icon: "⚙",
				cap: "meta.manage",
				group: "admin"
			},
			{
				to: "/audit",
				label: "Audit log",
				icon: "⌸",
				cap: "audit.view",
				group: "admin"
			}
		];
	}));
	//#endregion
	//#region src/pages/LoginPage.jsx
	var import_client = require_client();
	init_AppShell();
	var import_react$18 = /* @__PURE__ */ __toESM(require_react(), 1);
	init_dist();
	init_client();
	init_AuthContext();
	init_ui();
	init_Toast();
	init_constants();
	var import_jsx_runtime$19 = require_jsx_runtime();
	/**
	* Sign-in.  Deliberately plain and fast: this screen is used on a shared lab computer and on
	* a phone standing in a corridor.  No password is remembered in storage; the session lives in
	* an httpOnly cookie (see docs/SECURITY.md).
	*/
	function LoginPage() {
		const { login, isAuthenticated, status, notice } = useAuth();
		const navigate = useNavigate();
		const location = useLocation();
		const [form, setForm] = (0, import_react$18.useState)({
			email: "",
			password: "",
			remember: false
		});
		const [error, setError] = (0, import_react$18.useState)(null);
		const [busy, setBusy] = (0, import_react$18.useState)(false);
		const [config, setConfig] = (0, import_react$18.useState)(null);
		const [capsHint, setCapsHint] = (0, import_react$18.useState)(false);
		(0, import_react$18.useEffect)(() => {
			publicApi.config().then(setConfig).catch(() => {});
		}, []);
		(0, import_react$18.useEffect)(() => {
			setError(null);
		}, [form.email, form.password]);
		const redirectTo = (0, import_react$18.useMemo)(() => location.state?.from ?? "/", [location.state]);
		if (isAuthenticated && status === "authenticated") return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Navigate, {
			to: redirectTo,
			replace: true
		});
		const submit = async (event) => {
			event.preventDefault();
			if (!form.email.trim() || !form.password) {
				setError("Enter both your email address and password.");
				return;
			}
			setBusy(true);
			setError(null);
			try {
				await login(form);
				navigate(redirectTo, { replace: true });
			} catch (err) {
				setError(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
			className: "login",
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("form", {
				className: "login__card",
				onSubmit: submit,
				noValidate: true,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "login__brand",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("span", {
							className: "login__mark",
							"aria-hidden": "true",
							children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("svg", {
								viewBox: "0 0 32 32",
								width: "26",
								height: "26",
								children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("path", {
									d: "M3 17h5l2.4-7 3.3 13.4L17 13l2 4h10",
									fill: "none",
									stroke: "currentColor",
									strokeWidth: "2.4",
									strokeLinecap: "round",
									strokeLinejoin: "round"
								})
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("h1", {
							className: "login__title",
							children: "Equipment Maintenance & Fault Reporting"
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("p", {
							className: "login__sub",
							children: [config?.institution ? `${config.institution} · ` : "", config?.department ?? "Biomedical Engineering Department"]
						})] })]
					}),
					notice ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Callout, {
						tone: "warn",
						children: notice
					}) : null,
					error ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Callout, {
						tone: "bad",
						title: "Could not sign you in",
						children: error
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "login__form",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Field, {
								label: "Email address",
								htmlFor: "login-email",
								required: true,
								error: !form.email && error && !/password/i.test(error) ? "Enter your email address" : null,
								children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(TextInput, {
									id: "login-email",
									type: "email",
									autoComplete: "username",
									inputMode: "email",
									enterKeyHint: "next",
									required: true,
									value: form.email,
									onChange: (e) => setForm({
										...form,
										email: e.target.value
									}),
									placeholder: "you@institution.edu"
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Field, {
								label: "Password",
								htmlFor: "login-password",
								required: true,
								hint: capsHint ? "Caps Lock appears to be on." : "Sessions end automatically after 12 hours of inactivity.",
								children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(TextInput, {
									id: "login-password",
									type: "password",
									autoComplete: "current-password",
									enterKeyHint: "go",
									required: true,
									value: form.password,
									onChange: (e) => setForm({
										...form,
										password: e.target.value
									}),
									onKeyUp: (e) => setCapsHint(e.getModifierState?.("CapsLock") === true)
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Checkbox, {
								label: "Keep me signed in on this device",
								hint: "Only use this on a computer that nobody else can reach.",
								checked: form.remember,
								onChange: (v) => setForm({
									...form,
									remember: v
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
								type: "submit",
								size: "lg",
								tone: "primary",
								loading: busy,
								className: "btn--block",
								children: busy ? "Signing in…" : "Sign in"
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
						className: "form-note",
						style: {
							textAlign: "center",
							fontSize: ".78rem"
						},
						children: "Forgotten password? Ask the department administrator to reset it — self-service reset needs the email integration that is not enabled on this deployment."
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
						className: "login__foot",
						children: SAFETY_NOTICE
					})
				]
			})
		});
	}
	//#endregion
	//#region src/pages/QrLandingPage.jsx
	init_dist();
	init_client();
	init_AuthContext();
	init_ui();
	init_display();
	init_Toast();
	init_constants();
	init_format();
	/**
	* `/e/:tag` — the destination of the QR label on the equipment.
	*
	* It resolves *without* signing in (a student holding a dead microscope should learn whether
	* the department already knows), then offers sign-in or sign-out paths to actually report.
	*/
	function QrLandingPage() {
		const { tag } = useParams();
		const navigate = useNavigate();
		const { isAuthenticated, is } = useAuth();
		const [state, setState] = (0, import_react$18.useState)({
			loading: true,
			error: null,
			data: null,
			config: null
		});
		const [manual, setManual] = (0, import_react$18.useState)("");
		(0, import_react$18.useEffect)(() => {
			let alive = true;
			setState({
				loading: true,
				error: null,
				data: null,
				config: state.config
			});
			(async () => {
				const [config, data] = await Promise.allSettled([publicApi.config(), isAuthenticated ? equipment.get(tag) : publicApi.equipment(tag)]);
				if (!alive) return;
				setState({
					loading: false,
					config: config.status === "fulfilled" ? config.value : null,
					data: data.status === "fulfilled" ? data.value : null,
					error: data.status === "rejected" ? data.reason : null
				});
			})();
			return () => {
				alive = false;
			};
		}, [tag, isAuthenticated]);
		const view = state.data;
		const equipment$2 = view?.equipment ?? view;
		view?.openFaults ?? (view?.openFaults || view?.openFaults);
		if (state.loading) return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
			className: "login",
			style: { minHeight: "100vh" },
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
				className: "login__card",
				style: { textAlign: "center" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Spinner, {
					label: "Looking up this asset tag",
					size: "md"
				}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("p", {
					style: { marginTop: 12 },
					children: [
						"Looking up ",
						/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("code", { children: tag }),
						"…"
					]
				})]
			})
		});
		if (state.error || !equipment$2) return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
			className: "login",
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
				className: "login__card",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
					className: "login__brand",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("span", {
							className: "login__mark",
							"aria-hidden": "true",
							children: "?"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("h1", {
							className: "login__title",
							children: "Equipment not found"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
							className: "login__sub",
							children: errorText(state.error)
						})
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("form", {
					onSubmit: (e) => {
						e.preventDefault();
						if (manual.trim()) navigate(`/e/${encodeURIComponent(manual.trim().toUpperCase())}`);
					},
					children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Field, {
						label: "Check the asset tag",
						hint: "It is printed under the QR code, for example BMU-ECG-0014.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(TextInput, {
							value: manual,
							onChange: (e) => setManual(e.target.value),
							placeholder: "BMU-…-0000",
							autoCapitalize: "characters"
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "row",
						style: { marginTop: 12 },
						children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
							tone: "primary",
							type: "submit",
							children: "Look it up"
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
							as: Link,
							to: "/equipment",
							tone: "ghost",
							children: "Browse the inventory"
						})]
					})]
				})]
			})
		});
		const canReport = isAuthenticated;
		return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
			className: "login",
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
				className: "login__card",
				style: { maxWidth: 560 },
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "login__brand",
						style: {
							justifyItems: "start",
							textAlign: "left"
						},
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
								className: "page-head__eyebrow",
								style: { margin: 0 },
								children: state.config?.department ?? "Biomedical Engineering"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("h1", {
								className: "login__title",
								children: equipment$2.name
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
								className: "asset-tag",
								children: equipment$2.assetTag
							})
						]
					}),
					equipment$2.imageAttachmentId ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("img", {
						className: "eq-photo",
						src: attachments.viewUrl(equipment$2.imageAttachmentId),
						alt: `${equipment$2.name} as recorded by the department`
					}) : /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
						className: "eq-photo--empty",
						"aria-hidden": "true",
						children: "No photograph on file"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "row",
						style: { gap: 8 },
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(EquipmentStatusPill, {
								status: equipment$2.status,
								size: "lg"
							}),
							equipment$2.maintenanceState ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(MaintenanceLight, {
								state: equipment$2.maintenanceState,
								daysUntil: equipment$2.daysUntilPm
							}) : null,
							equipment$2.openFaults > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)(Badge, {
								tone: "warn",
								children: [
									"⚑ ",
									equipment$2.openFaults,
									" open fault report",
									equipment$2.openFaults > 1 ? "s" : ""
								]
							}) : null
						]
					}),
					equipment$2.openFaults > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
						className: "callout callout--warn",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("span", {
							className: "callout__icon",
							"aria-hidden": "true",
							children: "⚠"
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("div", {
							className: "callout__main",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
								className: "callout__title",
								children: "The department already knows about a problem with this item"
							}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)("p", {
								className: "callout__body",
								children: [
									"Open report ",
									equipment$2.openFaultReference ?? "",
									" is being handled. Adding a duplicate report slows the repair down — only report it again if the problem is different or worse than described."
								]
							})]
						})]
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(KeyValue, {
						columns: 1,
						items: [
							["Category", equipment$2.categoryName ?? equipment$2.category?.name],
							["Location", equipment$2.locationLabel ?? equipment$2.location?.name ?? "Not recorded"],
							["Manufacturer", equipment$2.manufacturer],
							["Model", equipment$2.model],
							...isAuthenticated && !is("reporter") ? [["Serial number", equipment$2.serialNumber], ["Status recorded", formatDateTime(equipment$2.updatedAt ?? equipment$2.createdAt)]] : [],
							["Last repair completed", equipment$2.lastRepairedAt ? formatDateTime(equipment$2.lastRepairedAt) : "No repair recorded"]
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
						className: "stack",
						children: canReport ? /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)(import_jsx_runtime$19.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
							as: Link,
							to: `/report/${encodeURIComponent(equipment$2.assetTag)}`,
							tone: "primary",
							size: "lg",
							className: "btn--block",
							children: "Report a fault with this equipment"
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
							as: Link,
							to: `/equipment/${equipment$2.id ?? equipment$2.assetTag}`,
							tone: "secondary",
							className: "btn--block",
							children: "Open the full equipment record"
						})] }) : /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)(import_jsx_runtime$19.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
							className: "form-note",
							children: "Sign in to report a fault — every report is attributed to a named person so nothing gets lost."
						}), /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Button, {
							as: Link,
							to: "/login",
							tone: "primary",
							size: "lg",
							className: "btn--block",
							children: "Sign in to report a fault"
						})] })
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("p", {
						className: "login__foot",
						children: SAFETY_NOTICE
					})
				]
			})
		});
	}
	//#endregion
	//#region src/components/charts.jsx
	function ChartFrame({ title, subtitle, legend, children, footnote, className, tableData }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("figure", {
			className: cx("chart", className),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("figcaption", {
					className: "chart__cap",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("p", {
						className: "chart__title",
						children: title
					}), subtitle ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("p", {
						className: "chart__sub",
						children: subtitle
					}) : null] }), legend ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("ul", {
						className: "chart__legend",
						children: legend.map((l) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("li", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("i", {
								style: { background: l.color ?? "currentColor" },
								"aria-hidden": "true"
							}),
							l.label,
							l.value !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("b", { children: l.value }) : null
						] }, l.label))
					}) : null]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("div", {
					className: "chart__plot",
					children
				}),
				footnote ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("p", {
					className: "chart__foot",
					children: footnote
				}) : null,
				tableData ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("table", {
					className: "sr-only",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("tr", { children: tableData.columns.map((c) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("th", {
						scope: "col",
						children: c
					}, c)) }) }), /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("tbody", { children: tableData.rows.map((r, i) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("tr", { children: r.map((cell, j) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("td", { children: String(cell) }, j)) }, i)) })]
				}) : null
			]
		});
	}
	/** Horizontal bars: the most legible shape for category comparisons on a narrow screen. */
	function BarList({ items, valueSuffix = "", max, onPick, className, toneBy = () => "primary" }) {
		const top = max ?? Math.max(1, ...items.map((i) => Number(i.value) || 0));
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("ul", {
			className: cx("barlist", className),
			children: items.map((item) => {
				const pct = Math.round((Number(item.value) || 0) / top * 100);
				return /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)(onPick ? "button" : "div", {
					...onPick ? {
						type: "button",
						onClick: () => onPick(item)
					} : {},
					className: cx("barlist__row", onPick && "barlist__row--link"),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", {
							className: "barlist__label",
							title: item.fullLabel ?? item.label,
							children: item.label
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", {
							className: "barlist__track",
							children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", {
								className: "barlist__fill",
								style: {
									width: `${Math.max(pct, item.value ? 3 : 0)}%`,
									background: TONE_COLOR[toneBy(item)] ?? toneBy(item)
								}
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("span", {
							className: "barlist__value",
							children: [item.value, valueSuffix]
						})
					]
				}) }, item.label);
			})
		});
	}
	function DonutChart({ slices, size = 168, thickness = 26, centerLabel, centerValue, className }) {
		const total = slices.reduce((n, s) => n + (Number(s.value) || 0), 0);
		const radius = size / 2 - thickness / 2;
		const circumference = 2 * Math.PI * radius;
		let offset = 0;
		const arcs = slices.map((s) => {
			const fraction = total ? (Number(s.value) || 0) / total : 0;
			const dash = fraction * circumference;
			const arc = {
				...s,
				dash,
				offset,
				fraction
			};
			offset += dash;
			return arc;
		});
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("div", {
			className: cx("donut", className),
			children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("svg", {
				width: size,
				height: size,
				viewBox: `0 0 ${size} ${size}`,
				role: "img",
				"aria-label": `${total} items across ${slices.length} categories: ${slices.map((s) => `${s.label} ${s.value}`).join(", ")}`,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("g", {
						transform: `rotate(-90 ${size / 2} ${size / 2})`,
						children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("circle", {
							cx: size / 2,
							cy: size / 2,
							r: radius,
							fill: "none",
							stroke: "var(--line)",
							strokeWidth: thickness
						}), total > 0 && arcs.map((a) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("circle", {
							className: "donut__arc",
							cx: size / 2,
							cy: size / 2,
							r: radius,
							fill: "none",
							stroke: TONE_COLOR[a.tone] ?? a.color ?? "var(--primary)",
							strokeWidth: thickness,
							strokeDasharray: `${a.dash} ${circumference - a.dash}`,
							strokeDashoffset: -a.offset,
							strokeLinecap: "butt",
							children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("title", { children: `${a.label}: ${a.value} (${Math.round(a.fraction * 100)}%)` })
						}, a.label))]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("text", {
						x: "50%",
						y: "47%",
						textAnchor: "middle",
						className: "donut__num",
						children: centerValue ?? total
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("text", {
						x: "50%",
						y: "61%",
						textAnchor: "middle",
						className: "donut__cap",
						children: centerLabel ?? "total"
					})
				]
			})
		});
	}
	/** Grouped monthly bars (reported vs resolved), with a critical overlay. */
	function TrendChart({ data, series = [{
		key: "reported",
		label: "Reported",
		tone: "primary"
	}, {
		key: "resolved",
		label: "Resolved",
		tone: "ok"
	}], height = 190, formatLabel = (l) => l, className }) {
		const max = Math.max(1, ...data.flatMap((d) => series.map((s) => Number(d[s.key]) || 0)));
		const ticks = [
			0,
			.25,
			.5,
			.75,
			1
		].map((f) => Math.round(max * f)).reverse();
		const barGroupWidth = 100 / Math.max(1, data.length);
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("div", {
			className: cx("trend", className),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("svg", {
					viewBox: "0 0 100 56",
					preserveAspectRatio: "none",
					className: "trend__svg",
					role: "img",
					"aria-label": `Monthly fault volume over ${data.length} months. ${data.map((d) => `${d.month}: ${d.reported ?? 0} reported`).join("; ")}`,
					children: [
						ticks.slice(0, -1).map((_, i) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("line", {
							x1: "0",
							x2: "100",
							y1: 4 + i * (44 / (ticks.length - 1)),
							y2: 4 + i * (44 / (ticks.length - 1)),
							className: "trend__grid",
							strokeWidth: "0.2",
							vectorEffect: "non-scaling-stroke"
						}, i)),
						data.map((d, di) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("g", { children: [series.map((s, si) => {
							const value = Number(d[s.key]) || 0;
							const h = value / max * 42;
							const w = Math.min(3.4, barGroupWidth * .62 / series.length);
							const x = di * barGroupWidth + barGroupWidth / 2 - series.length * w / 2 + si * w;
							return /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("g", { children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("rect", {
								x,
								y: 48 - h,
								width: w * .86,
								height: Math.max(h, value ? .6 : 0),
								rx: "0.4",
								fill: TONE_COLOR[s.tone] ?? SERIES_COLORS[si % SERIES_COLORS.length],
								children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("title", { children: `${formatLabel(d.month)} — ${s.label}: ${value}` })
							}) }, s.key);
						}), Number(d.critical) > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("circle", {
							cx: di * barGroupWidth + barGroupWidth / 2,
							cy: 3.2,
							r: .9,
							fill: "var(--bad)",
							children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("title", { children: `${formatLabel(d.month)} — ${d.critical} critical fault(s)` })
						}) : null] }, d.month)),
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("line", {
							x1: "0",
							x2: "100",
							y1: "48",
							y2: "48",
							className: "trend__axis",
							strokeWidth: "0.3",
							vectorEffect: "non-scaling-stroke"
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("div", {
					className: "trend__ticks",
					"aria-hidden": "true",
					children: ticks.map((t, i) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", { children: t }, i))
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("div", {
					className: "trend__x",
					children: data.map((d) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", {
						title: d.month,
						children: formatLabel(d.month)
					}, d.month))
				})
			]
		});
	}
	/** Compliance donut + the raw counts, because a ring alone hides "based on 3 records". */
	function Gauge({ value, label, segments = [], size = 150, tone = "ok", caption }) {
		const pct = Math.max(0, Math.min(100, Number(value) || 0));
		const r = size / 2 - 10;
		Math.PI * r;
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("div", {
			className: "gauge",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("svg", {
					width: size,
					height: size * .62,
					viewBox: `0 0 ${size} ${size * .62}`,
					role: "img",
					"aria-label": `${label}: ${Math.round(pct)} percent`,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("path", {
							d: describeArc(size / 2, size * .52, r, -90, 90),
							className: "gauge__track",
							fill: "none",
							strokeWidth: "12",
							strokeLinecap: "round"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("path", {
							d: describeArc(size / 2, size * .52, r, -90, -90 + pct / 100 * 180),
							className: `gauge__value gauge__value--${tone}`,
							fill: "none",
							strokeWidth: "12",
							strokeLinecap: "round"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("text", {
							x: "50%",
							y: "72%",
							textAnchor: "middle",
							className: "gauge__num",
							children: [Math.round(pct), "%"]
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("p", {
					className: "gauge__label",
					children: label
				}),
				caption ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("p", {
					className: "gauge__caption",
					children: caption
				}) : null,
				segments.length ? /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("ul", {
					className: "gauge__segs",
					children: segments.map((s) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("li", {
						className: `is-${s.tone ?? "neutral"}`,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("b", { children: s.value }),
							" ",
							s.label
						]
					}, s.label))
				}) : null
			]
		});
	}
	function polar(cxv, cyv, r, deg) {
		const rad = deg * Math.PI / 180;
		return [cxv + r * Math.cos(rad), cyv + r * Math.sin(rad)];
	}
	function describeArc(cxv, cyv, r, startAngle, endAngle) {
		const [sx, sy] = polar(cxv, cyv, r, startAngle);
		const [ex, ey] = polar(cxv, cyv, r, endAngle);
		return `M ${sx} ${sy} A ${r} ${r} 0 ${Math.abs(endAngle - startAngle) > 180 ? 1 : 0} 1 ${ex} ${ey}`;
	}
	/** Small stacked share bar for "3 of 168 items are overdue"-style comparisons. */
	function ShareBar({ parts, className }) {
		const total = Math.max(1, parts.reduce((n, p) => n + (Number(p.value) || 0), 0));
		return /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("div", {
			className: cx("sharebar", className),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("div", {
				className: "sharebar__track",
				role: "img",
				"aria-label": parts.map((p) => `${p.label}: ${p.value}`).join(", "),
				children: parts.map((p) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("span", {
					className: "sharebar__seg",
					style: {
						width: `${(Number(p.value) || 0) / total * 100}%`,
						background: TONE_COLOR[p.tone] ?? p.tone
					},
					title: `${p.label}: ${p.value}`,
					children: /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("span", {
						className: "sr-only",
						children: [
							p.label,
							": ",
							p.value
						]
					})
				}, p.label))
			}), /* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("ul", {
				className: "sharebar__legend",
				children: parts.map((p) => /* @__PURE__ */ (0, import_jsx_runtime$18.jsxs)("li", {
					className: `is-${p.tone}`,
					children: [/* @__PURE__ */ (0, import_jsx_runtime$18.jsx)("b", { children: p.value }), p.label]
				}, p.label))
			})]
		});
	}
	var import_jsx_runtime$18, TONE_COLOR, SERIES_COLORS;
	var init_charts = __esmMin((() => {
		init_format();
		import_jsx_runtime$18 = require_jsx_runtime();
		TONE_COLOR = {
			ok: "var(--ok)",
			warn: "var(--warn)",
			bad: "var(--bad)",
			info: "var(--info)",
			neutral: "var(--slate)",
			primary: "var(--primary)"
		};
		SERIES_COLORS = [
			"var(--primary)",
			"var(--teal)",
			"var(--warn)",
			"var(--bad)",
			"var(--violet)"
		];
	}));
	//#endregion
	//#region src/utils/useApi.js
	/**
	* Data-fetching hook for read endpoints: loading / error / refresh, plus AbortController on
	* unmount and on dependency change (so a fast filter change cannot let a stale response
	* overwrite a newer one — the classic "old list flashes back" bug).
	*/
	function useApi(fetcher, deps = [], { keepPrevious = false } = {}) {
		const [state, setState] = (0, import_react$17.useState)({
			data: void 0,
			loading: true,
			error: null
		});
		const mounted = (0, import_react$17.useRef)(true);
		const generation = (0, import_react$17.useRef)(0);
		(0, import_react$17.useEffect)(() => {
			mounted.current = true;
			return () => {
				mounted.current = false;
			};
		}, []);
		const run = (0, import_react$17.useCallback)(async ({ silent = false } = {}) => {
			const gen = ++generation.current;
			const controller = new AbortController();
			if (!silent) setState((s) => ({
				data: keepPrevious ? s.data : void 0,
				loading: true,
				error: null
			}));
			try {
				const data = await fetcher(controller.signal);
				if (!mounted.current || gen !== generation.current) return;
				setState({
					data,
					loading: false,
					error: null
				});
			} catch (err) {
				if (!mounted.current || gen !== generation.current) return;
				if (err?.name === "AbortError") return;
				setState({
					data: keepPrevious ? state.data : void 0,
					loading: false,
					error: err
				});
			}
		}, deps);
		(0, import_react$17.useEffect)(() => {
			run();
			return () => generation.current += 1;
		}, [run]);
		const setData = (0, import_react$17.useCallback)((updater) => setState((s) => ({
			...s,
			data: typeof updater === "function" ? updater(s.data) : updater
		})), []);
		return {
			...state,
			refresh: run,
			setData,
			isLoading: state.loading && state.data === void 0
		};
	}
	var import_react$17;
	var init_useApi = __esmMin((() => {
		import_react$17 = /* @__PURE__ */ __toESM(require_react(), 1);
	}));
	//#endregion
	//#region src/pages/DashboardPage.jsx
	var DashboardPage_exports = /* @__PURE__ */ __exportAll({ DashboardPage: () => DashboardPage$1 });
	/**
	* Dashboard.  The order is the department's own order of business: what is broken and being
	* handled → what needs attention soon → how the fleet is trending → who is carrying what.
	* Charts are secondary to numbers a person can act on.
	*/
	function DashboardPage$1() {
		const { can, is, user } = useAuth();
		const navigate = useNavigate();
		const [months, setMonths] = (0, import_react$16.useState)(12);
		const { data, loading, error, refresh } = useApi(() => dashboard.get({ chartMonths: months }), [months]);
		const kpis = data?.kpis;
		const personal = data?.scope === "personal";
		const kpiItems = (0, import_react$16.useMemo)(() => {
			if (!kpis) return [];
			const common = [
				{
					label: "Total equipment",
					value: kpis.totalEquipment,
					hint: `${kpis.totalIncludingDecommissioned - kpis.totalEquipment ?? 0} archived/decommissioned`,
					tone: "info",
					to: "/equipment"
				},
				{
					label: "Operational",
					value: kpis.operational,
					hint: `${kpis.fleetAvailabilityPercent}% of the live fleet`,
					tone: "ok",
					to: "/equipment?status=operational"
				},
				{
					label: "Faulty / unusable",
					value: kpis.faulty + kpis.underRepair + kpis.outOfService,
					hint: `${kpis.faulty} reported · ${kpis.underRepair} in repair · ${kpis.outOfService} out of service`,
					tone: "bad",
					to: "/equipment?needsAttention=1"
				},
				{
					label: "Open fault reports",
					value: kpis.openFaults,
					hint: `${kpis.unassignedFaults} not yet assigned`,
					tone: "warn",
					to: personal ? "/my-reports" : "/faults?scope=open"
				},
				{
					label: "Critical faults",
					value: kpis.criticalFaults,
					hint: kpis.slaBreachedFaults ? `${kpis.slaBreachedFaults} past the response target` : "within response target",
					tone: kpis.criticalFaults ? "bad" : "ok",
					to: "/faults?severity=critical"
				},
				{
					label: "Maintenance overdue",
					value: kpis.maintenanceOverdue,
					hint: `${kpis.maintenanceDueSoon} due within 14 days`,
					tone: kpis.maintenanceOverdue ? "bad" : "ok",
					to: "/maintenance?tab=due"
				}
			];
			if (!personal) common.push({
				label: "Average repair time",
				value: formatHours(kpis.averageRepairHours).replace(/ /g, " "),
				hint: `${pluralise(kpis.resolvedIn180Days, "fault")} resolved in 180 days`,
				tone: "info"
			}, {
				label: "Repair cost this year",
				value: formatMoney(kpis.repairCostYtd, kpis.currency),
				hint: `${pluralise(kpis.repairCountYtd, "repair")} recorded`,
				tone: "neutral"
			});
			return common;
		}, [kpis, personal]);
		return /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(PageHeader, {
				eyebrow: (/* @__PURE__ */ new Date()).toLocaleDateString(void 0, {
					weekday: "long",
					day: "numeric",
					month: "long"
				}),
				title: personal ? `Good ${greeting()}, ${user?.fullName?.split(" ")[0]}` : "Department status",
				description: personal ? "Your submitted reports and the equipment you interact with. Fault details are open to the whole department." : "Everything the department needs to answer “what do we have, is it working, who is fixing it, when is service due?”",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Button, {
					as: Link,
					to: "/faults/new",
					tone: "primary",
					children: "＋ Report a fault"
				}), can("equipment.create") ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Button, {
					as: Link,
					to: "/equipment/new",
					tone: "secondary",
					children: "＋ Add equipment"
				}) : null] })
			}),
			error ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(ErrorState, {
				error,
				onRetry: refresh
			}) : null,
			loading && !data ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Loading, {
				rows: 6,
				label: "Loading the department dashboard"
			}) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: [
				/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("div", {
					className: "grid grid--kpi",
					style: { marginBottom: "var(--sp-5)" },
					children: kpiItems.map((k) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Kpi, { ...k }, k.label))
				}),
				personal && kpis?.slaBreachedFaults ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Callout, {
					tone: "warn",
					title: "One or more of your reports is past the department response target",
					children: "The technicians have been reminded. If the equipment is unsafe to use rather than merely faulty, tell the department desk directly as well — this system does not replace that call."
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
					className: "grid grid--2",
					style: { marginBottom: "var(--sp-4)" },
					children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Card, {
						title: "Equipment status",
						subtitle: "Every active item by current state",
						children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
							className: "row",
							style: {
								alignItems: "center",
								gap: "var(--sp-5)"
							},
							children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(DonutChart, {
								slices: data.equipmentByStatus.map((s) => ({
									label: s.label,
									value: s.count,
									tone: s.tone
								})),
								centerValue: data.equipmentByStatus.reduce((n, s) => n + s.count, 0),
								centerLabel: "items"
							}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("div", {
								style: {
									flex: "1 1 200px",
									minWidth: 190
								},
								children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(BarList, {
									items: data.equipmentByStatus.filter((s) => s.count > 0).map((s) => ({
										label: s.label,
										value: s.count,
										tone: s.tone,
										to: `/equipment?status=${s.status}`
									})),
									onPick: (item) => navigate(item.to ?? "/equipment")
								})
							})]
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Card, {
						title: "Faults by category",
						subtitle: `Reported in the last 12 months${personal ? " by you" : ""}`,
						actions: /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Badge, {
							tone: "neutral",
							children: [data.faultsByCategory.reduce((n, c) => n + c.count, 0), " total"]
						}),
						children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(BarList, {
							items: data.faultsByCategory.filter((c) => c.count > 0).map((c) => ({
								label: c.label,
								value: c.count,
								tone: c.criticalCount ? "bad" : "primary",
								critical: c.criticalCount
							})),
							onPick: (item) => navigate(`/faults?categoryId=${item.id}`)
						}), data.faultsByCategory.every((c) => !c.count) ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
							className: "form-note",
							children: "No fault reports in this window."
						}) : null]
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
					className: "grid grid--2",
					style: { marginBottom: "var(--sp-4)" },
					children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Card, {
						title: "Fault volume by month",
						subtitle: "Bars are reports; the dot marks months containing a critical fault",
						actions: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Select, {
							value: months,
							onChange: (e) => setMonths(Number(e.target.value)),
							"aria-label": "Chart range",
							options: [
								6,
								12,
								18,
								24
							].map((n) => ({
								value: n,
								label: `${n} months`
							}))
						}),
						children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(ChartFrame, {
							title: null,
							tableData: {
								columns: [
									"Month",
									"Reported",
									"Resolved",
									"Critical"
								],
								rows: data.faultsByMonth.map((m) => [
									m.month,
									m.reported,
									m.resolved,
									m.critical
								])
							},
							children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(TrendChart, {
								data: data.faultsByMonth,
								formatLabel: monthLabel,
								series: [{
									key: "reported",
									label: "Reported",
									tone: "primary"
								}, {
									key: "resolved",
									label: "Resolved",
									tone: "ok"
								}]
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
							className: "chart__legend",
							style: { marginTop: 6 },
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("i", { style: { background: "var(--primary)" } }), " Reported"] }),
								/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("i", { style: { background: "var(--ok)" } }), " Resolved"] }),
								/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("i", { style: {
									background: "var(--bad)",
									borderRadius: 9
								} }), " month with critical faults"] })
							]
						})]
					}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Card, {
						title: "Preventive maintenance",
						subtitle: `Compliance over the last ${data.maintenanceCompliance.windowDays} days`,
						children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
							className: "row",
							style: {
								alignItems: "center",
								gap: "var(--sp-5)"
							},
							children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Gauge, {
								value: data.maintenanceCompliance.fleetCompliancePercent,
								tone: data.maintenanceCompliance.fleetCompliancePercent > 90 ? "ok" : data.maintenanceCompliance.fleetCompliancePercent > 75 ? "warn" : "bad",
								label: "Fleet not overdue",
								caption: data.maintenanceCompliance.onTimePercent === null ? "No PM records in the window yet" : `${data.maintenanceCompliance.recordsOnTime}/${data.maintenanceCompliance.recordsDone} visits on time`,
								segments: [
									{
										label: "up to date",
										value: data.maintenanceCompliance.upToDateEquipment,
										tone: "ok"
									},
									{
										label: "due soon",
										value: data.maintenanceCompliance.dueSoonEquipment,
										tone: "warn"
									},
									{
										label: "overdue",
										value: data.maintenanceCompliance.overdueEquipment,
										tone: "bad"
									}
								]
							}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
								style: {
									flex: "1 1 200px",
									minWidth: 200
								},
								children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(KeyValue, {
									columns: 1,
									items: [
										["Average lateness", data.maintenanceCompliance.averageLatenessDays !== null ? `${data.maintenanceCompliance.averageLatenessDays} days` : "—"],
										["Worst lateness", `${data.maintenanceCompliance.worstLatenessDays} days`],
										["Items with no schedule", `${data.maintenanceCompliance.unscheduledEquipment} — these are the blind spots`]
									]
								}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Button, {
									as: Link,
									to: "/maintenance?tab=due",
									tone: "secondary",
									size: "sm",
									style: { marginTop: 10 },
									children: "Open the due board"
								})]
							})]
						})
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
					className: "grid grid--split",
					style: { gap: "var(--sp-4)" },
					children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
						className: "grid",
						style: { gap: "var(--sp-4)" },
						children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Card, {
							title: "Most frequently failing equipment",
							subtitle: personal ? "Ranked by reports in the last two years" : "Ranked by reports in the last two years, worst first",
							actions: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Button, {
								as: Link,
								to: "/risk",
								tone: "ghost",
								size: "sm",
								children: "Risk register →"
							}),
							children: data.topFailingEquipment.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
								className: "form-note",
								children: "No repeat offenders. Either the fleet is healthy or reporting needs encouraging."
							}) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(DataTable, {
								dense: true,
								columns: [
									{
										key: "name",
										label: "Equipment",
										render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
											className: "cell-main",
											children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Link, {
												className: "cell-title",
												to: `/equipment/${r.id}`,
												children: r.name
											}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", {
												className: "cell-sub",
												children: [
													r.assetTag,
													" · ",
													r.categoryName ?? "—",
													r.locationName ? ` · ${r.locationName}` : ""
												]
											})]
										})
									},
									{
										key: "faultCount",
										label: "Faults",
										align: "center",
										render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("b", { children: r.faultCount })
									},
									{
										key: "criticalCount",
										label: "Critical",
										align: "center",
										render: (r) => r.criticalCount ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Badge, {
											tone: "bad",
											children: r.criticalCount
										}) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("span", {
											className: "muted",
											children: "0"
										})
									},
									{
										key: "daysSinceFault",
										label: "Last fault",
										align: "right",
										render: (r) => r.daysSinceFault === null ? "—" : r.daysSinceFault <= 1 ? "today" : `${r.daysSinceFault} d ago`
									}
								],
								rows: data.topFailingEquipment
							})
						}), can("dashboard.view") && data.technicianWorkload.length ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Card, {
							title: "Workload",
							subtitle: "Open faults per technician, last 90 days of activity",
							className: "print-only-none",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(ShareBar, { parts: data.technicianWorkload.map((t) => ({
								label: t.fullName.split(" ").slice(-1)[0],
								value: t.openCount,
								tone: t.openCount > 6 ? "bad" : t.openCount > 3 ? "warn" : "ok"
							})) }), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(DataTable, {
								dense: true,
								rows: data.technicianWorkload,
								columns: [
									{
										key: "fullName",
										label: "Technician",
										render: (t) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
											className: "cell-main",
											children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("span", {
												className: "cell-title",
												children: t.fullName
											}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("span", {
												className: "cell-sub",
												children: t.jobTitle ?? ""
											})]
										})
									},
									{
										key: "openCount",
										label: "Open",
										align: "center",
										render: (t) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("b", { children: t.openCount })
									},
									{
										key: "criticalCount",
										label: "Critical",
										align: "center",
										render: (t) => t.criticalCount ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Badge, {
											tone: "bad",
											children: t.criticalCount
										}) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("span", {
											className: "muted",
											children: "0"
										})
									},
									{
										key: "avgRepairHours",
										label: "Avg repair",
										align: "right",
										render: (t) => formatHours(t.avgRepairHours)
									}
								]
							})]
						}) : null]
					}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
						className: "grid",
						style: { gap: "var(--sp-4)" },
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Card, {
								title: "Highest maintenance risk",
								subtitle: "Rule-based indicator from this department's own history",
								actions: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Button, {
									as: Link,
									to: "/risk",
									tone: "ghost",
									size: "sm",
									children: "All"
								}),
								children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
									className: "stack",
									style: { gap: "var(--sp-3)" },
									children: [(data.risk?.top ?? []).map((r) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(Link, {
										to: `/equipment/${r.equipmentId}`,
										className: "listrow listrow--link",
										style: {
											border: "1px solid var(--line)",
											borderRadius: "var(--radius)",
											padding: "10px"
										},
										children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(RiskBadge, {
											level: r.level,
											score: r.score
										}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", {
											className: "listrow__main",
											children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("span", {
												className: "listrow__title",
												children: r.equipmentName
											}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("span", {
												className: "listrow__sub",
												children: [
													r.assetTag,
													" · ",
													r.inputSnapshot?.faultsIn12Months ?? 0,
													" faults/12 mo · next PM ",
													r.inputSnapshot?.nextMaintenanceOn ?? "not scheduled"
												]
											})]
										})]
									}, r.equipmentId)), !(data.risk?.top ?? []).length ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
										className: "form-note",
										children: "No active equipment to assess."
									}) : null]
								}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
									className: "chart__foot",
									style: { marginTop: 10 },
									children: SAFETY_NOTICE
								})]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Card, {
								title: "Recent activity",
								subtitle: "Every status change in the workflow, newest first",
								children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Timeline, { items: data.recentActivity.map((a) => ({
									id: a.id,
									title: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(import_jsx_runtime$17.Fragment, { children: a.fromStatus && a.fromStatus !== a.toStatus ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: [
										"moved ",
										a.reference,
										" to ",
										/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(FaultStatusPill, {
											status: a.toStatus,
											size: "sm"
										})
									] }) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: ["commented on ", a.reference] }) }),
									detail: a.comment,
									actor: a.actorName,
									meta: `${a.equipmentName} (${a.assetTag})`,
									at: a.changedAt,
									tone: a.severity === "critical" ? "bad" : null
								})) })
							}),
							data.recentMaintenance.length ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Card, {
								title: "Recent maintenance",
								subtitle: "Signed-off preventive visits",
								children: /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("div", {
									className: "stack",
									style: { gap: 10 },
									children: data.recentMaintenance.slice(0, 5).map((m) => /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
										className: "row row--between",
										style: { gap: 8 },
										children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("div", {
											style: { minWidth: 0 },
											children: [/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Link, {
												to: `/equipment/${m.equipmentId ?? ""}`,
												className: "cell-title",
												children: m.equipmentName
											}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)("p", {
												className: "cell-sub",
												children: [
													m.scheduleTitle ?? "Ad-hoc",
													" · ",
													m.performedByName
												]
											})]
										}), /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Badge, {
											tone: m.conditionFound === "pass" ? "ok" : m.conditionFound === "needs_attention" ? "warn" : "info",
											children: m.conditionFound?.replace(/_/g, " ")
										})]
									}, m.id))
								})
							}) : null
						]
					})]
				})
			] })
		] });
	}
	function Kpi({ label, value, hint, tone, to }) {
		const Body = /* @__PURE__ */ (0, import_jsx_runtime$17.jsxs)(import_jsx_runtime$17.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
				className: "kpi__label",
				children: label
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
				className: "kpi__value",
				children: value ?? "—"
			}),
			hint ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("p", {
				className: "kpi__hint",
				children: hint
			}) : null
		] });
		const cls = cx("kpi", `kpi--${tone ?? "neutral"}`, tone === "bad" && Number(value) && "kpi--alert");
		return to ? /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)(Link, {
			to,
			className: cls,
			children: Body
		}) : /* @__PURE__ */ (0, import_jsx_runtime$17.jsx)("div", {
			className: cls,
			children: Body
		});
	}
	var import_react$16, import_jsx_runtime$17, greeting;
	var init_DashboardPage = __esmMin((() => {
		import_react$16 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_ui();
		init_display();
		init_charts();
		init_useApi();
		init_format();
		init_constants();
		import_jsx_runtime$17 = require_jsx_runtime();
		greeting = () => {
			const h = (/* @__PURE__ */ new Date()).getHours();
			if (h < 12) return "morning";
			if (h < 17) return "afternoon";
			return "evening";
		};
	}));
	//#endregion
	//#region src/pages/EquipmentListPage.jsx
	var EquipmentListPage_exports = /* @__PURE__ */ __exportAll({ EquipmentListPage: () => EquipmentListPage$1 });
	/**
	* Inventory browser (Phase 2).  Search and filters live in the URL, so a filter state can be
	* pasted into a message ("we have 4 of these, 2 broken: <link>") and survives a reload.
	*/
	function EquipmentListPage$1() {
		const [params, setParams] = useSearchParams();
		const { can } = useAuth();
		const navigate = useNavigate();
		const [selected, setSelected] = (0, import_react$15.useState)([]);
		const filters = (0, import_react$15.useMemo)(() => ({
			q: params.get("q") ?? "",
			status: params.get("status") ?? "",
			categoryId: params.get("categoryId") ?? "",
			locationId: params.get("locationId") ?? "",
			criticality: params.get("criticality") ?? "",
			maintenanceState: params.get("maintenanceState") ?? "",
			technicianId: params.get("technicianId") ?? "",
			needsAttention: params.get("needsAttention") ?? "",
			sort: params.get("sort") ?? "name",
			dir: params.get("dir") ?? "asc",
			page: params.get("page") ?? 1,
			perPage: params.get("perPage") ?? 25
		}), [params]);
		const { data, loading, error, refresh } = useApi(() => equipment.list(filters), [(0, import_react$15.useMemo)(() => {
			const sp = new URLSearchParams();
			for (const [k, v] of Object.entries(filters)) if (v !== "" && v !== null && v !== void 0) sp.set(k, String(v));
			return sp.toString();
		}, [filters])]);
		const picklists = useApi(() => reference.picklists(), []);
		const set = (patch) => {
			const next = new URLSearchParams(params);
			for (const [k, v] of Object.entries(patch)) if (v === "" || v === null || v === void 0) next.delete(k);
			else next.set(k, String(v));
			if (!("page" in patch)) next.delete("page");
			setParams(next, { replace: true });
		};
		const active = Object.entries(filters).some(([k, v]) => v !== "" && v !== null && v !== void 0 && k !== "sort" && k !== "dir" && k !== "page" && k !== "perPage" && k !== "q");
		const rows = data?.items ?? [];
		const total = data?.pagination?.total ?? 0;
		const toggleSort = (key) => {
			const target = {
				name: "name",
				assetTag: "assetTag",
				status: "status",
				nextMaintenanceOn: "nextMaintenanceOn",
				totalFaults: "totalFaults",
				criticality: "criticality"
			}[key] ?? key;
			set({
				sort: target,
				dir: filters.sort === target && filters.dir === "asc" ? "desc" : "asc"
			});
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(import_jsx_runtime$16.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(PageHeader, {
				eyebrow: "Inventory",
				title: "Equipment",
				description: loading ? "Loading…" : `${total} item${total === 1 ? "" : "s"} matching the current filters. Scan a label or open a record for its full history.`,
				actions: /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(import_jsx_runtime$16.Fragment, { children: [can("equipment.create") ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Button, {
					as: Link,
					to: "/equipment/new",
					tone: "primary",
					children: "＋ Add equipment"
				}) : null, selected.length && can("equipment.qr") ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(Button, {
					tone: "secondary",
					onClick: () => navigate("/reference?tab=labels&ids=" + selected.join(",")),
					children: [
						"🏷 Print ",
						selected.length,
						" label",
						selected.length > 1 ? "s" : ""
					]
				}) : null] })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("form", {
				onSubmit: (e) => {
					e.preventDefault();
					refresh();
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(SearchInput, {
					value: filters.q,
					onSearch: (q) => set({ q }),
					placeholder: "Search by name, asset tag, serial, manufacturer, model, room or category"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("div", {
				style: { marginTop: "var(--sp-4)" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(FilterBar, {
					active,
					onClear: () => setParams(new URLSearchParams(), { replace: true }),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.status,
							onChange: (e) => set({ status: e.target.value }),
							"aria-label": "Filter by status",
							options: [{
								value: "",
								label: "Any status"
							}, ...picklists.data?.equipmentStatuses ?? []]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.categoryId,
							onChange: (e) => set({ categoryId: e.target.value }),
							"aria-label": "Filter by category",
							options: [{
								value: "",
								label: "Any category"
							}, ...(picklists.data?.categories ?? []).map((c) => ({
								value: c.id,
								label: c.name
							}))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.locationId,
							onChange: (e) => set({ locationId: e.target.value }),
							"aria-label": "Filter by location",
							options: [{
								value: "",
								label: "Any location"
							}, ...(picklists.data?.locations ?? []).map((l) => ({
								value: l.id,
								label: l.name
							}))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.maintenanceState,
							onChange: (e) => set({ maintenanceState: e.target.value }),
							"aria-label": "Filter by maintenance state",
							options: [
								{
									value: "",
									label: "Any maintenance state"
								},
								{
									value: "overdue",
									label: "🔴 Overdue"
								},
								{
									value: "due_soon",
									label: "🟡 Due soon"
								},
								{
									value: "up_to_date",
									label: "🟢 Up to date"
								},
								{
									value: "not_scheduled",
									label: "⚪ No schedule"
								}
							]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.criticality,
							onChange: (e) => set({ criticality: e.target.value }),
							"aria-label": "Filter by criticality",
							options: [{
								value: "",
								label: "Any criticality"
							}, ...(picklists.data?.criticalities ?? []).map((c) => ({
								value: c.value,
								label: c.label
							}))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Select, {
							value: filters.technicianId,
							onChange: (e) => set({ technicianId: e.target.value }),
							"aria-label": "Filter by responsible technician",
							options: [{
								value: "",
								label: "Any technician"
							}, ...(picklists.data?.technicians ?? []).map((t) => ({
								value: t.id,
								label: t.fullName
							}))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("label", {
							className: "check",
							style: { alignSelf: "end" },
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("input", {
									type: "checkbox",
									checked: filters.needsAttention === "1",
									onChange: (e) => set({ needsAttention: e.target.checked ? "1" : "" })
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
									className: "check__box",
									"aria-hidden": "true"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("span", {
									className: "check__text",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
										className: "check__label",
										children: "Needs attention only"
									}), /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
										className: "check__hint",
										children: "Broken, under work, or PM due"
									})]
								})
							]
						})
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(Card, {
					pad: false,
					children: [error ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("div", {
						style: { padding: "var(--sp-4)" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(ErrorState, {
							error,
							onRetry: refresh
						})
					}) : /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(DataTable, {
						loading: loading && !data,
						rows,
						onRowClick: (row) => navigate(`/equipment/${row.id}`),
						caption: "Equipment inventory",
						columns: [
							{
								key: "select",
								label: "",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("input", {
									type: "checkbox",
									className: "row-check",
									"aria-label": `Select ${r.name}`,
									checked: selected.includes(r.id),
									onClick: (e) => e.stopPropagation(),
									onChange: (e) => setSelected((list) => e.target.checked ? [...list, r.id] : list.filter((x) => x !== r.id))
								})
							},
							{
								key: "name",
								label: "Equipment",
								sortable: true,
								dir: filters.sort === "name" ? filters.dir : null,
								onSort: toggleSort,
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("div", {
									className: "cell-main",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
										className: "cell-title",
										children: r.name
									}), /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("span", {
										className: "cell-sub",
										children: [
											r.manufacturer ? `${r.manufacturer} ${r.model ?? ""}`.trim() : r.categoryName,
											" · ",
											r.assetTag
										]
									})]
								})
							},
							{
								key: "locationLabel",
								label: "Location",
								render: (r) => r.locationLabel ?? r.locationName ?? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
									className: "muted",
									children: "Not recorded"
								})
							},
							{
								key: "status",
								label: "Status",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)("div", {
									className: "row row--tight",
									style: { flexWrap: "wrap" },
									children: [/* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(EquipmentStatusPill, { status: r.status }), r.openFaultCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsxs)(Badge, {
										tone: "warn",
										children: ["⚑ ", r.openFaultCount]
									}) : null]
								})
							},
							{
								key: "maintenanceState",
								label: "Maintenance",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(MaintenanceLight, {
									state: r.maintenanceState,
									daysUntil: r.daysUntilPm
								})
							},
							{
								key: "totalFaults",
								label: "Faults",
								align: "center",
								sortable: true,
								dir: filters.sort === "totalFaults" ? filters.dir : null,
								onSort: toggleSort,
								render: (r) => r.totalFaults ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("b", { children: r.totalFaults }) : /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
									className: "muted",
									children: "0"
								})
							},
							{
								key: "nextMaintenanceOn",
								label: "Next PM",
								align: "right",
								render: (r) => r.nextMaintenanceOn ?? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)("span", {
									className: "muted",
									children: "—"
								})
							}
						],
						empty: /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(EmptyState, {
							icon: "⚕",
							title: filters.q || active ? "Nothing matches those filters" : "The inventory is empty",
							description: filters.q || active ? "Try a broader search, or clear the filters. Equipment is normally found by asset tag (BMU-…) or serial number." : "Add the department’s first items, or scan a label that already exists.",
							action: filters.q || active ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Button, {
								tone: "secondary",
								onClick: () => setParams(new URLSearchParams(), { replace: true }),
								children: "Clear filters"
							}) : can("equipment.create") ? /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Button, {
								as: Link,
								to: "/equipment/new",
								tone: "primary",
								children: "＋ Add equipment"
							}) : null
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime$16.jsx)(Pagination, {
						pagination: data?.pagination,
						onPage: (page) => set({ page })
					})]
				})]
			})
		] });
	}
	var import_react$15, import_jsx_runtime$16;
	var init_EquipmentListPage = __esmMin((() => {
		import_react$15 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_useApi();
		import_jsx_runtime$16 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/components/FileInput.jsx
	/**
	* Photograph picker for fault evidence and repair before/after shots.
	*
	* Client-side checks (type, size, count) exist only to save the user a wasted upload; the
	* server re-validates the bytes (magic bytes, per-kind policy, limits) — see
	* server/src/lib/files.js.  `capture="environment"` is what makes a phone open the camera
	* directly, which is the whole point on a lab bench.
	*/
	function FileInput({ label = "Photographs", accept = "image/*", multiple = true, maxFiles = 5, maxSizeMb = 8, onChange, value = [], hint, capture, error, disabled, kind = "photo" }) {
		const inputRef = (0, import_react$14.useRef)(null);
		const [localError, setLocalError] = (0, import_react$14.useState)(null);
		const files = value ?? [];
		const validate = (list) => {
			const problems = [];
			const next = [...files];
			const allowedExt = [
				".jpg",
				".jpeg",
				".png",
				".webp",
				".gif",
				".pdf",
				".txt",
				".csv",
				".docx"
			];
			for (const file of list) {
				const ext = `.${(file.name.split(".").pop() ?? "").toLowerCase()}`;
				if (kind === "photo" && !file.type.startsWith("image/")) problems.push(`“${file.name}” is not a photo.`);
				if (!allowedExt.includes(ext)) problems.push(`“${file.name}” has an unsupported type (${ext}).`);
				if (file.size > maxSizeMb * 1024 * 1024) problems.push(`“${file.name}” is larger than ${maxSizeMb} MB.`);
				if (file.size === 0) problems.push(`“${file.name}” is empty.`);
				if (next.length >= maxFiles) {
					problems.push(`Only ${maxFiles} files can be attached to one report.`);
					break;
				}
				next.push(file);
			}
			return {
				next,
				problems
			};
		};
		const add = (fileList) => {
			setLocalError(null);
			const { next, problems } = validate(Array.from(fileList ?? []));
			if (problems.length) setLocalError(problems.join(" "));
			if (next.length !== files.length) onChange?.(next);
			if (inputRef.current) inputRef.current.value = "";
		};
		const removeAt = (index) => {
			const next = files.filter((_, i) => i !== index);
			onChange?.(next);
		};
		const [urls, setUrls] = (0, import_react$14.useState)([]);
		(0, import_react$14.useEffect)(() => {
			const created = files.filter((f) => f.type.startsWith("image/")).map((f) => URL.createObjectURL(f));
			setUrls(created);
			return () => created.forEach((u) => URL.revokeObjectURL(u));
		}, [files]);
		let cursor = -1;
		const previews = files.map((f, i) => {
			const isImage = f.type.startsWith("image/");
			if (isImage) cursor += 1;
			return {
				file: f,
				i,
				url: isImage ? urls[cursor] : null
			};
		});
		return /* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("div", {
			className: cx("fileinput", error && "fileinput--error"),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("div", {
					className: "fileinput__head",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("p", {
						className: "fileinput__label",
						children: label
					}), /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("p", {
						className: "fileinput__hint",
						children: hint ?? `Up to ${maxFiles} files · max ${maxSizeMb} MB each · photos and PDFs`
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("div", {
					className: "fileinput__actions",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("label", {
						className: "btn btn--secondary",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("input", {
								ref: inputRef,
								type: "file",
								accept,
								multiple,
								disabled: disabled || files.length >= maxFiles,
								className: "sr-only",
								...capture ? { capture } : {},
								onChange: (e) => add(e.target.files)
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("span", {
								"aria-hidden": "true",
								children: "📷"
							}),
							" Add ",
							kind === "photo" ? "photo" : "file"
						]
					}), files.length ? /* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("span", {
						className: "fileinput__count",
						children: [
							files.length,
							"/",
							maxFiles,
							" attached"
						]
					}) : null]
				}),
				previews.length ? /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("ul", {
					className: "fileinput__list",
					children: previews.map(({ file, i, url }) => /* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("li", {
						className: "thumb",
						children: [
							url ? /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("img", {
								src: url,
								alt: `Selected: ${file.name}`,
								className: "thumb__img"
							}) : /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("span", {
								className: "thumb__doc",
								"aria-hidden": "true",
								children: (file.name.split(".").pop() ?? "file").toUpperCase()
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$15.jsxs)("span", {
								className: "thumb__meta",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("span", {
									className: "thumb__name",
									title: file.name,
									children: file.name
								}), /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("span", {
									className: "thumb__size",
									children: formatBytes(file.size)
								})]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$15.jsx)(Button, {
								size: "sm",
								tone: "ghost",
								onClick: () => removeAt(i),
								"aria-label": `Remove ${file.name}`,
								children: "✕"
							})
						]
					}, `${file.name}-${i}`))
				}) : /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("p", {
					className: "fileinput__empty",
					children: "No files chosen yet. A photo of the fault helps the technician enormously."
				}),
				localError || error ? /* @__PURE__ */ (0, import_jsx_runtime$15.jsx)("p", {
					className: "form-error",
					role: "alert",
					children: localError || error
				}) : null
			]
		});
	}
	var import_react$14, import_jsx_runtime$15;
	var init_FileInput = __esmMin((() => {
		import_react$14 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_ui();
		init_format();
		import_jsx_runtime$15 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/EquipmentDetailPage.jsx
	var EquipmentDetailPage_exports = /* @__PURE__ */ __exportAll({ EquipmentDetailPage: () => EquipmentDetailPage$1 });
	/**
	* The equipment profile — the single page that answers "what is this, is it working, who has
	* it, what has been done to it, when is service due, what is the risk".  It is also the page a
	* QR label lands on.
	*/
	function EquipmentDetailPage$1() {
		const { id } = useParams();
		const navigate = useNavigate();
		const { can, is } = useAuth();
		const toast = useToast();
		const [tab, setTab] = (0, import_react$13.useState)("overview");
		const [statusOpen, setStatusOpen] = (0, import_react$13.useState)(false);
		const [imageOpen, setImageOpen] = (0, import_react$13.useState)(false);
		const [imageFiles, setImageFiles] = (0, import_react$13.useState)([]);
		const [busy, setBusy] = (0, import_react$13.useState)(false);
		const [qrOpen, setQrOpen] = (0, import_react$13.useState)(false);
		const copy = useCopy();
		const { data, loading, error, refresh, setData } = useApi(() => equipment.get(id), [id]);
		const eq = data?.equipment;
		const perms = data?.permissions ?? {};
		if (error) return /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(ErrorState, {
			error,
			onRetry: refresh
		});
		if (loading && !eq) return /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Loading, {
			rows: 8,
			label: "Loading the equipment record"
		});
		const photo = (data.attachments ?? []).find((a) => a.kind === "photo");
		const isDecommissioned = eq.status === "decommissioned";
		const saveImage = async () => {
			if (!imageFiles.length) {
				toast.warn("Choose a photograph first.");
				return;
			}
			setBusy(true);
			try {
				await equipment.uploadImage(eq.id, imageFiles[0]);
				setImageOpen(false);
				setImageFiles([]);
				toast.success("Photograph updated.");
				refresh();
			} catch (err) {
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(PageHeader, {
				back: {
					to: "/equipment",
					label: "All equipment"
				},
				eyebrow: eq.categoryName,
				title: eq.name,
				actions: /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [
					/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
						as: Link,
						to: `/report/${eq.assetTag}`,
						tone: "danger",
						disabled: isDecommissioned,
						children: "⚑ Report a fault"
					}),
					perms.canSetStatus ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
						tone: "secondary",
						onClick: () => setStatusOpen(true),
						children: "Change status"
					}) : null,
					can("equipment.qr") ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
						tone: "secondary",
						onClick: () => setQrOpen(true),
						children: "▣ QR label"
					}) : null,
					perms.canEdit ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
						as: Link,
						to: `/equipment/${eq.id}/edit`,
						tone: "ghost",
						children: "Edit"
					}) : null
				] })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
				className: "detail-head",
				style: { marginBottom: "var(--sp-5)" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
					className: "grid",
					style: { gap: "var(--sp-3)" },
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
							className: "row",
							style: { gap: 8 },
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EquipmentStatusPill, {
									status: eq.status,
									size: "lg"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(MaintenanceLight, {
									state: eq.maintenanceState,
									daysUntil: eq.daysUntilPm,
									size: "lg"
								}),
								eq.openFaultCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Badge, {
									tone: "bad",
									size: "lg",
									children: ["⚑ ", pluralise(eq.openFaultCount, "open fault report")]
								}) : null,
								eq.isAvailable ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
									tone: "ok",
									size: "lg",
									children: "Available for use"
								}) : null,
								eq.criticality === "life_support" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
									tone: "bad",
									size: "lg",
									children: "Life-support class"
								}) : null
							]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
							className: "asset-tag",
							title: "Asset tag — printed on the QR label",
							children: eq.assetTag
						}),
						!eq.isAvailable ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Callout, {
							tone: "warn",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
								className: "callout__icon",
								"aria-hidden": "true",
								children: "⚠"
							}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
								className: "callout__main",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
									className: "callout__title",
									children: EQUIPMENT_STATUS[eq.status]?.hint ?? "This item is not currently available"
								}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
									className: "callout__body",
									children: "Do not put it back into teaching or clinical use without the technician sign-off recorded in this file."
								})]
							})]
						}) : null
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
					style: { width: "min(100%, 300px)" },
					children: [photo ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("figure", {
						style: {
							margin: 0,
							display: "grid",
							gap: 6
						},
						children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("img", {
							className: "eq-photo",
							src: attachments.viewUrl(photo.id),
							alt: `${eq.name} — ${photo.filename}`
						}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("figcaption", {
							className: "cell-sub",
							children: ["Photograph on file · ", formatDate(photo.createdAt)]
						})]
					}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
						className: "eq-photo--empty",
						children: perms.canEdit ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", { children: "No photograph on file" }), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
							size: "sm",
							tone: "secondary",
							onClick: () => setImageOpen(true),
							children: "Add one"
						})] }) : "No photograph on file"
					}), perms.canEdit && photo ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
						size: "sm",
						tone: "ghost",
						onClick: () => setImageOpen(true),
						children: "Replace photograph"
					}) : null]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Tabs, {
				active: tab,
				onChange: setTab,
				tabs: [
					{
						id: "overview",
						label: "Overview"
					},
					{
						id: "faults",
						label: "Faults",
						count: data.faults.length
					},
					{
						id: "maintenance",
						label: "Maintenance",
						count: (data.maintenance?.length ?? 0) + (data.schedules?.length ?? 0)
					},
					{
						id: "repairs",
						label: "Repairs",
						count: data.repairs?.length ?? 0
					},
					{
						id: "risk",
						label: "Risk",
						count: data.risk ? `${data.risk.score}` : null
					},
					{
						id: "files",
						label: "Files & label",
						count: data.attachments.length
					}
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
				className: "tabpanel",
				children: [
					tab === "overview" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
						className: "grid",
						style: { gap: "var(--sp-4)" },
						children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
							title: "Identification and ownership",
							children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(KeyValue, { items: [
								["Asset tag", /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("code", { children: eq.assetTag })],
								["Name", eq.name],
								["Category", eq.categoryName],
								["Manufacturer", eq.manufacturer],
								["Model", eq.model],
								["Serial number", /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("code", { children: eq.serialNumber })],
								["Department", eq.department],
								["Location", eq.locationLabel ?? eq.locationName],
								["Responsible person", eq.custodianName ?? eq.custodianNote],
								["Responsible technician", eq.responsibleTechnicianName],
								["Acquired", eq.acquiredOn ? `${formatDate(eq.acquiredOn)}${eq.ageYears ? ` (${eq.ageYears} years old)` : ""}` : null],
								["Warranty", eq.warrantyExpiresOn ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("span", { children: [
									formatDate(eq.warrantyExpiresOn),
									" ",
									eq.warrantyExpiresOn < (/* @__PURE__ */ new Date()).toISOString().slice(0, 10) ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
										tone: "neutral",
										size: "sm",
										children: "expired"
									}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
										tone: "ok",
										size: "sm",
										children: "in warranty"
									}),
									" ",
									eq.warrantyProvider ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("span", {
										className: "muted",
										children: [" · ", eq.warrantyProvider]
									}) : null
								] }) : null],
								["Criticality", eq.criticality],
								["Notes", eq.notes]
							] })
						}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
							className: "grid grid--2",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
								title: "At a glance",
								children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(KeyValue, {
									columns: 1,
									items: [
										["Fault reports (all time)", eq.totalFaults],
										["Open now", eq.openFaultCount],
										["Critical open", eq.criticalFaultCount],
										["Repairs recorded", eq.repairCount],
										["Preventive visits", eq.pmRecordCount],
										["Average time to repair", formatHours(data.statistics?.averageRepairHours)],
										["Lifetime repair cost", eq.lifetimeRepairCost !== null ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Money, { amount: eq.lifetimeRepairCost }) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
											className: "muted",
											children: "Not shown for your role"
										})],
										["Availability (180 d)", data.downtime?.availabilityPercent != null ? `${data.downtime.availabilityPercent}%` : "—"],
										["Downtime (180 d)", data.downtime ? `${data.downtime.days} days over ${pluralise(data.downtime.incidents, "incident")}` : "—"]
									]
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Card, {
								title: "Status trail",
								subtitle: "How this item reached its current state",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Timeline, { items: (data.statusHistory ?? []).slice(0, 8).map((h) => ({
									id: `${h.changedAt}-${h.toStatus}`,
									title: h.fromStatus ? `${h.fromStatus.replace(/_/g, " ")} → ${h.toStatus.replace(/_/g, " ")}` : `Set to ${h.toStatus.replace(/_/g, " ")}`,
									detail: h.reason,
									actor: h.changedByName,
									meta: h.faultReference ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Link, {
										to: `/faults/${h.faultId}`,
										children: h.faultReference
									}) : null,
									at: h.changedAt,
									tone: h.toStatus === "operational" ? "ok" : h.toStatus === "out_of_service" ? "bad" : "warn"
								})) }), !(data.statusHistory ?? []).length ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
									className: "form-note",
									children: "No status changes recorded yet."
								}) : null]
							})]
						})]
					}) : null,
					tab === "faults" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
						title: "Fault reports on this item",
						subtitle: "Newest first — open reports are being handled",
						pad: false,
						children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(DataTable, {
							rows: data.faults,
							empty: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyBlock, {
								title: "No fault reports recorded",
								text: "That is either good equipment or unreported problems. Encourage reporting: a log with nothing in it helps nobody."
							}),
							onRowClick: (r) => navigate(`/faults/${r.id}`),
							columns: [
								{
									key: "reference",
									label: "Reference",
									mono: true,
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("code", { children: r.reference }), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
										className: "cell-sub",
										children: formatDateTime(r.createdAt)
									})] })
								},
								{
									key: "title",
									label: "Title",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
										className: "cell-main",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
											className: "cell-title",
											children: r.title
										}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("span", {
											className: "cell-sub",
											children: [
												r.categoryName,
												" · ",
												r.reportedByName
											]
										})]
									})
								},
								{
									key: "severity",
									label: "Severity",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(SeverityPill, { severity: r.severity })
								},
								{
									key: "status",
									label: "Status",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(FaultStatusPill, { status: r.status })
								},
								{
									key: "assignedToName",
									label: "Assigned",
									render: (r) => r.assignedToName ?? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
										className: "muted",
										children: "Unassigned"
									})
								},
								{
									key: "open",
									label: "",
									render: (r) => OPEN.includes(r.status) ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
										tone: "warn",
										size: "sm",
										children: "open"
									}) : null
								}
							]
						})
					}) : null,
					tab === "maintenance" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
						className: "grid",
						style: { gap: "var(--sp-4)" },
						children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
							title: "Preventive maintenance schedules",
							actions: perms.canConfigureMaintenance ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
								size: "sm",
								as: Link,
								to: `/maintenance?equipment=${eq.id}`,
								children: "Manage schedules"
							}) : null,
							pad: false,
							children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(DataTable, {
								rows: data.schedules ?? [],
								empty: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyBlock, {
									title: "No schedule configured",
									text: "Without one, this item never appears on the due board — which is how equipment quietly becomes unserviceable."
								}),
								columns: [
									{
										key: "title",
										label: "Schedule"
									},
									{
										key: "intervalDays",
										label: "Interval",
										align: "center",
										render: (r) => `${r.intervalDays} days`
									},
									{
										key: "lastDoneOn",
										label: "Last done",
										render: (r) => formatDate(r.lastDoneOn)
									},
									{
										key: "nextDueOn",
										label: "Next due",
										render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("span", { children: [
											formatDate(r.nextDueOn),
											" ",
											/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(MaintenanceLight, { state: r.maintenanceState })
										] })
									},
									{
										key: "checklistCount",
										label: "Checks",
										align: "center"
									},
									{
										key: "responsibleTechnicianName",
										label: "Owner",
										render: (r) => r.responsibleTechnicianName ?? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
											className: "muted",
											children: "Unassigned"
										})
									}
								]
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
							title: "Maintenance history",
							pad: false,
							children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(DataTable, {
								rows: data.maintenance ?? [],
								empty: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyBlock, {
									title: "No preventive maintenance recorded",
									text: "First visit will appear here once a technician signs it off."
								}),
								columns: [
									{
										key: "reference",
										label: "Reference",
										mono: true
									},
									{
										key: "performedOn",
										label: "Date",
										render: (r) => formatDate(r.performedOn)
									},
									{
										key: "conditionFound",
										label: "Condition",
										render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
											tone: r.conditionFound === "pass" ? "ok" : r.conditionFound ? "warn" : "neutral",
											children: (r.conditionFound ?? "—").replace(/_/g, " ")
										})
									},
									{
										key: "findings",
										label: "Findings"
									},
									{
										key: "durationMinutes",
										label: "Time",
										align: "right",
										render: (r) => r.durationMinutes ? `${r.durationMinutes} min` : "—"
									},
									{
										key: "performedByName",
										label: "By"
									}
								]
							})
						})]
					}) : null,
					tab === "repairs" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
						title: "Repair records",
						subtitle: "What was found, done and proven — recorded and signed by a technician",
						pad: false,
						children: data.repairs?.length ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
							className: "stack",
							style: { padding: "var(--sp-4)" },
							children: data.repairs.map((r) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
								className: "repair-card",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
									className: "row row--between",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
										className: "cell-title",
										children: r.diagnosis
									}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("p", {
										className: "cell-sub",
										children: [
											r.reference,
											" · ",
											formatDate(r.dateRepaired),
											" · ",
											r.technicianName,
											r.faultReference ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [" · ", /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Link, {
												to: `/faults/${r.faultId ?? ""}`,
												children: r.faultReference
											})] }) : null
										]
									})] }), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Money, {
										amount: r.totalCost,
										currency: r.currency
									})]
								}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(KeyValue, {
									columns: 2,
									items: [
										["Root cause", r.rootCause],
										["Repair performed", r.repairActions],
										["Parts replaced", r.partsReplacedSummary ?? (r.parts?.length ? r.parts.map((p) => `${p.quantity} × ${p.partName}`).join(", ") : null)],
										["Test results", r.testResults],
										["Calibration", r.calibrationPerformed ? r.calibrationDetails ?? "performed" : null],
										["Safe to return to service", r.safeToReturnToService ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
											tone: "ok",
											children: "yes"
										}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
											tone: "bad",
											children: "no"
										})]
									]
								})]
							}, r.id))
						}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyBlock, {
							title: "No repairs recorded",
							text: "Repairs appear here as soon as a technician files them against a fault report."
						})
					}) : null,
					tab === "risk" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, { children: data.risk ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(RiskPanel, { risk: data.risk }) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
						className: "form-note",
						children: "No risk assessment available."
					}) }) : null,
					tab === "files" ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
						className: "grid grid--2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
							title: "Attachments",
							pad: false,
							children: data.attachments.length ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("ul", {
								className: "attach-grid",
								style: { padding: "var(--sp-4)" },
								children: data.attachments.map((a) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("li", {
									className: "attach",
									children: [a.mimeType.startsWith("image/") ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("img", {
										className: "attach__img",
										src: attachments.viewUrl(a.id),
										alt: a.caption ?? a.filename
									}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("a", {
										className: "attach__doc",
										href: attachments.viewUrl(a.id),
										target: "_blank",
										rel: "noreferrer",
										children: a.mimeType.includes("pdf") ? "📄 PDF" : "📄 file"
									}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
										className: "attach__meta",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", {
											className: "attach__name",
											title: a.filename,
											children: a.filename
										}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("span", { children: [
											a.kind.replace(/_/g, " "),
											" · ",
											formatDate(a.createdAt),
											" · ",
											a.uploadedByName ?? ""
										] })]
									})]
								}, a.id))
							}) : /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyBlock, {
								title: "No files attached",
								text: "Manuals, calibration certificates and photographs can be attached here."
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Card, {
							title: "QR label",
							subtitle: "Scan-to-report identity for this item",
							children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
								className: "qr-box",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("img", {
										className: "qr-box__img",
										src: equipment.qrUrl(eq.id),
										alt: `QR code linking to the profile of ${eq.assetTag}`
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
										className: "qr-box__tag",
										children: eq.assetTag
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
										className: "qr-box__note",
										children: "Anyone who scans this opens this equipment’s status and a fault-report form. Keep the printed tag legible; laminate for clinical areas."
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
										className: "row",
										style: { justifyContent: "center" },
										children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
											tone: "secondary",
											size: "sm",
											onClick: () => window.print(),
											children: "🖨 Print label"
										}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
											tone: "ghost",
											size: "sm",
											onClick: () => {
												copy.copy(data?.qr?.url ?? `${window.location.origin}/e/${eq.assetTag}`);
												toast.success("Profile link copied.");
											},
											children: "Copy link"
										})]
									})
								]
							})
						})]
					}) : null
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(StatusDialog, {
				open: statusOpen,
				onClose: () => setStatusOpen(false),
				equipment: eq,
				onSaved: async () => {
					setStatusOpen(false);
					await refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Modal, {
				open: imageOpen,
				onClose: () => setImageOpen(false),
				title: "Equipment photograph",
				description: "A clear photo of the front panel helps identification and shows existing damage.",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(FileInput, {
						label: "Photograph",
						kind: "photo",
						maxFiles: 1,
						value: imageFiles,
						onChange: setImageFiles
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
						className: "form-note",
						children: "Photos may contain location metadata from the camera. They are only visible to people who can open this equipment record."
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
						className: "row row--end",
						style: { marginTop: 8 },
						children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
							tone: "ghost",
							onClick: () => setImageOpen(false),
							children: "Cancel"
						}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
							tone: "primary",
							loading: busy,
							onClick: saveImage,
							children: "Save photograph"
						})]
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Modal, {
				open: qrOpen,
				onClose: () => setQrOpen(false),
				title: `QR label · ${eq.assetTag}`,
				description: "Print at 100% scale on adhesive stock. The code opens this item’s status and fault-report form.",
				children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
					className: "qr-box",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("img", {
							className: "qr-box__img",
							src: equipment.qrUrl(eq.id),
							alt: "QR code for this equipment"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
							className: "qr-box__tag",
							children: eq.assetTag
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
							className: "qr-box__note",
							children: eq.name
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
							className: "qr-box__note mono",
							children: data.qr?.url
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
							className: "row",
							style: { justifyContent: "center" },
							children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
								tone: "primary",
								onClick: () => window.print(),
								children: "🖨 Print"
							}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("a", {
								className: "btn btn--secondary",
								href: equipment.qrUrl(eq.id, "svg"),
								download: `${eq.assetTag}-qr.svg`,
								children: "Download SVG"
							})]
						})
					]
				})
			})
		] });
	}
	function EmptyBlock({ title, text }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
			style: { padding: "var(--sp-4)" },
			children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(EmptyState, {
				icon: "📄",
				title,
				description: text
			})
		});
	}
	function RiskPanel({ risk }) {
		useNavigate();
		return /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
			className: "stack",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
					className: "row row--between",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(RiskBadge, {
						level: risk.level,
						score: risk.score,
						size: "lg"
					}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Badge, {
						tone: "neutral",
						children: risk.modelVersion
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Callout, {
					tone: "safety",
					title: "Decision support, not a verdict",
					children: risk.disclaimer
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("p", {
					className: "form-note",
					children: [
						"Score ",
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("b", { children: [
							risk.rawScore,
							"/",
							risk.rawMax
						] }),
						" raw → ",
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("b", { children: [risk.score, "/100"] }),
						". Every point below comes from a row in this department’s own history — open the record it points at to check it."
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", { children: risk.factors.map((f) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
					className: "factor",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("div", {
							className: "factor__head",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
								className: "factor__label",
								children: f.label
							}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("p", {
								className: "factor__points",
								children: [
									f.points,
									" / ",
									f.maxPoints,
									" points"
								]
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
							className: "pbar",
							style: { marginBottom: 6 },
							children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
								className: "pbar__track",
								style: { height: 6 },
								children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("div", {
									className: `pbar__fill ${f.points > f.maxPoints * .6 ? "pbar__fill--bad" : f.points > 0 ? "pbar__fill--warn" : "pbar__fill--ok"}`,
									style: { width: `${Math.min(100, f.points / f.maxPoints * 100)}%` }
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
							className: "factor__basis",
							children: f.basis
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("ul", {
							className: "factor__list",
							children: (f.contributing ?? []).map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("span", { children: c }) }, i))
						}),
						f.advice ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("p", {
							className: "factor__advice",
							children: ["→ ", f.advice]
						}) : null
					]
				}, f.key)) }),
				risk.escalation ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Callout, {
					tone: "warn",
					title: "Escalated to High",
					children: [
						risk.escalation.rule,
						". ",
						risk.escalation.reason
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)("details", {
					className: "factor__details",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("summary", { children: "Inputs used" }), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(KeyValue, {
						columns: 2,
						items: Object.entries(risk.inputSnapshot ?? {}).map(([k, v]) => [k.replace(/([A-Z])/g, " $1").toLowerCase(), v ?? "—"])
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
					className: "chart__foot",
					children: SAFETY_NOTICE
				})
			]
		});
	}
	function StatusDialog({ open, onClose, equipment: equipment$1, onSaved }) {
		const [status, setStatus] = (0, import_react$13.useState)(equipment$1?.status ?? "operational");
		(0, import_react$13.useEffect)(() => {
			if (open) {
				setStatus(equipment$1?.status ?? "operational");
				setReason("");
				setFormError(null);
			}
		}, [
			open,
			equipment$1?.id,
			equipment$1?.status
		]);
		const [reason, setReason] = (0, import_react$13.useState)("");
		const [busy, setBusy] = (0, import_react$13.useState)(false);
		const [formError, setFormError] = (0, import_react$13.useState)(null);
		const needsReason = ["out_of_service", "decommissioned"].includes(status);
		const options = (0, import_react$13.useMemo)(() => Object.entries(EQUIPMENT_STATUS).map(([value, meta]) => ({
			value,
			label: meta.label
		})), []);
		const submit = async () => {
			setBusy(true);
			setFormError(null);
			try {
				await equipment.setStatus(equipment$1.id, status, reason || void 0);
				await onSaved();
				toast.success(`${equipment$1.assetTag} is now ${EQUIPMENT_STATUS[status].label.toLowerCase()}.`);
			} catch (err) {
				setFormError(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(Modal, {
			open,
			onClose,
			title: "Change equipment status",
			description: "Official status is a statement to the whole department, so it is recorded with who set it and why.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$14.jsxs)(import_jsx_runtime$14.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Button, {
				tone: "primary",
				loading: busy,
				disabled: needsReason && !reason.trim(),
				onClick: submit,
				children: "Save status"
			})] }),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Field, {
					label: "Status",
					children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Select, {
						value: status,
						onChange: (e) => setStatus(e.target.value),
						options
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(Field, {
					label: needsReason ? "Reason (required)" : "Reason (optional)",
					hint: status === "operational" ? "Equipment with an unresolved fault report cannot be returned to service — move the report forward first." : "What changed, and on whose instruction?",
					children: /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)(TextArea, {
						rows: 3,
						value: reason,
						onChange: (e) => setReason(e.target.value)
					})
				}),
				formError ? /* @__PURE__ */ (0, import_jsx_runtime$14.jsx)("p", {
					className: "form-error",
					role: "alert",
					children: formError
				}) : null
			]
		});
	}
	var import_react$13, import_jsx_runtime$14, OPEN;
	var init_EquipmentDetailPage = __esmMin((() => {
		import_react$13 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_format();
		init_FileInput();
		init_Toast();
		init_useApi();
		init_constants();
		import_jsx_runtime$14 = require_jsx_runtime();
		OPEN = [
			"reported",
			"assigned",
			"acknowledged",
			"under_inspection",
			"under_repair",
			"awaiting_parts"
		];
	}));
	//#endregion
	//#region src/pages/EquipmentFormPage.jsx
	var EquipmentFormPage_exports = /* @__PURE__ */ __exportAll({ EquipmentFormPage: () => EquipmentFormPage$1 });
	function EquipmentFormPage$1() {
		const { id } = useParams();
		const navigate = useNavigate();
		const toast = useToast();
		const isEdit = Boolean(id);
		const [form, setForm] = (0, import_react$12.useState)(null);
		const [errors, setErrors] = (0, import_react$12.useState)({});
		const [busy, setBusy] = (0, import_react$12.useState)(false);
		const [deleteOpen, setDeleteOpen] = (0, import_react$12.useState)(false);
		const [deleteBusy, setDeleteBusy] = (0, import_react$12.useState)(false);
		const picklists = useApi(() => reference.picklists(), []);
		const existing = useApi(() => isEdit ? equipment.get(id) : Promise.resolve(null), [id]);
		const technicians = picklists.data?.technicians ?? [];
		const custodians = picklists.data?.custodians ?? [];
		(0, import_react$12.useEffect)(() => {
			if (isEdit) {
				if (!existing.data) return;
				const e = existing.data.equipment;
				setForm({
					name: e.name ?? "",
					categoryId: e.categoryId ?? "",
					manufacturer: e.manufacturer ?? "",
					model: e.model ?? "",
					serialNumber: e.serialNumber ?? "",
					department: e.department ?? "",
					locationId: e.locationId ?? "",
					custodianUserId: e.custodianUserId ?? "",
					custodianNote: e.custodianNote ?? "",
					acquiredOn: toDateInput(e.acquiredOn),
					warrantyProvider: e.warrantyProvider ?? "",
					warrantyExpiresOn: toDateInput(e.warrantyExpiresOn),
					criticality: e.criticality ?? "medium",
					notes: e.notes ?? "",
					maintenanceIntervalDays: e.maintenanceIntervalDays ?? "",
					lastMaintenanceOn: toDateInput(e.lastMaintenanceOn),
					responsibleTechnicianId: e.responsibleTechnicianId ?? ""
				});
			} else setForm({
				name: "",
				categoryId: "",
				manufacturer: "",
				model: "",
				serialNumber: "",
				department: "Biomedical Engineering",
				locationId: "",
				custodianUserId: "",
				custodianNote: "",
				acquiredOn: "",
				warrantyProvider: "",
				warrantyExpiresOn: "",
				criticality: "medium",
				notes: "",
				maintenanceIntervalDays: "180",
				lastMaintenanceOn: "",
				responsibleTechnicianId: ""
			});
		}, [isEdit, existing.data]);
		const set = (patch) => {
			setForm((f) => ({
				...f,
				...patch
			}));
			setErrors((e) => {
				const n = { ...e };
				for (const k of Object.keys(patch)) delete n[k];
				return n;
			});
		};
		const validate = () => {
			const problems = {};
			if (!form.name?.trim() || form.name.trim().length < 2) problems.name = "Enter the equipment name (at least 2 characters).";
			if (!form.categoryId) problems.categoryId = "Choose a category.";
			if (form.serialNumber && form.serialNumber.length > LIMITS.serialNumber) problems.serialNumber = "Keep the serial number under 80 characters.";
			if (form.acquiredOn && form.acquiredOn > (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)) problems.acquiredOn = "Acquisition cannot be in the future.";
			if (form.acquiredOn && form.warrantyExpiresOn && form.warrantyExpiresOn < form.acquiredOn) problems.warrantyExpiresOn = "Warranty cannot expire before the item was acquired.";
			if (form.maintenanceIntervalDays && (Number(form.maintenanceIntervalDays) < 1 || Number(form.maintenanceIntervalDays) > 3650)) problems.maintenanceIntervalDays = "Between 1 and 3650 days.";
			for (const [key, max] of Object.entries(LIMITS)) if (form[key] && form[key].length > max) problems[key] = `Too long (max ${max} characters).`;
			return problems;
		};
		const submit = async (event) => {
			event.preventDefault();
			const problems = validate();
			if (Object.keys(problems).length) {
				setErrors(problems);
				document.querySelector(".field--error input, .field--error select, .field--error textarea")?.focus();
				return;
			}
			setBusy(true);
			const payload = Object.fromEntries(Object.entries(form).filter(([, v]) => v !== "" && v !== null));
			try {
				const saved = isEdit ? await equipment.update(id, payload) : await equipment.create(payload);
				toast.success(isEdit ? "Equipment record updated." : `Added ${saved.equipment?.assetTag ?? saved.assetTag}. A QR label is ready on its profile.`);
				navigate(`/equipment/${saved.equipment?.id ?? saved.id}`);
			} catch (err) {
				setErrors(mapFieldErrors(err));
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const remove = async ({ confirmText }) => {
			setDeleteBusy(true);
			try {
				await equipment.remove(id, { confirmTag: confirmText });
				toast.success("Record deleted.");
				navigate("/equipment");
			} catch (err) {
				setErrors(mapFieldErrors(err));
				toast.error(errorText(err));
				setDeleteOpen(false);
			} finally {
				setDeleteBusy(false);
			}
		};
		const nextDueHint = (0, import_react$12.useMemo)(() => {
			if (!form?.maintenanceIntervalDays) return "No interval set — this item will never appear on the due board.";
			const from = form?.lastMaintenanceOn || (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
			return `Next preventive maintenance would fall due ${new Date(Date.parse(from) + Number(form?.maintenanceIntervalDays) * 864e5).toISOString().slice(0, 10)}.`;
		}, [form?.maintenanceIntervalDays, form?.lastMaintenanceOn]);
		if (existing.error) return /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(ErrorState, {
			error: existing.error,
			onRetry: existing.refresh
		});
		if (!form) return /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Loading, {
			rows: 9,
			label: "Loading the form"
		});
		return /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)(import_jsx_runtime$13.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(PageHeader, {
				back: {
					to: isEdit ? `/equipment/${id}` : "/equipment",
					label: isEdit ? "Back to the record" : "All equipment"
				},
				eyebrow: isEdit ? "Edit record" : "New record",
				title: isEdit ? form.name || "Edit equipment" : "Add equipment",
				description: "Every field here exists because a department has been asked it at 8 pm when something failed. Leave a field empty rather than guessing — an empty field reads as “unknown”, a guess does not."
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("form", {
				onSubmit: submit,
				noValidate: true,
				className: "stack",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
					className: "grid grid--2",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Card, {
							title: "Identification",
							children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
								className: "form-grid",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Equipment name",
										required: true,
										error: errors.name,
										hint: `As the department calls it, e.g. “12-Lead ECG Workstation #02”.`,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.name,
											onChange: (e) => set({ name: e.target.value }),
											maxLength: LIMITS.name + 20,
											placeholder: "Patient monitor, ultrasound, centrifuge…"
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Category",
										required: true,
										error: errors.categoryId,
										hint: "Determines the asset-tag prefix and the printed label.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Select, {
											value: form.categoryId,
											onChange: (e) => set({ categoryId: e.target.value }),
											placeholder: "Choose a category…",
											options: (picklists.data?.categories ?? []).map((c) => ({
												value: c.id,
												label: `${c.name} (${c.code})`
											}))
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Manufacturer",
										error: errors.manufacturer,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.manufacturer,
											onChange: (e) => set({ manufacturer: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Model",
										error: errors.model,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.model,
											onChange: (e) => set({ model: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Serial number",
										error: errors.serialNumber,
										hint: "Must be unique; it is how the workshop matches a part or a warranty claim.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.serialNumber,
											onChange: (e) => set({ serialNumber: e.target.value }),
											className: "mono"
										})
									})
								]
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Card, {
							title: "Where it lives",
							children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
								className: "form-grid",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Room / location",
										error: errors.locationId,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Select, {
											value: form.locationId,
											onChange: (e) => set({ locationId: e.target.value }),
											placeholder: "Not assigned…",
											options: (picklists.data?.locations ?? []).map((l) => ({
												value: l.id,
												label: `${l.name}${l.room ? ` · ${l.room}` : ""}`
											}))
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Department / unit",
										error: errors.department,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.department,
											onChange: (e) => set({ department: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Responsible person",
										error: errors.custodianUserId,
										hint: "The staff member who keeps this item. Users are still able to report faults themselves.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Select, {
											value: form.custodianUserId,
											onChange: (e) => set({ custodianUserId: e.target.value }),
											placeholder: "Not recorded…",
											options: custodians.map((c) => ({
												value: c.id,
												label: c.fullName
											}))
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "…or free-text custodian",
										error: errors.custodianNote,
										hint: "For a lab or class that is not an account holder.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.custodianNote,
											onChange: (e) => set({ custodianNote: e.target.value }),
											placeholder: "e.g. Physiology demo bench 3"
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Criticality",
										error: errors.criticality,
										hint: "Feeds the response target and the risk indicator. Life-support class is escalated automatically.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Select, {
											value: form.criticality,
											onChange: (e) => set({ criticality: e.target.value }),
											options: (picklists.data?.criticalities ?? []).map((c) => ({
												value: c.value,
												label: c.label
											}))
										})
									})
								]
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Card, {
							title: "Acquisition and warranty",
							children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
								className: "form-grid",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Purchase / acquisition date",
										error: errors.acquiredOn,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											type: "date",
											value: form.acquiredOn,
											onChange: (e) => set({ acquiredOn: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Warranty provider",
										error: errors.warrantyProvider,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											value: form.warrantyProvider,
											onChange: (e) => set({ warrantyProvider: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Warranty expires",
										error: errors.warrantyExpiresOn,
										hint: "The department is shown a badge while an item is still in warranty, which is when a repair should be a claim, not a cost.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											type: "date",
											value: form.warrantyExpiresOn,
											onChange: (e) => set({ warrantyExpiresOn: e.target.value })
										})
									})
								]
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)(Card, {
							title: "Preventive maintenance",
							subtitle: nextDueHint,
							children: [/* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
								className: "form-grid",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Interval (days)",
										error: errors.maintenanceIntervalDays,
										hint: "Common: 30 daily checks, 90 quarterly, 180 half-yearly, 365 annual.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											type: "number",
											min: "1",
											max: "3650",
											inputMode: "numeric",
											value: form.maintenanceIntervalDays,
											onChange: (e) => set({ maintenanceIntervalDays: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Last maintenance performed",
										error: errors.lastMaintenanceOn,
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextInput, {
											type: "date",
											value: form.lastMaintenanceOn,
											onChange: (e) => set({ lastMaintenanceOn: e.target.value })
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
										label: "Responsible technician",
										error: errors.responsibleTechnicianId,
										hint: "Receives the due and overdue reminders.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Select, {
											value: form.responsibleTechnicianId,
											onChange: (e) => set({ responsibleTechnicianId: e.target.value }),
											placeholder: "Unassigned…",
											options: technicians.map((t) => ({
												value: t.id,
												label: `${t.fullName}${t.openFaults ? ` (${t.openFaults} open)` : ""}`
											}))
										})
									})
								]
							}), /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Callout, {
								tone: "info",
								title: "Checklist",
								children: "A default checklist is created from the interval name when you add a schedule on the maintenance screen. Edit it there to match the manufacturer manual — the manual always wins."
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Card, {
							title: "Notes",
							children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Field, {
								label: "Internal notes",
								error: errors.notes,
								counter: {
									text: `${form.notes.length}/${LIMITS.notes}`,
									over: form.notes.length > LIMITS.notes
								},
								children: /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(TextArea, {
									rows: 4,
									value: form.notes,
									onChange: (e) => set({ notes: e.target.value }),
									placeholder: "Handling cautions, shared-use arrangements, anything the next technician should know."
								})
							})
						})
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
					className: "row row--between",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)("p", {
						className: "form-note",
						children: "Required fields are marked. The department can still be served with unknowns recorded as unknowns."
					}), /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)("div", {
						className: "row",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Button, {
							tone: "ghost",
							as: Link,
							to: isEdit ? `/equipment/${id}` : "/equipment",
							children: "Cancel"
						}), /* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Button, {
							tone: "primary",
							type: "submit",
							loading: busy,
							children: isEdit ? "Save changes" : "Create record"
						})]
					})]
				})]
			}),
			isEdit && picklists ? /* @__PURE__ */ (0, import_jsx_runtime$13.jsxs)(Card, {
				title: "Danger zone",
				className: "danger-zone",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)("p", {
						className: "form-note",
						children: "Deleting removes the record entirely. Anything with fault, repair or maintenance history cannot be deleted — the department keeps that trail; deactivate or decommission the item instead."
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(Button, {
						tone: "danger",
						onClick: () => setDeleteOpen(true),
						children: "Delete this record…"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$13.jsx)(ConfirmDialog, {
						open: deleteOpen,
						title: "Delete this equipment record?",
						body: "Only records with no history can be deleted. Type the asset tag to confirm.",
						confirmLabel: "Delete permanently",
						requireText: existing.data?.equipment?.assetTag,
						busy: deleteBusy,
						onCancel: () => setDeleteOpen(false),
						onConfirm: remove
					})
				]
			}) : null
		] });
	}
	/** Server field keys arrive snake_case (equipment table columns); map them onto the form. */
	function mapFieldErrors(err) {
		const map = {
			category_id: "categoryId",
			location_id: "locationId",
			serial_number: "serialNumber",
			custodian_user_id: "custodianUserId",
			responsible_technician_id: "responsibleTechnicianId",
			acquired_on: "acquiredOn",
			warranty_expires_on: "warrantyExpiresOn",
			maintenance_interval_days: "maintenanceIntervalDays",
			last_maintenance_on: "lastMaintenanceOn"
		};
		const out = {};
		for (const [k, v] of Object.entries(err?.fieldErrors ?? {})) {
			const key = Object.keys(map).includes(k) ? map[k] : k;
			out[key] = Array.isArray(v) ? v.join(" ") : String(v);
		}
		return out;
	}
	var import_react$12, import_jsx_runtime$13, LIMITS;
	var init_EquipmentFormPage = __esmMin((() => {
		import_react$12 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$13 = require_jsx_runtime();
		LIMITS = {
			name: 160,
			manufacturer: 120,
			model: 120,
			serialNumber: 80,
			department: 120,
			custodianNote: 160,
			warrantyProvider: 120,
			notes: 2e3
		};
	}));
	//#endregion
	//#region src/pages/FaultListPage.jsx
	var FaultListPage_exports = /* @__PURE__ */ __exportAll({ FaultListPage: () => FaultListPage$1 });
	/** Every fault report the user is allowed to see, with the filters a supervisor asks for. */
	function FaultListPage$1({ mine = false }) {
		const [params, setParams] = useSearchParams();
		const { can } = useAuth();
		const navigate = useNavigate();
		const [tab, setTab] = (0, import_react$11.useState)(mine ? "mine" : params.get("scope") ?? (can("fault.view.any") ? "open" : "mine"));
		const filters = (0, import_react$11.useMemo)(() => ({
			q: params.get("q") ?? "",
			status: params.get("status") ?? "",
			severity: params.get("severity") ?? "",
			categoryId: params.get("categoryId") ?? "",
			technicianId: params.get("technicianId") ?? "",
			equipmentId: params.get("equipmentId") ?? "",
			overdue: params.get("overdue") ?? "",
			scope: tab || void 0,
			from: params.get("from") ?? "",
			to: params.get("to") ?? "",
			sort: params.get("sort") ?? "createdAt",
			dir: params.get("dir") ?? "desc",
			page: params.get("page") ?? 1,
			perPage: 25
		}), [params, tab]);
		const { data, loading, error, refresh } = useApi(() => faults.list(filters), [(0, import_react$11.useMemo)(() => {
			const sp = new URLSearchParams();
			for (const [k, v] of Object.entries(filters)) if (v !== "" && v !== void 0) sp.set(k, String(v));
			return sp.toString();
		}, [filters])]);
		const vocab = useApi(() => faults.vocabulary(), []);
		const picklists = useApi(() => reference.picklists(), []);
		const set = (patch) => {
			const next = new URLSearchParams(params);
			for (const [k, v] of Object.entries(patch)) if (!v) next.delete(k);
			else next.set(k, String(v));
			if (!("page" in patch)) next.delete("page");
			setParams(next, { replace: true });
		};
		const rows = data?.rows ?? [];
		const active = [
			"q",
			"status",
			"severity",
			"categoryId",
			"technicianId",
			"overdue",
			"from",
			"to"
		].some((k) => params.get(k));
		const scopes = can("fault.view.any") ? SCOPES : SCOPES.filter((s) => ["open", "mine"].includes(s.id));
		return /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)(import_jsx_runtime$12.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(PageHeader, {
				eyebrow: "Fault workflow",
				title: mine ? "My reports" : "Fault reports",
				description: mine ? "Everything you have submitted, with where each one stands." : "Reported problems, who owns them, and how long they have been waiting.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(Button, {
					as: Link,
					to: "/faults/new",
					tone: "danger",
					children: "＋ Report a fault"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("div", {
				className: "tabs",
				role: "tablist",
				"aria-label": "Report scope",
				style: { marginBottom: "var(--sp-4)" },
				children: scopes.map((s) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("button", {
					type: "button",
					role: "tab",
					"aria-selected": tab === s.id,
					className: `tabs__item${tab === s.id ? " tabs__item--active" : ""}`,
					onClick: () => {
						setTab(s.id);
						set({ scope: s.id });
					},
					children: s.label
				}, s.id || "all"))
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(SearchInput, {
				value: filters.q,
				onSearch: (q) => set({ q }),
				placeholder: "Search reference, title, description, asset tag…"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("div", {
				style: { marginTop: "var(--sp-4)" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)(FilterBar, {
					active,
					onClear: () => {
						setParams(new URLSearchParams(), { replace: true });
					},
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("select", {
							className: "input",
							value: filters.status,
							onChange: (e) => set({ status: e.target.value }),
							"aria-label": "Workflow status",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "",
								children: "Any status"
							}), (vocab.data?.statuses ?? []).map((s) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: s.value,
								children: s.label
							}, s.value))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("select", {
							className: "input",
							value: filters.severity,
							onChange: (e) => set({ severity: e.target.value }),
							"aria-label": "Severity",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "",
								children: "Any severity"
							}), (vocab.data?.severities ?? []).map((s) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: s.value,
								children: s.label
							}, s.value))]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("select", {
							className: "input",
							value: filters.categoryId,
							onChange: (e) => set({ categoryId: e.target.value }),
							"aria-label": "Fault category",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "",
								children: "Any category"
							}), (vocab.data?.categories ?? []).map((c) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: c.id,
								children: c.name
							}, c.id))]
						}),
						can("fault.view.any") ? /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("select", {
							className: "input",
							value: filters.technicianId,
							onChange: (e) => set({ technicianId: e.target.value }),
							"aria-label": "Technician",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "",
								children: "Any technician"
							}), (picklists.data?.technicians ?? []).map((t) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: t.id,
								children: t.fullName
							}, t.id))]
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("select", {
							className: "input",
							value: filters.overdue,
							onChange: (e) => set({ overdue: e.target.checked ? "1" : "" }),
							"aria-label": "Past response target",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "",
								children: "Any response time"
							}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("option", {
								value: "1",
								children: "Only past the target"
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("label", {
							className: "field",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
								className: "field__label",
								children: "From"
							}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("input", {
								type: "date",
								className: "input",
								value: filters.from,
								onChange: (e) => set({ from: e.target.value })
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("label", {
							className: "field",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
								className: "field__label",
								children: "To"
							}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("input", {
								type: "date",
								className: "input",
								value: filters.to,
								onChange: (e) => set({ to: e.target.value })
							})]
						})
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)(Card, {
					pad: false,
					children: [error ? /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("div", {
						style: { padding: "var(--sp-4)" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(ErrorState, {
							error,
							onRetry: refresh
						})
					}) : /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(DataTable, {
						loading: loading && !data,
						rows,
						onRowClick: (r) => navigate(`/faults/${r.id}`),
						caption: "Fault reports",
						columns: [
							{
								key: "reference",
								label: "Report",
								mono: true,
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("div", {
									className: "cell-main",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
										className: "cell-title",
										children: /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("code", { children: r.reference })
									}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("span", {
										className: "cell-sub",
										children: [formatDateTime(r.createdAt), r.assignedAt ? ` · owned ${pluralise(Math.round((Date.parse(r.assignedAt) - Date.parse(r.createdAt)) / 36e5), "hour")} later` : ""]
									})]
								})
							},
							{
								key: "title",
								label: "Fault",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("div", {
									className: "cell-main",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
										className: "cell-title",
										children: r.title
									}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("span", {
										className: "cell-sub",
										children: [
											r.equipmentName,
											" · ",
											r.assetTag,
											r.categoryName ? ` · ${r.categoryName}` : ""
										]
									})]
								})
							},
							{
								key: "severity",
								label: "Severity",
								align: "center",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(SeverityPill, { severity: r.severity })
							},
							{
								key: "status",
								label: "Status",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(FaultStatusPill, { status: r.status })
							},
							{
								key: "assignedToName",
								label: "With",
								render: (r) => r.assignedToName ? r.assignedToName : /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(Badge, {
									tone: "warn",
									size: "sm",
									children: "unassigned"
								})
							},
							{
								key: "sla",
								label: "Response",
								align: "right",
								render: (r) => r.isOverdue ? /* @__PURE__ */ (0, import_jsx_runtime$12.jsxs)("span", {
									className: "sla sla--late",
									title: `Target ${formatDateTime(r.dueAt)}`,
									children: [
										"⏱ ",
										Math.ceil((r.minutesOverdue ?? 0) / 60),
										" h late"
									]
								}) : r.minutesOverdue === 0 && r.dueAt ? /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
									className: "muted",
									children: "on target"
								}) : /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)("span", {
									className: "muted",
									children: "—"
								})
							}
						],
						empty: /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(EmptyState, {
							icon: "⚑",
							title: tab === "unassigned" ? "Nothing is waiting for an owner" : "No fault reports here",
							description: tab === "unassigned" ? "Every open report has someone responsible for it. Good." : "Either nothing is broken, or it is not being reported. The second one is the usual case.",
							action: /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(Button, {
								as: Link,
								to: "/faults/new",
								tone: "secondary",
								children: "Report a fault"
							})
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime$12.jsx)(Pagination, {
						pagination: data?.pagination,
						onPage: (page) => set({ page })
					})]
				})]
			})
		] });
	}
	var import_react$11, import_jsx_runtime$12, SCOPES;
	var init_FaultListPage = __esmMin((() => {
		import_react$11 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_useApi();
		init_format();
		import_jsx_runtime$12 = require_jsx_runtime();
		SCOPES = [
			{
				id: "open",
				label: "Open"
			},
			{
				id: "assigned",
				label: "Assigned to me"
			},
			{
				id: "unassigned",
				label: "Waiting for an owner"
			},
			{
				id: "mine",
				label: "My reports"
			},
			{
				id: "",
				label: "All"
			}
		];
	}));
	//#endregion
	//#region src/pages/FaultDetailPage.jsx
	var FaultDetailPage_exports = /* @__PURE__ */ __exportAll({ FaultDetailPage: () => FaultDetailPage$1 });
	/**
	* One fault report: what was said, who owns it, what was found, what it cost, and what the
	* reader is allowed to do about it.  The action panel is generated from `fault.nextStatuses`
	* — the server's own answer to "what is legal from here for this person" — so the UI cannot
	* offer a move the API would refuse.
	*/
	function FaultDetailPage$1() {
		const { id } = useParams();
		useNavigate();
		const { user, can, is } = useAuth();
		const toast = useToast();
		const { data, loading, error, refresh } = useApi(() => faults.get(id), [id]);
		const [repairOpen, setRepairOpen] = (0, import_react$10.useState)(false);
		const [assignOpen, setAssignOpen] = (0, import_react$10.useState)(false);
		const [noteOpen, setNoteOpen] = (0, import_react$10.useState)(false);
		const [evidenceOpen, setEvidenceOpen] = (0, import_react$10.useState)(false);
		const [amendOpen, setAmendOpen] = (0, import_react$10.useState)(false);
		const [confirm, setConfirm] = (0, import_react$10.useState)(null);
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		if (error) return /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(ErrorState, {
			error,
			onRetry: refresh
		});
		if (loading && !data) return /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Loading, {
			rows: 9,
			label: "Loading the fault report"
		});
		const f = data.fault;
		const eq = data.equipment;
		const repair = data.repair;
		const readiness = data.readiness;
		const canWork = is("technician") && (f.assignedTo === user.id || !f.assignedTo);
		const doTransition = async (target, comment) => {
			setBusy(true);
			try {
				await faults.transition(f.id, target, comment);
				toast.success(`Report moved to ${FAULT_STATUS[target]?.label ?? target}.`);
				await refresh();
			} catch (err) {
				toast.error(errorText(err), { detail: err.details?.missing ? `Still needed: ${err.details.missing.join(", ")}` : null });
			} finally {
				setBusy(false);
				setConfirm(null);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(PageHeader, {
				back: {
					to: is("reporter") ? "/my-reports" : can("fault.view.any") ? "/faults" : "/work",
					label: "Back to reports"
				},
				eyebrow: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("span", { children: ["Report ", /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("code", { children: f.reference })] }),
				title: f.title,
				actions: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [f.isOverdue ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
					tone: "bad",
					size: "lg",
					children: "Past the response target"
				}) : null, /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
					as: Link,
					to: `/equipment/${eq.id}`,
					tone: "secondary",
					children: "Equipment record"
				})] })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
				className: "row",
				style: {
					gap: 8,
					marginBottom: "var(--sp-4)",
					flexWrap: "wrap"
				},
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(SeverityPill, {
						severity: f.severity,
						size: "lg"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(FaultStatusPill, {
						status: f.status,
						size: "lg"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(EquipmentStatusPill, {
						status: eq.status,
						size: "lg"
					}),
					f.diagnosisConfirmed ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: "ok",
						children: "Diagnosis recorded by a technician"
					}) : null,
					f.repairReference ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Badge, {
						tone: "info",
						children: ["Repair record ", f.repairReference]
					}) : null,
					f.attachmentCount ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Badge, {
						tone: "neutral",
						children: ["📎 ", pluralise(f.attachmentCount, "file")]
					}) : null
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
				className: "grid grid--split",
				style: { alignItems: "start" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(WorkflowStrip, {
							current: f.status,
							steps: f.workflow
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Card, {
							title: "Reported problem",
							subtitle: `${formatDateTime(f.createdAt)} · by ${f.reportedByName}${f.onBehalfOf ? ` on behalf of ${f.onBehalfOf}` : ""}`,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
									style: {
										whiteSpace: "pre-line",
										overflowWrap: "anywhere"
									},
									children: f.description
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(KeyValue, {
									className: "detail-specs",
									columns: 3,
									items: [
										["Observed", formatDateTime(f.observedAt)],
										["Location", f.locationName ?? "As recorded on the equipment"],
										["Fault category", f.categoryName ?? "—"],
										["Response target", f.dueAt ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("span", { children: [
											formatDateTime(f.dueAt),
											" ",
											f.isOverdue ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Badge, {
												tone: "bad",
												size: "sm",
												children: [formatDuration(f.minutesOverdue), " late"]
											}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
												tone: "ok",
												size: "sm",
												children: "within target"
											})
										] }) : "—"],
										["Acknowledged", f.acknowledgedAt ? formatDateTime(f.acknowledgedAt) : null],
										["Repaired", f.repairedAt ? formatDateTime(f.repairedAt) : null],
										["Verified by", f.verifiedByName ? `${f.verifiedByName}${f.verifiedAt ? ` · ${formatDateTime(f.verifiedAt)}` : ""}` : null],
										["Closed", f.closedAt ? formatDateTime(f.closedAt) : null],
										["Time to repair", f.timings?.minutesToRepair ? formatDuration(f.timings.minutesToRepair) : null]
									]
								}),
								f.resolutionNote ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
									tone: "ok",
									title: "Resolution recorded",
									children: f.resolutionNote
								}) : null,
								f.status === "repaired" && is("reporter") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
									tone: "info",
									title: "This is waiting for you",
									children: "A technician says it is fixed. Try it and verify (or say what is still wrong) so the report can be closed."
								}) : null
							]
						}),
						repair ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(RepairRecordCard, {
							repair,
							canEdit: canWork && !["closed"].includes(f.status),
							onEdit: () => setRepairOpen(true),
							faultId: f.id,
							onRefresh: refresh
						}) : canWork ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Card, {
							title: "Repair record",
							subtitle: "Nothing recorded yet",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
								className: "form-note",
								children: "Write what you found and what you did. The equipment is only returned to service after the record contains a diagnosis, the work performed, the tests that prove it works and your safety confirmation."
							}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
								tone: "primary",
								onClick: () => setRepairOpen(true),
								children: "Record the repair"
							})]
						}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Card, {
							title: "Repair record",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
								className: "form-note",
								children: ["No repair record yet. ", is("reporter") ? "Technicians will add this when they work on it." : "Assign a technician who can diagnose it."]
							})
						}),
						data.attachments.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Card, {
							title: "Photographs and documents",
							subtitle: "Submitted with the report and during the repair",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("ul", {
								className: "attach-grid",
								children: data.attachments.map((a) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("li", {
									className: "attach",
									children: [
										a.mimeType.startsWith("image/") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("a", {
											href: attachments.viewUrl(a.id),
											target: "_blank",
											rel: "noreferrer",
											children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("img", {
												className: "attach__img",
												src: attachments.viewUrl(a.id),
												alt: a.caption ?? a.filename
											})
										}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("a", {
											className: "attach__doc",
											href: attachments.viewUrl(a.id),
											target: "_blank",
											rel: "noreferrer",
											children: ["📄 ", a.mimeType.includes("pdf") ? "PDF" : "file"]
										}),
										/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
											className: "attach__meta",
											children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
												className: "attach__name",
												title: a.filename,
												children: a.filename
											}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("span", { children: [
												a.kind.replace(/_/g, " "),
												" · ",
												formatDate(a.createdAt),
												" · ",
												a.uploadedByName
											] })]
										}),
										can("attachment.view.any") && f.isOpen ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("button", {
											type: "button",
											className: "attach__remove",
											onClick: async () => {
												await attachments.remove(a.id);
												toast.success("Attachment removed.");
												refresh();
											},
											children: "Remove"
										}) : null
									]
								}, a.id))
							})
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Card, {
							title: "Timeline",
							subtitle: "Every status change and note, with who made it",
							actions: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
								size: "sm",
								tone: "ghost",
								onClick: () => setNoteOpen(true),
								children: "Add a note"
							}),
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Timeline, { items: data.timeline.map((h) => ({
								id: h.id,
								title: h.isNote ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: ["Note ", h.fromStatus ? `(${FAULT_STATUS[h.fromStatus]?.label ?? h.fromStatus})` : ""] }) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [h.fromStatus ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [FAULT_STATUS[h.fromStatus]?.label ?? h.fromStatus, " → "] }) : "Reported ", /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(FaultStatusPill, {
									status: h.toStatus,
									size: "sm"
								})] }),
								detail: h.comment,
								actor: h.changedByName ?? "system",
								at: h.changedAt,
								tone: h.toStatus === "repaired" || h.toStatus === "verified" ? "ok" : h.toStatus === "closed" ? "neutral" : h.isNote ? "info" : null
							})) })
						})
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Card, {
							title: "Take the next step",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
								className: "stack",
								style: { gap: "var(--sp-3)" },
								children: [
									!f.assignedTo && can("fault.assign") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
										tone: "primary",
										className: "btn--block",
										onClick: () => setAssignOpen(true),
										children: "Assign a technician…"
									}) : null,
									f.assignedTo && can("fault.assign") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Button, {
										tone: "secondary",
										className: "btn--block",
										onClick: () => setAssignOpen(true),
										children: [
											"Reassign (currently ",
											f.assignedToName,
											")"
										]
									}) : null,
									f.nextStatuses?.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
										className: "stack",
										style: { gap: 8 },
										children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
											className: "field__label",
											children: "Move this report to"
										}), f.nextStatuses.map((s) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
											className: "btn--block",
											tone: s.value === "closed" ? "danger" : s.value === "repaired" || s.value === "verified" ? "primary" : "secondary",
											disabled: s.value === "repaired" && readiness && !readiness.canMarkRepaired,
											onClick: () => s.value === "repaired" || s.value === "closed" || s.value === "verified" ? setConfirm({ target: s.value }) : setConfirm({ target: s.value }),
											children: s.label
										}, s.value))]
									}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
										className: "form-note",
										children: [
											"No further moves are available to you from ",
											/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("b", { children: FAULT_STATUS[f.status]?.label }),
											f.isOpen ? ", but the report is still open." : "."
										]
									}),
									f.nextStatuses?.some((s) => s.value === "repaired") === false && canWork && !readiness?.canMarkRepaired && f.isOpen ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
										className: "form-note",
										children: [
											"“Repaired” unlocks once the repair record has ",
											readiness?.missing?.filter((m) => m !== "repairRecord").map((m) => MISSING_LABEL[m] ?? m).join(", ").toLowerCase(),
											"."
										]
									}) : null,
									canWork ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
										tone: "secondary",
										className: "btn--block",
										onClick: () => setRepairOpen(true),
										children: repair ? "Update repair record" : "Record the repair"
									}) : null,
									canWork || is("admin") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
										tone: "ghost",
										className: "btn--block",
										onClick: () => setEvidenceOpen(true),
										children: "Attach evidence"
									}) : null,
									f.status === "reported" && (f.reportedBy === user.id || is("admin")) ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
										tone: "ghost",
										className: "btn--block",
										onClick: () => setAmendOpen(true),
										children: "Amend my report"
									}) : null,
									f.canReopen ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
										tone: "danger",
										className: "btn--block",
										onClick: () => setConfirm({ target: "reopen" }),
										children: "Reopen this fault…"
									}) : null
								]
							}), f.reportedBy === user.id && f.status !== "reported" ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
								className: "form-note",
								style: { marginTop: 10 },
								children: "Once work starts, a report cannot be edited by its author — add a note instead so the original wording stays on file."
							}) : null]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Card, {
							title: "Equipment",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
								className: "stack",
								style: { gap: 10 },
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Link, {
										to: `/equipment/${eq.id}`,
										className: "cell-title",
										children: eq.name
									}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
										className: "cell-sub",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("code", { children: eq.assetTag }), eq.locationName ? ` · ${eq.locationName}` : ""]
									})] }),
									/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
										className: "row row--tight",
										style: { flexWrap: "wrap" },
										children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(EquipmentStatusPill, { status: eq.status }), eq.nextMaintenanceOn ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(MaintenanceLight, {
											state: eq.maintenanceState,
											daysUntil: eq.daysUntilPm
										}) : null]
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(KeyValue, {
										columns: 1,
										items: [
											["Serial", /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("code", { children: eq.serialNumber })],
											["Responsible technician", f.assignedToName ?? eq.responsibleTechnicianName],
											["Reported from", eq.locationLabel ?? eq.locationName]
										]
									})
								]
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Callout, {
							tone: "safety",
							title: "Accountability",
							children: [
								"Reported by ",
								f.reportedByName,
								" · handled by ",
								f.assignedToName ?? "nobody yet",
								f.verifiedByName ? ` · verified by ${f.verifiedByName}` : "",
								". Every change on this page is recorded with a name and a timestamp, and a diagnosis can only ever be written by a technician."
							]
						})
					]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TransitionConfirm, {
				state: confirm,
				fault: f,
				readiness,
				busy,
				onClose: () => setConfirm(null),
				onConfirm: (comment) => doTransition(confirm.target === "reopen" ? null : confirm.target, comment),
				onReopen: async (comment) => {
					setBusy(true);
					try {
						await faults.reopen(f.id, comment, "Reopened from the fault record");
						toast.success("Fault reopened.");
						await refresh();
					} catch (err) {
						toast.error(errorText(err));
					} finally {
						setBusy(false);
						setConfirm(null);
					}
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(AssignDialog, {
				open: assignOpen,
				onClose: () => setAssignOpen(false),
				fault: f,
				onDone: async () => {
					setAssignOpen(false);
					await refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(NoteDialog, {
				open: noteOpen,
				onClose: () => setNoteOpen(false),
				fault: f,
				onDone: async () => {
					setNoteOpen(false);
					await refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(EvidenceDialog, {
				open: evidenceOpen,
				onClose: () => setEvidenceOpen(false),
				fault: f,
				repair,
				onDone: async () => {
					setEvidenceOpen(false);
					await refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(AmendDialog, {
				open: amendOpen,
				onClose: () => setAmendOpen(false),
				fault: f,
				vocab: data.vocab,
				onDone: async () => {
					setAmendOpen(false);
					await refresh();
				},
				categories: void 0
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(RepairRecordModal, {
				open: repairOpen,
				onClose: () => setRepairOpen(false),
				fault: f,
				record: repair,
				onDone: async () => {
					setRepairOpen(false);
					await refresh();
				}
			})
		] });
	}
	function WorkflowStrip({ current, steps }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("div", {
			className: "workflow",
			role: "list",
			"aria-label": `Workflow position: ${FAULT_STATUS[current]?.label}`,
			children: (steps ?? []).map((s, i) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
				style: { display: "contents" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: cx("workflow__step", s.reached && "workflow__step--done", s.current && "workflow__step--current"),
					role: "listitem",
					"aria-current": s.current ? "step" : void 0,
					children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
						className: "workflow__dot",
						"aria-hidden": "true",
						children: s.current ? "●" : s.reached ? "✓" : s.step ?? i + 1
					}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
						className: "workflow__label",
						children: s.label
					})]
				}), i < steps.length - 1 ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
					className: "workflow__line",
					"aria-hidden": "true"
				}) : null]
			}, s.value))
		});
	}
	function RepairRecordCard({ repair, canEdit, onEdit, faultId, onRefresh }) {
		const toast = useToast();
		const removePhoto = async (attachmentId) => {
			try {
				await attachments.remove(attachmentId);
				toast.success("Removed.");
				onRefresh();
			} catch (err) {
				toast.error(errorText(err));
			}
		};
		const evidence = (repair.attachments ?? []).filter((a) => a.kind !== "document");
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Card, {
			title: "Repair record",
			subtitle: `${repair.reference} · ${repair.technicianName}${repair.technicianTitle ? `, ${repair.technicianTitle}` : ""} · ${formatDate(repair.dateRepaired)}`,
			actions: canEdit ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				size: "sm",
				tone: "secondary",
				onClick: onEdit,
				children: "Edit record"
			}) : null,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(KeyValue, {
					columns: 1,
					items: [
						["Diagnosis", repair.diagnosis],
						["Root cause", repair.rootCause],
						["Troubleshooting", repair.troubleshooting],
						["Repair performed", repair.repairActions],
						["Test results", repair.testResults],
						["Calibration", repair.calibrationPerformed ? repair.calibrationDetails ?? "Performed" : "Not performed"],
						["Parts replaced", repair.partsReplacedSummary]
					]
				}),
				repair.parts?.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("table", {
					className: "table table--inner",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("tr", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("th", { children: "Part" }),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("th", { children: "Number" }),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("th", {
								className: "is-center",
								children: "Qty"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("th", {
								className: "is-right",
								children: "Unit"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("th", {
								className: "is-right",
								children: "Line"
							})
						] }) }),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("tbody", { children: repair.parts.map((p) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("tr", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("td", { children: [p.partName, p.recovered ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
								size: "sm",
								tone: "neutral",
								children: " old part kept"
							}) : null] }),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("td", { children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("code", { children: p.partNumber ?? "—" }) }),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("td", {
								className: "is-center",
								children: p.quantity
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("td", {
								className: "is-right",
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Money, {
									amount: p.unitCost,
									currency: repair.currency
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("td", {
								className: "is-right",
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Money, {
									amount: p.lineCost,
									currency: repair.currency
								})
							})
						] }, p.id)) }),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("tfoot", { children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("tr", { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("td", {
							colSpan: 4,
							children: [
								"Parts ",
								formatMoney(repair.partsCost, repair.currency),
								" · Labour ",
								formatMoney(repair.labourCost, repair.currency),
								Number(repair.otherCost) ? ` · Other ${formatMoney(repair.otherCost, repair.currency)}` : ""
							]
						}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("td", {
							className: "is-right",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("b", { children: formatMoney(repair.totalCost, repair.currency) })
						})] }) })
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "row",
					style: { marginTop: 10 },
					children: [repair.safetyCheckConfirmed ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: "ok",
						children: "✓ Electrical safety check confirmed"
					}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: "bad",
						children: "Safety check not yet confirmed"
					}), repair.safeToReturnToService ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: "ok",
						children: "Technician: safe to return to service"
					}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: "bad",
						children: "Technician: NOT safe to return to service"
					})]
				}),
				!repair.safeToReturnToService ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
					tone: "bad",
					title: "This unit is not cleared for use",
					children: "The technician who worked on it has recorded that it must not go back into service. Keep it out of use until a further repair is recorded — this system cannot override that judgement."
				}) : null,
				evidence.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("ul", {
					className: "attach-grid",
					style: { marginTop: 10 },
					children: evidence.map((a) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("li", {
						className: "attach",
						children: [
							a.mimeType.startsWith("image/") ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("img", {
								className: "attach__img",
								src: attachments.viewUrl(a.id),
								alt: a.caption ?? a.filename
							}) : /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("a", {
								className: "attach__doc",
								href: attachments.viewUrl(a.id),
								target: "_blank",
								rel: "noreferrer",
								children: ["📄 ", a.filename]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
								className: "attach__meta",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
									className: "attach__name",
									children: a.kind.replace(/_/g, " ")
								}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", { children: a.caption ?? a.filename })]
							}),
							canEdit ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("button", {
								type: "button",
								className: "attach__remove",
								onClick: () => removePhoto(a.id),
								children: "Remove"
							}) : null
						]
					}, a.id))
				}) : null
			]
		});
	}
	function TransitionConfirm({ state, fault, readiness, busy, onClose, onConfirm, onReopen }) {
		const [comment, setComment] = (0, import_react$10.useState)("");
		(0, import_react$10.useEffect)(() => {
			if (state) setComment("");
		}, [state]);
		if (!state) return null;
		const target = state.target;
		const needsComment = target === "reopen" || [
			"closed",
			"awaiting_parts",
			"under_repair"
		].includes(target);
		target === "reopen" || FAULT_STATUS[target]?.label?.toLowerCase();
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Modal, {
			open: true,
			onClose,
			title: target === "reopen" ? "Reopen this fault report?" : `Move to ${FAULT_STATUS[target]?.label}?`,
			description: target === "reopen" ? "Reopening is an administrator action: it changes a finished record, so the reason is written into the timeline." : "This is recorded in the audit trail with your name and the time.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: target === "closed" || target === "reopen" ? "danger" : "primary",
				loading: busy,
				disabled: needsComment && comment.trim().length < 3,
				onClick: () => target === "reopen" ? onReopen(comment.trim()) : onConfirm(comment.trim() || void 0),
				children: target === "reopen" ? "Reopen the fault" : `Move to ${FAULT_STATUS[target]?.label}`
			})] }),
			children: [
				target === "repaired" && readiness && !readiness.canMarkRepaired ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Callout, {
					tone: "warn",
					title: "Not yet",
					children: [
						"The repair record is missing: ",
						readiness.missing.filter((m) => m !== "repairRecord").map((m) => MISSING_LABEL[m] ?? m).join(", ") || "the record itself",
						"."
					]
				}) : null,
				target === "repaired" ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Callout, {
					tone: "info",
					title: "Returning this item to service",
					children: [
						"Marking it repaired sets the equipment back to ",
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("b", { children: "Operational" }),
						". By confirming this you are stating the equipment has been tested and is fit for the department to use."
					]
				}) : null,
				target === "verified" ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
					className: "form-note",
					children: [
						"Verification says someone has checked the fix in real use",
						fault.reportedBy ? ` — the reporter (${fault.reportedByName}) is usually the right person, and a technician's own verification is also recorded as such` : "",
						"."
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: needsComment ? "Comment (required)" : "Comment (optional)",
					hint: target === "closed" ? "What was the outcome? This becomes the resolution note on the record." : "Anything the next person should know.",
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
						rows: 3,
						value: comment,
						onChange: (e) => setComment(e.target.value),
						autoFocus: true,
						placeholder: target === "closed" ? "Verified in use with the lab supervisor; documented in the history." : target === "awaiting_parts" ? "Part ordered from the supplier; expected in 5 working days." : "Optional note"
					})
				})
			]
		});
	}
	function AssignDialog({ open, onClose, fault, onDone }) {
		const toast = useToast();
		const [techId, setTechId] = (0, import_react$10.useState)(fault.assignedTo ?? "");
		const [note, setNote] = (0, import_react$10.useState)("");
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		const [error, setError] = (0, import_react$10.useState)(null);
		const candidates = (useApi(() => users.list({ status: "active" }), [open]).data?.items ?? []).filter((u) => ["technician", "admin"].includes(u.roleCode));
		const submit = async () => {
			setBusy(true);
			setError(null);
			try {
				await faults.assign(fault.id, Number(techId), note || void 0);
				toast.success("Assigned.");
				onDone();
			} catch (err) {
				setError(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Modal, {
			open,
			onClose: () => !busy && onClose(),
			title: "Assign a technician",
			description: "Assignment is who is responsible for the next step, not blame for the fault.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "primary",
				disabled: !techId,
				loading: busy,
				onClick: submit,
				children: "Assign"
			})] }),
			children: [
				error ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
					tone: "bad",
					children: error
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "Technician",
					required: true,
					hint: "Only technicians and administrators can be assigned repair work.",
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Select, {
						value: techId,
						onChange: (e) => setTechId(e.target.value),
						placeholder: "Choose a technician…",
						options: candidates.map((u) => ({
							value: u.id,
							label: `${u.fullName} — ${u.jobTitle ?? u.roleLabel}`
						}))
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "Note to the technician",
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
						rows: 2,
						value: note,
						onChange: (e) => setNote(e.target.value),
						placeholder: "e.g. Available this afternoon; the spare probe is in the store."
					})
				})
			]
		});
	}
	function NoteDialog({ open, onClose, fault, onDone }) {
		const toast = useToast();
		const [comment, setComment] = (0, import_react$10.useState)("");
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		const [error, setError] = (0, import_react$10.useState)(null);
		const submit = async () => {
			setBusy(true);
			setError(null);
			try {
				await faults.note(fault.id, comment.trim());
				toast.success("Note added to the timeline.");
				setComment("");
				onDone();
			} catch (err) {
				setError(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Modal, {
			open,
			onClose: () => !busy && onClose(),
			title: "Add to the timeline",
			description: "A note never changes the status — it adds context that the next person will need.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "primary",
				disabled: comment.trim().length < 3,
				loading: busy,
				onClick: submit,
				children: "Add note"
			})] }),
			children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
				label: "Note",
				error,
				children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
					rows: 4,
					value: comment,
					onChange: (e) => setComment(e.target.value),
					autoFocus: true,
					placeholder: "It also happens on a different mains socket, so it is not the room wiring."
				})
			})
		});
	}
	function EvidenceDialog({ open, onClose, fault, repair, onDone }) {
		const toast = useToast();
		const [files, setFiles] = (0, import_react$10.useState)([]);
		const [kind, setKind] = (0, import_react$10.useState)(repair ? "after_photo" : "photo");
		const [caption, setCaption] = (0, import_react$10.useState)("");
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		const [error, setError] = (0, import_react$10.useState)(null);
		const submit = async () => {
			setBusy(true);
			setError(null);
			try {
				if (repair) await faults.uploadRepairPhotos(fault.id, files, kind, caption || void 0);
				else await faults.attach(fault.id, files, kind === "after_photo" || kind === "before_photo" ? "photo" : "document", caption || void 0);
				toast.success(pluralise(files.length, "file") + " attached.");
				setFiles([]);
				onDone();
			} catch (err) {
				setError(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Modal, {
			open,
			onClose: () => !busy && onClose(),
			title: "Attach evidence",
			description: "Photos of the fault, the board before and after, or a signed test sheet.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Button, {
				tone: "primary",
				disabled: !files.length,
				loading: busy,
				onClick: submit,
				children: ["Attach ", files.length ? pluralise(files.length, "file") : ""]
			})] }),
			children: [
				error ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
					tone: "bad",
					children: error
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "What is it?",
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Select, {
						value: kind,
						onChange: (e) => setKind(e.target.value),
						options: [
							{
								value: "photo",
								label: "Photo of the fault"
							},
							{
								value: "before_photo",
								label: "Before the repair"
							},
							{
								value: "after_photo",
								label: "After the repair"
							},
							{
								value: "document",
								label: "Document (test sheet, PDF)"
							}
						]
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(FileInput, {
					label: "Files",
					kind: kind === "document" ? "document" : "photo",
					accept: kind === "document" ? "image/*,application/pdf" : "image/*",
					value: files,
					onChange: setFiles
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "Caption",
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
						value: caption,
						onChange: (e) => setCaption(e.target.value),
						placeholder: "e.g. Corrosion at the mains inlet, before cleaning"
					})
				})
			]
		});
	}
	function AmendDialog({ open, onClose, fault, onDone }) {
		const toast = useToast();
		const [form, setForm] = (0, import_react$10.useState)({
			title: fault.title,
			description: fault.description,
			severity: fault.severity
		});
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		const [errors, setErrors] = (0, import_react$10.useState)({});
		(0, import_react$10.useEffect)(() => {
			if (open) setForm({
				title: fault.title,
				description: fault.description,
				severity: fault.severity
			});
		}, [open, fault.id]);
		const submit = async () => {
			setBusy(true);
			setErrors({});
			try {
				await faults.update(fault.id, form);
				toast.success("Report updated.");
				onDone();
			} catch (err) {
				setErrors(err.fieldErrors ?? {});
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Modal, {
			open,
			onClose: () => !busy && onClose(),
			title: "Amend your report",
			description: "Allowed only while nobody has started work on it. The original wording stays in the timeline.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: "Save changes"
			})] }),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "Short summary",
					error: errors.title?.join?.(" "),
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
						value: form.title,
						onChange: (e) => setForm({
							...form,
							title: e.target.value
						})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "What you observed",
					error: errors.description?.join?.(" "),
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
						rows: 5,
						value: form.description,
						onChange: (e) => setForm({
							...form,
							description: e.target.value
						})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
					label: "Severity",
					error: errors.severity?.join?.(" "),
					children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(ChoiceChips, {
						name: "severity",
						value: form.severity,
						onChange: (v) => setForm({
							...form,
							severity: v
						}),
						options: Object.entries(SEVERITY).map(([value, m]) => ({
							value,
							label: m.label,
							tone: m.tone
						}))
					})
				})
			]
		});
	}
	function RepairRecordModal({ open, onClose, fault, record, onDone }) {
		const toast = useToast();
		const catalogue = useApi(() => parts.list({}), [open]);
		const [form, setForm] = (0, import_react$10.useState)(EMPTY_REPAIR);
		const [errors, setErrors] = (0, import_react$10.useState)({});
		const [busy, setBusy] = (0, import_react$10.useState)(false);
		const [showParts, setShowParts] = (0, import_react$10.useState)(false);
		(0, import_react$10.useEffect)(() => {
			if (!open) return;
			setForm(record ? {
				diagnosis: record.diagnosis ?? "",
				rootCause: record.rootCause ?? "",
				troubleshooting: record.troubleshooting ?? "",
				repairActions: record.repairActions ?? "",
				testResults: record.testResults ?? "",
				calibrationPerformed: !!record.calibrationPerformed,
				calibrationDetails: record.calibrationDetails ?? "",
				labourCost: record.labourCost ?? "",
				otherCost: record.otherCost ?? "",
				dateRepaired: toDateInput(record.dateRepaired),
				notes: record.notes ?? "",
				safetyCheckConfirmed: !!record.safetyCheckConfirmed,
				safeToReturnToService: record.safeToReturnToService !== false,
				parts: (record.parts ?? []).map((p) => ({
					partId: p.partId,
					partName: p.partName,
					partNumber: p.partNumber,
					quantity: p.quantity,
					unitCost: p.unitCost,
					recovered: !!p.recovered
				}))
			} : EMPTY_REPAIR);
			setErrors({});
			setShowParts(Boolean(record?.parts?.length));
		}, [open, record]);
		const set = (patch) => {
			setForm((f) => ({
				...f,
				...patch
			}));
			setErrors((e) => {
				const n = { ...e };
				for (const k of Object.keys(patch)) delete n[k];
				return n;
			});
		};
		const partsCost = (0, import_react$10.useMemo)(() => form.parts.reduce((n, p) => n + (Number(p.quantity) || 0) * (Number(p.unitCost) || 0), 0), [form.parts]);
		const total = Math.round((partsCost + (Number(form.labourCost) || 0) + (Number(form.otherCost) || 0)) * 100) / 100;
		const save = async ({ signOff = false } = {}) => {
			setBusy(true);
			setErrors({});
			const payload = {
				...form,
				parts: form.parts.map((p) => ({
					...p,
					quantity: Number(p.quantity) || 1,
					unitCost: Number(p.unitCost) || 0
				})),
				labourCost: Number(form.labourCost) || 0,
				otherCost: Number(form.otherCost) || 0,
				calibrationPerformed: !!form.calibrationPerformed,
				safetyCheckConfirmed: !!form.safetyCheckConfirmed,
				safeToReturnToService: !!form.safeToReturnToService
			};
			if (signOff) payload.safetyCheckConfirmed = true;
			try {
				const saved = await faults.saveRepair(fault.id, payload);
				toast.success(signOff ? "Repair record saved and signed off." : "Repair record saved as a draft.");
				if (saved?.record) onDone();
				else onDone();
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const complete = form.diagnosis.trim().length >= 5 && form.rootCause.trim().length >= 5 && form.repairActions.trim().length >= 5 && form.testResults.trim().length >= 5 && form.safetyCheckConfirmed;
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Modal, {
			open,
			onClose: () => !busy && onClose(),
			size: "lg",
			title: "Repair record",
			description: `For ${fault.reference} · ${fault.equipmentName}. Everything here becomes part of the permanent equipment history.`,
			footer: /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(import_jsx_runtime$11.Fragment, { children: [
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("span", {
					className: "row",
					style: {
						marginRight: "auto",
						gap: 6
					},
					children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Badge, {
						tone: complete ? "ok" : "warn",
						children: complete ? "Ready to sign off" : "Draft — incomplete"
					}), form.parts.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Badge, {
						tone: "neutral",
						children: [
							"Parts ",
							formatMoney(partsCost),
							" · Total ",
							formatMoney(total)
						]
					}) : null]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
					tone: "ghost",
					onClick: onClose,
					disabled: busy,
					children: "Close"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
					tone: "secondary",
					loading: busy,
					onClick: () => save({ signOff: false }),
					children: "Save draft"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
					tone: "primary",
					loading: busy,
					disabled: !complete,
					onClick: () => save({ signOff: true }),
					children: "Save & sign off"
				})
			] }),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "form-grid form-grid--2",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Fault diagnosis",
							required: true,
							error: errors.diagnosis,
							hint: "What was actually wrong, as you found it.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 3,
								value: form.diagnosis,
								onChange: (e) => set({ diagnosis: e.target.value }),
								placeholder: "e.g. Intermittent break in the patient-lead cable at the strain relief"
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Root cause",
							required: true,
							error: errors.rootCause,
							hint: "Why it happened — this is what stops it happening again.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 3,
								value: form.rootCause,
								onChange: (e) => set({ rootCause: e.target.value }),
								placeholder: "e.g. Cable repeatedly wrapped tightly around the unit during storage"
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Troubleshooting performed",
							error: errors.troubleshooting,
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 3,
								value: form.troubleshooting,
								onChange: (e) => set({ troubleshooting: e.target.value }),
								placeholder: "Tests, measurements and what they ruled out."
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Repair performed",
							required: true,
							error: errors.repairActions,
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 3,
								value: form.repairActions,
								onChange: (e) => set({ repairActions: e.target.value }),
								placeholder: "What you replaced, adjusted, cleaned or re-soldered."
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Test results",
							required: false,
							error: errors.testResults,
							hint: "Required before the equipment can be marked repaired. Include real readings, not “works”.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 3,
								value: form.testResults,
								onChange: (e) => set({ testResults: e.target.value }),
								placeholder: "e.g. Earth bond 0.11 Ω; leakage 22 µA; 12-lead trace stable over a 2 h wiggle test."
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
							className: "stack",
							style: { gap: "var(--sp-3)" },
							children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
								label: "Date repaired",
								error: errors.dateRepaired,
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
									type: "date",
									value: form.dateRepaired,
									onChange: (e) => set({ dateRepaired: e.target.value }),
									max: toDateInput(/* @__PURE__ */ new Date())
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
								label: "Currency",
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
									value: record?.currency ?? "USD",
									onChange: (e) => set({ currency: e.target.value }),
									maxLength: 3
								})
							})]
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
							className: "form-section__title",
							children: "Parts and costs"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(PartsEditor, {
							parts: form.parts,
							onChange: (parts) => set({ parts }),
							catalogue: catalogue.data?.items ?? [],
							open: showParts,
							onToggle: setShowParts,
							errors
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
							className: "form-grid form-grid--2",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
								label: "Labour cost",
								error: errors.labourCost,
								hint: "Internal charge or contracted rate. Leave blank if none.",
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
									type: "number",
									min: "0",
									step: "0.01",
									value: form.labourCost,
									onChange: (e) => set({ labourCost: e.target.value })
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
								label: "Other cost (callout, shipping)",
								error: errors.otherCost,
								children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
									type: "number",
									min: "0",
									step: "0.01",
									value: form.otherCost,
									onChange: (e) => set({ otherCost: e.target.value })
								})
							})]
						}),
						form.parts.length || Number(form.labourCost) || Number(form.otherCost) ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("p", {
							className: "form-note",
							children: [
								"Total recorded cost: ",
								/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("b", { children: formatMoney(total, form.currency) }),
								" (parts ",
								formatMoney(partsCost),
								" + labour ",
								formatMoney(Number(form.labourCost) || 0),
								" + other ",
								formatMoney(Number(form.otherCost) || 0),
								"). The total is calculated by the server from the part lines."
							]
						}) : null
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
							className: "form-section__title",
							children: "Calibration"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Checkbox, {
							label: "Calibration or verification was performed",
							hint: "Leave unchecked if this repair did not touch measurement accuracy.",
							checked: form.calibrationPerformed,
							onChange: (v) => set({ calibrationPerformed: v })
						}),
						form.calibrationPerformed ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Calibration details",
							required: true,
							error: errors.calibrationDetails,
							hint: "Reference used, deviation measured, and whether a certificate was issued.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 2,
								value: form.calibrationDetails,
								onChange: (e) => set({ calibrationDetails: e.target.value }),
								placeholder: "Verified against a calibrated NIBP simulator; deviation ≤2 mmHg across 50–250 mmHg. PM label updated."
							})
						}) : null
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
							className: "form-section__title",
							children: "Return to service — your professional judgement"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Checkbox, {
							label: "I have completed the required safety checks on this equipment",
							hint: "Required to mark a fault repaired. Record the readings in Test results above.",
							checked: form.safetyCheckConfirmed,
							onChange: (v) => set({ safetyCheckConfirmed: v })
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Checkbox, {
							label: "This equipment is safe to return to service",
							hint: "Uncheck to quarantine it: the fault cannot then be marked repaired, and the item stays out of use.",
							checked: form.safeToReturnToService,
							onChange: (v) => set({ safeToReturnToService: v })
						}),
						!form.safeToReturnToService ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Callout, {
							tone: "bad",
							title: "Quarantining this item",
							children: "While this box is unchecked the equipment cannot be returned to service from this report. Use “Awaiting parts”, keep it Under Repair, or set it Out of Service with a reason so users know not to touch it."
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Field, {
							label: "Additional notes",
							error: errors.notes,
							children: /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextArea, {
								rows: 2,
								value: form.notes,
								onChange: (e) => set({ notes: e.target.value }),
								placeholder: "Anything the department or the next technician should know."
							})
						})
					]
				})
			]
		});
	}
	function PartsEditor({ parts, onChange, catalogue, open, onToggle, errors }) {
		const add = () => onChange([...parts, {
			partId: "",
			partName: "",
			partNumber: "",
			quantity: 1,
			unitCost: "",
			recovered: false
		}]);
		const update = (index, patch) => onChange(parts.map((p, i) => i === index ? {
			...p,
			...patch
		} : p));
		const removeAt = (index) => onChange(parts.filter((_, i) => i !== index));
		const chooseCatalogue = (index, value) => {
			const item = catalogue.find((c) => String(c.id) === String(value));
			update(index, item ? {
				partId: item.id,
				partName: item.name,
				partNumber: item.code,
				unitCost: item.unitCost
			} : { partId: "" });
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
			className: "parts-editor",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "row row--between",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
						className: "form-note",
						children: "Add each part fitted. Parts not in the catalogue can be typed — they are still recorded on the line."
					}), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)(Button, {
						size: "sm",
						tone: "ghost",
						onClick: onToggle,
						children: [open ? "Hide" : "Show", " part lines"]
					})]
				}),
				open ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
					className: "stack",
					style: { gap: "var(--sp-2)" },
					children: [parts.map((p, i) => /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
						className: "part-line",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Select, {
								"aria-label": `Catalogue part ${i + 1}`,
								value: p.partId ?? "",
								onChange: (e) => chooseCatalogue(i, e.target.value),
								placeholder: "From catalogue…",
								options: [{
									value: "",
									label: "Not in catalogue / typed"
								}, ...catalogue.map((c) => ({
									value: c.id,
									label: `${c.name} (${c.code})`
								}))]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
								"aria-label": `Part name ${i + 1}`,
								value: p.partName,
								onChange: (e) => update(i, { partName: e.target.value }),
								placeholder: "Part name"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
								"aria-label": `Quantity ${i + 1}`,
								type: "number",
								min: "1",
								className: "part-line--qty",
								value: p.quantity,
								onChange: (e) => update(i, { quantity: e.target.value })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(TextInput, {
								"aria-label": `Unit cost ${i + 1}`,
								type: "number",
								min: "0",
								step: "0.01",
								className: "part-line--cost",
								value: p.unitCost,
								onChange: (e) => update(i, { unitCost: e.target.value }),
								placeholder: "Unit cost"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("span", {
								className: "part-line--line",
								children: formatMoney((Number(p.quantity) || 0) * (Number(p.unitCost) || 0))
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("button", {
								type: "button",
								className: "iconbtn iconbtn--bad",
								"aria-label": `Remove part line ${i + 1}`,
								onClick: () => removeAt(i),
								children: "✕"
							})
						]
					}, i)), /* @__PURE__ */ (0, import_jsx_runtime$11.jsxs)("div", {
						className: "row",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Button, {
							size: "sm",
							tone: "secondary",
							onClick: add,
							children: "＋ Add part"
						}), parts.length ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)(Checkbox, {
							label: "Keep removed parts for warranty",
							hint: "Marks the old part as recovered rather than thrown away.",
							checked: parts.some((p) => p.recovered),
							onChange: (v) => onChange(parts.map((p) => ({
								...p,
								recovered: v
							})))
						}) : null]
					})]
				}) : null,
				errors.parts ? /* @__PURE__ */ (0, import_jsx_runtime$11.jsx)("p", {
					className: "form-error",
					role: "alert",
					children: typeof errors.parts === "string" ? errors.parts : errors.parts.join(" ")
				}) : null
			]
		});
	}
	var import_react$10, import_jsx_runtime$11, MISSING_LABEL, EMPTY_REPAIR;
	var init_FaultDetailPage = __esmMin((() => {
		import_react$10 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_FileInput();
		init_Toast();
		init_useApi();
		init_format();
		init_constants();
		import_jsx_runtime$11 = require_jsx_runtime();
		MISSING_LABEL = {
			repairRecord: "the repair record itself",
			diagnosis: "a diagnosis",
			rootCause: "a root cause",
			repairActions: "what was done",
			testResults: "the test results",
			dateRepaired: "the date repaired",
			safetyCheckConfirmed: "your safety confirmation",
			safeToReturnToService: "the return-to-service decision"
		};
		EMPTY_REPAIR = {
			diagnosis: "",
			rootCause: "",
			troubleshooting: "",
			repairActions: "",
			testResults: "",
			calibrationPerformed: false,
			calibrationDetails: "",
			labourCost: "",
			otherCost: "",
			dateRepaired: toDateInput(/* @__PURE__ */ new Date()),
			notes: "",
			safetyCheckConfirmed: false,
			safeToReturnToService: true,
			parts: []
		};
	}));
	//#endregion
	//#region src/pages/ReportFaultPage.jsx
	var ReportFaultPage_exports = /* @__PURE__ */ __exportAll({ ReportFaultPage: () => ReportFaultPage$1 });
	/**
	* Fault reporting (Phase 4) — designed for a phone in a lab, in a hurry.
	* One screen, six decisions, no jargon, and a confirmation that tells the reporter what
	* happens next and how to find this report later.  Everything that can be derived
	* (location, equipment) is derived; everything that must be a human judgement is asked.
	*/
	function ReportFaultPage$1() {
		const { tag } = useParams();
		const navigate = useNavigate();
		const toast = useToast();
		const { user, is } = useAuth();
		const [picked, setPicked] = (0, import_react$9.useState)(null);
		const [pickerOpen, setPickerOpen] = (0, import_react$9.useState)(false);
		const [scanOpen, setScanOpen] = (0, import_react$9.useState)(false);
		const [submitting, setSubmitting] = (0, import_react$9.useState)(false);
		const [result, setResult] = (0, import_react$9.useState)(null);
		const [errors, setErrors] = (0, import_react$9.useState)({});
		const [form, setForm] = (0, import_react$9.useState)({
			categoryCode: "",
			severity: "medium",
			title: "",
			description: "",
			observedAt: toDateTimeLocal(/* @__PURE__ */ new Date()),
			locationId: "",
			onBehalfOf: "",
			photos: []
		});
		const vocab = useApi(() => faults.vocabulary(), []);
		const picklists = useApi(() => reference.picklists(), []);
		(0, import_react$9.useEffect)(() => {
			if (!tag) return;
			equipment.get(tag).then((r) => {
				setPicked(r.equipment);
				setForm((f) => ({
					...f,
					locationId: f.locationId || r.equipment.locationId || ""
				}));
			}).catch((err) => toast.error(errorText(err)));
		}, [tag, toast]);
		const set = (patch) => {
			setForm((f) => ({
				...f,
				...patch
			}));
			setErrors((e) => {
				const n = { ...e };
				for (const k of Object.keys(patch)) delete n[k];
				return n;
			});
		};
		const validate = () => {
			const p = {};
			if (!picked) p.equipment = "Choose which equipment is faulty.";
			if (!form.categoryCode) p.categoryCode = "Pick the closest category — “Unknown / other” is a valid answer.";
			if (!form.title.trim() || form.title.trim().length < 4) p.title = "Give it a short summary a technician will recognise (at least 4 characters).";
			if (form.description.trim().length < 10) p.description = "Describe what you see in a little more detail — what happens, when, and how often.";
			if (form.description.length > 4e3) p.description = "Please keep the description under 4000 characters.";
			return p;
		};
		const submit = async (event) => {
			event.preventDefault();
			const problems = validate();
			if (Object.keys(problems).length) {
				setErrors(problems);
				const el = document.querySelector(".field--error input, .field--error textarea, .field--error select");
				el?.scrollIntoView({
					block: "center",
					behavior: "smooth"
				});
				el?.focus();
				return;
			}
			const fd = new FormData();
			fd.append("equipmentId", String(picked.id));
			fd.append("categoryCode", form.categoryCode);
			fd.append("severity", form.severity);
			fd.append("title", form.title.trim());
			fd.append("description", form.description.trim());
			if (form.observedAt) fd.append("observedAt", new Date(form.observedAt).toISOString().slice(0, 19) + "Z");
			if (form.locationId) fd.append("locationId", String(form.locationId));
			if (form.onBehalfOf.trim()) fd.append("onBehalfOf", form.onBehalfOf.trim());
			for (const file of form.photos) fd.append("files", file);
			setSubmitting(true);
			try {
				const created = await faults.create(fd);
				setResult(created);
				toast.success(`Fault ${created.fault.reference} filed.`);
			} catch (err) {
				setErrors(mapErrors(err));
				toast.error(errorText(err));
			} finally {
				setSubmitting(false);
			}
		};
		if (result) {
			const f = result.fault;
			return /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
				className: "stack",
				style: {
					maxWidth: 640,
					marginInline: "auto"
				},
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
						className: "report-summary",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("p", {
								className: "page-head__eyebrow",
								style: { margin: 0 },
								children: "Report received"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("h1", {
								style: { fontSize: "1.2rem" },
								children: "Thank you — this is now in the workshop queue"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("p", {
								className: "report-summary__ref",
								children: f.reference
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("p", {
								className: "form-note",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("b", { children: result.equipment.name }),
									" (",
									result.equipment.assetTag,
									") is recorded as ",
									/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(EquipmentStatusPill, {
										status: result.equipment.status,
										size: "sm"
									}),
									". Keep it switched off and, if it is unsafe rather than merely faulty, tell the department desk as well."
								]
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Card, {
						title: "What happens next",
						children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("ol", {
							className: "steps",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("li", { children: [
									"An administrator or the responsible technician is told ",
									f.severity === "critical" ? "immediately — a critical report reaches the whole technical team at once" : "within the response target",
									"."
								] }),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("li", { children: "A technician inspects it. You will get a notification when the status changes." }),
								f.dueAt ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("li", { children: [
									"The department aims to respond by ",
									/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("b", { children: new Date(f.dueAt).toLocaleString() }),
									" for a ",
									f.severity,
									" report."
								] }) : null,
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("li", { children: "When it is fixed you will be asked to confirm it works before the report is closed — that last check matters, because you are the person who uses it." })
							]
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
						className: "row",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
								as: Link,
								to: `/faults/${f.id}`,
								tone: "primary",
								children: "Track this report"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
								as: Link,
								to: "/my-reports",
								tone: "secondary",
								children: "All my reports"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
								tone: "ghost",
								onClick: () => {
									setResult(null);
									setForm({
										...form,
										title: "",
										description: "",
										photos: [],
										categoryCode: "",
										severity: "medium"
									});
									setPicked(null);
									navigate("/faults/new", { replace: true });
								},
								children: "Report another fault"
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Callout, {
						tone: "safety",
						children: "This system records faults and maintenance for the department. It does not assess whether equipment is safe for a patient or a practical session — that judgement stays with the technician and the department."
					})
				]
			});
		}
		return /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(import_jsx_runtime$10.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(PageHeader, {
				back: {
					to: picked ? `/equipment/${picked.id}` : "/equipment",
					label: picked ? "Back to the equipment" : "All equipment"
				},
				eyebrow: "Fault report",
				title: picked ? `Report a fault · ${picked.name}` : "Report a fault",
				description: "A written report is what stops a fault being forgotten. Say what you see, not what you think is broken — that part is the technician's job.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
					tone: "secondary",
					icon: "▣",
					onClick: () => setScanOpen(true),
					children: "Scan label"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("form", {
				className: "report-form",
				onSubmit: submit,
				noValidate: true,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Card, {
						title: "1 · Which equipment",
						required: true,
						children: [picked ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
							className: "row row--between",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", { children: [
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("p", {
									className: "cell-title",
									children: picked.name
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("p", {
									className: "cell-sub",
									children: [picked.assetTag, picked.locationLabel ? ` · ${picked.locationLabel}` : ""]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
									className: "row row--tight",
									style: { marginTop: 6 },
									children: [
										/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(EquipmentStatusPill, { status: picked.status }),
										/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(MaintenanceLight, {
											state: picked.maintenanceState,
											daysUntil: picked.daysUntilPm
										}),
										picked.openFaultCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Badge, {
											tone: "warn",
											children: [
												"Already ",
												picked.openFaultCount,
												" open report",
												picked.openFaultCount > 1 ? "s" : ""
											]
										}) : null
									]
								})
							] }), /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("div", {
								className: "row row--tight",
								children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
									size: "sm",
									tone: "ghost",
									onClick: () => setPickerOpen(true),
									children: "Change"
								})
							})]
						}) : /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
							className: "row",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
									tone: "primary",
									onClick: () => setPickerOpen(true),
									children: "Choose equipment…"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
									tone: "secondary",
									onClick: () => setScanOpen(true),
									children: "Scan the QR label"
								}),
								errors.equipment ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("p", {
									className: "form-error",
									role: "alert",
									children: errors.equipment
								}) : null
							]
						}), picked?.openFaultCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Callout, {
							tone: "warn",
							title: "There is already an open report on this item",
							children: [
								"Someone has already reported a problem (",
								picked.openFaultReference,
								"). If yours is the same issue, please add to that report instead of creating a duplicate — ",
								`<`,
								"a duplicate splits the technician's attention",
								`>`,
								". Only continue if this is a different or worse problem."
							]
						}) : null]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Card, {
						title: "2 · What kind of problem",
						required: true,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(ChoiceChips, {
								name: "categoryCode",
								ariaLabel: "Fault category",
								value: form.categoryCode,
								onChange: (v) => set({ categoryCode: v }),
								size: "lg",
								options: (vocab.data?.categories ?? []).map((c) => ({
									value: c.code,
									label: c.name,
									hint: c.description
								}))
							}),
							vocab.loading ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Loading, {
								rows: 2,
								label: "Loading categories"
							}) : null,
							errors.categoryCode ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("p", {
								className: "form-error",
								role: "alert",
								children: errors.categoryCode
							}) : null,
							/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("p", {
								className: "form-note",
								children: [
									"If nothing fits, choose ",
									/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("b", { children: "Unknown / other" }),
									". Guessing a category is worse than not knowing one."
								]
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Card, {
						title: "3 · How bad is it",
						required: true,
						subtitle: "Choose by how it affects use — you are not being asked to judge the technical cause.",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(ChoiceChips, {
							name: "severity",
							ariaLabel: "Severity",
							value: form.severity,
							onChange: (v) => set({ severity: v }),
							size: "lg",
							options: Object.entries(SEVERITY).map(([value, meta]) => ({
								value,
								label: meta.label,
								hint: meta.hint,
								tone: meta.tone
							}))
						}), form.severity === "critical" ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Callout, {
							tone: "bad",
							title: "Critical means stop using it now",
							children: "Use this only when the equipment is unsafe or a safety function has failed. A critical report pages the technical team immediately. If anyone could be harmed by using it, also tell the department desk in person."
						}) : null]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Card, {
						title: "4 · What you observed",
						required: true,
						children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
							className: "form-grid",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
									label: "Short summary",
									required: true,
									error: errors.title,
									hint: "One line the workshop board will show.",
									children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(TextInput, {
										value: form.title,
										onChange: (e) => set({ title: e.target.value }),
										maxLength: 160,
										placeholder: "e.g. Left channel has no trace with known-good electrodes"
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
									label: "What happens, when, and how often",
									required: true,
									error: errors.description,
									hint: "Include what you already checked (cable seated? other electrodes? another socket?) and whether it is intermittent or constant.",
									counter: {
										text: `${form.description.length}/4000`,
										over: form.description.length > 4e3
									},
									children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(TextArea, {
										rows: 6,
										value: form.description,
										onChange: (e) => set({ description: e.target.value }),
										placeholder: "The unit powers on and the display works, but after about two minutes all channels flatline and it needs a power cycle. Happened three times in this morning's class."
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
									className: "form-grid form-grid--2",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
										label: "When did you first notice it",
										error: errors.observedAt,
										hint: "Defaults to now.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(TextInput, {
											type: "datetime-local",
											value: form.observedAt,
											onChange: (e) => set({ observedAt: e.target.value }),
											max: toDateTimeLocal(/* @__PURE__ */ new Date())
										})
									}), /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
										label: "Location",
										error: errors.locationId,
										hint: "Prefilled from the equipment record; change it if it has moved.",
										children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Select, {
											value: form.locationId,
											onChange: (e) => set({ locationId: e.target.value }),
											placeholder: "Same as the record",
											options: (picklists.data?.locations ?? []).map((l) => ({
												value: l.id,
												label: l.name
											}))
										})
									})]
								}),
								is("technician") || is("admin") ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
									label: "Reporting on behalf of",
									error: errors.onBehalfOf,
									hint: "Optional — a student or a ward, when you are logging it for them.",
									children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(TextInput, {
										value: form.onBehalfOf,
										onChange: (e) => set({ onBehalfOf: e.target.value }),
										placeholder: "e.g. Year 2 physiology group"
									})
								}) : null
							]
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Card, {
						title: "5 · Photographs (strongly recommended)",
						subtitle: "A photo of a display, error code or damaged cable often saves a whole day.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(FileInput, {
							label: "Evidence",
							kind: "photo",
							capture: "environment",
							maxFiles: vocab.data ? 5 : 5,
							maxSizeMb: 8,
							value: form.photos,
							onChange: (files) => set({ photos: files }),
							error: errors.files
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("div", {
						className: "row row--between",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("p", {
							className: "form-note",
							children: [
								"Filed as ",
								/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("b", { children: user?.fullName }),
								user?.department ? ` · ${user.department}` : "",
								". The technician can contact you through this report."
							]
						}), /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Button, {
							type: "submit",
							size: "lg",
							tone: "danger",
							loading: submitting,
							children: submitting ? "Filing…" : "Submit fault report"
						})]
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(EquipmentPicker, {
				open: pickerOpen,
				onClose: () => setPickerOpen(false),
				onPick: (e) => {
					setPicked(e);
					setPickerOpen(false);
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(QrScanner, {
				open: scanOpen,
				onClose: () => setScanOpen(false),
				onFound: (t) => navigate(`/report/${encodeURIComponent(t)}`, { replace: true }),
				title: "Scan the equipment label"
			})
		] });
	}
	function EquipmentPicker({ open, onClose, onPick }) {
		const [q, setQ] = (0, import_react$9.useState)("");
		const [rows, setRows] = (0, import_react$9.useState)([]);
		const [loading, setLoading] = (0, import_react$9.useState)(false);
		(0, import_react$9.useEffect)(() => {
			if (!open) return void 0;
			let alive = true;
			setLoading(true);
			const t = setTimeout(() => {
				equipment.list({
					q,
					perPage: 20,
					needsAttention: ""
				}).then((r) => {
					if (alive) setRows(r.items ?? []);
				}).catch(() => {}).finally(() => {
					if (alive) setLoading(false);
				});
			}, 250);
			return () => {
				alive = false;
				clearTimeout(t);
			};
		}, [open, q]);
		return /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)(Modal, {
			open,
			onClose,
			title: "Which equipment is faulty?",
			description: "Search by name, asset tag, serial or room — or scan the label.",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Field, {
				label: "Search",
				children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(TextInput, {
					autoFocus: true,
					value: q,
					onChange: (e) => setQ(e.target.value),
					placeholder: "e.g. centrifuge, BMU-CEN-0002, lab 2",
					"aria-label": "Search equipment"
				})
			}), loading ? /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(Loading, {
				rows: 4,
				label: "Searching"
			}) : /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("ul", {
				className: "cardlist",
				children: [rows.map((r) => /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("button", {
					type: "button",
					className: "listrow listrow--link",
					style: {
						width: "100%",
						border: "1px solid var(--line)",
						borderRadius: "var(--radius)",
						background: "var(--surface)",
						cursor: "pointer",
						font: "inherit",
						textAlign: "left"
					},
					onClick: () => onPick(r),
					children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("span", {
						className: "listrow__main",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("span", {
							className: "listrow__title",
							children: r.name
						}), /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("span", {
							className: "listrow__sub",
							children: [
								r.assetTag,
								" · ",
								r.locationLabel ?? "location not recorded"
							]
						})]
					}), /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("span", {
						className: "listrow__side",
						children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)(EquipmentStatusPill, {
							status: r.status,
							size: "sm"
						})
					})]
				}) }, r.id)), !rows.length && /* @__PURE__ */ (0, import_jsx_runtime$10.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$10.jsxs)("p", {
					className: "form-note",
					children: [
						"Nothing found",
						q ? ` for “${q}”` : "",
						". Try a shorter word, or the asset tag printed under the QR code."
					]
				}) })]
			})]
		});
	}
	function mapErrors(err) {
		const map = {
			category_id: "categoryCode",
			equipment_id: "equipment",
			observed_at: "observedAt",
			location_id: "locationId",
			on_behalf_of: "onBehalfOf"
		};
		const out = {};
		for (const [k, v] of Object.entries(err?.fieldErrors ?? {})) out[map[k] ?? k] = Array.isArray(v) ? v.join(" ") : String(v);
		if (err?.details?.files) out.files = "One or more files were rejected.";
		return out;
	}
	var import_react$9, import_jsx_runtime$10;
	var init_ReportFaultPage = __esmMin((() => {
		import_react$9 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_FileInput();
		init_QrScanner();
		init_Toast();
		init_useApi();
		init_constants();
		init_format();
		import_jsx_runtime$10 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/WorkQueuePage.jsx
	var WorkQueuePage_exports = /* @__PURE__ */ __exportAll({ WorkQueuePage: () => WorkQueuePage$1 });
	/**
	* The technician's working screen: what is mine, what nobody has taken, what is stuck, and
	* what needs a PM.  Quick actions are the two that unblock the board (claim, acknowledge);
	* everything else opens the report.
	*/
	function WorkQueuePage$1() {
		const { user } = useAuth();
		const toast = useToast();
		const [tab, setTab] = (0, import_react$8.useState)("mine");
		const [claiming, setClaiming] = (0, import_react$8.useState)(null);
		const [note, setNote] = (0, import_react$8.useState)("");
		const [busy, setBusy] = (0, import_react$8.useState)(false);
		const mine = useApi(() => faults.list({
			scope: "assigned",
			perPage: 50,
			sort: "severity",
			dir: "asc"
		}), []);
		const unassigned = useApi(() => faults.list({
			scope: "unassigned",
			perPage: 50,
			sort: "severity",
			dir: "asc"
		}), []);
		const board = useApi(() => maintenance.dueBoard({ days: 45 }), []);
		const rows = (tab === "mine" ? mine.data?.rows : tab === "unassigned" ? unassigned.data?.rows : []) ?? [];
		const act = async (fault, target, comment) => {
			setBusy(true);
			try {
				await faults.transition(fault.id, target, comment);
				toast.success(`Marked ${target.replace(/_/g, " ")}.`);
				setClaiming(null);
				setNote("");
				mine.refresh();
				unassigned.refresh();
			} catch (err) {
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)(import_jsx_runtime$9.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(PageHeader, {
				eyebrow: "Technicians",
				title: "Work queue",
				description: `Open faults by response urgency, plus the maintenance that is due. Sorted so the worst-waiting thing is at the top.`,
				actions: /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
					as: Link,
					to: "/faults/new",
					tone: "danger",
					children: "＋ Log a fault"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Tabs, {
				active: tab,
				onChange: setTab,
				tabs: [
					{
						id: "mine",
						label: "Assigned to me",
						count: mine.data?.pagination?.total ?? null
					},
					{
						id: "unassigned",
						label: "Unclaimed",
						count: unassigned.data?.pagination?.total ?? null
					},
					{
						id: "pm",
						label: "Maintenance due",
						count: board.data?.items?.length ?? null
					}
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)("div", {
				className: "tabpanel",
				children: tab === "pm" ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Card, {
					title: "Preventive maintenance due",
					subtitle: "Overdue first, then the next 45 days",
					pad: false,
					children: board.loading ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Loading, { rows: 4 }) : (board.data?.items ?? []).length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(EmptyState, {
						icon: "🟢",
						title: "Nothing due",
						description: "Every scheduled item on the fleet is inside its interval."
					}) : /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(DataTable, {
						dense: true,
						rows: board.data?.items ?? [],
						columns: [
							{
								key: "equipmentName",
								label: "Equipment",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("div", {
									className: "cell-main",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Link, {
										className: "cell-title",
										to: `/equipment/${r.equipmentId}`,
										children: r.equipmentName
									}), /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("span", {
										className: "cell-sub",
										children: [
											r.assetTag,
											" · ",
											r.locationName ?? "—"
										]
									})]
								})
							},
							{
								key: "nextScheduleTitle",
								label: "Schedule",
								render: (r) => r.nextScheduleTitle ?? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)("span", {
									className: "muted",
									children: "Ad-hoc date only"
								})
							},
							{
								key: "nextMaintenanceOn",
								label: "Due",
								render: (r) => r.nextMaintenanceOn ?? "—"
							},
							{
								key: "pmState",
								label: "State",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(MaintenanceLight, {
									state: r.pmState.state,
									label: r.pmState.label
								})
							},
							{
								key: "responsibleTechnicianName",
								label: "Owner",
								render: (r) => r.responsibleTechnicianName ?? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Badge, {
									tone: "warn",
									size: "sm",
									children: "unassigned"
								})
							},
							{
								key: "go",
								label: "",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									as: Link,
									to: `/maintenance?tab=schedules&equipment=${r.equipmentId}`,
									children: "Open"
								})
							}
						]
					})
				}) : /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Card, {
					pad: false,
					title: tab === "mine" ? "Your open faults" : "Waiting for an owner",
					subtitle: tab === "mine" ? "Oldest critical first" : "Claiming one makes you the responsible technician and starts the clock",
					children: (tab === "mine" ? mine.loading : unassigned.loading) ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("div", {
						className: "stack",
						style: { padding: "var(--sp-4)" },
						children: [rows.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(EmptyState, {
							icon: "✓",
							title: tab === "mine" ? "Your queue is clear" : "Nothing is unclaimed",
							description: tab === "mine" ? "Nice work. Verify anything waiting on a user to close it out." : "Every open report has someone responsible for it."
						}), rows.map((f) => /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("article", {
							className: "workrow",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("div", {
								className: "workrow__main",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("div", {
										className: "row row--tight",
										style: {
											gap: 6,
											flexWrap: "wrap"
										},
										children: [
											/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(SeverityPill, { severity: f.severity }),
											/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(FaultStatusPill, { status: f.status }),
											f.isOverdue ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)(Badge, {
												tone: "bad",
												children: [
													"⏱ ",
													formatDuration(f.minutesOverdue),
													" past target"
												]
											}) : f.dueAt ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)(Badge, {
												tone: "neutral",
												children: ["target ", relativeTime(f.dueAt)]
											}) : null
										]
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Link, {
										to: `/faults/${f.id}`,
										className: "workrow__title",
										children: f.title
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)("p", {
										className: "cell-sub",
										children: [
											/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)("code", { children: f.reference }),
											" · ",
											f.equipmentName,
											" (",
											f.assetTag,
											")",
											f.locationName ? ` · ${f.locationName}` : "",
											" · reported ",
											relativeTime(f.createdAt),
											" by ",
											f.reportedByName
										]
									})
								]
							}), /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)("div", {
								className: "workrow__actions",
								children: tab === "unassigned" ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)(import_jsx_runtime$9.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									tone: "primary",
									onClick: () => act(f, "under_inspection", "Claimed from the board"),
									children: "Claim & inspect"
								}), /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									tone: "ghost",
									onClick: () => {
										setClaiming(f);
										setNote("");
									},
									children: "With a note…"
								})] }) : f.status === "assigned" ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									tone: "primary",
									onClick: () => act(f, "acknowledged"),
									children: "Acknowledge"
								}) : f.status === "acknowledged" || f.status === "under_inspection" ? /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									tone: "primary",
									as: Link,
									to: `/faults/${f.id}`,
									children: "Open & work"
								}) : /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
									size: "sm",
									tone: "secondary",
									as: Link,
									to: `/faults/${f.id}`,
									children: "Open"
								})
							})]
						}, f.id))]
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Modal, {
				open: !!claiming,
				onClose: () => setClaiming(null),
				title: "Claim this fault",
				description: claiming ? `${claiming.reference} — ${claiming.title}` : "",
				footer: /* @__PURE__ */ (0, import_jsx_runtime$9.jsxs)(import_jsx_runtime$9.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
					tone: "ghost",
					onClick: () => setClaiming(null),
					children: "Cancel"
				}), /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Button, {
					tone: "primary",
					loading: busy,
					onClick: () => act(claiming, "under_inspection", note || void 0),
					children: "Claim and start inspecting"
				})] }),
				children: /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(Field, {
					label: "Note for the reporter (optional)",
					hint: "Say when you expect to look at it; that is usually all they want.",
					children: /* @__PURE__ */ (0, import_jsx_runtime$9.jsx)(TextArea, {
						rows: 3,
						value: note,
						onChange: (e) => setNote(e.target.value),
						placeholder: "I will test it tomorrow morning during the lab session."
					})
				})
			})
		] });
	}
	var import_react$8, import_jsx_runtime$9;
	var init_WorkQueuePage = __esmMin((() => {
		import_react$8 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$9 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/MaintenancePage.jsx
	var MaintenancePage_exports = /* @__PURE__ */ __exportAll({ MaintenancePage: () => MaintenancePage$1 });
	/**
	* Preventive maintenance: the due board, the schedules that produce it, and the record of
	* what was actually done.  One screen because a technician moves between all three in a visit.
	*/
	function MaintenancePage$1() {
		const [params, setParams] = useSearchParams();
		const { can } = useAuth();
		const tab = params.get("tab") ?? "due";
		const equipmentFilter = params.get("equipment") ?? "";
		const set = (patch) => {
			const next = new URLSearchParams(params);
			for (const [k, v] of Object.entries(patch)) if (!v) next.delete(k);
			else next.set(k, String(v));
			setParams(next, { replace: true });
		};
		const due = useApi(() => maintenance.dueBoard({
			days: tab === "due" ? 45 : 400,
			includeUpToDate: tab === "all" ? "1" : ""
		}), [tab]);
		const schedules = useApi(() => maintenance.schedules(equipmentFilter ? { equipmentId: equipmentFilter } : {}), [equipmentFilter, tab]);
		const records = useApi(() => maintenance.records({
			equipmentId: equipmentFilter || void 0,
			perPage: 25,
			page: params.get("page") ?? 1
		}), [
			equipmentFilter,
			params.get("page"),
			tab
		]);
		const compliance = useApi(() => maintenance.compliance({ days: 180 }), []);
		const [scheduleForm, setScheduleForm] = (0, import_react$7.useState)(null);
		const [complete, setComplete] = (0, import_react$7.useState)(null);
		const [detail, setDetail] = (0, import_react$7.useState)(null);
		const [confirmDelete, setConfirmDelete] = (0, import_react$7.useState)(null);
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(import_jsx_runtime$8.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(PageHeader, {
				eyebrow: "Preventive maintenance",
				title: "Maintenance programme",
				description: "Schedules decide what is due; signed-off records are the proof it happened. Both feed the equipment history and the compliance figures.",
				actions: can("maintenance.schedule.manage") ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
					tone: "primary",
					onClick: () => setScheduleForm({
						intervalDays: 180,
						checklist: ""
					}),
					children: "＋ New schedule"
				}) : null
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("div", {
				className: "grid grid--kpi",
				style: { marginBottom: "var(--sp-4)" },
				children: [
					{
						label: "Overdue",
						value: compliance.data?.overdueEquipment,
						tone: "bad",
						hint: "needs a visit now"
					},
					{
						label: "Due soon",
						value: compliance.data?.dueSoonEquipment,
						tone: "warn",
						hint: "inside 14 days"
					},
					{
						label: "Up to date",
						value: compliance.data?.upToDateEquipment,
						tone: "ok",
						hint: "inside interval"
					},
					{
						label: "No schedule",
						value: compliance.data?.unscheduledEquipment,
						tone: "neutral",
						hint: "invisible to the board"
					},
					{
						label: "Visits on time",
						value: compliance.data?.onTimePercent === null ? "—" : `${compliance.data?.onTimePercent ?? 0}%`,
						tone: "info",
						hint: `${compliance.data?.recordsOnTime ?? 0}/${compliance.data?.recordsDone ?? 0} in 180 days`
					}
				].map((k) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
					className: cx("kpi", `kpi--${k.tone}`),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
							className: "kpi__label",
							children: k.label
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
							className: "kpi__value",
							children: k.value ?? "—"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
							className: "kpi__hint",
							children: k.hint
						})
					]
				}, k.label))
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Tabs, {
				active: tab,
				onChange: (id) => set({ tab: id }),
				tabs: [
					{
						id: "due",
						label: "Due board"
					},
					{
						id: "schedules",
						label: "Schedules",
						count: schedules.data?.items?.length
					},
					{
						id: "records",
						label: "History"
					}
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
				className: "tabpanel",
				children: [
					tab === "due" || tab === "all" ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(Card, {
						pad: false,
						title: tab === "all" ? "Every scheduled item" : "Due in the next 45 days (and anything overdue)",
						children: [due.loading && !due.data ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(DataTable, {
							rows: due.data?.items ?? [],
							empty: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(EmptyState, {
								icon: "🟢",
								title: "Nothing is due",
								description: "Every scheduled item is inside its interval. If some equipment has no schedule, that is a gap worth closing."
							}),
							columns: [
								{
									key: "equipmentName",
									label: "Equipment",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "cell-main",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Link, {
											className: "cell-title",
											to: `/equipment/${r.equipmentId}`,
											children: r.equipmentName
										}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("span", {
											className: "cell-sub",
											children: [
												r.assetTag,
												" · ",
												r.locationName ?? "no room recorded"
											]
										})]
									})
								},
								{
									key: "pmState",
									label: "Status",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(MaintenanceLight, {
										state: r.pmState.state,
										label: r.pmState.label
									})
								},
								{
									key: "nextMaintenanceOn",
									label: "Due",
									render: (r) => formatDate(r.nextMaintenanceOn)
								},
								{
									key: "lastMaintenanceOn",
									label: "Last done",
									render: (r) => r.lastMaintenanceOn ? formatDate(r.lastMaintenanceOn) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
										className: "muted",
										children: "never"
									})
								},
								{
									key: "nextScheduleTitle",
									label: "Schedule",
									render: (r) => r.nextScheduleTitle ?? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Badge, {
										tone: "neutral",
										size: "sm",
										children: "no schedule"
									})
								},
								{
									key: "responsibleTechnicianName",
									label: "Owner",
									render: (r) => r.responsibleTechnicianName ?? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
										className: "muted",
										children: "unassigned"
									})
								},
								{
									key: "actions",
									label: "",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "row row--tight",
										children: [can("maintenance.record.write") && r.scheduleCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
											size: "sm",
											tone: "primary",
											onClick: () => {
												set({
													tab: "schedules",
													equipment: r.equipmentId
												});
											},
											children: "Record PM"
										}) : null, /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
											size: "sm",
											as: Link,
											to: `/equipment/${r.equipmentId}`,
											tone: "ghost",
											children: "Open item"
										})]
									})
								}
							]
						}), can("meta.manage") ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("div", {
							style: {
								padding: "var(--sp-3) var(--sp-4)",
								borderTop: "1px solid var(--line)"
							},
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(ReminderButton, { onDone: () => {
								due.refresh();
							} })
						}) : null]
					}) : null,
					tab === "schedules" ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Card, {
						pad: false,
						title: "Schedules",
						subtitle: "One per inspection type per item; the earliest due date drives the board",
						actions: equipmentFilter ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
							size: "sm",
							tone: "ghost",
							onClick: () => set({ equipment: "" }),
							children: "Clear equipment filter"
						}) : null,
						children: schedules.loading && !schedules.data ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(DataTable, {
							rows: schedules.data?.items ?? [],
							empty: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(EmptyState, {
								icon: "◷",
								title: "No schedules yet",
								description: "Add one per item — daily, quarterly, annual — with the checks the manufacturer manual requires."
							}),
							columns: [
								{
									key: "title",
									label: "Schedule",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "cell-main",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("button", {
											type: "button",
											className: "cell-title linklike",
											onClick: () => setDetail(r),
											children: r.title
										}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("span", {
											className: "cell-sub",
											children: [
												r.equipmentName,
												" · ",
												r.assetTag
											]
										})]
									})
								},
								{
									key: "intervalDays",
									label: "Interval",
									align: "center",
									render: (r) => `${r.intervalDays} d`
								},
								{
									key: "lastDoneOn",
									label: "Last",
									render: (r) => r.lastDoneOn ? formatDate(r.lastDoneOn) : "—"
								},
								{
									key: "nextDueOn",
									label: "Next due",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("span", { children: [
										formatDate(r.nextDueOn),
										" ",
										/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(MaintenanceLight, { state: r.pmState.state })
									] })
								},
								{
									key: "checklistCount",
									label: "Checks",
									align: "center"
								},
								{
									key: "timesDone",
									label: "Done",
									align: "center"
								},
								{
									key: "responsibleTechnicianName",
									label: "Owner",
									render: (r) => r.responsibleTechnicianName ?? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
										className: "muted",
										children: "—"
									})
								},
								{
									key: "actions",
									label: "",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "row row--tight",
										children: [
											can("maintenance.record.write") ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(PMCompleteDialog, {
												schedule: r,
												onDone: () => {
													schedules.refresh();
													due.refresh();
													records.refresh();
												},
												trigger: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
													size: "sm",
													tone: "primary",
													children: "Complete"
												})
											}) : null,
											can("maintenance.schedule.manage") ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
												size: "sm",
												tone: "ghost",
												onClick: () => setScheduleForm({
													...r,
													checklist: (r.checklist ?? []).map((c) => c.label).join("\n")
												}),
												children: "Edit"
											}) : null,
											can("meta.manage") ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
												size: "sm",
												tone: "ghost",
												onClick: () => setConfirmDelete(r),
												children: "Delete"
											}) : null
										]
									})
								}
							]
						})
					}) : null,
					tab === "records" ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Card, {
						pad: false,
						title: "Maintenance history",
						subtitle: "Signed-off preventive visits, with checklist findings",
						children: records.loading && !records.data ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Loading, { rows: 5 }) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(import_jsx_runtime$8.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(DataTable, {
							rows: records.data?.rows ?? [],
							empty: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(EmptyState, {
								icon: "📄",
								title: "No records",
								description: "Completed visits appear here, and in each item’s history."
							}),
							columns: [
								{
									key: "reference",
									label: "Reference",
									mono: true
								},
								{
									key: "equipmentName",
									label: "Equipment",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "cell-main",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Link, {
											className: "cell-title",
											to: `/equipment/${r.equipmentId ?? ""}`,
											children: r.equipmentName
										}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
											className: "cell-sub",
											children: r.assetTag
										})]
									})
								},
								{
									key: "performedOn",
									label: "Performed",
									render: (r) => formatDate(r.performedOn)
								},
								{
									key: "daysLate",
									label: "Late",
									align: "center",
									render: (r) => r.daysLate > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(Badge, {
										tone: "warn",
										size: "sm",
										children: [r.daysLate, " d"]
									}) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
										className: "muted",
										children: "on time"
									})
								},
								{
									key: "conditionFound",
									label: "Condition",
									render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Badge, {
										tone: r.conditionFound === "pass" ? "ok" : r.conditionFound?.includes("needs") ? "warn" : "neutral",
										children: (r.conditionFound ?? "—").replace(/_/g, " ")
									})
								},
								{
									key: "findings",
									label: "Findings"
								},
								{
									key: "performedByName",
									label: "By"
								}
							]
						}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Pagination, {
							pagination: records.data?.pagination,
							onPage: (page) => set({ page })
						})] })
					}) : null
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(ScheduleFormModal, {
				state: scheduleForm,
				onClose: () => setScheduleForm(null),
				onDone: () => {
					setScheduleForm(null);
					schedules.refresh();
					due.refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(ScheduleDetailModal, {
				row: detail,
				onClose: () => setDetail(null),
				onDone: () => {
					setDetail(null);
					schedules.refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(ConfirmDialog, {
				open: !!confirmDelete,
				title: "Delete this schedule?",
				body: confirmDelete ? `“${confirmDelete.title}” on ${confirmDelete.equipmentName}. Completed records stay in the history but lose their schedule link.` : "",
				confirmLabel: "Delete schedule",
				onCancel: () => setConfirmDelete(null),
				busy: false,
				onConfirm: async ({ confirmText }) => {
					try {
						await maintenance.deleteSchedule(confirmDelete.id, confirmText ?? "superseded");
						toast.success("Schedule deleted.");
						setConfirmDelete(null);
						schedules.refresh();
						due.refresh();
					} catch (err) {
						toast.error(errorText(err), { detail: err.details?.fields?.reason ? "Give a reason in the box, then try again." : null });
					}
				}
			})
		] });
	}
	function ReminderButton({ onDone }) {
		const toast = useToast();
		const [busy, setBusy] = (0, import_react$7.useState)(false);
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("form", {
			className: "row row--between",
			onSubmit: async (e) => {
				e.preventDefault();
				setBusy(true);
				try {
					const r = await maintenance.reminders();
					toast.success(`${r.sent} reminder(s) sent; ${r.skipped} already sent recently.`);
					onDone();
				} catch (err) {
					toast.error(errorText(err));
				} finally {
					setBusy(false);
				}
			},
			children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
				className: "form-note",
				children: "Reminder sweep — notifies owners of overdue and soon-due items. Idempotent: nothing is re-sent within 7 days."
			}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				size: "sm",
				tone: "secondary",
				loading: busy,
				type: "submit",
				children: "Run reminder sweep"
			})]
		});
	}
	function PMCompleteDialog({ schedule, onDone }) {
		const toast = useToast();
		const [open, setOpen] = (0, import_react$7.useState)(false);
		const [busy, setBusy] = (0, import_react$7.useState)(false);
		const [errors, setErrors] = (0, import_react$7.useState)({});
		const [form, setForm] = (0, import_react$7.useState)(null);
		const target = useApi(() => open ? maintenance.schedule(schedule.id) : Promise.resolve(null), [open, schedule.id]).data;
		(0, import_react$7.useEffect)(() => {
			if (!open) return;
			setForm({
				performedOn: toDateInput(/* @__PURE__ */ new Date()),
				durationMinutes: "",
				downtimeMinutes: "",
				findings: "",
				actionsTaken: "",
				conditionFound: "",
				markAsMissed: false,
				checklist: (target?.checklist ?? []).map((c) => ({
					itemId: c.id,
					label: c.label,
					outcome: "pass",
					note: ""
				}))
			});
		}, [open, target?.checklist]);
		const submit = async () => {
			setBusy(true);
			setErrors({});
			try {
				await maintenance.complete(target?.id ?? schedule.id, {
					performedOn: form.performedOn,
					durationMinutes: form.durationMinutes === "" ? void 0 : Number(form.durationMinutes),
					downtimeMinutes: form.downtimeMinutes === "" ? void 0 : Number(form.downtimeMinutes),
					findings: form.findings || void 0,
					actionsTaken: form.actionsTaken || void 0,
					conditionFound: form.conditionFound || void 0,
					markAsMissed: form.markAsMissed,
					checklistResults: form.checklist.map((c) => ({
						itemId: c.itemId,
						label: c.label,
						outcome: c.outcome,
						note: c.note || void 0
					}))
				});
				toast.success(form.markAsMissed ? "Missed visit recorded — the item stays overdue, as it should." : "Maintenance signed off; the next due date has moved forward.");
				setOpen(false);
				onDone();
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const failedCount = form?.checklist.filter((c) => c.outcome === "fail").length ?? 0;
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(import_jsx_runtime$8.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
			size: "sm",
			tone: "primary",
			onClick: () => setOpen(true),
			children: "Complete"
		}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Modal, {
			open,
			onClose: () => !busy && setOpen(false),
			size: "lg",
			title: `Complete “${target?.title ?? schedule.title ?? "maintenance"}”`,
			description: target ? `${target.equipmentName} · ${target.assetTag} · every ${target.intervalDays} days` : "",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(import_jsx_runtime$8.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				tone: "ghost",
				onClick: () => setOpen(false),
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: form?.markAsMissed ? "Record missed visit" : "Sign off maintenance"
			})] }),
			children: !form ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
				className: "stack",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
						className: "form-grid form-grid--2",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
								label: "Date performed",
								required: true,
								error: errors.performedOn,
								children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
									type: "date",
									max: toDateInput(/* @__PURE__ */ new Date()),
									value: form.performedOn,
									onChange: (e) => setForm({
										...form,
										performedOn: e.target.value
									})
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
								label: "Time spent (minutes)",
								error: errors.durationMinutes,
								children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
									type: "number",
									min: "0",
									value: form.durationMinutes,
									onChange: (e) => setForm({
										...form,
										durationMinutes: e.target.value
									}),
									placeholder: "e.g. 45"
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
								label: "Equipment downtime (minutes)",
								error: errors.downtimeMinutes,
								hint: "How long it was unavailable to the department.",
								children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
									type: "number",
									min: "0",
									value: form.downtimeMinutes,
									onChange: (e) => setForm({
										...form,
										downtimeMinutes: e.target.value
									})
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
								label: "Condition found",
								error: errors.conditionFound,
								hint: "Left blank, a clean pass is recorded; a failed check sets “needs attention”.",
								children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Select, {
									value: form.conditionFound,
									onChange: (e) => setForm({
										...form,
										conditionFound: e.target.value
									}),
									placeholder: "Auto from the checklist",
									options: [
										"pass",
										"pass_with_notes",
										"needs_attention",
										"needs_repair",
										"replaced"
									].map((v) => ({
										value: v,
										label: v.replace(/_/g, " ")
									}))
								})
							})
						]
					}),
					form.checklist.length ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
						className: "stack",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("p", {
							className: "form-section__title",
							children: [
								"Checklist (",
								form.checklist.length,
								")"
							]
						}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("ul", {
							className: "checklist",
							children: form.checklist.map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("li", {
								className: cx("checklist__item", c.outcome === "fail" && "checklist__item--fail"),
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
										className: "checklist__label",
										children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("input", {
											id: `cl-${i}`,
											type: "checkbox",
											className: "sr-only",
											checked: c.outcome === "pass",
											onChange: (e) => setForm({
												...form,
												checklist: form.checklist.map((x, j) => j === i ? {
													...x,
													outcome: e.target.checked ? "pass" : "fail"
												} : x)
											})
										}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("label", {
											htmlFor: `cl-${i}`,
											className: "checklist__toggle",
											title: "Pass",
											children: ["✓ ", c.label]
										})]
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("div", {
										className: "row row--tight",
										children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("select", {
											className: "input input--sm",
											value: c.outcome,
											onChange: (e) => setForm({
												...form,
												checklist: form.checklist.map((x, j) => j === i ? {
													...x,
													outcome: e.target.value
												} : x)
											}),
											"aria-label": `Outcome for ${c.label}`,
											children: [
												/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("option", {
													value: "pass",
													children: "Pass"
												}),
												/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("option", {
													value: "fail",
													children: "Fail"
												}),
												/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("option", {
													value: "na",
													children: "N/A"
												})
											]
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
										className: "input--sm",
										placeholder: "Note (required reading for the next visit)",
										value: c.note,
										"aria-label": `Note for ${c.label}`,
										onChange: (e) => setForm({
											...form,
											checklist: form.checklist.map((x, j) => j === i ? {
												...x,
												note: e.target.value
											} : x)
										})
									})
								]
							}, c.itemId ?? i))
						})]
					}) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Callout, {
						tone: "info",
						children: "This schedule has no checklist items. Consider adding the manual’s checks so the next technician has something to work against."
					}),
					failedCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(Callout, {
						tone: "warn",
						title: `${failedCount} check(s) failed`,
						children: [
							"This visit will be recorded as ",
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("b", { children: "needs attention" }),
							" and the department is notified. Preventive maintenance does not open a repair job: raise a ",
							/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Link, {
								to: "/faults/new",
								children: "fault report"
							}),
							" so the work is tracked, assigned and costed."
						]
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
						className: "form-grid form-grid--2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Findings",
							error: errors.findings,
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextArea, {
								rows: 3,
								value: form.findings,
								onChange: (e) => setForm({
									...form,
									findings: e.target.value
								}),
								placeholder: "What you measured and saw, including anything marginal."
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Actions taken",
							error: errors.actionsTaken,
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextArea, {
								rows: 3,
								value: form.actionsTaken,
								onChange: (e) => setForm({
									...form,
									actionsTaken: e.target.value
								}),
								placeholder: "Consumables renewed, cleaning, adjustments, labels updated."
							})
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Checkbox, {
						label: "This visit could not be carried out (record it as missed)",
						hint: "Honest bookkeeping: the item stays overdue and the compliance figure reflects the miss, rather than the date silently moving forward.",
						checked: form.markAsMissed,
						onChange: (v) => setForm({
							...form,
							markAsMissed: v
						})
					}),
					form.markAsMissed ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
						label: "Why not?",
						required: true,
						error: errors.findings,
						children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
							value: form.findings,
							onChange: (e) => setForm({
								...form,
								findings: e.target.value
							}),
							placeholder: "e.g. Room booked for OSCEs all week"
						})
					}) : null
				]
			})
		})] });
	}
	function ScheduleFormModal({ state, onClose, onDone }) {
		const toast = useToast();
		const [busy, setBusy] = (0, import_react$7.useState)(false);
		const [errors, setErrors] = (0, import_react$7.useState)({});
		const picklists = useApi(() => reference.picklists(), []);
		const vocab = useApi(() => maintenance.vocabulary(), []);
		const [form, setForm] = (0, import_react$7.useState)({});
		(0, import_react$7.useEffect)(() => {
			if (!state) return;
			setForm({
				id: state.id,
				equipmentId: state.equipmentId ?? state.equipment_id ?? "",
				title: state.title ?? "Annual full service",
				intervalDays: state.intervalDays ?? 180,
				responsibleTechnicianId: state.responsibleTechnicianId ?? "",
				nextDueOn: toDateInput(state.nextDueOn) || "",
				lastDoneOn: toDateInput(state.lastDoneOn) || "",
				notes: state.notes ?? "",
				checklist: Array.isArray(state.checklist) ? state.checklist.map((c) => typeof c === "string" ? c : c.label).join("\n") : (state.checklist ?? defaultChecklist(state.title, vocab.data?.checklistTemplates)).join("\n")
			});
			setErrors({});
		}, [state, vocab.data]);
		const submit = async () => {
			setBusy(true);
			setErrors({});
			const payload = {
				equipmentId: Number(form.equipmentId),
				title: form.title.trim(),
				intervalDays: Number(form.intervalDays),
				responsibleTechnicianId: form.responsibleTechnicianId ? Number(form.responsibleTechnicianId) : void 0,
				nextDueOn: form.nextDueOn || void 0,
				lastDoneOn: form.lastDoneOn || void 0,
				notes: form.notes || void 0,
				checklist: form.checklist.split("\n").map((s) => s.trim()).filter(Boolean)
			};
			try {
				if (form.id) await maintenance.updateSchedule(form.id, payload);
				else await maintenance.createSchedule(payload);
				toast.success(form.id ? "Schedule updated." : "Schedule created and added to the due board.");
				onDone();
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const applyTemplate = (title) => {
			const key = title.toLowerCase().split(" ")[0];
			const tpl = (vocab.data?.checklistTemplates ?? []).find((t) => t.interval === key);
			setForm((f) => ({
				...f,
				title,
				...tpl ? { checklist: tpl.items.join("\n") } : {}
			}));
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(Modal, {
			open: !!state,
			onClose: () => !busy && onClose(),
			size: "lg",
			title: state?.id ? "Edit maintenance schedule" : "New maintenance schedule",
			description: "Interval + checklist + owner. The equipment’s next-due date follows the earliest active schedule.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(import_jsx_runtime$8.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: state?.id ? "Save schedule" : "Create schedule"
			})] }),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
					className: "form-grid form-grid--2",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Equipment",
							required: true,
							error: errors.equipmentId,
							children: !state?.id ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(EquipmentChooser, {
								value: form.equipmentId,
								onChange: (v) => setForm({
									...form,
									equipmentId: v
								})
							}) : /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("p", {
								className: "form-note",
								children: [
									state.equipmentName,
									" · ",
									/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("code", { children: state.assetTag })
								]
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Schedule title",
							required: true,
							error: errors.title,
							hint: "Matching a template name (Monthly, Quarterly, Annual…) pre-fills a checklist you can edit.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
								value: form.title ?? "",
								onChange: (e) => applyTemplate(e.target.value),
								placeholder: "Annual full service"
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Interval (days)",
							required: true,
							error: errors.intervalDays,
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
								type: "number",
								min: "1",
								max: "3650",
								value: form.intervalDays ?? "",
								onChange: (e) => setForm({
									...form,
									intervalDays: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Next due",
							error: errors.nextDueOn,
							hint: "Blank = interval from today or from the last done date.",
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
								type: "date",
								value: form.nextDueOn ?? "",
								onChange: (e) => setForm({
									...form,
									nextDueOn: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Last performed",
							error: errors.lastDoneOn,
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
								type: "date",
								value: form.lastDoneOn ?? "",
								onChange: (e) => setForm({
									...form,
									lastDoneOn: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
							label: "Responsible technician",
							error: errors.responsibleTechnicianId,
							children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Select, {
								value: form.responsibleTechnicianId ?? "",
								onChange: (e) => setForm({
									...form,
									responsibleTechnicianId: e.target.value
								}),
								placeholder: "From the equipment record",
								options: (picklists.data?.technicians ?? []).map((t) => ({
									value: t.id,
									label: t.fullName
								}))
							})
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
					label: "Checklist (one item per line)",
					error: errors.checklist,
					hint: "These become individual rows, so each result can be recorded per visit.",
					children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextArea, {
						rows: 7,
						value: form.checklist ?? "",
						onChange: (e) => setForm({
							...form,
							checklist: e.target.value
						})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Field, {
					label: "Notes",
					error: errors.notes,
					children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextArea, {
						rows: 2,
						value: form.notes ?? "",
						onChange: (e) => setForm({
							...form,
							notes: e.target.value
						})
					})
				})
			]
		});
	}
	function EquipmentChooser({ value, onChange }) {
		const [q, setQ] = (0, import_react$7.useState)("");
		const res = useApi(() => equipment.list({
			q: q || void 0,
			perPage: 8
		}), [q]);
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
			className: "stack",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(TextInput, {
				placeholder: "Search equipment to attach this schedule to…",
				value: q,
				onChange: (e) => setQ(e.target.value),
				"aria-label": "Search equipment"
			}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("ul", {
				className: "chips",
				children: (res.data?.items ?? []).map((e) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("button", {
					type: "button",
					className: cx("chip", value === e.id && "chip--active"),
					onClick: () => onChange(e.id),
					children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
						className: "chip__label",
						children: e.name
					}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("span", {
						className: "chip__hint",
						children: e.assetTag
					})]
				}) }, e.id))
			})]
		});
	}
	function ScheduleDetailModal({ row, onClose, onDone }) {
		const detail = useApi(() => row ? maintenance.schedule(row.id) : Promise.resolve(null), [row?.id]);
		return /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Modal, {
			open: !!row,
			onClose,
			size: "lg",
			title: row ? `${row.title} · ${row.equipmentName}` : "",
			description: "Definition, recent visits and the state of the clock.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(import_jsx_runtime$8.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Close"
			}) }),
			children: detail.loading ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Loading, { rows: 4 }) : detail.data && /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", {
				className: "stack",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(KeyValue, {
						columns: 3,
						items: [
							["Interval", `${detail.data.intervalDays} days`],
							["Next due", /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("span", { children: [
								formatDate(detail.data.nextDueOn),
								" ",
								/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(MaintenanceLight, { state: detail.data.pmState.state })
							] })],
							["Last done", formatDate(detail.data.lastDoneOn)],
							["Owner", detail.data.responsibleTechnicianName ?? "Unassigned"],
							["Times completed", detail.data.recentRecords?.length ?? 0],
							["Notes", detail.data.notes]
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
						className: "form-section__title",
						children: "Checklist"
					}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("ol", {
						className: "steps",
						children: (detail.data.checklist ?? []).map((c) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("li", { children: [c.label, c.requiresEvidence ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Badge, {
							size: "sm",
							tone: "neutral",
							children: " evidence required"
						}) : null] }, c.id))
					})] }),
					/* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
						className: "form-section__title",
						children: "Recent visits"
					}), /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(DataTable, {
						dense: true,
						rows: detail.data.recentRecords ?? [],
						columns: [
							{
								key: "performedOn",
								label: "Date",
								render: (r) => formatDate(r.performedOn)
							},
							{
								key: "dueOn",
								label: "Was due",
								render: (r) => formatDate(r.dueOn)
							},
							{
								key: "daysLate",
								label: "Late",
								align: "center",
								render: (r) => r.daysLate > 0 ? /* @__PURE__ */ (0, import_jsx_runtime$8.jsxs)(Badge, {
									tone: "warn",
									size: "sm",
									children: [r.daysLate, " d"]
								}) : "—"
							},
							{
								key: "conditionFound",
								label: "Condition",
								render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)(Badge, {
									tone: r.conditionFound === "pass" ? "ok" : "warn",
									children: (r.conditionFound ?? "—").replace(/_/g, " ")
								})
							},
							{
								key: "findings",
								label: "Findings"
							}
						],
						empty: /* @__PURE__ */ (0, import_jsx_runtime$8.jsx)("p", {
							className: "form-note",
							children: "No visits recorded against this schedule yet."
						})
					})] })
				]
			})
		});
	}
	var import_react$7, import_jsx_runtime$8, defaultChecklist;
	var init_MaintenancePage = __esmMin((() => {
		import_react$7 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$8 = require_jsx_runtime();
		defaultChecklist = (title, templates = []) => {
			const key = String(title ?? "").toLowerCase().split(" ")[0];
			return templates.find((t) => t.interval === key)?.items ?? [];
		};
	}));
	//#endregion
	//#region src/pages/RiskPage.jsx
	var RiskPage_exports = /* @__PURE__ */ __exportAll({ RiskPage: () => RiskPage$1 });
	/**
	* Risk register (Phase 8).  Ranking + explanation, not a black box: the whole point is that a
	* department head can defend the decision to shorten a PM interval by pointing at the rows
	* behind the number.
	*/
	function RiskPage$1() {
		const [level, setLevel] = (0, import_react$6.useState)("");
		const [limit, setLimit] = (0, import_react$6.useState)(50);
		const [openRow, setOpenRow] = (0, import_react$6.useState)(null);
		const { data, loading, error, refresh } = useApi(() => dashboard.risk({
			level: level || void 0,
			limit
		}), [level, limit]);
		const model = useApi(() => dashboard.riskModel(), []);
		return /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)(import_jsx_runtime$7.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(PageHeader, {
				eyebrow: "Decision support",
				title: "Maintenance risk register",
				description: "A rule-based ranking of which equipment deserves attention next, computed from this department's own failure and maintenance history.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Button, {
					as: Link,
					to: "/maintenance",
					tone: "secondary",
					children: "Maintenance programme"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)(Callout, {
				tone: "safety",
				title: "What this is, and what it is not",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("p", { children: SAFETY_NOTICE }), /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("p", {
					style: { marginTop: 6 },
					children: [
						"It is ",
						/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("b", { children: "not" }),
						" a machine-learning model and does not predict failures. It scores ",
						model.data?.factors?.length ?? 7,
						" published factors (raw ceiling ",
						model.data?.maxRaw ?? 135,
						" points, normalised to 0–100) so that a number can always be traced to a record."
					]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("div", {
				className: "row",
				style: {
					margin: "var(--sp-4) 0",
					gap: "var(--sp-3)"
				},
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Select, {
						"aria-label": "Risk band",
						value: level,
						onChange: (e) => setLevel(e.target.value),
						style: { maxWidth: 200 },
						options: [
							{
								value: "",
								label: "All bands"
							},
							{
								value: "high",
								label: "High risk only"
							},
							{
								value: "moderate",
								label: "Moderate only"
							},
							{
								value: "low",
								label: "Low only"
							}
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Select, {
						"aria-label": "Number of rows",
						value: limit,
						onChange: (e) => setLimit(Number(e.target.value)),
						style: { maxWidth: 160 },
						options: [
							10,
							25,
							50,
							100
						].map((n) => ({
							value: n,
							label: `${n} items`
						}))
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("span", { className: "spacer" }),
					/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("div", {
						className: "row row--tight",
						children: Object.entries(data?.distribution ?? {}).map(([k, v]) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)(Badge, {
							tone: k === "high" ? "bad" : k === "moderate" ? "warn" : "ok",
							children: [
								v,
								" ",
								k
							]
						}, k))
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Card, {
				pad: false,
				children: error ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("div", {
					style: { padding: "var(--sp-4)" },
					children: /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(ErrorState, {
						error,
						onRetry: refresh
					})
				}) : /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(DataTable, {
					loading: loading && !data,
					rows: data?.items ?? [],
					onRowClick: setOpenRow,
					caption: "Maintenance risk ranking",
					columns: [
						{
							key: "equipmentName",
							label: "Equipment",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("div", {
								className: "cell-main",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("span", {
									className: "cell-title",
									children: r.equipmentName
								}), /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("span", {
									className: "cell-sub",
									children: [
										r.assetTag,
										" · ",
										r.criticality?.replace("_", " ")
									]
								})]
							})
						},
						{
							key: "score",
							label: "Risk",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(RiskBadge, {
								level: r.level,
								score: r.score
							})
						},
						{
							key: "faults",
							label: "Faults 12 mo",
							align: "center",
							render: (r) => r.inputSnapshot?.faultsIn12Months ?? 0
						},
						{
							key: "worst",
							label: "Worst severity",
							align: "center",
							render: (r) => r.inputSnapshot?.worstSeverityOnRecord ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Badge, {
								tone: r.inputSnapshot.worstSeverityOnRecord === "critical" ? "bad" : r.inputSnapshot.worstSeverityOnRecord === "high" ? "warn" : "neutral",
								children: r.inputSnapshot.worstSeverityOnRecord
							}) : /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("span", {
								className: "muted",
								children: "none"
							})
						},
						{
							key: "pm",
							label: "PM position",
							render: (r) => r.inputSnapshot?.nextMaintenanceOn ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("span", { children: [
								formatDate(r.inputSnapshot.nextMaintenanceOn),
								" ",
								new Date(r.inputSnapshot.nextMaintenanceOn) < /* @__PURE__ */ new Date() ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(MaintenanceLight, {
									state: "overdue",
									label: "overdue"
								}) : null
							] }) : /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(MaintenanceLight, {
								state: "not_scheduled",
								label: "no schedule"
							})
						},
						{
							key: "drivers",
							label: "Main drivers",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("span", {
								className: "cell-sub",
								children: r.factors.filter((f) => f.points > 0).sort((a, b) => b.points - a.points).slice(0, 2).map((f) => `${f.label} (${f.points})`).join(" · ") || "Nothing on record"
							})
						},
						{
							key: "go",
							label: "",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Button, {
								size: "sm",
								as: Link,
								to: `/equipment/${r.equipmentId}`,
								tone: "ghost",
								onClick: (e) => e.stopPropagation(),
								children: "Open"
							})
						}
					]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Modal, {
				open: !!openRow,
				onClose: () => setOpenRow(null),
				size: "lg",
				title: openRow ? `${openRow.equipmentName} — ${openRow.levelLabel}` : "",
				description: openRow ? `${openRow.assetTag} · score ${openRow.score}/100 (raw ${openRow.rawScore}/${openRow.rawMax}) · ${openRow.modelVersion}` : "",
				footer: /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)(import_jsx_runtime$7.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Button, {
					tone: "ghost",
					onClick: () => setOpenRow(null),
					children: "Close"
				}), openRow ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Button, {
					as: Link,
					to: `/equipment/${openRow.equipmentId}`,
					tone: "primary",
					children: "Open equipment record"
				}) : null] }),
				children: openRow ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("div", {
					className: "stack",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(Callout, {
							tone: "safety",
							children: openRow.disclaimer
						}),
						openRow.escalation ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)(Callout, {
							tone: "warn",
							title: "Band escalated",
							children: [
								openRow.escalation.rule,
								" — ",
								openRow.escalation.reason
							]
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("p", {
							className: "form-note",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("b", { children: "Advice:" }),
								" ",
								openRow.advice
							]
						}),
						openRow.factors.map((f) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("div", {
							className: "factor",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("div", {
									className: "factor__head",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("p", {
										className: "factor__label",
										children: f.label
									}), /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("p", {
										className: "factor__points",
										children: [
											f.points,
											" / ",
											f.maxPoints,
											" · ",
											f.weightPercent,
											"% of the scale"
										]
									})]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("p", {
									className: "factor__basis",
									children: f.basis
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("ul", {
									className: "factor__list",
									children: (f.contributing ?? []).map((c, i) => /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$7.jsx)("span", { children: c }) }, i))
								}),
								f.advice ? /* @__PURE__ */ (0, import_jsx_runtime$7.jsxs)("p", {
									className: "factor__advice",
									children: ["→ ", f.advice]
								}) : null
							]
						}, f.key)),
						/* @__PURE__ */ (0, import_jsx_runtime$7.jsx)(KeyValue, {
							columns: 3,
							items: Object.entries(openRow.inputSnapshot ?? {}).map(([k, v]) => [k.replace(/([A-Z])/g, " $1").toLowerCase(), v ?? "—"])
						})
					]
				}) : null
			})
		] });
	}
	var import_react$6, import_jsx_runtime$7;
	var init_RiskPage = __esmMin((() => {
		import_react$6 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AppShell();
		init_ui();
		init_display();
		init_useApi();
		init_constants();
		import_jsx_runtime$7 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/ReportsPage.jsx
	var ReportsPage_exports = /* @__PURE__ */ __exportAll({ ReportsPage: () => ReportsPage$1 });
	/**
	* Reports (Phase 10).  One query per report feeds the table, the CSV and the print view, so a
	* number in a file is always the number that was on the screen.  PDF comes from the browser's
	* print-to-PDF of the styled printable view (see docs/REPORTS.md for why).
	*/
	function ReportsPage$1() {
		const toast = useToast();
		const [key, setKey] = (0, import_react$5.useState)("");
		const [filters, setFilters] = (0, import_react$5.useState)({
			from: "",
			to: "",
			severity: "",
			status: "",
			technicianId: "",
			q: ""
		});
		const list = useApi(() => reports.list(), []);
		const picklists = useApi(() => reference.picklists(), []);
		const active = key || list.data?.items?.[0]?.key || "";
		const meta = (0, import_react$5.useMemo)(() => (list.data?.items ?? []).find((r) => r.key === active), [list.data, active]);
		const query = (0, import_react$5.useMemo)(() => Object.fromEntries(Object.entries(filters).filter(([, v]) => v)), [filters]);
		const report = useApi(() => active ? reports.run(active, query) : Promise.resolve(null), [active, JSON.stringify(query)]);
		const data = report.data;
		const showPrint = () => {
			const url = reports.printUrl(active, query);
			if (!window.open(url, "_blank", "noopener")) toast.warn("Your browser blocked the pop-up. Allow pop-ups for this site to print a report.");
		};
		if (list.loading) return /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Loading, {
			rows: 5,
			label: "Loading the report catalogue"
		});
		if (!list.data?.items?.length) return /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(EmptyState, {
			icon: "▤",
			title: "No reports available",
			description: "Your role is not permitted to generate departmental reports."
		});
		return /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)(import_jsx_runtime$6.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(PageHeader, {
				eyebrow: "Reporting",
				title: "Departmental reports",
				description: "Inventory, fault history, maintenance, costs, downtime, repeat failures and compliance — exportable, printable and attributable.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)(import_jsx_runtime$6.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Button, {
					tone: "secondary",
					onClick: showPrint,
					disabled: !data,
					children: "🖨 Print / save as PDF"
				}), /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Button, {
					tone: "primary",
					onClick: async () => {
						try {
							const res = await fetch(reports.csvUrl(active, query), { credentials: "same-origin" });
							if (!res.ok) throw new Error(`Export failed (${res.status})`);
							const blob = await res.blob();
							const a = document.createElement("a");
							a.href = URL.createObjectURL(blob);
							a.download = `${active}-${(/* @__PURE__ */ new Date()).toISOString().slice(0, 10)}.csv`;
							document.body.appendChild(a);
							a.click();
							a.remove();
							setTimeout(() => URL.revokeObjectURL(a.href), 4e3);
							toast.success("CSV downloaded.");
						} catch (err) {
							toast.error(errorText(err));
						}
					},
					disabled: !data,
					children: "⬇ Download CSV"
				})] })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)("div", {
				className: "row",
				style: {
					alignItems: "stretch",
					gap: "var(--sp-3)",
					marginBottom: "var(--sp-4)"
				},
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						style: {
							flex: "1 1 240px",
							minWidth: 210
						},
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Field, {
							label: "Report",
							children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Select, {
								value: active,
								onChange: (e) => setKey(e.target.value),
								options: (list.data.items ?? []).map((r) => ({
									value: r.key,
									label: r.title
								}))
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						style: { flex: "1 1 150px" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Field, {
							label: "From",
							children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(TextInput, {
								type: "date",
								value: filters.from,
								onChange: (e) => setFilters({
									...filters,
									from: e.target.value
								})
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						style: { flex: "1 1 150px" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Field, {
							label: "To",
							children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(TextInput, {
								type: "date",
								value: filters.to,
								onChange: (e) => setFilters({
									...filters,
									to: e.target.value
								})
							})
						})
					}),
					[
						"faults",
						"costs",
						"failures"
					].includes(active) ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						style: { flex: "1 1 150px" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Field, {
							label: "Severity",
							children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Select, {
								value: filters.severity,
								onChange: (e) => setFilters({
									...filters,
									severity: e.target.value
								}),
								placeholder: "Any",
								options: [
									"low",
									"medium",
									"high",
									"critical"
								].map((v) => ({
									value: v,
									label: v[0].toUpperCase() + v.slice(1)
								}))
							})
						})
					}) : null,
					[
						"faults",
						"costs",
						"maintenance",
						"failures"
					].includes(active) ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						style: { flex: "1 1 170px" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Field, {
							label: "Technician",
							children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Select, {
								value: filters.technicianId,
								onChange: (e) => setFilters({
									...filters,
									technicianId: e.target.value
								}),
								placeholder: "Any",
								options: (picklists.data?.technicians ?? []).map((t) => ({
									value: t.id,
									label: t.fullName
								}))
							})
						})
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						className: "row row--end",
						style: { alignItems: "flex-end" },
						children: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Button, {
							tone: "ghost",
							onClick: () => setFilters({
								from: "",
								to: "",
								severity: "",
								status: "",
								technicianId: "",
								q: ""
							}),
							children: "Reset"
						})
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Callout, {
				tone: "info",
				title: meta?.title,
				children: meta?.description
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
				style: { marginTop: "var(--sp-4)" },
				children: report.error ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(ErrorState, {
					error: report.error,
					onRetry: report.refresh
				}) : /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)(Card, {
					pad: false,
					title: `${meta?.title ?? "Report"} · ${data?.rowCount ?? 0} row${data?.rowCount === 1 ? "" : "s"}`,
					subtitle: data ? `Generated ${formatDateTime(data.generatedAt)} · ${Object.entries(data.filters ?? {}).filter(([, v]) => v).map(([k, v]) => `${k}=${v}`).join(" · ") || "no filters"}` : "",
					footer: data && Object.keys(data.totals ?? {}).length ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("div", {
						className: "row",
						style: {
							gap: "var(--sp-5)",
							flexWrap: "wrap"
						},
						children: Object.entries(data.totals).map(([k, v]) => /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)("span", {
							className: "report-totals",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("b", { children: typeof v === "number" ? k.includes("ost") || k.includes("otal") || k.includes("ost") ? formatMoney(v) : v.toLocaleString() : String(v) }),
								" ",
								k.replace(/([A-Z])/g, " $1").toLowerCase()
							]
						}, k))
					}) : null,
					children: [report.loading && !data ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(Loading, {
						rows: 6,
						label: "Running the report"
					}) : /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(DataTable, {
						dense: true,
						rows: (data?.rows ?? []).slice(0, 400),
						columns: (data?.columns ?? []).map((c) => ({
							key: c.key,
							label: c.label,
							render: (r) => {
								const v = r[c.key];
								if (v === null || v === void 0 || v === "") return /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("span", {
									className: "muted",
									children: "—"
								});
								if (typeof v === "number" && /(cost|total|price|percent)/i.test(c.key)) return /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)("span", {
									className: "mono",
									children: c.key.includes("percent") ? `${v}%` : formatMoney(v)
								});
								if (typeof v === "boolean") return v ? "yes" : "no";
								return String(v).length > 120 ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)("span", {
									title: String(v),
									children: [String(v).slice(0, 118), "…"]
								}) : String(v);
							}
						})),
						empty: /* @__PURE__ */ (0, import_jsx_runtime$6.jsx)(EmptyState, {
							icon: "▤",
							title: "This report is empty",
							description: "No rows match the filters — widen the date range or clear a filter."
						})
					}), data && data.rows.length > 400 ? /* @__PURE__ */ (0, import_jsx_runtime$6.jsxs)("p", {
						className: "form-note",
						style: { padding: "0 var(--sp-4) var(--sp-4)" },
						children: [
							"Showing the first 400 of ",
							data.rows.length,
							" rows; the CSV contains all of them."
						]
					}) : null]
				})
			})
		] });
	}
	var import_react$5, import_jsx_runtime$6;
	var init_ReportsPage = __esmMin((() => {
		import_react$5 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_client();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$6 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/UsersPage.jsx
	var UsersPage_exports = /* @__PURE__ */ __exportAll({ UsersPage: () => UsersPage$1 });
	/**
	* User administration.  Accounts are never deleted when they carry official history — they are
	* deactivated, because "who reported / repaired / verified this" must stay answerable.
	*/
	function UsersPage$1() {
		const [q, setQ] = (0, import_react$4.useState)("");
		const [status, setStatus] = (0, import_react$4.useState)("active");
		const [editing, setEditing] = (0, import_react$4.useState)(null);
		const [created, setCreated] = (0, import_react$4.useState)(null);
		const [reset, setReset] = (0, import_react$4.useState)(null);
		const [confirmRemove, setConfirmRemove] = (0, import_react$4.useState)(null);
		const list = useApi(() => users.list({
			q: q || void 0,
			status
		}), [q, status]);
		return /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)(import_jsx_runtime$5.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(PageHeader, {
				eyebrow: "Administration",
				title: "Users and roles",
				description: "Roles decide what a person may do. The reporter role is deliberately limited: anyone can raise a fault, only a technician can diagnose one.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
					tone: "primary",
					onClick: () => setEditing({
						roleCode: "reporter",
						mustChangePassword: true
					}),
					children: "＋ Add user"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
				className: "row",
				style: { marginBottom: "var(--sp-4)" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("div", {
					style: {
						flex: "1 1 320px",
						maxWidth: 460
					},
					children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(SearchInput, {
						value: q,
						onSearch: setQ,
						placeholder: "Search name, email or ID…"
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Select, {
					value: status,
					onChange: (e) => setStatus(e.target.value),
					"aria-label": "Account status",
					style: { maxWidth: 180 },
					options: [
						{
							value: "active",
							label: "Active"
						},
						{
							value: "disabled",
							label: "Deactivated"
						},
						{
							value: "all",
							label: "All accounts"
						}
					]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Card, {
				title: `${list.data?.items?.length ?? 0} account(s)`,
				pad: false,
				children: list.error ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("div", {
					style: { padding: "var(--sp-4)" },
					children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(ErrorState, {
						error: list.error,
						onRetry: list.refresh
					})
				}) : /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(DataTable, {
					loading: list.loading && !list.data,
					rows: list.data?.items ?? [],
					empty: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(EmptyState, {
						title: "No accounts match",
						description: "Try another search term, or include deactivated accounts."
					}),
					columns: [
						{
							key: "fullName",
							label: "Person",
							render: (u) => /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
								className: "cell-main",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("span", {
									className: "cell-title",
									children: [
										u.fullName,
										" ",
										!u.isActive && /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Badge, {
											tone: "neutral",
											size: "sm",
											children: "disabled"
										})
									]
								}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("span", {
									className: "cell-sub",
									children: [u.email, u.employeeId ? ` · ${u.employeeId}` : ""]
								})]
							})
						},
						{
							key: "roleLabel",
							label: "Role",
							render: (u) => /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Badge, {
								tone: u.roleCode === "admin" ? "bad" : u.roleCode === "technician" ? "info" : "neutral",
								children: u.roleLabel
							})
						},
						{
							key: "jobTitle",
							label: "Job title / department",
							render: (u) => [u.jobTitle, u.department].filter(Boolean).join(" · ") || "—"
						},
						{
							key: "lastLoginAt",
							label: "Last sign-in",
							render: (u) => u.lastLoginAt ? formatDateTime(u.lastLoginAt) : /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
								className: "muted",
								children: "never"
							})
						},
						{
							key: "mustChangePassword",
							label: "",
							align: "center",
							render: (u) => u.mustChangePassword ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Badge, {
								tone: "warn",
								size: "sm",
								children: "must change password"
							}) : null
						},
						{
							key: "actions",
							label: "",
							render: (u) => /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
								className: "row row--tight",
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
										size: "sm",
										tone: "ghost",
										onClick: () => setEditing(u),
										children: "Edit"
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
										size: "sm",
										tone: "ghost",
										onClick: () => setReset(u),
										children: "Password"
									}),
									/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
										size: "sm",
										tone: "ghost",
										onClick: () => setConfirmRemove(u),
										children: u.isActive ? "Deactivate" : "Activate"
									})
								]
							})
						}
					]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Callout, {
				tone: "info",
				title: "Role definitions",
				children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("div", {
					className: "grid grid--3",
					style: { marginTop: 6 },
					children: [
						["Administrator", "Inventory, users, assignment, schedules, configuration, all records and reports."],
						["Technician / biomedical engineer", "Owns the technical record: diagnosis, work performed, parts, costs, safety sign-off, PM."],
						["Student / staff reporter", "Views equipment, reports faults with evidence, tracks their own reports. Cannot alter any official record."]
					].map(([role, text]) => /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("p", {
						className: "cell-title",
						children: role
					}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("p", {
						className: "cell-sub",
						children: text
					})] }, role))
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(UserFormModal, {
				state: editing,
				onClose: () => setEditing(null),
				onCreated: setCreated,
				onSaved: () => {
					setEditing(null);
					list.refresh();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(PasswordModal, {
				state: reset,
				onClose: () => setReset(null)
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(ConfirmDialog, {
				open: !!confirmRemove,
				tone: confirmRemove?.isActive ? "bad" : "primary",
				title: confirmRemove?.isActive ? `Deactivate ${confirmRemove.fullName}?` : `Reactivate ${confirmRemove?.fullName}?`,
				body: confirmRemove?.isActive ? "They are signed out immediately and cannot sign in again. Their fault reports, assignments and repair records remain on file." : "They will be able to sign in again with their existing password.",
				confirmLabel: confirmRemove?.isActive ? "Deactivate account" : "Reactivate account",
				onCancel: () => setConfirmRemove(null),
				onConfirm: async () => {
					try {
						await users.update(confirmRemove.id, { isActive: !confirmRemove.isActive });
						setConfirmRemove(null);
						list.refresh();
					} catch (err) {
						toastError(err);
					}
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Modal, {
				open: !!created,
				onClose: () => setCreated(null),
				title: "Account created",
				description: "Share this password once, over a channel you control. It is not stored anywhere in readable form and cannot be shown again.",
				footer: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
					tone: "primary",
					onClick: () => {
						navigator.clipboard?.writeText(created.temporaryPassword ?? "");
						setCreated(null);
					},
					children: "Copy and close"
				}),
				children: created?.temporaryPassword ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
					className: "stack",
					children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("p", {
						className: "created-password mono",
						children: created.temporaryPassword
					}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("p", {
						className: "form-note",
						children: [created.user.fullName, " will be required to choose their own password at first sign-in."]
					})]
				}) : /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("p", {
					className: "form-note",
					children: "The account is ready; you set the password yourself, so nothing is displayed here."
				})
			})
		] });
	}
	function toastError(err) {
		toastHolder?.error(errorText(err));
	}
	function UserFormModal({ state, onClose, onSaved, onCreated }) {
		const toast = useToast();
		(0, import_react$4.useEffect)(() => {
			toastHolder = toast;
		}, [toast]);
		const [form, setForm] = (0, import_react$4.useState)({});
		const [errors, setErrors] = (0, import_react$4.useState)({});
		const [busy, setBusy] = (0, import_react$4.useState)(false);
		const isEdit = Boolean(state?.id);
		(0, import_react$4.useEffect)(() => {
			if (!state) return;
			setForm({
				fullName: state.fullName ?? "",
				email: state.email ?? "",
				roleCode: state.roleCode ?? "reporter",
				employeeId: state.employeeId ?? "",
				jobTitle: state.jobTitle ?? "",
				department: state.department ?? "Biomedical Engineering",
				phone: state.phone ?? "",
				password: "",
				mustChangePassword: state.mustChangePassword ?? true
			});
			setErrors({});
		}, [state]);
		const submit = async () => {
			setBusy(true);
			setErrors({});
			const payload = Object.fromEntries(Object.entries(form).filter(([, v]) => v !== "" && v !== null && !(v === false && isEdit)));
			try {
				if (isEdit) {
					await users.update(state.id, payload);
					onSaved();
				} else {
					onCreated(await users.create(payload));
					onClose();
				}
				toast.success(isEdit ? "Account updated." : "Account created.");
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		if (!state) return null;
		return /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)(Modal, {
			open: true,
			onClose,
			title: isEdit ? `Edit ${state.fullName}` : "Add a user",
			description: "Email is the sign-in name. Leave the password blank to generate one that must be changed at first sign-in.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)(import_jsx_runtime$5.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: isEdit ? "Save account" : "Create account"
			})] }),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
				className: "form-grid form-grid--2",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Full name",
						required: true,
						error: errors.fullName,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							value: form.fullName,
							onChange: (e) => setForm({
								...form,
								fullName: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Email",
						required: true,
						error: errors.email,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							type: "email",
							autoComplete: "off",
							value: form.email,
							onChange: (e) => setForm({
								...form,
								email: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Role",
						required: true,
						error: errors.roleCode,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Select, {
							value: form.roleCode,
							onChange: (e) => setForm({
								...form,
								roleCode: e.target.value
							}),
							options: [
								{
									value: "admin",
									label: "Administrator"
								},
								{
									value: "technician",
									label: "Technician / biomedical engineer"
								},
								{
									value: "reporter",
									label: "Student / staff reporter"
								}
							]
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Staff / student ID",
						error: errors.employeeId,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							value: form.employeeId,
							onChange: (e) => setForm({
								...form,
								employeeId: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Job title",
						error: errors.jobTitle,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							value: form.jobTitle,
							onChange: (e) => setForm({
								...form,
								jobTitle: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Department",
						error: errors.department,
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							value: form.department,
							onChange: (e) => setForm({
								...form,
								department: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Phone",
						error: errors.phone,
						hint: "Used only if SMS notifications are enabled later.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							value: form.phone,
							onChange: (e) => setForm({
								...form,
								phone: e.target.value
							})
						})
					}),
					!isEdit ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "Initial password",
						error: errors.password,
						hint: "At least 12 characters, three character families. Blank = generated.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							type: "password",
							autoComplete: "new-password",
							value: form.password,
							onChange: (e) => setForm({
								...form,
								password: e.target.value
							})
						})
					}) : null
				]
			}), !isEdit ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("label", {
				className: "check",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("input", {
						type: "checkbox",
						checked: form.mustChangePassword,
						onChange: (e) => setForm({
							...form,
							mustChangePassword: e.target.checked
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
						className: "check__box",
						"aria-hidden": "true"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
						className: "check__text",
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
							className: "check__label",
							children: "Require a password change at first sign-in"
						})
					})
				]
			}) : null]
		});
	}
	function PasswordModal({ state, onClose }) {
		const toast = useToast();
		const [form, setForm] = (0, import_react$4.useState)({
			newPassword: "",
			mustChange: true,
			revokeSessions: true
		});
		const [result, setResult] = (0, import_react$4.useState)(null);
		const [busy, setBusy] = (0, import_react$4.useState)(false);
		const [errors, setErrors] = (0, import_react$4.useState)({});
		(0, import_react$4.useEffect)(() => {
			if (state) {
				setForm({
					newPassword: "",
					mustChange: true,
					revokeSessions: true
				});
				setResult(null);
				setErrors({});
			}
		}, [state]);
		if (!state) return null;
		const submit = async () => {
			setBusy(true);
			setErrors({});
			try {
				const res = await users.resetPassword(state.id, {
					newPassword: form.newPassword || void 0,
					mustChange: form.mustChange,
					revokeSessions: form.revokeSessions
				});
				setResult(res);
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Modal, {
			open: true,
			onClose,
			title: `Reset password for ${state.fullName}`,
			description: "A reset signs the person out of every device, which is what you want when a laptop is lost or someone leaves.",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(import_jsx_runtime$5.Fragment, { children: !result ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)(import_jsx_runtime$5.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: "Reset password"
			})] }) : /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Button, {
				tone: "primary",
				onClick: onClose,
				children: "Done"
			}) }),
			children: result ? /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
				className: "stack",
				children: [/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("p", {
					className: "created-password mono",
					children: result.temporaryPassword
				}), /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("p", {
					className: "form-note",
					children: [
						"Give this to ",
						state.email,
						" now — it is not retrievable afterwards. ",
						result.sessionsRevoked ? ` ${result.sessionsRevoked} session(s) were revoked.` : ""
					]
				})]
			}) : /* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("div", {
				className: "stack",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(Field, {
						label: "New password",
						error: errors.password,
						hint: "Leave blank to have a strong one generated.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$5.jsx)(TextInput, {
							type: "password",
							autoComplete: "new-password",
							value: form.newPassword,
							onChange: (e) => setForm({
								...form,
								newPassword: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("label", {
						className: "check",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("input", {
								type: "checkbox",
								checked: form.mustChange,
								onChange: (e) => setForm({
									...form,
									mustChange: e.target.checked
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
								className: "check__box",
								"aria-hidden": "true"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
								className: "check__label",
								children: "Require a change at next sign-in"
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$5.jsxs)("label", {
						className: "check",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("input", {
								type: "checkbox",
								checked: form.revokeSessions,
								onChange: (e) => setForm({
									...form,
									revokeSessions: e.target.checked
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
								className: "check__box",
								"aria-hidden": "true"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$5.jsx)("span", {
								className: "check__label",
								children: "Sign them out of all devices"
							})
						]
					})
				]
			})
		});
	}
	var import_react$4, import_jsx_runtime$5, toastHolder;
	var init_UsersPage = __esmMin((() => {
		import_react$4 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_client();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$5 = require_jsx_runtime();
		toastHolder = null;
	}));
	//#endregion
	//#region src/pages/ReferencePage.jsx
	var ReferencePage_exports = /* @__PURE__ */ __exportAll({ ReferencePage: () => ReferencePage$1 });
	/**
	* Configuration (categories, locations, fault types, parts, settings) plus the QR label sheet.
	* Reference rows are never hard-deleteable while in use — the picker offers deactivation, which
	* keeps old records meaningful.
	*/
	function ReferencePage$1() {
		const [params, setParams] = useSearchParams();
		const tab = params.get("tab") ?? "categories";
		const { can } = useAuth();
		const set = (patch) => {
			const next = new URLSearchParams(params);
			for (const [k, v] of Object.entries(patch)) if (!v) next.delete(k);
			else next.set(k, String(v));
			setParams(next, { replace: true });
		};
		const readonly = !can("meta.manage");
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(PageHeader, {
				eyebrow: "Configuration",
				title: "Reference data & settings",
				description: readonly ? "You can view these lists; only an administrator can change them." : "The lists every other screen is built from. Keep them short and boring — a picker with 60 rooms is a picker nobody uses."
			}),
			readonly ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Callout, {
				tone: "info",
				children: "Read-only view. Ask the department administrator to change configuration."
			}) : null,
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Tabs, {
				active: tab,
				onChange: (id) => set({ tab: id }),
				tabs: [
					{
						id: "categories",
						label: "Equipment categories"
					},
					{
						id: "locations",
						label: "Locations"
					},
					{
						id: "fault-categories",
						label: "Fault categories"
					},
					{
						id: "parts",
						label: "Parts catalogue"
					},
					{
						id: "labels",
						label: "QR labels"
					},
					{
						id: "settings",
						label: "Settings"
					}
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
				className: "tabpanel",
				children: [
					tab === "categories" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(RefTable, {
						kind: "categories",
						route: "category",
						title: "Equipment categories",
						columns: [
							["code", "Code"],
							["name", "Name"],
							["description", "Description"]
						],
						hint: "The code becomes the asset-tag prefix (BMU-<code>-0001), so choose it once and keep it.",
						readonly
					}) : null,
					tab === "locations" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(RefTable, {
						kind: "locations",
						route: "location",
						title: "Locations",
						columns: [
							["code", "Code"],
							["name", "Name"],
							["building", "Building"],
							["floor", "Floor"],
							["room", "Room"]
						],
						hint: "Rooms, not people. Equipment moves; a room does not.",
						readonly
					}) : null,
					tab === "fault-categories" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(RefTable, {
						kind: "fault-categories",
						route: "faultCategory",
						title: "Fault categories",
						columns: [
							["code", "Code"],
							["name", "Name"],
							["description", "Description"],
							["defaultSeverity", "Default severity"]
						],
						hint: "These are what a reporter picks from. Keep the wording symptom-shaped, not cause-shaped.",
						readonly
					}) : null,
					tab === "parts" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(PartsPanel, { readonly }) : null,
					tab === "labels" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(LabelSheet, {}) : null,
					tab === "settings" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(SettingsPanel, { readonly }) : null
				]
			})
		] });
	}
	function RefTable({ kind, title, columns, hint, readonly }) {
		const toast = useToast();
		const res = useApi(() => reference[kind === "categories" ? "categories" : kind === "locations" ? "locations" : "faultCategories"](), [kind]);
		const [editing, setEditing] = (0, import_react$3.useState)(null);
		const [removing, setRemoving] = (0, import_react$3.useState)(null);
		const rows = res.data?.items ?? [];
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Card, {
				pad: false,
				title,
				subtitle: hint,
				actions: !readonly ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
					size: "sm",
					tone: "primary",
					onClick: () => setEditing({}),
					children: "＋ Add"
				}) : null,
				children: res.loading && !res.data ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(DataTable, {
					rows,
					empty: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(EmptyState, {
						title: "Nothing configured",
						description: "Add the first entry so equipment can be classified."
					}),
					columns: [
						...columns.map(([key, label]) => ({
							key,
							label,
							render: (r) => key.endsWith("Severity") || key === "code" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("code", { children: r[key] }) : r[key] ?? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
								className: "muted",
								children: "—"
							})
						})),
						{
							key: "usageCount",
							label: "In use",
							align: "center",
							render: (r) => r.usageCount ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Badge, {
								tone: r.usageCount > 0 ? "neutral" : "neutral",
								size: "sm",
								children: r.usageCount
							}) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
								className: "muted",
								children: "0"
							})
						},
						{
							key: "isActive",
							label: "",
							render: (r) => !r.isActive ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Badge, {
								tone: "neutral",
								size: "sm",
								children: "inactive"
							}) : null
						},
						...!readonly ? [{
							key: "actions",
							label: "",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
								className: "row row--tight",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
									size: "sm",
									tone: "ghost",
									onClick: () => setEditing(r),
									children: "Edit"
								}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
									size: "sm",
									tone: "ghost",
									onClick: () => setRemoving(r),
									children: "Delete"
								})]
							})
						}] : []
					]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(RefFormModal, {
				state: editing,
				kind,
				onClose: () => setEditing(null),
				onSaved: () => {
					setEditing(null);
					res.refresh();
					toast.success("Saved.");
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(ConfirmDialog, {
				open: !!removing,
				title: `Delete “${removing?.name}”?`,
				body: removing?.usageCount ? `${removing.usageCount} record(s) use this entry, so it cannot be deleted — deactivate it instead.` : "Nothing references this entry, so deleting it is safe.",
				confirmLabel: removing?.usageCount ? "Deactivate instead" : "Delete",
				onCancel: () => setRemoving(null),
				onConfirm: async () => {
					try {
						if (removing.usageCount) await reference.update(kind, removing.id, { isActive: false });
						else await reference.remove(kind, removing.id);
						setRemoving(null);
						res.refresh();
						toast.success(removing.usageCount ? "Deactivated — new records will not offer it." : "Deleted.");
					} catch (err) {
						toast.error(errorText(err));
					}
				}
			})
		] });
	}
	function RefFormModal({ state, kind, onClose, onSaved }) {
		const toast = useToast();
		const [form, setForm] = (0, import_react$3.useState)({});
		const [errors, setErrors] = (0, import_react$3.useState)({});
		const [busy, setBusy] = (0, import_react$3.useState)(false);
		(0, import_react$3.useEffect)(() => {
			if (state) {
				setForm({ ...state });
				setErrors({});
			}
		}, [state]);
		if (!state) return null;
		const isEdit = Boolean(state.id);
		const submit = async () => {
			setBusy(true);
			try {
				const payload = {
					code: form.code,
					name: form.name,
					description: form.description,
					building: form.building,
					floor: form.floor,
					room: form.room,
					defaultSeverity: form.defaultSeverity,
					isActive: form.isActive !== false
				};
				if (isEdit) await reference.update(kind, state.id, payload);
				else await reference.create(kind, payload);
				onSaved();
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const isLocation = kind === "locations";
		const isFault = kind === "fault-categories";
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Modal, {
			open: true,
			onClose,
			title: `${isEdit ? "Edit" : "Add"} ${kind.replace("fault-categories", "fault category").replace("categories", "category").replace("locations", "location")}`,
			footer: /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
				tone: "ghost",
				onClick: onClose,
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
				tone: "primary",
				loading: busy,
				onClick: submit,
				children: "Save"
			})] }),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
				className: "form-grid form-grid--2",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Code",
						required: true,
						error: errors.code,
						hint: isEdit ? "Changing a code does not rewrite existing asset tags." : "1–12 characters, A–Z 0–9.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: form.code ?? "",
							onChange: (e) => setForm({
								...form,
								code: e.target.value.toUpperCase()
							}),
							className: "mono"
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Name",
						required: true,
						error: errors.name,
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: form.name ?? "",
							onChange: (e) => setForm({
								...form,
								name: e.target.value
							})
						})
					}),
					isLocation ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [
						/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
							label: "Building",
							error: errors.building,
							children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
								value: form.building ?? "",
								onChange: (e) => setForm({
									...form,
									building: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
							label: "Floor",
							error: errors.floor,
							children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
								value: form.floor ?? "",
								onChange: (e) => setForm({
									...form,
									floor: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
							label: "Room",
							error: errors.room,
							children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
								value: form.room ?? "",
								onChange: (e) => setForm({
									...form,
									room: e.target.value
								})
							})
						})
					] }) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Description",
						error: errors.description,
						hint: "Shown as help text on the reporting form.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: form.description ?? "",
							onChange: (e) => setForm({
								...form,
								description: e.target.value
							})
						})
					}),
					isFault ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Default severity",
						error: errors.defaultSeverity,
						hint: "Used only if a report arrives without an explicit severity.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Select, {
							value: form.defaultSeverity ?? "medium",
							onChange: (e) => setForm({
								...form,
								defaultSeverity: e.target.value
							}),
							options: [
								"low",
								"medium",
								"high",
								"critical"
							].map((v) => ({
								value: v,
								label: v[0].toUpperCase() + v.slice(1)
							}))
						})
					}) : null
				]
			}), isEdit ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Checkbox, {
				label: "Active (available in pickers)",
				checked: form.isActive !== false,
				onChange: (v) => setForm({
					...form,
					isActive: v
				})
			}) : null]
		});
	}
	function PartsPanel() {
		const toast = useToast();
		const [q, setQ] = (0, import_react$3.useState)("");
		const res = useApi(() => parts.list({ q: q || void 0 }), [q]);
		const [editing, setEditing] = (0, import_react$3.useState)(null);
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Card, {
			pad: false,
			title: "Spare parts catalogue",
			subtitle: "Reference for repair lines, with typical unit costs. Stock counts are informational — this system does not run a stores module.",
			actions: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
				size: "sm",
				tone: "primary",
				onClick: () => setEditing({
					unit: "pcs",
					unitCost: 0,
					inStock: 0,
					isActive: true
				}),
				children: "＋ Add part"
			}),
			children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("div", {
				style: { padding: "var(--sp-3) var(--sp-4) 0" },
				children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
					placeholder: "Filter by code, name or category…",
					value: q,
					onChange: (e) => setQ(e.target.value),
					"aria-label": "Search parts"
				})
			}), res.loading && !res.data ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Loading, { rows: 4 }) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(DataTable, {
				dense: true,
				rows: res.data?.items ?? [],
				empty: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(EmptyState, {
					title: "No parts listed",
					description: "Add the consumables and common spares your technicians fit most often."
				}),
				columns: [
					{
						key: "code",
						label: "Code",
						mono: true
					},
					{
						key: "name",
						label: "Part"
					},
					{
						key: "category",
						label: "Category"
					},
					{
						key: "unit",
						label: "Unit",
						align: "center"
					},
					{
						key: "unitCost",
						label: "Unit cost",
						align: "right",
						render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
							className: "mono",
							children: Number(r.unitCost).toFixed(2)
						})
					},
					{
						key: "inStock",
						label: "On hand",
						align: "center",
						render: (r) => r.inStock === 0 ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Badge, {
							tone: "warn",
							size: "sm",
							children: "none"
						}) : r.inStock
					},
					{
						key: "actions",
						label: "",
						render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
							size: "sm",
							tone: "ghost",
							onClick: () => setEditing(r),
							children: "Edit"
						})
					}
				]
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Modal, {
			open: !!editing,
			onClose: () => setEditing(null),
			title: editing?.id ? "Edit part" : "Add part",
			footer: /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
				tone: "ghost",
				onClick: () => setEditing(null),
				children: "Cancel"
			}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
				tone: "primary",
				onClick: async () => {
					try {
						await parts.upsert(editing);
						setEditing(null);
						res.refresh();
						toast.success("Saved.");
					} catch (err) {
						toast.error(errorText(err));
					}
				},
				children: "Save"
			})] }),
			children: editing ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
				className: "form-grid form-grid--2",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Code",
						required: true,
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							className: "mono",
							value: editing.code ?? "",
							onChange: (e) => setEditing({
								...editing,
								code: e.target.value.toUpperCase()
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Name",
						required: true,
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: editing.name ?? "",
							onChange: (e) => setEditing({
								...editing,
								name: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Category",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: editing.category ?? "",
							onChange: (e) => setEditing({
								...editing,
								category: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Unit",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							value: editing.unit ?? "pcs",
							onChange: (e) => setEditing({
								...editing,
								unit: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "Unit cost",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							type: "number",
							min: "0",
							step: "0.01",
							value: editing.unitCost ?? 0,
							onChange: (e) => setEditing({
								...editing,
								unitCost: e.target.value
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: "On hand",
						hint: "Not decremented by repairs; update it when the shelf is checked.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							type: "number",
							min: "0",
							value: editing.inStock ?? 0,
							onChange: (e) => setEditing({
								...editing,
								inStock: e.target.value
							})
						})
					})
				]
			}) : null
		})] });
	}
	function LabelSheet() {
		const toast = useToast();
		const [ids, setIds] = (0, import_react$3.useState)([]);
		const [rows, setRows] = (0, import_react$3.useState)([]);
		const [busy, setBusy] = (0, import_react$3.useState)(false);
		const [search, setSearch] = (0, import_react$3.useState)("");
		const options = useApi(() => Promise.resolve().then(() => (init_client(), client_exports)).then((m) => m.equipment.list({
			q: search || void 0,
			perPage: 30
		})), [search]);
		const generate = async () => {
			if (!ids.length) {
				toast.warn("Pick at least one item.");
				return;
			}
			setBusy(true);
			try {
				const { equipment } = await Promise.resolve().then(() => (init_client(), client_exports));
				const res = await equipment.labels(ids);
				setRows(res.items);
				toast.success(`${res.items.length} label(s) ready.`);
			} catch (err) {
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Card, {
			title: "QR label sheet",
			subtitle: "Pick equipment, generate, then print on label stock at 100% scale. Each label opens that item’s profile and fault-report form.",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
				className: "row",
				style: { marginBottom: "var(--sp-3)" },
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
						style: { flex: "1 1 260px" },
						placeholder: "Search equipment…",
						value: search,
						onChange: (e) => setSearch(e.target.value),
						"aria-label": "Search equipment for labels"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Button, {
						tone: "primary",
						loading: busy,
						onClick: generate,
						children: ["Generate ", ids.length ? `${ids.length} label(s)` : ""]
					}),
					rows.length ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
						tone: "ghost",
						onClick: () => window.print(),
						children: "🖨 Print sheet"
					}) : null
				]
			}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("ul", {
				className: "chipcloud",
				children: (options.data?.items ?? []).map((e) => /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("button", {
					type: "button",
					className: `chip${ids.includes(e.id) ? " chip--active" : ""}`,
					onClick: () => setIds((list) => list.includes(e.id) ? list.filter((x) => x !== e.id) : [...list, e.id]),
					children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
						className: "chip__label",
						children: e.name
					}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
						className: "chip__hint",
						children: e.assetTag
					})]
				}) }, e.id))
			})]
		}), rows.length ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("div", {
			className: "label-sheet",
			children: rows.map((l) => /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("div", {
				className: "label",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("img", {
						src: l.png,
						alt: `QR code for ${l.assetTag}`,
						width: "120",
						height: "120"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("p", {
						className: "label__tag",
						children: l.assetTag
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("p", {
						className: "label__name",
						children: l.name
					}),
					/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("p", {
						className: "label__foot",
						children: "Report a fault: scan this code"
					})
				]
			}, l.id))
		}) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Callout, {
			tone: "info",
			children: "Tip: print labels for new equipment as it is commissioned, not later — a device without a tag gets fewer, vaguer reports."
		})] });
	}
	function SettingsPanel({ readonly }) {
		const toast = useToast();
		const res = useApi(() => reference.settings(), []);
		const [form, setForm] = (0, import_react$3.useState)({});
		(0, import_react$3.useEffect)(() => {
			if (!res.data) return;
			const out = {};
			for (const [k, v] of Object.entries(res.data.settings)) out[k] = v.type === "json" ? JSON.stringify(v.value, null, 0) : v.type === "bool" ? v.value : v.value ?? "";
			setForm(out);
		}, [res.data]);
		if (res.loading && !res.data) return /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Loading, { rows: 5 });
		const save = async () => {
			const patch = {};
			for (const [k, def] of Object.entries(res.data.definitions)) {
				const current = res.data.settings[k].value;
				let next = form[k];
				if (def.type === "bool") next = !!next;
				else if (def.type === "int") next = Number(next);
				else if (def.type === "json") try {
					next = JSON.parse(next);
				} catch {
					toast.error(`${def.label} must be valid JSON, e.g. {"critical":4}`);
					return;
				}
				if (JSON.stringify(next) !== JSON.stringify(current)) patch[k] = next;
			}
			if (!Object.keys(patch).length) {
				toast.info("Nothing changed.");
				return;
			}
			try {
				await reference.saveSettings(patch);
				res.refresh();
				toast.success("Settings saved.");
			} catch (err) {
				toast.error(errorText(err));
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(import_jsx_runtime$4.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Card, {
			title: "Department settings",
			subtitle: "Everything here is configuration, not code: names on reports, currency, response targets and reminder windows.",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("div", {
				className: "form-grid form-grid--2",
				children: Object.entries(res.data.definitions).map(([key, def]) => {
					const value = form[key];
					if (def.type === "bool") return /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("div", {
						className: "check",
						children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("label", {
							className: "check",
							style: {
								border: 0,
								padding: 0
							},
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("input", {
									type: "checkbox",
									disabled: readonly,
									checked: !!value,
									onChange: (e) => setForm({
										...form,
										[key]: e.target.checked
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
									className: "check__box",
									"aria-hidden": "true"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)("span", {
									className: "check__text",
									children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
										className: "check__label",
										children: def.label
									}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("span", {
										className: "check__hint",
										children: def.description
									})]
								})
							]
						})
					}, key);
					return /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Field, {
						label: def.label,
						hint: def.description,
						children: def.type === "json" ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextArea, {
							rows: 2,
							disabled: readonly,
							value: value ?? "",
							onChange: (e) => setForm({
								...form,
								[key]: e.target.value
							})
						}) : /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(TextInput, {
							disabled: readonly,
							type: def.type === "int" ? "number" : "text",
							value: value ?? "",
							onChange: (e) => setForm({
								...form,
								[key]: e.target.value
							})
						})
					}, key);
				})
			}), !readonly ? /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("div", {
				className: "row row--end",
				style: { marginTop: "var(--sp-4)" },
				children: /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)(Button, {
					tone: "primary",
					onClick: save,
					children: "Save settings"
				})
			}) : null]
		}), /* @__PURE__ */ (0, import_jsx_runtime$4.jsxs)(Callout, {
			tone: "warn",
			title: "Careful with these two",
			children: [/* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("p", { children: "“Require verification before closing” exists so a fault cannot disappear without someone checking the fix. Turning it off is a departmental decision, not a convenience." }), /* @__PURE__ */ (0, import_jsx_runtime$4.jsx)("p", {
				style: { marginTop: 6 },
				children: "Response targets change what counts as “overdue” on the dashboard, so agree them with the technical team."
			})]
		})] });
	}
	var import_react$3, import_jsx_runtime$4;
	var init_ReferencePage = __esmMin((() => {
		import_react$3 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		import_jsx_runtime$4 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/NotificationsPage.jsx
	var NotificationsPage_exports = /* @__PURE__ */ __exportAll({ NotificationsPage: () => NotificationsPage$1 });
	/** The in-app notification centre — the department's durable record of what it was told. */
	function NotificationsPage$1() {
		const { can } = useAuth();
		const toast = useToast();
		const [filter, setFilter] = (0, import_react$2.useState)("all");
		const res = useApi(() => notifications.list({
			unreadOnly: filter === "unread",
			limit: 60
		}), [filter]);
		const [open, setOpen] = (0, import_react$2.useState)(null);
		const markAll = async () => {
			try {
				await notifications.markRead([]);
				res.refresh();
				toast.success("Marked everything as read.");
			} catch (err) {
				toast.error(errorText(err));
			}
		};
		const remove = async (id) => {
			try {
				await notifications.remove(id);
				res.refresh();
			} catch (err) {
				toast.error(errorText(err));
			}
		};
		return /* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)(import_jsx_runtime$3.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(PageHeader, {
				eyebrow: "Notifications",
				title: "What the department has been told",
				description: "In-app delivery is the system of record. Email/SMS/push are configured per deployment; each notification keeps a delivery row per channel, so “we did not email anyone” is provable rather than assumed.",
				actions: /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Button, {
					tone: "secondary",
					onClick: markAll,
					disabled: !res.data?.unread,
					children: "Mark all read"
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("div", {
				className: "tabs",
				role: "tablist",
				style: { marginBottom: "var(--sp-4)" },
				children: [["all", "All"], ["unread", `Unread${res.data?.unread ? ` (${res.data.unread})` : ""}`]].map(([id, label]) => /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("button", {
					type: "button",
					role: "tab",
					"aria-selected": filter === id,
					className: cx("tabs__item", filter === id && "tabs__item--active"),
					onClick: () => setFilter(id),
					children: label
				}, id))
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Card, {
				pad: false,
				children: res.loading && !res.data ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Loading, { rows: 5 }) : res.error ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("div", {
					style: { padding: "var(--sp-4)" },
					children: /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(ErrorState, {
						error: res.error,
						onRetry: res.refresh
					})
				}) : (res.data?.items ?? []).length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(EmptyState, {
					icon: "◍",
					title: filter === "unread" ? "Nothing unread" : "No notifications yet",
					description: "You are told when a fault is assigned, when its status changes, when a repair is completed and when maintenance falls due."
				}) : /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("ul", { children: res.data.items.map((n) => /* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("li", {
					className: cx("notif", n.unread ? "notif--unread" : "notif--read"),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("span", {
							className: "notif__dot",
							"aria-hidden": "true"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("div", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("p", {
								className: "notif__title",
								children: [
									n.link ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Link, {
										to: n.link,
										children: n.title
									}) : n.title,
									" ",
									/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Badge, {
										tone: n.severity === "critical" ? "bad" : n.severity === "warning" ? "warn" : n.severity === "success" ? "ok" : "neutral",
										size: "sm",
										children: n.type.replace(/_/g, " ")
									})
								]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("p", {
								className: "notif__body",
								children: n.body
							}),
							can("meta.manage") ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("p", {
								className: "notif__meta",
								children: /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("button", {
									type: "button",
									className: "linklike",
									onClick: async () => {
										try {
											const d = await notifications.deliveries(n.id);
											setOpen({
												id: n.id,
												rows: d.items
											});
										} catch (err) {
											toast.error(errorText(err));
										}
									},
									children: "Delivery channels"
								})
							}) : null
						] }),
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("div", {
							className: "notif__meta",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("span", {
								title: formatDateTime(n.createdAt),
								children: relativeTime(n.createdAt)
							}), /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("button", {
								type: "button",
								className: "linklike",
								onClick: () => remove(n.id),
								children: "Dismiss"
							})]
						})
					]
				}, n.id)) })
			}),
			open ? /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Callout, {
				tone: "info",
				title: `Delivery for notification #${open.id}`,
				children: /* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("ul", {
					className: "deliveries",
					children: open.rows.map((d, i) => /* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("li", { children: [
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)("span", { children: d.channel }),
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsxs)("span", {
							className: "muted",
							children: [
								d.target ?? "—",
								" · ",
								d.detail ?? ""
							]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$3.jsx)(Badge, {
							tone: d.status === "sent" ? "ok" : d.status === "failed" ? "bad" : "neutral",
							size: "sm",
							children: d.status
						})
					] }, i))
				})
			}) : null
		] });
	}
	var import_react$2, import_jsx_runtime$3;
	var init_NotificationsPage = __esmMin((() => {
		import_react$2 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_display();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$3 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/ProfilePage.jsx
	var ProfilePage_exports = /* @__PURE__ */ __exportAll({ ProfilePage: () => ProfilePage$1 });
	/** Profile, password and sessions. The forced-change path lands here after an admin reset. */
	function ProfilePage$1() {
		const [params, setParams] = useSearchParams();
		const tab = params.get("tab") ?? "account";
		const { user, refresh, logout } = useAuth();
		const forced = params.get("forced") === "1";
		const set = (patch) => {
			const next = new URLSearchParams(params);
			for (const [k, v] of Object.entries(patch)) if (!v) next.delete(k);
			else next.set(k, String(v));
			setParams(next, { replace: true });
		};
		const sessions = useApi(() => tab === "sessions" ? auth.sessions() : Promise.resolve({ items: [] }), [tab]);
		return /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)(import_jsx_runtime$2.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(PageHeader, {
				eyebrow: "Your account",
				title: user?.fullName ?? "Profile",
				description: user ? `${user.roleLabel}${user.jobTitle ? ` · ${user.jobTitle}` : ""}${user.department ? ` · ${user.department}` : ""}` : ""
			}),
			forced ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Callout, {
				tone: "warn",
				title: "Choose your own password first",
				children: "An administrator set this account’s password. Pick one only you know — the temporary one is now invalid on every device."
			}) : null,
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Tabs, {
				active: tab,
				onChange: (id) => set({ tab: id }),
				tabs: [
					{
						id: "account",
						label: "Account"
					},
					{
						id: "security",
						label: "Password"
					},
					{
						id: "sessions",
						label: "Devices",
						count: sessions.data?.items?.length
					},
					{
						id: "capabilities",
						label: "What I can do"
					}
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("div", {
				className: "tabpanel",
				children: [
					tab === "account" ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Card, {
						title: "Your details",
						subtitle: "Held by the department administrator; ask them to correct anything wrong here.",
						children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(KeyValue, {
							columns: 2,
							items: [
								["Name", user?.fullName],
								["Email (sign-in)", user?.email],
								["Staff / student ID", user?.employeeId],
								["Role", user?.roleLabel],
								["Job title", user?.jobTitle],
								["Department", user?.department],
								["Phone", user?.phone],
								["Last sign-in", user?.lastLoginAt ? formatDateTime(user.lastLoginAt) : "—"]
							]
						})
					}) : null,
					tab === "security" ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(PasswordPanel, {
						forced,
						onDone: refresh,
						onSignedOut: logout
					}) : null,
					tab === "sessions" ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Card, {
						title: "Signed-in devices",
						subtitle: "Signing out of a device revokes that session immediately — a lost phone can be cut off without changing the password.",
						actions: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Button, {
							size: "sm",
							tone: "secondary",
							onClick: async () => {
								await auth.revokeAll();
								await logout();
								window.location.href = "/login";
							},
							children: "Sign out everywhere"
						}),
						pad: false,
						children: sessions.loading && !sessions.data ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Loading, { rows: 3 }) : /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("ul", { children: [(sessions.data?.items ?? []).map((s) => /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("li", {
							className: "listrow",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("div", {
								className: "listrow__main",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("span", {
									className: "listrow__title",
									children: [s.userAgent ? shortAgent(s.userAgent) : "Unknown device", s.current ? " · this device" : ""]
								}), /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("span", {
									className: "listrow__sub",
									children: [
										"IP ",
										s.ip ?? "—",
										" · last seen ",
										relativeTime(s.lastSeenAt),
										" · expires ",
										formatDateTime(s.expiresAt)
									]
								})]
							}), /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("div", {
								className: "listrow__side",
								children: s.current ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Badge, {
									tone: "ok",
									size: "sm",
									children: "current"
								}) : /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Button, {
									size: "sm",
									tone: "ghost",
									onClick: async () => {
										await auth.revokeSession(s.id);
										sessions.refresh();
									},
									children: "Revoke"
								})
							})]
						}, s.id)), !(sessions.data?.items ?? []).length ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("li", {
							style: { padding: "var(--sp-4)" },
							children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("p", {
								className: "form-note",
								children: "No other sessions."
							})
						}) : null] })
					}) : null,
					tab === "capabilities" ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)(Card, {
						title: "Your permissions",
						subtitle: "Read from the server; the same checks are enforced on every request, so this is a description, not a grant.",
						children: [/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("div", {
							className: "capgrid",
							children: (user?.capabilities ?? []).map((c) => /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("code", {
								className: "cap",
								children: c
							}, c))
						}), /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("p", {
							className: "form-note",
							style: { marginTop: 12 },
							children: "If something is missing that you need for your job, that is a role configuration question for the administrator — not something this screen can change."
						})]
					}) : null
				]
			})
		] });
	}
	function shortAgent(ua) {
		return [/Edg\//.test(ua) ? "Edge" : /OPR\//.test(ua) ? "Opera" : /Chrome\//.test(ua) ? "Chrome" : /Safari\//.test(ua) ? "Safari" : /Firefox\//.test(ua) ? "Firefox" : "Browser", /Windows/.test(ua) ? "Windows" : /Android/.test(ua) ? "Android" : /iPhone|iPad/.test(ua) ? "iOS" : /Mac OS X/.test(ua) ? "macOS" : /Linux/.test(ua) ? "Linux" : ""].filter(Boolean).join(" · ") || "Unknown device";
	}
	function PasswordPanel({ forced, onDone }) {
		const toast = useToast();
		const policy = useApi(() => auth.policy(), []);
		const [form, setForm] = (0, import_react$1.useState)({
			current: "",
			next: "",
			confirm: ""
		});
		const [errors, setErrors] = (0, import_react$1.useState)({});
		const [busy, setBusy] = (0, import_react$1.useState)(false);
		const [checks, setChecks] = (0, import_react$1.useState)([]);
		const [confirmOut, setConfirmOut] = (0, import_react$1.useState)(false);
		(0, import_react$1.useEffect)(() => {
			if (!form.next) {
				setChecks([]);
				return;
			}
			let alive = true;
			const t = setTimeout(() => {
				Promise.resolve().then(() => (init_client(), client_exports)).then(({ api }) => api.get(`/users/password-strength?candidate=${encodeURIComponent(form.next)}`)).then((r) => {
					if (alive) setChecks(r.problems ?? []);
				}).catch(() => {});
			}, 350);
			return () => {
				alive = false;
				clearTimeout(t);
			};
		}, [form.next]);
		const submit = async (event) => {
			event.preventDefault();
			if (form.next !== form.confirm) {
				setErrors({ confirm: "The two entries do not match." });
				return;
			}
			setBusy(true);
			setErrors({});
			try {
				await auth.changePassword(form.current, form.next);
				toast.success("Password changed. Other devices were signed out.");
				setForm({
					current: "",
					next: "",
					confirm: ""
				});
				onDone();
			} catch (err) {
				const mapped = {};
				for (const [k, v] of Object.entries(err.fieldErrors ?? {})) mapped[k] = Array.isArray(v) ? v.join(" ") : String(v);
				setErrors(mapped);
				toast.error(errorText(err));
			} finally {
				setBusy(false);
			}
		};
		const min = policy.data?.minPasswordLength ?? 12;
		return /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)(import_jsx_runtime$2.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Card, {
				title: "Change password",
				subtitle: `At least ${min} characters, mixing three of: lower case, upper case, digits, symbols.`,
				children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("form", {
					className: "form-grid",
					onSubmit: submit,
					noValidate: true,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Field, {
							label: "Current password",
							required: true,
							error: errors.currentPassword,
							children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(TextInput, {
								type: "password",
								autoComplete: "current-password",
								value: form.current,
								onChange: (e) => setForm({
									...form,
									current: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Field, {
							label: "New password",
							required: true,
							error: errors.newPassword,
							hint: checks.length ? void 0 : form.next ? "Looks good." : void 0,
							children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(TextInput, {
								type: "password",
								autoComplete: "new-password",
								value: form.next,
								onChange: (e) => setForm({
									...form,
									next: e.target.value
								})
							})
						}),
						checks.length ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("ul", {
							className: "pwcheck",
							children: checks.map((c) => /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("li", { children: c }, c))
						}) : form.next ? /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("ul", {
							className: "pwcheck",
							children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)("li", {
								className: "ok",
								children: "Meets the policy"
							})
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Field, {
							label: "Repeat the new password",
							required: true,
							error: errors.confirm,
							children: /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(TextInput, {
								type: "password",
								autoComplete: "new-password",
								value: form.confirm,
								onChange: (e) => setForm({
									...form,
									confirm: e.target.value
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime$2.jsxs)("div", {
							className: "row row--end",
							children: [/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Button, {
								tone: "primary",
								type: "submit",
								loading: busy,
								disabled: !form.current || !form.next,
								children: forced ? "Set my password" : "Change password"
							}), /* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Button, {
								tone: "ghost",
								type: "button",
								onClick: () => setConfirmOut(true),
								children: "Sign out everywhere"
							})]
						})
					]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(Callout, {
				tone: "info",
				title: "Why there is no “forgotten password” link",
				children: "Password reset by email needs the email integration, which is not enabled on this deployment. Until it is, the department administrator resets it for you — one conversation, and the audit trail records who did it."
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$2.jsx)(ConfirmDialog, {
				open: confirmOut,
				title: "Sign out of every device?",
				body: "You will be returned to the sign-in screen. This cannot be undone from the browser.",
				confirmLabel: "Sign out everywhere",
				onCancel: () => setConfirmOut(false),
				onConfirm: async () => {
					await auth.revokeAll();
					window.location.href = "/login";
				}
			})
		] });
	}
	var import_react$1, import_jsx_runtime$2;
	var init_ProfilePage = __esmMin((() => {
		import_react$1 = /* @__PURE__ */ __toESM(require_react(), 1);
		init_dist();
		init_client();
		init_AuthContext();
		init_AppShell();
		init_ui();
		init_Toast();
		init_useApi();
		init_format();
		import_jsx_runtime$2 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/AuditPage.jsx
	var AuditPage_exports = /* @__PURE__ */ __exportAll({ AuditPage: () => AuditPage$1 });
	/** The audit trail, as a browsable report. Read-only by design: nothing here can be edited. */
	function AuditPage$1() {
		const [q, setQ] = (0, import_react.useState)("");
		const [entity, setEntity] = (0, import_react.useState)("");
		const [open, setOpen] = (0, import_react.useState)(null);
		const params = (0, import_react.useMemo)(() => ({
			q: q || void 0,
			entityType: entity || void 0,
			perPage: 50,
			page: 1
		}), [q, entity]);
		const res = useApi(() => audit.list(params), [JSON.stringify(params)]);
		return /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)(import_jsx_runtime$1.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(PageHeader, {
				eyebrow: "Accountability",
				title: "Audit log",
				description: "Who changed what, when, and what it was before. Status changes, assignments, records and configuration all appear here."
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(SearchInput, {
				value: q,
				onSearch: setQ,
				placeholder: "Search action, reference or summary…"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)("div", {
				className: "row",
				style: { margin: "var(--sp-3) 0" },
				children: [/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Select, {
					value: entity,
					onChange: (e) => setEntity(e.target.value),
					style: { maxWidth: 240 },
					"aria-label": "Entity type",
					options: [{
						value: "",
						label: "All entity types"
					}, ...[
						"equipment",
						"fault_report",
						"repair_record",
						"maintenance_record",
						"maintenance_schedule",
						"user",
						"app_settings",
						"sessions",
						"equipment_categories",
						"locations"
					].map((v) => ({
						value: v,
						label: v.replace(/_/g, " ")
					}))]
				}), /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)("span", {
					className: "muted",
					style: { fontSize: ".8rem" },
					children: [res.data?.pagination?.total ?? "…", " entries"]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Card, {
				pad: false,
				children: res.loading && !res.data ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Loading, { rows: 8 }) : /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)(import_jsx_runtime$1.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(DataTable, {
					dense: true,
					rows: res.data?.items ?? [],
					onRowClick: setOpen,
					empty: /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(EmptyState, {
						title: "Nothing matches",
						description: "Clear the search or the entity filter."
					}),
					columns: [
						{
							key: "createdAt",
							label: "When",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("span", {
								className: "nowrap",
								children: formatDateTime(r.createdAt)
							})
						},
						{
							key: "actorName",
							label: "Actor",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)("div", {
								className: "cell-main",
								children: [/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("span", {
									className: "cell-title",
									children: r.actorName ?? "system"
								}), /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("span", {
									className: "cell-sub",
									children: r.actorRole
								})]
							})
						},
						{
							key: "action",
							label: "Action",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("code", { children: r.action })
						},
						{
							key: "entityRef",
							label: "Record",
							render: (r) => r.entityRef ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("code", { children: r.entityRef }) : /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("span", {
								className: "muted",
								children: r.entityType
							})
						},
						{
							key: "summary",
							label: "Summary"
						},
						{
							key: "ip",
							label: "IP",
							render: (r) => /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("span", {
								className: "muted mono",
								style: { fontSize: ".72rem" },
								children: r.ip ?? "—"
							})
						}
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Pagination, {
					pagination: res.data?.pagination,
					onPage: () => {}
				})] })
			}),
			open ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("div", {
				style: { marginTop: "var(--sp-4)" },
				children: /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)(Card, {
					title: `Audit entry · ${open.action}`,
					actions: /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Button, {
						size: "sm",
						tone: "ghost",
						onClick: () => setOpen(null),
						children: "Dismiss"
					}),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(KeyValue, {
							columns: 2,
							items: [
								["When", formatDateTime(open.createdAt, { withSeconds: true })],
								["Actor", `${open.actorName ?? "system"} (${open.actorRole ?? "—"})`],
								["Entity", `${open.entityType} #${open.entityId ?? "—"}`],
								["Reference", open.entityRef],
								["IP / client", `${open.ip ?? "—"} · ${open.userAgent ?? "—"}`]
							]
						}),
						open.before ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)(import_jsx_runtime$1.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("p", {
							className: "form-section__title",
							children: "Before"
						}), /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("pre", {
							className: "json-diff",
							children: JSON.stringify(open.before, null, 2)
						})] }) : null,
						open.after ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsxs)(import_jsx_runtime$1.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("p", {
							className: "form-section__title",
							children: "After"
						}), /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)("pre", {
							className: "json-diff",
							children: JSON.stringify(open.after, null, 2)
						})] }) : null,
						open.summary ? /* @__PURE__ */ (0, import_jsx_runtime$1.jsx)(Callout, {
							tone: "info",
							children: open.summary
						}) : null
					]
				})
			}) : null
		] });
	}
	var import_react, import_jsx_runtime$1;
	var init_AuditPage = __esmMin((() => {
		import_react = /* @__PURE__ */ __toESM(require_react(), 1);
		init_client();
		init_AppShell();
		init_ui();
		init_display();
		init_useApi();
		init_format();
		import_jsx_runtime$1 = require_jsx_runtime();
	}));
	//#endregion
	//#region src/pages/NotFoundPage.jsx
	var NotFoundPage_exports = /* @__PURE__ */ __exportAll({ NotFoundPage: () => NotFoundPage$1 });
	/** Not found — with the two routes a person in a lab actually needs from here. */
	function NotFoundPage$1() {
		const { can } = useAuth();
		return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Card, {
			title: "That page does not exist",
			className: "notfound",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: "form-note",
				children: "The link may be old, or a record was removed. The department’s two most useful screens are below."
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "row",
				style: { marginTop: "var(--sp-4)" },
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						as: Link,
						to: "/",
						tone: "primary",
						children: "Dashboard"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						as: Link,
						to: "/equipment",
						tone: "secondary",
						children: "Equipment list"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						as: Link,
						to: "/faults/new",
						tone: "ghost",
						children: "Report a fault"
					})
				]
			})]
		});
	}
	var import_jsx_runtime;
	var init_NotFoundPage = __esmMin((() => {
		init_dist();
		init_ui();
		init_AuthContext();
		import_jsx_runtime = require_jsx_runtime();
	}));
	//#endregion
	//#region src/App.jsx
	init_dist();
	init_AuthContext();
	init_Toast();
	init_AppShell();
	init_ui();
	var DashboardPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_DashboardPage(), DashboardPage_exports)).then((m) => ({ default: m.DashboardPage })));
	var EquipmentListPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_EquipmentListPage(), EquipmentListPage_exports)).then((m) => ({ default: m.EquipmentListPage })));
	var EquipmentDetailPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_EquipmentDetailPage(), EquipmentDetailPage_exports)).then((m) => ({ default: m.EquipmentDetailPage })));
	var EquipmentFormPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_EquipmentFormPage(), EquipmentFormPage_exports)).then((m) => ({ default: m.EquipmentFormPage })));
	var FaultListPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_FaultListPage(), FaultListPage_exports)).then((m) => ({ default: m.FaultListPage })));
	var FaultDetailPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_FaultDetailPage(), FaultDetailPage_exports)).then((m) => ({ default: m.FaultDetailPage })));
	var ReportFaultPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_ReportFaultPage(), ReportFaultPage_exports)).then((m) => ({ default: m.ReportFaultPage })));
	var WorkQueuePage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_WorkQueuePage(), WorkQueuePage_exports)).then((m) => ({ default: m.WorkQueuePage })));
	var MaintenancePage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_MaintenancePage(), MaintenancePage_exports)).then((m) => ({ default: m.MaintenancePage })));
	var RiskPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_RiskPage(), RiskPage_exports)).then((m) => ({ default: m.RiskPage })));
	var ReportsPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_ReportsPage(), ReportsPage_exports)).then((m) => ({ default: m.ReportsPage })));
	var UsersPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_UsersPage(), UsersPage_exports)).then((m) => ({ default: m.UsersPage })));
	var ReferencePage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_ReferencePage(), ReferencePage_exports)).then((m) => ({ default: m.ReferencePage })));
	var NotificationsPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_NotificationsPage(), NotificationsPage_exports)).then((m) => ({ default: m.NotificationsPage })));
	var ProfilePage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_ProfilePage(), ProfilePage_exports)).then((m) => ({ default: m.ProfilePage })));
	var AuditPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_AuditPage(), AuditPage_exports)).then((m) => ({ default: m.AuditPage })));
	var NotFoundPage = (0, import_react$18.lazy)(() => Promise.resolve().then(() => (init_NotFoundPage(), NotFoundPage_exports)).then((m) => ({ default: m.NotFoundPage })));
	function RequireAuth({ children }) {
		const { status } = useAuth();
		const location = useLocation();
		if (status === "loading") return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)("div", {
			style: { padding: 40 },
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Loading, {
				label: "Restoring your session",
				rows: 4
			})
		});
		if (status === "anonymous") return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Navigate, {
			to: "/login",
			replace: true,
			state: { from: location.pathname + location.search }
		});
		return children;
	}
	function RequireRole({ roles, children }) {
		const { user } = useAuth();
		if (!roles.includes(user?.roleCode)) return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Navigate, {
			to: "/",
			replace: true
		});
		return children;
	}
	/** Forces a password change on first sign-in when an administrator issued the password. */
	function MustChangePasswordGate({ children }) {
		const { user } = useAuth();
		const location = useLocation();
		if (user?.mustChangePassword && location.pathname !== "/profile") return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Navigate, {
			to: "/profile?tab=security&forced=1",
			replace: true
		});
		return children;
	}
	function Shell({ children }) {
		return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireAuth, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(MustChangePasswordGate, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(AppShell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(import_react$18.Suspense, {
			fallback: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Loading, {
				label: "Loading screen",
				rows: 5
			}),
			children
		}) }) }) });
	}
	function App() {
		return /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ToastProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(AuthProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsxs)(Routes, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/login",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(LoginPage, {})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/e/:tag",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(QrLandingPage, {})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(DashboardPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/equipment",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(EquipmentListPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/equipment/new",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin", "technician"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(EquipmentFormPage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/equipment/:id",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(EquipmentDetailPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/equipment/:id/edit",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin", "technician"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(EquipmentFormPage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/faults",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(FaultListPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/my-reports",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(FaultListPage, { mine: true }) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/faults/new",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ReportFaultPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/faults/:id",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(FaultDetailPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/report/:tag",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ReportFaultPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/work",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin", "technician"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(WorkQueuePage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/maintenance",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(MaintenancePage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/risk",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RiskPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/reports",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ReportsPage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/users",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(UsersPage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/reference",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin", "technician"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ReferencePage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/audit",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(RequireRole, {
					roles: ["admin"],
					children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(AuditPage, {})
				}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/notifications",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(NotificationsPage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "/profile",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(ProfilePage, {}) })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Route, {
				path: "*",
				element: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(Shell, { children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(NotFoundPage, {}) })
			})
		] }) }) });
	}
	//#endregion
	//#region test/entry.jsx
	init_dist();
	window.__unmount = null;
	window.__mount = (route) => {
		const container = document.createElement("div");
		container.id = "root";
		document.body.appendChild(container);
		const root = (0, import_client.createRoot)(container);
		root.render(/* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(MemoryRouter, {
			initialEntries: [route],
			children: /* @__PURE__ */ (0, import_jsx_runtime$19.jsx)(App, {})
		}));
		window.__unmount = () => {
			root.unmount();
			container.remove();
		};
		return root;
	};
	//#endregion
})();
